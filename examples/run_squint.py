"""
Example: run SQUINT on two AnnData sections from different platforms.

Data assumed at:
    /lustre/scratch126/cellgen/lotfollahi/DATASETS/silver/mmb0-1b_smb1-1b_1p_coord_aligned/
        ├── harmonised_merfish_mouse_brain_239-batch_batch82_shared_genes.h5ad
        └── harmonised_starmap_plus_mouse_cns_batch15_shared_genes.h5ad

Both files share the same gene panel (the "shared_genes" suffix). They are two
batches of one combined dataset called  `mmb0-1b_smb1-1b_1p_coord_aligned`  in
SQUINT's naming scheme:
    mmb0-1b   = MERFISH Mouse Brain dataset 0,    1 batch (batch82)
    smb1-1b   = STARmap+ Mouse Brain dataset 1,   1 batch (batch15)
    1p        = single shared gene panel
    coord_aligned = the xy_coordinates of the two sections have been aligned

All training artifacts (model checkpoints, configs, inference AnnData files,
metrics, etc.) are written under
    /nfs/team361/sb75/squint-reproducibility/artifacts
Training metrics are logged to wandb under the project "squint".


==============================================================================
                          USAGE  (4 commands)
==============================================================================

  # 0) one-time only — patch .uns metadata of the two .h5ad files if they
  #    don't yet contain the keys SQUINT needs (see "AnnData expectations"
  #    below). Skip if your files already have them.
  python examples/run_squint.py --patch-uns

  # 1) one-time only — preprocess the two .h5ad files into a cached PyG
  #    DatasetBlob.  Output:
  #        <silver-root>/gold/in-memory-PyG-dataset-blob/<DATASET_NAME>/
  python examples/run_squint.py --build-blob

  # 2) train SQUINT on the WHOLE dataset (both batches, every cell).
  #    --variant <name>  picks the ablation. Use --list-variants to see all.
  #
  #    Key variants:
  #      recon-cell        per-cell NB reconstruction only        (start here)
  #      recon-nbr         neighbourhood NB reconstruction only
  #      recon-both        per-cell + neighbourhood NB (two loss terms)
  #      recon-cell+adj    cell NB + adjacency BCE
  #      recon-nbr+adj     neighbourhood NB + adjacency BCE
  #      recon-both+adj    both NB + adjacency BCE
  #      recon-cell+film   cell NB + FiLM batch conditioning
  #      recon-cell+mask   cell NB + MAE feature masking
  #      recon-cell+mhvq   cell NB + multi-head VQ (10 heads, k=5000)
  #      recon-both+mhvq   cell+nbr NB + multi-head VQ (10 heads, k=5000)
  #
  #      RVQ ablations (Residual VQ, levels=[30, 200]):
  #        recon-cell+rvq, recon-nbr+rvq, recon-both+rvq,
  #        recon-cell+rvq+adj, recon-nbr+rvq+adj, recon-both+rvq+adj,
  #        recon-cell+rvq+film, recon-cell+rvq+mask
  #
  #      CVQ ablations (Conditional / Tree VQ, K1=30, K2=10):
  #        recon-cell+cvq, recon-nbr+cvq, recon-both+cvq,
  #        recon-cell+cvq+adj, recon-nbr+cvq+adj, recon-both+cvq+adj,
  #        recon-cell+cvq+film, recon-cell+cvq+mask
  #
  #      full              all components composed
  #
  #    Output (flat run dir, ablation-aware):
  #      <ARTIFACTS_DIR>/<DATASET_NAME>/<VARIANT>/<YYYYMMDD_HHMMSS>/
  #          user_specified_config.yaml      <- full materialised config
  #          ablation_summary.yaml           <- variant + description + patches
  #          checkpoints/<best>.ckpt
  #          wandb/run-<id>/...              (wandb local cache; cloud logging
  #                                           still goes to project "squint")
  python examples/run_squint.py --train --variant recon-cell
  python examples/run_squint.py --train --variant recon-both+adj

  # 3) inference on a folder of .h5ad files. Returns a single AnnData with
  #    the SQUINT outputs in .obsm / .obs / .layers, saved as .h5ad inside
  #    the run_dir itself (predicted_adata.h5ad alongside checkpoints and
  #    user_specified_config.yaml). --run-dir is the directory printed at
  #    the end of step 2.
  python examples/run_squint.py --predict \
      --run-dir <ARTIFACTS_DIR>/<DATASET_NAME>/<VARIANT>/<TIMESTAMP> \
      --silver-dir /lustre/scratch126/cellgen/lotfollahi/DATASETS/silver/mmb0-1b_smb1-1b_1p_coord_aligned

  # (you can omit --silver-dir to default to the path used during training.)


==============================================================================
                  Layout of the inference AnnData (step 3)
==============================================================================

  adata.X                  raw input counts (concatenated, sparse if input was)
  adata.var.index          gene symbols (shared panel)
  adata.obs['cell_type']           cell-type labels (string)
  adata.obs['adata_batch_id']      0 / 1 -> position of the source AnnData
  adata.obs['source_file']         filename of the source .h5ad
  adata.obs['code_index']          (only when codebook has 1 head) per-cell
                                   codebook index assigned by VQ
  adata.obsm['spatial']            (n_cells, 2) xy coordinates
  adata.obsm['X_squint']           SQUINT continuous latent (encoder output)
  adata.obsm['X_squint_quantized'] SQUINT VQ-snapped latent (codebook embed)
  adata.obsm['X_squint_adj']       latent used for adjacency reconstruction
  adata.obsm['code_indices']       (n_cells, n_heads) codebook indices, when
                                   the codebook has more than one head
  adata.layers['X_hat']            reconstructed counts (NB rate * read depth)
  adata.layers['X_hat_nbr']        reconstructed 1-hop neighborhood mean
  adata.uns['squint']              {codebook_size, num_heads, separate_codebook,
                                   run_dir, ckpt}


==============================================================================
                       AnnData expectations  (input)
==============================================================================

Each .h5ad must contain:
    .X                   raw integer counts (sparse CSR is fine)
    .var.index           gene symbols, identical across files
    .obs['cell_type']    cell-type labels (string)
    .obs['cell_id']      unique cell IDs that include "batch82" / "batch15"
                         (used by SQUINT to build per-cell batch one-hots
                         for FiLM conditioning)
    .obsm['spatial']     (n_cells, 2) array of xy coordinates
    .uns['batch']        e.g. "batch82" or "batch15"  (int(s[5:]) must work)
    .uns['dataset_id']   string, e.g. "mmb0" / "smb1"
    .uns['tissue']       string, e.g. "mouse_brain"
    .uns['species']      string, e.g. "mouse"

If any of these are missing, run `--patch-uns` once.
"""
import argparse
import json
import os
import sys
import warnings
from pathlib import Path
from typing import List, Optional, Sequence

# Silence two upstream FutureWarnings that fire at import time of our deps:
#   1. dask: legacy DataFrame deprecation ("Set dataframe.query-planning=True").
#      Triggered transitively (anndata -> ... -> dask) before any user code
#      runs. Prefer turning planning on directly when dask is available;
#      fall back to a warnings filter so older dask versions don't break.
#   2. anndata: "Importing read_text from `anndata` is deprecated. Import
#      anndata.io.read_text instead." Triggered by a transitive dep
#      (likely scvi-tools / scanpy / squidpy) using the old import path —
#      we don't import read_text ourselves. Filter by message text since
#      `category=FutureWarning` alone is too broad and would hide
#      genuine deprecations from our own code.
# Both filters must run BEFORE the dependencies are imported to take effect.
warnings.filterwarnings(
    "ignore",
    message=r".*Importing read_text from `anndata` is deprecated.*",
    category=FutureWarning,
)
warnings.filterwarnings(
    "ignore",
    message=r".*legacy Dask DataFrame implementation is deprecated.*",
    category=FutureWarning,
)
try:
    import dask
    dask.config.set({"dataframe.query-planning": True})
except Exception:  # noqa: BLE001
    # dask not installed or older version without query-planning -- the
    # warnings filter above will catch the FutureWarning anyway.
    pass

import yaml


# ---------------------------------------------------------------------------
# Configuration paths (edit these if you move the data or repos)
# ---------------------------------------------------------------------------

# Parent directory that holds  silver/  and  gold/  subfolders.
DATA_ROOT = Path("/lustre/scratch126/cellgen/lotfollahi/DATASETS/")

# Dataset name = subfolder name under  silver/
DATASET_NAME = "hst_corpus_110m"

# Path to your local clones of the two repos on the cluster.
# These are per-user and edited in place (see the header note). Pointing them at
# THIS workspace rather than another user's home, so `SQUINT_PKG` (prepended to
# sys.path below) and the reproducibility scripts resolve against the checkout
# actually being run.
_WORKSPACE = Path("/lustre/scratch126/cellgen/lotfollahi/hk11/squint_project")
REPRO_REPO = _WORKSPACE / "github" / "squint-reproducibility"
SQUINT_PKG = _WORKSPACE / "squint"

# Single root for everything we produce: configs, wandb logs, checkpoints,
# inference outputs. Deliberately OUTSIDE `squint/` so multi-GB run outputs can
# never be staged into the repo.
ARTIFACTS_DIR = _WORKSPACE / "artifacts"

# Where this example writes its tailored config files.
CONFIG_OUT_DIR = ARTIFACTS_DIR / "configs"

# Where wandb writes run directories. The training script's
# `set_wandb_experiment_dir` will append <dataset>/standalone/<model>/...
# under here.
LOG_ROOT = ARTIFACTS_DIR / "wandb"

# Inference results (predicted_adata.h5ad + plots + metrics) are written
# directly into the run_dir
# (`ARTIFACTS_DIR/<dataset>/<variant>/<timestamp>/`) so everything for one
# training run lives in a single folder.

# Project name in wandb.
WANDB_PROJECT = "squint"


# ---------------------------------------------------------------------------
# In-training metric toggle (Pearson correlations)
# ---------------------------------------------------------------------------
# The model's `on_validation_epoch_end` hook calls
# `compute_benchmarking_metrics(adata)` for every entry in the config's
# `train_metrics_list` (which is also used for val mode, see
# `BaseModel.compute_metrics`). Each Pearson metric reconstructs the full
# val AnnData from the inference cache and runs a cells x genes
# correlation, costing 5-15 s per metric on the 100k-cell datasets.
# With the 14-entry list below that compounds to 100-200 s of pure CPU
# work per val epoch, completely starving the GPU.
#
# DEFAULT: empty lists -> losses-only tracking during training. Final
# Pearson metrics for the benchmark CSVs are produced by the
# `compute_inference_metrics.py` step at the end of the pipeline,
# which is the source of truth that benchmark figure scripts read
# from. The per-epoch values were only ever for live wandb diagnostics.
#
# OPT-IN: set environment variable `SQUINT_WITH_PEARSON=1` (or pass
# `--with-pearson` to `run_squint.py`) to populate train/test
# `*_metrics_list` with the 14 Pearson variants. Use this for one-off
# diagnostic runs when investigating training instabilities — expect
# 10-20x slower epochs.
_PEARSON_METRICS_LIST = [
    "codebook_utilization",
    # cell branch — gene-wise
    "pearson_gene_wise_log1p",
    "pearson_gene_wise_log1p_median",
    "pearson_gene_wise_hvg50_log1p",
    "pearson_gene_wise_hvg50_log1p_median",
    # cell branch — cell-wise
    "pearson_cell_wise_log1p",
    "pearson_cell_wise_log1p_median",
    # neighbourhood branch — gene-wise
    "pearson_gene_wise_1hop_nbr_log1p",
    "pearson_gene_wise_1hop_nbr_log1p_median",
    "pearson_gene_wise_hvg50_1hop_nbr_log1p",
    "pearson_gene_wise_hvg50_1hop_nbr_log1p_median",
    # neighbourhood branch — cell-wise
    "pearson_cell_wise_1hop_nbr_log1p",
    "pearson_cell_wise_1hop_nbr_log1p_median",
    # legacy raw-count metric
    "pearson_gene_wise",
]


def _resolve_metrics_list() -> list:
    """Return the in-training metrics list for the current run.

    - `SQUINT_WITH_PEARSON=1` (or `--with-pearson` on the CLI, which
      exports this env var in `main()`): returns the full Pearson
      list. Per-epoch Pearson appears in wandb again at the cost of
      10-20x slower epochs.
    - Anything else (including unset): returns `[]`. Training runs
      losses-only; final Pearson breakdown still comes from
      `compute_inference_metrics.py` at the end of the pipeline.

    Evaluated at config-build time (inside `make_train_config*`), so
    the env var must be set BEFORE `train(variant)` is called. `main()`
    handles that for the `--with-pearson` flag; manual env-var
    invocations also work (`SQUINT_WITH_PEARSON=1 python ...`).
    """
    if os.environ.get("SQUINT_WITH_PEARSON", "0") == "1":
        return list(_PEARSON_METRICS_LIST)
    return []


# ---------------------------------------------------------------------------
# 1. Dataset-blob config (one-time preprocessing)
# ---------------------------------------------------------------------------

def make_dataset_blob_config_chl59() -> dict:
    """
    Dataset-blob build config for the CosMx Lung dataset
    (`/nfs/team361/sb75/DATASETS/silver/chl59-8b_1p`, 8 AnnDatas).

    Differences from `make_dataset_blob_config`:
      - root data path -> /nfs/team361/sb75/DATASETS
      - dataset name -> chl59-8b_1p
      - label_names empty by default — the Lung AnnDatas may not have
        an `obs['cell_type']` column. The blob loader is now defensive
        (skips missing label columns with a warning), so you can put
        labels back if your AnnDatas DO have them under a different
        name, e.g. `label_names=["cell_types=celltype"]`.
      - `graph_kwargs.batch_key` -> 'batch'. If your Lung AnnDatas don't
        have `obs['batch']` (the user confirmed batch info is encoded
        in `obs['cell_id']` strings as '..._batchN_...'), the loader
        will silently fall back to parsing `cell_id` for the batch
        one-hot. So this default is safe even when obs['batch'] is
        absent.
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": "CosMx Lung — 8 samples (Lung5/6/9/12/13 incl. replicates)",
        },
        "dataset": {
            "name": "chl59-8b_1p",
            "feature_names": ["cell_gene_counts"],
            "label_names": [],   # set to ["cell_types=<your_obs_key>"] if available
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                "n_neighs_list":      [8, 16],  # 8 = SQUINT default, 16 = knn16 variants
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",     # falls back to cell_id if absent
                "k": {
                    "lm_eigvecs": 128,
                },
            },
            "data_directory_path": "/nfs/team361/sb75/DATASETS",
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config_chl59_2b() -> dict:
    """
    Dataset-blob build config for the 2-sample CosMx Lung subset
    (`/lustre/scratch126/cellgen/lotfollahi/DATASETS/silver/chl59-2b_1p`).

    Same structure as `make_dataset_blob_config_chl59` (the 8-sample
    version), with two differences:
      - root data path -> /lustre/scratch126/cellgen/lotfollahi/DATASETS
      - dataset name   -> chl59-2b_1p

    Intended use: lightweight ablation runs (faster to train than the
    8-sample dataset, useful for ablating the s49_v23 spine when the
    full chl59-8b_1p compute budget is unavailable).
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": "CosMx Lung — 2-sample subset",
        },
        "dataset": {
            "name": "chl59-2b_1p",
            "feature_names": ["cell_gene_counts"],
            "label_names": [],   # set to ["cell_types=<your_obs_key>"] if available
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                # 8 = SQUINT default, 16 = knn16 variants (s49_v23 spine),
                # 24 = knn24 ablation in the v51 neighbors-axis sweep.
                "n_neighs_list":      [8, 16, 24],
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",
                "k": {
                    "lm_eigvecs": 128,
                },
            },
            "data_directory_path": "/lustre/scratch126/cellgen/lotfollahi/DATASETS",
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config_mmb20() -> dict:
    """
    Dataset-blob build config for the cross-platform retrieval dataset
    (`/lustre/.../silver/mmb0-1b_smb1-20b_1p_shared_genes`, 21 AnnDatas:
    1 MERFISH mouse-brain section + 20 STARmap+ mouse-CNS sections, all
    reindexed to the MERFISH gene panel via examples/harmonize_mmb_smb20_panels.py).

    Run order (one-time):
        1. python examples/harmonize_mmb_smb20_panels.py
           -> writes harmonised AnnDatas to <DATA_ROOT>/silver/<dataset>_shared_genes/
        2. python examples/run_squint.py --build-blob --build-blob-dataset mmb20
           -> reads from the dir written in step 1, builds dataset_blob.pt

    Notes:
      - root data path -> /lustre/scratch126/cellgen/lotfollahi/DATASETS
        (where the original mmb0-1b_smb1-20b_1p folder lives).
      - dataset name  -> mmb0-1b_smb1-20b_1p_shared_genes (matches the
        output dir of harmonize_mmb_smb20_panels.py).
      - label_names empty: STARmap obs columns vary (some have
        Sub_molecular_tissue_region, others may have cell_type only); the
        blob loader is defensive and skips missing label columns. The
        per-cell labels are still propagated to inference via
        `obs_per_batch_id` (we now carry the full obs DataFrames) — no
        need to register them as supervised y_* tensors here.
      - graph_kwargs.batch_key -> 'batch'. The harmonize script stamps
        every AnnData with obs['batch'] = 'batchN', so
        `build_batch_one_hot_from_obs` densifies cleanly.
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": (
                "MERFISH mouse-brain (1 section) + STARmap+ mouse-CNS "
                "(20 sections) on the shared MERFISH gene panel."
            ),
        },
        "dataset": {
            "name": "mmb0-1b_smb1-20b_1p_shared_genes",
            "feature_names": ["cell_gene_counts"],
            "label_names": [],
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                # k=8 (smoke test) + k=16 (the s49_v23 spine uses
                # graph_knn(n_neighs=16)); both spatial graphs are
                # precomputed so either sampler depth is available
                # without a rebuild.
                "n_neighs_list":      [8, 16],
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",
                "k": {
                    "lm_eigvecs": 128,
                },
            },
            "data_directory_path": str(DATA_ROOT),
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config_mmb239() -> dict:
    """
    Dataset-blob build config for the 239-section MERFISH mouse-brain
    dataset (`/lustre/.../silver/mmb0-239b_1p`, 239 AnnDatas:
    harmonised_merfish_mouse_brain_239-batch_batch{1..239}.h5ad).

    Unlike `make_dataset_blob_config_mmb20` (which mixes MERFISH + STARmap
    and therefore needs panel harmonization -> a `_shared_genes` dir), ALL
    239 sections here are MERFISH mouse brain on the SAME gene panel, so NO
    harmonization step is required: we read the silver dir directly and the
    dataset name has no `_shared_genes` suffix.

    Run order (one-time):
        python examples/run_squint.py --build-blob --build-blob-dataset mmb0-239b_1p
        -> reads /lustre/.../silver/mmb0-239b_1p, builds dataset_blob.pt

    Notes:
      - root data path -> /lustre/scratch126/cellgen/lotfollahi/DATASETS
        (same DATA_ROOT as mmb20; silver/mmb0-239b_1p lives under it).
      - n_neighs_list [8, 16]: 8 = SQUINT default / smoke; 16 = the
        s49_v23 spine (graph_knn(n_neighs=16)). Both precomputed so no
        rebuild per sampler depth.
      - batch_key 'batch': the silver AnnDatas carry obs['batch'] = the
        per-section id, so `build_batch_one_hot_from_obs` densifies cleanly
        across all 239 sections.
      - label_names empty: no supervised heads; per-cell obs is carried to
        inference via `obs_per_batch_id`.
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": (
                "MERFISH mouse-brain, 239 sections, single shared gene "
                "panel (no harmonization needed)."
            ),
        },
        "dataset": {
            "name": "mmb0-239b_1p",
            "feature_names": ["cell_gene_counts"],
            "label_names": [],
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                "n_neighs_list":      [8, 16],
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",
                # NO Laplacian spectral embeddings. The s49_v23 model uses
                # feature_names=["cell_gene_counts"] and never consumes
                # U_lm_eigvecs (0 references in run_squint.py), so computing
                # 128 eigvecs (eigsh, k=128) per graph per section is the
                # dominant, wasted build cost over 239 sections. Skipping it
                # is the single biggest speedup. Re-add {"lm_eigvecs": N} and
                # rebuild only if a future variant needs spectral features.
                "k": {},
            },
            "data_directory_path": str(DATA_ROOT),
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config_smb1_20b() -> dict:
    """
    Dataset-blob build config for the STARmap+ mouse-CNS 20-section dataset
    (`/nfs/team361/sb75/DATASETS/silver/smb1-20b_1p`, 20 AnnDatas).

    This is the STARmap-ONLY counterpart to mmb20: the same 20 STARmap+
    sections WITHOUT the MERFISH section, kept on their NATIVE STARmap gene
    panel (no MERFISH harmonization -> no `_shared_genes` suffix; read
    directly from the silver dir). Lives on team361 (like chl59 /
    squint_hln), not lustre.

    Run order (one-time):
        python examples/run_squint.py --build-blob --build-blob-dataset smb1-20b_1p
        -> reads /nfs/team361/sb75/DATASETS/silver/smb1-20b_1p, builds dataset_blob.pt

    Notes:
      - root data path -> /nfs/team361/sb75/DATASETS.
      - n_neighs_list [8, 16]: 8 = SQUINT default / smoke; 16 = the
        s49_v23 / s57 spine (graph_knn(n_neighs=16)). Both precomputed so
        either sampler depth is available without a rebuild.
      - batch_key 'batch': the silver AnnDatas carry obs['batch'] = the
        per-section id, so `build_batch_one_hot_from_obs` densifies cleanly
        across all 20 sections.
      - label_names empty: STARmap obs columns vary across sections; the
        blob loader is defensive and skips missing label columns. Per-cell
        obs is still carried to inference via `obs_per_batch_id` (matches
        mmb20).
      - NO Laplacian spectral embeddings (k: {}): the s49_v23 model uses
        feature_names=["cell_gene_counts"] and never consumes U_lm_eigvecs,
        so skipping eigsh (k=128 per graph per section) is the single
        biggest build speedup over 20 sections (matches mmb239).
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": (
                "STARmap+ mouse-CNS, 20 sections, native STARmap gene "
                "panel (no harmonization)."
            ),
        },
        "dataset": {
            "name": "smb1-20b_1p",
            "feature_names": ["cell_gene_counts"],
            "label_names": [],
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                "n_neighs_list":      [8, 16],
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",
                "k": {},
            },
            "data_directory_path": "/nfs/team361/sb75/DATASETS",
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def _make_spatch_blob_config(name: str, description: str,
                             label_names: Optional[List[str]] = None,
                             *,
                             data_directory_path: str = "/nfs/team361/sb75/DATASETS",
                             n_neighs_list: Optional[List[int]] = None,
                             k: Optional[dict] = None) -> dict:
    """
    Shared body for all `spatch_*_1p` blob configs (and reusable for other
    silver datasets with the same shape). Differs from
    `make_dataset_blob_config_spatch` (the original smoke-test variant)
    in two ways:
      - parameterised by `name` so the same config shape works for
        spatch_1p, spatch_ov_1p, spatch_hcc_1p, spatch_coad_1p, ...
      - n_neighs_list pre-populates k=8, 16, 24 so the s51 ablation
        sweep (which uses all three k values) doesn't require a
        rebuild per knn ablation.

    `label_names` maps SQUINT label tensors to the per-cell obs columns in
    the silver AnnDatas, as "<name>=<obs_column>" (the builder splits on
    '='). Defaults to the spatch convention
    (cell_types=annotation, niche_types=spatial_cluster); pass a custom list
    to register a dataset that keeps its labels under different column names
    (e.g. the tonsil / lymph-node atlases:
    ["cell_types=cell_type_annotation", "niche_types=niche_annotation"]).

    `data_directory_path`, `n_neighs_list` and `k` are keyword-only with the
    historical values as defaults, so the seven existing callers are unchanged.
    Override them for datasets under a different root, or to skip work nothing
    consumes: `k={}` drops the Laplacian eigenvectors, which cost
    n_graphs * 128 * 4 bytes PER CELL and are read by no variant
    (feature_names is always ["cell_gene_counts"]; the only consumer is the
    optional conditioning branch in transforms.py, which no config selects).
    See the rationale on `make_dataset_blob_config`.
    """
    if label_names is None:
        label_names = ["cell_types=annotation", "niche_types=spatial_cluster"]
    if n_neighs_list is None:
        n_neighs_list = [8, 16, 24]
    if k is None:
        k = {"lm_eigvecs": 128}
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": description,
        },
        "dataset": {
            "name": name,
            "feature_names": ["cell_gene_counts"],
            "label_names": list(label_names),
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                # 8 = SQUINT default; 16 = knn16 base; 24 = knn24
                # ablation in the s51 sweep. Pre-built so no rebuild
                # per knn variant.
                "n_neighs_list":      list(n_neighs_list),
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",
                "k": dict(k),
            },
            "data_directory_path": data_directory_path,
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config_spatch_ov() -> dict:
    """Ovarian-cancer spatch subset (`silver/spatch_ov_1p/dataset_id_*.h5ad`)."""
    return _make_spatch_blob_config(
        name="spatch_ov_1p",
        description="spatch_ov_1p — ovarian cancer subset (silver/spatch_ov_1p).",
    )


def make_dataset_blob_config_spatch_hcc() -> dict:
    """HCC (liver) spatch subset (`silver/spatch_hcc_1p/dataset_id_*.h5ad`)."""
    return _make_spatch_blob_config(
        name="spatch_hcc_1p",
        description="spatch_hcc_1p — hepatocellular carcinoma subset (silver/spatch_hcc_1p).",
    )


def make_dataset_blob_config_spatch_coad() -> dict:
    """COAD (colon) spatch subset (`silver/spatch_coad_1p/dataset_id_*.h5ad`)."""
    return _make_spatch_blob_config(
        name="spatch_coad_1p",
        description="spatch_coad_1p — colon adenocarcinoma subset (silver/spatch_coad_1p).",
    )


def make_dataset_blob_config_squint_hln() -> dict:
    """CosMx human lymph node with MANUAL niche annotations from the
    spatial-niche-benchmark study (silver/squint_hln). Single section.
    Labels are kept under their original obs columns and registered here:
    cell types -> obs['cell_type'] (NanoString),
    niches     -> obs['niche'] (4 manual niches: B cell Zone,
                  T cell Zone, Medulla, Germinal Center).
    NOTE: the silver AnnData stores these as 'cell_type' / 'niche' (NOT the
    '*_annotation' names) — the blob loader silently skips missing label
    columns, so a mismatch here yields UNLABELED cells + meaningless NMI/ARI."""
    return _make_spatch_blob_config(
        name="squint_hln",
        description="squint_hln — CosMx human lymph node, manual niches "
                    "(spatial-niche-benchmark).",
        label_names=["cell_types=cell_type",
                     "niche_types=niche"],
    )


def make_dataset_blob_config_squint_hln_geneset(name: str) -> dict:
    """Gene-filtered squint_hln variant blob config (all / hvg2k / svg2k).

    Identical to `make_dataset_blob_config_squint_hln` except for the dataset
    `name`, which points the blob at `silver/<name>/` — the gene-filtered
    AnnData written by `analysis/data_preparation/preprocess_squint_hln.py`
    (negative-control probes removed, then all-detected / top-2000-HVG /
    top-2000-SVG). Same single CosMx section, same manual-niche labels.
    `name` must be one of: squint_hln_allgenes, squint_hln_hvg2k,
    squint_hln_svg2k."""
    return _make_spatch_blob_config(
        name=name,
        description=f"{name} — CosMx human lymph node (gene-filtered), "
                    "manual niches (spatial-niche-benchmark).",
        label_names=["cell_types=cell_type",
                     "niche_types=niche"],
    )


def make_dataset_blob_config_squint_vht() -> dict:
    """Human Tonsil Cell Atlas Visium (silver/squint_vht). Labels kept under
    their original obs columns and registered here:
    cell types -> obs['annotation_20230508'] (121 cell types/states),
    niches     -> obs['area'] (expert-annotated histological areas).
    NOTE: confirm these exact column names from the fetch script's printout
    (zellkonverter export); adjust here if they differ."""
    return _make_spatch_blob_config(
        name="squint_vht",
        description="squint_vht — Human Tonsil Cell Atlas Visium, expert "
                    "histological areas.",
        label_names=["cell_types=annotation_20230508",
                     "niche_types=area"],
    )


def make_dataset_blob_config_xhs_3b() -> dict:
    """Xenium human skin, 3 SECTIONS (batches 11, 19, 32 of the xhs1000-39b_1p
    panel; actually ~4948 genes — the 'xhs1000' prefix is legacy). Populate
    silver/xhs1000-3b_1p/ with `analysis/data_preparation/prep_xhs_3b.py` (NOT a
    plain symlink): the raw files lack `uns['batch']` (the blob HARD-ERRORS
    without it) and have non-unique cell names, so prep stamps a distinct
    uns['batch']/obs['batch'] per section and makes obs_names globally unique.
    Labels kept under their original obs columns: cell types ->
    obs['new_annotation'], niches -> obs['niche_type']
    (both added to compute_inference_metrics' default label keys so they're
    scored automatically). MULTI-section, so cross-batch integration is
    meaningful — the cross-MNN reference variant is the integration-aware
    default; the within-batch variant is the direct analog of the squint_hln
    runs."""
    return _make_spatch_blob_config(
        name="xhs1000-3b_1p",
        description="xhs1000-3b_1p — Xenium human skin, 3 sections "
                    "(batch 11/19/32), 1000-gene panel.",
        label_names=["cell_types=new_annotation",
                     "niche_types=niche_type"],
    )


def make_dataset_blob_config_xhs1000_39b() -> dict:
    """
    R0 target: Xenium human skin, ALL 39 sections of the xhs1000-39b_1p panel.

    The first real scale-up for the streaming backend -- 39 sections / 519,058
    cells / one 4,948-gene panel, versus the paper's largest run at 6 sections.
    Deliberately single-panel, so it tests "does streaming scale?" without
    entangling the open cross-panel question.

    Silver already exists at `<DATA_ROOT>/silver/xhs1000-39b_1p` and needs no
    prep: verified to carry `uns['batch']` (batch0..batch38, unique), and
    `obs['cell_type']` + `obs['NICHE_NAMES']`. It is byte-equivalent to the
    corpus copy under `hst_corpus_110m/xhs1000-39b_1p` (same filenames, same
    4,948-gene panel). Gold lands in the on-disk subdir, which is distinct from
    the pre-existing `in-memory-PyG-dataset-blob/xhs1000-39b_1p`, so that blob
    is untouched.

    BOTH label sets are registered so the paper's niche AND cell-type
    identification metrics (NMI / ARI) are computable, not just reconstruction.

    `k={}` -- no Laplacian eigenvectors, for the reason spelled out in
    `make_dataset_blob_config`: nothing reads them and they cost
    n_graphs * 128 * 4 bytes per cell. `n_neighs_list=[8, 16]` covers the
    SQUINT default k=8 and the knn16 variants; the reference recipe uses 16.
    """
    return _make_spatch_blob_config(
        name="xhs1000-39b_1p",
        description="xhs1000-39b_1p -- Xenium human skin, 39 sections, "
                    "4,948-gene panel. R0 streaming scale-up target.",
        label_names=["cell_types=cell_type", "niche_types=NICHE_NAMES"],
        data_directory_path=str(DATA_ROOT),
        n_neighs_list=[8, 16],
        k={},
    )


def make_dataset_blob_config_xhc38_4b() -> dict:
    """
    Cross-panel milestone 1: `xhc38-4b_1p`, Xenium human colon, 4 sections.

    The NESTED case, and the gentler of the two: two panels of 419 and 319
    genes where 319 is a strict SUBSET of 419, so the block union is 419 and the
    mask hides exactly 100 of those genes for half the sections. Measured from
    the silver `.var` indices, not assumed.

    1,725,440 cells over 4 sections (576,336 / 619,349 / 272,341 / 257,414), so
    ~2.9 GB dense at 419 genes -- bigger than R0 in cells, far smaller in bytes.

    `label_names=[]` because these sections carry NO annotation: `obs` holds
    only Xenium QC columns (`cell_area`, `transcript_counts`, ...), with no
    cell-type or niche column. That is supported -- `SetExperimentDataKeys`
    returns a 0-channel `y` placeholder, giving `num_classes=0` -- and it costs
    nothing here, because the R0 reference recipe's `loss_names` contain no
    cross-entropy, so labels never enter training. It does mean the label-based
    benchmark metrics (NMI / ARI against cell type) are not computable on this
    dataset; it exists to validate the cross-panel DATA PATH, not to produce
    biology.
    """
    return _make_spatch_blob_config(
        name="xhc38-4b_1p",
        description="xhc38-4b_1p -- Xenium human colon, 4 sections, panels "
                    "419 and 319 (nested). Cross-panel milestone 1.",
        label_names=[],
        data_directory_path=str(DATA_ROOT),
        n_neighs_list=[8, 16],
        k={},
    )


def make_dataset_blob_config_xhb42_3b() -> dict:
    """
    Cross-panel milestone 2: `xhb42-3b_1p`, Xenium human brain, 3 sections.

    The HARD case -- a genuine two-way union where neither panel contains the
    other. Panels of 350 and 317 genes with intersection 262, so 88 genes are
    exclusive to one and 55 to the other, and the block union is 405 -- strictly
    larger than either panel. `xhc38` cannot test that: being nested, its union
    is just the wider panel, so a bug that quietly took `max` instead of the
    union would pass there and fail here.

    109,007 cells over 3 sections, ~153 MB dense. Minutes to build.

    `label_names=[]` for the same reason as `xhc38-4b_1p` -- see that config.
    """
    return _make_spatch_blob_config(
        name="xhb42-3b_1p",
        description="xhb42-3b_1p -- Xenium human brain, 3 sections, panels 350 "
                    "and 317 with 88/55 exclusive genes (true union). "
                    "Cross-panel milestone 2.",
        label_names=[],
        data_directory_path=str(DATA_ROOT),
        n_neighs_list=[8, 16],
        k={},
    )


def make_dataset_blob_config_spatch() -> dict:
    """
    Dataset-blob build config for the `spatch_1p` dataset
    (`/nfs/team361/sb75/DATASETS/silver/spatch_1p`, N AnnDatas named
    `dataset_id_<N>.h5ad`).

    Smoke-test scope: this config is intended for a 1-epoch pipeline
    sanity check. It uses cell-type label `obs['annotation']` and niche
    label `obs['spatial_cluster']` — the supervisory keys the user
    specified for this dataset.

    Expectations on the silver AnnDatas:
      - `adata.uns['batch']`           — integer / 'batchN' string for
        each section (required by `InMemoryDatasetBlob._derive_adata_batch_id`).
        `dataset_id_<N>.h5ad` should stamp `uns['batch']` so the file's
        intended id surfaces — e.g. `dataset_id_2.h5ad` -> `uns['batch']='batch2'`
        (or just the int `2`). See `examples/stamp_spatch_uns_batch.py`.
      - `adata.obs['annotation']`      — cell-type label column.
      - `adata.obs['spatial_cluster']` — niche / spatial-cluster label.
      - `adata.obsm['spatial']`        — XY coordinates for the spatial
        k-NN graph build.
      - `adata.obs['batch']`           — per-cell batch (used by the
        batch-key path in the blob's graph build + by
        `build_batch_one_hot_from_obs`). If absent, the loader falls
        back to parsing `cell_id`; either is fine for the smoke test.

    Differences from `make_dataset_blob_config`:
      - dataset name -> spatch_1p
      - data_directory_path -> /nfs/team361/sb75/DATASETS
      - label_names         -> ["cell_types=annotation",
                                "niche_types=spatial_cluster"]
      - n_neighs_list       -> [8] (single k for smoke; expand later
        if knn-ablations are added on spatch_1p).
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": (
                "spatch_1p dataset (silver/spatch_1p/dataset_id_*.h5ad). "
                "Cell-type label = obs['annotation']; niche label = "
                "obs['spatial_cluster']."
            ),
        },
        "dataset": {
            "name": "spatch_1p",
            "feature_names": ["cell_gene_counts"],
            "label_names": [
                "cell_types=annotation",
                "niche_types=spatial_cluster",
            ],
            "graph_kwargs": {
                "coord_type":         "generic",
                "spatial_key":        "spatial",
                "n_neighs_list":      [8, 16],  # 8 = SQUINT default, 16 = knn16 variants
                "radius_list":        None,
                "include_self_loop":  True,
                "batch_key":          "batch",   # falls back to cell_id parse if absent
                "k": {
                    "lm_eigvecs": 128,
                },
            },
            "data_directory_path": "/nfs/team361/sb75/DATASETS",
            "pre_transform": None,
            "pre_filter":    None,
            "overwrite":     True,
        },
        "software_paths": {
            "deepwalk": "",
            "gosh":     "",
        },
    }


def make_dataset_blob_config() -> dict:
    """
    Mirrors  config/create_in_memory_dataset_blob/mmb0-4b_1p.yaml  but for
    our two-section dataset. We turn off DeepWalk/GOSH (which need extra
    binaries) and only build Laplacian eigenvectors. We also build the 8-NN
    spatial graph that SQUINT uses by default.
    """
    return {
        "experiment": {
            "name": "in_memory_dataset_blob",
            "description": "MERFISH MB batch82 + STARmap+ MB batch15 (shared gene panel, coord aligned)",
        },
        "dataset": {
            "name": DATASET_NAME,
            "feature_names": ["cell_gene_counts"],
            # EMPTY for the corpus, deliberately. `cell_type` is present on
            # only 143 of the 636 sections (`_annocoverage.json`), and a
            # partially-present label produces a blob that builds cleanly over
            # ~48 h and then fails during TRAINING: PyG's `collate` takes its
            # key set from the first `Data` in a block, so a block mixing a
            # labelled and an unlabelled section raises KeyError -- or, if the
            # unlabelled one happens to come first, trains without labels and
            # says nothing. `OnDiskDatasetBlob.process()` now refuses this at
            # build time.
            #
            # Nothing is lost. No SQUINT variant registers a cross-entropy
            # loss, so `y_*` is unused during training; and expert labels reach
            # the predicted AnnData through the per-section `.obs` sidecars,
            # where every column appearing in ANY section is propagated and
            # NaN-filled elsewhere (type_conversions.py:296-304). NMI/ARI
            # benchmarking reads them from there.
            "label_names": [],
            "graph_kwargs": {
                "coord_type": "generic",
                "spatial_key": "spatial",     # adata.obsm['spatial']
                # Build the spatial k-NN graph at the k values variants
                # actually select at training time via
                # `cfg.dataset.graph_params.n_neighs`. k=8 is the historic
                # SQUINT default; k=16 is used by the +knn16 variants.
                # Every extra k costs ~14 GB of edge_index over a 110M-cell
                # corpus and is dead weight unless a variant selects it, so
                # this list is deliberately minimal — add a k here and
                # rebuild only when a variant needs it:
                #   python examples/run_squint.py --build-blob
                "n_neighs_list": [8, 16],
                "radius_list": None,
                "include_self_loop": True,
                # NO Laplacian spectral embeddings. Every variant trains
                # with feature_names=["cell_gene_counts"] and never reads
                # U_lm_eigvecs (the only consumer is the optional
                # `spatial_prior_feature` / conditioning path in
                # transforms.py, which no config selects). At 128 eigvecs
                # per graph that is 6*128*4 = 3072 bytes PER CELL, i.e.
                # ~338 GB over hst_corpus_110m plus an eigsh(k=128) solve
                # per graph per section — all of it written, re-read, and
                # ignored. Because the eigenvector table is one row per
                # cell it also survives the streaming loader's node-attr
                # filter and is copied into every mini-batch.
                # Re-add {"lm_eigvecs": N} and rebuild only if a future
                # variant genuinely consumes spectral features.
                "k": {},
            },
            "data_directory_path": str(DATA_ROOT),
            "pre_transform": None,
            "pre_filter": None,
            "overwrite": True,
        },
        "software_paths": {
            # not used because we omit deepwalk/gosh from k above
            "deepwalk": "",
            "gosh": "",
        },
    }


# ---------------------------------------------------------------------------
# 2a. Training config — minimal POC (Graph VQ-VAE baseline)
# ---------------------------------------------------------------------------

def make_train_config_poc() -> dict:
    """
    Right-sized Graph VQ-VAE baseline for 431-gene / ~100k-cell spatial data.

    Design choices:
    - No HVG subsetting (`apply_hvg=False`): the gene panel is already curated.
      NOTE HVG is NOT a way to handle multiple gene panels, despite what an
      earlier version of this note claimed. A genuinely multi-panel input never
      reaches HVG at all -- the blob build requires an identical `.var` across
      sections and raises first (in_memory_dataset_blob.py:243-263). And on
      same-panel input, `SubsetHVG` runs PER SECTION (PyG applies the dataset
      transform in `Dataset.__getitem__`), so with >1 section each section picks
      its own gene set and column j stops meaning the same gene. That is now
      refused by `initialize_dataset_blob` rather than silently corrupting
      features; see dataset/transform_scope.py.
    - Encoder: 431 -> MLP[400,256] -> GNN[128, 1 layer, 8-NN] -> VQ[30 codes].
      Latent dim = 128 keeps the VQ cosine-similarity manifold tractable for
      30 codes.  A 30-code codebook with 400-dim embeddings is over-parametrised
      and slows EMA convergence.
    - Decoder: latent[128] -> MLP[400] -> softmax * read_depth -> NB mean.
    - Adjacency decoder: kept at low capacity (128 -> MLP[64]) to avoid the
      BCE adj loss dominating when it's added.
    - Dropout: removed.  With a 30-code bottleneck the model has limited spare
      capacity and dropout further destabilises VQ code assignment.
    - Sampler: 1 hop, 8 neighbors — matches GNN num_layers=1.  Sampling 2 hops
      for a 1-layer GNN is wasted compute.
    - LR: 5e-4 (conservative; 1e-3 tends to cause sharp loss spikes on VQ init).
    - Batch: 256.  ~100k cells -> ~390 batches/epoch, sufficient gradient
      diversity for EMA codebook updates.
    - Epochs: 80.  EMA VQ needs ~40-60 epochs to stabilise dead-code rate.
    - Metrics: pearson_gene_wise_log1p is the primary checkpoint signal.
      It is the metric reported by scVI-tools, NicheCompass, and spatial-
      imputation papers (Tangram, SpaGE, STAGATE).  Per-gene Pearson on
      log1p(counts) assigns equal weight to all genes and is insensitive to
      the mean-count dominance that makes raw-count cell-wise Pearson decrease
      early in training.  pearson_cell_wise_log1p and pearson_gene_wise are
      tracked as secondary metrics.

    recon_mode is injected by patch helpers and defaults to 'cell' here.
    """
    return {
        "experiment": {"seed": 0, "mode": "standalone"},
        "logging": {
            "root_log_dir": str(LOG_ROOT),
            # Don't upload checkpoints to wandb. Lightning's
            # ModelCheckpoint already writes the best ckpt to
            # `<run_dir>/checkpoints/` on NFS, which is the canonical
            # location predict() reads from. Wandb upload of large
            # tensors stages each ckpt into $WANDB_CACHE_DIR (default
            # $HOME/.cache/wandb/artifacts/staging/) and across a
            # multi-variant sweep that overflows typical HPC home quotas.
            "log_model": False,
            "offline": False,
            "enabled": True,
        },
        "dataset": {
            "dataset_name": DATASET_NAME,
            # `dataset_tag` is the SHORT identifier used for artifact /
            # run directory paths (`<ARTIFACTS_DIR>/<dataset_tag>/<variant>/
            # <timestamp>/`). It strips the technical suffixes that live
            # in `dataset_name` (`_coord_aligned`, `_shared_genes`) — those
            # belong to the silver / gold blob paths but make on-disk
            # artifact dirs unwieldy. If a future variant doesn't set
            # `dataset_tag`, the artifact path falls back to
            # `dataset_name`.
            "dataset_tag": "mmb0-1b_smb1-1b_1p",
            # adata_batch_idx are *positions* in the sorted DatasetBlob:
            #   idx 0 -> batch15 (STARmap+)   idx 1 -> batch82 (MERFISH)
            "adata_batch_idx": [0, 1],
            "root_data_dir": str(DATA_ROOT),
            # HVG subsetting — OFF for this dataset (431 curated genes), and
            # off everywhere: `apply_hvg=True` is REFUSED whenever more than one
            # section is loaded, because `SubsetHVG` is applied per section and
            # would select a different gene set per section (see
            # dataset/transform_scope.py). It is also not a route to multi-panel
            # support: mismatched panels are rejected earlier, by the blob build.
            # Enabling it needs a corpus-wide gene set chosen once and the same
            # indices applied to every section, which does not exist yet.
            "apply_hvg": False,
            "n_hvg": 2000,                    # only used when apply_hvg=True
            "gene_count_transform_names": [], # populated by train() based on apply_hvg
            "gene_count_transform_params": {"n_genes": 2000},
            "graph_params": {
                "spatial_key": "spatial",
                "delaunay": False,
                "n_neighs": 8,
                "radius": None,
                # Source for per-cell batch labels used by FiLM batch-correction
                # and by `build_batch_one_hot_from_obs`. When this column is
                # present in adata.obs at blob-build time, its values are
                # stored per-cell and used downstream (densified to contiguous
                # indices). When absent, the loader falls back to parsing
                # "batchN" substrings out of `obs['cell_id']`.
                "batch_key": "batch",
            },
            "feature_names": ["X"],
            "label_name": "cell_types",
            "train_transform_names": ["SpatialBatchSplit"],
            "train_transform_params": {
                "region": None,
                "train_batches": [15, 82],
                "val_batches": [],
                "test_batches": [],
                "xy_key": "xy_coordinates",
                # Cell-level holdout for in-distribution early stopping +
                # best-checkpoint-on-val. ONLY applied when `val_batches`
                # is empty (no whole-section holdout configured). When
                # `val_batches` is non-empty, that section IS the val set
                # — we do NOT additionally sub-sample training cells.
                # Adversarial training can be unstable late; a 10%
                # in-section val sample gives a stable in-distribution
                # signal that early stopping and best-ckpt-on-val use.
                # Set to 0.0 to disable.
                "train_val_cell_split": 0.10,
                "cell_split_seed":      0,
            },
        },
        "datamodule": {
            "loader_name": "NeighborLoader",
            "loader_params": {
                # batch_size pinned at 256. PAIRED with `max_epochs=80`
                # — together these are the canonical config that
                # produced the published-style niche NMI numbers
                # (~0.66-0.70 on the +enc-deeper+adv-warmup10 family).
                # See the long `max_epochs` history comment below for
                # the full rationale on why both knobs were reverted
                # in lockstep. Bigger batches require lr rescaling
                # (5e-4 * sqrt(new/256) for sqrt, 5e-4 * (new/256)
                # for linear) and post-hoc iLISI / niche-NMI
                # validation — don't change just one knob.
                "batch_size":         256,
                # 22 DataLoader workers. Kept high (the dataloader-
                # throughput knobs aren't part of the speedups
                # rolled back 2026-05-12 — the regression is on the
                # model / precision / optimizer side, not the data
                # side). Pair with `LSF_CORES=24` in the submit
                # scripts: 22 workers + 1 main proc + 1 spare = 24
                # cores. `_loader_perf_kwargs()` adds `pin_memory=True`,
                # `persistent_workers=True`, and `prefetch_factor=8`
                # automatically, so don't set those here (that
                # triggers `TypeError: got multiple values for
                # keyword argument` on the loader call).
                "num_workers":        22,
            },
            "sampler_name": "NeighborSampler",
            "sampler_params": {"num_neighbors": [8]},  # 1 hop = num_layers
            # Two-knob inference-sampling config:
            #
            #   `sample_neighbors_for_inference`: VAL + TEST loaders.
            #     `True`  (current): val/test use the training `[8]`
            #             fanout. Cheap per step → GPU stays fed
            #             between training epochs. Was the v11 fix
            #             for the spike-then-long-gap GPU pattern.
            #     `False` (legacy): val/test sample
            #             `num_neighbors=[-1]` (every neighbor).
            #             Heavier per step, slow val epochs, but
            #             deterministic and identical to predict.
            #
            #   `predict_sample_neighbors_for_inference`: PREDICT loader
            #             only — i.e. the final pass that writes
            #             `neighborhood_emb` / `cell_emb` to the
            #             output AnnData consumed by the benchmark
            #             figures.
            #     `False` (current, DEFAULT): fan out `[-1]` → every
            #             neighbor → deterministic per-cell niche
            #             embedding. Required for iLISI / MMD /
            #             NMI to be meaningful (with `[8]` the
            #             niche embedding picks up the
            #             sampling-noise-correlated-with-batch
            #             signature and iLISI collapses to 0).
            #     `True`: stochastic predict (e.g. dropout-style
            #             uncertainty). Don't enable for figure runs.
            #
            # Split rationale: val running every epoch wants the
            # cheap sampler for throughput, but the single final
            # predict pass should be the heavy deterministic one —
            # those are different cost-vs-correctness trade-offs.
            "inference_params": {
                "sample_neighbors_for_inference":         True,
                "predict_sample_neighbors_for_inference": False,
                # Predict-only batch-size override. Training batch is
                # 256 (tuned for adversarial gradient dynamics);
                # predict runs under torch.no_grad() so batch noise is
                # irrelevant and the cost is dominated by kernel-launch
                # overhead at small batch sizes.
                #
                # DEFAULT: 65536 (= 64×1024, bumped 8× from 8192 on
                # 2026-05-12). Predict-time math is unaffected by
                # batch grouping (codebook argmin is per-cell,
                # deterministic regardless of batch composition), so
                # bigger is purely a throughput win for the no-grad
                # forward pass.
                #
                # Memory math on the mmb-smb graph with `[-1]` predict
                # fanout (avg degree ~12):
                #   nodes_per_batch ≈ 65536 × 13 ≈ ~852k nodes/batch
                #   input X tensor   ≈ 852k × 431 genes × 4 B ≈ ~1.4 GiB
                #   + model activations (no autograd, no opt state):
                #     ~5-10 GiB on the s17_v2 spine, less on s25's
                #     128-dim tight spine.
                # Total peak GPU mem ~10-15 GiB on the wide spine,
                # ~5-8 GiB on the tight s25 spine — comfortable on
                # any H100/H200 (80+ GiB) and trivial on the
                # gpu_high_memory (384 GiB) class.
                #
                # Fallback chain on OOM (most likely on denser
                # datasets like chl59-8b_1p, or mmb20 with 20+
                # sections):
                #   65536 → 32768 → 16384 → 8192 → 4096 → 2048 → 1024.
                "predict_batch_size":                     65536,
                # Val/test batch-size override. Val runs every
                # `check_val_every_n_epoch` (see trainer config below
                # — currently every 2 train epochs), and the per-val-
                # pass overhead used to be visible as a periodic
                # ~0% GPU-utilisation dip in wandb. Bumping val to
                # 4096 (= 16× training) makes each val pass ~4-8×
                # faster, shrinking those dips to near-invisible.
                # Val fanout is lighter than predict's [-1] (val uses
                # `sample_neighbors_for_inference=True` -> [8]), so
                # val can comfortably go larger than predict without
                # OOM. Same fallback logic as predict_batch_size if
                # you ever see val-time OOM:
                # 4096 → 2048 → 1024 → 512.
                "val_batch_size":                         4096,
                # Pre-cache one full epoch of training batches on the
                # first `train_dataloader()` call, then serve them from
                # CPU memory on every subsequent epoch (reshuffled order,
                # fixed content).
                #
                # DEFAULT: False  (BACKED OFF 2026-05-12).
                #
                # The cache path was on briefly with an internal
                # `num_neighbors=[-1]` override (every cell sees its
                # full neighborhood, frozen for all 80 epochs). Multi-
                # seed runs on that config dropped niche / cell metrics
                # severely vs the s17_v2 historic numbers — most
                # plausibly because:
                #   (a) The niche-branch GNN was trained with
                #       stochastic `[8]`-fanout sampling, so the
                #       per-epoch resampling acted as data
                #       augmentation. Freezing the neighbourhood
                #       (let alone freezing the FULL neighbourhood)
                #       removed that and pushed nbr_NMI / nbr_iLISI
                #       down.
                #   (b) The cached batches are ~2× bigger in node
                #       count than the live-sampled batches, which
                #       changes the per-step gradient and possibly
                #       OOM behaviour on smaller GPUs.
                # Neither (a) nor (b) was caught by a smoke test —
                # only the full multi-seed sweep surfaced the
                # regression. Default reverted to False until we have
                # a clean A/B vs the historic baseline.
                #
                # The plumbing (the kwarg in `InMemoryDataModule`, the
                # internal `[-1]` override, the `.cpu()` defensive
                # copy) is kept in place; opt back in by setting
                # `cache_train_batches: True` here or in a per-variant
                # patch once we trust it again. For sampler-heavy
                # datasets (chl59, mmb20) it's also worth keeping the
                # option visible.
                #
                # Lives in `inference_params` only because that's the
                # dict the initialiser unpacks into `InMemoryDataModule`'s
                # kwargs. It's a TRAIN-side flag (val/test/predict are
                # unaffected) — don't read meaning into the name.
                # CONFIRMED CULPRIT 2026-05-12: `cache_train_batches=True`
                # caused the multi-seed regression. The single-flip A/B
                # showed metrics dropping severely when only this knob
                # was changed (everything else was historic-compatible).
                # The cache-build internal [-1] fanout override (see
                # `in_memory_datamodule.py` `train_dataloader`) is the
                # most likely mechanism: freezing the full per-cell
                # neighborhood removes the per-epoch [8]-sample
                # regularisation the niche GNN depends on. Keeping the
                # default OFF; the plumbing stays available behind the
                # flag for future investigation.
                "cache_train_batches":                    False,
            },
        },
        "model": {
            "model_name": "VQNiche",
            "encoder_name": "VQNiche_Encoder",
            "attribute_decoder_name": "MLPSoftmax",
            "adjacency_decoder_name": "MLP_AdjacencyDecoder",
            "predictor_name": "Linear",
            "imputation_params": {
                "mask_strategy": "original",
                "base_mask_ratio": 0.0,
                "final_mask_ratio": 0.0,
                "warmup_epochs": 0,
                "deterministic_masking": False,
                "compute_mask_input_diversity": False,
                "mask_token_eps": 0.0001,
            },
            # All Pearson metrics are reported on every variant.
            # Neighbourhood metrics (1hop_nbr) are silently skipped when
            # X_nbr / X_hat_nbr are absent (recon_mode='cell' variants).
            # In-training metric computation is OFF by default. Both
            # lists are populated by `_resolve_metrics_list()`, which
            # returns `[]` unless `SQUINT_WITH_PEARSON=1` is set
            # (passing `--with-pearson` to `main()` exports that env
            # var automatically). See the module-level docstring for
            # `_resolve_metrics_list()` for the full rationale.
            #
            # Why both lists use the same source:
            #   - train_metrics_list is consumed by
            #     BaseModel.compute_metrics(mode='train' | 'val'),
            #     fired from on_validation_epoch_end.
            #   - test_metrics_list is consumed by the same helper
            #     with mode='test', fired from trainer.test().
            # The standard pipeline doesn't call trainer.test() (we
            # use predict + compute_inference_metrics.py instead), so
            # test_metrics_list is essentially dormant. But aligning
            # it with train avoids surprise behaviour if anyone wires
            # in a test loop later, and keeps a single switch for
            # "Pearson on / off everywhere during PL hooks".
            "train_metrics_list": _resolve_metrics_list(),
            "test_metrics_list":  _resolve_metrics_list(),
            "encoder_params": {
                "gnn_name": "SAGEConv",
                "mlp_params": {
                    "hidden_channels": [400, 256],
                    "dropout": 0.0,
                    "act": "relu",
                    "norm": None,
                },
                "gnn_params": {
                    # Latent dim doubled from 128 -> 256: this is the codebook
                    # embedding dim for ALL VQ variants. With codebook=30 and
                    # 431 input genes, 128 was a tight bottleneck; 256 gives
                    # the codebook ~2x more representational capacity.
                    "hidden_channels": 256,
                    "num_layers": 1,
                    "act_first": True,
                    "activation": "relu",
                    "norm": None,
                    "dropout": 0.0,
                    "init_method": "kaiming_uniform",
                },
                # POC: no FiLM conditioning
                "vq_params": {
                    "vq_name": "VectorQuantize",
                    "freeze_codebook": False,
                    "use_cosine_sim": True,
                    "ema_update": True,
                    "manual_ema_update": False,
                    "threshold_ema_dead_code": 2,
                    "manual_in_place_optimizer_update": False,
                    "learnable_codebook": False,
                    "codebook_size": 30,
                    "heads": 1,
                    "separate_codebook_per_head": False,
                    "decay": 0.8,
                    "eps": 0.00001,
                    "kmeans_init": True,
                    "kmeans_iters": 10,
                    "sync_kmeans": True,
                    "sample_codebook_temp": 0.0,
                    "commitment_weight": 0.0,
                    "commitment_use_cross_entropy_loss": False,
                    "codebook_diversity_loss_weight": 0.0,
                    "codebook_diversity_temperature": 100.0,
                    "orthogonal_reg_weight": 0.0,
                    "orthogonal_reg_max_codes": None,
                    "orthogonal_reg_active_codes_only": False,
                },
            },
            "attribute_decoder_params": {
                "apply_conditioning": None,
                "mlp_params": {
                    # Decoder depth bumped from [400] -> [400, 400] to give
                    # the NB-mean head more capacity to expand the 256-dim
                    # latent into a 431-gene rate vector.
                    "hidden_channels": [400, 400],
                    "dropout": 0.0,
                    "act": "gelu",
                    "norm": "layer_norm",
                },
            },
            "adjacency_decoder_params": {
                "out_channels": 128,
                "mlp_params": {
                    "hidden_channels": [64],
                    "dropout": 0.0,
                    "act": "relu",
                    "norm": "layer_norm",
                },
            },
            "optimizer_params": {
                "optimizer_name": "adam",
                "lr": 0.0005,
                "weight_decay": 0.001,
                "mask_lr_scale": 1.0,
                # `fused=True` coalesces parameter updates into a single
                # CUDA kernel instead of per-tensor launches. 5-15%
                # wall-clock saving on the optimizer step for SQUINT's
                # many-small-tensor regime.
                #
                # DEFAULT: False (REVERTED 2026-05-12, third pass).
                #
                # Turned off as the new bisection suspect after the
                # conservative-baseline rollback didn't restore
                # metrics. In theory fused Adam is mathematically
                # identical to the unfused path (same Adam update,
                # just better-batched kernels), but: torch's fused
                # Adam has had bf16-master-weight + dynamic-graph
                # edge cases in the past, and some PyTorch versions
                # silently skip the codebook EMA params (which have
                # `requires_grad=False` but still need to flow
                # through `optimizer.step` for the rest of the model
                # to update correctly). If toggling this off
                # restores metrics, we'll know fused is the culprit
                # and look at it post-deadline.
                "fused": False,
            },
            "loss_params": {
                "loss_names": [
                    "nb_attribute_reconstruction_loss",
                    "mse_commit_loss",
                ],
                "loss_kwargs": {
                    # recon_mode controls which NB targets are computed:
                    #   "cell" -> per-cell (default baseline)
                    #   "nbr"  -> 1-hop neighbourhood mean (niche-aware signal)
                    #   "both" -> cell + neighbourhood (requires adding
                    #             nb_attribute_reconstruction_loss_nbr to loss_names)
                    "recon_mode": "cell",
                    "only_masked": False,
                    "edge_sampling_ratio": 2,
                    "use_pos_weight": True,
                    "estimate_adj_kwargs": {"nonlinearity": "sigmoid", "k": 8},
                    "wt_attr_reconstr": 1.0,
                    "wt_attr_reconstr_nbr": 1.0,
                    # Code-conditional dispersion. When enabled, a small MLP
                    # maps z_q -> log(theta) per-cell-per-gene, replacing the
                    # global per-gene scalar parameter. Default OFF; ablate
                    # via a future +ccd patch.
                    # Format options:
                    #   False                              -> off
                    #   True                               -> on, linear head (no hidden)
                    #   {"enabled": True, "hidden_channels": [128]}
                    "code_conditional_dispersion": False,
                    # Adjacency-loss input mode.
                    # False (default): edge logits = h_adj_i^T h_adj_j where
                    #     h_adj is the output of the MLP adjacency_decoder.
                    # True            : edge logits = z_q_i^T z_q_j (the
                    #     quantized latent itself).  This is the
                    #     NicheCompass / standard graph-VAE formulation —
                    #     forces the codebook embeddings to directly encode
                    #     pairwise spatial proximity, which is exactly the
                    #     bias we want when codes should capture niches.
                    "bypass_adj_decoder": False,
                    "wt_commit": 1.0,
                },
            },
        },
        "trainer": {
            # Best-checkpoint metric: val_loss (sum of all weighted loss
            # terms over the in-distribution VAL set — same 10% cell-level
            # holdout from the training sections that early stopping
            # watches; see `train_val_cell_split`). Single source of truth
            # for "best epoch" — the saved ckpt is the one early stopping
            # would have stopped at. Robust to adversarial late-stage
            # instability: when the encoder oscillates, val_loss climbs
            # before train_loss does, so the kept snapshot is from before
            # the drift.
            "monitor": "val_loss",
            "checkpoint_params": {"mode": "min", "save_top_k": 1, "save_last": True},
            # max_epochs is the universal training budget cap — every
            # individual / multi-seed / ablation run picks this value
            # up from the base config. At batch=256 / ~100k-cell
            # dataset that's ~390 gradient steps/epoch, so 80 epochs
            # ≈ 31k total gradient steps.
            #
            # HISTORY (don't repeat the cycle):
            #
            #   - Original (2026-04 / early 2026-05): max_epochs=80,
            #     batch=256, lr=5e-4. The "good" Pareto: niche NMI
            #     ~0.66-0.70 on the +enc-deeper+adv-warmup10 family.
            #
            #   - 2026-05-11 v11 push: bumped to (1024, 200) to chase
            #     GPU duty cycle. lr was NOT rescaled; per-sample
            #     gradient dropped 4x; adversarial discriminator
            #     under-trained; `neighborhood_emb` iLISI collapsed to
            #     numerical noise. Reverted.
            #
            #   - 2026-05-12 partial revert: (256, 200) — i.e. kept the
            #     extended budget but restored the small-batch
            #     dynamics. Result: niche NMI dropped ~0.03-0.05 across
            #     EVERY variant in the v12-v16 ablations (0.69 -> 0.62
            #     family). Cause: early-stopping monitor is `val_loss`,
            #     which keeps inching down on the adversarial CE +
            #     reconstruction terms even when the encoder is
            #     over-correcting batch effects. With patience=20 +
            #     min_delta=0.1, stopping never fires before epoch 200,
            #     and the saved "best val_loss" checkpoint lands in
            #     the over-corrected regime — niche structure
            #     forgotten.
            #
            #   - Now (2026-05-12): reverted to max_epochs=80, the
            #     working budget. Don't bump this in isolation again.
            #     If you ever want to revisit "long-train better
            #     Pareto", do it with a niche-quality-aware checkpoint
            #     monitor (e.g. `val_pearson_gene_wise_1hop_nbr_log1p`
            #     via SQUINT_WITH_PEARSON=1) AND verify post-hoc
            #     niche NMI before publishing.
            "max_epochs": 80,
            # Training precision.
            #
            # DEFAULT: "32-true" (full rollback to pre-session
            # baseline 2026-05-12). All efficiency knobs are off
            # EXCEPT `deterministic=False / benchmark=True` (kept
            # below). Opt back in per-run with `--precision bf16-mixed`
            # once the conservative config confirms metrics back at
            # historic.
            "precision": "32-true",
            # Determinism vs benchmark mode.
            #
            # `deterministic: True` (DEFAULT, REVERTED 2026-05-12).
            # Was briefly False (with `benchmark=True`) for a 5-10%
            # wall-clock saving via cuDNN autotune; reverted after
            # the autotune-driven kernel variation turned out to
            # destabilise adversarial training on multiple variants
            # (CE jumped + early stopping fired). Adversarial min-max
            # is genuinely sensitive to per-step numerical
            # differences — cuDNN benchmark mode picks different
            # kernels per shape across runs, and the accumulated
            # noise breaks the encoder/discriminator co-evolution.
            #
            # Implies `benchmark=False` in the Trainer (Lightning
            # forces this when deterministic=True). Override per-run
            # via the `--deterministic`/`--no-deterministic` flags
            # if you want to A/B test benchmark mode on a non-
            # adversarial variant.
            "deterministic": True,
            # Lightning Trainer strategy.
            #
            # DEFAULT: "ddp_find_unused_parameters_true" (historic value,
            # REVERTED 2026-05-12). Was briefly "auto" — empirically
            # that turned out to destabilise adversarial training
            # across multiple variants (CE jumped + early stopping
            # fired). My earlier claim that "strategy=auto only
            # changes the Lightning wrapper class, not the math"
            # turned out to be wrong for SQUINT's adversarial dynamics
            # — Lightning's `find_unused_parameters_true` path
            # interacts with the GRL branch's gradient handling in a
            # way that single-device strategy doesn't, and the
            # difference is enough to flip the adversarial min-max
            # game on this workload. Sticking with the historic
            # default. Override per-run via `--strategy <STR>` for
            # experimentation only.
            "strategy": "ddp_find_unused_parameters_true",
            # torch.compile the model after init.
            #
            # DEFAULT: False (REVERTED 2026-05-12 after empirical test).
            #
            # The compile path is fully implemented and crash-free
            # (the VQ `gumbel_sample` incompatibility is handled by
            # `_install_compile_safe_one_hot()` — see definition near
            # `train()`). HOWEVER, on a multi-seed test run, compile
            # made wall-clock SLOWER, not faster. Most likely cause:
            # the compile / autotune cost per first-time shape isn't
            # amortised across enough iterations because:
            #   - The dataset has only ~340 batches/epoch at bs=256;
            #     `dynamic=True` still recompiles a few times for
            #     extreme-end shapes during the first epoch.
            #   - SQUINT's per-step compute is so small that even a
            #     few extra ms of dynamo overhead per step wipes out
            #     the kernel-fusion savings.
            # Opt back in via `--compile` once we have a way to keep
            # compile cheap (e.g. AOT compile + cached recompiles
            # across seeds), or for runs with a much higher per-step
            # cost (bigger batch, deeper model).
            "compile_model": False,
            # Run validation every 2 train epochs (Lightning default
            # would be every epoch). Each val pass is a sub-pass over
            # `val_dataloader` (cell-level 10% holdout from the train
            # split) which causes GPU utilization to dip to ~0% while
            # the train DataLoader workers idle and the val workers
            # spin up. On a ~50s/epoch budget those dips cost ~20% of
            # total throughput. Halving the val frequency recovers
            # ~10% wall-clock with no model risk — early stopping
            # (patience=20 val passes = 40 train epochs at this
            # setting) and best-checkpoint selection still work
            # exactly as before, just on a coarser val grid.
            "check_val_every_n_epoch": 2,
            "enable_checkpointing": True,
            "ckpt_path": "best",
            # Early stopping: watch the val_loss (= sum of all weighted
            # loss terms over the val set). Switching from train_loss to
            # val_loss makes early stopping respond to in-distribution
            # generalisation rather than the optimisation objective on
            # the training set, which matters in particular for the
            # adversarial variants where train_loss can keep declining
            # while val performance silently degrades.
            #
            # patience=10 VAL PASSES — IMPORTANT: Lightning counts
            # `patience` in val passes, not train epochs. With
            # `check_val_every_n_epoch=2` (see trainer config above),
            # one val pass = two train epochs, so patience=10 here
            # means "stop if val_loss hasn't improved for 20 train
            # epochs" — same effective horizon as the original
            # patience=20 / val-every-epoch setup.
            #
            # 20 train epochs ≈ 8k gradient steps at
            # batch_size=256 / ~100k cells — enough to distinguish
            # a true plateau from a codebook reshuffle, and enough
            # to ride through the adversarial alpha ramp-up that
            # comes after `adv-warmup10` (otherwise patience can
            # fire during the 10-epoch warm-up + ~5 post-warm-up
            # epochs, before the adv-driven val_loss drift has had
            # time to stabilise).
            #
            # min_delta=0.1: small absolute, but val_loss in this
            # codebase ranges ~140-170 so 0.1 ≈ 0.07% relative.
            "early_stopping_params": {
                "enabled":    True,
                "monitor":    "val_loss",
                "mode":       "min",
                "patience":   10,    # 10 val passes × 2 train epochs/pass = 20 train epochs
                "min_delta":  0.1,
            },
        },
    }


# ---------------------------------------------------------------------------
# 2b. Training config — full SQUINT (FiLM + masking + adjacency)
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# 2b. Ablation patch helpers
# ---------------------------------------------------------------------------
# Each patch is a small function that mutates a DEEP COPY of a base config and
# returns it. A patch changes ONE design component so its effect can be
# isolated. Compose patches to build compound variants.
#
# The full materialised config is always saved to disk per run, so
# reproducibility does not depend on patch helpers being preserved.
# ---------------------------------------------------------------------------

def _copy(cfg: dict) -> dict:
    import copy
    return copy.deepcopy(cfg)


# ---- Reconstruction mode ---------------------------------------------------

def _patch_recon_cell(cfg: dict) -> dict:
    """Per-cell NB reconstruction only (baseline). Explicitly sets recon_mode."""
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["recon_mode"] = "cell"
    losses = cfg["model"]["loss_params"]["loss_names"]
    # Ensure only the cell loss is present, remove nbr branch if any.
    if "nb_attribute_reconstruction_loss_nbr" in losses:
        losses.remove("nb_attribute_reconstruction_loss_nbr")
    if "nb_attribute_reconstruction_loss" not in losses:
        losses.insert(0, "nb_attribute_reconstruction_loss")
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_recon_nbr(cfg: dict) -> dict:
    """
    Neighbourhood-only NB reconstruction.
    Per-cell NB is deactivated: recon_mode='nbr' makes training_step route
    the 1-hop-aggregated pair through the legacy pred_attr/target_attr keys
    so `nb_attribute_reconstruction_loss` computes NB on neighbourhood means.
    Per-cell counts are not reconstructed in this variant.
    """
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["recon_mode"] = "nbr"
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "nb_attribute_reconstruction_loss_nbr" in losses:
        losses.remove("nb_attribute_reconstruction_loss_nbr")
    if "nb_attribute_reconstruction_loss" not in losses:
        losses.insert(0, "nb_attribute_reconstruction_loss")
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_recon_both(cfg: dict) -> dict:
    """
    Per-cell + 1-hop neighbourhood NB reconstruction simultaneously.
    Uses TWO separate loss dispatches:
      nb_attribute_reconstruction_loss     -> cell pair (pred_attr / target_attr)
      nb_attribute_reconstruction_loss_nbr -> nbr pair  (pred_attr_nbr / target_attr_nbr)
    Weighted equally by default (wt_attr_reconstr = wt_attr_reconstr_nbr = 1.0).
    """
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["recon_mode"] = "both"
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "nb_attribute_reconstruction_loss" not in losses:
        losses.insert(0, "nb_attribute_reconstruction_loss")
    if "nb_attribute_reconstruction_loss_nbr" not in losses:
        idx = losses.index("nb_attribute_reconstruction_loss") + 1
        losses.insert(idx, "nb_attribute_reconstruction_loss_nbr")
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


# ---- Adjacency reconstruction ----------------------------------------------

def _patch_adj(cfg: dict, weight: float = 1.0) -> dict:
    """
    Add BCE adjacency reconstruction loss.
    The adjacency decoder predicts which pairs of cells in the seed-node
    induced subgraph are connected in the 8-NN spatial graph. Pushes the
    H_adj latent toward encoding local spatial topology.
    Does NOT change recon_mode — stack with a recon patch for combinations.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "bce_adjacency_reconstruction_loss" not in losses:
        losses.append("bce_adjacency_reconstruction_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_adj_reconstr"] = float(weight)
    return cfg


# ---- FiLM conditioning -----------------------------------------------------

def _patch_film(cfg: dict) -> dict:
    """
    Add FiLM (feature-wise linear modulation) conditioning on
    (rbf_distances, cell_batch_id) in both encoder and attribute decoder.
    Enables the model to distinguish cells from different batches / spatial
    scales without leaking batch identity into the VQ codebook.
    """
    film_params = {
        "condition_list": ["rbf_distances", "cell_batch_id"],
        "use_bias": True,
        "use_residual": False,
        "residual_weight": 0.2,
        "init_mode": "identity",
    }
    cfg["model"]["encoder_params"]["conditioning_params"] = film_params.copy()
    cfg["model"]["attribute_decoder_params"]["apply_conditioning"] = "in-MLP"
    cfg["model"]["attribute_decoder_params"]["conditioning_params"] = film_params.copy()
    cfg["model"]["optimizer_params"]["mask_lr_scale"] = 1.0  # unchanged here
    return cfg


# ---- MAE-style masking -----------------------------------------------------

def _patch_masking(cfg: dict,
                   base_ratio: float = 0.2,
                   final_ratio: float = 0.6,
                   warmup: int = 5) -> dict:
    """
    Enable MAE-style gene masking. A learnable mask token replaces a random
    fraction of input genes; the model must reconstruct all genes from the
    partial signal. Forces the encoder to propagate neighbourhood context
    rather than copying the input. mask_lr_scale=2.0 gives the mask token
    a higher LR so it moves fast relative to the rest of the network.
    """
    cfg["model"]["imputation_params"] = {
        "mask_strategy": "learnable_parameter",
        "base_mask_ratio": base_ratio,
        "final_mask_ratio": final_ratio,
        "warmup_epochs": warmup,
        "deterministic_masking": False,
        "compute_mask_input_diversity": False,
        "mask_token_eps": 0.0001,
    }
    cfg["model"]["optimizer_params"]["mask_lr_scale"] = 2.0
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "mask_token_regularization" not in losses:
        losses.append("mask_token_regularization")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_mask_token_regularization"] = 0.001
    return cfg


# ---- Multi-head VQ ---------------------------------------------------------

def _patch_multihead_vq(cfg: dict, heads: int = 10, codebook_size: int = 5000) -> dict:
    """
    Switch from a single 30-code codebook to a multi-head VQ with `heads`
    parallel codebooks of size `codebook_size` each. Total number of
    representable niche combinations = codebook_size^heads (exponential), but
    the model learns to use a much smaller effective set. Used in full SQUINT.
    """
    cfg["model"]["encoder_params"]["vq_params"].update({
        "codebook_size": codebook_size,
        "heads": heads,
        "separate_codebook_per_head": False,
    })
    return cfg


# ---- Residual VQ (RQ-VAE) --------------------------------------------------

def _patch_rvq(cfg: dict, codebook_sizes=(30, 200)) -> dict:
    """
    Switch the VQ to ResidualVQ_Squint: an L-level residual quantizer where
    each cell is encoded as a SUM of L codes drawn from L independent
    codebooks of (potentially different) sizes.

    Default: levels=[30, 200] -> 30 * 200 = 6000 distinct quantized
    representations, with a clean coarse->fine interpretation.
    Level 1 (30) acts as the macro niche; level 2 (200) as a refinement
    code applied uniformly across the whole space (no per-bucket codebook).

    The lucidrains-internal commit_loss is left at 0 (commitment_weight=0.0)
    so that the external `mse_commit_loss` remains the single source of
    commit signal — same convention as the rest of the variants.
    """
    sizes = list(codebook_sizes)
    cfg["model"]["encoder_params"]["vq_params"] = {
        "vq_name": "ResidualVQ_Squint",
        "num_quantizers": len(sizes),
        "codebook_size": sizes,             # accepted as list by our wrapper
        "use_cosine_sim": True,
        "ema_update": True,
        "decay": 0.8,
        "eps": 0.00001,
        "threshold_ema_dead_code": 2,       # only applied to level 1 internally
        "kmeans_init": True,                # only applied to level 1 internally
        "kmeans_iters": 10,
        "sync_kmeans": True,
        "commitment_weight": 0.0,
        "sample_codebook_temp": 0.0,
    }
    return cfg


# ---- Conditional / Tree VQ -------------------------------------------------

def _patch_cvq(cfg: dict, k1: int = 30, k2: int = 10) -> dict:
    """
    Switch the VQ to ConditionalVQ: a true 2-level tree where each level-1
    code (K1 of them) selects a separate level-2 codebook (K1 codebooks of
    K2 codes each).

    Default: K1=30, K2=10 -> 300 distinct quantized representations,
    organised hierarchically. With ~100k cells / 30 buckets ≈ 3300 cells per
    bucket / 10 codes ≈ 330 cells per (l1, l2) pair, level-2 codebooks have
    enough data to train cleanly.

    Level-2 codebooks have kmeans_init disabled and dead-code resampling
    disabled (per-bucket data is too sparse to cluster reliably and we don't
    want to spuriously revive level-2 codes in small buckets).
    """
    cfg["model"]["encoder_params"]["vq_params"] = {
        "vq_name": "ConditionalVQ",
        "codebook_size_l1": int(k1),
        "codebook_size_l2": int(k2),
        "use_cosine_sim": True,
        "ema_update": True,
        "decay": 0.8,
        "eps": 0.00001,
        "threshold_ema_dead_code": 2,
        "kmeans_init": True,
        "kmeans_iters": 10,
        "sync_kmeans": True,
        "commitment_weight": 0.0,
        "sample_codebook_temp": 0.0,
    }
    return cfg


# ---- NicheCompass-style adjacency reconstruction ---------------------------

def _patch_nichecompass_adj(cfg: dict, weight: float = 1.0) -> dict:
    """
    Switch the BCE adjacency reconstruction to the NicheCompass / standard
    graph-VAE formulation, and weight it on par with the gene-expression NB
    loss (default `weight=1.0`).

    Two changes vs. `_patch_adj`:
      (a) `bypass_adj_decoder=True` makes the BCE compute its edge logits
          directly on the quantized latent z_q (raw inner product
          z_q_i^T z_q_j), bypassing the SQUINT adjacency_decoder MLP. This
          mirrors NicheCompass's `A_hat = sigmoid(Z @ Z^T)` formulation —
          there is no extra non-linearity between the latent and the
          edge prediction. The codebook embeddings themselves are forced
          to encode pairwise spatial proximity.
      (b) `wt_adj_reconstr=1.0` (vs. the legacy default 0.1) gives the
          edge reconstruction roughly the same loss-magnitude footprint as
          NB attribute reconstruction. The NicheCompass user guide
          explicitly notes that "finding a balance between gene expression
          and edge reconstruction is a key element for good niche
          identification" — the default 0.1 weight in SQUINT was 10x too
          weak for a niche-focused signal.

    Adds `bce_adjacency_reconstruction_loss` to the loss list if not
    already present.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "bce_adjacency_reconstruction_loss" not in losses:
        losses.append("bce_adjacency_reconstruction_loss")
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["wt_adj_reconstr"]    = float(weight)
    lk["bypass_adj_decoder"] = True
    return cfg


# ---- GATv2 encoder (NicheCompass-style GNN) --------------------------------

def _patch_gatv2_encoder(cfg: dict) -> dict:
    """
    Switch the encoder GNN from GraphSAGE to GATv2 (dynamic attention).
    NicheCompass recommends GATv2 over GCN/GraphSAGE for niche-focused
    representations — attention weights are spatially adaptive, so cells
    in dense parts of a niche aggregate from more informative neighbours
    than cells at niche boundaries.

    The MLP / hidden-channel / depth structure is left unchanged — this
    patch only flips `gnn_name`. Use it stacked with a recon patch and
    `_patch_nichecompass_adj` to get a NicheCompass-shaped pipeline.
    """
    cfg["model"]["encoder_params"]["gnn_name"] = "GATv2Conv"
    return cfg


# ---------------------------------------------------------------------------
# 2c. Variant registry
# ---------------------------------------------------------------------------
# Naming convention: <recon-mode>[+<component>][+<component>...]
#   recon-cell   per-cell NB only
#   recon-nbr    neighbourhood NB only
#   recon-both   per-cell + neighbourhood NB
#   +adj         + adjacency reconstruction
#   +film        + FiLM conditioning
#   +mask        + MAE-style masking
#   +mhvq        + multi-head VQ
#   full         every component active
# ---------------------------------------------------------------------------

def _B():
    """Shorthand: deep-copy of the base POC config."""
    return _copy(make_train_config_poc())


# ---------------------------------------------------------------------------
# 2c. VQNiche_Dual: dual-codebook config builder
# ---------------------------------------------------------------------------
# The dual model (`VQNiche_Dual`) has a separate cell and niche codebook + a
# separate decoder for each branch. The config below is built by starting
# from the single-codebook POC config and then:
#   - replacing model_name/encoder_name with the dual classes
#   - replacing `vq_params` with `vq_cell_params` + `vq_niche_params`
#   - replacing `attribute_decoder_params` with `attribute_decoder_cell_params`
#     + `attribute_decoder_niche_params`
#   - replacing `loss_names` with the dual loss list
#   - dropping `adjacency_decoder_params` (the dual model uses a direct
#     cosine-similarity adjacency BCE on z_q_niche, so there is no MLP
#     adjacency decoder)
# ---------------------------------------------------------------------------

def _default_vq_params(codebook_size: int = 30) -> dict:
    """
    Single-codebook (non-hierarchical) VectorQuantize defaults, identical
    settings as the POC config so cell- and niche-branches start from a
    matched VQ baseline. Swap in `_default_rvq_params(...)` for hierarchical.
    """
    return {
        "vq_name": "VectorQuantize",
        "freeze_codebook": False,
        "use_cosine_sim": True,
        "ema_update": True,
        "manual_ema_update": False,
        "threshold_ema_dead_code": 2,
        "manual_in_place_optimizer_update": False,
        "learnable_codebook": False,
        "codebook_size": int(codebook_size),
        "heads": 1,
        "separate_codebook_per_head": False,
        "decay": 0.8,
        "eps": 0.00001,
        "kmeans_init": True,
        "kmeans_iters": 10,
        "sync_kmeans": True,
        "sample_codebook_temp": 0.0,
        "commitment_weight": 0.0,
        "commitment_use_cross_entropy_loss": False,
        "codebook_diversity_loss_weight": 0.0,
        "codebook_diversity_temperature": 100.0,
        "orthogonal_reg_weight": 0.0,
        "orthogonal_reg_max_codes": None,
        "orthogonal_reg_active_codes_only": False,
    }


def _default_rvq_params(codebook_sizes=(30, 200)) -> dict:
    """Residual-VQ defaults (hierarchical). Plug into vq_cell_params or vq_niche_params."""
    return {
        "vq_name": "ResidualVQ_Squint",
        "num_quantizers": len(codebook_sizes),
        "codebook_size": list(codebook_sizes),
        "use_cosine_sim": True,
        "ema_update": True,
        "decay": 0.8,
        "eps": 0.00001,
        "threshold_ema_dead_code": 2,
        "kmeans_init": True,
        "kmeans_iters": 10,
        "sync_kmeans": True,
        "commitment_weight": 0.0,
        "sample_codebook_temp": 0.0,
        # Codebook-diversity loss (lucidrains feature) — propagated to
        # EVERY inner VectorQuantize level. 0.0 = legacy off. >0 adds a
        # softmax-similarity repulsion that pushes codes apart on the
        # embedding sphere (SwAV-style prototype repulsion). Use the
        # `_patch_dual_codebook_diversity` patch to enable.
        "codebook_diversity_loss_weight": 0.0,
        "codebook_diversity_temperature": 100.0,
    }


def _default_cvq_params(
        k1: int = 30,
        k2: int = 10,
        k3: Optional[int] = None,
    ) -> dict:
    """Conditional / tree VQ defaults. Plug into vq_cell_params or vq_niche_params.

    `k3=None` (default) gives the legacy 2-level CVQ (K1 macro buckets,
    K2 sub-children each — K1 × K2 distinct codes). Passing an integer
    `k3` activates 3-level CVQ: K1 × K2 × K3 distinct codes, with one
    leaf VQ of size K3 per (l1, l2) bucket. The 3-level pass is wired
    inside `ConditionalVQ.forward` and exposes `num_quantizers=3` so
    downstream codebook-utilisation metrics stratify all three levels.
    """
    out = {
        "vq_name": "ConditionalVQ",
        "codebook_size_l1": int(k1),
        "codebook_size_l2": int(k2),
        "use_cosine_sim": True,
        "ema_update": True,
        "decay": 0.8,
        "eps": 0.00001,
        "threshold_ema_dead_code": 2,
        "kmeans_init": True,
        "kmeans_iters": 10,
        "sync_kmeans": True,
        "commitment_weight": 0.0,
        "sample_codebook_temp": 0.0,
    }
    if k3 is not None:
        # ConditionalVQ.__init__ accepts `codebook_size_l3` as an
        # optional kwarg (None -> legacy 2-level). Setting it here
        # activates the 3-level pass.
        out["codebook_size_l3"] = int(k3)
    return out


def make_train_config_dualvq() -> dict:
    """
    Build the base config for VQNiche_Dual.

    Defaults (per design D1–D6):
      - encoder: shared MLP [400, 256], GraphSAGE GNN with hidden=256,
        num_layers=1 (matches POC config)
      - vq_cell  = VectorQuantize(k=30) on the post-MLP latent
      - vq_niche = VectorQuantize(k=30) on the post-GNN latent
      - cell decoder + niche decoder are independent MLPSoftmax with
        hidden_channels=[400, 400] each
      - losses (all weights = 1.0):
          nb_attribute_reconstruction_loss          (cell branch NB)
          nb_attribute_reconstruction_loss_nbr      (niche branch NB,
                                                     aggregated post-decoder)
          mse_commit_loss_cell
          mse_commit_loss_niche
          bce_cosine_adjacency_reconstruction_loss  (on z_q_niche, cosine sim,
                                                     NicheCompass-style)
      - adjacency BCE uses cosine_temperature=0.1, weight=250 (heavily
        upweighted to balance against gene-NB ~150 nats — empirical sweet
        spot on the MERFISH/STARmap MB panel)
      - cosine-similarity adjacency operates on z_gnn (continuous niche
        embedding, NicheCompass-faithful) by default; switch via
        `loss_kwargs['adj_loss_input'] = 'z_q_niche'` to use the
        quantized version
      - the BCE input tensor is the FULL batch (including sampled
        neighbours) so negative-edge sampling has enough nodes
    """
    cfg = make_train_config_poc()

    # --- model class names ------------------------------------------------
    cfg["model"]["model_name"]   = "VQNiche_Dual"
    cfg["model"]["encoder_name"] = "VQNiche_Dual_Encoder"
    cfg["model"]["adjacency_decoder_name"] = None   # not used

    # --- encoder: replace single vq_params with two slots ----------------
    enc = cfg["model"]["encoder_params"]
    enc["vq_cell_params"]  = _default_vq_params(codebook_size=30)
    enc["vq_niche_params"] = _default_vq_params(codebook_size=30)
    enc.pop("vq_params", None)   # legacy single-codebook key — removed

    # --- two attribute decoders ------------------------------------------
    cfg["model"]["attribute_decoder_cell_params"]  = _copy(cfg["model"]["attribute_decoder_params"])
    cfg["model"]["attribute_decoder_niche_params"] = _copy(cfg["model"]["attribute_decoder_params"])
    cfg["model"].pop("attribute_decoder_params", None)
    cfg["model"].pop("adjacency_decoder_params",  None)

    # --- losses & weights -------------------------------------------------
    # Niche NB uses the *_dual variant so it reads `dispersion_niche` instead
    # of `dispersion` — i.e. the niche branch has its own per-gene NB
    # dispersion, decoupled from the cell branch's dispersion. This is
    # essential because the per-cell counts (cell branch) and the
    # neighbourhood-mean counts (niche branch) have very different variance
    # structure; sharing the dispersion forces a compromise that makes the
    # cell-branch NB loss drift up over training.
    cfg["model"]["loss_params"]["loss_names"] = [
        "nb_attribute_reconstruction_loss",            # cell branch
        "nb_attribute_reconstruction_loss_nbr_dual",   # niche branch (separate theta)
        "mse_commit_loss_cell",
        "mse_commit_loss_niche",
        "bce_cosine_adjacency_reconstruction_loss",    # on z_q_niche
    ]
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    # Drop legacy single-codebook flags that don't apply.
    for k in ("recon_mode", "k_hop_nb_loss", "bypass_adj_decoder",
              "estimate_adj_kwargs"):
        lk.pop(k, None)
    lk.update({
        # Two primary reconstruction objectives at parity. Both are
        # NB log-likelihood summed over 431 genes -> magnitudes ~150 nats.
        "wt_attr_reconstr":     1.0,    # NB cell
        "wt_attr_reconstr_nbr": 1.0,    # NB nbr
        # VQ-VAE commit losses are SOFT regularisers, not reconstruction
        # objectives. Original VQ-VAE paper (van den Oord 2017) uses
        # beta=0.25 and notes "results are robust to beta". We default to
        # 1.0 (slightly tighter than the paper's 0.25) but importantly NOT
        # match-magnitude with NB — that would over-constrain the encoder.
        # See response notes: equal-magnitude weighting is NOT recommended
        # in VQ-VAE literature; commit terms are intentionally smaller.
        "wt_commit_cell":       1.0,    # commit_cell
        "wt_commit_niche":      1.0,    # commit_niche
        # Adjacency BCE has small absolute magnitude (~0.5-1 nat at init,
        # bounded by log(2)) — it must be UPWEIGHTED to balance against
        # the gene-NB losses (~150 nats). Empirically `wt_adj_reconstr=1000`
        # gives the strongest spatial-niche coherence on the MERFISH/STARmap
        # MB panel without measurably hurting NB Pearson once the wide
        # codebook setup is used (k=50 macro + RVQ refinement). The
        # earlier "Pearson drop after epoch 1" symptom turned out to be
        # the codebook-size bottleneck (NB→niche-mean predictor pathology),
        # NOT the adj weight — once that's fixed via wide+RVQ, the model
        # tolerates much higher adjacency pressure.
        "wt_adj_reconstr":      1000.0,  # cosine adj BCE (1.0 -> 100 -> 25 -> 250 -> 1000)
        "edge_sampling_ratio":  2.0,
        "use_pos_weight":       True,
        "cosine_temperature":   0.1,
        # Which embedding the adjacency BCE operates on:
        #   "z_gnn"     -> continuous, NicheCompass-faithful (default)
        #   "z_q_niche" -> quantized (opt-in via _patch_dual_adj_on_zqniche)
        # See `bce_cosine_adjacency_reconstruction_loss` for the rationale.
        "adj_loss_input":       "z_gnn",
        # K-hop aggregation radius for the niche reconstruction target.
        #   1 -> mean over the cell's 1-hop spatial neighbourhood (default,
        #        legacy behaviour).
        #   2 -> mean over the 2-hop neighbourhood. Smoother targets ->
        #        the niche codebook is forced to capture larger-scale
        #        spatial structure rather than 1-hop variation. Requires
        #        the datamodule sampler to sample at least 2-hop neighbours
        #        (`num_neighbors=[8, 8]` or deeper) to be exact.
        "nbr_aggregation_hops": 1,
    })

    # The dual model doesn't yet support recon_mode-style modes (it always
    # reconstructs both branches), but downstream code reads `recon_mode`
    # from loss_kwargs in some places — set it to a no-op marker.
    lk["recon_mode"]  = "dual"

    # Monitor metric: training-loss based (as set up by the early-stopping
    # patch). Pearson-based monitors still work; default to log1p-cell since
    # the dual model produces a meaningful per-cell X_hat.
    cfg["trainer"]["monitor"] = "val_loss"

    return cfg


def _BD():
    """Shorthand: deep-copy of the dual-VQ base config."""
    return _copy(make_train_config_dualvq())


# ---------------------------------------------------------------------------
# 2d. VQNiche_Dual ablation patches
# ---------------------------------------------------------------------------

def _patch_dual_no_adj(cfg: dict) -> dict:
    """Drop the adjacency BCE loss term (test if it's load-bearing)."""
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "bce_cosine_adjacency_reconstruction_loss" in losses:
        losses.remove("bce_cosine_adjacency_reconstruction_loss")
    return cfg


def _patch_dual_no_cell_recon(cfg: dict) -> dict:
    """
    Drop the cell-branch NB loss + cell commit loss (test the niche-only
    setup; the cell decoder still runs but receives no gradient signal).
    Useful as a ceiling test for whether the cell branch helps the niche
    branch via the shared MLP trunk.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in ("nb_attribute_reconstruction_loss", "mse_commit_loss_cell"):
        if ln in losses:
            losses.remove(ln)
    return cfg


def _patch_dual_no_spatial(cfg: dict) -> dict:
    """
    Drop EVERY spatial-supervision loss term so the model trains as a
    pure cell-branch VQ-VAE + (whatever batch-integration loss is set).
    Removes:
      - nb_attribute_reconstruction_loss_nbr_dual  (niche-branch NB recon)
      - mse_commit_loss_niche                       (niche VQ commit)
      - bce_cosine_adjacency_reconstruction_loss    (adjacency BCE)

    The niche-side modules (GNN, vq_niche, niche decoder, adjacency
    decoder) are still instantiated by the model and run forward, but
    receive zero gradient signal — they're effectively idle. This is
    the DIAGNOSTIC variant for the hypothesis "spatial losses are
    pulling the cell encoder away from cell-type-discriminative features".
    Under this patch, training conditions match scVI / Harmony shape:
    pure cell-NB + cell-commit + adversarial/MMD batch integration.

    Sibling of `_patch_dual_no_adj` (drops only adjacency) and
    `_patch_dual_no_cell_recon` (drops only cell-side). When all three
    are needed together for ablations, apply this LAST so the niche
    losses get removed regardless of order.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in (
        "nb_attribute_reconstruction_loss_nbr_dual",
        "nb_attribute_reconstruction_loss_nbr",   # legacy single-codebook name
        "mse_commit_loss_niche",
        "bce_cosine_adjacency_reconstruction_loss",
        # The vanilla (non-dual) adjacency-BCE name, in case earlier patches
        # left it in the loss list.
        "bce_adjacency_reconstruction_loss",
    ):
        if ln in losses:
            losses.remove(ln)
    return cfg


def _patch_dual_no_cell_nb(cfg: dict) -> dict:
    """
    Drop ONLY the cell-branch NB reconstruction loss
    (`nb_attribute_reconstruction_loss`). The cell commit loss
    (`mse_commit_loss_cell`) STAYS — so the cell-VQ still commits to
    its codebook and EMA updates continue normally.

    Difference from `_patch_dual_no_cell_recon`
    ------------------------------------------
    `_patch_dual_no_cell_recon` removes BOTH the cell-NB and the cell-
    commit loss; under that patch the cell-VQ has no gradient signal
    at all (encoder isn't pulled toward codebook, codebook still
    updates via EMA from whatever z_mlp happens to be). This patch
    is more surgical: it removes ONLY the cell-NB while keeping the
    encoder→nearest-code commit pressure intact.

    Intended use: diagnostic when paired with the LEGACY SHARED-MLP
    trunk (i.e. NO `+decoupled-enc`). Under that configuration
    `z_mlp` is shaped by:
      - cell-commit  (pulls z_mlp toward nearest cell codebook entry)
      - nbr-NB       (via the same shared trunk, fed into the GNN)
      - niche-commit (via the GNN path)
      - adjacency-BCE (on z_gnn, niche-side)

    If `cell_code_indices[level_0]` NMI is still high under this
    setting, the spatial losses on the shared trunk are doing the
    cell-type discrimination — the cell-NB term was only ever
    decorating a partition that the niche-side gradients already
    aligned with cell types via the shared `z_mlp`. If it crashes,
    cell-NB was load-bearing for cell-NMI.

    Sibling of `_patch_dual_no_cell_recon` (drops both terms) and
    `_patch_dual_no_recon` (drops every reconstruction term, keeps
    both commits).
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "nb_attribute_reconstruction_loss" in losses:
        losses.remove("nb_attribute_reconstruction_loss")
    return cfg


def _patch_dual_no_recon(cfg: dict) -> dict:
    """
    Drop ALL reconstruction losses (cell-NB + nbr-NB + adjacency-BCE)
    while keeping the commit losses alive. The encoder still receives
    gradient signal — but ONLY from the VQ commit term that pulls
    `z_mlp` toward its nearest codebook entry. The codebook still
    updates via EMA from whatever `z_mlp` samples are assigned to
    each code.

    Intended use: a NEGATIVE TEST for the architectural-leakage
    hypothesis.

    Reasoning
    ---------
    After s41–s43 the architecture is strictly decoupled (no parameter
    shared between the cell and niche branches downstream of the input
    tensor). If cell-NMI did NOT improve under that decoupling, the
    leakage hypothesis is wrong and the cell-NMI ceiling is set by one
    of:
      (1) codebook K₁ is too small vs cell-type cardinality,
      (2) the NB-reconstruction objective doesn't isolate cell-type
          identity (it also encodes state / depth / within-type
          variation),
      (3) k-means init on the *raw* z_mlp dominates, with EMA unable
          to escape the basin.

    Stripping reconstruction discriminates (3) from (1+2):
      - cell-NMI similar to the best s43 run -> structure is driven by
        k-means init / data geometry, NOT reconstruction. The codebook
        finds the same partition with or without NB pressure.
      - cell-NMI crashes to baseline -> reconstruction IS the driver,
        we're at its resolution ceiling, and the next move is K₁ ↑ or
        a fundamentally different objective (contrastive, code-
        orthogonality, etc.).

    What stays alive
    ----------------
      - mse_commit_loss_cell   (encoder -> nearest cell codebook entry)
      - mse_commit_loss_niche  (z_gnn -> nearest niche codebook entry)
      - EMA codebook updates (these are NOT gradient-based — they fire
        every step regardless of which losses are in the loss list)

    What goes
    ---------
      - nb_attribute_reconstruction_loss              (cell NB)
      - nb_attribute_reconstruction_loss_nbr_dual     (nbr NB)
      - nb_attribute_reconstruction_loss_nbr          (legacy single-codebook nbr NB)
      - bce_cosine_adjacency_reconstruction_loss      (adjacency BCE on z_gnn)
      - bce_adjacency_reconstruction_loss             (legacy single-codebook adj-BCE)

    Apply LAST in the patch chain so no later patch re-adds a recon
    loss name (none of the current patches do this, but conventional
    safety).
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in (
        "nb_attribute_reconstruction_loss",
        "nb_attribute_reconstruction_loss_nbr_dual",
        "nb_attribute_reconstruction_loss_nbr",
        "bce_cosine_adjacency_reconstruction_loss",
        "bce_adjacency_reconstruction_loss",
    ):
        if ln in losses:
            losses.remove(ln)
    return cfg


def _patch_dual_batch_lr(
        cfg: dict,
        batch_size: Optional[int] = None,
        lr: Optional[float] = None,
    ) -> dict:
    """
    Override `loader_params.batch_size` and/or `optimizer_params.lr` in
    the resolved config. Designed for ablation sweeps that BAKE a
    specific (batch, lr) pair into a variant key — different from the
    multiseed `--batch-size` / `--lr` CLI flags, which override at
    submission time without changing the variant identity.

    Either kwarg can be None to leave that knob untouched.

    Recommended pairings (matches the docstring on `submit_multi_seed.sh`):

      batch_size  | sqrt-scaled lr | linear-scaled lr
      ------------+----------------+------------------
       256 (def)  | 5e-4           | 5e-4
       512        | ~7e-4          | 1e-3
      1024        | 1e-3           | 2e-3
      2048        | ~1.4e-3        | 4e-3
      4096        | 2e-3           | 8e-3

    Sqrt-scaling is the safer starting point — linear can saturate the
    adversarial discriminator and collapse the encoder at 8x-16x batch.
    """
    if batch_size is not None:
        cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    if lr is not None:
        cfg["model"]["optimizer_params"]["lr"] = float(lr)
    return cfg


def _patch_dual_no_batch_int(cfg: dict) -> dict:
    """
    Drop EVERY batch-integration loss (adversarial CE + MMD-batch +
    MMD-prior). The cell-side encoder + decoders + VQ codebooks still
    train normally; no explicit batch-correction signal is applied.
    Useful for testing whether SQUINT's per-batch decoder-covariate
    (`+decoder-cov`) is enough to handle batch differences on its own,
    without an encoder-side correction.

    Removes:
      - `adversarial_batch_loss`               (current adv-GRL name)
      - `adversarial_cross_entropy_batch_loss` (legacy adv-GRL name)
      - `mmd_batch_loss`                       (pairwise per-batch MMD)
      - `mmd_prior_loss`                       (MMD against isotropic prior)
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in (
        "adversarial_batch_loss",
        "adversarial_cross_entropy_batch_loss",
        "mmd_batch_loss",
        "mmd_prior_loss",
    ):
        if ln in losses:
            losses.remove(ln)
    # Also disable the adversarial classifier head + GRL alpha so the
    # head isn't built into the model graph at all (otherwise it
    # consumes optimizer state and forward-pass time for nothing).
    cfg["model"]["adversarial_batch_dim_request"] = False
    cfg["model"]["adversarial_alpha"] = 0.0
    return cfg


def _patch_dual_no_encoder_film(cfg: dict) -> dict:
    """
    Disable the ENCODER-SIDE FiLM (`conditioning_module`) by removing
    its `condition_list`. The decoders' FiLM modules are left alive —
    each decoder owns its own FiLM instance — and `+decoder-cov` still
    threads the batch one-hot through the decoder inputs.

    Why this matters specifically for `+decoupled-enc`
    --------------------------------------------------
    The encoder's `conditioning_module` is a SINGLE FiLM instance that
    is called on BOTH `z_mlp_cell_path` and `z_mlp_niche_path` (see
    `vqniche_dual_encoder.py`, lines ~382 + ~410). Its affine MLP
    therefore receives gradients from cell-side losses AND every
    spatial loss in the niche path. Even when the cell and niche MLP
    trunks are fully decoupled via `niche_mlp_params`, the FiLM update
    driven by spatial gradients re-enters the cell path on the next
    forward — a backdoor that defeats the point of decoupling.

    This patch shuts that backdoor by removing `condition_list` from
    `encoder_params.conditioning_params`. The encoder build then takes
    the `else` branch at `vqniche_dual_encoder.py` line 309 and sets
    `self.conditioning_module = None`, so no FiLM module is ever
    instantiated on the encoder side.

    Use this WITH `+decoupled-enc` (or `+shared-specific-enc`) to get
    the strict architectural decoupling you intuited; using it on a
    legacy single-trunk variant just drops FiLM with no other effect.

    No-op if the cfg has no encoder `conditioning_params` to begin
    with.
    """
    enc = cfg["model"]["encoder_params"]
    cp = enc.get("conditioning_params")
    if not cp:
        return cfg
    # Remove just `condition_list` (the key the encoder checks at build
    # time, `vqniche_dual_encoder.py:300`). Keep the rest of the dict
    # so any downstream consumer that snapshots the cfg sees a sensible
    # record of "FiLM was configured but disabled".
    cp.pop("condition_list", None)
    return cfg


def _patch_dual_niche_neck(
        cfg: dict,
        hidden_channels: Optional[List[int]] = None,
        dropout: float = 0.0,
        norm: Optional[str] = None,
    ) -> dict:
    """
    Insert a niche-side neck MLP between `z_mlp` and the GNN. The
    cell-VQ still consumes `z_mlp` directly; only the GNN input is
    routed through this extra MLP. See the `VQNiche_Dual_Encoder`
    docstring for the schematic.

    Why this might help:
      - Currently the cell branch shares `z_mlp` with the niche-side
        GNN input. Niche-side gradients (adjacency BCE, nbr-NB,
        niche-commit) all flow back into `z_mlp`, which can pull the
        cell quantiser away from cell-type-discriminative geometry.
      - Inserting a learned linear transformation between `z_mlp` and
        the GNN lets the niche-side gradients shape that new tensor
        independently, leaving `z_mlp` (and therefore the cell-VQ
        input) cleaner.

    Default `hidden_channels=[256]` → a single 256-d projection layer
    that preserves the wide-spine latent dim. Pass `[128]` etc. for a
    smaller / different output dim (the GNN will be sized accordingly).
    """
    target = list(hidden_channels) if hidden_channels else [256]
    cfg["model"]["encoder_params"]["niche_neck_params"] = {
        "hidden_channels": target,
        "dropout":          float(dropout),
        "act":              "relu",
        "norm":             norm,
    }
    return cfg


def _patch_dual_decoupled_encoders(
        cfg: dict,
        niche_mlp_params: Optional[dict] = None,
    ) -> dict:
    """
    Decouple the niche-branch encoder from the cell-branch encoder by
    building a SEPARATE niche-side MLP module (independent weights).

    Default architecture (legacy, when this patch is NOT applied):

        x  ─►  shared MLP  ─►  z_mlp ─┬─► VQ_cell  (cell branch)
                                       └─► [niche_neck?] ─► GNN ─► VQ_niche

    Spatial-loss gradients (adjacency BCE, nbr-NB, niche-commit) flow
    through the GNN, into `z_mlp`, and update the SHARED MLP's weights
    — which ALSO produces the input to VQ_cell. The cell-VQ input is
    therefore gradient-coupled to the spatial losses and the cell
    codebook can end up encoding spatial / neighbourhood information
    rather than purely cell-intrinsic signal.

    After this patch:

        x ─┬─► MLP_cell   ─► z_mlp_cell  ─► VQ_cell  (cell branch)
           │
           └─► MLP_niche  ─► z_mlp_niche ─► [niche_neck?] ─► GNN ─► VQ_niche

    `MLP_cell` and `MLP_niche` have INDEPENDENT weights. Spatial-loss
    gradients update `MLP_niche.weight` only — the cell-side MLP is
    structurally shielded.

    Parameters
    ----------
    niche_mlp_params : dict, optional
        Architecture for the new niche-side MLP. When `None` (the
        default), uses a deep copy of `cfg['model']['encoder_params']
        ['mlp_params']` — i.e. the niche MLP has the same shape as
        the cell-side MLP, just independent weights. Pass an explicit
        dict to give the niche side a different architecture.
        IMPORTANT: keep `niche_mlp_params['hidden_channels'][-1]`
        equal to the cell-side `mlp_params['hidden_channels'][-1]`
        if FiLM batch correction is enabled (FiLM shares one module
        across both paths).

    Use cases
    ---------
      - Cell-NMI is dragged down by spatial information bleeding into
        the cell codes (the canonical scenario for this knob).
      - You want a fair test of whether the dual-VQ architecture's
        cell branch is intrinsically cell-type-discriminative — this
        is the cleanest "cell branch sees no spatial gradient" setup.

    Caveats
    -------
      - Doubles the encoder MLP parameter count (roughly).
      - Niche-side per-sample compute roughly doubles too (one extra
        MLP pass per training step).
      - Compatible with `_patch_dual_niche_neck` — the niche_neck is
        applied AFTER the new niche MLP, so the full niche path is
        `x -> MLP_niche -> niche_neck -> GNN`.
    """
    enc = cfg["model"]["encoder_params"]
    if niche_mlp_params is None:
        niche_mlp_params = _copy(enc.get("mlp_params", {})) or {}
    enc["niche_mlp_params"] = niche_mlp_params
    return cfg


def _patch_dual_coupled_encoders(cfg: dict) -> dict:
    """Force the COUPLED (shared-trunk) encoder — the INVERSE of
    `_patch_dual_decoupled_encoders` / `_patch_dual_shared_specific_encoders`.
    Removes the separate niche-side MLP (`niche_mlp_params`) and any Y-shape
    shared trunk (`shared_mlp_params`) so the cell and niche branches share a
    single MLP trunk (the legacy default). Spatial-loss gradients then flow back
    into the cell-VQ input — the coupling this ablation tests. Apply OUTERMOST on
    a build that has decoupled / shared-specific encoders (e.g. the s55_v3 spine).
    """
    enc = cfg["model"]["encoder_params"]
    enc.pop("niche_mlp_params", None)
    enc.pop("shared_mlp_params", None)
    return cfg


def _patch_dual_shared_specific_encoders(
        cfg: dict,
        shared_mlp_params: Optional[dict] = None,
        path_mlp_params: Optional[dict] = None,
        shared_last_dim: Optional[int] = None,
        path_last_dim: Optional[int] = None,
    ) -> dict:
    """
    Y-shape encoder architecture — one SHARED MLP + one PATH-SPECIFIC
    MLP per branch, with the shared output CONCATENATED into each
    path's downstream input.

    Architecture:

        x ─┬─►  MLP_shared  ─►  z_shared ────────────┐
           │                                         │ concat
           ├─►  MLP_cell    ─►  z_path_cell    ─► z_mlp_cell ─► VQ_cell
           │
           └─►  MLP_niche   ─►  z_path_niche   ─► z_mlp_niche ─► GNN ─► VQ_niche
                                                  ▲
                                          (concat with z_shared)

    Gradient routing:
      - Shared MLP receives gradients from BOTH branches (cell-NB +
        cell-commit AND spatial-adj + nbr-NB + niche-commit).
        Feature reuse happens here.
      - Cell-path MLP receives ONLY cell-side gradients (no spatial
        leakage).
      - Niche-path MLP receives ONLY niche-side gradients.

    Compared to the existing encoder modes on this codebase:
      - Legacy (only `mlp_params` in cfg): single shared MLP — full
        spatial-gradient leakage into the cell-VQ input.
      - Decoupled (`_patch_dual_decoupled_encoders`): TWO independent
        MLPs — full gradient isolation but no feature reuse.
      - Y-shape (this helper): THREE MLPs — shared core + two
        specialised paths. The cell-path MLP weights are insulated
        from spatial gradients; the shared trunk's weights are NOT
        (they see gradients from both branches).

    Default sizing
    --------------
    By default the helper HALVES the last layer dim of the cfg's
    `mlp_params` and uses that for all three MLPs:

        mlp_params (in cfg) = {hidden_channels: [400, 400, 256], ...}
                            ↓
        shared_mlp_params   = {hidden_channels: [400, 400, 128], ...}
        path_mlp_params     = {hidden_channels: [400, 400, 128], ...}

    Effective downstream dims stay at the original 256:
      - VQ_cell input  = 128 (shared) + 128 (cell-path) = 256
      - GNN input      = 128 (shared) + 128 (niche-path) = 256

    so codebook embedding dims, GNN config, and decoder shapes are
    UNCHANGED relative to the legacy shared-trunk variant.

    Parameters
    ----------
    shared_mlp_params : dict, optional
        Architecture for the shared trunk. `None` -> half-dim deep
        copy of `cfg['model']['encoder_params']['mlp_params']`.
    path_mlp_params : dict, optional
        Architecture for EACH of the two path-specific MLPs (cell
        and niche). The two paths share the SHAPE (this dict) but
        get INDEPENDENT WEIGHTS. `None` -> half-dim deep copy of
        the cfg's `mlp_params`.

    Use cases
    ---------
      - Cell-NMI is dragged by spatial information leaking into z_mlp
        but you want SOME shared feature reuse for parameter
        efficiency. This is the balanced fix.
      - You expect the cell + niche branches to share low-level
        features (e.g. raw gene-expression pattern recognition) but
        diverge in the higher-level abstraction toward each branch's
        own discrimination target.

    Caveats
    -------
      - Increases the encoder MLP parameter count by ~50% (three
        MLPs vs one). Compute scales similarly.
      - The shared trunk is STILL gradient-coupled to spatial losses.
        If your goal is FULL gradient isolation of the cell branch
        from spatial pulls, use `_patch_dual_decoupled_encoders`
        instead.
    """
    import copy as _cp
    enc = cfg["model"]["encoder_params"]
    base_mlp = _cp.deepcopy(enc.get("mlp_params", {}))

    def _halve_last(mlp_cfg: dict) -> dict:
        """Return a deep copy of `mlp_cfg` with its last hidden
        layer dim halved. No-op if `hidden_channels` is absent."""
        out = _cp.deepcopy(mlp_cfg)
        h = out.get("hidden_channels")
        if h:
            h = list(h)
            h[-1] = max(1, h[-1] // 2)
            out["hidden_channels"] = h
        return out

    def _override_last(mlp_cfg: dict, last_dim: int) -> dict:
        """Return a deep copy of `mlp_cfg` with its last hidden layer
        dim replaced by `last_dim`. No-op if `hidden_channels` is
        absent."""
        out = _cp.deepcopy(mlp_cfg)
        h = out.get("hidden_channels")
        if h:
            h = list(h)
            h[-1] = int(last_dim)
            out["hidden_channels"] = h
        return out

    if shared_mlp_params is None:
        shared_mlp_params = _halve_last(base_mlp)
    if path_mlp_params is None:
        path_mlp_params = _halve_last(base_mlp)

    # Shortcut overrides — apply AFTER the default fallback so callers
    # can pass just `shared_last_dim=` / `path_last_dim=` to vary the
    # bottleneck while keeping the trunk shape inherited from the cfg.
    if shared_last_dim is not None:
        shared_mlp_params = _override_last(shared_mlp_params, shared_last_dim)
    if path_last_dim is not None:
        path_mlp_params = _override_last(path_mlp_params, path_last_dim)

    enc["shared_mlp_params"] = shared_mlp_params
    enc["mlp_params"] = _cp.deepcopy(path_mlp_params)       # cell-path MLP
    enc["niche_mlp_params"] = _cp.deepcopy(path_mlp_params) # niche-path MLP
    return cfg


def _patch_dual_no_nbr_recon(cfg: dict) -> dict:
    """
    Drop ONLY the neighborhood-gene-expression NB reconstruction loss
    (`nb_attribute_reconstruction_loss_nbr_dual`). The niche encoder
    still runs and is still committed to its codebook (niche-commit
    kept), and the adjacency-BCE loss is still active — so the niche
    branch retains spatial-structure supervision via the adjacency
    decoder, but no longer tries to reconstruct neighborhood gene
    expression.

    Use this together with `_patch_dual_no_adj` to form a 2x2 grid
    that isolates the contribution of each spatial loss term to
    downstream cell-type / niche-type NMI:
       adj=ON,  nbr=ON   -> baseline
       adj=OFF, nbr=ON   -> `_patch_dual_no_adj`
       adj=ON,  nbr=OFF  -> `_patch_dual_no_nbr_recon` (this)
       adj=OFF, nbr=OFF  -> `_patch_dual_no_spatial`

    Sibling of `_patch_dual_no_adj` and `_patch_dual_no_spatial`.
    Apply LAST in the patch chain so the loss removal isn't undone
    by a later patch that re-adds the loss name.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in (
        "nb_attribute_reconstruction_loss_nbr_dual",
        "nb_attribute_reconstruction_loss_nbr",   # legacy single-codebook name
    ):
        if ln in losses:
            losses.remove(ln)
    return cfg


def _patch_dual_rvq(cfg: dict,
                    branch: str = "both",
                    codebook_sizes=(30, 200)) -> dict:
    """
    Swap one or both VQ slots for ResidualVQ_Squint. `branch` may be
    'cell', 'niche', or 'both'.
    """
    if branch in ("cell", "both"):
        cfg["model"]["encoder_params"]["vq_cell_params"]  = _default_rvq_params(codebook_sizes)
    if branch in ("niche", "both"):
        cfg["model"]["encoder_params"]["vq_niche_params"] = _default_rvq_params(codebook_sizes)
    return cfg


def _patch_dual_vq(cfg: dict,
                   branch: str = "cell",
                   codebook_size: int = 30) -> dict:
    """
    Swap one or both VQ slots for a single-level (non-hierarchical)
    `VectorQuantize` with the specified codebook_size. Mirrors
    `_patch_dual_rvq` but for the single-level case — useful for v7
    ablations that hold one branch at a non-hierarchical VQ while the
    other uses RVQ. `branch` may be 'cell', 'niche', or 'both'.
    """
    if branch in ("cell", "both"):
        cfg["model"]["encoder_params"]["vq_cell_params"]  = _default_vq_params(codebook_size=codebook_size)
    if branch in ("niche", "both"):
        cfg["model"]["encoder_params"]["vq_niche_params"] = _default_vq_params(codebook_size=codebook_size)
    return cfg


def _patch_dual_cvq(cfg: dict,
                    branch: str = "both",
                    k1: int = 30,
                    k2: int = 10,
                    k3: Optional[int] = None) -> dict:
    """
    Swap one or both VQ slots for ConditionalVQ (tree-structured VQ).

    `k3=None` -> legacy 2-level CVQ (K1 macro × K2 sub-children).
    `k3=<int>` -> 3-level CVQ (K1 × K2 × K3 distinct codes), with one
    leaf VQ of size K3 per (l1, l2) bucket. The leaf forward pass and
    extra residual STE are wired inside `ConditionalVQ.forward`.
    """
    if branch in ("cell", "both"):
        cfg["model"]["encoder_params"]["vq_cell_params"]  = _default_cvq_params(k1, k2, k3)
    if branch in ("niche", "both"):
        cfg["model"]["encoder_params"]["vq_niche_params"] = _default_cvq_params(k1, k2, k3)
    return cfg


def _patch_dual_gatv2(cfg: dict) -> dict:
    """Switch the GNN encoder from SAGE to GATv2 (still 1 layer)."""
    cfg["model"]["encoder_params"]["gnn_name"] = "GATv2Conv"
    return cfg


def _patch_dual_adj_weight(cfg: dict, weight: float) -> dict:
    """Override the adjacency BCE weight (default in the dual base is 250)."""
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_adj_reconstr"] = float(weight)
    return cfg


def _patch_dual_attr_recon_weight(cfg: dict, weight: float) -> dict:
    """
    Override the CELL-branch NB attribute-reconstruction weight
    (`wt_attr_reconstr` in `loss_kwargs`; default 1.0).

    Lowering this weight loosens how tightly the cell embedding is
    anchored to per-batch gene means → opens room for batch-invariance
    signals (adversarial / MMD) to move the cell embedding around →
    can lift cell_iLISI at the cost of cell-branch NB reconstruction
    quality. Raising it does the opposite. See `_patch_dual_attr_recon_nbr_weight`
    for the niche-branch sibling.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_attr_reconstr"] = float(weight)
    return cfg


def _patch_dual_attr_recon_nbr_weight(cfg: dict, weight: float) -> dict:
    """
    Override the NICHE-branch NB attribute-reconstruction weight
    (`wt_attr_reconstr_nbr` in `loss_kwargs`; default 1.0).

    The niche NB term reconstructs the K-hop neighbourhood-mean gene
    expression from `z_q_niche`. At weight=1.0 this is the dominant
    pull on the niche embedding alongside the cosine adjacency BCE
    (`wt_adj_reconstr=1000`) — both anchor the niche embedding to
    per-batch gene statistics, which fights batch integration on the
    neighborhood embedding (low nbr_iLISI).

    Lowering this weight is the easiest way to free `z_gnn` /
    `z_q_niche` for batch integration without disabling the niche
    reconstruction entirely (which `_patch_dual_no_nbr_recon` would
    do, dropping nbr_NMI hard — see s19_v3 in the historic results).

    Use cases:
      - weight=0.3-0.5: aggressive — niche embedding can integrate
        more freely, expect nbr_iLISI to rise. Watch nbr_NMI: too
        low and niche structure collapses (like s19_v3 at weight=0).
      - weight=2.0+: tighter anchor — niche embedding stays close to
        per-batch gene means, nbr_iLISI suffers further.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_attr_reconstr_nbr"] = float(weight)
    return cfg


def _patch_dual_codebook_orth_reg(
        cfg: dict,
        wt_codebook_orthogonal_regularization: float = 0.2,
    ) -> dict:
    """
    Add `l2_codebook_orthogonal_regularization_loss` to the loss
    dispatcher, regularising the CELL-BRANCH level-1 codebook toward
    mutually-orthogonal codes.

    The loss reads `codebook_embeddings` from `loss_data` (populated
    by `VQNiche_Dual._step` to point at the cell-branch L1 codebook).
    It computes pairwise cosine similarity between codes and penalises
    high off-diagonal similarity → codes get pushed apart in the
    embedding space.

    Why this can lift cell-NMI: NMI rewards cluster separation. The
    NB-reconstruction loss alone doesn't directly constrain inter-code
    distances; it only cares that each cell's nearest code reproduces
    its expression. With small codebooks (e.g. cell L1 = 15–30), two
    codes can drift close together → cells flip ambiguously between
    them → cluster purity drops → NMI drops. Orth-reg keeps codes
    well-separated by construction.

    Parameters
    ----------
    wt_codebook_orthogonal_regularization
        Loss weight. Default 0.2; 0.1–0.5 is the useful range. The
        un-weighted loss is in [0, 1] (it's cosine-similarity-squared
        minus 1/n_codes), so wt=0.2 keeps it on a comparable scale to
        the cell-NB term (~0.5–1 nat).
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "l2_codebook_orthogonal_regularization_loss" not in losses:
        losses.append("l2_codebook_orthogonal_regularization_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_codebook_orthogonal_regularization"] = float(
        wt_codebook_orthogonal_regularization
    )
    return cfg


def _patch_dual_contrastive_cell(
        cfg: dict,
        wt_contrastive_cell: float = 1.0,
        k_pos: int = 5,
        temperature: float = 0.1,
        log_transform_gene_space: bool = True,
    ) -> dict:
    """
    Add the NT-Xent contrastive auxiliary loss on the pre-quantization
    cell-branch latent (`contrastive_cell_attribute_loss`).

    The loss reads `quantizer_input_cell` (= z_mlp_cell of seed cells)
    and `target_attr` (= raw counts of seed cells) from `loss_data`.
    For each anchor cell it picks the top-`k_pos` other cells in the
    batch with the most similar log1p gene-expression profile as
    positive pairs, then runs NT-Xent in cosine-similarity space at
    `temperature`. This pulls same-type cells together in z_mlp space
    and pushes different-type apart — an explicit between-cell
    objective that complements NB's within-cell objective.

    Rationale (per the s44 negative-test result + s46 weight-escalation
    plateau): NB reconstruction at any weight saturates cell-NMI around
    the level achievable from "data-structure k-means", because NB
    optimises per-cell rate prediction, not between-cell cluster
    separation. The contrastive loss directly optimises the latter.

    Parameters
    ----------
    wt_contrastive_cell : float
        Loss weight. 0.5-2.0 is the useful range; 1.0 matches the
        scale of cell-NB at its default weight (~0.5-1.0 nat).
    k_pos : int
        Number of positive pairs per anchor (top-k nearest neighbours
        in log1p gene-expression cosine similarity). 3-10 is sensible
        for spatial transcriptomics; 5 is a good default.
    temperature : float
        NT-Xent softmax temperature. SimCLR convention is 0.1. Smaller
        -> sharper attraction / repulsion.
    log_transform_gene_space : bool
        Whether to log1p the raw counts before computing pair
        similarities. Strongly recommended (raw counts have huge
        dynamic range; a few highly-expressed genes dominate cosine
        similarity without log1p).
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "contrastive_cell_attribute_loss" not in losses:
        losses.append("contrastive_cell_attribute_loss")
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["wt_contrastive_cell"]       = float(wt_contrastive_cell)
    lk["k_pos"]                     = int(k_pos)
    lk["temperature"]               = float(temperature)
    lk["log_transform_gene_space"]  = bool(log_transform_gene_space)
    return cfg


def _patch_dual_contrastive_cell_within_batch(
        cfg: dict,
        wt_contrastive_cell: float = 1.0,
        k_pos: int = 5,
        temperature: float = 0.1,
        log_transform_gene_space: bool = True,
    ) -> dict:
    """
    Within-batch variant of `_patch_dual_contrastive_cell`. Adds the
    `contrastive_cell_attribute_within_batch_loss` to the loss list —
    same NT-Xent formulation as the global variant, but BOTH positive-
    pair candidates AND the NT-Xent denominator are restricted to
    cells sharing the anchor's `adata_batch_id` (== same MERFISH /
    STARmap section, etc.).

    Why use this over the global variant
    ------------------------------------
    Mirrors `adj_within_section_only=True` on the cosine adjacency
    BCE. Without the restriction, the global NT-Xent denominator
    includes cross-section pairs and its gradient pushes
    biologically-similar cells from different sections APART — which
    directly counters the batch-integration objective that the
    decoder-side batch covariate (`+decoder-cov`) and the spatial-
    graph training are trying to achieve. With the restriction,
    cross-section pairs are unpressured (neither pulled together nor
    pushed apart) and the encoder is left free to learn a batch-
    invariant cluster geometry without being fought against by this
    auxiliary loss.

    Numerical correctness was validated on a 5-test suite covering
    (i) finite/non-negative loss on aligned input, (ii) combined loss
    == average of per-batch losses (no cross-batch leakage in the
    forward), (iii) singleton-batch anchors contribute 0, (iv) batch
    1's z does NOT enter batch 0 anchors' gradients, (v) hand-
    computed tiny case agrees with expected magnitude.

    Parameters (same semantics as `_patch_dual_contrastive_cell`)
    ----------
    wt_contrastive_cell : float
        Loss weight. With B=512 split across 2 sections (~256 each),
        the per-anchor contrastive term is bounded by log(256) ≈ 5.5
        nats — comparable to cell-NB at its default weight=1 mass per
        cell summed over genes (~0.5-1 nat). Practical sweep range
        spans 1-1000.
    k_pos, temperature, log_transform_gene_space :
        Identical to the global variant.

    Requires
    --------
    `loss_data['node_adata_batch_ids']` must be populated by the
    model's `_step` (it is, unconditionally, in `VQNiche_Dual._step`).
    If absent the loss raises rather than silently falling back to
    the global behaviour.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "contrastive_cell_attribute_within_batch_loss" not in losses:
        losses.append("contrastive_cell_attribute_within_batch_loss")
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["wt_contrastive_cell"]       = float(wt_contrastive_cell)
    lk["k_pos"]                     = int(k_pos)
    lk["temperature"]               = float(temperature)
    lk["log_transform_gene_space"]  = bool(log_transform_gene_space)
    return cfg


def _patch_dual_codebook_diversity(
        cfg: dict,
        weight: float = 1.0,
        temperature: float = 100.0,
        branch: str = "cell",
    ) -> dict:
    """
    Enable lucidrains' codebook-diversity loss on the VQ codebook(s).
    The loss is computed INSIDE the VQ module on every forward pass:
    it takes the cosine-similarity matrix of the codes, applies a
    softmax at `temperature`, and penalises the resulting entropy
    being far from uniform — effectively a SwAV-style repulsion that
    keeps codes spread out on the embedding sphere.

    For ResidualVQ_Squint this applies to EVERY level (L1 + L2 + ...)
    with the same weight + temperature. Per-level tuning isn't worth
    the complexity given a 2-level RVQ.

    Parameters
    ----------
    weight : float
        Diversity loss weight. 0.1-10 is the useful range; 1.0 keeps
        the term comparable in scale to the NB-cell loss (~0.5-1 nat).
        Lucidrains' implementation adds it inside the VQ module's own
        `forward` return value, where it joins the commit loss list.
    temperature : float
        Softmax temperature on the within-codebook similarity matrix
        before computing the diversity term. Larger T -> softer
        repulsion across more code pairs; smaller T -> sharper, focuses
        on the most-similar pairs. 100.0 (lucidrains default) works
        well for codebook sizes 30-200.
    branch : 'cell' | 'niche' | 'both'
        Which VQ slot(s) to apply the diversity loss to.

    Mechanism
    ---------
    Sets `codebook_diversity_loss_weight` and
    `codebook_diversity_temperature` in `vq_cell_params` /
    `vq_niche_params`. `ResidualVQ_Squint.__init__` plumbs these
    through to every inner `VectorQuantize` module.
    """
    if branch not in ("cell", "niche", "both"):
        raise ValueError(f"branch must be 'cell'/'niche'/'both', got {branch!r}")
    enc = cfg["model"]["encoder_params"]
    if branch in ("cell", "both"):
        enc["vq_cell_params"]["codebook_diversity_loss_weight"]  = float(weight)
        enc["vq_cell_params"]["codebook_diversity_temperature"]  = float(temperature)
    if branch in ("niche", "both"):
        enc["vq_niche_params"]["codebook_diversity_loss_weight"] = float(weight)
        enc["vq_niche_params"]["codebook_diversity_temperature"] = float(temperature)
    return cfg


def _patch_dual_continuous_vq(cfg: dict, branch: str = "both") -> dict:
    """
    Replace the discrete VQ bottleneck with a CONTINUOUS passthrough
    (`ContinuousVQ`) on the cell and/or niche branch.

    This is the discretization ABLATION (addresses the reviewer comment that
    "the rationale for using discrete codebooks over continuous latents is
    not sufficiently supported"). It isolates whether the *discreteness* of
    the codebook — not the encoder/decoder capacity, depth, or any other
    architectural choice — drives SQUINT's benchmark gain.

    The encoder/decoder architecture, latent dimensionality, graph, sampler,
    and every non-VQ loss are left EXACTLY as-is; only the quantizer module
    is swapped for an identity map. Concretely, with `branch="both"`:
      - z_q = z  (the continuous encoder latent flows straight to the decoder,
        no nearest-codebook snap);
      - the commitment loss `mse_commit_loss = MSE(z, z_q) == 0`;
      - the codebook-diversity term is dropped (there is no codebook).

    "Comparable capacity": the continuous latent has the SAME dimensionality
    as the VQ embedding dim (the VQ quantizes in-place, so swapping it for an
    identity map changes neither the latent dim nor the decoder input shape).
    The continuous variant therefore has >= the representational capacity of
    the discrete one — the fair, conservative comparison the reviewer asks
    for (if discrete still matches/wins despite the continuous model having
    strictly more capacity, discretization is doing real work).

    Apply this OUTERMOST in a build chain so it owns the final
    `vq_*_params`, overriding any RVQ / codebook-diversity settings set by
    inner patches. That way the continuous build can be written as the exact
    discrete build wrapped in one call — self-evidently "same architecture,
    continuous bottleneck".

    Parameters
    ----------
    branch : 'cell' | 'niche' | 'both'
        Which VQ slot(s) to make continuous. The full "no VQ-VAE" ablation
        uses 'both'.
    """
    if branch not in ("cell", "niche", "both"):
        raise ValueError(f"branch must be 'cell'/'niche'/'both', got {branch!r}")
    enc = cfg["model"]["encoder_params"]
    if branch in ("cell", "both"):
        enc["vq_cell_params"] = {"vq_name": "ContinuousVQ"}
    if branch in ("niche", "both"):
        enc["vq_niche_params"] = {"vq_name": "ContinuousVQ"}
    return cfg


def _patch_dual_contrastive_cross_batch(
        cfg: dict,
        wt_cross: float = 1.0,
        k_cross: int = 1,
        mnn_floor: float = 0.0,
        mutual: bool = True,
    ) -> dict:
    """
    Swap the within-batch contrastive cell loss for its CROSS-BATCH MNN
    variant (`contrastive_cell_attribute_cross_batch_mnn_loss`): the
    within-batch NT-Xent is kept UNCHANGED (resolution) and a cross-batch
    mutual-nearest-neighbour PURE-ATTRACTION term is added (integration) —
    it pulls the most-similar cells across sections together with NO
    negatives, so it never repels cross-section cells (the failure mode that
    makes the within-batch loss hurt iLISI/MMD).

    Apply OUTERMOST on a build that already carries the within-batch
    contrastive loss (e.g. the s49_v23 spine): the within-batch kwargs
    (wt_contrastive_cell, k_pos, temperature) are inherited and the cross
    kwargs are added. `wt_cross=0` behaves exactly like the within-batch loss.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "contrastive_cell_attribute_within_batch_loss" in losses:
        losses.remove("contrastive_cell_attribute_within_batch_loss")
    if "contrastive_cell_attribute_cross_batch_mnn_loss" not in losses:
        losses.append("contrastive_cell_attribute_cross_batch_mnn_loss")
    lk = cfg["model"]["loss_params"]["loss_kwargs"]
    lk["wt_cross"] = float(wt_cross)
    lk["k_cross"] = int(k_cross)
    lk["mnn_floor"] = float(mnn_floor)
    lk["mutual"] = bool(mutual)
    return cfg


def _patch_dual_cross_stitch(
        cfg: dict,
        per_channel: bool = False,
        init_diag: float = 0.9,
        init_off: float = 0.1,
    ) -> dict:
    """
    Cross-stitch coupling (Misra et al., CVPR 2016): learn a 2x2 mix of
    the cell- and niche-branch post-MLP latents before the cell-VQ / GNN
    split. The "soft sharing" middle ground between the coupled shared
    trunk and the fully decoupled encoders — one cross-stitch unit spans
    the whole shared-to-split spectrum and learns the optimal mix from
    data, so there is no hand-picked split point.

    Operates on the DECOUPLED architecture: this patch ensures a
    separate niche MLP exists (adds one, a copy of the cell MLP, if the
    cfg doesn't already have it) and removes any Y-shape shared trunk,
    then attaches `cross_stitch_params` for the encoder to build a
    `CrossStitch` unit. `per_channel=False` -> one 2x2 (4 scalars);
    `per_channel=True` -> an independent 2x2 per feature channel.

    Apply OUTERMOST on a decoupled spine (e.g. s55_v3).
    """
    enc = cfg["model"]["encoder_params"]
    # Require / ensure the decoupled architecture (independent niche MLP).
    if enc.get("niche_mlp_params") is None:
        enc["niche_mlp_params"] = _copy(enc.get("mlp_params", {})) or {}
    enc.pop("shared_mlp_params", None)   # cross-stitch is incompatible with Y-shape
    enc["cross_stitch_params"] = {
        "per_channel": bool(per_channel),
        "init_diag": float(init_diag),
        "init_off": float(init_off),
    }
    return cfg


def _patch_dual_stopgrad_coupled(cfg: dict) -> dict:
    """
    Gradient-level coupling (stop-gradient; cf. Yu et al. 2020 family):
    keep ONE shared MLP trunk (coupled), but DETACH the niche-path input
    so spatial-loss gradients (adjacency BCE, nbr-NB, niche-commit) never
    flow back into the trunk that also produces the cell-VQ input. The
    trunk is then trained purely by the cell objective; the GNN, niche VQ
    and niche decoder still train on top of the detached trunk features.

    This is the cheapest way to protect the cell-intrinsic codes from
    spatial corruption while physically sharing the encoder — near-zero
    parameter overhead (a single `.detach()` in the forward pass).

    Forces the coupled trunk (removes any separate niche/shared MLP) and
    sets `detach_gnn_input=True`. Apply OUTERMOST on a spine.
    """
    enc = cfg["model"]["encoder_params"]
    enc.pop("niche_mlp_params", None)
    enc.pop("shared_mlp_params", None)
    enc.pop("cross_stitch_params", None)
    enc["detach_gnn_input"] = True
    return cfg


def _patch_dual_soft_coupling(cfg: dict, wt: float = 1e-3) -> dict:
    """
    Soft parameter sharing (Duong et al. 2015; Yang & Hospedales 2017):
    keep the two encoders fully DECOUPLED (independent cell + niche MLP
    trunks) but add an L2 penalty on the distance between their matched
    weight tensors, so the two trunks are encouraged to stay similar
    without physically sharing parameters. The lightest-touch coupling
    of the decoupled architecture.

    Ensures the decoupled architecture (adds a niche MLP copy if absent)
    and sets `loss_kwargs['wt_encoder_coupling'] = wt`; the dual model
    adds `wt * sum_l ||W_cell[l] - W_niche[l]||_F^2` to the total loss.
    `wt` is small by default because the penalty sums over all MLP
    weights (large absolute magnitude). Apply OUTERMOST on a decoupled
    spine (e.g. s55_v3).
    """
    enc = cfg["model"]["encoder_params"]
    if enc.get("niche_mlp_params") is None:
        enc["niche_mlp_params"] = _copy(enc.get("mlp_params", {})) or {}
    enc.pop("shared_mlp_params", None)
    enc.pop("cross_stitch_params", None)
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_encoder_coupling"] = float(wt)
    return cfg


def _patch_dual_compositional_cell_to_niche(
        cfg: dict,
        source: str = "z_q_cell",
        detach: bool = True,
        project_dim: Optional[int] = None,
    ) -> dict:
    """
    Compositional cell->niche coupling (idea #1): inject the cell branch's
    representation as extra node features for the niche GNN, so the GNN
    aggregates neighbours' cell identity (= local cell-type composition,
    CellCharter/Banksy-style). Directional, asymmetric: with `detach=True`
    (default) the injected signal carries NO gradient back to the cell branch,
    so the cell codes stay protected while the niche branch gets strictly more
    (biologically-relevant) information.

    `source`     : 'z_q_cell' (quantized = denoised identity, default) or
                   'z_mlp_cell' (continuous pre-VQ).
    `detach`     : protect the cell branch (default True).
    `project_dim`: optionally project the cell signal before concatenation
                   (None = full concat).

    Apply OUTERMOST on a decoupled spine (e.g. s55_v3).
    """
    enc = cfg["model"]["encoder_params"]
    enc["cell_to_niche"] = {
        "source": source,
        "detach": bool(detach),
        "project_dim": (int(project_dim) if project_dim else None),
    }
    return cfg


def _patch_dual_disentangle(cfg: dict, wt: float = 100.0) -> dict:
    """
    Disentanglement penalty (idea #2): add a Barlow-Twins-style
    cross-correlation decorrelation loss between the cell and niche latents,
    pushing the niche code to carry only signal complementary to the cell
    code. Adds `disentangle_cell_niche_loss` to loss_names and sets
    `loss_kwargs['wt_disentangle']`. The penalty is the mean squared
    cross-correlation (in [0, 1]), so `wt` plays the same upweighting role as
    the adjacency BCE weight. Apply OUTERMOST on any dual spine.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "disentangle_cell_niche_loss" not in losses:
        losses.append("disentangle_cell_niche_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_disentangle"] = float(wt)
    return cfg


def _patch_dual_branch_adapter(
        cfg: dict,
        kind: str = "lora",
        rank: Optional[int] = None,
        hidden: Optional[int] = None,
    ) -> dict:
    """
    Parameter-efficient coupling: force the COUPLED (shared) trunk and add a
    small identity-initialized per-branch adapter (cell adapter before VQ_cell,
    niche adapter before the GNN). One shared trunk carries the expensive input
    layers; the tiny adapters give each branch its specialization — far fewer
    params than the two full trunks of the decoupled encoder.

    `kind`   : 'affine' (~2*dim params/branch), 'lora' (rank-r residual,
               ~2*r*dim), or 'mlp' (small residual head, ~2*dim*hidden).
    `rank`   : LoRA rank (lora only).
    `hidden` : hidden width (mlp only; defaults to dim).

    Apply OUTERMOST on a spine (e.g. s55_v3) — it removes any decoupled / Y-shape
    / cross-stitch / cell->niche encoder config so the trunk is genuinely shared.
    """
    enc = cfg["model"]["encoder_params"]
    enc.pop("niche_mlp_params", None)
    enc.pop("shared_mlp_params", None)
    enc.pop("cross_stitch_params", None)
    enc.pop("cell_to_niche", None)
    ad = {"kind": kind}
    if rank is not None:
        ad["rank"] = int(rank)
    if hidden is not None:
        ad["hidden"] = int(hidden)
    enc["branch_adapter"] = ad
    return cfg


# --- s60 cross-branch coupling patches (all dim-preserving, encoder-contained) ---
def _patch_dual_shared_token(cfg: dict, dim: int = 64, codebook_size: int = 64) -> dict:
    """[s60 #1] Additive shared-token factorization: a third shared codebook
    whose quantized token is projected and ADDED to both z_q_cell and z_q_niche
    (a single discrete 'shared' code couples the branches; each keeps its
    private code). Dim-preserving."""
    cfg["model"]["encoder_params"]["shared_token"] = {
        "dim": int(dim), "codebook_size": int(codebook_size)}
    return cfg


def _patch_dual_cross_branch_residual(cfg: dict, detach: bool = True) -> dict:
    """[s60 #2] Cross-branch residual VQ: the niche VQ quantizes z_gnn after
    subtracting a projection of the (detached) cell code, so the niche token
    encodes spatial signal beyond ego-cell identity."""
    cfg["model"]["encoder_params"]["cross_branch_residual"] = {"detach": bool(detach)}
    return cfg


def _patch_dual_shared_codebook(cfg: dict) -> dict:
    """[s60 #3] Shared codebook: both branches quantize into one codebook."""
    cfg["model"]["encoder_params"]["shared_codebook"] = True
    return cfg


def _patch_dual_cell_conditioned_niche(cfg: dict, mode: str = "bias") -> dict:
    """[s60 #4/#5] Cell-conditioned niche: the niche code is conditioned on the
    (detached) level-0 cell code — 'bias' adds a per-cell-code embedding to the
    niche VQ input; 'film' affine-modulates z_q_niche by the cell code."""
    cfg["model"]["encoder_params"]["cell_conditioned_niche"] = {"mode": mode}
    return cfg


def _patch_dual_niche_attends_cell_codebook(cfg: dict, heads: int = 1) -> dict:
    """[s60 #6] The niche representation attends (optionally multi-head) over the
    cell codebook prototypes (a soft cell-type-composition descriptor) before the
    niche VQ. `heads` must divide the cell-VQ embedding dim."""
    cfg["model"]["encoder_params"]["niche_attends_cell_codebook"] = {
        "heads": int(heads)}
    return cfg


def _patch_dual_niche_nbr_expr_augment(cfg: dict, dim: int = 32) -> dict:
    """[s60 #8] BANKSY/CellCharter-style: concat a non-learned neighbourhood-mean
    of a projection of the raw counts onto the niche GNN input."""
    cfg["model"]["encoder_params"]["niche_nbr_expr_augment"] = {"dim": int(dim)}
    return cfg


def _patch_dual_cell_niche_alignment(cfg: dict, wt: float = 100.0) -> dict:
    """[s60 #7] Cross-branch ALIGNMENT loss (Barlow invariance term): push the
    matched cell/niche dims to be correlated (opposite sign of disentanglement).
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "cell_niche_alignment_loss" not in losses:
        losses.append("cell_niche_alignment_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_align"] = float(wt)
    return cfg


def _patch_dual_no_contrastive(cfg: dict) -> dict:
    """Drop every cell-side contrastive loss (within-batch / cross-batch MNN /
    global). Used to build the pure NICHE-only single-task baseline so it
    carries no cell-branch objective."""
    losses = cfg["model"]["loss_params"]["loss_names"]
    for ln in (
        "contrastive_cell_attribute_within_batch_loss",
        "contrastive_cell_attribute_cross_batch_mnn_loss",
        "contrastive_cell_attribute_loss",
    ):
        if ln in losses:
            losses.remove(ln)
    return cfg


def _build_s51_spine_codebook(cell_sizes, niche_sizes):
    """Build the s51_v1 / s49_v23 spine with the given per-branch RVQ codebook
    sizes. This is BYTE-IDENTICAL to the s52_v1 build chain (decoupled-enc +
    within-batch contrastive wt=10/k=5 + cell-VQ codebook-diversity wt=10 +
    decoder-cov + no-batch-int + enc-deeper [400,400,256] + dec-w[32] + knn16 +
    sampler[16] + gnn=1 + bs=512 + lr=7e-4 + within-sec), parametrised only by
    `cell_sizes` / `niche_sizes` (e.g. (10, 30)). Used for the L0 codebook-size
    ablation (s54_*) so every such variant is correct-by-construction and
    differs from the s51_v1 reference ONLY in the codebook size."""
    return _patch_dual_codebook_diversity(
        _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_decoupled_encoders(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=niche_sizes,
                                                        ),
                                                        branch="cell", codebook_sizes=cell_sizes,
                                                    ),
                                                ),
                                                hidden_channels=[400, 400, 256],
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
        weight=10.0, temperature=100.0, branch="cell",
    )


def _patch_dual_vq_warmup(cfg: dict, vq_warmup_epochs: int = 10) -> dict:
    """
    Defer codebook init + EMA updates for the first `vq_warmup_epochs`
    training epochs.

    Mechanism (see `VQNiche_Dual.on_train_epoch_start` and the
    `freeze_updates` buffer on `CosineSimCodebook` / `EuclideanCodebook`):
      - During warmup: lazy kmeans-init is held off, EMA updates are
        skipped, dead-code expiry is skipped. Commit losses are zeroed
        in `_step` (so the encoder isn't pulled toward arbitrary
        uniform-init codes).
      - At epoch `vq_warmup_epochs`: the next forward pass triggers
        natural kmeans-init on a z_mlp distribution that has been
        shaped by the reconstruction + integration losses, so the
        initial code centroids are much less batch-correlated than
        they would be at epoch 0.

    Hypothesis: the EMA update over batch-correlated early-epoch
    z_mlp locks some codes into "this batch only" territory; once
    that happens, the dead-code revival is the only way to recover,
    and it doesn't fire on codes that ARE active (just active on
    one batch). Warmup avoids this lock-in.

    Parameters
    ----------
    vq_warmup_epochs : int
        Number of epochs to freeze the codebook. Default 10. Set 0
        to disable (legacy behaviour).
    """
    cfg["model"]["vq_warmup_epochs"] = int(vq_warmup_epochs)
    return cfg


def _patch_dual_adj_within_section_only(cfg: dict, enabled: bool = True) -> dict:
    """
    Set `loss_kwargs['adj_within_section_only']` (default in code: True).

    When True, the BCE adjacency reconstruction loss only computes BCE
    over within-section pairs — cross-section pairs (which are negatives
    by construction, since spatial edges are intra-section) are dropped
    from the candidate-negative pool and from positive-weight accounting.
    This prevents the loss from pushing apart biologically-similar
    cells in different AnnData sections, an anti-integration pressure
    baked into the legacy global-pair behaviour.

    Use this helper to set the flag EXPLICITLY in a variant's
    materialised YAML even though True is the code default — useful for
    sweeps where you want the YAML to record the choice unambiguously
    next to the runs that explicitly chose `enabled=False`.

    See `bce_cosine_adjacency_reconstruction_loss` for the masking
    implementation details.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["adj_within_section_only"] = bool(enabled)
    return cfg


def _patch_dual_max_epochs(cfg: dict, max_epochs: int = 200) -> dict:
    """
    Override the training-budget cap (`trainer.max_epochs`).

    The base config uses `max_epochs=80` (see the long history comment
    inside `make_train_config_dualvq` near the `max_epochs` field for
    why — short version: the 2026-05-11 jump to 200 + patience=20 +
    val_loss monitor regressed niche NMI by ~0.03-0.05 across the
    board because the val_loss optimum drifted past the niche-NMI
    optimum).

    Use cases for bumping this:
      - Variants whose codebook hasn't visibly converged at epoch 80
        (rare but real for high-cardinality 3-level RVQ / large CVQ
        trees).
      - "Long-train better-Pareto?" probes — pair with
        `SQUINT_WITH_PEARSON=1` so the niche-quality-aware
        `val_pearson_gene_wise_1hop_nbr_log1p` monitor is available
        and verify post-hoc niche NMI before drawing conclusions.

    Note: this only RAISES the cap; the run can still early-stop earlier
    (patience=10 val passes ≈ 20 train epochs at the default
    check_val_every_n_epoch=2). To disable early stopping entirely,
    additionally set
    `cfg["trainer"]["early_stopping_params"]["enabled"] = False`.
    """
    cfg["trainer"]["max_epochs"] = int(max_epochs)
    return cfg


def _patch_dual_wide(
        cfg: dict,
        cell_codebook_size:  int = 50,
        niche_codebook_size: int = 50,
        gnn_layers:          int = 2,
        sampler_neighbors:   List[int] = [8, 8],
        nbr_hops:            int = 2,
    ) -> dict:
    """
    "Wide" variant of the dual model: bigger codebooks + deeper GNN +
    deeper sampler + multi-hop nbr aggregation. These knobs are coupled —
    if you bump the GNN depth without bumping the sampler depth, the GNN
    will see partial neighbourhoods and learn nonsense; if you bump the
    nbr-target aggregation without the sampler, the K-th-hop aggregate is
    approximate (computed on incomplete neighbour info). This patch sets
    them all together.

    Defaults (calibrated for ~100k cells / 50 niches):
      - cell VQ k=50          (was 30)
      - niche VQ k=50         (was 30)
      - GNN num_layers=2      (was 1) — niche encoder sees 2-hop context
      - sampler [8, 8]        (was [8]) — needed for the deeper GNN
      - nbr_aggregation_hops=2 — niche target is the 2-hop neighbourhood
                                 mean, encouraging codes to capture
                                 larger-scale spatial structure
    """
    enc = cfg["model"]["encoder_params"]
    enc["vq_cell_params"]["codebook_size"]  = int(cell_codebook_size)
    enc["vq_niche_params"]["codebook_size"] = int(niche_codebook_size)
    enc["gnn_params"]["num_layers"]         = int(gnn_layers)

    cfg["datamodule"]["sampler_params"]["num_neighbors"] = list(sampler_neighbors)

    cfg["model"]["loss_params"]["loss_kwargs"]["nbr_aggregation_hops"] = int(nbr_hops)
    return cfg


# ---------------------------------------------------------------------------
# Architecture ablations (mouse data)
# ---------------------------------------------------------------------------

def _patch_dual_scvi_compact(
        cfg: dict,
        encoder_hidden: int = 128,
        latent_dim:     int = 32,
        decoder_hidden: int = 128,
    ) -> dict:
    """
    Tight scVI-style architecture: 1 hidden layer per MLP + small
    latent dim. The whole geometry is collapsed onto `latent_dim`:

      - encoder MLP: hidden_channels = [encoder_hidden, latent_dim]
                    → 431 input → encoder_hidden hidden → latent_dim
                      output. 1 intermediate layer; output IS the
                      cell-VQ codebook input.
      - GNN:        SAGEConv, hidden_channels = latent_dim,
                      num_layers = 1 (stays at the dual-base default).
                      The niche-VQ codebook input.
      - cell decoder:  hidden_channels = [decoder_hidden]
                    → latent_dim (+ covariate) → decoder_hidden → 431
                      genes. 1 hidden layer; matches the encoder geometry
                      mirror-symmetrically.
      - niche decoder: same as cell decoder.

    Codebook embedding dim = latent_dim for BOTH branches (cell-VQ
    embedding dim = MLP output, niche-VQ embedding dim = GNN hidden).
    Compose with `_patch_dual_rvq(branch=..., codebook_sizes=...)` to
    set codebook geometry on top.

    Why bother:
      - At the SQUINT default (latent=256 + decoder [400, 400]) the
        decoder has room to absorb batch-specific patterns even with
        a `+decoder-cov` covariate, so the encoder doesn't NEED to
        produce batch-invariant z to keep reconstruction good. A
        tighter bottleneck (32) removes that escape hatch: there's
        no longer enough capacity for the decoder to memorise
        per-batch noise, so the encoder has to discard it. scVI's
        20-30 dim latent is the canonical example.
      - Smaller latent also means the cosine-distance VQ codebook
        operates in a lower-dim sphere, where the geometry is
        sharper and code assignment is less affected by random
        per-batch axes.

    Does NOT touch:
      - sampler_params (stays at the base default `[8]` since GNN
        depth stays at 1).
      - nbr_aggregation_hops (stays at 1 in the dual base).
      - decoder_covariate (compose `_patch_dual_decoder_covariate` on
        top — pass `embed_dim=latent_dim` for the symmetric variant
        the user prefers).
      - codebook sizes (compose `_patch_dual_rvq` on top).

    Parameters
    ----------
    encoder_hidden
        Width of the encoder MLP's hidden layer (default 128, like
        scVI's encoder).
    latent_dim
        Bottleneck dim (= MLP output = GNN hidden = codebook embedding).
        Default 32. scVI uses 10-30; 32 is a safe spatial-VQ analogue.
    decoder_hidden
        Width of the decoder MLP's hidden layer (default 128, mirrors
        the encoder).
    """
    enc = cfg["model"]["encoder_params"]
    enc["mlp_params"]["hidden_channels"]   = [int(encoder_hidden), int(latent_dim)]
    enc["gnn_params"]["hidden_channels"]   = int(latent_dim)
    # GNN num_layers stays at the dual-base default (=1) — caller
    # should compose `_patch_dual_gnn2` etc. on top if they want
    # 2-hop spatial context.
    for dec_key in ("attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        cfg["model"][dec_key]["mlp_params"]["hidden_channels"] = [int(decoder_hidden)]
    return cfg


def _patch_dual_small(cfg: dict, hidden: int = 128) -> dict:
    """
    scvi-style compact architecture: a single hidden layer of `hidden`
    dim (default 128) for the encoder MLP, the GNN, and both decoders.

    Configures:
      - encoder MLP:   hidden_channels = [hidden]   (was [400, 256])
      - GNN:           hidden_channels = hidden, num_layers = 1
      - cell decoder:  hidden_channels = [hidden]   (was [400, 400])
      - niche decoder: hidden_channels = [hidden]   (was [400, 400])

    Latent / codebook embedding dim becomes `hidden` (cell_dim and
    niche_dim both = `hidden`), so the codebook itself is also more
    compact. Codebook size is left untouched (still 30 by default; bump
    via `_patch_dual_rvq` etc. on top of this).

    Tests whether the smaller-is-better intuition that drives scvi
    transfers to spatial VQ-VAE. Parameter count drops ~5x vs the
    SQUINT default.
    """
    enc = cfg["model"]["encoder_params"]
    enc["mlp_params"]["hidden_channels"]  = [int(hidden)]
    enc["gnn_params"]["hidden_channels"]  = int(hidden)
    enc["gnn_params"]["num_layers"]       = 1
    for dec_key in ("attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        cfg["model"][dec_key]["mlp_params"]["hidden_channels"] = [int(hidden)]
    return cfg


def _patch_dual_gnn2(
        cfg: dict,
        sampler_neighbors: List[int] = [8, 8],
        nbr_aggregation_hops: int = 2,
    ) -> dict:
    """
    Bump GNN depth from 1 to 2 layers WITHOUT changing the codebook
    size (`_patch_dual_wide` couples both). Sampler depth and niche
    target aggregation hops move with it (otherwise the GNN samples
    nodes it won't aggregate over, or the nbr-target aggregation runs
    on incomplete neighbours — see `_patch_dual_wide` docstring).

    Use this as a clean isolation of "does deeper spatial context
    help?" — codebook stays at 30 (or whatever was set upstream),
    everything else as default.
    """
    cfg["model"]["encoder_params"]["gnn_params"]["num_layers"] = 2
    cfg["datamodule"]["sampler_params"]["num_neighbors"] = list(sampler_neighbors)
    cfg["model"]["loss_params"]["loss_kwargs"]["nbr_aggregation_hops"] = int(
        nbr_aggregation_hops
    )
    return cfg


def _patch_dual_decoder_width(
        cfg: dict,
        hidden_channels,
        branch: str = "both",
    ) -> dict:
    """
    Override the attribute-decoder MLP `hidden_channels` for the cell
    and/or niche branches independently.

    Defaults in the dual base: both decoders use `[400, 400]` (2
    hidden layers, gelu, layer_norm). Shrinking the decoder
    constrains the reconstruction pathway: less capacity for the
    decoder to absorb per-cell / per-batch gene patterns → more
    pressure on the latent / codebook to encode the reconstruction
    signal directly. Hypothesis: tighter decoder forces the encoder
    to produce more discriminative latents → potentially better
    NMI/iLISI at the cost of NB Pearson on hard genes.

    Parameters
    ----------
    hidden_channels
        Decoder hidden widths as a list of ints. Examples:
          - `[256, 256]` : 2 hidden layers, narrower than default
          - `[128, 128]` : 2 hidden layers, much narrower
          - `[64, 64]`   : 2 hidden layers, extreme bottleneck
          - `[400]`      : 1 hidden layer, baseline width (depth-only
                           reduction)
          - `[128]`      : 1 hidden layer, narrow
          - `[64]`       : 1 hidden layer, extreme bottleneck
        Last entry feeds the NB rate head (softmax × read_depth);
        the head itself is appended internally and isn't included
        in this list.
    branch
        "both" (default) | "cell" | "niche". Apply to cell decoder,
        niche decoder, or both independently. Asymmetric variants
        (e.g. tight niche decoder + baseline cell decoder) test
        whether the decoder pressure helps one branch more than
        the other.
    """
    target = list(hidden_channels)
    if branch in ("cell", "both"):
        cfg["model"]["attribute_decoder_cell_params"]["mlp_params"]["hidden_channels"] = target
    if branch in ("niche", "both"):
        cfg["model"]["attribute_decoder_niche_params"]["mlp_params"]["hidden_channels"] = target
    return cfg


def _patch_dual_small_latent(cfg: dict, latent_dim: int = 128) -> dict:
    """
    Shrink the latent / codebook-embedding dimension. Both `cell_dim`
    (= last layer of encoder MLP) and `niche_dim` (= GNN hidden) move
    to `latent_dim` (default 128, down from 256).

    Tighter bottleneck → fewer dimensions to spread information across,
    so each code's embedding has to be more semantically concentrated.
    NicheCompass / scvi typically use 10–32; 128 still leaves headroom
    while halving the SQUINT default.

    Decoder hidden widths and codebook size are unchanged.
    """
    enc = cfg["model"]["encoder_params"]
    h = list(enc["mlp_params"].get("hidden_channels", []))
    if len(h) == 0:
        # No MLP trunk — just set the GNN dim. The cell branch then
        # quantises raw input directly which is unusual; warn but
        # respect.
        print("WARN: _patch_dual_small_latent: no MLP trunk configured "
              "(mlp_params.hidden_channels is empty). Setting GNN hidden "
              "only — cell branch latent dim will equal in_channels.")
    else:
        h[-1] = int(latent_dim)
        enc["mlp_params"]["hidden_channels"] = h
    enc["gnn_params"]["hidden_channels"] = int(latent_dim)
    return cfg


def _patch_dual_dropout(cfg: dict, p: float = 0.1) -> dict:
    """
    Add dropout `p` (default 0.1) to encoder MLP, GNN, and both
    decoders. Cheap regularization — useful when val Pearson plateaus
    while train Pearson keeps climbing (a sign of overfitting). With
    val_loss-based early stopping, dropout's effect is monitorable
    directly in wandb.
    """
    enc = cfg["model"]["encoder_params"]
    enc["mlp_params"]["dropout"] = float(p)
    enc["gnn_params"]["dropout"] = float(p)
    for dec_key in ("attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        cfg["model"][dec_key]["mlp_params"]["dropout"] = float(p)
    return cfg


def _patch_dual_mlp_width(
        cfg: dict,
        encoder_hidden: List[int],
        decoder_hidden: List[int],
    ) -> dict:
    """
    Override the MLP hidden widths for BOTH the encoder trunk and the
    cell + niche attribute decoders. Useful for capacity ablations
    that want a coordinated change (encoder + decoder grow / shrink
    together) rather than a separate sweep on each side.

    The LAST element of `encoder_hidden` is the encoder MLP's output
    dim — i.e. the latent / codebook embedding dim. Pick it equal to
    the existing latent (typically 256) to isolate "hidden width"
    from "latent dim"; pick something else to ablate latent dim
    along with width.

    Decoders take only `decoder_hidden` (their output dim is fixed at
    n_genes by the model wiring).
    """
    enc = cfg["model"]["encoder_params"]
    enc["mlp_params"]["hidden_channels"] = list(encoder_hidden)
    for dec_key in ("attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        if dec_key in cfg["model"]:
            cfg["model"][dec_key]["mlp_params"]["hidden_channels"] = list(decoder_hidden)
    return cfg


def _patch_dual_encoder_deeper(
        cfg: dict,
        hidden_channels: Optional[List[int]] = None,
    ) -> dict:
    """
    Deepen the encoder MLP trunk: from `[400, 256]` (2 hidden layers)
    to `[400, 400, 256]` (3 hidden layers) by default. Latent dim
    (= last layer = 256) is preserved so codebook + GNN dims don't
    shift; only the depth grows. The extra layer adds ~160k params
    and a bit of FLOPs but lets the encoder learn richer non-linear
    feature mixes before quantisation.
    """
    enc = cfg["model"]["encoder_params"]
    target = list(hidden_channels) if hidden_channels else [400, 400, 256]
    enc["mlp_params"]["hidden_channels"] = target
    return cfg


def _patch_dual_gnn_hidden(
        cfg: dict,
        hidden: int = 384,
    ) -> dict:
    """
    Bump the niche-branch GNN hidden width (default 256 -> 384).
    Note: this changes the GNN's *internal* dim AND the niche latent
    dim (= GNN output) — codebook embedding dim follows. Larger spatial
    capacity at modest cost (~1.5x params on the GNN).
    """
    cfg["model"]["encoder_params"]["gnn_params"]["hidden_channels"] = int(hidden)
    return cfg


def _patch_dual_gnn_layers(
        cfg: dict,
        num_layers: int = 2,
    ) -> dict:
    """
    Override the niche-branch GNN depth (`gnn_params.num_layers`). Base
    default is 1 layer (= 1-hop aggregation). Setting `num_layers=2`
    gives 2-hop aggregation — the niche encoder sees neighbours of
    neighbours.

    IMPORTANT: the sampler depth MUST match the GNN depth, otherwise
    the second GNN layer sees incomplete neighbour sets and the
    aggregation is silently wrong. For `num_layers=2` on a knn=16
    graph, set the sampler to `[16, 16]` via
    `_patch_dual_sampler_neighbors(num_neighbors=[16, 16])`.

    This helper is a surgical "GNN-only" knob — it does NOT touch
    `nbr_aggregation_hops` (the niche-side reconstruction TARGET k-hop
    radius) or codebook sizes; for the coupled "everything wide"
    sweep see `_patch_dual_wide`. Keeping these orthogonal lets you
    ablate GNN depth without confounding it with target-aggregation
    radius or codebook capacity.

    Memory: each extra GNN layer adds ~D^2 params (default D=256 → 65k
    params/layer). Activation memory scales with the deeper sampler
    fan-out — `[16, 16]` is ~16x the per-step memory of `[16]`.
    """
    cfg["model"]["encoder_params"]["gnn_params"]["num_layers"] = int(num_layers)
    return cfg


def _patch_dual_sampler_neighbors(
        cfg: dict,
        num_neighbors: Optional[List[int]] = None,
    ) -> dict:
    """
    Override the `NeighborLoader.num_neighbors` per layer (default
    `[8]` for 1-hop). Increasing to `[16]` doubles the niche
    aggregation neighborhood, often improving spatial-domain coherence
    at the cost of larger per-step memory.
    """
    target = list(num_neighbors) if num_neighbors else [16]
    cfg["datamodule"]["sampler_params"]["num_neighbors"] = target
    return cfg


def _patch_dual_graph_knn(cfg: dict, n_neighs: int = 8) -> dict:
    """
    Select which pre-built spatial k-NN graph to use at training time.

    Sets `cfg["dataset"]["graph_params"]["n_neighs"]` — the loader
    then picks the corresponding `edge_index_*` from the dataset
    blob. This is a SELECTION at runtime, not a graph build: the
    target k MUST exist in the blob's `n_neighs_list` (set in
    `make_dataset_blob_config()`).

    To use k values not in the existing blob:
      1. Add the desired k(s) to `make_dataset_blob_config()`'s
         `graph_kwargs.n_neighs_list` (e.g. `[6, 8, 12]`).
      2. Rebuild the blob:  `python examples/run_squint.py --build-blob`
      3. Use this patch to select the new k at training time.

    Parameters
    ----------
    n_neighs : int
        Target spatial k-NN k value. Must be present in the blob's
        n_neighs_list. Smaller k → sparser graph → less spatial
        signal per cell + faster sampling. Larger k → richer
        spatial context but heavier per-step memory.
    """
    cfg["dataset"]["graph_params"]["n_neighs"] = int(n_neighs)
    return cfg


def _patch_dual_adversarial(
        cfg: dict,
        alpha: float = 1.0,
        wt_adv_batch: float = 150.0,
        hidden_channels: Optional[List[int]] = None,
        warmup_epochs: int = 0,
    ) -> dict:
    """
    Enable a domain-adversarial batch-invariance head on `z_mlp`.

    What it does: a small MLP classifier predicts the per-cell batch
    label from `z_mlp`. A Gradient Reversal Layer (GRL) is placed between
    the encoder and the classifier, so:
      - The classifier learns to predict batch (gets the true gradient).
      - The encoder is pushed AWAY from being batch-predictive (gets the
        negated gradient).

    The model classifies the FULL `z_mlp` (seeds + sampled GNN neighbours),
    not only the seed-node prefix — see VQNiche_Dual._step for the rationale.
    This makes the niche branch's GNN aggregate over batch-invariant inputs.

    Effect: provides the missing batch-invariance pressure on the latent
    that NicheCompass gets from its KL prior. Pair with
    `_patch_dual_decoder_covariate` for the full NicheCompass-shaped
    setup (decoder absorbs per-batch gene patterns, encoder is pushed
    to be batch-invariant).

    Parameters
    ----------
    alpha: float
        Strength of the gradient reversal (encoder pressure). Default 1.0
        — the standard Ganin-Lempitsky setting; multiplies the negated
        gradient that flows back to the encoder. Earlier we tried alpha
        =2.0 (with wt_adv_batch=100) and the codes started to collapse
        (encoder pressure too aggressive). Reverted to alpha=1.0 and
        bumped `wt_adv_batch` instead — the encoder gradient magnitude
        is `alpha * wt_adv_batch * d_CE/d_z`, so the two knobs are
        interchangeable for encoder pressure; the difference is that
        `wt_adv_batch` also accelerates the classifier (sharper
        adversary signal at the cost of faster CE→0 saturation).
    wt_adv_batch: float
        Weight of the CE loss in the total loss. Default 150.0. With 2
        batches the binary CE is bounded by log(2) ≈ 0.69 nats while
        the cell + niche NB losses sit at ~150 nats each; at
        `wt_adv_batch=1.0` the adversarial gradient is ~150x weaker
        than reconstruction and the encoder ignores it. 150.0 brings
        the encoder pressure to ~1.5x the "almost good" alpha=1, wt=100
        baseline without the codebook collapse seen at alpha=2. Sweep
        log-ish (100, 150, 200, 300) — the right value depends on
        dataset, codebook size, and how aggressive the decoder's batch-
        absorption pathway (concat / FiLM) already is.
    hidden_channels: list of int, optional
        Hidden widths for the classifier MLP. Default [128] (one
        hidden layer of 128 units).
    """
    cfg["model"]["adversarial_batch_dim_request"] = True
    cfg["model"]["adversarial_alpha"] = float(alpha)
    if hidden_channels is not None:
        cfg["model"]["adversarial_hidden_channels"] = list(hidden_channels)
    # `warmup_epochs` is wired through to VQNiche_Dual.__init__ and
    # consumed inside `_step`: during the first `warmup_epochs`
    # training epochs, the GRL is run with alpha=0 so the encoder
    # gets zero adversarial gradient (codes settle on biology first).
    # The classifier still trains and the CE term still appears in
    # total_loss for logging. Default 0 = legacy behaviour.
    cfg["model"]["adversarial_warmup_epochs"] = int(warmup_epochs)

    losses = cfg["model"]["loss_params"]["loss_names"]
    if "adversarial_batch_loss" not in losses:
        losses.append("adversarial_batch_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_adv_batch"] = float(wt_adv_batch)
    return cfg


def _patch_dual_adversarial_cosine(
        cfg: dict,
        total_epochs: int = 100,
    ) -> dict:
    """
    Switch the adversarial alpha to a cosine schedule (vs the legacy
    constant). After warmup, alpha follows a half-sine envelope: rises
    from 0 at `warmup_epochs`, peaks at the midpoint of the post-warmup
    phase, decays back to ~0 at `total_epochs`. Past `total_epochs` the
    alpha stays at 0.

    Why use this: the constant schedule keeps adversarial pressure on
    the encoder until the very last epoch. Combined with `adv-warmup`,
    the cell token doesn't get a "consolidation phase" to refine
    cell-type-discriminative features without the gradient reversal
    pulling them apart. The cosine decay creates that consolidation
    phase in the final ~25% of training, while still giving strong
    integration pressure mid-training.

    Pair with `_patch_dual_adversarial(warmup_epochs=10, ...)` and an
    `adv-warmup10` variant tag for the intended use case.

    NOTE: requires `_patch_dual_adversarial` to have been called first
    on the cfg (this patch only flips the schedule flag — the
    adversarial head itself is built by the prior patch).
    """
    if "adversarial_batch_dim_request" not in cfg["model"]:
        raise RuntimeError(
            "_patch_dual_adversarial_cosine: call "
            "_patch_dual_adversarial() first to enable the adversarial "
            "head, then chain this patch on top."
        )
    cfg["model"]["adversarial_schedule"] = "cosine"
    cfg["model"]["adversarial_total_epochs"] = int(total_epochs)
    return cfg


def _patch_dual_adversarial_cell_only(cfg: dict) -> dict:
    """
    Restrict the adversarial classifier to the SEED prefix of z_mlp
    (i.e. the per-cell embedding before VQ), instead of classifying
    the full tensor (seeds + sampled neighbours).

    Why: the niche-branch GNN aggregates over neighbour z_mlp rows to
    produce z_gnn. With the legacy `apply_to='full'` setting, the
    adversarial gradient reaches BOTH the cell pathway (z_mlp[:B] -> VQ
    cell) and the niche pathway (z_mlp[B:] -> GNN -> VQ niche). Both
    branches lose batch-correlated info. With `apply_to='cell'`, only
    the seed rows feed the adversary's CE — the niche branch is left
    un-pressured by the adversary, keeping its spatial-aggregation
    capacity intact.

    Hypothesis: cell-type identification benefits from concentrating
    the adversarial "budget" on the cell branch where it matters most;
    the niche branch's batch-mixing is achieved instead by the
    decoder covariate + adjacency reconstruction (which already
    absorb batch-specific patterns elsewhere).

    NOTE: requires `_patch_dual_adversarial` to have been called first.
    """
    if "adversarial_batch_dim_request" not in cfg["model"]:
        raise RuntimeError(
            "_patch_dual_adversarial_cell_only: call "
            "_patch_dual_adversarial() first to enable the adversarial "
            "head, then chain this patch on top."
        )
    cfg["model"]["adversarial_apply_to"] = "cell"
    return cfg


def _patch_dual_mmd_batch(
        cfg: dict,
        wt_mmd_batch: float = 50.0,
        n_sub: int = 512,
    ) -> dict:
    """
    Add a non-adversarial MMD batch-invariance loss on the cell-token
    input (z_mlp[:batch_size]).

    Computes a kernel-based MMD between per-batch distributions on
    each mini-batch and adds it directly to the total loss. Unlike
    the adversarial CE — which involves a min-max game between the
    classifier and the encoder, and can fail to remove
    batch-correlated features that were learned during a warmup phase —
    MMD applies a deterministic distribution-matching pressure on
    every step.

    Recipe (mirrors `mmd_comparable` in compute_inference_metrics.py
    so train-time pressure and test-time evaluation use the same
    kernel):
      - z-score per-dim normalisation
      - RBF kernel with sigma chosen by median pairwise distance
      - Pairwise MMD^2 across all batches present, summed

    Use cases:
      1. Pair with `_patch_dual_adversarial(warmup_epochs=10)` to give
         a non-adversarial integration safety-net during the warmup
         period (during which the GRL alpha is 0).
      2. Replace the adversarial CE entirely (drop
         `_patch_dual_adversarial`) to test whether MMD alone is
         sufficient for batch alignment — simpler architecture, no
         classifier to tune.

    Parameters
    ----------
    wt_mmd_batch
        Loss weight for the MMD term. Default 50.0. The unweighted
        MMD^2 is in [0, 2] for a normalised kernel; values around
        50-200 put it on a comparable scale to the adversarial CE
        (which sits at log(2) * wt_adv_batch ≈ 100 for wt=150).
    n_sub
        Sub-sample size per batch. Default 512 — keeps the pairwise
        kernel matrix at ~1 MB.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "mmd_batch_loss" not in losses:
        losses.append("mmd_batch_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_mmd_batch"] = float(wt_mmd_batch)
    cfg["model"]["loss_params"]["loss_kwargs"]["mmd_n_sub"] = int(n_sub)
    # The model's `_step` populates `mmd_target` and `mmd_target_labels`
    # from `z_mlp[:batch_size]` and `adata_batch_ids[:batch_size]`
    # unconditionally; the dispatcher only consumes them when
    # `mmd_batch_loss` is in `loss_names`.
    return cfg


def _patch_dual_mmd_prior(
        cfg: dict,
        wt_mmd_prior: float = 50.0,
        n_sub: int = 512,
        prior_std: float = 1.0,
    ) -> dict:
    """
    Add a non-adversarial, NON-PARAMETRIC MMD-to-prior loss.

    Pulls the empirical distribution of `z_mlp[:batch_size]` (the
    cell-token input pre-VQ) toward samples from the isotropic
    Gaussian prior `N(0, prior_std² * I)`. This is the parameterless
    analogue of NicheCompass's `KL(q(z|x) || N(0, I))` divergence —
    achieves the same prior-matching pressure without needing to
    make the encoder probabilistic (output mu, sigma) and without
    requiring KL annealing.

    Mechanism:
      - The prior is batch-agnostic by construction.
      - Pulling the embedding toward the prior forces per-batch z
        distributions to converge to the same prior → integrated.
      - Combined with `+decoder-cov` (which absorbs per-batch
        differences on the decoder side), this mirrors NicheCompass's
        integration without the adversarial min-max game OR the
        parametric VAE complexity.

    Compared to siblings:
      - `_patch_dual_adversarial`: min-max GRL game. Unstable
        dynamics, but strong gradient when working.
      - `_patch_dual_mmd_batch`: matches per-batch distributions to
        EACH OTHER (pairwise). Requires batch labels.
      - `_patch_dual_mmd_prior` (this): matches the empirical
        distribution to a FIXED prior. No batch labels. Single MMD
        per call.

    All three can coexist — they consume overlapping data but
    compute different distances.

    Use cases:
      1. Replace `+adv` entirely → pure prior-matching integration.
      2. Combine with `+adv` → stacked: GRL game + prior pull.
      3. Combine with `+mmd-w*` (batch MMD) → "match to each other
         AND to a prior".

    Parameters
    ----------
    wt_mmd_prior
        Loss weight on the MMD^2 vs prior. Default 50 (similar
        magnitude to `_patch_dual_mmd_batch`'s default).
    n_sub
        Sub-sample size for the pairwise kernel. Default 512.
    prior_std
        Std of the isotropic Gaussian prior. Default 1.0 (= N(0, I)).
        Bump up if the encoder's natural scale is large and the
        prior pull fights reconstruction too hard.
    """
    losses = cfg["model"]["loss_params"]["loss_names"]
    if "mmd_prior_loss" not in losses:
        losses.append("mmd_prior_loss")
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_mmd_prior"] = float(wt_mmd_prior)
    cfg["model"]["loss_params"]["loss_kwargs"]["mmd_n_sub"] = int(n_sub)
    cfg["model"]["loss_params"]["loss_kwargs"]["mmd_prior_std"] = float(prior_std)
    # The model's `_step` populates `mmd_target` from
    # `z_mlp[:batch_size]` unconditionally; this loss consumes the
    # same tensor as `mmd_batch_loss` but ignores `mmd_target_labels`
    # (no batch labels needed for prior-matching).
    return cfg


def _patch_dual_encoder_shallow(
        cfg: dict,
        hidden_channels: Optional[List[int]] = None,
    ) -> dict:
    """
    Shrink the encoder MLP trunk: from `[400, 256]` (2 hidden layers,
    the default) to `[256]` (1 hidden layer) by default. Latent dim
    (= last layer output = 256) is preserved so codebook + GNN dims
    don't shift; only the depth shrinks.

    Hypothesis: a smaller-capacity encoder has less room to memorise
    batch-specific features, which (combined with a brief warmup
    phase) may push the cell-type discriminative signal up while
    keeping the integration cost manageable. Sibling of
    `_patch_dual_encoder_deeper`; both target the same MLP trunk
    inside the encoder.

    NOTE: this is the trunk only — the GNN depth and decoder MLP
    width are controlled separately by `_patch_dual_wide` / the
    sampler patches / `_patch_dual_small`. For an even more
    aggressive capacity bottleneck, pair with `_patch_dual_small`.
    """
    enc = cfg["model"]["encoder_params"]
    target = list(hidden_channels) if hidden_channels else [256]
    enc["mlp_params"]["hidden_channels"] = target
    return cfg


def _patch_dual_commit_niche_weight(
        cfg: dict,
        wt_commit_niche: float = 2.0,
    ) -> dict:
    """
    Increase (or decrease) the niche-branch commit-loss weight. Default 1.0
    (set in `_make_default_dual_config`). Sibling of
    `_patch_dual_commit_cell_weight`; both can be applied in sequence
    via patch chaining (e.g. cell=3.0 + niche=0.5 to asymmetrically
    sharpen the cell side while loosening the niche side).

    The niche commit loss pulls `z_gnn[:batch_size]` (the niche-branch
    pre-VQ representation) toward `z_q_niche` (post-VQ). Higher weight
    → niche codes more concentrated → more distinct spatial regions
    per code. Lower weight → niche codes smear → finer-grained niche
    structure can survive in the residual.

    Use cases:
      - `wt_commit_niche > 1.0`: sharpen niche codes, often boosts
        Niche NMI on the composite RVQ code; risk of over-coarsening
        the spatial niches (multiple distinct regions collapse to one
        code).
      - `wt_commit_niche < 1.0`: looser niche commit, codes carry
        less variance — useful when the niche encoder seems to be
        overfitting batch-correlated spatial structure.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_commit_niche"] = float(wt_commit_niche)
    return cfg


def _patch_dual_commit_cell_weight(
        cfg: dict,
        wt_commit_cell: float = 2.0,
    ) -> dict:
    """
    Increase the cell-branch commit-loss weight from the default 1.0
    (set per-loss inside `_make_default_dual_config`) to `wt_commit_cell`.

    The commit loss pulls `z_mlp[:batch_size]` (the cell-branch input
    pre-VQ) toward `z_q_cell` (the post-VQ embedding). Higher weight
    means z_mlp commits more aggressively to discrete codes — codes
    are sharper / less smeared, and within-class variance in the
    cell-token shrinks.

    Hypothesis: with the cell-token discrete codes "sharpened", cells
    of the same cell-type cluster around fewer codes, raising the
    NMI(cell-token, cell_type). Trade-off: higher cell-commit can
    starve the niche-branch GNN, which consumes the SAME z_mlp tensor
    (sampled neighbours) — if z_mlp is dominated by sharp cell-cluster
    geometry, the GNN's spatial aggregation may degrade. Pair with
    a weaker niche-branch lever (e.g. lower wt_commit_niche) if you
    see niche-NMI regress.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["wt_commit_cell"] = float(wt_commit_cell)
    return cfg


# NOTE: `_patch_dual_cross_entropy` was removed on purpose.
#
# That patch enabled a supervised cross-entropy loss on the cell-type
# label, which would have turned SQUINT into a semi-supervised method
# (consuming ground-truth `cell_type` at training time). We're keeping
# SQUINT strictly unsupervised so it stays directly comparable to the
# unsupervised / zero-shot baselines (scVI, NicheCompass, BANKSY,
# Geneformer, scGPT, ...). The Linear predictor submodule itself is
# still built by BaseModel (256 -> n_cell_types) but its output is
# never folded into the loss, so it's harmless dead weight.
#
# If you ever need a supervised oracle / upper-bound number, do it as
# a one-off external experiment outside the published variant set —
# don't reintroduce a `+ce-w*` patch here.


def _patch_dual_decoder_film(
        cfg: dict,
        condition_list: List[str] = ["cell_batch_id"],
        init_mode: str = "small_random",
        small_random_std: float = 0.1,
    ) -> dict:
    """
    Enable FiLM batch-correction INSIDE the dual-model attribute decoders
    (cell + niche). Mutually exclusive with `_patch_dual_decoder_covariate`
    (which concatenates the one-hot onto z_q instead).

    What it does: sets `apply_conditioning='in-MLP'` on both decoder
    parameter dicts and configures their FiLM `condition_list` (default
    ['cell_batch_id'], i.e. per-cell batch one-hot). The data loader's
    `attr_decoder_conditions` tensor is then threaded through to each
    decoder by VQNiche_Dual.forward, and FiLM modulates each decoder
    layer with γ/β derived from the batch one-hot.

    Why init_mode defaults to 'small_random', not 'identity'
    --------------------------------------------------------
    Empirically, with `init_mode='identity'` the niche branch in this
    setup did NOT integrate well across platforms while `+decoder-cov`
    (concat) DID. The reason is the gradient signal at init:
      - 'identity' starts with γ=1, β=0 *constants* regardless of the
        batch one-hot. The decoder defaults to "ignore the batch info"
        and the path of least resistance is to encode batch in z_q
        instead of via FiLM. Gradient pressure to deviate from identity
        only kicks in if the codes ALREADY don't carry batch — which
        they do, because of the per-batch spatial graph.
      - 'small_random' starts with weights ~ N(0, std²) and identity
        bias, so γ ≈ 1 ± std and β ≈ 0 ± std *per channel per batch*
        from epoch 0. Different batches see different (small) γ/β
        perturbations → there is condition-dependent gradient signal
        from the very first step → the decoder readily learns to
        absorb per-batch shifts via FiLM, freeing the codes (same
        mechanism that makes concat work).
    Concat works for the same reason at the architectural level: the
    decoder's first linear layer initialises W_b with kaiming_uniform,
    so the batch one-hot already injects condition-dependent noise that
    the decoder is incentivised to replace with a meaningful per-batch
    bias. 'small_random' is the FiLM-side analog of that.

    `small_random_std=0.1` makes the per-channel perturbation magnitude
    on the order of the activation scale at init (10%) — comparable to
    a Kaiming-init linear layer's contribution from a one-hot input.
    """
    film_params = {
        "condition_list":    list(condition_list),
        "use_bias":          True,
        "use_residual":      False,
        "residual_weight":   0.2,
        "init_mode":         init_mode,
        "small_random_std":  float(small_random_std),
    }
    for dec_key in ("attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        cfg["model"][dec_key]["apply_conditioning"] = "in-MLP"
        cfg["model"][dec_key]["conditioning_params"] = film_params.copy()
    return cfg


def _patch_dual_decoder_covariate(
        cfg: dict,
        embed_dim: int = 16,
    ) -> dict:
    """
    Enable NicheCompass-style decoder covariate via a LEARNED batch
    embedding (concat-based batch correction).

    What it does: at runtime, `train()` sets `decoder_covariate_dim` to
    the number of distinct samples in the dataset (= n train batches).
    The dual model then learns an `nn.Embedding(n_batches, embed_dim)`
    and concatenates `embedding[adata_batch_ids]` (an `embed_dim`-dim
    learned vector per cell) onto z_q_cell and z_q_niche before each
    decoder. The decoders are constructed with extended `in_channels`
    by `embed_dim` (NOT by `n_batches` — the old one-hot scheme).

    Why a learned embedding instead of one-hot:
      - Generalises to NOVEL batches at inference. The mean of trained
        embeddings is a meaningful "neutral" covariate for held-out
        sections; one-hot has no valid index for unseen batches and
        forces an arbitrary "treat as if it were train batch 0"
        fallback. The dual model's forward path swaps in the mean
        embedding for cells flagged via `adata_batch_ids_unseen_mask`
        (set by `build_batch_one_hot_from_obs` when the cell's label
        wasn't in the train-time densification map).
      - Lower-dim conditional (default embed_dim=16 vs n_batches which
        can be 8-20+).
      - Encoder still doesn't see batch info → codes (= VQ output) are
        batch-invariant by construction; only the DECODER reconstruction
        depends on the embedding lookup.

    Effect:
      - Decoders use the learned batch embedding to fit per-batch
        gene patterns (different gene-capture rates, different noise
        structure across MERFISH/STARmap/CosMx).
      - Encoder is "freed" from encoding batch info in z_q because the
        decoder can absorb per-batch differences via the embedding.
      - Codebook (shared across batches) is biased toward batch-
        invariant biological identity.

    Parameters
    ----------
    embed_dim : int
        Dimensionality of the learned batch embedding. 16 is a good
        default — large enough to capture meaningful cross-batch
        differences, small enough to keep the decoder input compact.
    """
    cfg["model"]["decoder_covariate_dim_request"] = True
    cfg["model"]["decoder_covariate_embed_dim"]   = int(embed_dim)
    return cfg


def _patch_dual_decoupled_decoder_cov(cfg: dict) -> dict:
    """
    Split the decoder-covariate `nn.Embedding` into TWO independent
    modules — one per decoder. Each decoder's batch covariate then
    receives gradients from THAT decoder's reconstruction loss ONLY;
    no parameter is shared between the cell-NB pathway and the
    nbr-NB pathway downstream of the codebook.

    Why this matters under `+decoupled-enc`
    --------------------------------------
    Architectural gradient-isolation audit of the dual model under
    `+decoupled-enc` (`vqniche_dual_encoder.py` + `vqniche_dual.py`):

      Module                         Per-branch?  Shared?
      ─────────────────────────────  ───────────  ──────────
      mlp_module (cell trunk)        cell-only    no
      niche_mlp_module (niche trunk) niche-only   no
      vq_cell                        cell-only    no
      vq_niche                       niche-only   no
      gnn_module, niche_neck         niche-only   no
      attribute_decoder_cell         cell-only    no
      attribute_decoder_niche        niche-only   no
      encoder.conditioning_module    n/a          NOT BUILT (no `condition_list` in cfg by default)
      batch_embedding                BOTH         **YES (single nn.Embedding)**

    Without this patch the `batch_embedding`'s rows receive gradient
    updates from cell-NB AND nbr-NB on every step — the last remaining
    learnable parameter shared between the two branches. With this
    patch, that's gone too: `batch_embedding_cell` is updated by
    cell-NB only, `batch_embedding_niche` by nbr-NB only.

    Sizing
    ------
    Doubles the number of decoder-covariate parameters (was
    `n_batches × 16` floats, now `2 × n_batches × 16`). Negligible
    relative to the rest of the model.

    Requires
    --------
    Must be applied AFTER `_patch_dual_decoder_covariate` (which sets
    `decoder_covariate_dim_request=True`); the runtime resolves the
    actual `decoder_covariate_dim` from the dataloader. No-op if
    `+decoder-cov` isn't enabled — the model sees the flag but the
    `decoder_covariate_dim==0` branch skips embedding creation entirely.
    """
    cfg["model"]["decoupled_decoder_covariate"] = True
    return cfg


def _patch_dual_chl59_lung5(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx: Optional[List[int]] = None,
        batch_size: int = 128,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Swap the dataset to /nfs/team361/sb75/DATASETS/silver/chl59-8b_1p (8
    CosMx Lung samples) and configure whole-section holdout via
    SpatialBatchSplit.

    Parameters
    ----------
    train_batch_idx, test_batch_idx:
        `adata_batch_id` values (= the integer parsed out of
        `uns['batch']`, e.g. `uns['batch']='batch6'` -> id=6), NOT
        alphabetical file positions in the silver dir. SpatialBatchSplit
        matches `data.adata_batch_id` against these lists, so they MUST
        be the actual ids.

        Translation to `cfg['dataset']['adata_batch_idx']` (which IS
        position-based, used by `initialize_databatch` to slice the
        dataset blob) is done at runtime in `train()` after the blob
        is loaded, by reading each section's actual `adata_batch_id`
        and inverting the id -> position mapping.

        Two buckets:
          - `train_batch_idx`: sections used for gradient updates.
                               OPTIONAL — when omitted (default), train()
                               picks every blob section that isn't in
                               test/val. The base config's 10% cell-level
                               split runs on these and provides the val
                               signal for early stopping +
                               best-checkpoint.
          - `test_batch_idx`:  whole sections held out ENTIRELY from
                               training (NOT loaded into the training
                               data graph). Evaluated post-hoc by
                               `predict()` + the inference metrics
                               pipeline. Default `None` = no test set.

    Memory: lowers `batch_size` to 128 (from the 256 default inherited
    from the mmb-smb config) and `edge_sampling_ratio` to 1.0 (from 2.0).
    The Lung sections are denser than the mouse-brain ones AND have a
    larger gene panel (~946 vs. 431), so per-step autograd memory is
    roughly 2x the mmb-smb cost at the same batch_size. Override via the
    kwargs if your GPU has more memory.
    """
    test_batch_idx = list(test_batch_idx) if test_batch_idx else []
    # train_batch_idx is OPTIONAL — leave the train_batches list empty
    # when not specified so train() defaults to "every blob section not
    # in test/val".
    train_batch_idx = list(train_batch_idx) if train_batch_idx else []

    cfg["dataset"]["dataset_name"]   = "chl59-8b_1p"
    cfg["dataset"]["dataset_tag"]    = "chl59-8b_1p"
    cfg["dataset"]["root_data_dir"]  = "/nfs/team361/sb75/DATASETS"
    # `adata_batch_idx` (positions) is filled in at runtime in `train()`
    # by translating `train_batches` (ids) via the loaded blob's
    # id-to-position mapping. We leave it as a placeholder here so the
    # variant config archives the user-intended ids rather than positions
    # baked under a particular id-derivation assumption.
    cfg["dataset"]["adata_batch_idx"] = []

    cfg["dataset"]["train_transform_params"] = {
        "region":         None,
        "train_batches":  list(train_batch_idx),
        "val_batches":    [],   # no whole-section val; cell-level split provides val
        # `test_batches` here is INFORMATIONAL — these sections aren't
        # in `adata_batch_idx` so the data graph never contains them and
        # `SpatialBatchSplit.forward` is never called on them at training
        # time. We stamp the ids anyway so `predict()` reads them from
        # the saved config and tags inference cells with
        # `data_split = "test"` for stratified post-hoc metrics.
        "test_batches":   test_batch_idx,
        "xy_key":         "xy_coordinates",
        # Cell-level 10% split provides the val signal during training.
        "train_val_cell_split":
            cfg["dataset"]["train_transform_params"].get("train_val_cell_split", 0.10),
        "cell_split_seed":
            cfg["dataset"]["train_transform_params"].get("cell_split_seed", 0),
    }

    # The Lung AnnDatas have `obs['batch']` populated with per-sample
    # labels; the batch-key flag tells the blob builder to read it.
    cfg["dataset"]["graph_params"]["batch_key"] = "batch"

    # Memory tuning for the larger Lung gene panel + denser sampling
    # (see docstring). Defaults are conservative for a 44 GiB GPU; bump
    # them on bigger cards.
    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio
    )

    # Validation runs on the held-out replicate(s); name the monitor
    # accordingly so checkpoints are saved on test-set Pearson.
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_dual_chl59_2b_1p(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx: Optional[List[int]] = None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Swap the dataset to /lustre/scratch126/cellgen/lotfollahi/DATASETS/silver/chl59-2b_1p
    (2-sample CosMx Lung subset). Sibling of `_patch_dual_chl59_lung5`
    but pointing at the 2-sample silver directory and defaulting to
    bs=512 (no per-section memory bottleneck at 2 samples vs 8).

    Parameters
    ----------
    train_batch_idx, test_batch_idx:
        Same semantics as in `_patch_dual_chl59_lung5` — `adata_batch_id`
        values, NOT positions. Empty lists default to "every blob
        section used for training, none held out". With only 2 sections
        in this dataset, leaving both empty trains on both.
    batch_size:
        Default 512 (matches the s49_v23 spine). The chl59-8b helper
        defaults to 128 due to the larger gene panel + denser sampling
        at 8 sections; with only 2 sections the per-step autograd memory
        is much lower, so 512 is fine on the standard 44 GiB cards.
    edge_sampling_ratio:
        Default 1.0 (matches chl59-8b setting).
    """
    test_batch_idx = list(test_batch_idx) if test_batch_idx else []
    train_batch_idx = list(train_batch_idx) if train_batch_idx else []

    cfg["dataset"]["dataset_name"]   = "chl59-2b_1p"
    cfg["dataset"]["dataset_tag"]    = "chl59-2b_1p"
    cfg["dataset"]["root_data_dir"]  = "/lustre/scratch126/cellgen/lotfollahi/DATASETS"
    cfg["dataset"]["adata_batch_idx"] = []

    cfg["dataset"]["train_transform_params"] = {
        "region":         None,
        "train_batches":  list(train_batch_idx),
        "val_batches":    [],
        "test_batches":   test_batch_idx,
        "xy_key":         "xy_coordinates",
        "train_val_cell_split":
            cfg["dataset"]["train_transform_params"].get("train_val_cell_split", 0.10),
        "cell_split_seed":
            cfg["dataset"]["train_transform_params"].get("cell_split_seed", 0),
    }

    cfg["dataset"]["graph_params"]["batch_key"] = "batch"

    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio
    )

    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _resolve_train_rows(dataset_blob, split_sections: dict) -> List[int]:
    """
    DB rows of the TRAIN sections, given a whole-section split spec.

    Train is whatever no other split claims — the caller lists only the
    held-out sections. Listing 416 train sections by hand invites a typo that
    would silently shrink the training set, and a shrunk train set is invisible
    in every metric.

    Mirrors `OnDiskStreamingDataModule._resolve_section_rows`, and must keep
    mirroring it: the map derived here sizes the model, the one derived there
    stamps the loader's ids, and the two have to agree exactly.
    """
    claimed = set()
    for split, names in split_sections.items():
        if split == "train" or not names:
            continue
        claimed.update(dataset_blob.section_rows_for(names))
    if "train" in split_sections and split_sections["train"]:
        return dataset_blob.section_rows_for(split_sections["train"])
    rows = [r for r in range(len(dataset_blob)) if r not in claimed]
    if not rows:
        raise ValueError("split_sections leaves no sections for training.")
    return rows


def _patch_corpus_split(cfg: dict, split_json: str | Path,
                        val_key: str = "validation",
                        test_key: str = "test") -> dict:
    """
    Apply TERRA's whole-section split, read from `_splitspec.json`.

    Whole-section, not an in-section cell split, because that is what the
    zero-shot claim requires: a held-out SECTION is one the model has never
    seen in any form, which is what TERRA's generalisation ladder measures and
    what makes our numbers comparable to a published model on identical data.

    Sections are named by REL PATH ("subdir/file.h5ad"). File stems collide for
    162 of the corpus's 636 files (`adata_batch0` appears in 8 datasets), so a
    stem-keyed split would silently mis-scope. `section_rows_for` raises on an
    ambiguous name rather than guessing.

    Train is implicit — everything not named here. See `_resolve_train_rows`.
    """
    doc = json.loads(Path(split_json).read_text())
    # `_splitspec.json` nests the section lists under "splits"; a hand-written
    # spec may put them at the top level. Accept both.
    spec = doc.get("splits", doc)

    # A MISSING KEY RAISES. It used to be skipped with `.get()`, and that
    # silently produced a leak: with no `val` entry the val loader fell back to
    # an in-section cell mask spanning EVERY section, so validation cells were
    # drawn from the training sections. The run looked healthy -- `val_*`
    # metrics appeared and val_loss improved -- which is precisely why this
    # cannot be quiet.
    split_sections = {}
    for want, key in (("val", val_key), ("test", test_key)):
        if not key:
            continue
        if key not in spec or not spec[key]:
            raise KeyError(
                f"{split_json} has no non-empty {key!r} for the {want!r} "
                f"split. Available keys: {sorted(spec)}. Refusing to continue, "
                f"because falling back to an in-section cell mask would draw "
                f"{want} cells from the TRAINING sections."
            )
        split_sections[want] = list(spec[key])

    overlap = (set(split_sections.get("val", ())) &
               set(split_sections.get("test", ())))
    if overlap:
        raise ValueError(
            f"val and test share {len(overlap)} section(s), e.g. "
            f"{sorted(overlap)[:3]}. Note `_splitspec.json`'s 'held_out' is "
            f"validation + test combined, so pass test_key='test', not "
            f"'held_out', when a separate val split is also in use."
        )

    cfg.setdefault("datamodule", {})["split_sections"] = split_sections
    n = {k: len(v) for k, v in split_sections.items()}
    print(f"Split from {split_json}: {n} (train = every other section).")
    return cfg


def _patch_streaming(
        cfg: dict,
        sections_per_block: int = 4,
        prefetch: bool = True,
        num_workers: int = 2,
        max_cells_per_block: Optional[int] = None,
    ) -> dict:
    """
    Switch the dataset backend from the collated in-memory blob to on-disk
    streaming.

    The in-memory backend loads every section into one resident graph; for
    hst_corpus_110m that object is ~0.6 TB, so it is not an option. Streaming
    stores one section per SQLite row and holds only `sections_per_block` at a
    time.

    Requires a blob built with `--build-blob-backend on-disk` (it lives in a
    separate gold subdir, `on-disk-PyG-dataset-blob/`, so it never collides
    with an in-memory blob of the same name).

    Parameters
    ----------
    sections_per_block:
        K — how many sections are co-resident and mixed per block. Mini-batches
        draw seed cells across all K, which is what gives the batch-correction
        losses cross-section signal; K=1 would put one section per mini-batch
        and remove that signal entirely. Budget roughly 2xK sections of memory
        (the concatenating copy coexists with the sources) plus one more block
        in flight when `prefetch` is on.
    prefetch:
        Build the next block on a background thread while the current one
        trains, so the GPU does not idle at block boundaries. Note this does
        NOT currently succeed at R0 scale — see the measurement in
        `REMAINING_WORK.md` and the postponed optimisation recorded there.
    num_workers:
        DataLoader workers per block loader, and the reason this parameter
        exists rather than inheriting `loader_params['num_workers']`.

        The streaming path creates a NEW `NeighborLoader` per block, so every
        block boundary forks `num_workers` processes against a block that is
        already resident in RAM — 2-4 GB at K=8 on R0. The in-memory backend
        forks once against one shared graph, so its default of 22 is fine
        there; here it is a per-boundary multiplier. R0 measured the cost
        directly: host RSS peaked at 96.5 GB and LSF killed the run at epoch
        35 of 80, while GPU utilisation sat at 8.1% — so those 22 workers
        bought no throughput whatsoever, they only bought the OOM.

        Default 2: enough to overlap the sampler with the training step,
        cheap enough that the fork cost and the resident copies stay small.
        The block read itself is already covered by `prefetch`, so workers
        are not the mechanism hiding disk latency here.
    """
    cfg["dataset"]["backend"] = "on-disk"
    cfg["datamodule"]["sections_per_block"] = int(sections_per_block)
    # Cap a block by CELLS too. `sections_per_block` alone does not bound
    # memory on a corpus: a block widens every section to the union of its
    # panels, and hst_corpus_110m's sections span 1,049 to 1,617,614 cells with
    # stored widths from 169 to 9,540. Measured over random shuffles, K=8 gives
    # a median block of 26.7 GB and a worst case of 144 GB -- a 96 GB run died
    # at 102 GB. Since the union can never exceed the vocabulary V, cells x V x
    # 4 bytes is a hard per-block ceiling, so a cell budget bounds memory by
    # construction. It also mixes BETTER than shrinking K: the median section is
    # 89,752 cells, so a budget sized for one large section admits several
    # typical ones, and section mixing is what supplies the batch-integration
    # signal. None keeps the pure section-count behaviour.
    cfg["datamodule"]["max_cells_per_block"] = (
        int(max_cells_per_block) if max_cells_per_block else None)
    cfg["datamodule"]["prefetch"] = bool(prefetch)
    cfg["datamodule"]["loader_params"]["num_workers"] = int(num_workers)
    return cfg


def _patch_no_val(
        cfg: dict,
        checkpoint_every_n_steps: int = 2000,
        save_top_k: Optional[int] = None,
    ) -> dict:
    """
    Disable the validation loop and checkpoint on a pure step schedule.

    WHY THIS IS DEFENSIBLE, AND ONLY IN ONE REGIME. A streaming val pass is
    expensive because the val loader has its own `KSectionBlockLoader` and
    rebuilds blocks across every section in the blob to reach the 10% of cells
    masked for validation. Extrapolating R0's measured 4.8 s/section, one
    corpus val pass is ~51 min, so even a derived 10-check cadence costs ~8.4 h
    against ~6.1 h of actual training on a 200,000-step budget. Validation
    would be the majority of the run.

    What validation buys is the ability to notice generalisation degrading. At
    corpus scale that risk is largely absent, for a concrete reason: 200,000
    steps at batch 512 is ~1.15 passes over the corpus, so **each cell is seen
    about once**. A model cannot memorise data it sees once. R0 corroborates
    the direction — train 2007.7 vs val 2043.7, a 1.8% gap with the two curves
    tracking each other, and that was after 36 passes, far more repetition than
    a corpus run gets.

    Note also that the base config's stated reason for monitoring `val_loss`
    ("matters in particular for the adversarial variants where train_loss can
    keep declining while val performance silently degrades") does not apply to
    the R0 reference stack, which has no adversarial term.

    Final evaluation does not depend on this either way: it comes from the
    WHOLE-SECTION test holdout via `examples/compute_inference_metrics.py`,
    which never touches the in-section val split.

    So: use this for corpus-scale, ~single-pass runs. Do NOT use it for
    R0-scale runs (~36 passes over 519k cells), where repetition makes
    overfitting real and a val pass costs ~3.1 min rather than ~51 min.

    WHY NOT `limit_val_batches=K` INSTEAD, which looks like the obvious middle
    ground. It would be cheap for the right reason — the block build dominates
    and Lightning stops consuming after K batches, so only the first block or
    two ever gets built. But stopping mid-iteration ABANDONS the block
    generator, and `KSectionBlockLoader._blocks()` runs its producer on a
    background thread writing into a `queue.Queue(maxsize=1)`. An abandoned
    generator leaves that producer blocked in `q.put()` holding a fully-built
    block, and the thread is a daemon so nothing reaps it: one leaked thread
    and up to two leaked blocks (~2-4 GB at K=8) per val pass. Fixing that
    means touching the prefetch threading, which is deliberately postponed
    (`REMAINING_WORK.md` item 0d). Disabling validation outright never iterates
    the val loader at all, so it sidesteps the leak instead of provoking it.

    Parameters
    ----------
    checkpoint_every_n_steps:
        Checkpoint cadence, in optimizer steps. `monitor` is set to None so
        ModelCheckpoint saves on this schedule alone rather than ranking by a
        metric — see the `monitor in (None, ...)` branch in `train()` for why
        `train_loss` cannot stand in for `val_loss` here.

        MEASURED, because the cadence looks alarming and is not: a checkpoint
        is 60 MB (R0's are 60,242,202 B) and an fsync'd 60 MB write to this
        Lustre filesystem takes 0.19 s. Each event writes the tracked file and
        refreshes `last.ckpt`, so on a 200,000-step corpus run:

            every  1,000 steps = 200 events = 1.2 min = 0.34% of a 6.1 h run
            every  2,000 steps = 100 events = 0.6 min = 0.17%
            every 10,000 steps =  20 events = 0.1 min = 0.03%

        Disk does not accumulate either: with `save_top_k=1` and no monitor,
        `_save_none_monitor_checkpoint` deletes the previous file after saving
        the new one (pytorch_lightning 2.4.0, model_checkpoint.py:717-718), so
        the directory holds 2 files (~120 MB), not one per event.

        So pick this from CRASH EXPOSURE, not from write cost: the cadence is
        what you lose when a job dies, and `stream+r0` dying at epoch 35 after
        111 min shows that is the live risk. At 2,000 the exposure is ~3.7 min
        of training. There is no reason to economise here — for scale, the val
        pass this patch removes was 8.4 h, three orders of magnitude larger.
    save_top_k:
        Override `checkpoint_params['save_top_k']`. Leave as None to keep the
        base config's 1 (rolling: newest kept, previous deleted). Pass -1 to
        retain every save, which is what a test wants — with save_top_k=1 the
        deletions make a working step schedule indistinguishable from one that
        only fired once.
    """
    cfg["trainer"]["limit_val_batches"] = 0
    cfg["trainer"]["monitor"] = None
    cfg["trainer"]["checkpoint_params"]["every_n_train_steps"] = int(
        checkpoint_every_n_steps
    )
    if save_top_k is not None:
        cfg["trainer"]["checkpoint_params"]["save_top_k"] = int(save_top_k)
    # EarlyStopping keys off `val_loss`, and by default it evaluates at
    # validation end -- with no validation it would simply never fire, so
    # leaving it "enabled" would be a lie in the archived config.
    cfg["trainer"]["early_stopping_params"]["enabled"] = False
    return cfg


def _patch_no_logging(cfg: dict) -> dict:
    """
    Turn off wandb entirely for this run (`cfg['logging']['enabled'] = False`,
    consumed at `train()`'s logger block).

    For MEASUREMENT runs -- short comparisons whose output is a number, not a
    tracked experiment -- wandb contributes nothing and adds a failure mode.
    `WANDB_MODE=offline` is not enough: wandb still spawns a local service
    process, and on a busy node that can miss its 30 s startup window and take
    the whole job with it:

        wandb.sdk.lib.service.service_port_file.ServicePollForTokenError:
        Failed to read port info after 30.0 seconds

    which is exactly how `smoke-xpanel-holdout+xhc38-4b_1p` died on its first
    submission, after the model had already been built correctly. Nothing to do
    with training; just an avoidable dependency.

    Deliberately NOT applied to the existing `smoke-xpanel+*` variants: those
    have already been run, and changing their resolved config would make the
    runs on disk irreproducible from the registry.
    """
    cfg["logging"]["enabled"] = False
    return cfg


def _patch_step_budget(
        cfg: dict,
        max_steps: int,
        val_check_interval: Optional[int] = None,
        val_checks: int = 10,
        max_epochs: int = 1000,
        heartbeat_every_n_steps: int = 0,
    ) -> dict:
    """
    Train to a STEP budget rather than an epoch count.

    Epochs stop being a meaningful unit once the dataset is large. The base
    config's `max_epochs: 80` was calibrated on paper-scale data (a handful of
    sections); carried across a 212x increase in cells it becomes absurd:

        R0 (519,058 cells, batch 512)      817 steps/epoch
          36 epochs actually completed  =  29,422 steps
        hst_corpus_110m (~89M train seeds, batch 512)
          ONE epoch                     = 174,023 steps
          80 epochs                     = ~14,000,000 steps

    One corpus epoch is already 5.9x the total step count R0 ran, and R0's
    29,422 steps is reached after 16.9% of a single corpus pass. So the budget
    that matters is steps seen, and a corpus run is a fraction of one epoch.

    The budget is therefore PER DATASET, not a global convention — the same
    step count means completely different things (steps x batch / train seeds):

        200,000 steps @ batch 512   =   1.15 passes over the corpus
                                    = 244.71 passes over R0

    ~200k is the natural corpus figure: a little over one pass, which is where
    a corpus run belongs. On R0 it would be 245 passes of a dataset that
    plateaued after 36 (29,422 steps, both first-level codebooks already fully
    occupied), so R0-scale runs want ~30k. Set the ceiling generously and let
    EarlyStopping on `val_loss` decide the real endpoint.

    Three settings, and all three are load-bearing:

    `max_steps`
        The actual budget. Lightning stops at whichever of max_steps /
        max_epochs comes first.
    `max_epochs`
        Raised out of the way (default 1000) so it cannot bind first. Left as
        a finite number rather than -1 so a misconfigured run still terminates.
    `val_check_interval` + `check_val_every_n_epoch = 1`
        Validation MUST become step-grained. DERIVED from `max_steps` by
        default (`max_steps // val_checks`) rather than being a fixed number,
        because a fixed interval silently becomes pathological as the budget
        grows and the two must not drift apart.

        The cost driver is that the val loader has its own
        `KSectionBlockLoader` and rebuilds blocks across every section in the
        blob to reach the 10% of cells masked for validation — 39 sections at
        R0, all 636 at corpus scale. Extrapolating R0's measured build rate of
        ~4.8 s/section, one corpus val pass is ~51 min, against a
        training-only rate of ~0.11 s/step. So for a 200,000-step corpus run:

            val every  2,000 steps = 100 passes = 84.3 h of validation
                                                   vs 6.1 h of training
            val every 20,000 steps =  10 passes =  8.4 h

        A fixed 2,000 would have spent 14x more wall clock validating than
        training. Deriving the interval keeps the ratio bounded whatever the
        budget: `val_checks=10` gives 10 passes, and EarlyStopping's
        patience=10 then spans the whole run (tighten `val_checks` if you want
        early stopping to be able to fire sooner).

        Even 10 passes is ~8.4 h at corpus scale, which is real and is NOT
        hidden here: if that matters, `limit_val_batches` is the other lever,
        at the cost of biasing validation towards the first blocks (the val
        loader is built with `shuffle=False`, so it would always be the same
        leading sections rather than a random sample). A corpus step budget is a
        fraction of one epoch, so an epoch-grained cadence would never fire —
        and because `ModelCheckpoint._should_save_on_train_epoch_end()`
        returns False whenever `check_val_every_n_epoch != 1`
        (pytorch_lightning 2.4.0, model_checkpoint.py:425-432), the callback
        saves only from `on_validation_end`. The run would then finish having
        logged no `val_*` metric and written an EMPTY `checkpoints/`, leaving
        nothing for `--predict`. That is the same failure `_patch_quick`
        already had to fix for 1-epoch smoke tests, arriving from a different
        direction. EarlyStopping counts `patience` in val checks, so it keeps
        working on the step grid.

    Safe to apply to the R0 reference stack: it is entirely epoch-agnostic —
    `warmup_epochs: 0`, `vq_warmup_epochs` 0, no LR scheduler (plain
    constant-LR Adam) and static loss temperatures, so nothing silently
    changes schedule when the epoch count does. NOT safe for the
    `adv-warmup10` variants, whose adversarial alpha ramp is keyed to
    `current_epoch` and would never complete under a short budget.
    """
    if val_check_interval is None:
        val_check_interval = max(1, int(max_steps) // max(1, int(val_checks)))
    cfg["trainer"]["max_steps"] = int(max_steps)
    # Periodic step log. A corpus epoch is 187,562 steps, so the whole
    # budget finishes inside epoch 0 and nothing else prints between the
    # epoch banners -- a healthy run looks identical to a hung one.
    cfg["trainer"]["heartbeat_every_n_steps"] = int(heartbeat_every_n_steps)
    cfg["trainer"]["max_epochs"] = int(max_epochs)
    # MUST be None, not 1. With an int here Lightning treats
    # `val_check_interval` as an offset WITHIN one epoch and hard-raises if it
    # exceeds the batches per epoch:
    #
    #   ValueError: `val_check_interval` (1000) must be less than or equal to
    #   the number of the training batches (821). ... If you want to validate
    #   based on the total training batches, set `check_val_every_n_epoch=None`
    #
    # (measured on `smoke-steps+stream+xhs1000-39b_1p`, R0 = 821 train batches
    # per epoch.) None switches it to the GLOBAL step grid, which is the whole
    # point of a step budget — the interval must be free to span epoch
    # boundaries. Note this bites hardest at SMALL scale: R0 has 821
    # batches/epoch so any interval above that is rejected, whereas one corpus
    # epoch is ~174,023 steps and would have swallowed the same setting
    # silently. `val_check_interval` must be an int when this is None, which
    # the line below guarantees.
    cfg["trainer"]["check_val_every_n_epoch"] = None
    cfg["trainer"]["val_check_interval"] = int(val_check_interval)
    return cfg


def _patch_dual_mmb20(
        cfg: dict,
        train_adata_ids: Optional[List[int]] = None,
        test_adata_ids: Optional[List[int]] = None,
        batch_size: int = 64,
        edge_sampling_ratio: float = 1.0,
        gnn_layers: int = 1,
        sampler_neighbors: List[int] = [8],
        nbr_aggregation_hops: int = 1,
    ) -> dict:
    """
    Swap the dataset to /lustre/.../silver/mmb0-1b_smb1-20b_1p_shared_genes
    (1 MERFISH + 20 STARmap, all reindexed to MERFISH's gene panel by
    examples/harmonize_mmb_smb20_panels.py) and configure whole-section
    holdout via SpatialBatchSplit.

    Parameters
    ----------
    train_adata_ids, test_adata_ids:
        `adata_batch_id` values (= the integer parsed out of `uns['batch']`
        that the harmonize script stamps from the filename), NOT
        positions in the sorted blob. STARmap sections are 1..20; MERFISH
        is 82. `SpatialBatchSplit` matches `data.adata_batch_id` against
        these lists, so they MUST be the actual ids — passing positions
        would silently produce no masks for any section and trigger a
        `KeyError: 'train_mask'` at the next collate step.

        Two buckets:
          - `train_adata_ids`: sections used for gradient updates. The
                               base config's 10% cell-level split runs
                               on these and provides the val signal for
                               early stopping + best-checkpoint
                               selection.
          - `test_adata_ids`:  sections held out ENTIRELY from training
                               (NOT loaded into the training data graph
                               at all — excluded from `adata_batch_idx`).
                               Evaluated post-hoc by `predict()` + the
                               inference metrics pipeline. Default
                               `None` = no test set.

        `cfg['dataset']['adata_batch_idx']` is position-based (used by
        `initialize_databatch` to slice the dataset blob); the
        translation from ids to positions happens at runtime in
        `train()` once the blob is loaded.

    batch_size, edge_sampling_ratio:
        Memory tuning. mmb20 stays on the 431-gene MERFISH panel so
        per-step activations are similar to mmb-smb, but it keeps
        ~19 sections worth of static `x` + edge_index on GPU (~2.5 GiB
        more than mmb-smb's 2 sections) before sampling starts. The
        edge sampling ratio caps the negative-sampling memory in the
        cosine adjacency loss.

    gnn_layers, sampler_neighbors, nbr_aggregation_hops:
        Niche-branch depth knobs that move TOGETHER. The mmb20 default
        narrows back to single-hop (gnn=1, sampler=[8], nbr_hops=1)
        instead of the `+wide` 2-hop stack. The big memory win is the
        sampler depth: `[8, 8]` worst-case expands 64 seeds → 64·(1+8+64)
        = 4,672 sampled nodes; `[8]` expands to 64·(1+8) = 576 — almost
        an order of magnitude smaller everywhere downstream (input `x`,
        MLP/GNN activations, decoder activations, NB loss intermediates,
        cosine-adjacency pairs). The cost is less spatial context per
        cell (1-hop instead of 2-hop niche), but that's NicheCompass's
        default and the integration question (cross-platform retrieval)
        doesn't fundamentally need 2-hop context.

        These three are coupled — narrowing one without the others is
        wasteful: a 2-hop sampler feeding a 1-hop GNN samples neighbors
        the GNN won't use; a 2-hop nbr-target aggregation on a 1-hop
        sampler is approximate (computed on incomplete neighbour info).

    Reads `obs['batch']` (also stamped by the harmonize script) for the
    per-cell batch one-hots used by the encoder/decoder batch correction
    (FiLM, decoder covariate, adversarial head).
    """
    test_adata_ids = list(test_adata_ids) if test_adata_ids else []
    # train_adata_ids is OPTIONAL — when omitted train() defaults to
    # every blob section not in test/val.
    train_adata_ids = list(train_adata_ids) if train_adata_ids else []

    cfg["dataset"]["dataset_name"]   = "mmb0-1b_smb1-20b_1p_shared_genes"
    cfg["dataset"]["dataset_tag"]    = "mmb0-1b_smb1-20b_1p"
    cfg["dataset"]["root_data_dir"]  = "/lustre/scratch126/cellgen/lotfollahi/DATASETS"
    # `adata_batch_idx` (positions) is resolved at runtime in `train()`
    # from `train_batches` (ids) via the loaded blob's id-to-position
    # mapping. Test sections are deliberately excluded — they never
    # enter the training data graph; `predict()` builds its own dataset
    # from the silver dir and CAN run inference on them post-hoc.
    cfg["dataset"]["adata_batch_idx"] = []

    cfg["dataset"]["train_transform_params"] = {
        "region":         None,
        "train_batches":  list(train_adata_ids),
        "val_batches":    [],   # no whole-section val; cell-level split provides val
        # `test_batches` here is INFORMATIONAL — these sections aren't
        # in `adata_batch_idx` so the data graph never contains them and
        # `SpatialBatchSplit.forward` is never called on them at training
        # time. We stamp the ids anyway so `predict()` reads them from
        # the saved config and tags inference cells with
        # `data_split = "test"` for stratified post-hoc metrics.
        "test_batches":   test_adata_ids,
        "xy_key":         "xy_coordinates",
        # Cell-level 10% split provides the val signal during training.
        "train_val_cell_split":
            cfg["dataset"]["train_transform_params"].get("train_val_cell_split", 0.10),
        "cell_split_seed":
            cfg["dataset"]["train_transform_params"].get("cell_split_seed", 0),
    }

    cfg["dataset"]["graph_params"]["batch_key"] = "batch"

    # Memory tuning (see docstring). Defaults are conservative for a
    # 44 GiB GPU; bump them on bigger cards.
    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio
    )

    # Niche-branch depth (GNN / sampler / nbr-target aggregation move
    # together — see docstring). These OVERRIDE whatever `_patch_dual_wide`
    # set upstream, since this patch is applied last in the variant chain.
    cfg["model"]["encoder_params"]["gnn_params"]["num_layers"] = int(gnn_layers)
    cfg["datamodule"]["sampler_params"]["num_neighbors"] = list(sampler_neighbors)
    cfg["model"]["loss_params"]["loss_kwargs"]["nbr_aggregation_hops"] = int(
        nbr_aggregation_hops
    )

    # Validation runs on the held-out section(s); name the monitor
    # accordingly so checkpoints are saved on test-set Pearson.
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_dual_spatch_subset(
        cfg: dict,
        dataset_name: str,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
        *,
        root_data_dir: str = "/nfs/team361/sb75/DATASETS",
    ) -> dict:
    """
    Generic dataset-switch helper for the spatch tissue-specific subsets
    (spatch_ov_1p, spatch_hcc_1p, spatch_coad_1p). Mirrors
    `_patch_dual_spatch` exactly but takes `dataset_name` as an argument
    so the same chain works across the 3 subsets.

    Defaults differ from the original `_patch_dual_spatch` smoke-test
    helper:
      - `batch_size=512` (matches the s51 spine; the smoke-test default
        of 16 was for fast iteration on a tiny GPU).
      - `train_batch_idx=None` -> "every blob section not in test/val"
        (i.e. train on ALL sections in the subset). The smoke-test
        defaulted to `[2]` because it wanted a single training
        section for fast iteration.

    Parameters
    ----------
    dataset_name : str
        One of "spatch_ov_1p", "spatch_hcc_1p", "spatch_coad_1p". Sets
        `cfg.dataset.dataset_name`, `dataset_tag`, and the artifact path.
    train_batch_idx, test_batch_idx
        Section-ID lists. Same semantics as `_patch_dual_spatch`. Both
        default to empty/unset, which `train()` resolves to "all
        sections for training, none held out". Pass an explicit list
        to do whole-section holdout.
    batch_size, edge_sampling_ratio
        Memory tuning; defaults match the s51 spine.
    root_data_dir
        Keyword-only, defaulting to the historical hardcoded value so the
        existing spatch wrappers are unchanged. Override for datasets living
        under a different DATASETS root (e.g. the R0 skin target).
    """
    train_batch_idx = list(train_batch_idx) if train_batch_idx else []
    if test_batch_idx is None:
        test_batch_idx = []
    if isinstance(test_batch_idx, str):
        if test_batch_idx != "rest":
            raise ValueError(
                f"test_batch_idx string must be 'rest'; got {test_batch_idx!r}."
            )
    else:
        test_batch_idx = list(test_batch_idx)

    cfg["dataset"]["dataset_name"]   = dataset_name
    cfg["dataset"]["dataset_tag"]    = dataset_name
    cfg["dataset"]["root_data_dir"]  = root_data_dir
    cfg["dataset"]["adata_batch_idx"] = []

    cfg["dataset"]["train_transform_params"] = {
        "region":         None,
        "train_batches":  list(train_batch_idx),
        "val_batches":    [],
        "test_batches":   test_batch_idx,
        "xy_key":         "xy_coordinates",
        "train_val_cell_split":
            cfg["dataset"]["train_transform_params"].get("train_val_cell_split", 0.10),
        "cell_split_seed":
            cfg["dataset"]["train_transform_params"].get("cell_split_seed", 0),
    }

    cfg["dataset"]["graph_params"]["batch_key"] = "batch"

    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio
    )

    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_dual_xhc38_4b(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Cross-panel dataset switch: xhc38-4b_1p (Xenium human colon, 4 sections).

    Panels 419 and 319 with 319 a strict SUBSET of 419, so the block union is
    419 and 100 genes are masked for the two 319-gene sections. Batch ids and
    their panels: batch0=319, batch1=419, batch2=419, batch3=319.

    Whichever holdout is chosen must leave BOTH panels in training -- otherwise
    every block carries one panel, the mask is all-ones and the run does not
    exercise cross-panel at all while appearing to succeed.

    Unannotated dataset (`label_names=[]` at build): `SetExperimentDataKeys`
    returns a 0-channel `y` (transforms.py:699), giving `num_classes=0`, which
    is fine because the reference recipe registers no cross-entropy. Label-based
    benchmark metrics are therefore not computable here.
    """
    return _patch_dual_spatch_subset(
        cfg,
        dataset_name="xhc38-4b_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
        root_data_dir=str(DATA_ROOT),
    )


def _patch_dual_xhb42_3b(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 256,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Cross-panel dataset switch: xhb42-3b_1p (Xenium human brain, 3 sections).

    The HARD case -- panels 350 and 317 with intersection 262, so 88 genes are
    exclusive to one and 55 to the other and the union (405) is strictly WIDER
    than either panel. `xhc38-4b_1p` cannot catch a bug that takes `max()`
    instead of a real union; this can. Batch ids: batch0=350, batch1=317,
    batch2=317.

    Only 109,007 cells, hence the smaller default batch. Unannotated, same as
    `_patch_dual_xhc38_4b`.
    """
    return _patch_dual_spatch_subset(
        cfg,
        dataset_name="xhb42-3b_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
        root_data_dir=str(DATA_ROOT),
    )


def _patch_dual_hst_corpus(
        cfg: dict,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Dataset switch to the full corpus: hst_corpus_110m, 636 sections,
    112,578,039 cells, cross-panel vocabulary V = 9,574.

    Deliberately takes NO `train_batch_idx` / `test_batch_idx`, unlike every
    other `_patch_dual_*`. Those select sections by `adata_batch_id`, which is
    parsed from `uns['batch']` -- a within-dataset counter that names up to 80
    unrelated corpus sections at once. Splits here come from
    `--split-sections-json`, which names sections by REL PATH (the only
    corpus-unique identifier) and applies them as whole-section train/val/test
    via `split_sections`. Passing batch indices as well would silently
    intersect the two.

    `train_transform_params` is therefore left empty: no `SpatialBatchSplit`,
    no in-section cell masks. `OnDiskStreamingDataModule._make_loader` drops
    the mask attribute entirely for any split named by `split_sections`, and
    refuses to fall back to a cell mask for one that is not.
    """
    cfg["dataset"]["dataset_name"]    = DATASET_NAME
    cfg["dataset"]["dataset_tag"]     = DATASET_NAME
    cfg["dataset"]["root_data_dir"]   = str(DATA_ROOT)
    # Declared, and enforced in `train()`. Forgetting the split spec would
    # train on all 636 sections while every log line still looked healthy, and
    # the resulting model could support no zero-shot claim -- a full overnight
    # run silently spent on the wrong thing. Refuse instead.
    cfg["dataset"]["requires_split_sections"] = True
    cfg["dataset"]["adata_batch_idx"] = []
    cfg["dataset"]["train_transform_params"] = {}
    # The blob was built with batch_key='dataset_batch'; the loader reads the
    # labels from the manifest, so this only records the intent.
    cfg["dataset"]["graph_params"]["batch_key"] = "batch"
    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio)
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_dual_xhs1000_39b(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    R0 dataset switch: xhs1000-39b_1p (Xenium human skin, 39 sections).

    Thin wrapper over `_patch_dual_spatch_subset` -- the holdout mechanics
    (SpatialBatchSplit whole-section test holdout plus a 10% in-section cell
    split for val/early-stopping) are identical, only the dataset and root
    differ.

    `train_batch_idx` / `test_batch_idx` are `adata_batch_id` VALUES (the
    integer parsed out of `uns['batch']`, i.e. batchN -> N), NOT positions in
    the sorted blob. `SpatialBatchSplit` matches `data.adata_batch_id` against
    these lists, so passing positions silently produces no masks for any
    section and then fails with `KeyError: 'train_mask'` at collate. Empty
    `train_batch_idx` means "every section not held out".
    """
    return _patch_dual_spatch_subset(
        cfg, dataset_name="xhs1000-39b_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
        root_data_dir=str(DATA_ROOT),
    )


def _patch_dual_spatch_ov(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """Thin wrapper: spatch_ov_1p (ovarian cancer subset)."""
    return _patch_dual_spatch_subset(
        cfg, dataset_name="spatch_ov_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
    )


def _patch_dual_spatch_hcc(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """Thin wrapper: spatch_hcc_1p (hepatocellular carcinoma subset)."""
    return _patch_dual_spatch_subset(
        cfg, dataset_name="spatch_hcc_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
    )


def _patch_dual_spatch_coad(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """Thin wrapper: spatch_coad_1p (colon adenocarcinoma subset)."""
    return _patch_dual_spatch_subset(
        cfg, dataset_name="spatch_coad_1p",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
    )


def _patch_dual_squint_hln(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 1.0,
        max_epochs: int = 400,
    ) -> dict:
    """Thin wrapper: squint_hln (CosMx human lymph node, manual niches from
    the spatial-niche-benchmark study; SINGLE section). Defaults train on all
    sections in the blob (here, the one section) with a 10% cell-level val
    split and no whole-section holdout. Labels are read from the original obs
    columns via the blob's label_names (cell_type_annotation / niche_annotation).

    max_epochs defaults to 400 (vs the base config's 80) — the single small
    section gives few gradient steps/epoch, so a larger budget is warranted.
    NOTE: early stopping (patience=10 val passes) can still stop earlier;
    disable it (trainer.early_stopping_params.enabled=False) to force all 400."""
    cfg = _patch_dual_spatch_subset(
        cfg, dataset_name="squint_hln",
        train_batch_idx=train_batch_idx,
        test_batch_idx=test_batch_idx,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
    )
    return _patch_dual_max_epochs(cfg, max_epochs=max_epochs)


def _patch_dataset_name(cfg: dict, dataset_name: str) -> dict:
    """Redirect a fully-built TRAINING cfg to a different dataset blob by name.

    Only `cfg["dataset"]["dataset_name"]` / `["dataset_tag"]` are changed (the
    pair `_patch_dual_spatch_subset` writes), so the loaded blob (and its
    `silver/<dataset_name>/` source + artifact path) switches while the MODEL
    config stays byte-identical. Used to run the squint_hln FiLM-scale reference
    on the gene-filtered blobs (squint_hln_allgenes / _hvg2k / _svg2k) produced
    by `analysis/data_preparation/preprocess_squint_hln.py`. The model's gene
    input dim is derived from the blob at runtime, so a 2000-gene blob just
    works without any config change."""
    cfg["dataset"]["dataset_name"] = dataset_name
    cfg["dataset"]["dataset_tag"]  = dataset_name
    return cfg


def _patch_dual_spatch(
        cfg: dict,
        train_batch_idx: Optional[List[int]] = None,
        test_batch_idx=None,
        batch_size: int = 16,
        edge_sampling_ratio: float = 1.0,
    ) -> dict:
    """
    Swap the dataset to /nfs/team361/sb75/DATASETS/silver/spatch_1p
    (N AnnDatas named `dataset_id_<N>.h5ad`) for the spatch_1p smoke
    test. Configures whole-section holdout via `SpatialBatchSplit`.

    Defaults match the user-described smoke-test setup:
      train: `dataset_id_2.h5ad`  (adata_batch_id = 2)
      test : every OTHER section in the spatch_1p silver dir (sentinel
             value `"rest"` resolved at runtime in `train()` once
             the blob is loaded — see the train_batches resolution
             block).

    Parameters
    ----------
    train_batch_idx
        `adata_batch_id` values used for gradient updates. Default
        `[2]` — picks `dataset_id_2.h5ad` as the sole training
        section.
    test_batch_idx
        Either an explicit list of ids OR the sentinel string
        `"rest"` (default). `"rest"` is resolved at runtime to
        "every blob section not in train/val" (mirrors the
        existing train-side auto-default).
    batch_size, edge_sampling_ratio
        Memory tuning. The smoke test uses a small batch_size
        (16) and conservative edge sampling so the run fits on
        a small GPU and finishes quickly. Bump for full training.

    Labels (set in `make_dataset_blob_config_spatch`):
      cell-type label  -> obs['annotation']
      niche / spatial  -> obs['spatial_cluster']
    """
    if train_batch_idx is None:
        train_batch_idx = [2]
    train_batch_idx = list(train_batch_idx)

    # Accept either an explicit list OR the sentinel string "rest".
    # The sentinel survives yaml round-trips and is resolved at
    # runtime inside `train()` (see the train_batches resolution
    # block) into "all blob ids not in train+val".
    if test_batch_idx is None:
        test_batch_idx = "rest"
    if isinstance(test_batch_idx, str):
        if test_batch_idx != "rest":
            raise ValueError(
                f"test_batch_idx string must be 'rest'; got "
                f"{test_batch_idx!r}. Use a list for explicit ids."
            )
    else:
        test_batch_idx = list(test_batch_idx)

    cfg["dataset"]["dataset_name"]   = "spatch_1p"
    cfg["dataset"]["dataset_tag"]    = "spatch_1p"
    cfg["dataset"]["root_data_dir"]  = "/nfs/team361/sb75/DATASETS"
    # `adata_batch_idx` (positions) is resolved at runtime in
    # `train()` from `train_batches` (ids) via the blob's
    # id-to-position mapping.
    cfg["dataset"]["adata_batch_idx"] = []

    cfg["dataset"]["train_transform_params"] = {
        "region":         None,
        "train_batches":  list(train_batch_idx),
        "val_batches":    [],   # no whole-section val; cell-level split provides val
        "test_batches":   test_batch_idx,   # explicit list OR "rest" sentinel
        "xy_key":         "xy_coordinates",
        "train_val_cell_split":
            cfg["dataset"]["train_transform_params"].get("train_val_cell_split", 0.10),
        "cell_split_seed":
            cfg["dataset"]["train_transform_params"].get("cell_split_seed", 0),
    }

    # spatch_1p AnnDatas are expected to carry obs['batch']; falls back to
    # cell_id parse if absent (same defensive path as chl59).
    cfg["dataset"]["graph_params"]["batch_key"] = "batch"

    # Memory tuning. Defaults are conservative for a smoke test.
    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    cfg["model"]["loss_params"]["loss_kwargs"]["edge_sampling_ratio"] = float(
        edge_sampling_ratio
    )

    # Validation runs on the cell-level 10% split inside the single
    # training section; the held-out test sections are evaluated by
    # `predict()` post-hoc.
    cfg["trainer"]["monitor"] = "val_loss"
    return cfg


def _patch_dual_mmb239(
        cfg: dict,
        train_adata_ids: Optional[List[int]] = None,
        test_adata_ids: Optional[List[int]] = None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 2.0,
        gnn_layers: int = 1,
        sampler_neighbors: List[int] = [16],
        nbr_aggregation_hops: int = 1,
    ) -> dict:
    """
    Swap the dataset to /lustre/.../silver/mmb0-239b_1p (239 MERFISH
    mouse-brain sections, single shared gene panel). Reuses
    `_patch_dual_mmb20`'s whole-section split + memory + niche-depth wiring,
    then overrides the dataset identity (no `_shared_genes` suffix — these
    are all MERFISH, so no panel harmonization is needed).

    Defaults reproduce the s49_v23 config faithfully (bs=512, edge_sampling
    2.0, gnn=1, sampler=[16], nbr_hops=1). With `train_adata_ids=None` and
    `test_adata_ids=None`, train() trains on EVERY section in the blob and
    the base config's 10% cell-level split provides the val signal (no
    whole-section test holdout).

    !! SCALE WARNING: 239 sections is ~12x the mmb20 run, whose static graph
    (x + edge_index for all training sections) already sat resident on the
    GPU. At bs=512 this WILL be extremely memory-heavy and is likely to OOM
    except on the largest GPUs. If it OOMs, dial down (in this order):
    `batch_size` (256 -> 128 -> 64), `edge_sampling_ratio` (-> 1.0/0.5),
    `sampler_neighbors` ([16] -> [8]). All are exposed as parameters here.
    """
    cfg = _patch_dual_mmb20(
        cfg,
        train_adata_ids=train_adata_ids,
        test_adata_ids=test_adata_ids,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
        gnn_layers=gnn_layers,
        sampler_neighbors=sampler_neighbors,
        nbr_aggregation_hops=nbr_aggregation_hops,
    )
    # Override the dataset identity set by _patch_dual_mmb20 (which targets
    # the harmonized mmb20 dir). mmb0-239b_1p is read directly from the
    # silver dir of the same name under root_data_dir.
    cfg["dataset"]["dataset_name"] = "mmb0-239b_1p"
    cfg["dataset"]["dataset_tag"]  = "mmb0-239b_1p"
    return cfg


def _patch_dual_smb1_20b(
        cfg: dict,
        train_adata_ids: Optional[List[int]] = None,
        test_adata_ids: Optional[List[int]] = None,
        batch_size: int = 512,
        edge_sampling_ratio: float = 2.0,
        gnn_layers: int = 1,
        sampler_neighbors: List[int] = [16],
        nbr_aggregation_hops: int = 1,
    ) -> dict:
    """
    Swap the dataset to /nfs/team361/sb75/DATASETS/silver/smb1-20b_1p (the
    STARmap+ mouse-CNS 20-section dataset — the STARmap-ONLY counterpart to
    mmb20, on its native panel). Reuses `_patch_dual_mmb20`'s whole-section
    split + memory + niche-depth wiring, then overrides the dataset identity
    AND the data root (team361, not lustre — no `_shared_genes` suffix since
    there's no MERFISH to harmonize against).

    Defaults reproduce the s49_v23 / s57 spine faithfully (bs=512,
    edge_sampling=2.0, gnn=1, sampler=[16], nbr_hops=1) — matches
    `_patch_dual_mmb239`. With `train_adata_ids=None` and
    `test_adata_ids=None`, train() trains on EVERY section in the blob and
    the base config's 10% cell-level split provides the val signal (no
    whole-section test holdout — i.e. produce SQUINT codes across all 20
    sections).

    !! MEMORY: 20 sections is ~mmb20 scale (mmb20's conservative default was
    bs=64). bs=512 keeps the static graph (x + edge_index for all 20
    sections) resident on the GPU and is memory-heavy — faithful to the
    spine but needs a large-memory GPU. If it OOMs, dial down (in order):
    `batch_size` (256 -> 128 -> 64), `edge_sampling_ratio` (-> 1.0),
    `sampler_neighbors` ([16] -> [8]). All are exposed as parameters.
    """
    cfg = _patch_dual_mmb20(
        cfg,
        train_adata_ids=train_adata_ids,
        test_adata_ids=test_adata_ids,
        batch_size=batch_size,
        edge_sampling_ratio=edge_sampling_ratio,
        gnn_layers=gnn_layers,
        sampler_neighbors=sampler_neighbors,
        nbr_aggregation_hops=nbr_aggregation_hops,
    )
    # Override the dataset identity (mmb20 targets the harmonized lustre
    # dir) -> smb1-20b_1p is read directly from its silver dir on team361.
    cfg["dataset"]["dataset_name"]  = "smb1-20b_1p"
    cfg["dataset"]["dataset_tag"]   = "smb1-20b_1p"
    cfg["dataset"]["root_data_dir"] = "/nfs/team361/sb75/DATASETS"
    return cfg


def _patch_dual_mmb20_holdout(cfg: dict) -> dict:
    """
    Convenience holdout for the mmb20 cross-platform retrieval experiment.

    Train: STARmap+ batches 1..14 + 16..20 (19 sections used for gradient
           updates).
    Val  : (empty) — no whole-section val. The base config's cell-level
           10% in-section sample of the 19 training sections is the
           in-distribution early-stopping / best-ckpt-on-val signal.
    Test : STARmap+ batch 15 (within-platform baseline; matches the
           STARmap section held out in the smaller mmb-smb experiment)
           AND MERFISH batch 82 (cross-platform OOD). Both are TRUE
           held-outs — not loaded into the training data graph at all.
           They are evaluated post-hoc by the predict +
           compute_inference_metrics pipeline (which loads every file
           in the silver dir, runs inference, and computes Pearson /
           NMI / ARI / batch-integration metrics on every section). The
           training pipeline itself never sees them.

    These are `adata_batch_id` values (not positions); see
    `_patch_dual_mmb20` for the rationale.

    Only `test_adata_ids` is specified — `train()` defaults to "every
    blob section that isn't in test/val", so STARmap+ batches 1..14 +
    16..20 (19 sections) get used for training automatically.
    """
    return _patch_dual_mmb20(
        cfg,
        test_adata_ids=[15, 82],
    )


def _patch_holdout_regions(
        cfg: dict,
        regions: Optional[dict] = None,
    ) -> dict:
    """
    Hold out spatially-contiguous patches WITHIN sections for the
    gene-reconstruction downstream task (NicheCompass / MLGenX-style).

    Both source sections stay in the train graph (so the model still
    sees most of each tissue), but the cells inside the per-batch
    `regions` rectangle become `test_mask=True` and are masked OUT of
    the training loss. At inference, predict() runs over every cell and
    `compute_inference_metrics.py` reports stratified Pearson on the
    `data_split == "test"` subset only.

    Parameters
    ----------
    regions : dict[int, dict]
        Mapping `adata_batch_id -> region_spec`. Each region spec can mix
        absolute keys (`x_min`, `x_max`, `y_min`, `y_max`) and percentile
        keys (`x_min_pct`, `x_max_pct`, `y_min_pct`, `y_max_pct`,
        fractions of the section's xy range). Percentile keys are
        recommended for cross-platform datasets where MERFISH and
        STARmap+ live in very different coordinate frames — the same
        spec then resolves to a comparable patch in both.

        Default: a central 25% × 25% patch in batch 15 (STARmap+ MB) and
        a different central-but-shifted 25% × 25% patch in batch 82
        (MERFISH MB), so the two held-outs cover different anatomical
        zones across platforms. Override to pick specific anatomical
        landmarks (e.g. cortex, hippocampus).

    Notes
    -----
    - Independent from `test_batches` (which holds out whole sections).
    - The base mmb-smb config has `train_batches=[15, 82]` and empty
      `test_batches`, which is exactly what this patch needs — both
      batches stay in training, just with cell-level masks applied.
    - Predict() reads `test_regions` from the saved config and tags
      held-out cells with `obs["data_split"] = "test"` so post-inference
      Pearson stratifies correctly.
    """
    # Default regions: rectangular patches near the centre of each
    # section (STARmap+ batch 15 and MERFISH batch 82 live in different
    # coordinate frames, so percentile-based regions resolve cleanly in
    # both).
    if regions is None:
        regions = {
            # STARmap+ batch 15: a patch in the upper-left quadrant.
            15: {
                "x_min_pct": 0.10, "x_max_pct": 0.35,
                "y_min_pct": 0.55, "y_max_pct": 0.80,
            },
            # MERFISH batch 82: a patch in the lower-right quadrant
            # (different anatomical region than batch 15's hold-out).
            82: {
                "x_min_pct": 0.55, "x_max_pct": 0.80,
                "y_min_pct": 0.20, "y_max_pct": 0.45,
            },
        }

    cfg["dataset"]["train_transform_params"]["test_regions"] = {
        int(k): v for k, v in regions.items()
    }
    return cfg


def _patch_dual_adj_on_zqniche(cfg: dict) -> dict:
    """
    Switch the adjacency BCE input from continuous z_gnn (default) to the
    quantized z_q_niche. Lets you A/B continuous vs. quantized adjacency
    while keeping every other knob fixed.
    """
    cfg["model"]["loss_params"]["loss_kwargs"]["adj_loss_input"] = "z_q_niche"
    return cfg


def _patch_quick(
        cfg: dict,
        max_epochs: int = 1,
        batch_size: int = 32,
    ) -> dict:
    """
    Smoke-test patch: cap training at `max_epochs` (default 1), disable
    early stopping so the run definitely finishes, force a single-hop GNN
    config, and set a small batch_size. The narrow GNN + small batch
    keeps memory low so the smoke test can run on the largest dataset
    (chl59 with 6 dense Lung sections, mmb20 with 19 STARmap sections)
    without OOMing.

    The narrow GNN settings (num_layers=1, sampler=[8], nbr_aggregation_hops=1)
    are EXPLICITLY re-asserted here even though `_BD()` already defaults
    to them — that way later patches in the smoke-test build chain
    (e.g. `_patch_dual_decoder_covariate`, `_patch_dual_adversarial`)
    can't accidentally drift the smoke test into a heavier config.
    Dataset patches applied AFTER `_patch_quick` (e.g. `_patch_dual_mmb20`)
    can still override batch_size if they need to.

    Also forces `check_val_every_n_epoch = 1` for a 1-epoch cap. The base
    config sets it to 2 (a throughput optimisation for real 80-epoch runs),
    and Lightning schedules validation when `(current_epoch + 1) %
    check_val_every_n_epoch == 0` — so with `max_epochs=1` the only epoch is
    epoch 0 and validation NEVER runs. That is not merely a missing metric:
    `ModelCheckpoint._should_save_on_train_epoch_end()` returns False as soon
    as `check_val_every_n_epoch != 1` (pytorch_lightning 2.4.0,
    model_checkpoint.py:425-432), so the callback only ever saves from
    `on_validation_end` — which is never reached. The result is a smoke test
    that logs no `val_*` metric and writes an EMPTY `checkpoints/`, i.e. one
    that covers neither the validation path nor checkpointing, and leaves
    nothing for `--predict` to load. Diagnosed on
    `smoke-test+stream+xhs1000-39b_1p` (run 20260823_010659_seed0): full train
    epoch (6,548 steps), empty Validation banner, empty `checkpoints/`.
    Only applied when `max_epochs < 2`, so a caller asking for more epochs
    keeps the base cadence.
    """
    cfg["trainer"]["max_epochs"] = int(max_epochs)
    cfg["trainer"]["early_stopping_params"]["enabled"] = False
    if int(max_epochs) < 2:
        cfg["trainer"]["check_val_every_n_epoch"] = 1
    # Narrow GNN — single-hop everywhere.
    cfg["model"]["encoder_params"]["gnn_params"]["num_layers"] = 1
    cfg["datamodule"]["sampler_params"]["num_neighbors"] = [8]
    cfg["model"]["loss_params"]["loss_kwargs"]["nbr_aggregation_hops"] = 1
    # Smaller batch for memory headroom.
    cfg["datamodule"]["loader_params"]["batch_size"] = int(batch_size)
    return cfg


# ---------------------------------------------------------------------------
# R0: the paper-faithful reference recipe, as a named stack
# ---------------------------------------------------------------------------

def _r0_reference_stack() -> dict:
    """
    The reference recipe, applied in order, written sequentially.

    Identical to the composition inside
    `s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+
    sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+
    contrastWB-w10-k5+...`, but as a sequence of assignments rather than a
    fourteen-deep nest whose arguments trail after the closing parens. Same
    patches, same order, same arguments -- verified against that entry.

    Everything here is machinery the paper relies on (Eq. 6): dual RVQ at
    (30, 90) per branch, the decoder section covariate as the batch-correction
    lever, the within-section adjacency BCE, the within-section contrastive
    term and the cell-branch codebook-diversity regulariser. Deliberately NO
    adversarial head -- Eq. 6 does not have one.

    Reusing this keeps R0's numbers comparable with the existing reference runs.
    """
    cfg = _BD()
    cfg = _patch_dual_rvq(cfg, branch="niche", codebook_sizes=(30, 90))
    cfg = _patch_dual_rvq(cfg, branch="cell", codebook_sizes=(30, 90))
    cfg = _patch_dual_decoder_covariate(cfg)
    cfg = _patch_dual_encoder_deeper(cfg, hidden_channels=[400, 400, 256])
    cfg = _patch_dual_decoupled_encoders(cfg)
    cfg = _patch_dual_decoder_width(cfg, hidden_channels=[32])
    cfg = _patch_dual_graph_knn(cfg, n_neighs=16)
    cfg = _patch_dual_sampler_neighbors(cfg, num_neighbors=[16])
    cfg = _patch_dual_attr_recon_weight(cfg, weight=1.0)
    cfg = _patch_dual_batch_lr(cfg, batch_size=512, lr=7e-4)
    cfg = _patch_dual_no_batch_int(cfg)
    cfg = _patch_dual_adj_within_section_only(cfg, enabled=True)
    cfg = _patch_dual_contrastive_cell_within_batch(
        cfg, wt_contrastive_cell=10.0, k_pos=5, temperature=0.1)
    cfg = _patch_dual_codebook_diversity(
        cfg, weight=10.0, temperature=100.0, branch="cell")
    return cfg


# R0 whole-section test holdout, as `adata_batch_id` VALUES (batchN -> N), NOT
# blob positions -- SpatialBatchSplit matches data.adata_batch_id against these.
# Chosen to span the size distribution of the 39 sections
# (2,351 / 5,706 / 13,487 / 32,573 cells = 54,117, i.e. 10.4% of 519,058).
# The remaining 35 sections train; val is a 10% in-section cell split.
_R0_TEST_BATCHES = [3, 7, 0, 22]


VARIANTS: dict = {
    # ---- R0: first real streaming scale-up (39 sections, 519,058 cells) ----
    "smoke-test+stream+xhs1000-39b_1p": {
        "description": (
            "1-epoch end-to-end pipeline test of the STREAMING path through the "
            "real driver, on the R0 dataset (xhs1000-39b_1p, 39 Xenium human "
            "skin sections, one 4,948-gene panel). Reference recipe + on-disk "
            "streaming, capped at 1 epoch with a narrow GNN and small batch. "
            "Run this before stream+r0: it is the first thing to exercise "
            "`--train --variant` on the streaming backend at all."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhs1000-39b_1p dataset, test_batches=[3,7,0,22]",
            "+streaming(sections_per_block=2)",
            "+quick(max_epochs=1, batch_size=64, narrow GNN)",
        ],
        "build": lambda: _patch_streaming(
            _patch_quick(
                _patch_dual_xhs1000_39b(
                    _r0_reference_stack(),
                    test_batch_idx=list(_R0_TEST_BATCHES),
                    batch_size=64,
                ),
                max_epochs=1, batch_size=64,
            ),
            sections_per_block=2,
        ),
    },
    "stream+r0+xhs1000-39b_1p": {
        "description": (
            "R0 -- the first real scale-up of the streaming backend. "
            "xhs1000-39b_1p: 39 Xenium human skin sections, 519,058 cells, one "
            "4,948-gene panel, so it tests 'does streaming scale?' without "
            "entangling the open cross-panel question. ~6x the paper's largest "
            "run (6 sections). Paper-faithful reference recipe throughout; the "
            "only additions are the dataset switch and the on-disk backend. "
            "Whole-section test holdout of 4 sections spanning the size range "
            "(10.4% of cells), 35 sections training, 10% in-section cell split "
            "for val / early stopping."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhs1000-39b_1p dataset, test_batches=[3,7,0,22]",
            "+streaming(sections_per_block=8)",
        ],
        "build": lambda: _patch_streaming(
            _patch_dual_xhs1000_39b(
                _r0_reference_stack(),
                test_batch_idx=list(_R0_TEST_BATCHES),
                batch_size=512,
            ),
            # 8 sections resident. Largest R0 section is 0.64 GB dense, so ~5 GB,
            # ~10 GB at the 2xK concatenation peak plus one prefetch block. K=8
            # gives the contrastive / covariate terms far more cross-section
            # signal than the default 4. Sweep {4, 8, 16} in Phase 2 proper.
            sections_per_block=8,
        ),
    },

    # ---- cross-panel (union + mask) smoke tests --------------------------
    "smoke-xpanel+xhc38-4b_1p": {
        "description": (
            "First cross-panel training run: xhc38-4b_1p, Xenium human colon, "
            "panels 419 and 319 where 319 is a strict SUBSET of 419 (block "
            "union 419, 100 genes masked for the two 319-gene sections). "
            "Exercises the model side end to end -- vocabulary-width weights "
            "gathered per block, masked softmax, masked-sum NB. 300 steps."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhc38-4b_1p dataset, test_batches=[3]",
            "+streaming(sections_per_block=4, num_workers=2)",
            "+step-budget(max_steps=300)",
        ],
        "build": lambda: _patch_step_budget(
            _patch_streaming(
                _patch_dual_xhc38_4b(
                    _r0_reference_stack(),
                    # Hold out ONE 319-gene section, leaving batch0(319),
                    # batch1(419) and batch2(419) in training. Both panels must
                    # stay: with one panel every block's mask is all-ones and
                    # the run would exercise nothing while appearing to pass.
                    test_batch_idx=[3],
                    batch_size=512,
                ),
                # All 4 sections co-resident, so every block spans both panels.
                sections_per_block=4,
                num_workers=2,
            ),
            max_steps=300,
            val_checks=3,
        ),
    },
    "smoke-xpanel-holdout+xhc38-4b_1p": {
        "description": (
            "xhc38-4b_1p with ONE SECTION OF EACH PANEL held out, so the "
            "panel-width comparison can be made on held-out data with both "
            "condition and split controlled. The earlier smoke-xpanel run held "
            "out only batch3, which put the sole test section in the 319 group "
            "and confounded panel width with train/test. Holding out batch3 "
            "(319) + batch1 (419) -- BOTH Non-diseased -- leaves batch0 (319) + "
            "batch2 (419), both Cancer, in training: panel diversity survives "
            "on both sides, and the two test sections differ ONLY in panel "
            "width. ([0, 2] would serve equally, both Cancer; the other two "
            "one-of-each pairings mix conditions and reintroduce the confound.) "
            "300 steps."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhc38-4b_1p dataset, test_batches=[3, 1] (one per panel)",
            "+streaming(sections_per_block=4, num_workers=2)",
            "+step-budget(max_steps=300)", "+no-logging (wandb off)",
        ],
        "build": lambda: _patch_no_logging(_patch_step_budget(
            _patch_streaming(
                _patch_dual_xhc38_4b(
                    _r0_reference_stack(),
                    test_batch_idx=[3, 1],
                    batch_size=512,
                ),
                sections_per_block=4,
                num_workers=2,
            ),
            max_steps=300,
            val_checks=3,
        )),
    },
    "smoke-xpanel+xhb42-3b_1p": {
        "description": (
            "The HARD cross-panel case: xhb42-3b_1p, Xenium human brain, panels "
            "350 and 317 with intersection 262 -- 88 and 55 exclusive genes, so "
            "the union (405) is strictly WIDER than either panel. xhc38 is "
            "nested and cannot catch a max()-instead-of-union bug; this can. "
            "300 steps."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhb42-3b_1p dataset, test_batches=[2]",
            "+streaming(sections_per_block=3, num_workers=2)",
            "+step-budget(max_steps=300)",
        ],
        "build": lambda: _patch_step_budget(
            _patch_streaming(
                _patch_dual_xhb42_3b(
                    _r0_reference_stack(),
                    # Hold out batch2(317), leaving batch0(350) + batch1(317):
                    # both panels, and their union is wider than either.
                    test_batch_idx=[2],
                    batch_size=256,
                ),
                sections_per_block=3,
                num_workers=2,
            ),
            max_steps=300,
            val_checks=3,
        ),
    },

    "corpus-holdout": {
        "description": (
            "SQUINT-holdout: the paper run. Trains on TERRA's 416 train "
            "sections of hst_corpus_110m (417 files -- 47_batch0 is two) and "
            "never sees the 219 held-out ones, so every benchmark number it "
            "produces is genuinely zero-shot and directly comparable to "
            "TERRA-96M on identical data. 200,000 steps at batch 512 is ~1.15 "
            "passes over the 96M training cells. Cross-panel throughout: the "
            "model is built at the 9,574-gene vocabulary and gathers per block, "
            "with masked softmax and masked-sum NB. REQUIRES "
            "--split-sections-json _splitspec.json; without it this trains on "
            "all 636 sections and can support no zero-shot claim at all."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+hst_corpus_110m (636 sections, 112.6M cells, V=9,574)",
            "+streaming(sections_per_block=8, max_cells_per_block=900k, "
            "num_workers=2)",
            "+step-budget(max_steps=200000, val every 20000 steps)",
            "+whole-section splits via --split-sections-json",
        ],
        "build": lambda: _patch_step_budget(
            _patch_streaming(
                _patch_dual_hst_corpus(
                    _r0_reference_stack(),
                    batch_size=512,
                ),
                sections_per_block=8,
                # 2, not the default fan-out: R0 measured 96.5 GB of RSS at 22
                # workers against 26 GB at 2, and a NeighborLoader is built per
                # BLOCK so workers are spawned and torn down for every one of
                # them.
                num_workers=2,
                # Measured over random shuffles of the 417 train sections
                # (cells and widths from `_vocabpolicy.json`), blocks come out
                # at a median of 12.1 GB and a worst case of 21.6 GB, so 43.2 GB
                # counting the prefetched block. The first attempt used K=8 with
                # no cell budget and died at 102 GB (TERM_MEMLIMIT) against a
                # 96 GB request; unbudgeted K=8 has a 144 GB worst case.
                #
                # 900k rather than less because the worst case is set by the
                # largest SINGLE section (1,617,614 cells, which forms its own
                # block whatever the budget) and is therefore flat below ~600k:
                # 400k and 600k both peak at 19.2 GB while holding a median of
                # only 1-2 sections. 900k lifts the median to 3 sections per
                # block for +2.4 GB of peak. Section mixing is what supplies the
                # batch-integration signal, so that is worth having.
                #
                # NOTE the corpus's sections are simply large: even at 900k a
                # block holds ~3 of them, against K=8 on the 39-section R0. Each
                # step's adversary therefore sees ~3 batches out of 416. Watch
                # the adversarial term in the preflight -- a 416-way head fed 3
                # classes per step may need its weight revisited.
                max_cells_per_block=900_000,
            ),
            max_steps=200_000,
            # Every 2,000 steps is ~4.6 min at the 7.23 steps/s measured on the
            # first corpus run -- often enough that a stall is obvious, rare
            # enough not to bloat the log over 200,000 steps. Without it the job
            # log shows one "Start of Epoch 0" and then nothing for hours,
            # because a corpus epoch is 187,562 steps and the whole budget
            # finishes inside epoch 0.
            heartbeat_every_n_steps=2_000,
            # 10 validation passes over the run. Affordable only because Part D
            # restricts the val loader to the 32 val SECTIONS -- unrestricted it
            # would rebuild every block in the corpus to reach them, ~51 min a
            # pass, which is what forced validation off entirely before.
            val_checks=10,
        ),
    },
    "smoke-steps+stream+xhs1000-39b_1p": {
        "description": (
            "Short proof of the STEP-BUDGET machinery on the streaming backend "
            "-- the paths `_patch_step_budget` introduced have never executed. "
            "3,000 steps with validation every 1,000, so it exercises: does "
            "`max_steps` actually stop the run; does step-grained validation "
            "fire mid-epoch; does ModelCheckpoint write from `on_validation_end` "
            "on the step grid (an empty checkpoints/ is the failure mode that "
            "would silently waste a long corpus run); and does num_workers=2 "
            "hold memory down. Same recipe/data/batch as stream+r0-steps, only "
            "the budget is short -- ~10 min, not a model run."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhs1000-39b_1p dataset, test_batches=[3,7,0,22]",
            "+streaming(sections_per_block=8, num_workers=2)",
            "+step-budget(max_steps=3000, val every 1000 steps)",
        ],
        "build": lambda: _patch_step_budget(
            _patch_streaming(
                _patch_dual_xhs1000_39b(
                    _r0_reference_stack(),
                    test_batch_idx=list(_R0_TEST_BATCHES),
                    batch_size=512,
                ),
                sections_per_block=8,
                num_workers=2,
            ),
            max_steps=3_000,
            val_checks=3,
        ),
    },
    "smoke-steps-noval+stream+xhs1000-39b_1p": {
        "description": (
            "Same 3,000-step budget as smoke-steps, but with the validation "
            "loop DISABLED and checkpointing on a pure step schedule -- the "
            "configuration a corpus-scale run would use, where ~10 val passes "
            "would cost ~8.4 h against ~6.1 h of training. Proves the "
            "monitor=None path writes checkpoints from `on_train_batch_end` "
            "without any val_loss to rank by, which is the whole risk of "
            "turning validation off."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhs1000-39b_1p dataset, test_batches=[3,7,0,22]",
            "+streaming(sections_per_block=8, num_workers=2)",
            "+step-budget(max_steps=3000)",
            "+no-val(checkpoint every 1000 steps)",
        ],
        "build": lambda: _patch_no_val(
            _patch_step_budget(
                _patch_streaming(
                    _patch_dual_xhs1000_39b(
                        _r0_reference_stack(),
                        test_batch_idx=list(_R0_TEST_BATCHES),
                        batch_size=512,
                    ),
                    sections_per_block=8,
                    num_workers=2,
                ),
                max_steps=3_000,
            ),
            checkpoint_every_n_steps=1000,
            # Keep all 3 saves. With the default save_top_k=1 each save
            # deletes the previous, so a schedule that fired 3 times and one
            # that fired once leave an identical directory — which would make
            # this test unable to prove the thing it exists to prove.
            save_top_k=-1,
        ),
    },
    "stream+r0-steps+xhs1000-39b_1p": {
        "description": (
            "R0 on a STEP budget instead of an epoch count, and with the "
            "streaming loader's worker fan-out corrected. Same reference recipe "
            "and same data as stream+r0, so the two are directly comparable; "
            "the only changes are 30,000 steps with step-grained validation "
            "(epochs are not a meaningful unit at corpus scale) and "
            "num_workers=2 (22 forked copies of each 2-4 GB block are what "
            "made LSF kill stream+r0 at 96.5 GB, while GPU utilisation sat at "
            "8.1%). 30k steps is the budget stream+r0 had already reached by "
            "epoch 36, where both first-level codebooks were fully occupied."
        ),
        "patches": [
            "+reference recipe (rvq-both, decoder-cov, knn16, within-sec, "
            "diversity-w10, contrastWB-w10-k5)",
            "+xhs1000-39b_1p dataset, test_batches=[3,7,0,22]",
            "+streaming(sections_per_block=8, num_workers=2)",
            "+step-budget(max_steps=30000, val every 3000 steps)",
        ],
        "build": lambda: _patch_step_budget(
            _patch_streaming(
                _patch_dual_xhs1000_39b(
                    _r0_reference_stack(),
                    test_batch_idx=list(_R0_TEST_BATCHES),
                    batch_size=512,
                ),
                sections_per_block=8,
                num_workers=2,
            ),
            max_steps=30_000,      # ~36 passes over R0 -- where it plateaued
        ),
    },

    # ---- Smoke test (1 epoch end-to-end) ----------------------------------
    "smoke-test+mmb0-1b_smb1-1b_1p": {
        "description": (
            "1-epoch end-to-end pipeline test on the mmb-smb mouse data. "
            "Dual-VQ + NicheCompass-style decoder covariate + adversarial "
            "GRL on the FULL z_mlp (alpha=1.0, wt_adv_batch=150) — same "
            "batch-correction recipe used by the overnight sweep, capped "
            "at max_epochs=1, early stopping disabled, narrow GNN "
            "(num_layers=1, sampler=[8], nbr_hops=1), batch_size=32. Use "
            "before launching the sweep to verify train -> predict -> "
            "plots -> metrics works end-to-end on the current blob in a "
            "few minutes."
        ),
        "patches": [
            "+quick(max_epochs=1, batch_size=32, narrow GNN)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_quick(_BD(), max_epochs=1, batch_size=32)
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "smoke-test+chl59-8b_1p": {
        "description": (
            "1-epoch end-to-end pipeline test on the chl59-8b_1p CosMx Lung "
            "data (test ids = [2, 3], train = the other 6 sections, val = "
            "10% in-section cell sample). Dual-VQ + decoder covariate + "
            "adversarial GRL — same batch-correction recipe as the "
            "overnight sweep, capped at 1 epoch, early stopping disabled, "
            "narrow GNN (num_layers=1, sampler=[8], nbr_hops=1), "
            "batch_size lowered to 16 (the Lung sections have a ~2x "
            "larger gene panel than mmb-smb, so per-step autograd memory "
            "needs an extra cushion)."
        ),
        "patches": [
            "+quick(max_epochs=1, batch_size=16, narrow GNN)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_quick(_BD(), max_epochs=1, batch_size=16)
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            test_batch_idx=[2, 3],
            batch_size=16,
        ),
    },
    "smoke-test+spatch_1p": {
        "description": (
            "1-epoch end-to-end pipeline test on the spatch_1p dataset "
            "(/nfs/team361/sb75/DATASETS/silver/spatch_1p). Train = "
            "dataset_id_2.h5ad (adata_batch_id = 2); test = ALL other "
            "sections (resolved at runtime via the `test_batches='rest'` "
            "sentinel — predict() then runs inference on every "
            "non-training section). Cell-type label = obs['annotation'], "
            "niche label = obs['spatial_cluster']. Dual-VQ + decoder "
            "covariate + adversarial GRL — same batch-correction recipe "
            "as the other smoke tests. Capped at 1 epoch, early stopping "
            "disabled, narrow GNN (num_layers=1, sampler=[8], "
            "nbr_hops=1), batch_size=16.\n\n"
            "PREREQUISITE: build the spatch_1p blob ONCE before running "
            "this variant:\n"
            "    python examples/run_squint.py --build-blob "
            "--build-blob-dataset spatch_1p\n"
            "(each spatch_1p silver `dataset_id_<N>.h5ad` must carry "
            "`adata.uns['batch']` — int / 'batchN' string — so the "
            "blob loader can derive section ids. Use "
            "`examples/stamp_spatch_uns_batch.py` to stamp the files.)"
        ),
        "patches": [
            "+quick(max_epochs=1, batch_size=16, narrow GNN)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+spatch_1p(train_ids=[2], test='rest')",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_quick(_BD(), max_epochs=1, batch_size=16)
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            train_batch_idx=[2],
            test_batch_idx="rest",
            batch_size=16,
        ),
    },
    "smoke-test+mmb0-1b_smb1-20b_1p": {
        "description": (
            "1-epoch end-to-end pipeline test on the mmb20 MERFISH+STARmap+ "
            "21-section data (test ids = [15, 82], train = the other 19 "
            "STARmap+ sections, val = 10% in-section cell sample). "
            "Dual-VQ + decoder covariate + adversarial GRL — same "
            "batch-correction recipe as the overnight sweep, capped at 1 "
            "epoch, early stopping disabled, narrow GNN (num_layers=1, "
            "sampler=[8], nbr_hops=1), batch_size lowered to 16 (memory "
            "is tight at 19 training sections worth of static `x` + "
            "edge_index on GPU)."
        ),
        "patches": [
            "+quick(max_epochs=1, batch_size=16, narrow GNN)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmb0-1b_smb1-20b_1p(test_ids=[15,82])",
        ],
        "build": lambda: _patch_dual_mmb20(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_quick(_BD(), max_epochs=1, batch_size=16)
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            test_adata_ids=[15, 82],
            batch_size=16,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p 1-layer-GNN focused ablation matrix.
    # ========================================================================
    # Spine baseline = RVQ on both branches (k1=30, k2=90) + NicheCompass-
    # style decoder covariate + adversarial GRL on FULL z_mlp (alpha=1.0,
    # wt_adv_batch=150) + 1-layer SAGEConv GNN. Each variant ablates ONE
    # axis from this spine. Group via `--all-dataset mmb0-1b_smb1-1b_1p-ablations`.
    # The 1-layer spine (vs. the +wide 2-layer one used by the broader
    # sweep) is the user's preferred starting point on this dataset.
    #
    # Existing variants reused for the matrix:
    #   - dualvq+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p     (the spine itself)
    #   - dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p (2-layer GNN)
    #   - dualvq+small+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p (scvi-style encoder)
    "dualvq+rvq-both+decoder-cov+adv+gatv2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — SAGEConv -> GATv2Conv on the 1-layer GNN. "
            "Tests per-edge attention vs. uniform mean aggregation at "
            "1-hop on mmb-smb. Direct A/B against the +wide+gatv2 variant "
            "to see whether GAT helps at narrow GNN depth."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+gatv2(SAGEConv -> GATv2Conv, num_layers=1)",
        ],
        "build": lambda: _patch_dual_gatv2(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — adversarial weight bumped 150 -> 300 (2x). "
            "Pushes the encoder harder toward batch-invariant z_mlp; "
            "useful diagnostic for whether the default 150 is leaving "
            "headroom or whether stronger pressure starts collapsing "
            "codes."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=300.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(_BD(),
                        branch="niche", codebook_sizes=(30, 90)),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=300.0,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — adversarial weight cut 150 -> 50 (~3x "
            "lower). Tests whether the default GRL is over-regularising "
            "z_mlp at the cost of biological signal."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=50.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(_BD(),
                        branch="niche", codebook_sizes=(30, 90)),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=50.0,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — cosine adjacency BCE weight bumped 1000 -> "
            "3000 (3x). Stronger spatial-niche pressure on z_gnn; tests "
            "whether the codebooks tolerate even more spatial coherence "
            "than the default."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=3000.0,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — cosine adjacency BCE weight cut 1000 -> 250 "
            "(4x lower; matches an earlier default). Tests whether the "
            "default 1000 is over-pressuring spatial coherence at the "
            "cost of NB Pearson."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p 1-layer-spine ablation matrix v2.
    # ========================================================================
    # Eight more ablations on top of the spine
    # (dualvq+rvq-both+decoder-cov+adv, 1-layer GNN), picked for
    # plausible upside on a small dataset (~85k cells, 431 genes).
    # Each varies ONE knob; group via
    # `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v2`.
    "dualvq+rvq-both-large+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — RVQ levels (30, 90) -> (50, 150). Bigger "
            "primary + residual codebook capacity; tests whether the "
            "default 30/90 is the bottleneck on Pearson."
        ),
        "patches": [
            "+rvq(branch=both, levels=[50, 150])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(_BD(),
                        branch="niche", codebook_sizes=(50, 150)),
                    branch="cell", codebook_sizes=(50, 150),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+rvq-both-3level+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — 2-level RVQ (30, 90) -> 3-level RVQ "
            "(30, 60, 120). Finer hierarchical decomposition; each "
            "successive residual layer captures finer-grained variation. "
            "Worth trying when a 2-level RVQ saturates on Pearson."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(_BD(),
                        branch="niche", codebook_sizes=(30, 60, 120)),
                    branch="cell", codebook_sizes=(30, 60, 120),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+latent-128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — latent / codebook embedding dim 256 -> 128 "
            "(only the encoder MLP last hidden + GNN hidden shrink; "
            "decoder hidden widths and codebook sizes are unchanged). "
            "Tighter bottleneck à la NicheCompass / scvi; forces each "
            "code's embedding to be more semantically concentrated."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+small-latent(latent_dim=128)",
        ],
        "build": lambda: _patch_dual_small_latent(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            latent_dim=128,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+dropout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — dropout p=0.1 on encoder MLP, GNN, and "
            "both decoders. Cheap regularization. Particularly relevant "
            "on a small dataset (~85k cells) where the model can over-"
            "fit; pair with val_loss-based early stopping for clear "
            "wandb diagnostics."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+dropout(p=0.1)",
        ],
        "build": lambda: _patch_dual_dropout(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            p=0.1,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+batch-emb32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — learned batch embedding dim 16 -> 32. "
            "More capacity for the decoder's per-batch covariate to "
            "absorb cross-platform / cross-replicate gene-pattern "
            "differences. Encoder is unchanged (codes still batch-"
            "invariant by construction); only the decoder side gets "
            "a richer batch context."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate(embed_dim=32)",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(_BD(),
                        branch="niche", codebook_sizes=(30, 90)),
                    branch="cell", codebook_sizes=(30, 90),
                ),
                embed_dim=32,
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — NeighborLoader.num_neighbors [8] -> [16]. "
            "Doubles the per-step niche-aggregation neighborhood; the "
            "GNN sees richer spatial context per cell, at the cost of "
            "~2x larger sampled subgraph (memory + step time). Often "
            "improves spatial-domain coherence on dense tissues."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler(num_neighbors=[16])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[16],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — encoder MLP [400, 256] -> [400, 400, 256] "
            "(2 hidden layers -> 3 hidden layers; latent dim preserved "
            "at 256). Lets the encoder learn richer non-linear feature "
            "mixes before the VQ bottleneck."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Sibling of `+enc-deeper` with the cell + niche decoders "
            "shrunk from [400, 400] (2 hidden of 400) → [256] (1 hidden "
            "of 256). Tests whether decoder pressure on the latent "
            "improves NMI / iLISI on the known-good +enc-deeper spine "
            "without sacrificing NB reconstruction. All other knobs "
            "(1-layer SAGE GNN, RVQ (30, 90), decoder-cov embed=16, "
            "adv α=1 wt=150) match the parent variant — only the "
            "decoder geometry changes."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Tighter-decoder sibling of `+enc-deeper+dec-w256`: cell "
            "+ niche decoders shrunk from [256] (1 hidden of 256) → "
            "[128] (1 hidden of 128). Encoder stays at [400, 400, "
            "256] (`+enc-deeper`), latent dim stays 256, GNN stays "
            "1-layer SAGE h=256. Tests whether further decoder "
            "shrinking (half the hidden width) puts useful extra "
            "pressure on the 256-dim latent + codebook without "
            "compromising NB reconstruction."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[128]  (both cell and niche, 1 hidden of 128)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[128],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Even-tighter decoder sibling of `+enc-deeper+dec-w256`: "
            "cell + niche decoders shrunk from [256] → [64] (1 hidden "
            "of 64, 4× narrower). Encoder stays at [400, 400, 256] "
            "(`+enc-deeper`), latent dim stays 256, GNN stays "
            "1-layer SAGE h=256. Extreme decoder bottleneck — tests "
            "the limit of decoder-pressure-on-latent. NB Pearson "
            "likely takes a hit; the question is whether the codebook "
            "compensates enough to keep NMI strong."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]  (both cell and niche, 1 hidden of 64)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[64],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Most-extreme decoder shrink in the +enc-deeper family: "
            "cell + niche decoders [32] (1 hidden of 32, 12.5× "
            "narrower than the default [400, 400], 2× narrower than "
            "the +dec-w64 sibling). Encoder stays at [400, 400, 256] "
            "(+enc-deeper), latent stays 256, GNN stays 1-layer "
            "SAGE h=256, adversarial keeps α=1 wt=150. Pushes the "
            "decoder-pressure-on-latent hypothesis past the +dec-w64 "
            "limit. Expected NB Pearson floor — useful for finding "
            "where reconstruction quality drops off a cliff."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]  (both cell and niche, 1 hidden of 32)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Sibling of `+enc-deeper+dec-w32` with the NeighborSampler "
            "fanout cut from default [8] → [4] (1-hop, 4 neighbours "
            "per cell instead of 8). 1-hop is preserved (matches the "
            "1-layer SAGE GNN); only the per-hop fanout halves. "
            "Sampled neighbourhood per cell drops from 1 + 8 = 9 "
            "nodes to 1 + 4 = 5 nodes (~44% reduction).\n\n"
            "Tests two things:\n"
            "  - Throughput: thinner neighbourhood → smaller batches "
            "→ faster per-step.\n"
            "  - Niche-GNN robustness: does the niche embedding "
            "tolerate a thinner 1-hop neighbourhood, or do we lose "
            "spatial signal?\n\n"
            "The historic +wide+sampler4 variant (s20_v4) collapsed "
            "nbr_NMI to 0.11 — but that was the +wide spine where "
            "the GNN had 2 layers and sampler [4] left the second "
            "hop with empty neighbourhoods. THIS variant uses 1-layer "
            "GNN, so sampler [4] is fully consistent (no empty hops). "
            "Should be safe."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+sampler([4])  (1-hop, 4 nbrs)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            num_neighbors=[4],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Sibling of `+enc-deeper+dec-w32` with the "
            "NeighborSampler fanout bumped from default [8] → [12] "
            "(1-hop, 12 nbrs requested per cell). 1-hop preserved "
            "(matches the 1-layer SAGE GNN); only the per-hop "
            "fanout changes.\n\n"
            "Note: this is the SAMPLER fanout (training-time "
            "neighbor selection), NOT the spatial graph k. The "
            "underlying k-NN graph is still k=8 (the historic "
            "default), so PyG's NeighborSampler will sample up to 8 "
            "available neighbors per cell — the [12] request "
            "effectively caps at the graph's actual k. For a true "
            "12-neighbor receptive field, pair with `+knn12` (which "
            "selects the k=12 graph from the rebuilt blob)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+sampler([12])  (1-hop, 12 nbrs requested — capped at "
            "graph's actual k=8 unless paired with +knn12)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            num_neighbors=[12],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Sibling of `+enc-deeper+dec-w32` with the SPATIAL "
            "k-NN graph k bumped from 8 → 12 AND the sampler fanout "
            "bumped to match: `[12]`. Each cell has 12 spatial "
            "neighbours in the graph and the GNN samples ALL of "
            "them per cell at training time — clean 'thicker "
            "spatial neighborhood' ablation vs the parent's k=8 + "
            "[8]-sampler.\n\n"
            "Hypothesis: a denser spatial graph captures larger-"
            "scale spatial structure → richer niche context → "
            "potentially better nbr_NMI. Cost: ~50% more edges + "
            "larger sampled neighbourhood per step → bigger memory "
            "footprint. Combined with the tight dec-w32 decoder, "
            "the codebook should learn richer spatial relationships.\n\n"
            "REQUIRES BLOB REBUILD: `n_neighs_list = [6, 8, 12]` is "
            "now configured in `make_dataset_blob_config()`, but the "
            "existing blob on disk only has k=8. Rebuild with:\n"
            "    python examples/run_squint.py --build-blob\n"
            "(one-off, takes ~10-20 min depending on dataset size). "
            "After rebuild, the blob will contain k=6, 8, and 12 "
            "graphs."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)  (REQUIRES blob rebuild)",
            "+sampler([12])           (matches graph k)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w64+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Looser-decoder sibling of `+enc-deeper+dec-w32+knn12+"
            "sampler12`: same denser-spatial-neighborhood setup "
            "(k=12 graph + matching `[12]` sampler) but with the "
            "decoder relaxed from [32] → [64]. Tests whether the "
            "weak integration of the dec-w32 sibling was caused by "
            "the extreme decoder bottleneck over-constraining the "
            "latent — at dec-w64 the decoder has 2× more capacity, "
            "so the encoder may have more room to learn batch-"
            "invariant features.\n\n"
            "All other knobs match the parent `+enc-deeper+dec-w32+"
            "knn12+sampler12`: encoder [400, 400, 256], 1-layer SAGE "
            "GNN h=256 (using k=12 graph), RVQ (30, 90), decoder-cov "
            "embed=16, adv α=1 wt=150. Niche-GNN sees ALL 12 graph "
            "neighbors per cell.\n\n"
            "REQUIRES BLOB REBUILD (same as +knn12+sampler12 "
            "sibling) — `python examples/run_squint.py --build-blob` "
            "to populate the k=12 graph in the blob."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]",
            "+graph_knn(n_neighs=12)  (REQUIRES blob rebuild)",
            "+sampler([12])           (matches graph k)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[64],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Asymmetric variant: SPATIAL k-NN graph k=12 (each cell "
            "has 12 spatial neighbours in the graph) BUT sampler "
            "fanout `[8]` (the GNN only samples 8 of the 12 per cell "
            "per step). Unlike `+knn12+sampler12` (which uses ALL 12 "
            "neighbours deterministically per step), this version "
            "introduces stochastic neighbour subsampling: each epoch "
            "the GNN sees a different random 8-of-12 per cell. Acts "
            "as a per-epoch data-augmentation on the niche side.\n\n"
            "Hypothesis: the denser k=12 graph carries richer "
            "spatial signal than k=8, but the stochastic 8-of-12 "
            "sampling preserves the original SQUINT training "
            "dynamic (8-neighbour fanout) rather than forcing the "
            "GNN to aggregate the full neighborhood every step. "
            "The augmentation may help niche-side generalisation "
            "and integration.\n\n"
            "Compared to siblings:\n"
            "  - `+knn8+sampler8` (parent, no patches): static, all "
            "    8 of 8 neighbours per step.\n"
            "  - `+knn12+sampler12`: static, all 12 of 12 per step "
            "    (denser deterministic).\n"
            "  - `+knn12+sampler8` (THIS): denser graph + stochastic "
            "    8-of-12 sampling.\n\n"
            "REQUIRES BLOB REBUILD (same as other +knn12 siblings) — "
            "`python examples/run_squint.py --build-blob` to "
            "populate the k=12 graph in the blob."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-12 per epoch)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[8],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Densest paired-neighbourhood variant in the +enc-deeper"
            "+dec-w32 family: SPATIAL k-NN graph k=16 + sampler "
            "fanout `[16]` (each cell has 16 spatial neighbours in "
            "the graph and the GNN samples ALL of them per step). "
            "2× denser than the parent's k=8.\n\n"
            "Tests whether even larger spatial context (vs k=12 "
            "sibling) further lifts niche metrics — diminishing-"
            "returns probe. Cost: 2× more edges than parent, ~33% "
            "more than +knn12+sampler12 → bigger per-step memory.\n\n"
            "REQUIRES BLOB REBUILD: `n_neighs_list = [6, 8, 12, 16]` "
            "is now in `make_dataset_blob_config()` but the existing "
            "blob on disk doesn't yet have the k=16 graph. Rebuild "
            "with:\n"
            "    python examples/run_squint.py --build-blob\n"
            "After rebuild, k=6/8/12/16 are all available."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([16])           (matches graph k)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "No-batch-integration sibling of `+enc-deeper+dec-w32+"
            "knn16+sampler16`. Same architecture (RVQ (30, 90), "
            "decoder-cov embed=16, encoder MLP [400, 400, 256], "
            "1-layer SAGE GNN h=256, decoders [32], k=16 spatial "
            "graph + sampler [16]) — but with EVERY explicit batch-"
            "integration loss DROPPED (no adv, no mmd-batch, no "
            "mmd-prior). The within-section BCE adjacency scope "
            "(`adj_within_section_only=True`) is set explicitly: "
            "cross-section pairs are dropped from the cosine BCE "
            "negative pool, so the spatial-adjacency loss is no "
            "longer an anti-integration pressure.\n\n"
            "Hypothesis to test (the s36 sweep showed promising "
            "no-batch-int results — this variant is the multi-seed "
            "version of `s36_v10` / `s37_v9`): with the BCE fix in "
            "place, the decoder covariate is sufficient to handle "
            "batch effects on its own and an explicit z_mlp "
            "integration loss is not needed.\n\n"
            "Architecturally identical to `s36_v10_...` and "
            "`s37_v9_...`; this is the canonical (sweep-prefix-free) "
            "name intended for `submit_multi_seed.sh` so the run "
            "directory lives at the same level as the other "
            "multi-seed comparators.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Batch=512 / lr=7e-4 sibling of the canonical no-batch-"
            "int +enc-deeper+dec-w32+knn16+sampler16 spine. "
            "sqrt-scaled 2× the base bs=256/lr=5e-4. Larger batch = "
            "smoother per-step gradient; sqrt-lr keeps the per-"
            "sample gradient roughly constant.\n\n"
            "Differs from `s38_v12`: that variant carries cell-"
            "w=2.0 on top; this one keeps cell-w at the default 1.0 "
            "so the bs/lr axis is isolated.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs1024+lr1e-3+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Batch=1024 / lr=1e-3 sibling of the canonical no-batch-"
            "int +enc-deeper+dec-w32+knn16+sampler16 spine. "
            "sqrt-scaled 4× the base bs=256/lr=5e-4.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=1024", "+lr=1e-3",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=1024, lr=1e-3,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs2048+lr1.4e-3+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Batch=2048 / lr=1.4e-3 sibling of the canonical no-"
            "batch-int +enc-deeper+dec-w32+knn16+sampler16 spine. "
            "sqrt-scaled 8× the base bs=256/lr=5e-4 (lr 5e-4 × "
            "sqrt(8) ≈ 1.414e-3).\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`) and "
            "MORE GPU MEMORY — bs=2048 with the knn16+sampler16 "
            "stack is ~8× the per-step memory of the bs=256 base. "
            "Set `LSF_MEM_MB=200000` or higher when submitting."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=2048", "+lr=1.4e-3",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=2048, lr=1.4e-3,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs4096+lr2e-3+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Batch=4096 / lr=2e-3 sibling of the canonical no-"
            "batch-int +enc-deeper+dec-w32+knn16+sampler16 spine. "
            "sqrt-scaled 16× the base bs=256/lr=5e-4 — extends the "
            "ladder beyond the user-requested 3 sizes (512/1024/"
            "2048) so the bs sweep brackets the historic 8x-16x "
            "regime cleanly.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`) and "
            "SIGNIFICANT GPU memory — bs=4096 + knn16+sampler16 "
            "may exceed even 80GB cards. Set `LSF_MEM_MB=300000` "
            "or higher and pick the largest-memory GPU available. "
            "If this OOMs, drop to bs=2048 (the v15-equivalent of "
            "this ladder) and look at trends across bs=512 / 1024 "
            "/ 2048 instead."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=4096", "+lr=2e-3",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=4096, lr=2e-3,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs4096+lr2e-3+100ep+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Longer-train sibling of the +bs4096+lr2e-3 winner "
            "(`+enc-deeper+dec-w32+knn16+sampler16+bs4096+lr2e-3+"
            "within-sec`). Only delta: `max_epochs=100` (vs the "
            "base config's 80). Early stopping is still enabled "
            "(patience=10 val passes ≈ 20 train epochs on val_loss), "
            "so the run can finish earlier if val_loss plateaus.\n\n"
            "Rationale: at bs=4096 the dataset's ~340 batches/epoch "
            "drop to ~85, so each epoch is ~4× fewer gradient steps "
            "than at bs=256. 80 epochs × 85 steps/epoch = 6,800 "
            "total steps — about 22% of the bs=256/80-epoch budget "
            "(31k steps). 100 epochs at bs=4096 gets us to ~8,500 "
            "steps — closer to (but still below) the historic "
            "step-count, while keeping per-epoch wall time low.\n\n"
            "CAVEAT (see `make_train_config_dualvq` max_epochs "
            "comment): the 2026-05-12 max_epochs=200 sweep on the "
            "+wide spine regressed niche NMI because val_loss "
            "drifted past the niche-NMI optimum. 100 is a mild "
            "bump and the no-batch-int regime is gentler (no "
            "adversarial CE creep), so this is much less risky — "
            "but watch the niche-NMI ckpt-selection trade-off "
            "anyway. Pair with `SQUINT_WITH_PEARSON=1` to also log "
            "`val_pearson_gene_wise_1hop_nbr_log1p` as a sanity "
            "monitor.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=4096", "+lr=2e-3",
            "+max_epochs=100  (default 80; early stop still ON)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_max_epochs(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=4096, lr=2e-3,
                    ),
                    max_epochs=100,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs8192+lr2.8e-3+200ep+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Double-batch + long-train sibling of the +bs4096+lr2e-3 "
            "winner. Two deltas:\n"
            "  1. Batch size 4096 -> 8192 (2× the working config, "
            "     32× the base bs=256). lr sqrt-scaled to "
            "     2.83e-3 ≈ 2.8e-3 (5e-4 × sqrt(32)).\n"
            "  2. max_epochs 80 -> 200. At bs=8192 the dataset has "
            "     ~43 batches/epoch, so 200 epochs gets us to "
            "     ~8,500 steps — comparable to the +100ep sibling "
            "     above at bs=4096.\n\n"
            "Hypothesis: the +bs4096 setting works well; pushing "
            "to 2× the batch with the same sqrt-lr scaling tests "
            "whether the no-batch-int regime tolerates the bigger "
            "batch (it has no adversarial discriminator to "
            "saturate, unlike the historic regimes where >8× "
            "batches collapsed integration). max_epochs=200 keeps "
            "the total-steps budget honest given the larger batch.\n\n"
            "MEMORY: bs=8192 + knn16+sampler16 samples "
            "(1 + 16) × 8192 = 139,264 nodes per training step. "
            "This is ~2× the bs=4096 memory footprint and is "
            "**likely** to OOM on standard 40GB cards. Submit with "
            "`LSF_MEM_MB=400000` AND on the largest-memory GPU "
            "available; pair with `--gpu mode=exclusive_process:num=1` "
            "via the bsub flag. If it OOMs, drop bs to 6144 (still "
            "1.5× the bs=4096 winner) and rerun.\n\n"
            "CAVEAT (see `make_train_config_dualvq` max_epochs "
            "comment): max_epochs=200 historically regressed niche "
            "NMI on the +wide spine. The no-batch-int regime is "
            "gentler — no adv CE creep — but the val_loss optimum "
            "may still drift past niche-NMI optimum. Pair with "
            "`SQUINT_WITH_PEARSON=1` and inspect both ckpts post-"
            "hoc.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+batch_size=8192", "+lr=2.8e-3",
            "+max_epochs=200  (default 80; early stop still ON)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_max_epochs(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=8192, lr=2.8e-3,
                    ),
                    max_epochs=200,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn20+sampler20+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Denser-spatial-graph sibling of `+enc-deeper+dec-w32+"
            "knn16+sampler16+within-sec` (no-batch-int + within-sec "
            "BCE). The only delta is the spatial graph k: 16 → 20, "
            "with sampler fanout matched to `[20]` so the GNN sees "
            "ALL 20 neighbours per cell per step.\n\n"
            "Tests whether even denser spatial context (vs k=16) "
            "lifts niche metrics further — diminishing-returns probe "
            "in the no-batch-int regime where within-section BCE + "
            "decoder-cov are the only integration pressures.\n\n"
            "Cost: ~25% more edges than k=16, ~17·bs sampled nodes "
            "per training step. Should be tolerable at the default "
            "bs=256 but may need extra GPU memory if you pair this "
            "with a bigger bs.\n\n"
            "REQUIRES BLOB REBUILD: `n_neighs_list = [6, 8, 12, 16, "
            "20, 24]` is now configured in `make_dataset_blob_config"
            "()` but the existing on-disk blob predates the k=20/24 "
            "addition. Rebuild with:\n"
            "    python examples/run_squint.py --build-blob\n"
            "(one-off, takes ~10-20 min). After rebuild, k=20 and "
            "k=24 are available."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=20)  (REQUIRES blob rebuild)",
            "+sampler([20])           (matches graph k)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=20,
                    ),
                    num_neighbors=[20],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Even-denser-spatial-graph sibling of the +knn16+"
            "sampler16 variant. Spatial graph k=24, sampler [24] — "
            "DENSEST spatial neighbourhood configured in the blob.\n\n"
            "Tests the upper end of the spatial-context-density "
            "axis. If k=24 still lifts niche metrics over k=20, the "
            "spine isn't yet saturated and the graph could be made "
            "denser still; if it plateaus or regresses, k=16-20 is "
            "the right operating point.\n\n"
            "Cost: 50% more edges than k=16, ~17·bs → ~25·bs "
            "sampled nodes per training step. Plan on extra GPU "
            "memory.\n\n"
            "REQUIRES BLOB REBUILD (same as the +knn20 sibling) — "
            "rebuild once and both k=20 and k=24 become available."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)  (REQUIRES blob rebuild)",
            "+sampler([24])           (matches graph k)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=24,
                    ),
                    num_neighbors=[24],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn20+sampler20+bs4096+lr2e-3+100ep+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Stacked combo of two recent winners on the no-batch-"
            "int + enc-deeper + dec-w32 + within-sec spine:\n"
            "  - knn=20 spatial graph + sampler [20] (denser spatial "
            "    context than the +knn16 default).\n"
            "  - bs=4096 + lr=2e-3 (sqrt-scaled 16× the base bs=256/"
            "    lr=5e-4 — known-good operating point).\n"
            "  - max_epochs=100 (vs base 80) — compensates for the "
            "    ~4× fewer batches/epoch at bs=4096 (only ~85 steps/"
            "    epoch on this dataset), so 100 epochs ≈ 8,500 "
            "    gradient steps. Early stopping still ON.\n\n"
            "Pair-wise A/B vs `+knn16+sampler16+bs4096+lr2e-3+100ep`: "
            "isolates the spatial-density delta (k=16 → k=20) at the "
            "winning bs/lr/epoch combo.\n\n"
            "REQUIRES BLOB REBUILD (k=20 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=20)  (REQUIRES blob rebuild)",
            "+sampler([20])",
            "+batch_size=4096", "+lr=2e-3",
            "+max_epochs=100  (default 80; early stop still ON)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_max_epochs(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=20,
                            ),
                            num_neighbors=[20],
                        ),
                        batch_size=4096, lr=2e-3,
                    ),
                    max_epochs=100,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+bs4096+lr2e-3+100ep+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "knn=24 sibling of `+knn20+sampler20+bs4096+lr2e-3+100ep+"
            "within-sec`. Spatial graph k=24, sampler [24]. Same "
            "bs=4096 + lr=2e-3 + max_epochs=100 + within-sec + "
            "no-batch-int spine.\n\n"
            "Forms a clean three-point density ladder at the winning "
            "bs/lr/epoch combo: knn=16 / 20 / 24. Reading the trend "
            "tells you whether spatial-density gains saturate around "
            "k=20 or whether k=24 still helps.\n\n"
            "Memory: ~25% more sampled nodes per training step than "
            "the k=20 sibling (1 + 24 = 25 nodes per cell vs 1 + 20 "
            "= 21). At bs=4096 that's `25 × 4096 = 102,400` sampled "
            "nodes/step. Should fit comfortably with `LSF_MEM_MB=200000` "
            "but may need more if you pair this with bigger bs.\n\n"
            "REQUIRES BLOB REBUILD (k=24 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)  (REQUIRES blob rebuild)",
            "+sampler([24])",
            "+batch_size=4096", "+lr=2e-3",
            "+max_epochs=100  (default 80; early stop still ON)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_max_epochs(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=24,
                            ),
                            num_neighbors=[24],
                        ),
                        batch_size=4096, lr=2e-3,
                    ),
                    max_epochs=100,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w10.0+nbr-w0.5+adj-w2000+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "VERY-STRONG-CELL-NB sibling of `s38_v16` (the kitchen-"
            "sink combo: tighter encoder [200, 200, 128] + gnn-h128 "
            "+ cell-w=3.0 + nbr-w=0.5 + adj-w=2000 + bs=512 + "
            "lr=7e-4 + within-sec + no-batch-int). Only delta: "
            "`wt_attr_reconstr=10.0` (vs s38_v16's 3.0) — nearly 4x "
            "stronger cell-NB pressure than the s38_v16 base, 10x "
            "stronger than the default 1.0.\n\n"
            "Hypothesis: in the no-batch-int regime, the cell branch "
            "doesn't have to compete with an adversarial / MMD head "
            "for `z_mlp` gradients — the only competing pulls are "
            "the cosine-BCE adjacency (already at adj-w=2000 here) "
            "and the niche-NB (already weakened to 0.5). Pushing "
            "cell-NB to 10.0 makes per-cell gene-expression "
            "reconstruction by far the dominant signal on z_mlp / "
            "z_q_cell — should maximally sharpen cell-type NMI on "
            "the cell side, potentially at some cost to niche "
            "metrics if z_gnn (downstream of z_mlp) inherits "
            "stronger cell-type signal at the expense of spatial.\n\n"
            "Canonical (sweep-prefix-free) variant name — intended "
            "for `submit_multi_seed.sh` so multi-seed artifacts land "
            "in a clean directory alongside the other canonical-name "
            "comparators.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])", "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=10.0", "+wt_attr_reconstr_nbr=0.5",
            "+wt_adj_reconstr=2000",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_adj_weight(
                        _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_gnn_hidden(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[200, 200, 128],
                                                ),
                                                hidden=128,
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=10.0,
                            ),
                            weight=0.5,
                        ),
                        weight=2000.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "STRONGER-CELL-NB sibling of `s38_v12` (same +enc-deeper "
            "+dec-w32+knn16+sampler16+bs=512+lr=7e-4 spine, "
            "no-batch-int, within-sec). Only delta: "
            "`wt_attr_reconstr=5.0` (vs s38_v12's 2.0). Brackets the "
            "cell-w axis on the s38_v12 spine between the existing "
            "cell-w=2.0 / 3.0 / 5.0 / 10 points so the Pareto knee "
            "is easier to read.\n\n"
            "Hypothesis: in the no-batch-int regime, cell-NB is the "
            "main pull on z_mlp / z_q_cell toward per-cell gene-"
            "expression fidelity. Mid-strong pressure (5.0) may "
            "give the best cell-NMI without over-fitting / "
            "compromising niche metrics the way cell-w=10 might.\n\n"
            "Canonical (sweep-prefix-free) variant name — intended "
            "for `submit_multi_seed.sh` so multi-seed artifacts land "
            "in a clean directory alongside the other canonical-name "
            "comparators.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "DECOUPLED-ENCODERS sibling of the current best variant "
            "(`+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+"
            "lr7e-4+within-sec`). Same architecture as the parent on "
            "every axis EXCEPT the encoder: instead of a shared MLP "
            "trunk feeding both branches, this variant builds a "
            "SEPARATE niche-side MLP with INDEPENDENT weights.\n\n"
            "Architecture diff:\n"
            "  Parent (shared trunk):\n"
            "    x  ->  shared MLP  ->  z_mlp ─┬─► VQ_cell\n"
            "                                  └─► GNN ─► VQ_niche\n"
            "  This variant (decoupled):\n"
            "    x ─┬─►  MLP_cell   ─►  z_mlp_cell  ─► VQ_cell\n"
            "       └─►  MLP_niche  ─►  z_mlp_niche ─► GNN ─► VQ_niche\n\n"
            "Both MLPs share the [400, 400, 256] architecture but have "
            "INDEPENDENT WEIGHTS — spatial-loss gradients (adjacency "
            "BCE + nbr-NB + niche-commit) update `MLP_niche.weight` "
            "only, NEVER reaching `MLP_cell`. The cell-VQ input is "
            "structurally shielded from spatial-loss pull.\n\n"
            "Hypothesis (user's): cell-NMI on the parent variant is "
            "dragged down because the shared encoder MLP's gradients "
            "encode spatial information into z_mlp (which then "
            "becomes the cell-VQ input). Decoupling the MLPs lets the "
            "cell encoder optimise PURELY for cell-NB reconstruction "
            "while the niche encoder owns the spatial pulls. Expected: "
            "cell-NMI lifts, niche metrics roughly stable (the niche "
            "branch sees no architectural change).\n\n"
            "Cost: doubles the encoder MLP parameter count and adds "
            "one extra MLP pass per training step (negligible vs the "
            "GNN + decoder cost).\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared-specific-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "SHARED + PATH-SPECIFIC encoders (Y-shape) sibling of the "
            "current best variant. Same architecture as the parent on "
            "every axis EXCEPT the encoder: instead of a single shared "
            "MLP trunk, this variant uses THREE MLPs — a shared trunk "
            "plus one path-specific MLP per branch — and concatenates "
            "the shared output with each path's output before the "
            "downstream cell-VQ / GNN modules.\n\n"
            "Architecture diff:\n"
            "  Parent (shared trunk only):\n"
            "    x  ->  MLP  ->  z_mlp ─┬─► VQ_cell\n"
            "                            └─► GNN ─► VQ_niche\n"
            "  This variant (Y-shape):\n"
            "    x ─┬─►  MLP_shared (->128) ─► z_shared ──┐\n"
            "       │                                      │ concat\n"
            "       ├─►  MLP_cell   (->128) ─► z_cell_p   ┴─► VQ_cell (256-dim)\n"
            "       │\n"
            "       └─►  MLP_niche  (->128) ─► z_niche_p ─► (concat w/ shared) ─► GNN ─► VQ_niche\n\n"
            "Default sizing halves the last-layer dim of `mlp_params` "
            "[400, 400, 256] for all THREE MLPs ([400, 400, 128]), so "
            "the concatenated downstream input is still 256-dim — "
            "codebook dim, GNN config, decoders are all unchanged.\n\n"
            "Gradient routing:\n"
            "  - Shared MLP: receives gradients from BOTH branches.\n"
            "  - Cell-path MLP: receives ONLY cell-side gradients\n"
            "    (cell-NB + cell-commit). Shielded from spatial pulls.\n"
            "  - Niche-path MLP: receives ONLY niche-side gradients\n"
            "    (adj-BCE + nbr-NB + niche-commit). Shielded from\n"
            "    cell-side pulls.\n\n"
            "Intermediate between the legacy shared trunk (full "
            "gradient coupling) and `+decoupled-enc` (full gradient "
            "isolation). The shared part allows feature reuse for "
            "parameter efficiency; the path-specific parts give each "
            "branch room to specialise. The shared trunk is still "
            "spatial-gradient-touched — if you need FULL isolation "
            "of the cell branch from spatial pulls, use "
            "`+decoupled-enc` instead.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(3 MLPs, each [400, 400, 128] last-layer)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+no-batch-int+enc-512+dec-w256+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "No-batch-integration sibling of `+wide+rvq-both+decoder-"
            "cov+adv+enc-512+dec-w256+within-sec` (the s28_v1 spine "
            "under within-section BCE). Same architecture as the "
            "+adv parent (+wide -> 2-layer SAGE GNN h=256, sampler "
            "[8, 8], 2-hop nbr aggregation; RVQ (30, 90); decoder-"
            "cov embed=16; shallow encoder [512] -> latent dim 512; "
            "decoders [256]) — but with EVERY explicit batch-"
            "integration loss DROPPED (no adv, no mmd-batch, no "
            "mmd-prior). The within-section BCE adjacency scope "
            "(`adj_within_section_only=True`) is set explicitly: "
            "cross-section pairs are dropped from the cosine BCE "
            "negative pool.\n\n"
            "Architecturally identical to `s36_v16_...`; this is the "
            "canonical (sweep-prefix-free) name intended for "
            "`submit_multi_seed.sh` so multi-seed artifacts land in "
            "a clean directory at the same level as the other "
            "canonical-name comparators."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[512])  (latent dim = 512)",
            "+dec-w=[256]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        hidden_channels=[512],
                    ),
                    hidden_channels=[256],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w32+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "No-adv + no-wide + VQ-warmup sibling of `s36_v13` "
            "(`+wide+adv+adj-w3000+enc-shallow+dec-w32+within-sec`). "
            "Three changes from the s36_v13 parent:\n"
            "  1. Drop the adversarial head + loss (no-batch-int).\n"
            "  2. Drop `+wide` — reverts the niche side to 1-layer "
            "     SAGE GNN h=256, sampler [8], 1-hop nbr-NB.\n"
            "  3. Add VQ warmup (`vq_warmup_epochs=10`): the codebook "
            "     is frozen at uniform-init for the first 10 epochs "
            "     (no kmeans init, no EMA, no dead-code expiry; commit "
            "     losses zeroed). The natural codebook init then runs "
            "     at epoch 10 on a z_mlp that's been shaped by the "
            "     spatial-adjacency BCE + cell-NB + nbr-NB rather "
            "     than the epoch-0 (batch-correlated) distribution.\n\n"
            "All other knobs match the s36_v13 architecture: RVQ "
            "(30, 90), decoder-cov embed=16, shallow encoder [256] "
            "(latent dim 256), cell + niche decoders [32], "
            "`wt_adj_reconstr=3000` (very tight spatial pressure), "
            "`adj_within_section_only=True` (cross-section pairs "
            "dropped from the cosine BCE negative pool).\n\n"
            "Canonical (sweep-prefix-free) variant name — intended for "
            "`submit_multi_seed.sh` so multi-seed artifacts land in a "
            "clean directory alongside the other no-batch-int "
            "multiseed comparators.\n\n"
            "Hypothesis: the historic `+wide+adj-w3000+enc-shallow+"
            "dec-w32` family had very strong cluster-quality metrics "
            "(NMI / ARI) but historically relied on the adversarial "
            "head for integration. With cross-section pairs now "
            "masked out of the BCE AND codebook init deferred past "
            "epoch 0, the adversarial head may be redundant — and "
            "the deferred init may avoid the early-epoch lock-in "
            "that the (now-deprecated) global BCE encouraged. "
            "Dropping `+wide` further simplifies: 1-layer GNN + "
            "1-hop nbr-NB stay the historic SQUINT default, so this "
            "variant tests the no-batch-int + VQ-warmup story on the "
            "minimal-architecture spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_shallow(
                            _patch_dual_adj_weight(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                weight=3000.0,
                            ),
                            hidden_channels=[256],
                        ),
                        hidden_channels=[32],
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "No-adv sibling of `s36_v13` "
            "(`+wide+adv+adj-w3000+enc-shallow+dec-w32+within-sec`). "
            "Same architecture as the parent (`+wide` -> 2-layer "
            "SAGE GNN h=256, sampler [8, 8], 2-hop nbr-NB; RVQ "
            "(30, 90); decoder-cov embed=16; shallow encoder [256] "
            "(latent dim 256); cell + niche decoders [32]; "
            "`wt_adj_reconstr=3000`; `adj_within_section_only=True`) "
            "— but with the adversarial head + loss DROPPED via "
            "`+no-batch-int`. No mmd-batch / mmd-prior either; the "
            "ONLY integration pressure left is the (within-section) "
            "BCE adjacency loss + the decoder covariate.\n\n"
            "Architecturally identical to `s36_v14_...`; this is the "
            "canonical (sweep-prefix-free) name intended for "
            "`submit_multi_seed.sh` so multi-seed artifacts land in "
            "a clean directory at the same level as the other "
            "canonical-name comparators.\n\n"
            "Hypothesis: the `+wide+adj-w3000+enc-shallow+dec-w32` "
            "family historically had very strong NMI / ARI but "
            "relied on the adversarial GRL for integration. With "
            "cross-section pairs now masked out of the BCE negative "
            "pool, the adversarial head may be redundant — and "
            "removing it simplifies the optimization (no min-max "
            "game, no GRL alpha to tune, no classifier head in the "
            "optimizer state)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_adj_weight(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD()),
                                        branch="niche", codebook_sizes=(30, 90),
                                    ),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            weight=3000.0,
                        ),
                        hidden_channels=[256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "No-adv + no-wide sibling of `s36_v13` "
            "(`+wide+adv+adj-w3000+enc-shallow+dec-w32+within-sec`). "
            "Two changes from the s36_v13 parent:\n"
            "  1. Drop the adversarial head + loss (no-batch-int).\n"
            "  2. Drop `+wide` — reverts the niche side to 1-layer "
            "     SAGE GNN h=256, sampler [8], 1-hop nbr-NB.\n\n"
            "All other knobs match s36_v13: RVQ (30, 90), decoder-"
            "cov embed=16, shallow encoder [256] (latent dim 256), "
            "cell + niche decoders [32], `wt_adj_reconstr=3000` "
            "(very tight spatial pressure), "
            "`adj_within_section_only=True` (cross-section pairs "
            "dropped from the cosine BCE negative pool).\n\n"
            "Canonical (sweep-prefix-free) variant name — intended "
            "for `submit_multi_seed.sh` so multi-seed artifacts land "
            "in a clean directory alongside the other no-batch-int "
            "multiseed comparators.\n\n"
            "Architectural diff vs the canonical `+wide+...` "
            "sibling: just the `+wide` removal — both drop adv, "
            "both keep `adj-w3000` + within-sec BCE. This is the "
            "minimal-architecture no-batch-int variant on the +adj-"
            "w3000+enc-shallow+dec-w32 spine.\n\n"
            "Companion to the existing `+vq-warmup10` sibling of "
            "the same spine — together they test whether the "
            "codebook-warmup helps over the naive lazy-init."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_adj_weight(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            weight=3000.0,
                        ),
                        hidden_channels=[256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Asymmetric variant: SPATIAL k-NN graph k=16 (densest) "
            "BUT sampler fanout `[8]` (the GNN samples only 8 of 16 "
            "per cell per step). Each epoch the GNN sees a random "
            "8-of-16 per cell — stochastic neighbour subsampling at "
            "the highest available graph density.\n\n"
            "Compared to the +knn12+sampler8 sibling: the underlying "
            "graph has MORE choices (16 vs 12), so the random 8 "
            "selected per epoch covers a wider spatial radius on "
            "average. Tests whether maximising graph density + "
            "keeping the historic sampler fanout `[8]` gives the "
            "best of both: richer spatial signal + per-epoch "
            "augmentation.\n\n"
            "REQUIRES BLOB REBUILD (same as +knn16+sampler16 "
            "sibling)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[8],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-256-256-128+gnn-h128+dec-w32+knn16+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Smaller-latent sibling of `+enc-deeper+dec-w32+knn16+"
            "sampler8`: encoder MLP shrinks from [400, 400, 256] -> "
            "[256, 256, 128] AND niche-branch GNN hidden shrinks "
            "from 256 -> 128 (lock-step with encoder output, so cell "
            "+ niche latents stay SYMMETRIC at 128). Tightens the "
            "codebook embedding dim from 256 -> 128 while keeping "
            "the deep-encoder trunk depth (3 hidden layers) and "
            "the densest-spatial-graph asymmetric sampler "
            "(k=16 graph + sampler [8] = stochastic 8-of-16 per "
            "epoch).\n\n"
            "Hypothesis: at the densest spatial graph, a tighter "
            "latent forces the codebook to discard batch-specific "
            "signal AND average over the richer 16-neighbourhood "
            "more aggressively — potentially a Pareto winner if "
            "the +enc-deeper sibling is over-parametrised relative "
            "to the (30, 90) codebook capacity.\n\n"
            "REQUIRES BLOB REBUILD (same as the parent +knn16+"
            "sampler8 sibling) — the k=16 graph must be present "
            "in the blob."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_gnn_hidden(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            hidden_channels=[256, 256, 128],
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[8],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-256-256-128+gnn-h128+dec-w32+knn16+sampler8+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Within-section-BCE sibling of `+enc-256-256-128+gnn-h128+"
            "dec-w32+knn16+sampler8`. Architecturally identical to the "
            "parent (RVQ (30, 90), decoder-cov embed=16, adv α=1 "
            "wt=150, encoder MLP [256, 256, 128], GNN h=128 — "
            "symmetric 128-dim cell + niche latents, decoders [32], "
            "k=16 spatial graph + stochastic 8-of-16 sampler) — the "
            "only change is `loss_kwargs['adj_within_section_only']="
            "True`, which restricts the cosine BCE adjacency loss to "
            "within-section pairs (cross-section pairs are dropped "
            "from the negative pool, so two biologically-similar "
            "cells in different sections aren't pushed apart by the "
            "spatial-adjacency loss).\n\n"
            "True is now the code default, but the flag is set "
            "explicitly here so the materialised YAML records the "
            "choice unambiguously — useful for the multiseed sweep "
            "(this variant is intended for `submit_multi_seed.sh`).\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_gnn_hidden(
                            _patch_dual_encoder_deeper(
                                _patch_dual_adversarial(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    alpha=1.0, wt_adv_batch=150.0,
                                ),
                                hidden_channels=[256, 256, 128],
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[8],
            ),
            enabled=True,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-256-256-128+gnn-h128+dec-w32+knn16+sampler8+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "VQ-warmup sibling of `+enc-256-256-128+gnn-h128+dec-w32+"
            "knn16+sampler8+within-sec`. Identical to that parent "
            "except for `vq_warmup_epochs=10`: for the first 10 "
            "training epochs the VQ codebook is held at its uniform-"
            "init state (no lazy kmeans-init, no EMA updates, no "
            "dead-code expiry, commit losses zeroed). The encoder "
            "still trains under reconstruction + adversarial + the "
            "newly within-section BCE adjacency loss, so by epoch 10 "
            "`z_mlp` and `z_gnn` have settled away from epoch-0 batch-"
            "correlated directions. At epoch 10 the codebook does its "
            "natural lazy kmeans-init on that much-less-batch-correlated "
            "z_mlp distribution.\n\n"
            "Hypothesis (codebook EMA lock-in): early kmeans-init + EMA "
            "imprints batch-discriminative directions in the codes "
            "before the integration losses have had time to shape z_mlp. "
            "Once imprinted, the EMA only ever sees cells already "
            "routed to those codes, so the lock-in is permanent. "
            "Deferring init avoids this. Symptom we'd expect to see if "
            "this is the right knob: per-batch code utilisation looks "
            "more uniform here than in the no-warmup sibling.\n\n"
            "Note: balanced kmeans-init (stratified-by-section sample "
            "for the kmeans data) is NOT applied here. Argument: the "
            "natural init at epoch 10 already runs on a much-less-"
            "batch-correlated z_mlp, so stratification on top is "
            "expected to be marginal. If this variant lifts `nbr_iLISI`"
            " / `cell_iLISI` materially, we'll layer balanced init on "
            "top as a v2; if not, the lock-in hypothesis was wrong and "
            "balanced init wouldn't have helped either.\n\n"
            "Intended for `submit_multi_seed.sh` — multi-seed run so we "
            "can read off whether the variance reduction (codebook "
            "lock-in is a stochastic-init phenomenon) shows up across "
            "seeds.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_gnn_hidden(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_adversarial(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        alpha=1.0, wt_adv_batch=150.0,
                                    ),
                                    hidden_channels=[256, 256, 128],
                                ),
                                hidden=128,
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[8],
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-128-128-64+gnn-h128+dec-w32+knn16+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "ASYMMETRIC encoder/GNN sibling of `+enc-deeper+dec-w32+"
            "knn16+sampler8`: encoder MLP [128, 128, 64] (tight "
            "cell-branch latent = 64) BUT GNN hidden = 128 (wider "
            "niche-branch latent). z_mlp and z_gnn end up at "
            "DIFFERENT dimensions — the cell-branch codebook lives "
            "in a 64-dim space, the niche-branch codebook in a 128-"
            "dim space.\n\n"
            "Hypothesis: cell-type signal is lower-rank than spatial-"
            "context signal, so the cell branch tolerates / benefits "
            "from a tighter bottleneck while the niche branch "
            "(consuming the k=16 spatial graph + stochastic 8-of-16 "
            "sampler) gets a bit more representational room.\n\n"
            "Compared to the +enc-256-256-128+gnn-h128 sibling: same "
            "GNN width, but the encoder trunk is 2x narrower AND the "
            "cell-branch latent halves to 64. Tests both axes "
            "together — useful if either alone (s31's symmetric "
            "shrinks) wasn't enough.\n\n"
            "REQUIRES BLOB REBUILD (same as the parent +knn16+"
            "sampler8 sibling)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 64])  (cell latent = 64)",
            "+gnn-h=128                       (niche latent = 128; ASYMMETRIC)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_gnn_hidden(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            hidden_channels=[128, 128, 64],
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[8],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-128-128-64+gnn-h128+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "All-16-neighbours sibling of `+enc-128-128-64+gnn-h128+"
            "dec-w32+knn16+sampler8`. Same asymmetric architecture "
            "(encoder MLP [128, 128, 64] -> cell-branch latent 64, "
            "GNN h=128 -> niche-branch latent 128, decoders [32], "
            "k=16 spatial graph, RVQ (30, 90), decoder-cov embed=16, "
            "adv α=1 wt=150) — the only delta is the sampler fanout: "
            "`[16]` instead of `[8]`, so the GNN sees ALL 16 spatial "
            "neighbours per cell per step deterministically (no "
            "stochastic 8-of-16 subsampling).\n\n"
            "Pair-wise A/B vs the `+sampler8` sibling: tests whether "
            "the stochastic neighbour subsampling on the sampler8 "
            "variant acts as useful per-epoch augmentation, or "
            "whether deterministic full-16-neighbour aggregation "
            "(more spatial context per step, no augmentation) is "
            "better for this asymmetric encoder.\n\n"
            "Hypothesis: cell-branch tight latent (64) + niche-branch "
            "wider latent (128) is well-matched to densest spatial "
            "aggregation — niche-side benefits from full 16-NN "
            "context, cell-side benefits from the bottleneck.\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 64])  (cell latent = 64)",
            "+gnn-h=128                       (niche latent = 128; ASYMMETRIC)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([16])           (all 16 neighbours per cell)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_gnn_hidden(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            hidden_channels=[128, 128, 64],
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler16+200ep+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Long-train sibling of `+enc-deeper+dec-w32+knn16+sampler16`: "
            "every architectural knob identical (RVQ (30, 90), "
            "decoder-cov, adv α=1 wt=150, encoder [400, 400, 256], "
            "1-layer SAGE GNN h=256, decoders [32], k=16 graph + "
            "sampler [16]) — only the training-budget cap changes "
            "from `max_epochs=80` -> `max_epochs=200`.\n\n"
            "Hypothesis: the densest-spatial-graph variant may need "
            "more steps for the niche-branch codebook to settle on "
            "the larger 16-NN neighbourhood. Early stopping is "
            "still ENABLED (patience=10 val passes ≈ 20 train "
            "epochs on val_loss), so the run can finish before "
            "epoch 200 if val_loss plateaus.\n\n"
            "CAVEAT (see `make_train_config_dualvq` max_epochs "
            "comment): in the 2026-05-12 sweep, bumping max_epochs "
            "80 -> 200 with the val_loss monitor regressed niche "
            "NMI by ~0.03-0.05 across the board — the val_loss "
            "optimum drifted past the niche-NMI optimum. If this "
            "variant under-performs the 80-epoch sibling on niche "
            "NMI, suspect the same dynamic. Pair with "
            "`SQUINT_WITH_PEARSON=1` to also log "
            "`val_pearson_gene_wise_1hop_nbr_log1p` and pick the "
            "best-niche-Pearson ckpt for post-hoc analysis.\n\n"
            "REQUIRES BLOB REBUILD (same as the 80-epoch sibling) — "
            "the k=16 graph must be present in the blob."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([16])           (matches graph k)",
            "+max_epochs=200          (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            max_epochs=200,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler8+200ep+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Long-train sibling of `+enc-deeper+dec-w32+knn16+sampler8`: "
            "every architectural knob identical (RVQ (30, 90), "
            "decoder-cov, adv α=1 wt=150, encoder [400, 400, 256], "
            "1-layer SAGE GNN h=256, decoders [32], k=16 graph + "
            "sampler [8] — stochastic 8-of-16 per epoch) — only the "
            "training-budget cap changes from `max_epochs=80` -> "
            "`max_epochs=200`.\n\n"
            "Hypothesis: the asymmetric (k=16 graph, sampler [8]) "
            "setup gives per-epoch stochastic augmentation; longer "
            "training lets the niche codebook integrate over more "
            "of those 16-neighbour subsets, potentially improving "
            "spatial-context robustness. Early stopping is still "
            "ENABLED (patience=10 val passes on val_loss).\n\n"
            "CAVEAT (see `make_train_config_dualvq` max_epochs "
            "comment): the 2026-05-12 max_epochs=200 sweep "
            "regressed niche NMI vs the 80-epoch siblings — pair "
            "with `SQUINT_WITH_PEARSON=1` to also log the "
            "niche-Pearson val-monitor and check whether the "
            "best-val_loss ckpt is the best-niche-Pearson ckpt.\n\n"
            "REQUIRES BLOB REBUILD (same as the 80-epoch sibling)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)  (REQUIRES blob rebuild)",
            "+sampler([8])            (stochastic 8-of-16 per epoch)",
            "+max_epochs=200          (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[8],
            ),
            max_epochs=200,
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn6+sampler6+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Sibling of `+enc-deeper+dec-w32` with the SPATIAL "
            "k-NN graph k REDUCED from 8 → 6 AND the sampler fanout "
            "set to match: `[6]`. Each cell has 6 spatial neighbours "
            "in the graph and the GNN samples ALL of them per cell. "
            "Clean 'thinner spatial neighborhood' ablation vs the "
            "parent's k=8 + [8]-sampler.\n\n"
            "Tests whether the niche embedding can still extract "
            "good spatial structure from a thinner k=6 graph.\n\n"
            "REQUIRES BLOB REBUILD (same as +knn12+sampler12 "
            "sibling) — the current blob only has k=8. After "
            "rebuild, k=6 / k=8 / k=12 are all available."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=6)   (REQUIRES blob rebuild)",
            "+sampler([6])            (matches graph k)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=6,
            ),
            num_neighbors=[6],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Most-extreme decoder shrink yet on the +enc-deeper family: "
            "cell + niche decoders [16] (1 hidden of 16, 25× narrower "
            "than the default [400, 400], 2× narrower than the "
            "+dec-w32 sibling). Encoder stays at [400, 400, 256] "
            "(+enc-deeper), latent stays 256, GNN stays 1-layer SAGE "
            "h=256, adv α=1 wt=150.\n\n"
            "Decoder shape: 256 (z_q_cell) + 16 (covariate) = 272 → "
            "16 → 431. The first layer compresses ~17×, the second "
            "expands ~27× to gene space. Tests the absolute floor of "
            "decoder bottleneck on this spine — if this still works, "
            "the codebook (RVQ 30×90 = 2,700 entries) really IS the "
            "representation, and the decoder is little more than a "
            "code→rates lookup table."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[16]  (both cell and niche, 1 hidden of 16)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[16],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-400-400-128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Lower-latent sibling of `+enc-deeper+dec-w32`: encoder "
            "kept wide ([400, 400, 128]) but latent dim halved from "
            "256 → 128. GNN hidden matches at 128 (symmetric "
            "latents). Decoder stays at [32]. Tests whether the "
            "codebook embedding dim alone affects clustering / "
            "integration, with encoder hidden widths unchanged.\n\n"
            "Codebook embedding dim: 128 (was 256). The codebook "
            "still has 30 × 90 = 2,700 composite codes, just at "
            "half the embedding dim — same discrete capacity, "
            "tighter geometry."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 128])",
            "+gnn-hidden(128)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[400, 400, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Moderate joint reduction: encoder hidden widths halved "
            "to [200, 200, 128] AND latent dim halved to 128. GNN "
            "hidden=128 (symmetric latents). Decoder [32]. Tests "
            "the combined effect of narrower encoder + smaller "
            "codebook embedding dim. Parameter-efficient sibling of "
            "`+enc-deeper+dec-w32` — encoder hidden widths shrink "
            "from 400 → 200 (~2× narrower) plus latent from 256 → "
            "128 (~2× smaller codebook geometry)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-hidden(128)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-200-200-64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Aggressive joint reduction: encoder hidden widths "
            "halved to [200, 200, 64] AND latent dim quartered to "
            "64. GNN hidden=64 (symmetric latents). Decoder [32]. "
            "Most compact spine variant — encoder ~2× narrower, "
            "codebook embedding dim ~4× smaller than the dec-w32 "
            "parent. Tests how far the codebook geometry can shrink "
            "before clustering quality breaks."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 64])",
            "+gnn-hidden(64)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 64],
                ),
                hidden=64,
            ),
            hidden_channels=[32],
        ),
    },

    # ---- Asymmetric variants: low encoder dim + WIDER 2-LAYER GNN -----
    # Hypothesis: the parent `+enc-200-200-64+dec-w32` lifted cell-side
    # metrics + integration via the tight cell-VQ codebook (64-dim),
    # but the niche codebook suffered because z_gnn is also 64-dim
    # (limited spatial expressivity at 1-hop). Keep the small encoder
    # latent (cell-side benefit) but give the niche-side GNN MORE
    # capacity: bigger hidden width AND 2 layers (+wide).
    #
    # With these patches the two branches are ASYMMETRIC:
    #   - z_mlp (cell-branch input to VQ_cell) = encoder output dim
    #     (64 or 128, varies per variant)
    #   - z_gnn (niche-branch input to VQ_niche) = GNN hidden_channels
    #     (256 default or 384, varies per variant)
    # The GNN's first SAGEConv layer accepts the small z_mlp and
    # projects to its hidden_channels; the second SAGEConv aggregates
    # over the sampled 2-hop neighbourhood at the bigger width.

    "dualvq+wide+rvq-both+decoder-cov+adv+enc-200-200-64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Same encoder + decoder as `+enc-200-200-64+dec-w32` but "
            "with +wide added: 2-layer SAGE GNN h=256, sampler "
            "[8, 8], 2-hop nbr aggregation. Asymmetric latents — "
            "z_mlp=64 (cell branch, small), z_gnn=256 (niche branch, "
            "big). Tests whether the niche-side richer GNN context "
            "recovers the niche-codebook quality lost in the parent "
            "while preserving the cell-side integration gain."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 64])  (z_mlp = 64)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[200, 200, 64],
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-200-200-64+gnn-h384+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Same as the +wide variant above but with the GNN hidden "
            "width bumped from 256 → 384. Encoder stays at [200, "
            "200, 64], so z_mlp=64 (tight cell branch) and z_gnn=384 "
            "(wider niche branch). Most asymmetric variant in this "
            "family — niche-side codebook geometry becomes 384-dim "
            "while cell-side stays at 64-dim. Tests whether more "
            "niche-side capacity translates to better niche metrics "
            "without breaking the cell-side wins."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 64])  (z_mlp = 64)",
            "+gnn-hidden(384)  (z_gnn = 384)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 64],
                ),
                hidden=384,
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-200-200-128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Encoder one-step-up from [200, 200, 64] → [200, 200, "
            "128] (z_mlp = 128). +wide spine: 2-layer SAGE GNN "
            "h=256, sampler [8, 8], 2-hop nbr aggregation. "
            "Asymmetric latents — z_mlp=128, z_gnn=256. Modest "
            "step-up in cell-branch latent capacity vs the 64-dim "
            "variant, keeps niche side at the wide default."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])  (z_mlp = 128)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[200, 200, 128],
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h384+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Same as the +enc-200-200-128 +wide variant above but "
            "with GNN hidden bumped to 384. z_mlp=128 (cell branch), "
            "z_gnn=384 (niche branch). Combines moderate cell-side "
            "expansion with maximum niche-side capacity in this "
            "family."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])  (z_mlp = 128)",
            "+gnn-hidden(384)  (z_gnn = 384)",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 128],
                ),
                hidden=384,
            ),
            hidden_channels=[32],
        ),
    },

    "dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Same architecture as `+enc-deeper+dec-w64` (encoder MLP "
            "[400, 400, 256], 1L SAGE GNN h=256, decoders [64], "
            "RVQ (30, 90), decoder-cov embed=16) but with the "
            "adversarial head REPLACED by `+mmd-prior(w=50, "
            "prior_std=1.0)`. Tests the NicheCompass-style prior-"
            "matching integration mechanism on the most decoder-"
            "constrained spine — the extreme bottleneck combined "
            "with non-adversarial integration."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-prior(w=50, prior_std=1.0)  (NO +adv)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]  (both cell and niche, 1 hidden of 64)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_prior(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    wt_mmd_prior=50.0, prior_std=1.0,
                ),
            ),
            hidden_channels=[64],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-shallow+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Symmetric tight variant — both encoder MLP AND decoders "
            "are 1 hidden layer of 256. Encoder shrunk from default "
            "[400, 256] (2 hidden) → [256] (1 hidden, output 256), "
            "decoders shrunk from [400, 400] (2 hidden of 400) → "
            "[256] (1 hidden of 256). Latent dim preserved at 256 "
            "across both branches. All other knobs (1-layer SAGE GNN, "
            "RVQ (30, 90), decoder-cov embed=16, adv α=1 wt=150) "
            "match the +enc-deeper+dec-w256 sibling — only the "
            "encoder depth changes (3 → 1 hidden layer)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[256])",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_shallow(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[256],
            ),
            hidden_channels=[256],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-shallow+niche-neck-256-256+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Cell-branch-only-shallow architecture with a SEPARATE "
            "deeper niche path. The cell branch sees a SHORT encoder "
            "MLP `[256]` (1 hidden of 256) → z_mlp → VQ_cell. The "
            "niche branch reuses z_mlp but routes it through an "
            "EXTRA `[256, 256]` niche-neck MLP (2 hidden of 256) "
            "before the GNN, so niche-side gradients (adjacency BCE, "
            "nbr-NB, niche-commit) shape THAT new tensor instead of "
            "pulling z_mlp away from cell-type-discriminative "
            "geometry.\n\n"
            "Schematic:\n"
            "  Input(431) → MLP[256] → z_mlp ─→ VQ_cell → cell decoder\n"
            "                              └─→ MLP[256, 256] → GNN\n"
            "                                                  → z_gnn → VQ_niche → niche decoder\n\n"
            "All latent dims stay at 256 (z_mlp = niche_neck_out = "
            "z_gnn = 256), so the codebooks operate on matched-dim "
            "embeddings. Other knobs (RVQ (30, 90), 1-layer SAGE "
            "GNN, decoder-cov embed=16, adv α=1 wt=150, decoder "
            "[256]) match the +enc-shallow+dec-w256 sibling — the "
            "ONLY change is the addition of the 2-hidden niche-neck."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[256])  (cell-branch encoder)",
            "+niche-neck(mlp=[256, 256])  (niche-only extra MLP before GNN)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_niche_neck(
                _patch_dual_encoder_shallow(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[256],
                ),
                hidden_channels=[256, 256],
            ),
            hidden_channels=[256],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-shallow+niche-neck-256-256+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Tight-decoder sibling of `+enc-shallow+niche-neck-256-256"
            "+dec-w256`: same niche-neck decoupling architecture "
            "(encoder MLP [256], niche-neck MLP [256, 256], 1L SAGE "
            "GNN h=256, RVQ (30, 90), decoder-cov embed=16, adv α=1 "
            "wt=150) but with the cell + niche decoders shrunk from "
            "[256] → [32] (1 hidden of 32, 8× narrower).\n\n"
            "Combines two structural pressures that empirically each "
            "worked: (a) niche-neck decoupling — protects the cell "
            "quantiser from niche-side gradients — and (b) extreme "
            "decoder bottleneck — forces the codebook to carry the "
            "reconstruction signal. The +dec-w32 size already worked "
            "on the +enc-deeper spine; this tests whether it also "
            "works on the decoupled niche-neck spine that gave good "
            "cell-NMI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[256])  (cell-branch encoder)",
            "+niche-neck(mlp=[256, 256])  (niche-only extra MLP before GNN)",
            "+dec-w=[32]  (both cell and niche, 1 hidden of 32)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_niche_neck(
                _patch_dual_encoder_shallow(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[256],
                ),
                hidden_channels=[256, 256],
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-256-256+niche-neck-256+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Inverse-asymmetry of the parent `+enc-shallow+niche-neck"
            "-256-256+dec-w256`: the original variant had a SHALLOW "
            "cell-encoder [256] + DEEP niche-neck [256, 256], which "
            "gave good cell-NMI but weak integration. This variant "
            "FLIPS the depth allocation — DEEP cell-encoder [256, 256] "
            "+ SHALLOW niche-neck [256]. Tests whether more cell-side "
            "encoder capacity (before z_mlp reaches VQ_cell AND the "
            "niche-neck branch) gives the encoder more room to learn "
            "batch-invariant features for both branches.\n\n"
            "Schematic:\n"
            "  Input(431) → MLP[256, 256] → z_mlp ─→ VQ_cell → cell decoder\n"
            "                                    └─→ MLP[256] → GNN\n"
            "                                                  → z_gnn → VQ_niche → niche decoder\n\n"
            "Latent dims symmetric at 256 (z_mlp = niche_neck_out = "
            "z_gnn = 256). RVQ (30, 90), decoder [256], decoder-cov "
            "embed=16, adv α=1 wt=150 all unchanged."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256])  (cell-branch encoder)",
            "+niche-neck(mlp=[256])  (shallow neck — 1 hidden of 256)",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_niche_neck(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[256, 256],
                ),
                hidden_channels=[256],
            ),
            hidden_channels=[256],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-256-256+niche-neck-256-256+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Symmetric-depth decoupled variant — encoder MLP and "
            "niche-neck MLP have MATCHED depth (both 2 hidden layers "
            "of 256). The cell branch sees `MLP[256, 256]` → z_mlp → "
            "VQ_cell. The niche branch routes z_mlp through a SECOND "
            "`MLP[256, 256]` (niche-neck) before the GNN, so the "
            "niche path effectively has 4 MLP layers (encoder + neck) "
            "between input and GNN.\n\n"
            "Schematic:\n"
            "  Input(431) → MLP[256, 256] → z_mlp ─→ VQ_cell → cell decoder\n"
            "                                    └─→ MLP[256, 256] → GNN\n"
            "                                                       → z_gnn → VQ_niche → niche decoder\n\n"
            "All latents stay at 256 (z_mlp = niche_neck_out = z_gnn "
            "= 256). Compare to the `+enc-shallow+niche-neck-256-256` "
            "sibling: that one had a single-layer encoder MLP; this "
            "one has a 2-layer encoder. Tests whether MORE cell-side "
            "encoder capacity (when paired with the niche-neck "
            "decoupling) lifts cell-branch metrics."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256])  (cell-branch encoder)",
            "+niche-neck(mlp=[256, 256])  (niche-only extra MLP before GNN)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_niche_neck(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[256, 256],
                ),
                hidden_channels=[256, 256],
            ),
            hidden_channels=[256],
        ),
    },

    # ---- Sweep 27: encoder-shape ablations on the +enc-deeper+dec-w256 spine
    # All 4 variants share the same parent spine as
    # `dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p`:
    #   - 1-layer SAGE GNN, h=256
    #   - RVQ (30, 90) on both branches
    #   - decoder-cov embed=16
    #   - adversarial α=1, wt=150 (no warmup)
    #   - cell + niche decoders [256] (1 hidden of 256)
    # Only the ENCODER MLP shape changes.
    #
    # IMPORTANT: the GNN's hidden_channels stays at 256, so when the
    # encoder output (= last entry of mlp_params.hidden_channels)
    # differs from 256, the two branches have ASYMMETRIC latent dims:
    #   - z_mlp (cell-branch input to VQ_cell) = encoder output dim
    #   - z_gnn (niche-branch input to VQ_niche) = 256 (unchanged)
    # The cell-branch codebook embedding dim follows z_mlp; the
    # niche-branch codebook embedding dim follows z_gnn = 256. This
    # is the minimal-change interpretation of "change the encoder
    # only" — if you want symmetric latents, layer a
    # `_patch_dual_gnn_hidden(<latent>)` patch on top.
    #
    # Variants:
    #   v1: encoder [512]                        (1 hidden of 512; latent=512)
    #   v2: encoder [128, 128, 128]              (3 hidden of 128; latent=128)
    #   v3: encoder [256, 256, 256, 256]         (4 hidden of 256; latent=256 — symmetric)
    #   v4: encoder [512, 256, 128]              (funnel; latent=128)
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v27

    "s27_v1_dualvq+rvq-both+decoder-cov+adv+enc-512+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s27 sweep, variant 1: parent spine + encoder MLP [512] "
            "(1 hidden layer of 512, output dim 512). WIDE single "
            "hidden layer — latent dim becomes 512, asymmetric vs "
            "the niche branch's z_gnn=256. Tests whether a wide "
            "shallow encoder captures more pre-VQ signal than the "
            "deeper [400, 400, 256] parent."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[512])  (latent dim = 512)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_shallow(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[512],
            ),
            hidden_channels=[256],
        ),
    },
    "s27_v2_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s27 sweep, variant 2: parent spine + encoder MLP "
            "[128, 128, 128] (3 hidden layers of 128, output dim "
            "128). DEEP and NARROW — latent dim becomes 128, "
            "asymmetric vs z_gnn=256. Tests whether a tight "
            "multi-layer encoder gives a more compact + abstract "
            "cell-branch latent than the wider parent."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 128])  (latent dim = 128)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[128, 128, 128],
            ),
            hidden_channels=[256],
        ),
    },
    "s27_v3_dualvq+rvq-both+decoder-cov+adv+enc-256-256-256-256+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s27 sweep, variant 3: parent spine + encoder MLP "
            "[256, 256, 256, 256] (4 hidden layers of 256, output "
            "dim 256). MAX DEPTH at matched latent — latent stays "
            "at 256 (symmetric vs z_gnn). Tests whether more "
            "encoder depth alone improves the pre-VQ representation "
            "without changing the latent geometry."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256, 256, 256])  (latent dim = 256)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[256, 256, 256, 256],
            ),
            hidden_channels=[256],
        ),
    },
    "s27_v4_dualvq+rvq-both+decoder-cov+adv+enc-512-256-128+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s27 sweep, variant 4: parent spine + encoder MLP "
            "[512, 256, 128] (3 hidden layers, FUNNEL shape — wide "
            "→ narrow). Latent dim ends at 128 (the funnel "
            "bottom). Asymmetric vs z_gnn=256. Tests whether a "
            "gradually-funneling encoder captures cell features at "
            "multiple scales before bottlenecking, vs the uniform-"
            "width parent."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[512, 256, 128])  (latent dim = 128, funnel)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[512, 256, 128],
            ),
            hidden_channels=[256],
        ),
    },

    # ---- Sweep 28: s27 encoder shapes with 2-LAYER GNN ----------------
    # Mirror of s27 with the GNN bumped from 1 layer to 2 layers via
    # `+wide`. Same 4 encoder shapes as s27 (v1..v4 mappings preserved),
    # same decoder [256], same RVQ (30, 90), decoder-cov, adv. Only the
    # GNN depth changes:
    #   - GNN num_layers: 1 → 2 (from +wide)
    #   - Sampler fanout: [8] → [8, 8] (from +wide, matches 2L GNN)
    #   - Nbr-aggregation hops: 1 → 2 (from +wide)
    #
    # `+wide`'s codebook bump (50) is overwritten by the subsequent
    # +rvq patches, so codebook stays at RVQ (30, 90).
    #
    # The same encoder-output-dim asymmetry note applies as in s27:
    # where encoder output ≠ 256, z_mlp (cell branch) and z_gnn (niche
    # branch, still =256) have different dims.
    #
    # Cross-reference s27_vN ↔ s28_vN for the "does 2-hop niche
    # aggregation help this encoder shape?" A/B at each shape.
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v28

    "s28_v1_dualvq+wide+rvq-both+decoder-cov+adv+enc-512+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s28 sweep, variant 1: same encoder shape as s27_v1 "
            "([512], 1 hidden of 512, latent=512), decoder [256], "
            "but with 2-LAYER GNN via +wide (sampler [8, 8], 2-hop "
            "nbr aggregation). Tests whether the 2-hop niche-side "
            "aggregation lifts metrics on the wide-shallow encoder."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[512])  (latent dim = 512)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_shallow(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[512],
            ),
            hidden_channels=[256],
        ),
    },
    "s28_v2_dualvq+wide+rvq-both+decoder-cov+adv+enc-128-128-128+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s28 sweep, variant 2: same encoder shape as s27_v2 "
            "([128, 128, 128], 3 hidden of 128, latent=128), decoder "
            "[256], but with 2-LAYER GNN via +wide. Tests whether "
            "the deeper narrow encoder benefits from richer spatial "
            "context on the niche side."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 128])  (latent dim = 128)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[128, 128, 128],
            ),
            hidden_channels=[256],
        ),
    },
    "s28_v3_dualvq+wide+rvq-both+decoder-cov+adv+enc-256-256-256-256+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s28 sweep, variant 3: same encoder shape as s27_v3 "
            "([256, 256, 256, 256], 4 hidden of 256, latent=256), "
            "decoder [256], but with 2-LAYER GNN via +wide. "
            "Symmetric latents (z_mlp=z_gnn=256) plus 2-hop niche "
            "aggregation — strongest depth + symmetry candidate."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[256, 256, 256, 256])  (latent dim = 256)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[256, 256, 256, 256],
            ),
            hidden_channels=[256],
        ),
    },
    "s28_v4_dualvq+wide+rvq-both+decoder-cov+adv+enc-512-256-128+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s28 sweep, variant 4: same encoder shape as s27_v4 "
            "([512, 256, 128], funnel, latent=128), decoder [256], "
            "but with 2-LAYER GNN via +wide. Tests whether the "
            "funneling encoder pairs well with deeper spatial "
            "aggregation."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[512, 256, 128])  (latent dim = 128, funnel)",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
                hidden_channels=[512, 256, 128],
            ),
            hidden_channels=[256],
        ),
    },

    # ---- Sweep 29: batch-integration mechanism comparison ----------
    # Tests the new MMD-to-prior loss against the existing adversarial
    # + MMD-batch mechanisms. All 6 variants share the
    # `+enc-deeper+dec-w256` spine (the user's "works reasonably well"
    # reference). Only the BATCH-INTEGRATION term varies.
    #
    # Spine (all variants):
    #   - Encoder MLP [400, 400, 256] (+enc-deeper)
    #   - GNN: SAGE h=256, 1 layer (dual-base default)
    #   - VQ cell + niche: RVQ (30, 90)
    #   - Decoders [256] (+dec-w256)
    #   - Decoder cov embed=16
    #
    # Integration variants:
    #   v1: +adv only                            (reference; = parent)
    #   v2: +mmd-prior(w=50)                     (NEW: prior-matching only)
    #   v3: +mmd-prior(w=200)                    (NEW: stronger prior pull)
    #   v4: +mmd-batch(w=200)                    (existing: per-batch MMD only)
    #   v5: +adv + +mmd-prior(w=50)              (stacked: GRL + prior)
    #   v6: +mmd-prior(w=50) + +mmd-batch(w=100) (stacked: prior + batch-MMD)
    #
    # Reading the grid:
    #   - v2 vs v1: does prior-matching alone replace adv?
    #   - v3 vs v2: does more prior weight help?
    #   - v4 vs v2: prior-matching vs batch-MMD as adv replacement
    #   - v5 vs v1: does stacking prior with adv lift things?
    #   - v6 vs v2/v4: do the two MMD axes complement each other?
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v29

    "s29_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 1 (REFERENCE): the +enc-deeper+dec-w256 "
            "spine with canonical +adv (α=1, wt=150). Direct alias of "
            "the parent variant; included here so the s29 sweep is "
            "self-contained for the integration-mechanism A/B."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "s29_v2_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 2: parent spine WITHOUT +adv, with "
            "+mmd-prior(w=50, prior_std=1.0). Pure NicheCompass-style "
            "prior-matching integration — pulls z_mlp toward N(0, I) "
            "via MMD. No min-max game, no batch labels needed."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-prior(w=50, prior_std=1.0)  (NO +adv)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_prior(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    wt_mmd_prior=50.0, prior_std=1.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "s29_v3_dualvq+rvq-both+decoder-cov+mmd-prior-w200+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 3: same as v2 but with stronger "
            "+mmd-prior(w=200, prior_std=1.0). Tests the prior-pull "
            "weight axis — 4× the v2 weight."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-prior(w=200, prior_std=1.0)  (NO +adv)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_prior(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    wt_mmd_prior=200.0, prior_std=1.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "s29_v4_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 4: parent spine WITHOUT +adv, with "
            "+mmd-batch(w=200) — the existing per-batch MMD loss. "
            "Direct comparison to v2/v3 (prior-matching) at matched "
            "weight magnitude. Tests: does matching batches to each "
            "other work better than matching them to a prior?"
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-batch(w=200)  (NO +adv)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_batch(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    wt_mmd_batch=200.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "s29_v5_dualvq+rvq-both+decoder-cov+adv+mmd-prior-w50+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 5: parent spine + canonical +adv + "
            "+mmd-prior(w=50). STACKED: GRL adversarial pressure on "
            "top of prior-matching. Tests whether the two mechanisms "
            "are complementary."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmd-prior(w=50, prior_std=1.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_prior(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    wt_mmd_prior=50.0, prior_std=1.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },
    "s29_v6_dualvq+rvq-both+decoder-cov+mmd-prior-w50+mmd-batch-w100+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s29 sweep, variant 6: parent spine WITHOUT +adv, with "
            "+mmd-prior(w=50) + +mmd-batch(w=100). STACKED: two MMD "
            "axes — match batches to each other AND to a prior. "
            "Tests whether the two distance-matching pressures are "
            "complementary, with no adversarial."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-prior(w=50, prior_std=1.0)",
            "+mmd-batch(w=100)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_batch(
                    _patch_dual_mmd_prior(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        wt_mmd_prior=50.0, prior_std=1.0,
                    ),
                    wt_mmd_batch=100.0,
                ),
            ),
            hidden_channels=[256],
        ),
    },

    "dualvq+rvq-both+decoder-cov+adv+gnn-h384+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Spine ablation — GNN hidden 256 -> 384. Wider niche-branch "
            "GNN; larger spatial-aggregation capacity. Note: this also "
            "widens the niche latent dim (= GNN output) so the niche "
            "codebook embedding is wider too. Modest param + memory "
            "increase (~1.5x on the GNN); likely upside on datasets "
            "with rich spatial domain structure."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+gnn-h(384)",
        ],
        "build": lambda: _patch_dual_gnn_hidden(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            hidden=384,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p 2-layer-spine ablation matrix v3 (codebook scan).
    # ========================================================================
    # Spine combines the v1 winners: +wide (best on niche ID) + adj-w250
    # (best on batch integration), plus the standard RVQ-both +
    # decoder-cov + adv. Each variant ablates one knob from this spine,
    # focused on CODEBOOK CAPACITY + STRUCTURE — the lever most likely
    # to push niche ID further now that the spatial-context (wide) and
    # batch-correction (w250) axes are pinned to their winners. Group
    # via `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v3`.
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine — combine the v1 winners. +wide (2-layer GNN, "
            "sampler [8,8], 2-hop nbr aggregation) was best on niche "
            "identification; +adj-w250 (cosine adjacency BCE weight "
            "1000 -> 250) was best on batch integration. Stack both; "
            "everything else is the standard recipe (RVQ-both 30/90, "
            "decoder covariate, adversarial GRL alpha=1.0 wt=150)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both-50-90+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + RVQ k1: 30 -> 50. Same residual layer (k2=90); "
            "macro codebook gets ~1.7x more entries. Targets niche ID "
            "headroom: with `+wide` already capturing more spatial "
            "context, a finer macro partition is the natural next step."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[50, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(50, 90),
                        ),
                        branch="cell", codebook_sizes=(50, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both-80-90+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + RVQ k1: 30 -> 80. Aggressive scan on the macro "
            "codebook; k1=80 is roughly the empirical n_unique cell "
            "subtypes in mouse-brain MERFISH/STARmap atlases. If 50 "
            "isn't enough, this should saturate the metric."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[80, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(80, 90),
                        ),
                        branch="cell", codebook_sizes=(80, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both-50-150+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + RVQ levels 30/90 -> 50/150. Bumps BOTH layers "
            "(macro + residual). Tests whether the extra residual "
            "headroom helps or just adds redundant codes."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[50, 150])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(50, 150),
                        ),
                        branch="cell", codebook_sizes=(50, 150),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both-80-200+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + RVQ levels 30/90 -> 80/200. Most aggressive "
            "codebook scan; most likely to overfit on a 2-section "
            "dataset (~85k cells), but worth knowing where the cliff "
            "is for the wide+w250 spine."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[80, 200])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(80, 200),
                        ),
                        branch="cell", codebook_sizes=(80, 200),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + 3-level RVQ (30, 60, 120) instead of 2-level "
            "(30, 90). Same total nominal capacity (3*60 vs 1*90 "
            "residual entries) but split across two refinement layers, "
            "encouraging hierarchical decomposition."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+cvq-both-30-10+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + ConditionalVQ (tree-structured) on both branches "
            "in place of RVQ. K1=30 macro buckets, K2=10 sub-children "
            "per bucket = 300 effective codes per branch. Different "
            "inductive bias from RVQ: each level-2 entry is "
            "CONDITIONAL on the level-1 macro bucket (strict tree), "
            "while RVQ refines additively. Fits when the data is "
            "naturally hierarchical (cell type -> cell state)."
        ),
        "patches": [
            "+wide", "+cvq(branch=both, k1=30, k2=10)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_cvq(
                        _patch_dual_wide(_BD()),
                        branch="both", k1=30, k2=10,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+cvq-both-50-10+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + larger ConditionalVQ. K1=50 macro buckets, "
            "K2=10 sub-children = 500 effective codes per branch. "
            "Direct A/B against `+cvq-both-30-10` to scan macro "
            "capacity within the tree-VQ family."
        ),
        "patches": [
            "+wide", "+cvq(branch=both, k1=50, k2=10)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=250)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_cvq(
                        _patch_dual_wide(_BD()),
                        branch="both", k1=50, k2=10,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=250.0,
        ),
    },
    # ========================================================================
    # v4 ablations (final round): hybridise the two strongest non-wide
    # signals (`+enc-deeper`, `+adj-w3000`) with the niche-NMI winner
    # `+wide+rvq-both+decoder-cov+adv`, and explore a NEW axis —
    # adversarial warmup — to fix the cell-pearson + cell-NMI regressions
    # that `+wide` introduced. Spine for all 8: the v3 best
    # `dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p`.
    #
    # Justification (from v1+v2+v3 metrics on summary_long.csv):
    #   - `+enc-deeper` is the empirical winner on every batch-integration
    #     metric (cell iLISI 0.51 vs 0.23 baseline; nbr iLISI 0.075 vs
    #     0.007; cell MMD 0.0033 vs 0.0063), at only 4% niche-NMI cost.
    #     Untried with `+wide`. Highest expected ROI.
    #   - `+adj-w3000` is the second-strongest signal (niche NMI 0.6618
    #     + cell iLISI 0.40 simultaneously). High adjacency pressure
    #     pushes the GNN to encode shared spatial structure across
    #     batches. Untried with `+wide` (we've done w250 / w1000 only).
    #   - `+adv-warmup10` is a NEW axis: zero out the GRL alpha for the
    #     first 10 epochs so codes settle on biology before
    #     batch-invariance pressure kicks in. Targets the cell-pearson +
    #     cell-NMI regression.
    # ========================================================================
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + deeper encoder MLP ([400, 400, 256] vs [400, "
            "256]). `+enc-deeper` was the standalone empirical winner "
            "on every batch-integration metric; this variant tests "
            "whether stacking on top of `+wide` recovers the iLISI "
            "gap without sacrificing niche NMI."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Combination of: `+wide` spine (2-layer SAGE GNN h=256, "
            "sampler [8, 8], 2-hop nbr aggregation) + `+enc-deeper` "
            "encoder ([400, 400, 256], 3 hidden layers) + extreme "
            "decoder bottleneck `+dec-w64` ([64], 1 hidden of 64) + "
            "the new NicheCompass-style `+mmd-prior(w=50, "
            "prior_std=1.0)` integration mechanism (REPLACES "
            "adversarial). RVQ (30, 90) on both branches, decoder-cov "
            "embed=16.\n\n"
            "Stacks four pressures simultaneously: (1) richer "
            "spatial context via 2-hop GNN, (2) deeper encoder for "
            "pre-VQ features, (3) tight decoder forcing the latent "
            "to encode reconstruction signal, (4) prior-matching "
            "MMD pulling z_mlp toward N(0, I) for batch invariance. "
            "Aggressive multi-axis architecture — high-risk, "
            "high-potential-reward candidate if all four pressures "
            "compose well."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd-prior(w=50, prior_std=1.0)  (NO +adv)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_mmd_prior(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    wt_mmd_prior=50.0, prior_std=1.0,
                ),
            ),
            hidden_channels=[64],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+dec-w64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Adversarial sibling of `+wide+mmd-prior-w50+enc-deeper+"
            "dec-w64`: same architecture (+wide 2L SAGE GNN h=256, "
            "+enc-deeper MLP [400, 400, 256], decoders [64], RVQ "
            "(30, 90), decoder-cov embed=16) but uses canonical "
            "adversarial `+adv` (α=1, wt=150) instead of the new "
            "MMD-to-prior loss. Direct A/B for the integration-"
            "mechanism question on the multi-axis aggressive spine."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[64],
        ),
    },
    # Throughput-test variant: same as `+enc-deeper` above but with
    # the NeighborSampler fanout cut from [8, 8] → [4, 4]. 2-hop is
    # preserved (matching the +wide GNN's 2 layers); each hop just
    # samples 4 instead of 8. Sampled neighbourhood per cell drops
    # from 1 + 8 + 8×8 = 73 nodes to 1 + 4 + 4×4 = 21 nodes (~70%
    # reduction). Big expected wall-clock win on both data prep AND
    # the niche-GNN forward pass; the question is whether nbr_NMI /
    # iLISI hold.
    #
    # Why [4, 4] specifically (not [4] like s20_v4 / s18):
    #   - +wide sets GNN num_layers=2, so the second layer aggregates
    #     over hop-2 neighbours sampled by the second entry of the
    #     fanout list. A [4]-fanout would leave hop-2 with zero
    #     sampled neighbours — the GNN's second layer aggregates
    #     over an empty set → niche embedding collapses (s20_v4
    #     showed exactly this: nbr_NMI 0.68 → 0.11).
    #   - [4, 4] preserves the 2-hop structure, just thinner.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+sampler4-4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Throughput-test sibling of the +enc-deeper variant above: "
            "identical build chain, plus `+sampler([4, 4])` to cut the "
            "NeighborSampler fanout from [8, 8] → [4, 4]. Tests whether "
            "the niche GNN tolerates a thinner 2-hop neighborhood "
            "(~70% fewer sampled nodes per batch) without losing "
            "nbr_NMI / iLISI. If metrics hold, this is a cheap "
            "wall-clock win we can roll out to other variants."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+sampler([4, 4])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            num_neighbors=[4, 4],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + much stronger adjacency BCE (3000 vs default "
            "1000). On the non-wide spine, `+adj-w3000` was the best "
            "co-optimiser of niche NMI (0.66) and cell iLISI (0.40); "
            "untried with `+wide`. Tests whether wide+higher-adj is "
            "Pareto-optimal on niche identification + batch integration."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=3000.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Symmetric tight variant on the `+wide+adj-w3000` spine. "
            "Encoder MLP shrunk from default [400, 256] (2 hidden) → "
            "[256] (1 hidden of 256, output 256). Cell + niche decoders "
            "shrunk from [400, 400] (2 hidden of 400) → [256] (1 hidden "
            "of 256). Latent dim preserved at 256 across both branches. "
            "All other knobs (+wide → 2-layer SAGE GNN h=256, sampler "
            "[8, 8], 2-hop nbr aggregation; RVQ (30, 90); decoder-cov "
            "embed=16; adv α=1 wt=150; adj weight 3000) match the "
            "parent +wide+adj-w3000 variant — only the MLP geometries "
            "change."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)",
            "+enc-shallow(mlp=[256])",
            "+dec-w=[256]  (both cell and niche, 1 hidden layer)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_shallow(
                _patch_dual_adj_weight(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    weight=3000.0,
                ),
                hidden_channels=[256],
            ),
            hidden_channels=[256],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Tight-decoder sibling of `+wide+adj-w3000+enc-shallow"
            "+dec-w256`: same +wide spine (2-layer SAGE GNN h=256, "
            "sampler [8, 8], 2-hop nbr aggregation) + enc-shallow "
            "[256] + adj-w3000 + RVQ (30, 90) + decoder-cov embed=16 "
            "+ adv α=1 wt=150, but the cell + niche decoders shrunk "
            "from [256] → [32] (1 hidden of 32, 8× narrower).\n\n"
            "Parent variant had very good cluster-quality metrics "
            "(NMI / ARI) but weak batch integration (iLISI). "
            "Hypothesis: the smaller decoder forces the codebook to "
            "carry the reconstruction signal, which may pressure the "
            "encoder to drop batch-specific features (since the "
            "decoder no longer has slack capacity to absorb them via "
            "the decoder covariate). If integration lifts without "
            "sacrificing the NMI/ARI win, this is a Pareto-improved "
            "sibling.\n\n"
            "Decoder shape: 256 (z_q) + 16 (covariate) = 272 → 32 → "
            "431. Heavy compression (~8.5×) at the input, large "
            "expansion (~13.5×) at the output."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)",
            "+enc-shallow(mlp=[256])",
            "+dec-w=[32]  (both cell and niche, 1 hidden of 32)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_shallow(
                _patch_dual_adj_weight(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    weight=3000.0,
                ),
                hidden_channels=[256],
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adj-w3000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + the two strongest empirical knobs stacked: "
            "deeper encoder MLP + adj weight 3000. Best-case Pareto "
            "winner; if the two signals interact additively this is "
            "the variant most likely to top all four priority metrics "
            "simultaneously."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper", "+adj(weight=3000)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            weight=3000.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Same as `+wide+adv+enc-deeper+dec-w32+sampler4` but with "
            "the DEFAULT [8, 8] sampler (no fanout reduction). Spine: "
            "+wide (2-layer SAGE GNN h=256, sampler [8, 8], 2-hop "
            "nbr aggregation) + +enc-deeper ([400, 400, 256]) + "
            "RVQ (30, 90) + decoder-cov embed=16 + adv α=1 wt=150 + "
            "extreme decoder bottleneck `+dec-w=[32]`.\n\n"
            "Clean A/B pair with the `+sampler4` sibling — same "
            "architecture, only the per-hop fanout differs ([8, 8] "
            "here vs [4, 4] there). Isolates the sampler-fanout "
            "effect from everything else.\n\n"
            "Also compare to:\n"
            "  - `+enc-deeper+dec-w32` (1-layer GNN): isolates the "
            "+wide (2-hop GNN) contribution at the extreme decoder "
            "bottleneck.\n"
            "  - `+wide+adv+enc-deeper` (default decoder [400, 400]): "
            "isolates the decoder shrink on the +wide+enc-deeper "
            "spine."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Combines `+wide` (2-layer SAGE GNN h=256) + `+enc-deeper` "
            "([400, 400, 256] encoder) + `+dec-w=[32]` (extreme decoder "
            "bottleneck) + reduced sampler. RVQ (30, 90), decoder-cov "
            "embed=16, adv α=1 wt=150 unchanged.\n\n"
            "Sampler: `[4, 4]` (2-hop, 4 nbrs each hop) — matches the "
            "2-layer GNN from +wide. NOT `[4]` (1-hop only) which "
            "would leave the GNN's second layer aggregating over "
            "empty neighbourhoods — that's the failure mode that "
            "caused the historic s20_v4 (+wide+sampler4) nbr_NMI "
            "collapse to 0.11. The `+sampler4` tag here is shorthand "
            "for `[4, 4]` on the +wide spine.\n\n"
            "Tests three orthogonal compressions stacked:\n"
            "  - Extreme decoder bottleneck `[32]` (forces codebook "
            "to carry recon signal)\n"
            "  - 2-hop GNN context (+wide spine: spatial signal at 2 "
            "hops)\n"
            "  - Thinner sampling [4, 4] (less spatial signal per "
            "step, but consistent with GNN depth)\n"
            "Sampled neighbourhood per cell drops from 1 + 8 + 8×8 = "
            "73 nodes to 1 + 4 + 4×4 = 21 nodes (~70% reduction)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+sampler([4, 4])  (2-hop, 4 nbrs each — matches 2L GNN)",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            num_neighbors=[4, 4],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + 10-epoch adversarial warmup. During the first "
            "10 train epochs the GRL is run with alpha=0 (encoder gets "
            "zero adversarial gradient) so cell + niche codes settle "
            "on biology before batch-invariance pressure kicks in. "
            "Targets the cell-NMI / cell-pearson regression that "
            "`+wide+adv` introduced. NEW axis (untried)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + lower adversary weight (50 vs default 150). "
            "On the non-wide spine, `+adv-w50` was the best niche-NMI "
            "variant. Tests whether `+wide` over-corrected on adversary "
            "weight; if so this should improve niche NMI further."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=50.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=50.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + deeper encoder + 10-epoch adversarial warmup. "
            "Combines the strongest empirical iLISI signal with the "
            "new warmup axis: the encoder gets to learn rich features "
            "(deeper MLP) AND has 10 epochs to commit them to the "
            "codebook before adversarial pressure starts."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v6 — push cell-token NMI on cell_type
    # WITHOUT regressing batch integration. Diagnostic from summary_long.csv:
    #   - `adv-warmup10` family: best cell-NMI (~0.50) but iLISI ≈ 0
    #     (warm-up lets the cell token learn batch-correlated features
    #     early; the late-arriving adversary can't remove them).
    #   - `enc-deeper` family: best iLISI (~0.50) but cell-NMI ~0.40
    #     (strong adv from epoch 0 keeps the cell token batch-clean
    #     but never lets it commit batch-discriminating features).
    # The 8 variants below test orthogonal mechanisms for restoring
    # integration AFTER a warmup phase, plus capacity-bottleneck
    # variants with built-in protection. Group via
    # `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v6`.
    # ========================================================================
    # Group A — smaller encoder + warmup
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — encoder MLP shrunk to 1 hidden layer ([256] vs the "
            "default [400, 256]) + 5-epoch warmup. Hypothesis: capacity "
            "bottleneck reduces room to memorise batch-specific features; "
            "short warmup gives cell token a head start but limits how "
            "much batch-correlated info it can encode before the "
            "adversary kicks in. Expected: cell-NMI 0.42-0.46; "
            "iLISI 0.15-0.25."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=5)",
            "+enc-shallow(mlp=[256])",
        ],
        "build": lambda: _patch_dual_encoder_shallow(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=5,
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — same as above but warmup extended to 10 epochs. "
            "Tests whether the capacity bottleneck is enough to keep "
            "iLISI alive at the longer warmup that previously killed "
            "integration entirely. Expected: cell-NMI 0.46-0.50; "
            "iLISI 0.10-0.20."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-shallow(mlp=[256])",
        ],
        "build": lambda: _patch_dual_encoder_shallow(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+small+rvq-both+decoder-cov+adv+adv-warmup5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — `+small` (scvi-style 1-layer encoder + narrow GNN + "
            "narrow decoders) + warmup 5. The most aggressive capacity "
            "limit; tests whether a much smaller model finds a sweet "
            "spot. Existing `+small+rvq+adv` (no warmup) already gave "
            "cell-NMI 0.465 / iLISI 0.071; warmup should push cell-NMI "
            "higher. Expected: cell-NMI 0.46-0.50; iLISI 0.05-0.15."
        ),
        "patches": [
            "+small(hidden=128)", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=5)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=5,
        ),
    },
    # Group B — warmup paired with non-adversarial integration mechanism (MMD)
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — best-performing warmup10 spine + MMD-batch loss "
            "(λ=50) on the cell-token input z_mlp[:B]. MMD has no "
            "min-max game so it doesn't suffer the warmup pathology: "
            "it pushes per-batch distributions together by ordinary "
            "backprop on every step, including during the adversarial "
            "warmup window. Expected: the breakthrough variant — "
            "cell-NMI 0.48-0.51; iLISI 0.15-0.30."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w200+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — same as above but MMD λ=200 (4x stronger). Tests "
            "whether aggressive MMD makes integration a hard "
            "constraint. Risk: kills the cell-type signal if too "
            "strong. Expected: cell-NMI 0.45-0.50; iLISI 0.25-0.40."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+mmd_batch(wt=200, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            wt_mmd_batch=200.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — REPLACE adversary with MMD (λ=100), no warmup needed. "
            "Critical comparison: tests whether the adversary is even "
            "necessary, or whether MMD alone gives both integration "
            "and a clean cell-token. Simpler architecture (no "
            "classifier to tune). Expected: cell-NMI 0.42-0.48; "
            "iLISI 0.20-0.35."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+mmd_batch(wt=100, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            wt_mmd_batch=100.0, n_sub=512,
        ),
    },
    # Group C — schedule + asymmetric adversary
    "dualvq+wide+rvq-both+decoder-cov+adv-cell-only+adv-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — adversarial classifier sees ONLY z_mlp[:B] (the "
            "cell-branch input pre-VQ) instead of the full tensor. "
            "Niche-branch GNN consumes un-classified neighbour rows "
            "of z_mlp, leaving the niche pathway un-pressured by "
            "GRL. Adv weight bumped to 300 to compensate for the "
            "smaller per-step adversarial signal. Hypothesis: "
            "concentrating the adversary on the cell branch where "
            "batch-correction matters most, while letting niche keep "
            "spatial info. Expected: cell-NMI 0.43-0.48; "
            "iLISI 0.30-0.45 on cell branch."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=300.0, apply_to=cell)",
        ],
        "build": lambda: _patch_dual_adversarial_cell_only(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=300.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv-cosine+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6 — cosine schedule for adversarial alpha + warmup 10. "
            "Alpha = 0 during warmup, then ramps up via half-sine "
            "envelope to peak at the midpoint of the post-warmup "
            "phase, decays to ~0 at total_epochs=100. The decay "
            "phase lets the cell token consolidate cell-type-"
            "discriminative features WITHOUT active gradient reversal "
            "in the final epochs. Expected: cell-NMI 0.47-0.51; "
            "iLISI 0.10-0.25."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10, schedule=cosine)",
        ],
        "build": lambda: _patch_dual_adversarial_cosine(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            total_epochs=100,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v6b — second wave targeting cell-token NMI.
    # The first v6 wave (3 MMD variants + enc-shallow + small + adv-cell-only +
    # adv-cosine) had the MMD variants error out on a kwarg name mismatch
    # (now fixed) and the rest underperform. v6b re-runs the 3 MMD ones
    # and adds 5 new mechanisms that ablate different parts of the
    # warmup-NMI / integration trade-off:
    #   - Post-warmup adv-weight scan (w50, w300) — does the ceiling come
    #     from the warmup phase or the post-warmup adversarial pressure?
    #   - Sharper cell VQ commit (commit-cell-w2) — discrete codes more
    #     concentrated, less smearing of cell-type clusters across codes.
    #   - Combo safety stack (enc-shallow + warmup10 + mmd-w50) — pulls
    #     the three best individual mechanisms together.
    # (An earlier draft included a `+ce-w10` semi-supervised entry —
    # removed when SQUINT was committed to the unsupervised-only design.
    # See the comment block where `_patch_dual_cross_entropy` used to be.)
    # Group via `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v6b`.
    # ========================================================================
    "dualvq+wide+rvq-both+decoder-cov+adv-w50+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6b — adv weight 50 (vs default 150) + 10-epoch warmup. "
            "Tests whether the warmup-NMI ceiling at ~0.50 comes from "
            "the post-warmup adversarial pressure being too strong "
            "(scrubbing cell-type signal) vs too weak (failing to "
            "remove batch info). Light adv after warmup = cell branch "
            "barely loses cell-type-discriminative signal. Expected: "
            "cell-NMI 0.50-0.53; iLISI 0.05-0.15 (low integration is "
            "the cost; report alongside MMD variants)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=50.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=50.0, warmup_epochs=10,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv-w300+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6b — adv weight 300 (2x default) + 10-epoch warmup. "
            "Other half of the post-warmup-adv scan: tests whether "
            "STRONGER adversary after warmup recovers integration "
            "without fully scrubbing the cell-type signal that warmup "
            "established. Expected: cell-NMI 0.46-0.50; iLISI 0.10-"
            "0.25 (better integration than w50, lower NMI than w50)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=300.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=300.0, warmup_epochs=10,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w2+adv+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6b — cell-branch commit weight 2 (vs default 1) + 10-"
            "epoch warmup. Sharpens the VQ assignment: z_mlp pulled "
            "harder toward z_q_cell, codes become more concentrated "
            "(less smearing of within-cell-type variance across codes). "
            "Hypothesis: cells of the same cell-type collapse to fewer "
            "codes -> NMI(cell-token, cell_type) up. Risk: niche "
            "branch's GNN consumes the SAME z_mlp tensor (sampled "
            "neighbours), so a sharper z_mlp may degrade niche "
            "aggregation. Expected: cell-NMI 0.50-0.55; niche-NMI "
            "potentially down 0.02-0.05; iLISI similar to baseline."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+commit_cell(weight=2.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_commit_cell_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            wt_commit_cell=2.0,
        ),
    },
    # NOTE: the `+ce-w10` semi-supervised entry that used to live here
    # (v6b "auxiliary cross-entropy on the Linear predictor head") was
    # removed when we decided to keep SQUINT strictly unsupervised. See
    # the comment block where `_patch_dual_cross_entropy` used to be
    # defined for the rationale. No replacement in v6b — the remaining
    # 7 v6b entries are all unsupervised.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v6b — combination safety stack: shallow encoder + 10-epoch "
            "warmup + MMD λ=50 for integration. Pulls together the "
            "three mechanisms expected to cooperate:\n"
            "  1. enc-shallow: capacity bottleneck reduces batch "
            "memorisation\n"
            "  2. adv-warmup10: cell token learns rich features early\n"
            "  3. mmd-w50: non-adversarial integration during warmup, "
            "stays active throughout\n"
            "Expected: cell-NMI 0.47-0.51; iLISI 0.20-0.35. The "
            "headline target — both axes simultaneously above the "
            "current Pareto frontier."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-shallow(mlp=[256])",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_encoder_shallow(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+nbr-hops-3+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + 3-hop neighbourhood mean as the niche target "
            "(was 2-hop). Sampler bumped to [8, 8, 8] so the GNN sees "
            "a complete 3-hop neighbourhood. Pushes niche codes to "
            "encode tissue-region-scale structure rather than 1-2-hop "
            "local context. Risk: niche becomes too coarse, niche NMI "
            "drops."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+nbr-hops=3", "+sampler=[8, 8, 8]",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            # Override nbr_aggregation_hops AFTER
                            # _patch_dual_wide (which sets it to 2).
                            (lambda c: (c["model"]["loss_params"]["loss_kwargs"].update(
                                {"nbr_aggregation_hops": 3}) or c))(
                                _patch_dual_wide(_BD())
                            ),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[8, 8, 8],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adj-w3000+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v3 spine + the kitchen sink of v4's three independently-"
            "promising signals: deeper encoder + adj weight 3000 + "
            "10-epoch adversarial warmup. If pairwise interactions are "
            "additive, this is the variant most likely to be Pareto-"
            "best across all four priority metrics simultaneously."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+adj(weight=3000)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 90),
                            ),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            weight=3000.0,
        ),
    },
    # ========================================================================
    # v5 ablations (final-final round): codebook-capacity scan on the new
    # champion spine `+wide+rvq-both-3level+decoder-cov+adv+enc-deeper`.
    #
    # Insight from v1+v3+v4: `+enc-deeper` drives batch integration,
    # `+wide` drives niche NMI, `+rvq-both-3level` drives Pearson. v5
    # combines all three and varies ONLY the codebook structure.
    #
    # 6 RVQ-3-level variants (capacity scan on residual hierarchy) +
    # 2 CVQ-3-level variants (architectural alternative: tree
    # partitioning instead of residual decomposition, at matched depth).
    # CVQ depth was extended from 2 to 3 levels via a ~80-line addition
    # to `ConditionalVQ.forward` (`vq_l3 = K1*K2 leaf codebooks of
    # size K3`); the residual STE now flows through three levels and
    # `num_quantizers=3` so codebook-utilisation metrics stratify all
    # three.
    #
    # Two of the symmetric scaling slots were swapped for MLP-width
    # ablations (per user request): one variant shrinks the encoder +
    # decoder MLPs in lockstep, one widens them.
    # ========================================================================
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 anchor + new project default. Combines the three "
            "v1-v4 winners on independent axes: `+wide` (niche NMI), "
            "`+enc-deeper` (batch integration), `+rvq-both-3level` "
            "(Pearson). Codebook structure: 3-level residual VQ with "
            "(30, 60, 120) sizes per branch -- 216 k effective codes."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level-50-100-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 spine + RVQ-3-level scaled up symmetrically: "
            "(30, 60, 120) -> (50, 100, 200). 5x effective capacity "
            "(216 k -> 1 M). Tests whether the 3-level structure is "
            "capacity-limited at the v5 anchor."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[50, 100, 200])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(50, 100, 200),
                        ),
                        branch="cell", codebook_sizes=(50, 100, 200),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level-80-160-320+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 spine + RVQ-3-level scaled up aggressively: "
            "(30, 60, 120) -> (80, 160, 320). ~19x effective capacity "
            "(216 k -> 4.1 M). Tests the ceiling. Risk: dead codes if "
            "per-leaf residual gets too sparse."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[80, 160, 320])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(80, 160, 320),
                        ),
                        branch="cell", codebook_sizes=(80, 160, 320),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level-20-40-80+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 spine + RVQ-3-level scaled DOWN symmetrically: "
            "(30, 60, 120) -> (20, 40, 80). 0.3x effective capacity "
            "(216 k -> 64 k). Tests whether the model actually needs "
            "the 216 k codes or if a tighter bottleneck does just as "
            "well -- a meaningful ablation for the paper since smaller "
            "codebooks have lower memory + faster inference."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[20, 40, 80])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(20, 40, 80),
                        ),
                        branch="cell", codebook_sizes=(20, 40, 80),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mlp-h256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 spine + NARROWER MLPs. Encoder hidden widths shrink "
            "[400, 400, 256] -> [256, 256, 256]; cell + niche decoders "
            "[400, 400] -> [256, 256]. Latent / codebook embedding dim "
            "stays at 256. Tests whether `+enc-deeper`'s benefit comes "
            "from depth alone or also from the wider intermediate "
            "layers."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper", "+mlp-h256(enc=[256,256,256], dec=[256,256])",
        ],
        "build": lambda: _patch_dual_mlp_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 60, 120),
                            ),
                            branch="cell", codebook_sizes=(30, 60, 120),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            encoder_hidden=[256, 256, 256],
            decoder_hidden=[256, 256],
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mlp-h512+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 spine + WIDER MLPs. Encoder hidden widths grow "
            "[400, 400, 256] -> [512, 512, 256]; cell + niche decoders "
            "[400, 400] -> [512, 512]. Latent / codebook embedding dim "
            "stays at 256. Tests whether the v4 `+enc-deeper` win "
            "saturates at hidden=400 or scales further with width."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper", "+mlp-h512(enc=[512,512,256], dec=[512,512])",
        ],
        "build": lambda: _patch_dual_mlp_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(
                                _patch_dual_wide(_BD()),
                                branch="niche", codebook_sizes=(30, 60, 120),
                            ),
                            branch="cell", codebook_sizes=(30, 60, 120),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            encoder_hidden=[512, 512, 256],
            decoder_hidden=[512, 512],
        ),
    },
    "dualvq+wide+cvq-both-3level-30-10-5+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 architectural alternative: 3-level Conditional (tree) "
            "VQ instead of Residual VQ, at matched depth. K1=30 macro "
            "buckets, K2=10 sub-children per bucket, K3=5 leaves per "
            "(l1, l2) pair = 1500 distinct codes. Tree partitioning "
            "produces a DISJOINT bucket structure (each cell hits "
            "exactly one of K1*K2*K3 leaves) versus RVQ's residual "
            "decomposition (additive contributions from each level). "
            "Useful for downstream interpretability where leaf bucket "
            "membership is what matters."
        ),
        "patches": [
            "+wide", "+cvq(branch=both, k1=30, k2=10, k3=5)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_cvq(
                        _patch_dual_wide(_BD()),
                        branch="both", k1=30, k2=10, k3=5,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+cvq-both-3level-50-15-5+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v5 architectural alternative: 3-level Conditional VQ "
            "with larger fan-out. K1=50 macro x K2=15 sub-children x "
            "K3=5 leaves = 3 750 distinct codes. A/B against the "
            "smaller `+cvq-both-3level-30-10-5` variant to scan the "
            "tree-VQ capacity-vs-structure trade-off."
        ),
        "patches": [
            "+wide", "+cvq(branch=both, k1=50, k2=15, k3=5)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_cvq(
                        _patch_dual_wide(_BD()),
                        branch="both", k1=50, k2=15, k3=5,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v7 — push cell-token NMI on cell_type
    # by varying the CELL codebook structure while NICHE is held fixed at
    # 3-level RVQ (30, 60, 120). All variants use a fixed level-1 size of
    # 30 (matches niche level 1), and explicitly DROP the 10-epoch
    # adversarial warmup that the v6/v6b families relied on — empirically
    # the post-warmup loss spiked dramatically as the late-arriving
    # adversary tried to undo features the encoder committed during
    # warmup. v7 sticks with adv-from-epoch-0 (alpha=1.0, wt=150.0) and
    # asks: with a fixed niche codebook, what cell-side codebook
    # configuration maximises cell→cell_type NMI?
    #
    # Spine (all 8 share):
    #   +wide (2-layer GNN + sampler [8,8] + nbr_aggregation_hops=2)
    #   +rvq(niche, levels=[30, 60, 120])     # 3-level RVQ, fixed
    #   +decoder-cov                           # NicheCompass-style
    #   +adv(alpha=1.0, wt=150.0)              # NO warmup
    #   +enc-deeper                            # mlp=[400,400,256]
    #
    # Cell codebook varies across:
    #   1-level VQ:   k=30 / k=60 / k=200       (3 variants)
    #   2-level RVQ:  (30,60) / (30,90) / (30,200)  (3 variants)
    #   3-level RVQ:  (30,60,120) / (30,90,270)     (2 variants)
    #
    # Group via `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v7`.
    # ========================================================================
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-30+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell SINGLE-level "
            "VQ k=30. Tightest cell bottleneck (30 effective codes). "
            "Tests whether cell-type signal can be fully captured at "
            "level-1 granularity alone, matching the niche level 1. "
            "Empirical question: is the 30-code level-1 codebook "
            "sufficient for cell-type discrimination, or does extra "
            "residual capacity help?"
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+vq(branch=cell, k=30)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_vq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_size=30,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-60+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell SINGLE-level "
            "VQ k=60. 2x the level-1 capacity. Tests whether widening "
            "the single-level cell codebook past 30 helps disentangle "
            "cell types that share level-1 niche structure."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+vq(branch=cell, k=60)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_vq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_size=60,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell SINGLE-level "
            "VQ k=200. Wide single-level cell codebook at the same "
            "scale as the legacy 2-level RVQ second tier (30, 200). "
            "A/B test against `+rvq-cell-30-200` to disentangle "
            "'capacity' from 'residual hierarchy' on the cell side."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+vq(branch=cell, k=200)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_vq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_size=200,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-60+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell 2-level RVQ "
            "(30, 60) — i.e. cell shares niche's first two levels. "
            "Effective cell capacity 30 × 60 = 1 800 codes. Tightest "
            "2-level cell variant; tests minimum residual depth needed "
            "for cell-NMI."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+rvq(branch=cell, levels=[30, 60])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-90+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell 2-level RVQ "
            "(30, 90). Replicates the legacy v3 spine cell codebook "
            "while niche moves to the v5 3-level structure. Effective "
            "cell capacity 30 × 90 = 2 700 codes. Closest to the "
            "well-characterised v3 cell side; useful as the v7 "
            "reference point for the asymmetric (3-level niche, "
            "2-level cell) design."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell 2-level RVQ "
            "(30, 200). Matches `_default_rvq_params` defaults for the "
            "cell side; widest 2-level cell variant. Effective cell "
            "capacity 30 × 200 = 6 000 codes. Tests whether a wider "
            "level-2 (vs (30, 90) / (30, 60)) helps cell-NMI when the "
            "level-1 is bottlenecked at 30."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+rvq(branch=cell, levels=[30, 200])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 200),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-60-120+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche AND cell at MATCHED 3-level RVQ (30, 60, 120). "
            "Same shape as the v5 anchor; included here as the v7 "
            "reference point so the asymmetric variants can be "
            "compared against a symmetric baseline run with the v7 "
            "harness (no warmup). Effective cell capacity 30 × 60 × "
            "120 = 216 k codes."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+rvq(branch=cell, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-90-270+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v7 — niche 3-level RVQ (30, 60, 120) + cell 3-level RVQ "
            "(30, 90, 270). Cell is widened relative to niche at every "
            "post-level-1 tier — niche stays at the v5 anchor for fair "
            "comparison. Effective cell capacity 30 × 90 × 270 = 729 k "
            "codes (~3.4x the v5 anchor cell). Tests whether the cell "
            "side benefits from MORE residual capacity than niche."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+rvq(branch=cell, levels=[30, 90, 270])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 90, 270),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v8 — 8 variants split into two groups of 4:
    #
    # Spine (all 8 share — v5-anchor minus +enc-deeper to leave a clean
    # baseline for the encoder/decoder size sweep):
    #   +wide (2-layer GNN + sampler [8,8] + nbr_aggregation_hops=2)
    #   +rvq(branch=both, levels=[30, 60, 120])     # 3-level RVQ both sides
    #   +decoder-cov                                 # NicheCompass-style
    #   +adv(alpha=1.0, wt=150.0, no warmup)         # only Group B varies
    #
    # Group A — smaller encoder/decoder MLPs. The v5 default is enc=[400, 256]
    # / dec=[400, 400]. v8-A asks: how aggressively can we shrink the trunks
    # before cell/niche-NMI regress? Four shrink axes, picked to be
    # mechanistically distinct (depth vs width vs latent-dim vs full compact):
    #   A1. enc-shallow:   encoder 2-layer [400,256] -> 1-layer [256]
    #   A2. mlp-h256:      encoder [400,256] -> [256,256]; decoder [400,400] -> [256,256]
    #   A3. small-latent:  encoder [400,256] -> [400,128]; latent + GNN hidden -> 128
    #   A4. small:         scvi-style compact (hidden=128 in encoder + GNN + decoders;
    #                       drops +wide because +small re-sets GNN num_layers=1)
    #
    # Group B — MMD + adversarial batch-integration sweep. Replaces or
    # supplements the default adversary with MMD on the cell-token input
    # (z_mlp[:B]). Designed as a 2x2 grid so each variant can be read as
    # one axis of {MMD-only vs adv+MMD} x {weak (50) vs strong (200)}:
    #   B1. mmd-w50, NO adv         (MMD-only, weak)
    #   B2. mmd-w200, NO adv        (MMD-only, strong)
    #   B3. adv + mmd-w50           (combo, weak MMD)
    #   B4. adv + mmd-w200          (combo, strong MMD)
    #
    # Group via `--all-dataset mmb0-1b_smb1-1b_1p-ablations-v8`.
    # ========================================================================
    # ------ Group A: encoder/decoder size sweep (4 variants) ----------------
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-shallow+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-A1 — v5 spine without +enc-deeper; instead the encoder "
            "trunk is SHRUNK from 2 layers [400, 256] to 1 layer [256]. "
            "Latent dim (= last layer = 256), GNN, decoders, and codebook "
            "all unchanged. Tests whether reducing encoder capacity "
            "(fewer hidden layers) helps cell-NMI by preventing the "
            "encoder from memorising batch-correlated features, without "
            "the adversarial-warmup pathology that bit v6/v6b."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+enc-shallow(mlp=[256])",
        ],
        "build": lambda: _patch_dual_encoder_shallow(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mlp-h256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-A2 — v5 spine without +enc-deeper; encoder + decoder MLPs "
            "are NARROWED in lockstep. Encoder [400, 256] -> [256, 256] "
            "(latent dim preserved, intermediate width shrunk); both cell "
            "and niche decoders [400, 400] -> [256, 256]. Same depth as "
            "the v5 default, just narrower hidden widths. Tests the "
            "width axis of MLP capacity, holding depth fixed at 2."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+mlp-h256(enc=[256, 256], dec=[256, 256])",
        ],
        "build": lambda: _patch_dual_mlp_width(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            encoder_hidden=[256, 256],
            decoder_hidden=[256, 256],
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+small-latent+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-A3 — v5 spine + half-sized latent / codebook-embedding "
            "dim. Encoder last layer 256 -> 128, GNN hidden 256 -> 128, "
            "codebook embedding dim 256 -> 128. Encoder depth (2 layers) "
            "and decoder widths ([400, 400]) are unchanged. Tests the "
            "BOTTLENECK axis: fewer dims for codes to spread across "
            "forces each code's embedding to be more semantically "
            "concentrated. NicheCompass / scvi typically use 10-32; 128 "
            "still leaves headroom but halves the SQUINT default."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+small-latent(latent_dim=128)",
        ],
        "build": lambda: _patch_dual_small_latent(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            latent_dim=128,
        ),
    },
    "dualvq+small+rvq-both-3level+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-A4 — fully compact scvi-style architecture. Replaces "
            "+wide with +small(hidden=128): encoder MLP [128], GNN 1 "
            "layer hidden=128, both decoders [128]. Latent / codebook "
            "embedding dim become 128. Drops the deeper spatial context "
            "from +wide (sampler=[8], nbr_hops=1) — this is the most "
            "aggressive capacity ablation on the v5 spine and tests "
            "whether SQUINT's gains over scvi require the wider "
            "architecture or come from RVQ + decoder-cov + adv alone."
        ),
        "patches": [
            "+small(hidden=128)",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD()),
                        branch="niche", codebook_sizes=(30, 60, 120),
                    ),
                    branch="cell", codebook_sizes=(30, 60, 120),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    # ------ Group B: MMD + adversarial batch-integration sweep (4 variants) -
    "dualvq+wide+rvq-both-3level+decoder-cov+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-B1 — REPLACE adversary with MMD (λ=50, n_sub=512), no "
            "warmup. MMD-only weak corner of the 2x2 mmd-x-adv grid. "
            "MMD applies a deterministic distribution-matching pressure "
            "on the cell-token input every step (no min-max instability "
            "from the GRL). Tests whether weak MMD alone is enough to "
            "integrate batches at this codebook structure."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 60, 120),
                    ),
                    branch="cell", codebook_sizes=(30, 60, 120),
                ),
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-B2 — REPLACE adversary with MMD (λ=200, n_sub=512), no "
            "warmup. MMD-only strong corner of the 2x2 grid. 4x B1; "
            "tests whether aggressive MMD makes integration a hard "
            "constraint without an adversary. Risk: kills cell-type "
            "signal if too strong, just as a heavy adv does."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+mmd_batch(wt=200, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 60, 120),
                    ),
                    branch="cell", codebook_sizes=(30, 60, 120),
                ),
            ),
            wt_mmd_batch=200.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-B3 — v5 spine adv (α=1.0, wt=150.0, no warmup) PLUS MMD "
            "(λ=50, n_sub=512) on the cell-token input. Weak combo "
            "corner of the 2x2 grid. Tests whether a light non-adv "
            "regulariser, working alongside the adv, gives smoother "
            "integration than adv alone — without the warmup pathology "
            "from v6/v6b. Both losses target z_mlp[:B] but via "
            "different mechanisms (CE-based min-max vs kernel MMD)."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mmd-w200+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v8-B4 — same as v8-B3 but MMD λ=200 (4x stronger). Strong "
            "combo corner of the 2x2 grid. Adv + strong MMD: tests "
            "whether stacking both batch-integration pressures gives "
            "the BEST iLISI without collapsing cell-NMI, or whether "
            "the encoder loses too much cell-type signal under "
            "combined pressure."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, no warmup)",
            "+mmd_batch(wt=200, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 60, 120),
                        ),
                        branch="cell", codebook_sizes=(30, 60, 120),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            wt_mmd_batch=200.0, n_sub=512,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v9 — 8 variants on a SMALLER + FASTER
    # architecture, organised as a 2 x 4 grid:
    #
    #   architectural size   ∈ {"compact"  (h32 + codebook (30, 10, 10)),
    #                           "medium"   (h64 + codebook (30, 20, 10))}
    #
    #   batch integration    ∈ {adv (wt=150),         # default
    #                           adv-w300,             # boosted adv
    #                           adv + mmd-w50,        # combo (adv + MMD)
    #                           mmd-w100 (no adv)}    # MMD only
    #
    # = 8 variants. The architectural size axis pairs encoder dim with
    # codebook width (compact uses both smaller; medium uses both larger)
    # — this is intentional because we want to read each "size scale" as
    # a single point rather than disentangling encoder dim from codebook
    # capacity within this sweep. The batch-integration axis is the
    # main thing being varied at each size scale.
    #
    # Shared spine (NO +wide so 1-hop GNN/sampler=[8]/nbr_hops=1):
    #   +small(hidden=N)                  # encoder MLP, GNN, decoders all -> N
    #   +rvq(branch=both, levels=[30, L1, L2])
    #   +decoder_covariate                # NicheCompass-shape batch slot
    #   (per-variant batch-integration loss combination)
    #
    # Effective codebook sizes:
    #   (30, 10, 10) -> 3,000 codes  (vs v5 anchor 216k)
    #   (30, 20, 10) -> 6,000 codes
    #
    # Motivation: smaller / shallower encoders helped niche-NMI in past
    # ablations but hurt iLISI. The four batch-integration strategies
    # span the space from default adv pressure to MMD-only — pairing
    # each strategy at TWO size scales lets us read "does compact
    # architecture work AND how much batch-integration pressure does
    # it need?" off the resulting 4x2 metric grid.
    #
    # Submit all 8 via:
    #   bash examples/submit_mmb_smb_ablations_v9.sh
    # ========================================================================
    # ------ compact size: hidden=32, codebook (30, 10, 10) ------------------
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (compact size, default adv) — scvi-style compact "
            "architecture (hidden=32 in encoder MLP, GNN, decoders) + "
            "smallest 3-level RVQ (30, 10, 10) + default adv (wt=150). "
            "Floor of the sweep on every capacity axis; tests whether "
            "the very tightest config still produces useful niche / "
            "cell-type signal under default batch pressure."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 10, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=32),
                        branch="niche", codebook_sizes=(30, 10, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 10, 10),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (compact size, boosted adv) — hidden=32 + (30, 10, 10) "
            "with adversarial weight 2x the default (wt=300). Tests "
            "whether the compact encoder needs extra batch-integration "
            "pressure to recover iLISI."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 10, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=300.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=32),
                        branch="niche", codebook_sizes=(30, 10, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 10, 10),
                ),
            ),
            alpha=1.0, wt_adv_batch=300.0,
        ),
    },
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (compact size, adv + MMD combo) — hidden=32 + "
            "(30, 10, 10) with default adv PLUS a light MMD on the "
            "cell-token input (lambda=50). MMD applies deterministic "
            "distribution-matching every step (no min-max instability), "
            "so the combo gives the encoder two complementary "
            "batch-mixing signals."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 10, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 10, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 10, 10),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (compact size, MMD only) — hidden=32 + (30, 10, 10) with "
            "REPLACED adv: kernel MMD (lambda=100, n_sub=512) on the "
            "cell-token input and NO adversarial classifier. Tests "
            "whether the compact encoder can integrate batches via "
            "MMD alone — simpler architecture (no classifier to tune), "
            "no min-max game."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 10, 10])",
            "+decoder_covariate",
            "+mmd_batch(wt=100, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=32),
                        branch="niche", codebook_sizes=(30, 10, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 10, 10),
                ),
            ),
            wt_mmd_batch=100.0, n_sub=512,
        ),
    },
    # ------ medium size: hidden=64, codebook (30, 20, 10) -------------------
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (medium size, default adv) — hidden=64 (twice the compact "
            "encoder) + (30, 20, 10) (twice the effective codes) + "
            "default adv. Mid-size baseline; tests whether doubling "
            "both capacity axes recovers any NMI lost at the compact "
            "size under default batch-integration pressure."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=64),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (medium size, boosted adv) — hidden=64 + (30, 20, 10) + "
            "adv wt=300. Same boosted-pressure question as the compact "
            "h32 sibling but at higher capacity."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=300.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=64),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
            ),
            alpha=1.0, wt_adv_batch=300.0,
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (medium size, adv + MMD combo) — hidden=64 + "
            "(30, 20, 10) with default adv plus MMD lambda=50. "
            "Combo-pressure question at the higher capacity."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmd_batch(wt=50, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            wt_mmd_batch=50.0, n_sub=512,
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v9 (medium size, MMD only) — hidden=64 + (30, 20, 10) with "
            "MMD lambda=100 replacing the adversary. Simpler architecture "
            "(no classifier), no min-max game; same MMD-only question "
            "as the compact h32 sibling but at higher capacity."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+mmd_batch(wt=100, n_sub=512)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=64),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
            ),
            wt_mmd_batch=100.0, n_sub=512,
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v10 — "is spatial supervision hurting
    # cell-type NMI?" diagnostic. 4 variants on a fixed medium-size
    # architecture (h64 + codebook (30, 20, 10)), crossed over:
    #
    #   batch integration       ∈ { adv (wt=150),  mmd-w100 (no adv) }
    #   spatial-loss treatment  ∈ { +no-spatial   (drops nbr-NB + niche-commit + adj-BCE),
    #                                +no-adj        (keeps nbr-NB + niche-commit;
    #                                                drops ONLY the adjacency BCE) }
    #
    # = 2 x 2 = 4 variants. Architectural size is held constant so the
    # diagnostic isolates the effect of LOSSES (not capacity) on
    # cell-type NMI. h64 + (30,20,10) was chosen as a balance: small
    # enough to be fast, large enough not to be the bottleneck.
    #
    # Read of the 4 outcomes:
    #   - +no-adj alone recovers cell-NMI -> adjacency BCE is the
    #     specific culprit; niche NB recon is fine.
    #   - Only +no-spatial recovers -> all spatial supervision contributes;
    #     the full two-stage train (cell first, then frozen-cell +
    #     spatial) is the right next step.
    #   - Neither recovers -> bottleneck is elsewhere (codebook structure,
    #     encoder capacity, adversary calibration). Don't build two-stage.
    #
    # Submit all 4 via:
    #   bash examples/submit_mmb_smb_ablations_v10.sh
    # ========================================================================
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+no-spatial+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v10 — medium (h64 + (30,20,10)) + default adv + NO spatial "
            "losses. Trains as a pure cell-branch VQ-VAE + adversarial "
            "batch integration, matching scVI / Harmony training shape "
            "on the cell branch. Tests whether spatial supervision is "
            "pulling cell-NMI down."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+no-spatial",
        ],
        "build": lambda: _patch_dual_no_spatial(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+no-spatial+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v10 — medium (h64 + (30,20,10)) + MMD-only (wt=100) + NO "
            "spatial losses. Same diagnostic as the +adv sibling but with "
            "MMD instead (no adversarial min-max game)."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+mmd_batch(wt=100, n_sub=512)",
            "+no-spatial",
        ],
        "build": lambda: _patch_dual_no_spatial(
            _patch_dual_mmd_batch(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                ),
                wt_mmd_batch=100.0, n_sub=512,
            ),
        ),
    },
    # ------ v10 "no-adj only" variants: keeps niche NB recon + niche
    # commit, drops ONLY the cosine adjacency BCE. Tests whether the
    # adjacency loss specifically is the cell-NMI drag, vs `+no-spatial`
    # which drops every spatial supervision term at once. -------------------
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+no-adj+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v10 — medium (h64 + (30,20,10)) + default adv with the "
            "ADJACENCY BCE dropped. Niche NB recon + niche VQ commit "
            "are kept, so the niche branch still receives gradient — "
            "but the encoder no longer has the cosine-adjacency loss "
            "pulling z_q_niche toward local spatial topology. Partners "
            "with the `+no-spatial` sibling: if `+no-adj` alone recovers "
            "cell-NMI, adjacency is the specific culprit; if only "
            "`+no-spatial` recovers, all spatial supervision contributes."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+no-adj",
        ],
        "build": lambda: _patch_dual_no_adj(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+no-adj+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v10 — medium (h64 + (30,20,10)) + MMD-only (wt=100) with the "
            "adjacency BCE dropped. Niche NB recon + niche commit kept."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 20, 10])",
            "+decoder_covariate",
            "+mmd_batch(wt=100, n_sub=512)",
            "+no-adj",
        ],
        "build": lambda: _patch_dual_no_adj(
            _patch_dual_mmd_batch(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                ),
                wt_mmd_batch=100.0, n_sub=512,
            ),
        ),
    },
    # ========================================================================
    # mmb0-1b_smb1-1b_1p held-out region (downstream gene-reconstruction
    # task, NicheCompass / MLGenX-style). Both batches stay in training
    # but a contiguous patch in each is masked out cell-wise; Pearson is
    # then reported only on the held-out cells via
    # `compute_inference_metrics.py`'s `data_split == "test"` rows.
    # See `_patch_holdout_regions` for the default region geometry; use
    # `examples/plot_holdout_regions.py` to visualise / iterate on it.
    # ========================================================================
    "dualvq+rvq-both+decoder-cov+adv+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "1-layer-spine baseline (RVQ-both + decoder-cov + adv) WITH "
            "per-batch held-out spatial patches: a 25% x 25% box near "
            "the upper-left of STARmap+ batch15 and a 25% x 25% box near "
            "the lower-right of MERFISH batch82. Both sections stay in "
            "training; the held-out cells are masked out cell-wise and "
            "are tagged `data_split=\"test\"` so post-inference Pearson "
            "isolates the gene-reconstruction task from the trained-on "
            "cells."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+region_holdout(test_regions={15: 25%x25% upper-left, "
            "82: 25%x25% lower-right})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Region-holdout port of the current best mmb-smb variant "
            "`+enc-deeper+dec-w32`. Spine: 1-layer SAGE GNN h=256 + "
            "RVQ (30, 90) + decoder-cov embed=16 + adv α=1 wt=150 + "
            "deeper encoder MLP [400, 400, 256] + extreme decoder "
            "bottleneck [32]. Adds the same 25%x25% spatial holdout "
            "patches (batch15 upper-left + batch82 lower-right) used "
            "by the existing region-holdout reference. Held-out cells "
            "are tagged `data_split=\"test\"` so post-inference "
            "Pearson is computed exclusively on cells the model never "
            "saw during training."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+region_holdout(test_regions={15: 25%x25% upper-left, "
            "82: 25%x25% lower-right})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Region-holdout port of the current default SQUINT variant (the multiseed default for the benchmarking plots). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + dec-w[32] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Adds 25%x25% spatial holdout patches (batch15 upper-left + batch82 lower-right) matching the existing region-holdout reference. Held-out cells get the test data-split tag so post-inference Pearson reports only on unseen cells. Default max_epochs=80."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+region-holdout+200ep+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Long-train sibling of the +region-holdout variant on the no-batch-int + within-sec spine with cell-w=5.0 / bs=512 / lr=7e-4. Only delta: max_epochs=200 (vs base config's 80). Early stopping still ENABLED (patience=10 val passes on val_loss). CAVEAT: max_epochs=200 historically regressed niche NMI on the +wide spine when val_loss drifted past the niche-NMI peak. Pair with SQUINT_WITH_PEARSON=1 to also log a niche-Pearson sanity monitor."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
            "+max_epochs=200  (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_max_epochs(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 400, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=5.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                max_epochs=200,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Port of the new default SQUINT variant (the multiseed default for the benchmarking plots) to chl59-8b_1p (CosMx Lung, 8 sections). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + dec-w[32] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). default max_epochs=80. REQUIRES BLOB REBUILD for chl59-8b_1p (k=16 was just added to its n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    # -----------------------------------------------------------------------
    # chl59-8b_1p — 5 larger-decoder variants of the cell-w=5.0 spine
    # -----------------------------------------------------------------------
    # Stand-alone single-seed variants (not registered in a DATASET_VARIANTS
    # sweep). Same spine as the existing `+dec-w32+chl59-8b_1p` variant;
    # ONLY decoder hidden_channels varies. Submit individually via
    # `python examples/run_squint.py --all --variant <name>` or via
    # multi-seed if needed later.
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w128-256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p port of the s42-winner spine (cell-w=5.0), with WIDER decoder: MLP [128, 256] (vs the default `[32]`). Per-decoder param count ~311k (vs the original ~22k; ~14.1x bigger). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission (no multi-seed sweep) to probe decoder-capacity sensitivity on the CosMx Lung gene panel (946 genes, larger than mmb's 431 — wider output head amplifies decoder cost)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[128, 256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[128, 256],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w200-400+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p port of the s42-winner spine (cell-w=5.0), with WIDER decoder: MLP [200, 400] (vs the default `[32]`). Per-decoder param count ~514k (vs the original ~22k; ~23.4x bigger). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission (no multi-seed sweep) to probe decoder-capacity sensitivity on the CosMx Lung gene panel (946 genes, larger than mmb's 431 — wider output head amplifies decoder cost)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[200, 400]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[200, 400],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w256-512+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p port of the s42-winner spine (cell-w=5.0), with WIDER decoder: MLP [256, 512] (vs the default `[32]`). Per-decoder param count ~687k (vs the original ~22k; ~31.2x bigger). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission (no multi-seed sweep) to probe decoder-capacity sensitivity on the CosMx Lung gene panel (946 genes, larger than mmb's 431 — wider output head amplifies decoder cost)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[256, 512]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[256, 512],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w400-400+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p port of the s42-winner spine (cell-w=5.0), with WIDER decoder: MLP [400, 400] (vs the default `[32]`). Per-decoder param count ~649k (vs the original ~22k; ~29.5x bigger). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission (no multi-seed sweep) to probe decoder-capacity sensitivity on the CosMx Lung gene panel (946 genes, larger than mmb's 431 — wider output head amplifies decoder cost)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[400, 400]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[400, 400],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w200-400-400+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p port of the s42-winner spine (cell-w=5.0), with WIDER decoder: MLP [200, 400, 400] (vs the default `[32]`). Per-decoder param count ~675k (vs the original ~22k; ~30.7x bigger). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission (no multi-seed sweep) to probe decoder-capacity sensitivity on the CosMx Lung gene panel (946 genes, larger than mmb's 431 — wider output head amplifies decoder cost)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[200, 400, 400]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[200, 400, 400],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+200ep+chl59-8b_1p": {
        "description": (
            "Port of the new default SQUINT variant (the multiseed default for the benchmarking plots) to chl59-8b_1p (CosMx Lung, 8 sections). Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + dec-w[32] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). max_epochs=200 (vs base 80; early stop still ON). REQUIRES BLOB REBUILD for chl59-8b_1p (k=16 was just added to its n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+chl59-8b_1p(test=[2, 3])",
            "+max_epochs=200  (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_max_epochs(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 400, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=5.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                max_epochs=200,
            ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+spatch_1p": {
        "description": (
            "Port of the new default SQUINT variant (the multiseed default for the benchmarking plots) to spatch_1p. Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + dec-w[32] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: spatch_1p adata_batch_id=2 only (dataset_id_2.h5ad). Test: every other section. default max_epochs=80. REQUIRES BLOB REBUILD for spatch_1p (k=16 was just added to its n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+spatch_1p(test=rest)",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            train_batch_idx=[2], test_batch_idx="rest", batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+200ep+spatch_1p": {
        "description": (
            "Port of the new default SQUINT variant (the multiseed default for the benchmarking plots) to spatch_1p. Spine: no-batch-int + within-sec BCE + enc-deeper [400, 400, 256] + dec-w[32] + knn16 + sampler[16] + cell-w=5.0 + bs=512 + lr=7e-4. Train: spatch_1p adata_batch_id=2 only (dataset_id_2.h5ad). Test: every other section. max_epochs=200 (vs base 80; early stop still ON). REQUIRES BLOB REBUILD for spatch_1p (k=16 was just added to its n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+spatch_1p(test=rest)",
            "+max_epochs=200  (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_max_epochs(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 400, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=5.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                max_epochs=200,
            ),
            train_batch_idx=[2], test_batch_idx="rest", batch_size=512,
        ),
    },
    # ---- Reconstruction-mode ablations (all other components fixed) --------
    "recon-cell": {
        "description": (
            "Baseline: per-cell NB reconstruction only. No neighbourhood "
            "loss, no FiLM, no masking, no adjacency. recon_mode='cell'."
        ),
        "patches": ["recon_mode=cell"],
        "build": lambda: _patch_recon_cell(_B()),
    },
    "recon-nbr": {
        "description": (
            "Neighbourhood-only NB reconstruction. Per-cell counts are NOT "
            "reconstructed; the NB target is the 1-hop neighbourhood mean "
            "(including self-loop). recon_mode='nbr'. Tests whether a purely "
            "niche-oriented signal produces spatially coherent codes."
        ),
        "patches": ["recon_mode=nbr"],
        "build": lambda: _patch_recon_nbr(_B()),
    },
    "recon-both": {
        "description": (
            "Per-cell + neighbourhood NB reconstruction simultaneously. Two "
            "separate loss dispatches: nb_attribute_reconstruction_loss (cell) "
            "and nb_attribute_reconstruction_loss_nbr (nbr). recon_mode='both'. "
            "Tests the additive contribution of the neighbourhood signal."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr"],
        "build": lambda: _patch_recon_both(_B()),
    },
    # ---- Adjacency ablations (cell recon base) ----------------------------
    "recon-cell+adj": {
        "description": (
            "Per-cell NB + BCE adjacency reconstruction. Tests whether "
            "predicting the 8-NN spatial graph connectivity improves "
            "niche-aware code structure."
        ),
        "patches": ["recon_mode=cell", "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_recon_cell(_B())),
    },
    "recon-nbr+adj": {
        "description": (
            "Neighbourhood NB + BCE adjacency reconstruction. "
            "Both losses drive the latent toward spatial structure."
        ),
        "patches": ["recon_mode=nbr", "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_recon_nbr(_B())),
    },
    "recon-both+adj": {
        "description": (
            "Per-cell + neighbourhood NB + BCE adjacency reconstruction. "
            "Maximum spatial supervision without FiLM or masking."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_recon_both(_B())),
    },
    # ---- FiLM ablation (cell recon base) ----------------------------------
    "recon-cell+film": {
        "description": (
            "Per-cell NB + FiLM conditioning on (rbf_distances, cell_batch_id). "
            "Tests batch-correction and spatial-scale conditioning without "
            "neighbourhood or adjacency losses."
        ),
        "patches": ["recon_mode=cell", "+film(rbf_distances, cell_batch_id)"],
        "build": lambda: _patch_film(_patch_recon_cell(_B())),
    },
    # ---- Masking ablation (cell recon base) -------------------------------
    "recon-cell+mask": {
        "description": (
            "Per-cell NB + MAE-style gene masking (learnable_parameter, "
            "0.2->0.6 over 5 warmup epochs). Forces the encoder to use "
            "neighbourhood context rather than copying the input."
        ),
        "patches": ["recon_mode=cell", "+masking(base=0.2, final=0.6, warmup=5)"],
        "build": lambda: _patch_masking(_patch_recon_cell(_B())),
    },
    # ---- Multi-head VQ ablation (cell recon base) -------------------------
    "recon-cell+mhvq": {
        "description": (
            "Per-cell NB + multi-head VQ (heads=10, codebook_size=5000). "
            "Tests the capacity gain of a larger, multi-head codebook while "
            "holding all other components fixed."
        ),
        "patches": ["recon_mode=cell", "+mhvq(heads=10, codebook_size=5000)"],
        "build": lambda: _patch_multihead_vq(_patch_recon_cell(_B()),
                                             heads=10, codebook_size=5000),
    },
    # ========================================================================
    # RVQ ablations — same axes as the standard-VQ variants above, with the
    # codebook swapped for ResidualVQ_Squint (levels=[30, 200]).
    # ========================================================================
    "recon-cell+rvq": {
        "description": (
            "Per-cell NB + Residual VQ (levels=[30, 200]). Tests whether the "
            "RVQ capacity bump alone improves per-cell reconstruction without "
            "any neighbourhood / adjacency / FiLM / masking signal."
        ),
        "patches": ["recon_mode=cell", "+rvq(levels=[30, 200])"],
        "build": lambda: _patch_rvq(_patch_recon_cell(_B())),
    },
    "recon-nbr+rvq": {
        "description": (
            "Neighbourhood-only NB + RVQ (levels=[30, 200]). Tests RVQ on the "
            "purely niche-oriented objective."
        ),
        "patches": ["recon_mode=nbr", "+rvq(levels=[30, 200])"],
        "build": lambda: _patch_rvq(_patch_recon_nbr(_B())),
    },
    "recon-cell+rvq+adj": {
        "description": (
            "Per-cell NB + RVQ + BCE adjacency reconstruction. RVQ + spatial "
            "graph supervision."
        ),
        "patches": ["recon_mode=cell", "+rvq(levels=[30, 200])",
                    "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_rvq(_patch_recon_cell(_B()))),
    },
    "recon-nbr+rvq+adj": {
        "description": (
            "Neighbourhood NB + RVQ + BCE adjacency reconstruction."
        ),
        "patches": ["recon_mode=nbr", "+rvq(levels=[30, 200])",
                    "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_rvq(_patch_recon_nbr(_B()))),
    },
    "recon-both+rvq+adj": {
        "description": (
            "Cell + neighbourhood NB + RVQ + BCE adjacency reconstruction. "
            "Maximum spatial supervision with RVQ capacity."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+rvq(levels=[30, 200])", "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_rvq(_patch_recon_both(_B()))),
    },
    "recon-cell+rvq+film": {
        "description": (
            "Per-cell NB + RVQ + FiLM conditioning on (rbf_distances, "
            "cell_batch_id). Tests RVQ + batch/spatial conditioning."
        ),
        "patches": ["recon_mode=cell", "+rvq(levels=[30, 200])",
                    "+film(rbf_distances, cell_batch_id)"],
        "build": lambda: _patch_film(_patch_rvq(_patch_recon_cell(_B()))),
    },
    "recon-cell+rvq+mask": {
        "description": (
            "Per-cell NB + RVQ + MAE-style gene masking (0.2->0.6 over 5 "
            "warmup epochs). Tests RVQ + forced neighbourhood-aware encoding."
        ),
        "patches": ["recon_mode=cell", "+rvq(levels=[30, 200])",
                    "+masking(base=0.2, final=0.6, warmup=5)"],
        "build": lambda: _patch_masking(_patch_rvq(_patch_recon_cell(_B()))),
    },
    # ========================================================================
    # CVQ ablations — same axes again, with the codebook swapped for the
    # tree-structured ConditionalVQ (K1=30, K2=10).
    # ========================================================================
    "recon-cell+cvq": {
        "description": (
            "Per-cell NB + Conditional / Tree VQ (K1=30, K2=10). Tests the "
            "tree codebook structure on the per-cell objective."
        ),
        "patches": ["recon_mode=cell", "+cvq(k1=30, k2=10)"],
        "build": lambda: _patch_cvq(_patch_recon_cell(_B())),
    },
    "recon-nbr+cvq": {
        "description": (
            "Neighbourhood-only NB + CVQ (K1=30, K2=10). Tests CVQ on the "
            "niche-only objective; expect strong level-1 spatial structure."
        ),
        "patches": ["recon_mode=nbr", "+cvq(k1=30, k2=10)"],
        "build": lambda: _patch_cvq(_patch_recon_nbr(_B())),
    },
    "recon-cell+cvq+adj": {
        "description": (
            "Per-cell NB + CVQ + BCE adjacency reconstruction."
        ),
        "patches": ["recon_mode=cell", "+cvq(k1=30, k2=10)",
                    "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_cvq(_patch_recon_cell(_B()))),
    },
    "recon-nbr+cvq+adj": {
        "description": (
            "Neighbourhood NB + CVQ + BCE adjacency reconstruction."
        ),
        "patches": ["recon_mode=nbr", "+cvq(k1=30, k2=10)",
                    "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_cvq(_patch_recon_nbr(_B()))),
    },
    "recon-both+cvq+adj": {
        "description": (
            "Cell + neighbourhood NB + CVQ + BCE adjacency reconstruction. "
            "Maximum spatial supervision with hierarchical tree codebook."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+cvq(k1=30, k2=10)", "+bce_adjacency_reconstruction_loss"],
        "build": lambda: _patch_adj(_patch_cvq(_patch_recon_both(_B()))),
    },
    "recon-cell+cvq+film": {
        "description": (
            "Per-cell NB + CVQ + FiLM conditioning on (rbf_distances, "
            "cell_batch_id). Tests tree codebook + batch/spatial conditioning."
        ),
        "patches": ["recon_mode=cell", "+cvq(k1=30, k2=10)",
                    "+film(rbf_distances, cell_batch_id)"],
        "build": lambda: _patch_film(_patch_cvq(_patch_recon_cell(_B()))),
    },
    "recon-cell+cvq+mask": {
        "description": (
            "Per-cell NB + CVQ + MAE-style gene masking (0.2->0.6 over 5 "
            "warmup epochs). Tests tree codebook + masked-pretraining."
        ),
        "patches": ["recon_mode=cell", "+cvq(k1=30, k2=10)",
                    "+masking(base=0.2, final=0.6, warmup=5)"],
        "build": lambda: _patch_masking(_patch_cvq(_patch_recon_cell(_B()))),
    },
    # ---- Combined-loss capacity ablations (recon-both base) ---------------
    # These three share recon_mode='both' so the cell + neighbourhood NB
    # objectives are both active, isolating the effect of the VQ architecture.
    "recon-both+mhvq": {
        "description": (
            "Cell + neighbourhood NB + multi-head VQ (heads=10, codebook_size=5000). "
            "Tests whether a larger product-quantization codebook benefits the "
            "joint cell/niche objective."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+mhvq(heads=10, codebook_size=5000)"],
        "build": lambda: _patch_multihead_vq(_patch_recon_both(_B()),
                                             heads=10, codebook_size=5000),
    },
    "recon-both+rvq": {
        "description": (
            "Cell + neighbourhood NB + Residual VQ (RQ-VAE; levels=[30, 200]). "
            "Each cell is encoded as a SUM of two codes from independent "
            "codebooks. Total expressivity 30*200=6000, with a coarse->fine "
            "interpretation (level 1 = macro niche, level 2 = global refinement)."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+rvq(levels=[30, 200])"],
        "build": lambda: _patch_rvq(_patch_recon_both(_B()),
                                    codebook_sizes=(30, 200)),
    },
    "recon-both+cvq": {
        "description": (
            "Cell + neighbourhood NB + Conditional / Tree VQ (K1=30, K2=10). "
            "Each cell is routed to a coarse niche by level 1; conditional on "
            "that niche it is refined by a niche-specific level-2 codebook of "
            "10 codes. Total expressivity 30*10=300 with a strict tree "
            "interpretation (level-2 codes are NOT shared across buckets)."
        ),
        "patches": ["recon_mode=both", "+nb_attribute_reconstruction_loss_nbr",
                    "+cvq(k1=30, k2=10)"],
        "build": lambda: _patch_cvq(_patch_recon_both(_B()), k1=30, k2=10),
    },
    # ========================================================================
    # NicheCompass-style ablation matrix on the recon-nbr base.
    #
    # The full `recon-nbr+ncc` variant bundled three changes vs. plain
    # `recon-nbr`:
    #     (i)   adjacency BCE active           = "+adj" or "+adj-bypass"
    #     (ii)  adjacency input = z_q directly = "-bypass" suffix
    #     (iii) encoder GNN = GATv2            = "+gatv2"
    # The variants below isolate each change so we can attribute any
    # improvement (or absence of improvement) to the specific component.
    #
    # Note:  `recon-nbr+adj` already exists above (standard +adj with MLP
    # adjacency decoder, weight=1.0, GraphSAGE encoder). Together with the
    # three ablations below + the full `recon-nbr+ncc`, this gives the full
    # 2x3 matrix {SAGE, GATv2} x {no adj, adj-MLP, adj-bypass}.
    # ========================================================================
    "recon-nbr+adj-bypass": {
        "description": (
            "recon-nbr + NicheCompass-style adjacency BCE on z_q (raw "
            "inner-product formulation: edge_logit_ij = z_q_i^T z_q_j, "
            "bypassing the SQUINT adjacency_decoder MLP) at weight 1.0. "
            "Encoder is unchanged (GraphSAGE). This isolates the bypass-MLP "
            "change vs. `recon-nbr+adj` (which uses the same weight but "
            "keeps the adjacency_decoder MLP)."
        ),
        "patches": [
            "recon_mode=nbr",
            "+bce_adjacency_reconstruction_loss",
            "+nichecompass_adj(weight=1.0, bypass_adj_decoder=True)",
        ],
        "build": lambda: _patch_nichecompass_adj(
                            _patch_recon_nbr(_B()),
                            weight=1.0),
    },
    "recon-nbr+gatv2": {
        "description": (
            "recon-nbr with the encoder GNN switched from GraphSAGE to "
            "GATv2 (dynamic attention). No adjacency reconstruction loss. "
            "Isolates the GATv2 change vs. plain `recon-nbr`."
        ),
        "patches": ["recon_mode=nbr", "+gatv2_encoder"],
        "build": lambda: _patch_gatv2_encoder(_patch_recon_nbr(_B())),
    },
    "recon-nbr+gatv2+adj": {
        "description": (
            "recon-nbr + GATv2 encoder + standard SQUINT adjacency BCE "
            "(weight=1.0, MLP adjacency_decoder still on the gradient path). "
            "Tests GATv2 + standard adjacency — between `recon-nbr+gatv2` "
            "(GATv2 alone) and `recon-nbr+ncc` (GATv2 + bypass adj)."
        ),
        "patches": ["recon_mode=nbr", "+gatv2_encoder",
                    "+bce_adjacency_reconstruction_loss(weight=1.0)"],
        "build": lambda: _patch_adj(
                            _patch_gatv2_encoder(_patch_recon_nbr(_B())),
                            weight=1.0),
    },
    "recon-nbr+ncc": {
        "description": (
            "NicheCompass-shaped recon-nbr: GATv2 encoder + neighbourhood-only "
            "NB reconstruction + NicheCompass-style adjacency BCE on z_q "
            "(bypassing the SQUINT adjacency_decoder MLP) at weight 1.0. "
            "Tests the full bundle of NicheCompass-style changes."
        ),
        "patches": [
            "recon_mode=nbr",
            "+gatv2_encoder",
            "+bce_adjacency_reconstruction_loss",
            "+nichecompass_adj(weight=1.0, bypass_adj_decoder=True)",
        ],
        "build": lambda: _patch_nichecompass_adj(
                            _patch_gatv2_encoder(
                                _patch_recon_nbr(_B())),
                            weight=1.0),
    },
    # ========================================================================
    # VQNiche_Dual variants
    # ========================================================================
    # Two parallel codebooks: cell (post-MLP, pre-aggregation) and niche
    # (post-GNN). Cell decoder reconstructs per-cell counts; niche decoder
    # reconstructs neighbourhood-mean counts; cosine-similarity BCE on
    # z_q_niche enforces spatial coherence on the niche codebook.
    # ========================================================================
    "dualvq": {
        "description": (
            "VQNiche_Dual baseline: two single-codebook VQs (k=30 each), "
            "shared MLP trunk, GraphSAGE GNN feeding the niche branch. "
            "Joint losses: NB(cell) + NB(nbr) + commit_cell + commit_niche "
            "+ cosine-sim adjacency BCE on z_q_niche. All weights = 1.0."
        ),
        "patches": ["dualvq_baseline"],
        "build": lambda: _BD(),
    },
    "dualvq+rvq-niche": {
        "description": (
            "Dual VQ with RVQ on the niche branch only "
            "(levels=[30, 200]). Cell branch is plain VectorQuantize(k=30). "
            "Tests whether scaling capacity on the niche side helps niche "
            "discrimination without affecting the cell branch."
        ),
        "patches": ["+rvq(branch=niche, levels=[30, 200])"],
        "build": lambda: _patch_dual_rvq(_BD(), branch="niche", codebook_sizes=(30, 200)),
    },
    "dualvq+rvq-cell": {
        "description": (
            "Dual VQ with RVQ on the cell branch only "
            "(levels=[30, 200]). Niche branch is plain VectorQuantize(k=30). "
            "Tests whether the cell codebook benefits from extra capacity."
        ),
        "patches": ["+rvq(branch=cell, levels=[30, 200])"],
        "build": lambda: _patch_dual_rvq(_BD(), branch="cell", codebook_sizes=(30, 200)),
    },
    "dualvq+rvq-both": {
        "description": (
            "Dual VQ with RVQ on BOTH branches (levels=[30, 200] each). "
            "Maximum capacity per branch."
        ),
        "patches": ["+rvq(branch=both, levels=[30, 200])"],
        "build": lambda: _patch_dual_rvq(_BD(), branch="both", codebook_sizes=(30, 200)),
    },
    "dualvq+cvq-niche": {
        "description": (
            "Dual VQ with ConditionalVQ on the niche branch (K1=30, K2=10). "
            "Cell branch unchanged. Tree-structured niche codebook."
        ),
        "patches": ["+cvq(branch=niche, k1=30, k2=10)"],
        "build": lambda: _patch_dual_cvq(_BD(), branch="niche", k1=30, k2=10),
    },
    "dualvq+gatv2": {
        "description": (
            "Dual VQ with GATv2 GNN (instead of GraphSAGE). Tests whether "
            "attention-weighted aggregation makes the niche branch sharper."
        ),
        "patches": ["+gatv2_encoder"],
        "build": lambda: _patch_dual_gatv2(_BD()),
    },
    "dualvq+adj-mild": {
        "description": (
            "Dual VQ with adjacency BCE DOWN-weighted to 100 (vs. default 1000). "
            "Tests how much spatial coherence is sacrificed at 1/10 the "
            "default weight. Useful sanity check that the high default is "
            "actually doing useful work."
        ),
        "patches": ["+cosine_adj(weight=100)"],
        "build": lambda: _patch_dual_adj_weight(_BD(), weight=100.0),
    },
    "dualvq+adj-strong": {
        "description": (
            "Dual VQ with adjacency BCE UP-weighted to 2500 (vs. default 1000). "
            "Tests whether 2.5x default weight further improves niche-code "
            "spatial coherence. May sacrifice some NB Pearson."
        ),
        "patches": ["+cosine_adj(weight=2500)"],
        "build": lambda: _patch_dual_adj_weight(_BD(), weight=2500.0),
    },
    "dualvq+adj-extreme": {
        "description": (
            "Dual VQ with adjacency BCE UP-weighted to 5000 (vs. default 1000). "
            "Maximum spatial-coherence pressure. Expect noticeable NB "
            "Pearson degradation — useful only as an upper-bound run for "
            "what spatial coherence looks like when adjacency dominates."
        ),
        "patches": ["+cosine_adj(weight=5000)"],
        "build": lambda: _patch_dual_adj_weight(_BD(), weight=5000.0),
    },
    # ========================================================================
    # "Wide" dualvq variants — deeper GNN + deeper sampler + bigger codebooks
    # + 2-hop nbr aggregation. Recommended starting point for niche-focused
    # runs once the basic dualvq architecture is validated.
    # ========================================================================
    "dualvq+wide": {
        "description": (
            "Wide dual VQ: GNN num_layers=2 (sees 2-hop context), sampler "
            "[8, 8] (provides 2-hop neighbours), VQ k=50 per branch, niche "
            "target aggregated over 2-hop neighbourhood. The GNN/sampler/"
            "aggregation depths are all coupled so the receptive field is "
            "consistent end-to-end."
        ),
        "patches": [
            "+gnn_layers=2", "+sampler=[8,8]",
            "+codebook_size=50 (cell+niche)", "+nbr_aggregation_hops=2",
        ],
        "build": lambda: _patch_dual_wide(_BD()),
    },
    "dualvq+wide+rvq-niche-3level": {
        "description": (
            "Wide dual VQ + RVQ on the niche branch with THREE levels "
            "(K=[30, 90, 30], 81000 effective codes). Macro -> sub-niche "
            "-> fine-grained refinement. Cell branch is plain VQ k=50. "
            "Run only after `+rvq-niche` (2-level) results are confirmed "
            "good — adding a level adds ~K3=30 codebook vectors of params "
            "and a small per-step overhead, so worth a comparison."
        ),
        "patches": ["+wide", "+rvq(branch=niche, levels=[30, 90, 30])"],
        "build": lambda: _patch_dual_rvq(
            _patch_dual_wide(_BD()),
            branch="niche", codebook_sizes=(30, 90, 30),
        ),
    },
    "dualvq+wide+rvq-niche-larger": {
        "description": (
            "Wide dual VQ + RVQ on the niche branch with larger level-2 "
            "(K=[30, 200], 6000 effective codes). Cell branch is plain "
            "VQ k=50. Use if the level-2 utilization in `+rvq-niche` "
            "([30, 90]) saturates near 90/90."
        ),
        "patches": ["+wide", "+rvq(branch=niche, levels=[30, 200])"],
        "build": lambda: _patch_dual_rvq(
            _patch_dual_wide(_BD()),
            branch="niche", codebook_sizes=(30, 200),
        ),
    },
    "dualvq+wide+rvq-niche-finer-l1": {
        "description": (
            "Wide dual VQ + RVQ on the niche branch with finer level-1 "
            "(K=[50, 90], 4500 effective codes). Cell branch is plain "
            "VQ k=50. Use if the level-1 utilization in `+rvq-niche` "
            "([30, 90]) saturates near 30/30 — gives more macro-niche "
            "resolution at the same level-2 capacity."
        ),
        "patches": ["+wide", "+rvq(branch=niche, levels=[50, 90])"],
        "build": lambda: _patch_dual_rvq(
            _patch_dual_wide(_BD()),
            branch="niche", codebook_sizes=(50, 90),
        ),
    },
    "dualvq+wide+rvq-niche": {
        "description": (
            "Wide dual VQ + RVQ on the niche branch (levels=[30, 90], "
            "2700 effective niche codes). Cell branch is plain VQ k=50. "
            "Smaller-than-max capacity for first-pass testing — converges "
            "faster, less risk of underused level-2 codes, still 54x more "
            "effective codes than the K=50 baseline."
        ),
        "patches": [
            "+wide", "+rvq(branch=niche, levels=[30, 90])",
        ],
        "build": lambda: _patch_dual_rvq(
            _patch_dual_wide(_BD()),
            branch="niche", codebook_sizes=(30, 90),
        ),
    },
    "dualvq+wide+rvq-both": {
        "description": (
            "Wide dual VQ + RVQ on BOTH branches (levels=[30, 90] each, "
            "2700 effective codes per branch). Symmetric capacity for "
            "easy A/B against `+rvq-niche`. Use when cell-state granularity "
            "matters too. Bump to [50, 200] / [50, 100] later if the "
            "smaller sizes saturate."
        ),
        "patches": [
            "+wide", "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
        ],
        "build": lambda: _patch_dual_rvq(
            _patch_dual_rvq(
                _patch_dual_wide(_BD()),
                branch="niche", codebook_sizes=(30, 90),
            ),
            branch="cell", codebook_sizes=(30, 90),
        ),
    },
    "dualvq+wide+cvq-niche": {
        "description": (
            "Wide dual VQ + ConditionalVQ (tree) on the niche branch "
            "(K1=30 macro niches, K2=5 sub-niches per macro = 150 "
            "effective). Conservative tree size: ~100k / 30 ≈ 3300 cells "
            "per macro niche, then / 5 ≈ 660 cells per (macro, sub) pair "
            "— well above the threshold where level-2 EMA becomes noisy."
        ),
        "patches": [
            "+wide", "+cvq(branch=niche, k1=30, k2=5)",
        ],
        "build": lambda: _patch_dual_cvq(
            _patch_dual_wide(_BD()),
            branch="niche", k1=30, k2=5,
        ),
    },
    # ========================================================================
    # CosMx Lung holdout-replicate variants (dataset: chl59-8b_1p)
    # ========================================================================
    # Whole-replicate holdout: train batches go entirely to the train loader,
    # test batches go entirely to the val loader -> wandb logs `train_*`
    # metrics on the training replicates and `val_*` metrics on the held-out
    # replicate(s).
    "dualvq+wide+rvq-both+decoder-cov+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p split + decoder-covariate batch correction. "
            "Train: adata_batch_id 0 + 1 + 4 + 5 + 6 + 7; Test: "
            "adata_batch_id 2 + 3. Decoder covariate concatenates "
            "per-cell batch one-hot to z_q before each decoder, freeing "
            "the codebook to be batch-invariant across replicates."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            test_batch_idx=[2, 3],
        ),
    },
    "dualvq+wide+rvq-both+dec-film+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p split + FiLM batch-correction INSIDE the "
            "cell + niche decoders (`apply_conditioning='in-MLP'`, "
            "condition=cell_batch_id). Train: adata_batch_id "
            "0 + 1 + 4 + 5 + 6 + 7; Test: adata_batch_id 2 + 3. Direct "
            "A/B against `+decoder-cov+chl59-8b_1p` (concat covariate) "
            "and `+decoder-cov+adv+chl59-8b_1p` (concat + adversary). "
            "FiLM is recommended over concat for the deeper non-linear "
            "MLPSoftmax decoders used here."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+dec-film(in-MLP, cell_batch_id)",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_decoder_film(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            test_batch_idx=[2, 3],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p split + decoder covariate + domain-"
            "adversarial batch-invariance head. Train: adata_batch_id "
            "0 + 1 + 4 + 5 + 6 + 7; Test: adata_batch_id 2 + 3. "
            "NicheCompass-shape recipe: covariate concat lets the "
            "decoder absorb per-batch gene patterns; adversarial GRL "
            "(applied to FULL z_mlp incl. sampled neighbours) actively "
            "pushes the encoder toward batch-invariance. wt_adv_batch="
            "150 calibrated against the ~150-nat NB reconstruction "
            "losses."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            test_batch_idx=[2, 3],
        ),
    },
    "dualvq+wide+rvq-both+adv+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p split + adversarial-only batch correction "
            "(no decoder covariate, no FiLM). Train: adata_batch_id "
            "0 + 1 + 4 + 5 + 6 + 7; Test: adata_batch_id 2 + 3. Tests "
            "whether the GRL alone is enough — should not work as "
            "well as the +decoder-cov+adv combo since the decoder has "
            "no way to fit per-batch gene patterns."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_adversarial(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            test_batch_idx=[2, 3],
        ),
    },
    # ------------------------------------------------------------------------
    # chl59 port of the mmb-smb winner variant.
    # Same spine as `dualvq+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p`
    # (NO +wide -> 1-hop GNN; rvq-both with codebook sizes (30, 90);
    # decoder-covariate; adversarial-batch (alpha=1.0, wt=150.0); enc-deeper
    # MLP [400, 400, 256]) — only the dataset patch differs. Standard
    # chl59-8b_1p train/test split: train on adata_batch_id 0+1+4+5+6+7,
    # hold out 2+3 (replicate + new donor) for downstream evaluation.
    # ------------------------------------------------------------------------
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p port of the mmb-smb winner variant "
            "(`dualvq+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p`). "
            "1-hop GNN spine + RVQ-both (30, 90) + decoder-covariate + "
            "adversarial-batch (alpha=1.0, wt=150.0) + deeper encoder MLP "
            "[400, 400, 256]. Train: adata_batch_id 0+1+4+5+6+7. "
            "Test: 2+3 (held-out replicate + new donor)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            test_batch_idx=[2, 3],
        ),
    },
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+chl59-8b_1p": {
        "description": (
            "chl59-8b_1p port of the current best mmb-smb variant "
            "`+enc-deeper+dec-w32`. Adds the extreme decoder "
            "bottleneck [32] on top of the existing "
            "`+enc-deeper+chl59-8b_1p` sibling. Spine: 1-layer SAGE "
            "GNN h=256 + RVQ (30, 90) + decoder-cov embed=16 + adv "
            "α=1 wt=150 + deeper encoder MLP [400, 400, 256] + "
            "decoders [32]. Train: chl59 adata_batch_id 0+1+4+5+6+7. "
            "Test: 2+3 (held-out replicate + new donor)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+chl59-8b_1p(test_ids=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            test_batch_idx=[2, 3],
        ),
    },
    # ========================================================================
    # spatch_1p variants (dataset: spatch_1p)
    # ========================================================================
    # Train on dataset_id_2.h5ad (adata_batch_id = 2); every OTHER section
    # in /nfs/team361/sb75/DATASETS/silver/spatch_1p/ is held out for
    # post-hoc evaluation via predict + compute_inference_metrics
    # (resolved by the `test_batches='rest'` sentinel at runtime).
    # Labels: cell-type = obs['annotation'], niche = obs['spatial_cluster'].
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+spatch_1p": {
        "description": (
            "spatch_1p port of the current best mmb-smb variant "
            "`+enc-deeper+dec-w32`. Architecture is identical to the "
            "mmb-smb baseline — RVQ (30, 90) on both branches + "
            "decoder-cov embed=16 + adv α=1 wt=150 + encoder MLP "
            "[400, 400, 256] + 1-layer SAGE GNN h=256 + decoders "
            "[32]. Train: spatch_1p adata_batch_id=2 only "
            "(dataset_id_2.h5ad). Test: every other section in the "
            "spatch_1p silver dir (resolved at runtime via the "
            "`test_batches='rest'` sentinel — predict() runs "
            "inference on all non-training sections post-hoc).\n\n"
            "Memory: batch_size=128 by default (vs the mmb-smb 256 "
            "default; the smaller batch is conservative for a new "
            "dataset whose section sizes haven't been profiled). "
            "Bump via the LSF_MEM_MB env-var + a custom variant if "
            "the GPU has headroom.\n\n"
            "PREREQUISITES:\n"
            "  1. Stamp metadata on every silver "
            "`dataset_id_<N>.h5ad` (three one-off scripts):\n"
            "       python examples/stamp_spatch_uns_batch.py   "
            "# uns['batch'] (REQUIRED)\n"
            "       python examples/stamp_spatch_cell_id.py     "
            "# obs['cell_id'] (REQUIRED)\n"
            "       python examples/stamp_spatch_metadata.py    "
            "# uns['dataset_id'/'tissue'/'species'] (optional;\n"
            "                                                   "
            "#  safe defaults supplied by the loader otherwise)\n"
            "  2. Build the spatch_1p blob:\n"
            "       python examples/run_squint.py --build-blob "
            "--build-blob-dataset spatch_1p\n"
            "  3. (Sanity-check) run the 1-epoch smoke test first:\n"
            "       python examples/run_squint.py --all "
            "--variant smoke-test+spatch_1p"
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+spatch_1p(train_ids=[2], test='rest')",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            train_batch_idx=[2],
            test_batch_idx="rest",
            batch_size=128,
        ),
    },
    # ========================================================================
    # mmb20 cross-platform retrieval (dataset: mmb0-1b_smb1-20b_1p_shared_genes)
    # ========================================================================
    # Train on 19 STARmap+ sections (mouse CNS, batches 1..14 + 16..20);
    # hold out STARmap batch 15 (within-platform baseline; matches the
    # STARmap section held out in the smaller mmb-smb experiment) and
    # MERFISH batch 82 (cross-platform OOD test). Both holdouts are TRUE
    # held-outs — they never enter the training data graph; post-hoc
    # evaluation runs through predict + compute_inference_metrics on the
    # full silver dir.
    "dualvq+narrow+rvq-both+dec-film+adv+mmb0-1b_smb1-20b_1p": {
        "description": (
            "mmb0-1b_smb1-20b_1p (1 MERFISH + 20 STARmap, shared MERFISH "
            "gene panel) with FiLM-in-decoder + domain-adversarial batch "
            "correction. Train: 19 STARmap+ sections (batches 1..14 + "
            "16..20). Val: 10% cell-level sample of the training "
            "sections (in-distribution early-stopping signal). Test: "
            "STARmap+ batch15 + MERFISH batch82 — BOTH held out from "
            "training entirely (not loaded into the training data graph) "
            "and evaluated post-hoc by the predict + analysis pipeline "
            "(within-platform vs cross-platform retrieval). "
            "Recommended starting recipe for the cross-platform retrieval "
            "experiment — FiLM gives the decoder a per-layer batch-"
            "modulation surface; the adversary on FULL z_mlp pushes the "
            "encoder toward batch-invariant codes (wt_adv_batch=150). "
            "'narrow' = single-hop GNN + sampler=[8] + nbr_hops=1, "
            "calibrated for memory on a 44 GiB GPU at this dataset scale."
        ),
        "patches": [
            "+narrow(gnn=1, sampler=[8], nbr_hops=1)",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+dec-film(in-MLP, cell_batch_id)",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmb0-1b_smb1-20b_1p(test_ids=[15,82])",
        ],
        "build": lambda: _patch_dual_mmb20_holdout(
            _patch_dual_adversarial(
                _patch_dual_decoder_film(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _BD(),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+narrow+rvq-both+decoder-cov+adv+mmb0-1b_smb1-20b_1p": {
        "description": (
            "mmb0-1b_smb1-20b_1p with concat decoder covariate + "
            "adversarial. Same holdout split as the dec-film+adv "
            "variant (train=batches 1..14 + 16..20, val=10% in-section "
            "cell sample, test=STARmap batch15 + MERFISH batch82 "
            "evaluated post-hoc); A/B against it to measure "
            "FiLM-in-decoder vs. concat-based covariate on the 21-"
            "section retrieval setup. Same 'narrow' GNN/sampler config."
        ),
        "patches": [
            "+narrow(gnn=1, sampler=[8], nbr_hops=1)",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmb0-1b_smb1-20b_1p(test_ids=[15,82])",
        ],
        "build": lambda: _patch_dual_mmb20_holdout(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _BD(),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    # Alias for the mmb20 narrow baseline under the dataset-agnostic
    # recipe name (`dualvq+rvq-both+decoder-cov+adv+<dataset_tag>`,
    # same name pattern as the mmb-smb / chl59 1-layer baselines). The
    # build is identical to `dualvq+narrow+rvq-both+decoder-cov+adv+...`
    # — the mmb20 dataset patch enforces narrow (1-layer GNN) for
    # memory regardless of whether `+wide` is applied, so dropping the
    # `+narrow` tag from the variant name doesn't change the model.
    "dualvq+rvq-both+decoder-cov+adv+mmb0-1b_smb1-20b_1p": {
        "description": (
            "mmb0-1b_smb1-20b_1p baseline with RVQ on both branches + "
            "decoder covariate + adversarial GRL. Same recipe name as "
            "the mmb-smb / chl59 1-layer baselines; the underlying GNN "
            "is 1-layer (enforced by `_patch_dual_mmb20` regardless of "
            "+wide/+narrow tag, due to memory)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmb0-1b_smb1-20b_1p(test_ids=[15,82])",
        ],
        "build": lambda: _patch_dual_mmb20_holdout(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _BD(),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "dualvq+narrow+rvq-both+adv+mmb0-1b_smb1-20b_1p": {
        "description": (
            "mmb0-1b_smb1-20b_1p with adversarial-only batch correction "
            "(no decoder covariate, no FiLM). Train on 19 STARmap+ "
            "sections (batches 1..14 + 16..20), hold out STARmap "
            "batch15 + MERFISH batch82. The strictest 'biology-only' "
            "setup — the decoder has no batch-specific pathway, so the "
            "codes have to be platform-invariant by themselves. Useful "
            "as a control for the cross-platform retrieval experiment. "
            "Same 'narrow' GNN/sampler config."
        ),
        "patches": [
            "+narrow(gnn=1, sampler=[8], nbr_hops=1)",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+mmb0-1b_smb1-20b_1p(test_ids=[15,82])",
        ],
        "build": lambda: _patch_dual_mmb20_holdout(
            _patch_dual_adversarial(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _BD(),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    # NOTE: the previous `lung5+lung9-multi-test` variants were removed
    # when chl59 switched to `adata_batch_id`-based splits — their
    # position-based indices `[0,1,2,3,5,6]` / `[4,7]` were derived from
    # alphabetical filename order at a time when chl59 files lacked
    # `uns['batch']`, and don't translate to a clean `uns['batch']`
    # split without re-inspecting the silver files. Re-add them by
    # passing the corresponding `adata_batch_id` lists once the mapping
    # is confirmed.
    # ========================================================================
    # NicheCompass-style decoder-covariate batch correction (concat).
    # Replaces the previous FiLM-at-encoder mechanism, which conceptually
    # AMPLIFIES batch effects rather than removing them (no
    # batch-invariance pressure on the encoder).
    # ========================================================================
    "dualvq+wide+decoder-cov": {
        "description": (
            "Wide dual VQ + NicheCompass-style decoder covariate "
            "(per-cell batch one-hot is concatenated to z_q before each "
            "decoder). The decoders absorb per-batch gene patterns, "
            "freeing the codebook to encode batch-invariant biological "
            "identity. Recommended starting point for cross-sample / "
            "cross-platform integration."
        ),
        "patches": ["+wide", "+decoder_covariate(concat per-cell batch one-hot)"],
        "build": lambda: _patch_dual_decoder_covariate(_patch_dual_wide(_BD())),
    },
    # ========================================================================
    # Architecture ablations on mmb0-1b_smb1-1b_1p (mouse-brain MERFISH+STARmap) — fixed batch correction
    # (decoder-cov + adversarial), varying encoder/decoder/GNN/latent only.
    # All assume the current best batch-correction recipe and isolate the
    # architecture effect.
    # ========================================================================
    "dualvq+decoder-cov+adv": {
        "description": (
            "Basic dual VQ baseline (no +wide, no +rvq) with decoder "
            "covariate + adversarial batch correction. Reference point "
            "for the architecture ablations below — same batch correction "
            "everywhere, only the encoder/decoder/GNN/latent shape "
            "differs."
        ),
        "patches": [
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_BD()),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+small+decoder-cov+adv": {
        "description": (
            "scvi-style compact: 1 hidden layer of 128 dim everywhere "
            "(encoder MLP, GNN, both decoders). Latent / codebook "
            "embedding dim = 128 (down from 256). Codebook size stays "
            "at 30. ~5x fewer parameters than the SQUINT default; tests "
            "whether scvi's smaller-is-better intuition transfers to "
            "spatial VQ-VAE."
        ),
        "patches": [
            "+small(hidden=128 — encoder MLP + GNN + both decoders)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_patch_dual_small(_BD())),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+gnn2+decoder-cov+adv": {
        "description": (
            "Like the basic baseline but with GNN depth=2 (sampler=[8,8], "
            "nbr_hops=2). Isolates the effect of deeper spatial context "
            "on the niche branch from `+wide`'s codebook-size bump."
        ),
        "patches": [
            "+gnn2(num_layers=2, sampler=[8,8], nbr_hops=2)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_patch_dual_gnn2(_BD())),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+gatv2+decoder-cov+adv": {
        "description": (
            "Like the basic baseline but swap the GNN from GraphSAGE "
            "(SAGEConv, default) to GATv2 (GATv2Conv). 1 layer either "
            "way. SAGEConv aggregates neighbours uniformly (mean / "
            "weighted-by-degree); GATv2 learns per-edge attention "
            "weights so the GNN can up- or down-weight individual "
            "neighbours based on feature similarity. For sparser, more "
            "heterogeneous spatial neighbourhoods this can sharpen "
            "niche signal; for dense, homogeneous ones (mouse brain) "
            "it usually doesn't change much. Direct A/B against "
            "`dualvq+decoder-cov+adv` (same config, just SAGEConv)."
        ),
        "patches": [
            "+gatv2(SAGEConv -> GATv2Conv, num_layers=1)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_patch_dual_gatv2(_BD())),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+small-latent+decoder-cov+adv": {
        "description": (
            "Tighter latent bottleneck: cell_dim and niche_dim = 128 "
            "(down from 256). Encoder MLP keeps its first layer at 400; "
            "only the last hidden layer + GNN hidden shrink. Decoder "
            "widths unchanged. Tests whether forcing more "
            "semantically-concentrated codebook embeddings helps."
        ),
        "patches": [
            "+small-latent(latent_dim=128)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_patch_dual_small_latent(_BD())),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+dropout+decoder-cov+adv": {
        "description": (
            "Add dropout p=0.1 to encoder MLP, GNN, and both decoders. "
            "Default architecture otherwise. Useful if the model is "
            "overfitting (val Pearson plateaus while train Pearson keeps "
            "climbing) — early-stopping on val_loss already mitigates "
            "this, but dropout reduces the gap to the unattainable "
            "ceiling."
        ),
        "patches": [
            "+dropout(p=0.1)",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(_patch_dual_dropout(_BD())),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv": {
        "description": (
            "Wide dual VQ + RVQ on both branches + decoder covariate + "
            "domain-adversarial batch-invariance head. The combination is "
            "the NicheCompass-shape recipe: covariate concat lets the "
            "decoder absorb per-batch gene patterns; adversarial GRL "
            "(applied to the FULL z_mlp incl. sampled neighbours) "
            "actively pushes the encoder to make z_mlp batch-invariant. "
            "Recommended cross-tissue / cross-platform integration variant."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+wide+rvq-both+adv": {
        "description": (
            "Wide dual VQ + RVQ on both branches + adversarial-only batch "
            "correction (no decoder covariate). Tests whether the GRL "
            "alone is enough — should not work as well as +decoder-cov+adv "
            "since the decoder has no way to fit per-batch noise."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov": {
        "description": (
            "Wide dual VQ + RVQ on both branches (levels=[30, 90] each) + "
            "NicheCompass-style decoder covariate. Combines best "
            "single-sample setup with concat-based cross-sample batch "
            "correction. Recommended cross-tissue (MERFISH+STARmap) "
            "starting point."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+decoder_covariate(concat per-cell batch one-hot)",
        ],
        "build": lambda: _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
    },
    "dualvq+wide+rvq-both+dec-film": {
        "description": (
            "Wide dual VQ + RVQ on both branches (levels=[30, 90] each) + "
            "FiLM batch-correction INSIDE the cell + niche decoders "
            "(`apply_conditioning='in-MLP'`, condition=cell_batch_id). "
            "No decoder-input concat, no adversarial head. The encoder "
            "is pushed toward batch-invariant codes purely by the "
            "structural separation between encoder (sees only counts) "
            "and decoder (gets the per-cell batch one-hot via FiLM, "
            "modulating each layer's activations). A/B against "
            "`+decoder-cov` (concat-based) and `+decoder-cov+adv` "
            "(concat + adversarial)."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+dec-film(in-MLP, cell_batch_id)",
        ],
        "build": lambda: _patch_dual_decoder_film(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
    },
    "dualvq+wide+rvq-both+dec-film+adv": {
        "description": (
            "Wide dual VQ + RVQ on both branches + FiLM INSIDE the cell + "
            "niche decoders (`apply_conditioning='in-MLP'`, condition="
            "cell_batch_id) + domain-adversarial batch-invariance head. "
            "Completes the 2x2: {FiLM, concat} x {with adv, without adv}. "
            "FiLM gives the decoder a structurally cleaner per-layer "
            "modulation surface than concat (better fit for the deeper "
            "non-linear MLPSoftmax decoders); the adversary (applied to "
            "the FULL z_mlp incl. sampled neighbours, wt_adv_batch=150) "
            "acts as a regularizer against the decoder over-explaining "
            "genuine biology as batch."
        ),
        "patches": [
            "+wide",
            "+rvq(branch=niche, levels=[30, 90])",
            "+rvq(branch=cell, levels=[30, 90])",
            "+dec-film(in-MLP, cell_batch_id)",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_film(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "dualvq+wide+cvq-both": {
        "description": (
            "Wide dual VQ + ConditionalVQ (tree) on BOTH branches "
            "(K1=30, K2=5 each = 150 effective codes per branch). "
            "Symmetric tree-structured codebooks for easy A/B against "
            "`+rvq-both`. Cell branch gets a coarse cell-type → "
            "cell-state hierarchy; niche branch gets a macro-niche → "
            "sub-niche hierarchy. Same per-(K1, K2)-bucket data as "
            "`+cvq-niche` (~660 cells), so level-2 EMA is well-conditioned."
        ),
        "patches": [
            "+wide", "+cvq(branch=both, k1=30, k2=5)",
        ],
        "build": lambda: _patch_dual_cvq(
            _patch_dual_wide(_BD()),
            branch="both", k1=30, k2=5,
        ),
    },
    "dualvq+adj-on-zqniche": {
        "description": (
            "Dual VQ with the adjacency BCE input switched from z_gnn "
            "(continuous, default) to z_q_niche (quantized). Useful for "
            "A/B testing whether the continuous-vs-quantized choice matters "
            "in practice. Adjacency weight kept at the default (100); pair "
            "with `_patch_dual_adj_weight` for higher weights."
        ),
        "patches": ["+cosine_adj(input=z_q_niche)"],
        "build": lambda: _patch_dual_adj_on_zqniche(_BD()),
    },
    "dualvq-no-adj": {
        "description": (
            "Dual VQ WITHOUT the cosine-sim adjacency BCE. Niche branch is "
            "supervised only by neighbourhood NB. Tests whether the "
            "adjacency term is load-bearing for spatial code coherence."
        ),
        "patches": ["-cosine_adj"],
        "build": lambda: _patch_dual_no_adj(_BD()),
    },
    "dualvq-niche-only": {
        "description": (
            "Dual VQ with the cell-branch NB and cell-commit losses dropped: "
            "only the niche branch is supervised. The cell decoder/codebook "
            "still run forward but receive no gradient. Tests whether the "
            "cell branch contributes useful signal to the shared MLP trunk."
        ),
        "patches": ["-nb_cell", "-commit_cell"],
        "build": lambda: _patch_dual_no_cell_recon(_BD()),
    },
    # ---- Full SQUINT (all components) -------------------------------------
    "full": {
        "description": (
            "Full SQUINT: recon-both + adjacency + FiLM + masking + "
            "multi-head VQ (heads=10, codebook=5000). All design components "
            "active. Run only after ablations confirm each component helps."
        ),
        "patches": [
            "recon_mode=both",
            "+nb_attribute_reconstruction_loss_nbr",
            "+bce_adjacency_reconstruction_loss",
            "+film(rbf_distances, cell_batch_id)",
            "+masking(base=0.2, final=0.6, warmup=5)",
            "+mhvq(heads=10, codebook_size=5000)",
        ],
        "build": lambda: _patch_multihead_vq(
            _patch_masking(
                _patch_film(
                    _patch_adj(
                        _patch_recon_both(_B())))),
            heads=10, codebook_size=5000),
    },

    # ========================================================================
    # mmb0-1b_smb1-1b_1p ablations v12-v15 — entire design space exploration.
    # SPINE (held constant across all 32 entries):
    #   dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10
    # = the strongest published single-variant config so far on this dataset
    #   (niche NMI ~0.69, cell NMI ~0.50). Each set varies one orthogonal
    #   axis off this spine.
    #
    #   v12 — commit-loss weights (cell-side, niche-side, both)
    #   v13 — codebook structure (single-level VQ, RVQ depth/sizes,
    #         asymmetric cell vs niche)
    #   v14 — loss-tuning (cross-entropy weight, MMD vs adversarial,
    #         adversarial weight & warmup)
    #   v15 — architecture (MLP/GNN width, latent dim, dropout,
    #         attention, GNN depth)
    #
    # Submit each set via:
    #   bash examples/submit_mmb_smb_ablations_v12.sh   # commit-losses
    #   bash examples/submit_mmb_smb_ablations_v13.sh   # codebook-design
    #   bash examples/submit_mmb_smb_ablations_v14.sh   # loss-tuning
    #   bash examples/submit_mmb_smb_ablations_v15.sh   # architecture
    # ========================================================================

    # ---- v12: commit-loss weights -----------------------------------------
    # Cell-side commit weight had a measurable impact on cell-code-vs-cell-
    # type NMI (commit-cell-w2 → 0.503 vs spine 0.495). v12 scans both
    # weights independently and tests asymmetric combinations to map the
    # commit-loss landscape.
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: cell commit weight 0.5 (looser cell "
            "VQ assignment, more residual variance in z_mlp). Tests "
            "whether SMALLER commit pressure on the cell side helps "
            "niche-branch GNN aggregation (which consumes the same "
            "z_mlp)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_cell(weight=0.5)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_cell_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_cell=0.5,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: cell commit weight 3 (vs default 1 / "
            "previously-tried 2). Pushes z_mlp harder onto cell codes; "
            "expected cell-NMI bump beyond the 0.503 we got at w=2."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_cell(weight=3.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_cell_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_cell=3.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: cell commit weight 5 — aggressive. "
            "Tests whether higher commit pushes z_mlp into a degenerate "
            "code-collapse regime (only a few codes used)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_cell(weight=5.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_cell_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_cell=5.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: niche commit weight 0.5 (looser niche "
            "VQ assignment). Lets the niche encoder maintain finer-"
            "grained spatial variance pre-quantisation."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_niche(weight=0.5)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_niche_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_niche=0.5,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: niche commit weight 3. Tightens niche "
            "VQ → niche codes more distinct; expected niche NMI bump."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_niche(weight=3.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_niche_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_niche=3.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: niche commit weight 5 — aggressive. "
            "Same intent as commit-cell-w5: probe the niche-side "
            "collapse boundary."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+commit_niche(weight=5.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_niche_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_commit_niche=5.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w3+commit-niche-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: asymmetric — cell sharp (3), niche "
            "loose (0.5). Tests whether the two branches should be "
            "differently regularised given their different downstream "
            "tasks (cell-type clustering vs spatial-domain clustering)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+commit_cell(weight=3.0)", "+commit_niche(weight=0.5)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_niche_weight(
            _patch_dual_commit_cell_weight(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                    ),
                ),
                wt_commit_cell=3.0,
            ),
            wt_commit_niche=0.5,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w0.5+commit-niche-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v12 commit-losses: asymmetric — cell loose (0.5), niche "
            "sharp (3). Mirror of the previous variant."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+commit_cell(weight=0.5)", "+commit_niche(weight=3.0)",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_commit_niche_weight(
            _patch_dual_commit_cell_weight(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                    ),
                ),
                wt_commit_cell=0.5,
            ),
            wt_commit_niche=3.0,
        ),
    },

    # ---- v13: codebook structure ------------------------------------------
    # Test single-level (VQ) vs multi-level (RVQ) cell codebooks at
    # various sizes, asymmetric cell-vs-niche configs, and composite VQ.
    "dualvq+wide+vq-cell-30+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: single-level VQ on cell, K=30 (≈ # cell "
            "types). Tests the hypothesis 'cell codebook size should "
            "match cell-type cardinality'."
        ),
        "patches": [
            "+wide", "+vq(branch=cell, K=30)",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_vq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60, 120)),
                        branch="cell", codebook_size=30,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-cell-30-60+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: 2-level cell RVQ with modest level-1 growth "
            "(30, 60) — moderate refinement on top of the fixed "
            "level-0 = 30 cell codebook. Niche stays at the standard "
            "3-level (30, 60, 120)."
        ),
        "patches": [
            "+wide", "+rvq(branch=cell, levels=[30, 60])",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60, 120)),
                        branch="cell", codebook_sizes=(30, 60),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-cell-30-200+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: 2-level cell RVQ with BIG level-1 (30, 200) "
            "— ~6.7x growth on top of the fixed level-0. Tests whether "
            "the cell side benefits from heavy residual refinement; "
            "niche stays at 3-level (30, 60, 120)."
        ),
        "patches": [
            "+wide", "+rvq(branch=cell, levels=[30, 200])",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60, 120)),
                        branch="cell", codebook_sizes=(30, 200),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-cell-30-30+rvq-niche-30-60+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: 2-level cell RVQ with NO level-1 growth "
            "(30, 30). The residual quantiser at level 1 has the same "
            "capacity as level 0 — tests whether the cell side needs "
            "any level-1 expansion at all."
        ),
        "patches": [
            "+wide", "+rvq(branch=cell, levels=[30, 30])",
            "+rvq(branch=niche, levels=[30, 60])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60)),
                        branch="cell", codebook_sizes=(30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-cell-30-90-270+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: 3-level cell RVQ (30, 90, 270) — deepest "
            "cell refinement we're testing; niche is also 3-level "
            "(30, 60, 120). Symmetric depth, asymmetric per-level "
            "growth rates (3x cell / 2x niche)."
        ),
        "patches": [
            "+wide", "+rvq(branch=cell, levels=[30, 90, 270])",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60, 120)),
                        branch="cell", codebook_sizes=(30, 90, 270),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: symmetric 3-level RVQ with NO growth across "
            "levels — all levels at 30. The residuals at levels 1 and "
            "2 refine within a fixed-size codebook. Tests whether the "
            "growth pattern matters or only the depth."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 30, 30)),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+cvq-cell-30-10+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: composite VQ on cell (30 x 10 = 300 "
            "effective codes via Cartesian product), RVQ on niche. "
            "Tests CVQ's compositional code structure for the cell "
            "branch."
        ),
        "patches": [
            "+wide", "+cvq(branch=cell, levels=[30, 10])",
            "+rvq(branch=niche, levels=[30, 60, 120])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_cvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 60, 120)),
                        # `_patch_dual_cvq` takes k1/k2/k3, not `codebook_sizes`
                        # (that is `_patch_dual_rvq`'s parameter). This entry
                        # passed the wrong name, raised TypeError at build, and
                        # has never been runnable. k1=30, k2=10 is what the
                        # variant NAME (`cvq-cell-30-10`) and its description
                        # ("30 x 10 = 300 effective codes") specify, and matches
                        # the working sibling that uses k1=30, k2=10, k3=5.
                        # Inferred from the name, not from a recorded run:
                        # confirm the intent before treating results as
                        # comparable to the other v13 entries.
                        branch="cell", k1=30, k2=10,
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-cell-30-90-270+rvq-niche-30-90+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v13 codebook: deeper cell RVQ (30, 90, 270) — niche stays "
            "2-level (30, 90). Asymmetric depth favouring the cell side."
        ),
        "patches": [
            "+wide", "+rvq(branch=cell, levels=[30, 90, 270])",
            "+rvq(branch=niche, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90, 270),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
        ),
    },

    # ---- v14: loss-tuning (unsupervised) ---------------------------------
    # ALL UNSUPERVISED. The earlier draft of v14 included a `+ce-w*`
    # cross-entropy family for cell-type supervision; those were pulled
    # when SQUINT was committed to the unsupervised-only design (see
    # the comment block where `_patch_dual_cross_entropy` used to be
    # defined). v14 now scans:
    #   - Adjacency loss weight (3 weights spanning ~20x range)
    #   - Adversarial schedule (shorter / longer warmup, higher weight)
    #   - MMD layered on top of the adv+warmup spine (lighter weight)
    #   - Pure MMD (no adv) as the simpler-batch-integration baseline
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w250+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: adjacency-BCE weight 250 (vs default "
            "1000). Lighter spatial structure pressure on the niche "
            "branch — tests whether the default over-constrains "
            "spatial geometry at the cost of cell-side flexibility."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+adj_weight(250.0)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            weight=250.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w500+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: adjacency-BCE weight 500. Mid-point "
            "between adj-w250 and the default 1000; completes the scan "
            "alongside the existing adj-w3000 variant."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+adj_weight(500.0)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            weight=500.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w5000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: adjacency-BCE weight 5000 — heavier "
            "than the existing adj-w3000 data point. Pushes the niche "
            "encoder more aggressively toward spatial-cluster geometry."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+adj_weight(5000.0)",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            weight=5000.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: 5-epoch adversarial warmup (vs the spine's "
            "10 and v14's other 20-epoch variant). Completes the warmup-"
            "duration scan {5, 10, 20}."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=5)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=5,
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: light MMD-w50 layered on top of the adv "
            "spine. MMD operates every step (vs adv which is gated by "
            "warmup) — provides batch-integration safety during the "
            "10-epoch adversarial warmup window."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+mmd_batch(weight=50.0)",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            wt_mmd_batch=50.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+enc-deeper+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: pure MMD-w100 batch integration on the "
            "enc-deeper spine — no adversarial. Tests MMD-only as a "
            "simpler alternative to the GRL adversary."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd_batch(weight=100.0)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_mmd_batch(
            _patch_dual_encoder_deeper(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
            ),
            wt_mmd_batch=100.0,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv-w500+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: adversarial weight 500 (vs default 150). "
            "Aggressive batch removal; expected better iLISI/MMD but "
            "risk of crushing biological signal."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=500.0, warmup=10)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=500.0, warmup_epochs=10,
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup20+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v14 loss-tuning: 20-epoch adversarial warmup (vs 10). "
            "More time for encoder to commit codes before adversarial "
            "pressure kicks in."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=20)",
            "+enc-deeper",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(_BD(),
                            branch="niche", codebook_sizes=(30, 90)),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=20,
            ),
        ),
    },

    # ---- v15: architecture -----------------------------------------------
    # Encoder + GNN width, latent dim, dropout, GNN depth, attention.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gnn-h512+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: GNN hidden 256 → 512. Wider niche "
            "branch — more spatial aggregation capacity."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+gnn_hidden(512)",
        ],
        "build": lambda: _patch_dual_gnn_hidden(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            hidden=512,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mlp-h512+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: MLP hidden 400 → 512 (default + 28%). "
            "Wider cell encoder."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+mlp_width(512)",
        ],
        "build": lambda: _patch_dual_mlp_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            # `_patch_dual_mlp_width` takes `encoder_hidden` /
            # `decoder_hidden`, not `hidden_channels` (that is
            # `_patch_dual_encoder_deeper`'s parameter), so this entry raised
            # TypeError at build and has never been runnable. Widths follow the
            # working `mlp-h512` sibling, whose `patches` string spells out
            # `enc=[512,512,256], dec=[512,512]`: the base after `+enc-deeper`
            # is [400, 400, 256] and this entry's description says
            # "MLP hidden 400 -> 512". The trailing 256 is the latent /
            # codebook embedding dim and is held fixed, so this stays a WIDTH
            # ablation rather than also moving the latent dim, as
            # `_patch_dual_mlp_width`'s docstring prescribes. Inferred from the
            # name, not from a recorded run: confirm the intent before treating
            # results as comparable.
            encoder_hidden=[512, 512, 256],
            decoder_hidden=[512, 512],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mlp-h128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: MLP hidden 400 → 128. Narrow cell "
            "encoder — tests whether the spine over-parameterises "
            "the cell side."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+mlp_width(128)",
        ],
        "build": lambda: _patch_dual_mlp_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            # `_patch_dual_mlp_width` takes `encoder_hidden` /
            # `decoder_hidden`, not `hidden_channels` (that is
            # `_patch_dual_encoder_deeper`'s parameter), so this entry raised
            # TypeError at build and has never been runnable. Widths follow the
            # working `mlp-h512` sibling, whose `patches` string spells out
            # `enc=[512,512,256], dec=[512,512]`: the base after `+enc-deeper`
            # is [400, 400, 256] and this entry's description says
            # "MLP hidden 400 -> 128". The trailing 256 is the latent /
            # codebook embedding dim and is held fixed, so this stays a WIDTH
            # ablation rather than also moving the latent dim, as
            # `_patch_dual_mlp_width`'s docstring prescribes. Inferred from the
            # name, not from a recorded run: confirm the intent before treating
            # results as comparable.
            encoder_hidden=[128, 128, 256],
            decoder_hidden=[128, 128],
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+dropout-p0.1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: dropout p=0.1 in MLP / GNN. Light "
            "regularisation."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+dropout(p=0.1)",
        ],
        "build": lambda: _patch_dual_dropout(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            p=0.1,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+dropout-p0.3+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: dropout p=0.3. Stronger regularisation; "
            "tests whether the spine overfits to training batches."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+dropout(p=0.3)",
        ],
        "build": lambda: _patch_dual_dropout(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            p=0.3,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+small-latent-128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: latent dim 256 → 128. Smaller "
            "pre-codebook bottleneck."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+small_latent(128)",
        ],
        "build": lambda: _patch_dual_small_latent(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
            latent_dim=128,
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gnn2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: 2-layer GNN (vs default 1). Deeper "
            "spatial aggregation; the sampler depth is bumped to "
            "match (`num_neighbors=[8, 8]`)."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+gnn2(num_layers=2, sampler=[8,8])",
        ],
        "build": lambda: _patch_dual_gnn2(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
        ),
    },
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gatv2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v15 architecture: GATv2 attention in the niche GNN "
            "(vs default GraphSAGE-style mean aggregation). Attention "
            "lets the GNN weight neighbours by content rather than "
            "uniformly."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper", "+gatv2",
        ],
        "build": lambda: _patch_dual_gatv2(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90)),
                            branch="cell", codebook_sizes=(30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
                ),
            ),
        ),
    },

    # ---- v16: encoder/decoder dim grid (codebook 30-30-30 fixed) --------
    # 4 hidden dims (<256) × 2 encoder depths = 8 variants.
    # Codebook is fixed at (30, 30, 30) — symmetric 3-level RVQ with no
    # growth across levels. The point of v16 is the dim sweep, NOT the
    # codebook design (that's v13). `_patch_dual_small(hidden=N)` sets
    # the encoder MLP + GNN + BOTH decoders to width N — so each
    # variant is a uniform-width architecture, comparable apples-to-
    # apples on parameter count.
    #
    # The shared spine here is `dualvq+small-h<N>` (no `+wide`), so
    # the GNN runs at depth 1 with sampler `[8]` — same as the default
    # base config. Adversarial + decoder-cov + adv-warmup10 match the
    # rest of v12-v15.
    "dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=32 (encoder MLP + GNN + both "
            "decoders), single-layer MLP. Smallest configuration; "
            "tests whether the model can still learn 30-cluster "
            "codebooks at extreme parameter compression."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=32),
                        branch="niche", codebook_sizes=(30, 30, 30),
                    ),
                    branch="cell", codebook_sizes=(30, 30, 30),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=64, single-layer MLP. ~2x params vs "
            "h=32; the standard 'compact' point in the size sweep."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=64),
                        branch="niche", codebook_sizes=(30, 30, 30),
                    ),
                    branch="cell", codebook_sizes=(30, 30, 30),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
        ),
    },
    "dualvq+small-h128+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=128, single-layer MLP. Matches the "
            "non-wide default GNN width — moderate parameter count."
        ),
        "patches": [
            "+small(hidden=128)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=128),
                        branch="niche", codebook_sizes=(30, 30, 30),
                    ),
                    branch="cell", codebook_sizes=(30, 30, 30),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
        ),
    },
    "dualvq+small-h192+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=192, single-layer MLP. Largest sub-"
            "256 point — close to (but below) the +wide spine's 256."
        ),
        "patches": [
            "+small(hidden=192)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_small(_BD(), hidden=192),
                        branch="niche", codebook_sizes=(30, 30, 30),
                    ),
                    branch="cell", codebook_sizes=(30, 30, 30),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
        ),
    },
    "dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=32 + 3-layer encoder MLP "
            "([32, 32, 32]). Tests whether depth compensates for the "
            "small width."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper(mlp=[32, 32, 32])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            hidden_channels=[32, 32, 32],
        ),
    },
    "dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=64 + 3-layer encoder MLP "
            "([64, 64, 64])."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper(mlp=[64, 64, 64])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            hidden_channels=[64, 64, 64],
        ),
    },
    "dualvq+small-h128+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=128 + 3-layer encoder MLP "
            "([128, 128, 128])."
        ),
        "patches": [
            "+small(hidden=128)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper(mlp=[128, 128, 128])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=128),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            hidden_channels=[128, 128, 128],
        ),
    },
    "dualvq+small-h192+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "v16 dim grid: hidden=192 + 3-layer encoder MLP "
            "([192, 192, 192]). Largest sub-256 point with depth."
        ),
        "patches": [
            "+small(hidden=192)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0, warmup=10)",
            "+enc-deeper(mlp=[192, 192, 192])",
        ],
        "build": lambda: _patch_dual_encoder_deeper(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=192),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0, warmup_epochs=10,
            ),
            hidden_channels=[192, 192, 192],
        ),
    },

    # ---- Sweep 18: small-arch + no-warmup + sampler-{4,8} grid -----------
    # Goal: find the smallest working architecture before scaling up. All
    # 8 variants share a strictly-unsupervised spine with NO adversarial
    # warmup (`+adv` only, alpha=1.0 from epoch 0), `_patch_dual_small`
    # for uniform-width encoder MLP + GNN + both decoders, and codebook
    # level-0 fixed at 30.
    #
    # 2 × 2 × 2 = 8 variants:
    #   hidden  ∈ {32, 64}
    #   codebook ∈ {(30, 30) 2-level, (30, 30, 30) 3-level}
    #   sampler  ∈ {[4], [8]}
    #
    # If small variants do well, the next sweep can scale up; if not, the
    # sweep tells us SQUINT needs more capacity than 64-d to learn
    # cell-type + niche structure under unsupervised losses.
    #
    # NOTE: variants are registered with their full "s18_v<N>_<base>"
    # names — there's no separate flat-name entry for these, since they
    # are genuinely new (not aliases of existing variants).
    "s18_v1_dualvq+small-h32+rvq-both-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 1: hidden=32, 2-level RVQ (30, 30), "
            "sampler=[4], adv-from-epoch-0 (no warmup). Smallest of "
            "the grid."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([4])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[4],
        ),
    },
    "s18_v2_dualvq+small-h64+rvq-both-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 2: hidden=64, 2-level RVQ (30, 30), "
            "sampler=[4]. 2x width vs s18_v1."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([4])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[4],
        ),
    },
    "s18_v3_dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 3: hidden=32, 3-level RVQ (30, 30, 30), "
            "sampler=[4]. Tests whether deeper codebook helps the "
            "smallest architecture."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([4])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[4],
        ),
    },
    "s18_v4_dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 4: hidden=64, 3-level RVQ (30, 30, 30), "
            "sampler=[4]. Slightly larger arch + deeper codebook on "
            "narrow sampler."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([4])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[4],
        ),
    },
    "s18_v5_dualvq+small-h32+rvq-both-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 5: hidden=32, 2-level RVQ (30, 30), "
            "sampler=[8] (default fanout). Pair to s18_v1 — isolates "
            "the sampler effect."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([8])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[8],
        ),
    },
    "s18_v6_dualvq+small-h64+rvq-both-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 6: hidden=64, 2-level RVQ (30, 30), "
            "sampler=[8]. Pair to s18_v2."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([8])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[8],
        ),
    },
    "s18_v7_dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 7: hidden=32, 3-level RVQ (30, 30, 30), "
            "sampler=[8]. Pair to s18_v3."
        ),
        "patches": [
            "+small(hidden=32)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([8])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=32),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[8],
        ),
    },
    "s18_v8_dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s18 sweep, variant 8: hidden=64, 3-level RVQ (30, 30, 30), "
            "sampler=[8]. Largest of the s18 grid; pair to s18_v4."
        ),
        "patches": [
            "+small(hidden=64)",
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+sampler([8])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_small(_BD(), hidden=64),
                            branch="niche", codebook_sizes=(30, 30, 30),
                        ),
                        branch="cell", codebook_sizes=(30, 30, 30),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            num_neighbors=[8],
        ),
    },

    # ---- Sweep 19: spatial-loss ablation -------------------------------
    # Question being tested: do the SPATIAL supervision losses (adjacency
    # BCE + neighborhood-NB gene-expression reconstruction) help the
    # CELL branch learn cell-type-discriminative codes? The cell branch
    # shares `z_mlp` with the niche GNN (the GNN consumes z_mlp from
    # sampled-neighbor cells), so spatial losses on the niche side
    # backprop into z_mlp via the shared trunk — they MAY help or hurt
    # cell-NMI depending on whether spatial structure correlates with
    # cell-type identity.
    #
    # 2x2 grid (4 variants):
    #               adj-BCE ON          adj-BCE OFF
    #   nbr-NB ON   s19_v1 baseline     s19_v2 +no-adj
    #   nbr-NB OFF  s19_v3 +no-nbr      s19_v4 +no-spatial
    #
    # Shared spine: `dualvq+wide+rvq-both+decoder-cov+adv` — the
    # historic top-niche-NMI variant (0.695 niche NMI in the s17 sweep).
    # Picking a strong spine means the deltas read cleanly as
    # "what the spatial losses contribute on top of an otherwise
    # working model" rather than getting tangled up with under-trained
    # other components.
    "s19_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s19 sweep, variant 1: baseline (adj-BCE ON, nbr-NB ON). "
            "Spine = +wide+rvq-both+decoder-cov+adv with both spatial "
            "supervision losses active. Reference for the other three "
            "v2..v4 ablations."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
        ],
        "build": lambda: _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
    },
    "s19_v2_dualvq+wide+rvq-both+decoder-cov+adv+no-adj+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s19 sweep, variant 2: drop adjacency-BCE only (nbr-NB "
            "ON). Tests whether the adjacency-decoder spatial-structure "
            "supervision is load-bearing for cell-NMI. The niche "
            "encoder still gets neighborhood-NB reconstruction "
            "gradients."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+no-adj",
        ],
        "build": lambda: _patch_dual_no_adj(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "s19_v3_dualvq+wide+rvq-both+decoder-cov+adv+no-nbr+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s19 sweep, variant 3: drop neighborhood-NB only (adj-BCE "
            "ON). Tests whether the niche-branch gene-expression "
            "reconstruction target is load-bearing for cell-NMI. The "
            "niche encoder still gets adjacency-BCE spatial structure "
            "supervision + niche commit loss; it just isn't asked to "
            "predict neighborhood gene expression."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+no-nbr",
        ],
        "build": lambda: _patch_dual_no_nbr_recon(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },
    "s19_v4_dualvq+wide+rvq-both+decoder-cov+adv+no-spatial+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s19 sweep, variant 4: drop ALL spatial losses — "
            "no adj-BCE, no nbr-NB, no niche-commit. Niche-branch "
            "modules still run forward but receive zero gradient "
            "signal (effectively idle). Tests the cell-NMI ceiling "
            "for a purely cell-branch VQ-VAE with adversarial batch "
            "integration — matches the scVI/Harmony loss shape. If "
            "this variant has the BEST cell-NMI, spatial losses are "
            "hurting the cell branch on this dataset."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+no-spatial",
        ],
        "build": lambda: _patch_dual_no_spatial(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
        ),
    },

    # ========================================================================
    # Sweeps s30 — s35: six 8-variant ablation grids on the
    # `+enc-deeper+dec-w32` baseline.
    # ========================================================================
    # All six sweeps share the SAME spine:
    #   `dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p`
    # i.e. RVQ (30, 90) on both branches, decoder-cov, adversarial
    # (α=1, wt=150), encoder MLP [400, 400, 256], 1-layer SAGE GNN h=256,
    # decoders [32] (both branches), spatial k-NN k=8, sampler [8].
    #
    # Sweep design:
    #   s30 — 3-level RVQ codebook with L0=30, varying L1, L2.
    #   s31 — reduced codebook + encoder dim (down to [128], [64]).
    #   s32 — knn=12 and knn=16 × 4 batch-integration approaches.
    #   s33 — loss-weight tweaks (stronger cell NB, weaker/stronger adj).
    #   s34 — combinations of s30..s33 winners (Pareto candidates).
    #   s35 — replace RVQ with CVQ (2- and 3-level ConditionalVQ).
    #
    # Submit each via:
    #   bash examples/submit_dataset_sweep.sh \
    #       mmb0-1b_smb1-1b_1p-ablations-v{30,31,32,33,34,35}
    # ========================================================================

    # ------------------------------------------------------------------------
    # s30 — 3-level RVQ codebook (L0=30, varying L1, L2)
    # ------------------------------------------------------------------------
    # Baseline RVQ is 2-level (30, 90) → 2,700 composite codes. These
    # variants add a third codebook level: 3-level RVQ gives composite
    # cardinality = L0 × L1 × L2. The hypothesis is that a third level
    # of residual quantisation provides finer-grained partitioning of
    # the latent — useful for distinguishing rare cell types / niches
    # that share the top-level macro cluster. L0=30 stays fixed (matches
    # the baseline's macro level) so the macro structure is held
    # constant; only L1 and L2 vary across the 8 variants.
    "s30_v1_dualvq+rvq-both-30-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 1: 3-level RVQ (30, 30, 30) — "
            "27,000 composite codes. Smallest 3-level tree on the "
            "+enc-deeper+dec-w32 spine. Balanced level sizes: each "
            "residual gets the same quantisation budget. Tests whether "
            "more levels (with smaller per-level budgets) beat the "
            "2-level (30, 90) parent."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 30, 30)),
                            branch="cell", codebook_sizes=(30, 30, 30),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v2_dualvq+rvq-both-30-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 2: 3-level RVQ (30, 30, 60) — "
            "54,000 composite codes. Mild expansion at the leaf "
            "level; mid level stays compact at 30."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 30, 60])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 30, 60)),
                            branch="cell", codebook_sizes=(30, 30, 60),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v3_dualvq+rvq-both-30-30-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 3: 3-level RVQ (30, 30, 90) — "
            "81,000 composite codes. Direct extension of the parent "
            "2-level (30, 90) by adding a middle level of 30. "
            "If this beats the parent, the extra residual STAGE is "
            "what matters (not just leaf cardinality)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 30, 90)),
                            branch="cell", codebook_sizes=(30, 30, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v4_dualvq+rvq-both-30-60-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 4: 3-level RVQ (30, 60, 30) — "
            "54,000 composite codes, middle-heavy. Tests whether "
            "putting capacity in the mid level (where coarse niche / "
            "cell-type structure lives) helps more than at the leaf."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 30])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 60, 30)),
                            branch="cell", codebook_sizes=(30, 60, 30),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v5_dualvq+rvq-both-30-60-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 5: 3-level RVQ (30, 60, 60) — "
            "108,000 composite codes. Balanced 2nd + 3rd levels."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 60])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 60, 60)),
                            branch="cell", codebook_sizes=(30, 60, 60),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v6_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 6: 3-level RVQ (30, 60, 90) — "
            "162,000 composite codes. Increasing-cardinality tree: "
            "each level adds finer detail on top of the previous."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 60, 90)),
                            branch="cell", codebook_sizes=(30, 60, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v7_dualvq+rvq-both-30-60-120+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 7: 3-level RVQ (30, 60, 120) — "
            "216,000 composite codes. Same shape as the historic "
            "s25 variants' codebook but on the +enc-deeper+dec-w32 "
            "spine (not the +gatv2+enc128+dec128 one)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 120])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 60, 120)),
                            branch="cell", codebook_sizes=(30, 60, 120),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s30_v8_dualvq+rvq-both-30-90-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s30 sweep, variant 8: 3-level RVQ (30, 90, 90) — "
            "243,000 composite codes, largest in s30. Direct "
            "comparison to v3 (30, 30, 90): same leaf budget, "
            "but a fatter middle level."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 90, 90)),
                            branch="cell", codebook_sizes=(30, 90, 90),
                        ),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },

    # ------------------------------------------------------------------------
    # s31 — reduced codebook + encoder dim (down to [128], [64])
    # ------------------------------------------------------------------------
    # Baseline encoder is [400, 400, 256] with latent dim 256. These
    # variants progressively shrink the latent dimension (and therefore
    # codebook embedding dim + GNN hidden width). Hypothesis: a tighter
    # bottleneck forces the encoder to discard batch-specific features
    # and concentrate on biologically discriminative ones (NicheCompass-
    # style integration without an adversarial head), at the cost of
    # raw reconstruction fidelity. The GNN hidden is bumped in lock-
    # step with the latent so the niche-branch latent stays SYMMETRIC
    # with the cell-branch latent (z_mlp dim = z_gnn dim).
    "s31_v1_dualvq+rvq-both+decoder-cov+adv+enc-400-400-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 1: encoder MLP [400, 400, 128], "
            "GNN h=128. Halves the latent dim from 256 → 128 while "
            "keeping the encoder trunk wide. Cleanest 'shrink the "
            "codebook embedding dim only' test."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[400, 400, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v2_dualvq+rvq-both+decoder-cov+adv+enc-400-400-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 2: encoder MLP [400, 400, 64], "
            "GNN h=64. Quarters the latent dim from 256 → 64. "
            "Same wide trunk as v1, even tighter bottleneck."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 64])",
            "+gnn-h=64",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[400, 400, 64],
                ),
                hidden=64,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v3_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 3: encoder MLP [200, 200, 128], "
            "GNN h=128. Halves both trunk width and latent dim. "
            "Tests whether a uniformly smaller encoder is enough."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v4_dualvq+rvq-both+decoder-cov+adv+enc-200-200-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 4: encoder MLP [200, 200, 64], "
            "GNN h=64. Half the trunk width and quartered latent. "
            "More aggressive than v3."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 64])",
            "+gnn-h=64",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[200, 200, 64],
                ),
                hidden=64,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v5_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 5: encoder MLP [128, 128, 128] "
            "(uniform 128), GNN h=128. Compact + uniform — every "
            "layer in the encoder + GNN works in the same 128-dim "
            "space."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[128, 128, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v6_dualvq+rvq-both+decoder-cov+adv+enc-128-128-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 6: encoder MLP [128, 128, 64], "
            "GNN h=64. Compact trunk with a 64-dim funnel."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 64])",
            "+gnn-h=64",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[128, 128, 64],
                ),
                hidden=64,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v7_dualvq+rvq-both+decoder-cov+adv+enc-shallow128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 7: SHALLOW encoder MLP [128] "
            "(1 hidden layer of 128, output dim 128), GNN h=128. "
            "Direct 'down to [128]' as requested — drops the "
            "encoder-deeper trunk entirely."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[128])",
            "+gnn-h=128",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_shallow(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "s31_v8_dualvq+rvq-both+decoder-cov+adv+enc-shallow64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s31 sweep, variant 8: SHALLOW encoder MLP [64] "
            "(1 hidden layer of 64), GNN h=64. Extreme 'down to "
            "[64]' as requested — tightest bottleneck in this sweep."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[64])",
            "+gnn-h=64",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_shallow(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[64],
                ),
                hidden=64,
            ),
            hidden_channels=[32],
        ),
    },

    # ------------------------------------------------------------------------
    # s32 — spatial k-NN graph k × batch-integration loss
    # ------------------------------------------------------------------------
    # 2 × 4 grid: k ∈ {12, 16} (selected from the rebuilt blob's
    # `n_neighs_list = [6, 8, 12, 16]`) × batch-integration ∈ {default
    # adv α=1 wt=150, stronger adv α=2 wt=300, mmd-batch wt=200,
    # mmd-prior wt=50}. Each variant uses sampler == k (the GNN sees
    # ALL spatial neighbours per cell). Hypothesis: a denser spatial
    # graph gives the niche branch richer context, which may either
    # help integration (more cells from each batch in each
    # neighbourhood → smoother batch averaging) or hurt it (more
    # cross-batch edges → harder to separate niches).
    "s32_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 1: k=12 spatial graph + sampler [12] "
            "+ default adv (α=1, wt=150). Alias of the existing "
            "`+enc-deeper+dec-w32+knn12+sampler12` variant — re-run "
            "as the within-sweep adv-baseline."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)",
            "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s32_v2_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 2: k=12 spatial graph + sampler [12] "
            "+ STRONGER adv (α=2, wt=300). Tests whether the denser "
            "graph tolerates more aggressive batch-integration "
            "pressure (the α=2 / wt=300 setting historically helped "
            "iLISI on the +wide spine, but pushed too hard on the "
            "+enc-deeper spine — k=12's richer neighbourhood may "
            "let the GNN absorb the extra pressure better)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=2.0, wt=300.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)",
            "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=2.0, wt_adv_batch=300.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s32_v3_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 3: k=12 spatial graph + sampler [12] "
            "+ MMD-BATCH (wt=200) INSTEAD of adv. Pairwise per-batch "
            "MMD as the integration mechanism — no adversarial CE. "
            "Tests whether non-adversarial integration works with "
            "the denser graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd_batch(wt=200)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)",
            "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_mmd_batch(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            wt_mmd_batch=200.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s32_v4_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 4: k=12 spatial graph + sampler [12] "
            "+ MMD-PRIOR (wt=50) INSTEAD of adv. NicheCompass-style "
            "prior-matching (non-parametric KL analog) as the "
            "integration mechanism. Tests whether prior-matching "
            "works on the denser graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd_prior(wt=50)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)",
            "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_mmd_prior(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            wt_mmd_prior=50.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s32_v5_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 5: k=16 spatial graph + sampler [16] "
            "+ default adv. Alias of the existing "
            "`+enc-deeper+dec-w32+knn16+sampler16` variant — re-run "
            "as the within-sweep k=16-baseline."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },
    "s32_v6_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 6: k=16 spatial graph + sampler [16] "
            "+ STRONGER adv (α=2, wt=300). Sibling of v2 on the "
            "k=16 graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=2.0, wt=300.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=2.0, wt_adv_batch=300.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },
    "s32_v7_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 7: k=16 spatial graph + sampler [16] "
            "+ MMD-BATCH (wt=200) INSTEAD of adv. Sibling of v3 "
            "on the k=16 graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd_batch(wt=200)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_mmd_batch(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            wt_mmd_batch=200.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },
    "s32_v8_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s32 sweep, variant 8: k=16 spatial graph + sampler [16] "
            "+ MMD-PRIOR (wt=50) INSTEAD of adv. Sibling of v4 on "
            "the k=16 graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd_prior(wt=50)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_mmd_prior(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            wt_mmd_prior=50.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=16,
            ),
            num_neighbors=[16],
        ),
    },

    # ------------------------------------------------------------------------
    # s33 — loss-weight tweaks (stronger cell NB, weaker/stronger adj)
    # ------------------------------------------------------------------------
    # The cell-branch NB reconstruction weight `wt_attr_reconstr`
    # defaults to 1.0; raising it tightens the cell embedding to per-
    # batch gene means → can improve cell-type NMI (sharper cell
    # signal) at the cost of cell_iLISI. The adjacency BCE weight
    # `wt_adj_reconstr` defaults to 1000; tweaking it scales the niche-
    # branch spatial-structure pressure. The combo (v8) stacks the
    # individually-promising values from both axes.
    "s33_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w1.5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 1: STRONGER cell-NB recon "
            "(wt_attr_reconstr = 1.5, default 1.0). Mildly tighter "
            "cell embedding → potentially sharper cell-type NMI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=1.5",
        ],
        "build": lambda: _patch_dual_attr_recon_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=1.5,
        ),
    },
    "s33_v2_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 2: STRONGER cell-NB recon "
            "(wt_attr_reconstr = 2.0). 2× the default — clearer "
            "test of the 'tighten cell to gain cell-NMI' hypothesis."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0",
        ],
        "build": lambda: _patch_dual_attr_recon_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=2.0,
        ),
    },
    "s33_v3_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w3.0+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 3: STRONGER cell-NB recon "
            "(wt_attr_reconstr = 3.0). Aggressive — expect lower "
            "cell_iLISI but sharper cell-type NMI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=3.0",
        ],
        "build": lambda: _patch_dual_attr_recon_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=3.0,
        ),
    },
    "s33_v4_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 4: WEAKER adjacency BCE "
            "(wt_adj_reconstr = 300, default 1000). Loosens the "
            "niche-side spatial pressure → niche embedding is "
            "freer to drift away from rigid cosine-similarity "
            "spatial structure. Tests whether the default is too "
            "strong for this dataset."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=300",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=300.0,
        ),
    },
    "s33_v5_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w500+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 5: WEAKER adjacency BCE "
            "(wt_adj_reconstr = 500). Half the default — gentler "
            "tweak vs v4."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=500",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=500.0,
        ),
    },
    "s33_v6_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w2000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 6: STRONGER adjacency BCE "
            "(wt_adj_reconstr = 2000). 2× default — pushes the "
            "niche embedding harder onto the spatial graph "
            "structure."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=2000",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=2000.0,
        ),
    },
    "s33_v7_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w3000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 7: STRONGER adjacency BCE "
            "(wt_adj_reconstr = 3000). 3× default — aggressive."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=3000",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            weight=3000.0,
        ),
    },
    "s33_v8_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+adj-w2000+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s33 sweep, variant 8: BOTH stronger cell-NB "
            "(wt_attr_reconstr = 2.0) AND stronger adjacency "
            "(wt_adj_reconstr = 2000). Combination of v2 + v6. "
            "Sharper cell + tighter spatial — tests whether the "
            "two axes are additive."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0",
            "+wt_adj_reconstr=2000",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_attr_recon_weight(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                weight=2.0,
            ),
            weight=2000.0,
        ),
    },

    # ------------------------------------------------------------------------
    # s34 — combinations of s30..s33 (Pareto candidates)
    # ------------------------------------------------------------------------
    # 8 stacked variants combining the most-promising knobs from the
    # earlier sweeps. Use these as candidate winners — they pre-stack
    # multiple ablation results so the best combination shows up in
    # the same run as the single-knob variants.
    "s34_v1_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 1: s30_v6 (3-level RVQ (30, 60, 90)) "
            "stacked on s32_v1 (k=12 graph + sampler [12]). Tests "
            "whether deeper codebook + denser graph compound."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 60, 90)),
                                    branch="cell", codebook_sizes=(30, 60, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s34_v2_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 2: s33_v2 (cell-NB w=2.0) stacked "
            "on s32_v1 (k=12 + sampler [12]). Sharper cell + denser "
            "spatial graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_attr_recon_weight(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    weight=2.0,
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s34_v3_dualvq+rvq-both-30-60-90+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 3: s30_v6 (3-level RVQ (30, 60, 90)) "
            "+ stronger adv (α=2, wt=300). Bigger codebook + harder "
            "integration pressure."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=2.0, wt=300.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_rvq(
                            _patch_dual_rvq(_BD(),
                                branch="niche", codebook_sizes=(30, 60, 90)),
                            branch="cell", codebook_sizes=(30, 60, 90),
                        ),
                    ),
                    alpha=2.0, wt_adv_batch=300.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s34_v4_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 4: s31_v3 (encoder [200, 200, 128] "
            "+ gnn-h128) stacked on s32_v1 (k=12 + sampler [12]). "
            "Smaller latent + denser spatial graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_gnn_hidden(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            hidden_channels=[200, 200, 128],
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },
    "s34_v5_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+adj-w500+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 5: s30_v6 (3-level RVQ (30, 60, 90)) "
            "+ s33_v2 (cell-NB w=2.0) + s33_v5 (adj w=500). Pareto "
            "stack: deeper codebook + sharper cell + looser "
            "spatial."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 60, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0", "+wt_adj_reconstr=500",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_attr_recon_weight(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 60, 90)),
                                    branch="cell", codebook_sizes=(30, 60, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
                weight=2.0,
            ),
            weight=500.0,
        ),
    },
    "s34_v6_dualvq+rvq-both-30-30-30+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 6: s30_v1 (3-level RVQ (30, 30, 30)) "
            "+ s31_v5 (encoder [128, 128, 128] + gnn-h128). "
            "Compact spine: small uniform-128 trunk + small "
            "uniform-30 codebook tree."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 30, 30])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_gnn_hidden(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 30, 30)),
                                branch="cell", codebook_sizes=(30, 30, 30),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[128, 128, 128],
                ),
                hidden=128,
            ),
            hidden_channels=[32],
        ),
    },
    "s34_v7_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-200-200-128+gnn-h128+dec-w32+adj-w300+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 7: s31_v3 (enc [200, 200, 128] + "
            "gnn-h128) + s32_v2-style stronger adv (α=2, wt=300) "
            "+ s33_v4 (adj w=300). Smaller latent + harder "
            "integration + much-looser spatial pressure."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=2.0, wt=300.0)",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+wt_adj_reconstr=300",
        ],
        "build": lambda: _patch_dual_adj_weight(
            _patch_dual_decoder_width(
                _patch_dual_gnn_hidden(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=2.0, wt_adv_batch=300.0,
                        ),
                        hidden_channels=[200, 200, 128],
                    ),
                    hidden=128,
                ),
                hidden_channels=[32],
            ),
            weight=300.0,
        ),
    },
    "s34_v8_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s34 sweep, variant 8: s31_v5 (encoder [128, 128, 128] "
            "+ gnn-h128) stacked on s32_v1 (k=12 + sampler [12]). "
            "Compact uniform-128 trunk + denser spatial graph."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[128, 128, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
        ],
        "build": lambda: _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                    _patch_dual_gnn_hidden(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            hidden_channels=[128, 128, 128],
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[32],
                ),
                n_neighs=12,
            ),
            num_neighbors=[12],
        ),
    },

    # ------------------------------------------------------------------------
    # s35 — replace RVQ with CVQ (ConditionalVQ, tree-structured)
    # ------------------------------------------------------------------------
    # ConditionalVQ uses a hard tree: each level-1 macro bucket has its
    # own level-2 (and optionally level-3) sub-codebook, so children are
    # macro-specialised rather than shared as in RVQ. Composite codes
    # are K1 (× K2 (× K3)). Tests whether macro-specialised children
    # outperform RVQ's residual+shared scheme at the same nominal
    # cardinality.
    "s35_v1_dualvq+cvq-both-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 1: CVQ (30, 30) — 2-level, "
            "900 composite codes. Direct CVQ analog of an RVQ "
            "(30, 30)."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=30)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=30),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v2_dualvq+cvq-both-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 2: CVQ (30, 60) — 2-level, "
            "1,800 composite codes."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=60)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=60),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v3_dualvq+cvq-both-30-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 3: CVQ (30, 90) — 2-level, "
            "2,700 composite codes. DIRECT comparison to the "
            "parent RVQ (30, 90): same nominal cardinality, "
            "but CVQ uses macro-specialised children while RVQ "
            "shares them across macros."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=90)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=90),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v4_dualvq+cvq-both-30-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 4: CVQ (30, 30, 30) — 3-level, "
            "27,000 composite codes. Direct CVQ analog of s30_v1's "
            "RVQ (30, 30, 30)."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=30, K3=30)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=30, k3=30),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v5_dualvq+cvq-both-30-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 5: CVQ (30, 30, 60) — 3-level, "
            "54,000 composite codes. CVQ analog of s30_v2."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=30, K3=60)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=30, k3=60),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v6_dualvq+cvq-both-30-60-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 6: CVQ (30, 60, 60) — 3-level, "
            "108,000 composite codes. CVQ analog of s30_v5."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=60, K3=60)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=60, k3=60),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v7_dualvq+cvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 7: CVQ (30, 60, 90) — 3-level, "
            "162,000 composite codes. CVQ analog of s30_v6 "
            "(the s34_v1 codebook shape)."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=60, K3=90)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=60, k3=90),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },
    "s35_v8_dualvq+cvq-both-30-90-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s35 sweep, variant 8: CVQ (30, 90, 90) — 3-level, "
            "243,000 composite codes. CVQ analog of s30_v8."
        ),
        "patches": [
            "+cvq(branch=both, K1=30, K2=90, K3=90)",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
                _patch_dual_adversarial(
                    _patch_dual_decoder_covariate(
                        _patch_dual_cvq(_BD(),
                            branch="both", k1=30, k2=90, k3=90),
                    ),
                    alpha=1.0, wt_adv_batch=150.0,
                ),
            ),
            hidden_channels=[32],
        ),
    },

    # ========================================================================
    # s36 — 8 best-previous-run baselines × {with batch-int, no batch-int}
    # under the new `adj_within_section_only=True` BCE adjacency scope.
    # ========================================================================
    # The BCE adjacency reconstruction loss now (default) drops cross-section
    # pairs from the negative pool. This sweep re-runs 8 of the strongest
    # previous configurations under that new default, AND pairs each with a
    # "no batch-integration" sibling (drops adv / mmd-batch / mmd-prior),
    # so we can read off:
    #   (a) "did each previous winner survive the BCE-scope change?" and
    #   (b) "is the BCE-scope change strong enough to integrate batches
    #        even WITHOUT an explicit adversarial / MMD loss on z_mlp?"
    #
    # Variants (each pair = with-batch-int / no-batch-int):
    #   v1  / v2  : +enc-deeper+dec-w32                                              (the +adv baseline)
    #   v3  / v4  : +enc-deeper+dec-w32+knn16+sampler8                               (knn16 stochastic 8-of-16)
    #   v5  / v6  : +enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12                (s34_v4 spine)
    #   v7  / v8  : +enc-deeper+dec-w64 + mmd-prior-w50                              (NicheCompass-style integration)
    #   v9  / v10 : +enc-deeper+dec-w32+knn16+sampler16 + mmd-batch-w200             (s32_v7 spine)
    #   v11 / v12 : +wide+adj-w3000+enc-shallow+dec-w256                             (wide+adj-w3000 winner)
    #   v13 / v14 : +wide+adj-w3000+enc-shallow+dec-w32                              (dec-w32 sibling of v11)
    #   v15 / v16 : +wide+enc-512+dec-w256                                            (s28_v1 spine)
    #
    # All 16 set `loss_kwargs['adj_within_section_only']=True` explicitly via
    # `_patch_dual_adj_within_section_only(enabled=True)` so the materialised
    # YAML records the choice unambiguously (even though True is now the
    # code default). The "no-batch-int" siblings additionally apply
    # `_patch_dual_no_batch_int` after their respective batch-int patch to
    # strip the integration loss term from `loss_names`.
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v36
    #
    # Note: variants using +knn12 / +knn16 / +sampler[12 or 16] REQUIRE the
    # blob to contain those k values. The current `make_dataset_blob_config()`
    # ships `n_neighs_list = [6, 8, 12, 16]`; if the on-disk blob predates
    # that, rebuild with `python examples/run_squint.py --build-blob`.

    "s36_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 1: re-run of the `+enc-deeper+dec-w32` "
            "baseline under the new within-section BCE adjacency scope. "
            "Identical to the parent variant except for the explicit "
            "`adj_within_section_only=True` flag (already the code "
            "default, set explicitly here so the materialised YAML "
            "records the choice)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                ),
                hidden_channels=[32],
            ),
            enabled=True,
        ),
    },
    "s36_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 2: v1 spine WITHOUT any explicit "
            "batch-integration loss (no adv, no mmd-batch, no "
            "mmd-prior). Tests whether the within-section BCE adjacency "
            "scope + decoder covariate alone are enough for batch "
            "integration on z_mlp."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v3_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler8+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 3: re-run of `+enc-deeper+dec-w32+"
            "knn16+sampler8` (k=16 spatial graph + stochastic 8-of-16 "
            "sampler) under the new within-section BCE adjacency scope. "
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([8])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_deeper(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[8],
            ),
            enabled=True,
        ),
    },
    "s36_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 4: v3 spine WITHOUT any explicit "
            "batch-integration loss. Tests within-section BCE + "
            "decoder-cov alone on the knn16+sampler8 spatial spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([8])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_adversarial(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    alpha=1.0, wt_adv_batch=150.0,
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[8],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v5_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 5: re-run of s34_v4 (enc [200, 200, "
            "128] + gnn h=128 + dec-w32 + knn12 + sampler [12]) under "
            "the new within-section BCE adjacency scope. REQUIRES "
            "BLOB REBUILD (k=12 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128", "+dec-w=[32]",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_gnn_hidden(
                            _patch_dual_encoder_deeper(
                                _patch_dual_adversarial(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    alpha=1.0, wt_adv_batch=150.0,
                                ),
                                hidden_channels=[200, 200, 128],
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=12,
                ),
                num_neighbors=[12],
            ),
            enabled=True,
        ),
    },
    "s36_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 6: v5 spine WITHOUT any explicit "
            "batch-integration loss. Tests within-section BCE + "
            "decoder-cov alone on the smaller-latent + knn12 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128", "+dec-w=[32]",
            "+graph_knn(n_neighs=12)", "+sampler([12])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_gnn_hidden(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_adversarial(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        alpha=1.0, wt_adv_batch=150.0,
                                    ),
                                    hidden_channels=[200, 200, 128],
                                ),
                                hidden=128,
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=12,
                    ),
                    num_neighbors=[12],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v7_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w64+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 7: re-run of `+mmd-prior-w50+enc-"
            "deeper+dec-w64` (NicheCompass-style prior matching as the "
            "integration mechanism, NO +adv) under the new within-"
            "section BCE adjacency scope."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd-prior(w=50, prior_std=1.0)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_decoder_width(
                _patch_dual_encoder_deeper(
                    _patch_dual_mmd_prior(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(_BD(),
                                    branch="niche", codebook_sizes=(30, 90)),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        wt_mmd_prior=50.0, prior_std=1.0,
                    ),
                ),
                hidden_channels=[64],
            ),
            enabled=True,
        ),
    },
    "s36_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w64+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 8: v7 spine WITHOUT mmd-prior — "
            "strips the MMD-to-prior integration loss so it's a clean "
            "no-batch-int test on the +enc-deeper+dec-w64 architecture."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[64]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_deeper(
                        _patch_dual_mmd_prior(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            wt_mmd_prior=50.0, prior_std=1.0,
                        ),
                    ),
                    hidden_channels=[64],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v9_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 9: re-run of s32_v7 (pairwise per-batch "
            "MMD wt=200 as the integration mechanism, NO +adv) + "
            "knn16 + sampler [16] under the new within-section BCE "
            "adjacency scope. REQUIRES BLOB REBUILD (k=16 in "
            "`n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+mmd-batch(w=200)",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_deeper(
                            _patch_dual_mmd_batch(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                wt_mmd_batch=200.0,
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            enabled=True,
        ),
    },
    "s36_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 10: v9 spine WITHOUT mmd-batch — "
            "strips the pairwise-per-batch MMD integration loss so "
            "it's a clean no-batch-int test on the +enc-deeper+dec-"
            "w32+knn16+sampler16 architecture."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_mmd_batch(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    wt_mmd_batch=200.0,
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v11_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w256+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 11: re-run of `+wide+adj-w3000+enc-"
            "shallow+dec-w256` (2-layer GNN, very tight adjacency "
            "weight 3000, shallow encoder [256], decoder [256]) under "
            "the new within-section BCE adjacency scope."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[256]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_adj_weight(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD()),
                                        branch="niche", codebook_sizes=(30, 90),
                                    ),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                        weight=3000.0,
                    ),
                    hidden_channels=[256],
                ),
                hidden_channels=[256],
            ),
            enabled=True,
        ),
    },
    "s36_v12_dualvq+wide+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w256+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 12: v11 spine WITHOUT adv. The +wide "
            "+adj-w3000 winner family relied heavily on adversarial "
            "integration historically; this tests whether dropping "
            "adv and falling back to within-section-BCE + decoder-cov "
            "still works on this geometry."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[256]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_adj_weight(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _patch_dual_wide(_BD()),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            weight=3000.0,
                        ),
                        hidden_channels=[256],
                    ),
                    hidden_channels=[256],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v13_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 13: re-run of `+wide+adj-w3000+enc-"
            "shallow+dec-w32` (same as v11 but with dec-w=[32] for "
            "the extreme decoder bottleneck) under the new within-"
            "section BCE adjacency scope."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_adj_weight(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD()),
                                        branch="niche", codebook_sizes=(30, 90),
                                    ),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                        weight=3000.0,
                    ),
                    hidden_channels=[256],
                ),
                hidden_channels=[32],
            ),
            enabled=True,
        ),
    },
    "s36_v14_dualvq+wide+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 14: v13 spine WITHOUT adv. Same test "
            "as v12 but on the dec-w32 sibling."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+adj(weight=3000)", "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_adj_weight(
                            _patch_dual_adversarial(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _patch_dual_wide(_BD()),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                alpha=1.0, wt_adv_batch=150.0,
                            ),
                            weight=3000.0,
                        ),
                        hidden_channels=[256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s36_v15_dualvq+wide+rvq-both+decoder-cov+adv+enc-512+dec-w256+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 15: re-run of s28_v1 (+wide + enc-"
            "shallow [512] + dec-w256, 2-layer GNN, wide-shallow "
            "encoder) under the new within-section BCE adjacency "
            "scope."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)",
            "+enc-shallow(mlp=[512])  (latent dim = 512)",
            "+dec-w=[256]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_adversarial(
                        _patch_dual_decoder_covariate(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD()),
                                    branch="niche", codebook_sizes=(30, 90),
                                ),
                                branch="cell", codebook_sizes=(30, 90),
                            ),
                        ),
                        alpha=1.0, wt_adv_batch=150.0,
                    ),
                    hidden_channels=[512],
                ),
                hidden_channels=[256],
            ),
            enabled=True,
        ),
    },
    "s36_v16_dualvq+wide+rvq-both+decoder-cov+no-batch-int+enc-512+dec-w256+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s36 sweep, variant 16: v15 spine WITHOUT adv. Tests "
            "the wide-shallow-512 encoder geometry under within-"
            "section-BCE + decoder-cov alone."
        ),
        "patches": [
            "+wide", "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[512])  (latent dim = 512)",
            "+dec-w=[256]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_adversarial(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD()),
                                        branch="niche", codebook_sizes=(30, 90),
                                    ),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            alpha=1.0, wt_adv_batch=150.0,
                        ),
                        hidden_channels=[512],
                    ),
                    hidden_channels=[256],
                ),
            ),
            enabled=True,
        ),
    },

    # ========================================================================
    # s37 — 16 ablations on TWO no-batch-int spines under within-section BCE.
    # ========================================================================
    # Now that the adjacency-BCE fix (cross-section pairs excluded from
    # negatives) makes the +no-batch-int siblings actually train sensibly,
    # this sweep ablates the remaining knobs the user identified as most
    # likely to matter:
    #   (1) adjacency reconstruction weight `wt_adj_reconstr` (default 1000)
    #   (2) encoder size BEFORE the cell codebook (shared encoder MLP)
    #   (3) encoder size AFTER the cell codebook (cell + niche decoder MLP)
    #   (4) cell-level NB weight `wt_attr_reconstr` (default 1.0)
    #
    # Two spines × 8 variants each:
    #
    # Spine A (v1..v8) — niche-neck decoupled architecture:
    #   parent = `+enc-shallow+niche-neck-256-256+dec-w32+adv` minus adv,
    #   plus +no-batch-int + within-sec BCE.
    #
    # Spine B (v9..v16) — densest spatial graph, mmd-batch parent minus
    # mmd-batch:
    #   parent = `s32_v7 (+enc-deeper+dec-w32+knn16+sampler16+mmd-batch)`
    #   minus mmd-batch, plus +no-batch-int + within-sec BCE.
    #
    # Per-spine variants:
    #   vN+0: control (no other knob change beyond +no-batch-int + within-sec)
    #   vN+1: deeper/tighter encoder MLP (axis 2 — "before cell codebook")
    #   vN+2: wider cell decoder        (axis 3 — "after cell codebook")
    #   vN+3: narrower cell decoder     (axis 3 — "after cell codebook")
    #   vN+4: adj weight 500            (axis 1 — weaker spatial pressure)
    #   vN+5: adj weight 2000           (axis 1 — stronger spatial pressure)
    #   vN+6: cell-NB weight 2.0        (axis 4 — stronger cell NB)
    #   vN+7: combo of axes 1+2+4       (Pareto candidate)
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v37
    # Spine B variants v9..v16 REQUIRE the blob to contain k=16.

    # ------------------------------------------------------------------------
    # Spine A: niche-neck spine, no-batch-int, within-sec
    # ------------------------------------------------------------------------
    "s37_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 1 (Spine A control): niche-neck "
            "decoupled spine (enc-shallow [256] + niche-neck [256, "
            "256] + dec-w[32]) with NO batch-integration loss (no "
            "adv, no mmd-batch, no mmd-prior) under the new within-"
            "section BCE adjacency scope. Control point for the rest "
            "of Spine A (v2..v8 vary one knob each)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_niche_neck(
                        _patch_dual_encoder_shallow(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            hidden_channels=[256],
                        ),
                        hidden_channels=[256, 256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+niche-neck-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 2 (Spine A, axis 2 — encoder BEFORE "
            "cell codebook): deeper shared encoder MLP `[256, 256]` "
            "(2 hidden of 256, vs the parent's `[256]`). Tests "
            "whether more cell-branch encoder capacity BEFORE the "
            "cell-VQ helps the codebook see a richer pre-quantised "
            "representation."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256, 256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_niche_neck(
                        _patch_dual_encoder_shallow(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            hidden_channels=[256, 256],
                        ),
                        hidden_channels=[256, 256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 3 (Spine A, axis 3 — niche-side "
            "encoder AFTER cell codebook): DEEPER niche-neck "
            "`[256, 256, 256]` (3 hidden of 256, vs the parent's "
            "`[256, 256]` = 2 hidden). Adds a layer of capacity to "
            "the niche-only path AFTER the z_mlp tap into the cell "
            "VQ — gives the GNN richer input without changing the "
            "shared encoder or the cell-side path. Output dim stays "
            "256 so the GNN's input dim is unchanged."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256, 256, 256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_niche_neck(
                        _patch_dual_encoder_shallow(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            hidden_channels=[256],
                        ),
                        hidden_channels=[256, 256, 256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 4 (Spine A, axis 3 — niche-side "
            "encoder AFTER cell codebook): SHALLOWER niche-neck "
            "`[256]` (1 hidden of 256, vs the parent's `[256, 256]` "
            "= 2 hidden). Drops a layer of capacity from the niche-"
            "only path. Tests whether less niche-side post-tap "
            "capacity hurts niche metrics — also a cheaper baseline "
            "to compare against v3."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256])",
            "+dec-w=[32]",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_decoder_width(
                    _patch_dual_niche_neck(
                        _patch_dual_encoder_shallow(
                            _patch_dual_decoder_covariate(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(_BD(),
                                        branch="niche", codebook_sizes=(30, 90)),
                                    branch="cell", codebook_sizes=(30, 90),
                                ),
                            ),
                            hidden_channels=[256],
                        ),
                        hidden_channels=[256],
                    ),
                    hidden_channels=[32],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+adj-w500+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 5 (Spine A, axis 1 — adjacency "
            "weight): WEAKER cosine BCE adjacency loss "
            "`wt_adj_reconstr=500` (vs default 1000). Tests whether "
            "the previous default was too strong now that "
            "cross-section pairs are masked out."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=500",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_decoder_width(
                        _patch_dual_niche_neck(
                            _patch_dual_encoder_shallow(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[256],
                            ),
                            hidden_channels=[256, 256],
                        ),
                        hidden_channels=[32],
                    ),
                    weight=500.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 6 (Spine A, axis 1 — adjacency "
            "weight): STRONGER cosine BCE adjacency loss "
            "`wt_adj_reconstr=2000` (vs default 1000). With cross-"
            "section pairs now masked, stronger spatial pressure may "
            "be safe / beneficial."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_decoder_width(
                        _patch_dual_niche_neck(
                            _patch_dual_encoder_shallow(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[256],
                            ),
                            hidden_channels=[256, 256],
                        ),
                        hidden_channels=[32],
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 7 (Spine A, axis 4 — cell-NB "
            "weight): STRONGER cell-level NB recon "
            "`wt_attr_reconstr=2.0` (vs default 1.0). Sharpens the "
            "cell-branch reconstruction pressure — expected to lift "
            "cell-type NMI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_decoder_width(
                        _patch_dual_niche_neck(
                            _patch_dual_encoder_shallow(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[256],
                            ),
                            hidden_channels=[256, 256],
                        ),
                        hidden_channels=[32],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+niche-neck-256-256+dec-w32+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 8 (Spine A combo): stacks v2 (deeper "
            "encoder `[256, 256]`), v7 (cell-w=2.0), and v6 (adj-"
            "w=2000) on the niche-neck no-batch-int spine. Tests "
            "whether the individually-promising axes are additive."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256, 256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+wt_attr_reconstr=2.0", "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoder_width(
                            _patch_dual_niche_neck(
                                _patch_dual_encoder_shallow(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    hidden_channels=[256, 256],
                                ),
                                hidden_channels=[256, 256],
                            ),
                            hidden_channels=[32],
                        ),
                        weight=2.0,
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },

    # ------------------------------------------------------------------------
    # Spine B: enc-deeper + knn16 + sampler16, no-batch-int, within-sec
    # ------------------------------------------------------------------------
    "s37_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 9 (Spine B control): s32_v7 spine "
            "(`+enc-deeper+dec-w32+knn16+sampler16`) with NO batch-"
            "integration loss (mmd-batch stripped) under the new "
            "within-section BCE adjacency scope. Control point for "
            "the rest of Spine B (v10..v16 vary one knob each).\n\n"
            "REQUIRES BLOB REBUILD (k=16 in `n_neighs_list`)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(_BD(),
                                            branch="niche", codebook_sizes=(30, 90)),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 10 (Spine B, axis 2 — encoder "
            "BEFORE cell codebook): TIGHTER shared encoder "
            "`[200, 200, 128]` + GNN h=128 (vs the parent's "
            "`[400, 400, 256]` + h=256). Halves the latent dim — "
            "tests whether a smaller pre-cell-VQ representation is "
            "enough now that within-section BCE handles integration."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_gnn_hidden(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    hidden_channels=[200, 200, 128],
                                ),
                                hidden=128,
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+niche-neck-256+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 11 (Spine B, axis 3 — niche-side "
            "encoder AFTER cell codebook): ADD a niche-neck `[256]` "
            "(1 hidden of 256). Spine B has no niche-neck by "
            "default; introducing one decouples the niche-side "
            "path from the cell-VQ input. Tests whether decoupling "
            "alone (a mild form of the Spine A architecture) lifts "
            "niche / cell metrics on this spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+niche-neck(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_niche_neck(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[256],
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+niche-neck-256-256+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 12 (Spine B, axis 3 — niche-side "
            "encoder AFTER cell codebook): ADD a DEEPER niche-neck "
            "`[256, 256]` (2 hidden of 256). Matches Spine A's "
            "default niche-neck depth on the Spine B knn16+sampler16 "
            "spatial setup."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+niche-neck(mlp=[256, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_sampler_neighbors(
                    _patch_dual_graph_knn(
                        _patch_dual_decoder_width(
                            _patch_dual_niche_neck(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[256, 256],
                            ),
                            hidden_channels=[32],
                        ),
                        n_neighs=16,
                    ),
                    num_neighbors=[16],
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+adj-w500+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 13 (Spine B, axis 1 — adjacency "
            "weight): WEAKER cosine BCE adjacency loss "
            "`wt_adj_reconstr=500`."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_adj_reconstr=500",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=500.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 14 (Spine B, axis 1 — adjacency "
            "weight): STRONGER cosine BCE adjacency loss "
            "`wt_adj_reconstr=2000`."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 15 (Spine B, axis 4 — cell-NB "
            "weight): STRONGER cell-level NB recon "
            "`wt_attr_reconstr=2.0`."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s37_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s37 sweep, variant 16 (Spine B combo): stacks v10 "
            "(tighter encoder `[200, 200, 128]` + gnn-h=128), v15 "
            "(cell-w=2.0), and v14 (adj-w=2000) on the +knn16+"
            "sampler16 no-batch-int spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0", "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_gnn_hidden(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden=128,
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },

    # ========================================================================
    # s38 — 16 ablations around the s37_v15 base (no-batch-int + cell-w2.0
    # on the +enc-deeper+dec-w32+knn16+sampler16 spine, under within-section
    # BCE).
    # ========================================================================
    # Base architecture (= s37_v15):
    #   RVQ (30, 90) on both branches, decoder-cov embed=16,
    #   NO batch-integration loss (no adv / mmd-batch / mmd-prior),
    #   encoder MLP [400, 400, 256], 1-layer SAGE GNN h=256,
    #   decoders [32], k=16 spatial graph + sampler [16],
    #   wt_attr_reconstr = 2.0, adj_within_section_only = True.
    #
    # Axes (each variant changes ONE knob — combos at v15/v16):
    #   v1-v5  : encoder dimensions & sizes (axis 1)
    #   v6-v11 : loss weights — adjacency / cell-NB / nbr-NB (axis 2)
    #   v12-v14: batch_size + learning rate (sqrt-scaled pairs)   (axis 3)
    #   v15-v16: Pareto combos
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v38
    # REQUIRES BLOB REBUILD if the on-disk blob is missing k=16
    # (`make_dataset_blob_config()` ships `n_neighs_list = [6, 8, 12, 16]`).

    # ------------------------------------------------------------------------
    # Axis 1 — encoder dimensions & sizes
    # ------------------------------------------------------------------------
    "s38_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-64+gnn-h64+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 1 (axis 1 — encoder, latent 64): "
            "encoder MLP `[256, 256, 64]` (3 hidden, funnels to "
            "latent 64) + GNN h=64. TIGHTEST latent in the sweep — "
            "quarters the codebook embedding dim vs the base's 256. "
            "Tests how far we can shrink before reconstruction or "
            "niche metrics degrade."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 64])", "+gnn-h=64",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[256, 256, 64],
                                    ),
                                    hidden=64,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-128+gnn-h128+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 2 (axis 1 — encoder, latent 128): "
            "encoder MLP `[256, 256, 128]` (3 hidden, funnels to "
            "latent 128) + GNN h=128. Halves the codebook embedding "
            "dim vs the base's 256."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 128])", "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[256, 256, 128],
                                    ),
                                    hidden=128,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-256+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 3 (axis 1 — encoder, latent 256): "
            "encoder MLP `[256, 256, 256]` (uniform-width 3-layer "
            "trunk at 256, vs the base's `[400, 400, 256]` whose "
            "first two layers are 400-wide). GNN h=256 (default) "
            "matches latent. Tests whether the 400-wide intermediate "
            "layers are necessary or a uniform 256-wide trunk works."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    hidden_channels=[256, 256, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-512-512-512+gnn-h512+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 4 (axis 1 — encoder, latent 512): "
            "encoder MLP `[512, 512, 512]` (uniform-width 3-layer "
            "at 512) + GNN h=512. WIDEST latent in this sweep — "
            "doubles the codebook embedding dim vs the base's 256. "
            "Tests whether more representational room helps cell-"
            "type / niche separation."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[512, 512, 512])", "+gnn-h=512",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[512, 512, 512],
                                    ),
                                    hidden=512,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 5 (axis 1 — encoder, depth = 1): "
            "SHALLOW encoder `[256]` (1 hidden layer of 256, latent "
            "dim 256). Depth comparison vs v3 (`[256, 256, 256]`, "
            "3 layers at the same latent): does the 3-layer trunk "
            "carry information the 1-layer trunk loses?"
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-shallow(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_shallow(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                    hidden_channels=[256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },

    # ------------------------------------------------------------------------
    # Axis 2 — loss weights (adjacency / cell-NB / nbr-NB)
    # ------------------------------------------------------------------------
    "s38_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+adj-w500+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 6 (axis 2 — adjacency weight): "
            "WEAKER cosine BCE adjacency `wt_adj_reconstr=500` "
            "(vs default 1000). Tests whether 1000 is too strong "
            "on the no-batch-int spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0", "+wt_adj_reconstr=500",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    weight=500.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 7 (axis 2 — adjacency weight): "
            "STRONGER cosine BCE adjacency `wt_adj_reconstr=2000` "
            "(vs default 1000). With cross-section pairs masked, "
            "stronger spatial pressure may be safe / beneficial."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0", "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w3.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 8 (axis 2 — cell-NB weight): "
            "STRONGER cell NB recon `wt_attr_reconstr=3.0` (vs the "
            "base's 2.0). Pushes the cell-level reconstruction "
            "harder — should sharpen cell-type NMI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=3.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=3.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 9 (axis 2 — cell-NB weight): "
            "VERY STRONG cell NB recon `wt_attr_reconstr=5.0` (vs "
            "the base's 2.0). Aggressive bump — expect cell-NMI to "
            "improve at the possible cost of niche metrics."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 90),
                                        ),
                                    ),
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    weight=5.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+nbr-w0.5+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 10 (axis 2 — niche NB weight): "
            "WEAKER niche NB recon `wt_attr_reconstr_nbr=0.5` (vs "
            "default 1.0). Cell-w stays at 2.0. Freeing the niche "
            "embedding from per-batch nbr-gene means tends to lift "
            "niche-side iLISI."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0", "+wt_attr_reconstr_nbr=0.5",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_nbr_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    weight=0.5,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+nbr-w2.0+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 11 (axis 2 — niche NB weight): "
            "STRONGER niche NB recon `wt_attr_reconstr_nbr=2.0` "
            "(vs default 1.0). Symmetric with cell-w=2.0 — matched "
            "pressure on cell and niche branches."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0", "+wt_attr_reconstr_nbr=2.0",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_attr_recon_nbr_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    weight=2.0,
                ),
            ),
            enabled=True,
        ),
    },

    # ------------------------------------------------------------------------
    # Axis 3 — batch size + learning rate (sqrt-scaled pairs)
    # ------------------------------------------------------------------------
    "s38_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 12 (axis 3 — bs+lr): batch_size=512, "
            "lr=7e-4 (sqrt-scaled 2× the base's bs=256/lr=5e-4). "
            "Larger batch = smoother per-step gradient; sqrt-lr keeps "
            "the per-sample gradient roughly constant."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs1024+lr1e-3+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 13 (axis 3 — bs+lr): batch_size=1024, "
            "lr=1e-3 (sqrt-scaled 4× the base's bs=256/lr=5e-4). "
            "Per s22 sweep history, sqrt scaling at this level worked "
            "on the +wide spine; this re-tests on the no-batch-int "
            "spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+batch_size=1024", "+lr=1e-3",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    batch_size=1024, lr=1e-3,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs128+lr3.5e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 14 (axis 3 — bs+lr): batch_size=128, "
            "lr=3.5e-4 (sqrt-scaled 0.5× the base's bs=256/lr=5e-4). "
            "Smaller batch + smaller lr — slower / noisier gradient "
            "but more update steps per epoch. Useful if the base's "
            "batch=256 is too smooth for the codebook to settle into "
            "the right partition."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=2.0",
            "+batch_size=128", "+lr=3.5e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=2.0,
                    ),
                    batch_size=128, lr=3.5e-4,
                ),
            ),
            enabled=True,
        ),
    },

    # ------------------------------------------------------------------------
    # Pareto combos
    # ------------------------------------------------------------------------
    "s38_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w3.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 15 (combo): stacks v3 (tighter "
            "encoder [200, 200, 128] + gnn-h128) + v7 (adj-w=2000) "
            "+ v8 (cell-w=3.0) on the base spine. Pareto candidate "
            "combining the most-promising single-knob bumps."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])", "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=3.0", "+wt_adj_reconstr=2000",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_adj_weight(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_gnn_hidden(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden=128,
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=3.0,
                    ),
                    weight=2000.0,
                ),
            ),
            enabled=True,
        ),
    },
    "s38_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w3.0+nbr-w0.5+adj-w2000+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s38 sweep, variant 16 (kitchen-sink combo): tighter "
            "encoder [200, 200, 128] + gnn-h128 + cell-w=3.0 + "
            "nbr-w=0.5 + adj-w=2000 + bs=512 + lr=7e-4. Stacks all "
            "of v3, v7, v8, v10, v12 — if these axes are mostly "
            "additive, this should be the Pareto winner of the "
            "sweep."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])", "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=3.0", "+wt_attr_reconstr_nbr=0.5",
            "+wt_adj_reconstr=2000",
            "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_adj_weight(
                        _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_gnn_hidden(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[200, 200, 128],
                                                ),
                                                hidden=128,
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=3.0,
                            ),
                            weight=0.5,
                        ),
                        weight=2000.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },

    # ========================================================================
    # s39 — cell-NMI Pareto sweep on the s37_v15 spine (cell-w removed)
    # ========================================================================
    # User observation: bumping wt_attr_reconstr from 1.0 -> 5.0 (s38 cell-w
    # axis) didn't lift cell-NMI meaningfully — NB-recon weight tightens
    # per-cell accuracy but NMI rewards cluster separation, which is a
    # different property. This sweep targets cluster separation directly
    # via FOUR orthogonal axes:
    #   A — cell-branch codebook cardinality (v1 / v2 + mixes)
    #   B — VQ warmup (v3 + mixes)
    #   C — tighter latent (v4 + mixes)
    #   D — codebook orthogonality reg (v5 + mixes)
    # plus 11 mix variants covering pairs (v6-v14) and triples / kitchen
    # sink (v15, v16).
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v39
    # Base spine requires the blob to contain k=16 (already present after
    # any s32/s36/s37/s38 sweep rebuild).

    "s39_v1_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 1 (A — cell codebook (30,30)): "
            "cell-branch RVQ (30, 30) — halves L2 budget vs the base's (30, 90); niche stays at (30, 90). "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 30),
                                        ),
                                    ),
                                    hidden_channels=[400, 400, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s39_v2_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 2 (A — cell codebook (30,10)): "
            "cell-branch RVQ (30, 10) — only 300 composite cell codes, matched to the ~15-30 cell-type cardinality; niche stays at (30, 90). "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 10], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(30, 10),
                                        ),
                                    ),
                                    hidden_channels=[400, 400, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s39_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 3 (B — VQ warmup): "
            "vq_warmup_epochs=10 — defer codebook init / EMA past epoch 0 so codes anchor on a settled z_mlp. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "s39_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 4 (C — tighter latent): "
            "encoder MLP [200, 200, 128] + GNN h=128 (latent dim 128 vs 256). "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[200, 200, 128],
                                    ),
                                    hidden=128,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s39_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 5 (D — orth-reg): "
            "l2_codebook_orthogonal_regularization_loss on the cell-branch L1 codebook (wt=0.2). "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },
    "s39_v6_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 6 (A×B): "
            "cell RVQ (30, 30) + vq-warmup10. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 30),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "s39_v7_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 7 (A×C): "
            "cell RVQ (30, 30) + tighter latent. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 30),
                                            ),
                                        ),
                                        hidden_channels=[200, 200, 128],
                                    ),
                                    hidden=128,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s39_v8_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 8 (A×D): "
            "cell RVQ (30, 30) + orth-reg w=0.2. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 30),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },
    "s39_v9_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 9 (A×B (30-10)): "
            "cell RVQ (30, 10) + vq-warmup10. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 10], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 10),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "s39_v10_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 10 (A×C (30-10)): "
            "cell RVQ (30, 10) + tighter latent. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 10], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_gnn_hidden(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 10),
                                            ),
                                        ),
                                        hidden_channels=[200, 200, 128],
                                    ),
                                    hidden=128,
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s39_v11_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 11 (A×D (30-10)): "
            "cell RVQ (30, 10) + orth-reg w=0.2. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 10], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 10),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },
    "s39_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 12 (B×C): "
            "vq-warmup10 + tighter latent. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_gnn_hidden(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden=128,
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "s39_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 13 (B×D): "
            "vq-warmup10 + orth-reg w=0.2. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_vq_warmup(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                vq_warmup_epochs=10,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },
    "s39_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 14 (C×D): "
            "tighter latent + orth-reg w=0.2. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_gnn_hidden(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden=128,
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },
    "s39_v15_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 15 (A×B×C triple): "
            "cell RVQ (30, 30) + vq-warmup10 + tighter latent. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
        ],
        "build": lambda: _patch_dual_vq_warmup(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_gnn_hidden(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 30),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden=128,
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            vq_warmup_epochs=10,
        ),
    },
    "s39_v16_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+orth-reg-w0.2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s39 sweep, variant 16 (kitchen sink): "
            "cell RVQ (30, 30) + vq-warmup10 + tighter latent + orth-reg w=0.2. "
            "Base: no-batch-int + within-sec BCE on the +enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4 spine."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+gnn-h=128",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+vq_warmup_epochs=10",
            "+codebook_orth_reg(wt=0.2)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_vq_warmup(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_gnn_hidden(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 30),
                                                    ),
                                                ),
                                                hidden_channels=[200, 200, 128],
                                            ),
                                            hidden=128,
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                vq_warmup_epochs=10,
            ),
            wt_codebook_orthogonal_regularization=0.2,
        ),
    },

    # ========================================================================
    # s40 — targeted cell-NMI sweep (8 variants, focused fixes)
    # ========================================================================
    # Follow-up to s39: orth-reg at wt=0.2 was 1000x too weak to matter,
    # and changing cell RVQ L2 didn't help because cell-NMI is computed
    # against L1 indices. This sweep fixes both errors:
    #   v1 — niche-neck (move one encoder layer to niche-only path)
    #   v2 — cell RVQ (15, 90)  — reduce L1 to ~cell-type cardinality
    #   v3 — cell RVQ (20, 90)  — bracket v2 from above
    #   v4 — single-level cell VQ K=15  — drop residual on cell branch
    #   v5 — orth-reg wt=20    (100x the failed s39 weight)
    #   v6 — orth-reg wt=200   (1000x — comparable to adj-BCE scale)
    #   v7 — cell RVQ (15, 90) + orth-reg wt=200
    #   v8 — single-level cell VQ K=15 + orth-reg wt=200
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v40

    "s40_v1_dualvq+rvq-both+niche-neck-256+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 1: ADD a niche-only MLP [256] between z_mlp and the GNN. Niche-side gradients (adj-BCE, nbr-NB) no longer flow back into the layer that was previously shared with the cell branch — the cell-VQ input is now insulated from niche-side pulls. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+niche-neck(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_niche_neck(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s40_v2_dualvq+rvq-cell-15-90+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 2: Cell RVQ (15, 90) — L1 reduced from 30 to 15 to match the cell-type cardinality on mmb-smb. Direct fix for the L1-vs-cell-types size mismatch I missed in s39. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[15, 90], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(15, 90),
                                        ),
                                    ),
                                    hidden_channels=[400, 400, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s40_v3_dualvq+rvq-cell-20-90+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 3: Cell RVQ (20, 90) — L1 reduced from 30 to 20. Brackets v2 from above; pick whichever matches the true cell-type count better. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[20, 90], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_rvq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_sizes=(20, 90),
                                        ),
                                    ),
                                    hidden_channels=[400, 400, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s40_v4_dualvq+vq-cell-15+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 4: Single-level VQ on the cell branch (K=15) — drops the residual hierarchy entirely. RVQ residualizes; the second level only helps cell-NMI if you actually want sub-types. A flat 15-way VQ is the cleanest 'cell-type-by-codebook' setup. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+vq(cell, K=15)",
            "+rvq(niche, [30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_graph_knn(
                            _patch_dual_decoder_width(
                                _patch_dual_encoder_deeper(
                                    _patch_dual_decoder_covariate(
                                        _patch_dual_vq(
                                            _patch_dual_rvq(_BD(),
                                                branch="niche", codebook_sizes=(30, 90)),
                                            branch="cell", codebook_size=15,
                                        ),
                                    ),
                                    hidden_channels=[400, 400, 256],
                                ),
                                hidden_channels=[32],
                            ),
                            n_neighs=16,
                        ),
                        num_neighbors=[16],
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s40_v5_dualvq+rvq-both+orth-reg-w20+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 5: Orth-reg at wt=20 (100x the failed s39 wt=0.2). Brings the weighted orth-reg contribution to ~20 nats max — comparable to other loss terms. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=20.0)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=20.0,
        ),
    },
    "s40_v6_dualvq+rvq-both+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 6: Orth-reg at wt=200 (1000x the failed s39 wt=0.2) — comparable scale to the BCE adjacency loss. Strong cluster-separation pressure on the cell L1 codebook. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=200.0)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=200.0,
        ),
    },
    "s40_v7_dualvq+rvq-cell-15-90+rvq-niche-30-90+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 7: Combo: cell RVQ (15, 90) AND orth-reg at wt=200. If the L1 cardinality fix lifts cell-NMI alone, this tests whether orth-reg adds on top. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[15, 90], niche=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=200.0)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(15, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=200.0,
        ),
    },
    "s40_v8_dualvq+vq-cell-15+rvq-niche-30-90+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s40 sweep, variant 8: Combo: single-level cell VQ (K=15) AND orth-reg at wt=200. Pareto candidate — flat 15-way codebook with strong inter-code separation pressure. "
            "Base: no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. "
            "REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+vq(cell, K=15)",
            "+rvq(niche, [30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook_orth_reg(wt=200.0)",
        ],
        "build": lambda: _patch_dual_codebook_orth_reg(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_vq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_size=15,
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_codebook_orthogonal_regularization=200.0,
        ),
    },
    # -----------------------------------------------------------------------
    # s42 sweep — 16 ablations: encoder architecture size sweep
    # -----------------------------------------------------------------------
    # Hypothesis: the current best variant has cell-NMI dragged down by
    # spatial-gradient leakage into z_mlp. Two structural fixes were added:
    #   - +decoupled-enc: two INDEPENDENT MLPs (cell + niche paths)
    #   - +shared-specific-enc: shared trunk + two path-specific MLPs
    # s42 sweeps the SIZE of each architecture.
    #
    # Variants 1-8: +decoupled-enc, varying the trunk shape
    #   v1 [256]            — shallow single-layer
    #   v2 [400, 256]       — 2-layer (medium depth)
    #   v3 [400, 400, 64]   — default depth, TIGHT latent 64
    #   v4 [400, 400, 128]  — default depth, mid latent 128
    #   v5 [400, 400, 256]  — default enc-deeper (decoupled-enc control)
    #   v6 [400, 400, 512]  — default depth, WIDE latent 512
    #   v7 [200, 200, 128]  — narrower trunk + tight latent
    #   v8 [800, 800, 256]  — wider trunk
    #
    # Variants 9-16: +shared-specific-enc, varying (shared_last_dim, path_last_dim)
    #   total concat dim = shared + path (= VQ_cell embed dim, = GNN input dim)
    #   v9  (64, 64)    — tight total 128
    #   v10 (128, 128)  — default total 256 (Y-shape control)
    #   v11 (256, 256)  — wide total 512
    #   v12 (192, 64)   — total 256, shared dominates
    #   v13 (64, 192)   — total 256, path dominates
    #   v14 (128, 64)   — total 192, moderate shared-bigger
    #   v15 (64, 128)   — total 192, moderate path-bigger
    #   v16 (192, 128)  — total 320, both larger
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v42
    "s42_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 1 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [256]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 2 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [400, 256]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-64+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 3 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [400, 400, 64]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 64])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 64],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 4 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [400, 400, 128]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 128])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 128],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 5 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [400, 400, 256]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-512+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 6 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [400, 400, 512]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 512])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 512],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 7 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [200, 200, 128]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[200, 200, 128])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[200, 200, 128],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-800-800-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 8 (decoupled-enc): two independent encoder MLPs (cell + niche paths), both shaped [800, 800, 256]. Spatial-loss gradients NEVER reach the cell MLP — the cell-VQ input is structurally shielded from spatial pulls. Tests trunk shape + latent-dim sensitivity for the decoupled mode. Base: cell-w5.0 + no-batch-int + within-sec + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[800, 800, 256])",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[800, 800, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 9 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=64, path MLP last dim=64, downstream concat dim=128 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=64, path_last=64, concat_dim=128)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=64,
                            path_last_dim=64,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared128-path128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 10 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=128, path MLP last dim=128, downstream concat dim=256 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=128, path_last=128, concat_dim=256)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=128,
                            path_last_dim=128,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared256-path256+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 11 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=256, path MLP last dim=256, downstream concat dim=512 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=256, path_last=256, concat_dim=512)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=256,
                            path_last_dim=256,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared192-path64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 12 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=192, path MLP last dim=64, downstream concat dim=256 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=192, path_last=64, concat_dim=256)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=192,
                            path_last_dim=64,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path192+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 13 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=64, path MLP last dim=192, downstream concat dim=256 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=64, path_last=192, concat_dim=256)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=64,
                            path_last_dim=192,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared128-path64+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 14 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=128, path MLP last dim=64, downstream concat dim=192 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=128, path_last=64, concat_dim=192)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=128,
                            path_last_dim=64,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 15 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=64, path MLP last dim=128, downstream concat dim=192 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=64, path_last=128, concat_dim=192)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=64,
                            path_last_dim=128,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s42_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared192-path128+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s42 sweep, variant 16 (Y-shape encoder): shared trunk + two path-specific MLPs. Shared MLP last dim=192, path MLP last dim=128, downstream concat dim=320 (= VQ_cell embed dim, also = GNN input dim). All three MLPs inherit the trunk shape [400, 400, *] from enc-deeper. Tests the trade-off between shared (gradient-coupled, parameter-efficient feature reuse) and path-specific (gradient-isolated, specialised) capacity. Base: cell-w5.0 + no-batch-int + within-sec + enc-deeper + dec-w32 + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+shared-specific-enc(shared_last=192, path_last=128, concat_dim=320)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_shared_specific_encoders(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            shared_last_dim=192,
                            path_last_dim=128,
                        ),
                        weight=5.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    # -----------------------------------------------------------------------
    # s43 sweep — 16 ablations: strictly-decoupled architecture SIZE grid
    # -----------------------------------------------------------------------
    # `+decoupled-enc` separates the cell + niche encoder MLPs. `+decoupled-
    # dec-cov` further splits the decoder's batch embedding into TWO
    # independent nn.Embeddings — one per decoder. Together they remove
    # EVERY learnable parameter shared between the cell and niche pathways.
    # (Encoder FiLM is already OFF by default in the dual cfg — no patch.)
    #
    # 4 × 4 grid of (encoder shape × decoder width):
    #   E1 [128]            shallow + tight latent 128
    #   E2 [256]            shallow, latent 256
    #   E3 [256, 128]       2-layer narrow + tight latent 128
    #   E4 [400, 256]       2-layer medium, latent 256
    #
    #   D1 [32]    current default — control
    #   D2 [64]
    #   D3 [128]
    #   D4 [256]
    #
    # Variant index order: v(4*i+j+1) = (E(i+1), D(j+1)), 0<=i,j<4.
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v43
    "s43_v1_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 1: STRICTLY decoupled architecture + (encoder shape [128], decoder width [32]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v2_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 2: STRICTLY decoupled architecture + (encoder shape [128], decoder width [64]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[64]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128],
                                            ),
                                            hidden_channels=[64],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v3_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 3: STRICTLY decoupled architecture + (encoder shape [128], decoder width [128]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[128]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128],
                                            ),
                                            hidden_channels=[128],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v4_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 4: STRICTLY decoupled architecture + (encoder shape [128], decoder width [256]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128],
                                            ),
                                            hidden_channels=[256],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v5_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 5: STRICTLY decoupled architecture + (encoder shape [256], decoder width [32]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v6_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 6: STRICTLY decoupled architecture + (encoder shape [256], decoder width [64]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[64]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256],
                                            ),
                                            hidden_channels=[64],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v7_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 7: STRICTLY decoupled architecture + (encoder shape [256], decoder width [128]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[128]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256],
                                            ),
                                            hidden_channels=[128],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v8_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 8: STRICTLY decoupled architecture + (encoder shape [256], decoder width [256]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256],
                                            ),
                                            hidden_channels=[256],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v9_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 9: STRICTLY decoupled architecture + (encoder shape [256, 128], decoder width [32]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v10_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 10: STRICTLY decoupled architecture + (encoder shape [256, 128], decoder width [64]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[64]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128],
                                            ),
                                            hidden_channels=[64],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v11_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 11: STRICTLY decoupled architecture + (encoder shape [256, 128], decoder width [128]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[128]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128],
                                            ),
                                            hidden_channels=[128],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v12_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 12: STRICTLY decoupled architecture + (encoder shape [256, 128], decoder width [256]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128],
                                            ),
                                            hidden_channels=[256],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v13_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 13: STRICTLY decoupled architecture + (encoder shape [400, 256], decoder width [32]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v14_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 14: STRICTLY decoupled architecture + (encoder shape [400, 256], decoder width [64]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+dec-w=[64]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256],
                                            ),
                                            hidden_channels=[64],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v15_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 15: STRICTLY decoupled architecture + (encoder shape [400, 256], decoder width [128]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+dec-w=[128]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256],
                                            ),
                                            hidden_channels=[128],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    "s43_v16_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s43 sweep, variant 16: STRICTLY decoupled architecture + (encoder shape [400, 256], decoder width [256]). `+decoupled-enc` separates the cell-side and niche-side MLPs; `+decoupled-dec-cov` further splits the decoder-side batch embedding (`batch_embedding`) into TWO independent `nn.Embedding` modules so each decoder owns its own batch covariate. Combined with `+decoupled-enc`, this leaves ZERO learnable parameters shared between the cell and niche pathways. (Encoder FiLM is OFF by default in the dual cfg — no patch needed for that.) Tests the (shallow encoder × wide decoder) trade-off under the strictly-decoupled architecture. Base: cell-w5.0 + no-batch-int + within-sec + knn16 + sampler16 + bs=512 + lr=7e-4. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+dec-w=[256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_decoupled_encoders(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256],
                                            ),
                                            hidden_channels=[256],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    # -----------------------------------------------------------------------
    # s44 sweep — 1 ablation: negative test of architectural-leakage hypothesis
    # -----------------------------------------------------------------------
    # Spine identical to s43_v5; only +no-recon added on top. Direct A/B for
    # the diagnostic question: does the cell codebook get its structure from
    # the reconstruction signal, or from k-means init + EMA on raw z_mlp?
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v44
    "s44_v1_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+no-recon+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s44 NEGATIVE TEST: identical spine to s43_v5 (strictly decoupled encoders + decoupled decoder-cov, encoder=[256], decoder=[32]) with the ONE additional change of `+no-recon`: every reconstruction loss is removed. Only the commit losses (mse_commit_loss_cell + mse_commit_loss_niche) remain in the loss list; EMA codebook updates still fire every step. The encoder receives gradient signal ONLY from the commit pull toward the nearest codebook entry. \n\nCell-NMI is computed DIRECTLY on the discrete L0 macro-code index `cell_code_indices[level_0]` (= K1=30 partition over all cells) against the `cell_type` label — no Leiden, no continuous embedding. So the ceiling for level_0 NMI is hard-bounded by K1=30 vs the cell-type cardinality of the dataset; raising K1 (e.g. +rvq(cell, [60, 90])) is the most direct lever on this metric. The `cell_code_indices[composite]` view (up to K1*K2=2700 leaves) is the more sensitive diagnostic when level_0 NMI is at the cardinality ceiling. \n\nHypothesis discrimination for THIS variant: if cell-NMI here is similar to s43_v5's cell-NMI, then reconstruction wasn't the lever — k-means initialisation on z_mlp (just a random projection of gene expression) plus EMA already produces a 30-cluster partition that's as cell-type-aligned as the model achieves with full training. Architectural changes can't push past that init-driven ceiling. If cell-NMI crashes to baseline here, reconstruction IS the driver and the level_0 ceiling is whatever s43 produces — K1 has to go up. \n\nRemoved from loss_names: nb_attribute_reconstruction_loss, nb_attribute_reconstruction_loss_nbr_dual, bce_cosine_adjacency_reconstruction_loss. Kept: mse_commit_loss_cell, mse_commit_loss_niche. (cell-w=5.0 is a no-op here since cell-NB is gone, kept in the spine for variant-name comparability with s43_v5.) REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0  (no-op: cell-NB is removed)",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True  (no-op: adj-BCE is removed)",
            "+decoupled-enc(niche MLP = independent copy of cell MLP)",
            "+no-recon(drops cell-NB + nbr-NB + adj-BCE; keeps commits)",
        ],
        "build": lambda: _patch_dual_no_recon(
            _patch_dual_decoupled_decoder_cov(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_decoupled_encoders(
                                    _patch_dual_sampler_neighbors(
                                        _patch_dual_graph_knn(
                                            _patch_dual_decoder_width(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[256],
                                                ),
                                                hidden_channels=[32],
                                            ),
                                            n_neighs=16,
                                        ),
                                        num_neighbors=[16],
                                    ),
                                ),
                                weight=5.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
            ),
        ),
    },
    # -----------------------------------------------------------------------
    # s45 sweep — 1 diagnostic: shared MLP trunk + no cell-NB
    # -----------------------------------------------------------------------
    # Pairs with s44_v1 (decoupled enc + no recon) as a 2x2 diagnostic.
    # Tests whether the niche-side losses on the SHARED z_mlp are doing
    # cell-type discrimination by themselves, without cell-NB pressure.
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v45
    "s45_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+no-cell-nb+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s45 DIAGNOSTIC: identical spine to the s42 winner (SHARED MLP trunk — no +decoupled-enc, `+enc-deeper` = [400, 400, 256], +rvq-both [30, 90], +decoder-cov, +no-batch-int, +dec-w32, knn16, sampler16, cell-w5.0, bs=512, lr=7e-4, within-sec). Only modification: `+no-cell-nb` drops `nb_attribute_reconstruction_loss` while keeping `mse_commit_loss_cell` and the entire niche-side loss stack (`nb_attribute_reconstruction_loss_nbr_dual`, `mse_commit_loss_niche`, `bce_cosine_adjacency_reconstruction_loss`). \n\nWhat gets gradient signal in this variant: z_mlp (shared trunk) is shaped by cell-commit + nbr-NB + niche-commit + adjacency-BCE (the last three all flow through the shared trunk because there's no +decoupled-enc). The cell decoder still runs but receives no gradient signal — its outputs are unsupervised. \n\nHypothesis test for `cell_code_indices[level_0]` NMI (= K1=30 macro codes vs cell_type, directly via normalized_mutual_info_score). \n  - level_0 NMI ≈ s42 winner: spatial-side losses on the shared     trunk were doing the cell-type discrimination; cell-NB was     decorative for this metric. \n  - level_0 NMI crashes: cell-NB was load-bearing for the L0     partition and the niche-side gradients on the shared trunk     were NOT producing cell-type-aligned z_mlp geometry. \n\nPairs with s44_v1 (decoupled enc + no recon at all) as a 2x2 negative-test matrix: \n      shared trunk  decoupled trunk \n  full recon:  s42 winner    s43_v5         \n  no cell-NB:  s45_v1        (s44_v1 = no recon at all) \n\n(cell-w=5.0 is a no-op here since cell-NB is removed; kept in the spine for variant-name comparability with the s42 winner.)"
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0  (no-op: cell-NB is removed)",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+no-cell-nb(drops nb_attribute_reconstruction_loss; keeps mse_commit_loss_cell)",
        ],
        "build": lambda: _patch_dual_no_cell_nb(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=5.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
        ),
    },
    # -----------------------------------------------------------------------
    # s46 sweep — 4 ablations: drastically increased cell-NB weight
    # -----------------------------------------------------------------------
    # Follow-up to s44_v1's negative-test finding: cell-NB at wt=5
    # contributes ~0.15 NMI to cell_code_indices[level_0]. This sweep
    # tests whether pushing the weight to 100 / 300 / 500 / 1000 (vs
    # the s42 winner's 5.0) continues to raise cell-NMI or saturates /
    # causes the niche branch to collapse from gradient imbalance.
    #
    # Spine: s42 winner (shared MLP, `+enc-deeper`).
    # Only knob changed: cell-NB weight.
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v46
    "s46_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w100+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s46 sweep, variant 1: identical spine to the s42 winner (shared MLP, `+enc-deeper` = [400, 400, 256], RVQ-both [30, 90], +decoder-cov, +no-batch-int, +dec-w=[32], knn16, sampler16, bs=512, lr=7e-4, within-sec) — but with `wt_attr_reconstr=100` (vs the s42 winner's 5.0). \n\nContext: s44_v1 (no-recon ablation) brought cell-NMI on `cell_code_indices[level_0]` from 0.4 down to 0.25 and simultaneously RAISED the niche-NMI of cell codes (codes drifted into spatial structure once cell-NB pressure was removed). cell-NB is therefore the cell-NMI lever in the shared-trunk design. This sweep tests whether pushing the weight 20x-200x continues to lift cell-NMI, saturates, or causes the niche branch to collapse (since the same shared `z_mlp` is increasingly dominated by cell-NB gradient mass). \n\nExpected response curve: \n  - cell-NMI on cell codes: monotonic up until saturation. \n  - niche-NMI on cell codes: monotonic down (codes become     more cell-only, less spatial). \n  - niche-NMI on niche codes: may drop (niche branch's z_mlp     is now shaped primarily by cell-NB; the spatial losses     are gradient-overpowered). \n  - Pearson reconstruction: should improve on cell-branch     metrics, may worsen on nbr-branch metrics. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=100.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=100.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s46_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w300+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s46 sweep, variant 2: identical spine to the s42 winner (shared MLP, `+enc-deeper` = [400, 400, 256], RVQ-both [30, 90], +decoder-cov, +no-batch-int, +dec-w=[32], knn16, sampler16, bs=512, lr=7e-4, within-sec) — but with `wt_attr_reconstr=300` (vs the s42 winner's 5.0). \n\nContext: s44_v1 (no-recon ablation) brought cell-NMI on `cell_code_indices[level_0]` from 0.4 down to 0.25 and simultaneously RAISED the niche-NMI of cell codes (codes drifted into spatial structure once cell-NB pressure was removed). cell-NB is therefore the cell-NMI lever in the shared-trunk design. This sweep tests whether pushing the weight 20x-200x continues to lift cell-NMI, saturates, or causes the niche branch to collapse (since the same shared `z_mlp` is increasingly dominated by cell-NB gradient mass). \n\nExpected response curve: \n  - cell-NMI on cell codes: monotonic up until saturation. \n  - niche-NMI on cell codes: monotonic down (codes become     more cell-only, less spatial). \n  - niche-NMI on niche codes: may drop (niche branch's z_mlp     is now shaped primarily by cell-NB; the spatial losses     are gradient-overpowered). \n  - Pearson reconstruction: should improve on cell-branch     metrics, may worsen on nbr-branch metrics. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=300.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=300.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s46_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w500+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s46 sweep, variant 3: identical spine to the s42 winner (shared MLP, `+enc-deeper` = [400, 400, 256], RVQ-both [30, 90], +decoder-cov, +no-batch-int, +dec-w=[32], knn16, sampler16, bs=512, lr=7e-4, within-sec) — but with `wt_attr_reconstr=500` (vs the s42 winner's 5.0). \n\nContext: s44_v1 (no-recon ablation) brought cell-NMI on `cell_code_indices[level_0]` from 0.4 down to 0.25 and simultaneously RAISED the niche-NMI of cell codes (codes drifted into spatial structure once cell-NB pressure was removed). cell-NB is therefore the cell-NMI lever in the shared-trunk design. This sweep tests whether pushing the weight 20x-200x continues to lift cell-NMI, saturates, or causes the niche branch to collapse (since the same shared `z_mlp` is increasingly dominated by cell-NB gradient mass). \n\nExpected response curve: \n  - cell-NMI on cell codes: monotonic up until saturation. \n  - niche-NMI on cell codes: monotonic down (codes become     more cell-only, less spatial). \n  - niche-NMI on niche codes: may drop (niche branch's z_mlp     is now shaped primarily by cell-NB; the spatial losses     are gradient-overpowered). \n  - Pearson reconstruction: should improve on cell-branch     metrics, may worsen on nbr-branch metrics. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=500.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=500.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    "s46_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1000+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s46 sweep, variant 4: identical spine to the s42 winner (shared MLP, `+enc-deeper` = [400, 400, 256], RVQ-both [30, 90], +decoder-cov, +no-batch-int, +dec-w=[32], knn16, sampler16, bs=512, lr=7e-4, within-sec) — but with `wt_attr_reconstr=1000` (vs the s42 winner's 5.0). \n\nContext: s44_v1 (no-recon ablation) brought cell-NMI on `cell_code_indices[level_0]` from 0.4 down to 0.25 and simultaneously RAISED the niche-NMI of cell codes (codes drifted into spatial structure once cell-NB pressure was removed). cell-NB is therefore the cell-NMI lever in the shared-trunk design. This sweep tests whether pushing the weight 20x-200x continues to lift cell-NMI, saturates, or causes the niche branch to collapse (since the same shared `z_mlp` is increasingly dominated by cell-NB gradient mass). \n\nExpected response curve: \n  - cell-NMI on cell codes: monotonic up until saturation. \n  - niche-NMI on cell codes: monotonic down (codes become     more cell-only, less spatial). \n  - niche-NMI on niche codes: may drop (niche branch's z_mlp     is now shaped primarily by cell-NB; the spatial losses     are gradient-overpowered). \n  - Pearson reconstruction: should improve on cell-branch     metrics, may worsen on nbr-branch metrics. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1000.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
        ],
        "build": lambda: _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_encoder_deeper(
                                        _patch_dual_decoder_covariate(
                                            _patch_dual_rvq(
                                                _patch_dual_rvq(_BD(),
                                                    branch="niche", codebook_sizes=(30, 90)),
                                                branch="cell", codebook_sizes=(30, 90),
                                            ),
                                        ),
                                        hidden_channels=[400, 400, 256],
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1000.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
    },
    # -----------------------------------------------------------------------
    # s47 sweep — 8 ablations: contrastive + codebook diversity
    # -----------------------------------------------------------------------
    # Spine: s42 winner with cell-w lowered 5.0 -> 1.0 (s46 escalation did
    # not help). New aux losses on top:
    #   v1-v4: contrastive only (NT-Xent on z_mlp_cell, top-k log1p-gene-
    #          expression nearest neighbours as positives)
    #   v5-v6: codebook diversity only (SwAV-style code repulsion)
    #   v7-v8: contrastive + diversity combined
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v47
    "s47_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w0.5-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 1: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=0.5, k_pos=5, temp=0.1). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=0.5, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=0.5,
            k_pos=5, temperature=0.1,
        ),
    },
    "s47_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 2: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=1.0, k_pos=5, temp=0.1). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=1.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=1.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s47_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w2.0-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 3: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=2.0, k_pos=5, temp=0.1). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=2.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=2.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s47_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 4: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=1.0, k_pos=10, temp=0.1). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=1.0, k_pos=10, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=1.0,
            k_pos=10, temperature=0.1,
        ),
    },
    "s47_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+diversity-w1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 5: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus codebook diversity on cell-VQ (wt=1.0, temp=100.0). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook-diversity(branch=cell, wt=1.0, temperature=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            weight=1.0, temperature=100.0, branch="cell",
        ),
    },
    "s47_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+diversity-w10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 6: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus codebook diversity on cell-VQ (wt=10.0, temp=100.0). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+codebook-diversity(branch=cell, wt=10.0, temperature=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s47_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+diversity-w1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 7: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=1.0, k_pos=5, temp=0.1) + codebook diversity on cell-VQ (wt=1.0, temp=100.0). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=1.0, k_pos=5, temperature=0.1)",
            "+codebook-diversity(branch=cell, wt=1.0, temperature=100.0)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_codebook_diversity(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            weight=1.0, temperature=100.0, branch="cell",
        ),
            wt_contrastive_cell=1.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s47_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+diversity-w10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s47 sweep, variant 8: s42 winner spine with cell-w lowered 5.0 -> 1.0 (per user: weight escalation didn't help), plus contrastive cell loss (wt=1.0, k_pos=5, temp=0.1) + codebook diversity on cell-VQ (wt=10.0, temp=100.0). \n\nContrastive (when active): NT-Xent on `quantizer_input_cell` (= z_mlp_cell of seed cells). Positive pairs are the top-k cells with the most similar log1p gene-expression profile in the batch; negatives are the rest. Pulls same-type cells together in z_mlp space and pushes different-type apart — a between-cell objective that complements NB's within-cell objective. \n\nDiversity (when active): lucidrains' codebook-diversity loss on every level of the cell-branch RVQ (L1 K=30 + L2 K=90). Penalises softmax-similarity between codes -> pushes codes apart on the cosine-sphere -> reduces dead-code collapse and keeps cluster centroids well-separated by construction. \n\nHypothesis: contrastive directly attacks the NB/NMI objective mismatch (per the s46 weight-escalation plateau result). Diversity attacks the K1=30 'codes collapse together' failure mode independently. Combined (v7, v8): both at once. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell(wt=1.0, k_pos=5, temperature=0.1)",
            "+codebook-diversity(branch=cell, wt=10.0, temperature=100.0)",
        ],
        "build": lambda: _patch_dual_contrastive_cell(
            _patch_dual_codebook_diversity(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            wt_contrastive_cell=1.0,
            k_pos=5, temperature=0.1,
        ),
    },
    # -----------------------------------------------------------------------
    # s48 sweep — 8 ablations: within-batch contrastive cell loss
    # -----------------------------------------------------------------------
    # Same architecture as s42 winner (cell-w=1.0). Adds the WITHIN-BATCH
    # variant of the contrastive cell loss (restrict positives + NT-Xent
    # denominator to same-section cells). Variants 1-6 weight-sweep over
    # 1, 10, 30, 100, 300, 1000. v7 doubles k_pos at wt=100. v8 stacks
    # codebook diversity at wt=1 on top of wt=100 contrastive.
    #
    # Numerical correctness of the within-batch loss validated by 5-test
    # suite (see contrastive_cell_attribute.py docstring).
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v48
    "s48_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w1-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 1: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=1.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=1.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 2: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=10.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=10.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w30-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 3: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=30.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=30.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=30.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 4: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=100.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=100.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=100.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w300-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 5: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=300.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=300.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=300.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 6: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=1000.0, k_pos=5, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=1000.0,
            k_pos=5, temperature=0.1,
        ),
    },
    "s48_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 7: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=100.0, k_pos=10, temp=0.1). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=100.0, k_pos=10, temperature=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            wt_contrastive_cell=100.0,
            k_pos=10, temperature=0.1,
        ),
    },
    "s48_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k5+diversity-w1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s48 sweep, variant 8: s42 winner spine with cell-w=1.0 + WITHIN-BATCH contrastive (wt=100.0, k_pos=5, temp=0.1) + cell-VQ codebook diversity (wt=1.0, temp=100.0). \n\nWITHIN-BATCH variant of the contrastive cell loss: BOTH the positive-pair candidate set AND the NT-Xent denominator are restricted to cells sharing the anchor's adata_batch_id. This mirrors `adj_within_section_only=True` on the cosine adjacency BCE — without the restriction, cross-section pairs leak into the gradient and push biologically-similar cells from different MERFISH/STARmap sections apart (counter to the batch-integration objective). With the restriction, cross-section pairs are left unpressured. \n\nSweep design: variants 1-6 weight-sweep at k_pos=5 over four orders of magnitude (1, 10, 30, 100, 300, 1000). The s47 global-variant range topped out at 2.0; the within-batch version has roughly halved per-anchor mass (denominator over ~256 instead of ~512 cells) so higher weights are needed to reach comparable per-cell gradient mass. v7 doubles k_pos at the centre weight; v8 stacks codebook diversity at wt=1 on top of the same centre weight. \n\nNumerical correctness validated by a 5-test suite (finite/non-negative loss, combined == per-batch average, singleton skipped, zero cross-batch gradient flow, hand-computed tiny case). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=100.0, k_pos=5, temperature=0.1)",
            "+codebook-diversity(branch=cell, wt=1.0, temperature=100.0)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_codebook_diversity(
            _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                    _patch_dual_batch_lr(
                        _patch_dual_attr_recon_weight(
                            _patch_dual_sampler_neighbors(
                                _patch_dual_graph_knn(
                                    _patch_dual_decoder_width(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(_BD(),
                                                        branch="niche", codebook_sizes=(30, 90)),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                        hidden_channels=[32],
                                    ),
                                    n_neighs=16,
                                ),
                                num_neighbors=[16],
                            ),
                            weight=1.0,
                        ),
                        batch_size=512, lr=7e-4,
                    ),
                ),
                enabled=True,
            ),
            weight=1.0, temperature=100.0, branch="cell",
        ),
            wt_contrastive_cell=100.0,
            k_pos=5, temperature=0.1,
        ),
    },
    # -----------------------------------------------------------------------
    # s49 sweep — 24 ablations on the s48_v2 spine (contrastWB-w10-k5)
    # -----------------------------------------------------------------------
    # Spine = s42 winner + cell-w=1.0 + within-batch contrastive cell loss
    # (wt=10, k=5). Axes explored:
    #   v1     cell-w back to 5
    #   v2-v5  encoder sizes (smaller, single-layer, decoupled one-layer)
    #   v6-v8  codebook diversity at higher weights (10, 100, 300)
    #   v9-v12 L2 codebook sweep (L1 fixed at 30; L2 in {30, 60, 200, 500})
    #   v13-v14 decoupled batch embedding
    #   v15-v18 epoch sweep at contrastive=1000 (ep 120, 160, 240, 320)
    #   v19-v20 decoupled-enc + contrastive {200, 1000}
    #   v21-v22 Y-shape (shared-specific-enc) + contrastive {200, 1000}
    #   v23-v24 synergistic combinations
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v49
    "s49_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5+bs512+lr7e-4+within-sec+cell-w5+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 1: cell-w bumped 1.0 -> 5.0 on top of s48_v2 spine. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=5.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=5.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-256-128+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 2: smaller encoder trunk [256, 128] (was [400, 400, 256]). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[256, 128],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-128+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 3: single-layer encoder trunk [128] -- tight latent. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[128],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-256+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 4: single-layer encoder trunk [256] -- latent 256. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc-256+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 5: DECOUPLED encoders, one-layer [256] MLP per branch -- user's specific request. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_decoupled_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 6: cell-VQ codebook diversity loss at wt=10 (alongside contrastive). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10.0, T=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s49_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 7: cell-VQ codebook diversity loss at wt=100. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100.0, T=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s49_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w300+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 8: cell-VQ codebook diversity loss at wt=300 (very high). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=300.0, T=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
            weight=300.0, temperature=100.0, branch="cell",
        ),
    },
    "s49_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-30+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 9: cell RVQ L2 shrunk 90 -> 30. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[30, 30], niche=[30,90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 30),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-60+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 10: cell RVQ L2 shrunk 90 -> 60. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[30, 60], niche=[30,90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 60),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-200+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 11: cell RVQ L2 grown 90 -> 200. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[30, 200], niche=[30,90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 200),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-500+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 12: cell RVQ L2 grown 90 -> 500 (very large composite codebook). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[30, 500], niche=[30,90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 500),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-dec-cov+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 13: split decoder batch_embedding into independent per-decoder modules. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
        ),
    },
    "s49_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-dec-cov+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 14: decoupled decoder batch_embedding + diversity-w10 combination. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-dec-cov(separate nn.Embedding per decoder)",
            "+codebook-diversity(branch=cell, wt=10.0, T=100.0)",
        ],
        "build": lambda: _patch_dual_decoupled_decoder_cov(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
        ),
    },
    "s49_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep120+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 15: contrastive wt=1000 (vs spine 10), epochs 80 -> 120. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+max_epochs=120",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
            max_epochs=120,
        ),
    },
    "s49_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep160+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 16: contrastive wt=1000, epochs 80 -> 160. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+max_epochs=160",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
            max_epochs=160,
        ),
    },
    "s49_v17_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep240+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 17: contrastive wt=1000, epochs 80 -> 240. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+max_epochs=240",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
            max_epochs=240,
        ),
    },
    "s49_v18_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep320+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 18: contrastive wt=1000, epochs 80 -> 320 (4x default). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+max_epochs=320",
        ],
        "build": lambda: _patch_dual_max_epochs(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
            max_epochs=320,
        ),
    },
    "s49_v19_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 19: +decoupled-enc + contrastive wt=200. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=200.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_decoupled_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=200.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v20_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 20: +decoupled-enc + contrastive wt=1000. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_decoupled_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v21_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+shared-specific-enc+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 21: +shared-specific-enc (Y-shape) + contrastive wt=200. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=200.0, k_pos=5, T=0.1)",
            "+shared-specific-enc(Y-shape, default halved-dim)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_shared_specific_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=200.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v22_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+shared-specific-enc+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 22: +shared-specific-enc (Y-shape) + contrastive wt=1000. Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=1000.0, k_pos=5, T=0.1)",
            "+shared-specific-enc(Y-shape, default halved-dim)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_shared_specific_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=1000.0, k_pos=5, temperature=0.1,
        ),
    },
    "s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 23: decoupled-enc + diversity-w10 (architectural isolation + code repulsion). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
            "+codebook-diversity(branch=cell, wt=10.0, T=100.0)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_decoupled_encoders(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 90),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
        ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-20b_1p": {
        "description": (
            "s49_v23 ported to mmb0-1b_smb1-20b_1p (1 MERFISH + 20 STARmap+ = 21 sections, cross-platform retrieval). IDENTICAL model/loss config to the mmb0-1b_smb1-1b_1p s49_v23 benchmarking variant (decoupled-enc + within-batch contrastive wt=10/k=5 + cell-VQ codebook-diversity wt=10 on the s48_v2 spine: cell-w=1, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec; cell RVQ = (30,90), niche RVQ = (30,90); gnn=1, nbr_hops=1, edge_sampling_ratio=2.0). Dataset swapped to mmb20 via _patch_dual_mmb20 with the canonical holdout test=[15 (STARmap within-platform), 82 (MERFISH cross-platform OOD)]; train = the other 19 STARmap+ sections; val = 10% in-section cell split. bs kept at 512 (faithful; preserves the within-batch contrastive negative pool) — MEMORY-HEAVY at 19 training sections, needs a large-memory GPU. REQUIRES the harmonized silver dir (harmonize_mmb_smb20_panels.py) and a BLOB REBUILD with k=16 in n_neighs_list."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
            "+codebook-diversity(branch=cell, wt=10.0, T=100.0)",
            "+mmb20(test=[15,82], train=other-19, bs512, sampler[16], gnn1, nbr1, edge_sampling=2.0)",
        ],
        "build": lambda: _patch_dual_mmb20(
    _patch_dual_codebook_diversity(
                _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                _patch_dual_attr_recon_weight(
                _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                _patch_dual_decoupled_encoders(
                _patch_dual_encoder_deeper(
                _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                _patch_dual_rvq(
                _BD(),
                branch="niche", codebook_sizes=(30, 90),
            ),
                branch="cell", codebook_sizes=(30, 90),
            ),
            ),
                hidden_channels=[400, 400, 256],
            ),
            ),
                hidden_channels=[32],
            ),
                n_neighs=16,
            ),
                num_neighbors=[16],
            ),
                weight=1.0,
            ),
                batch_size=512, lr=7e-4,
            ),
            ),
                enabled=True,
            ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
                weight=10.0, temperature=100.0, branch="cell",
            ),
            test_adata_ids=[15, 82],
            batch_size=512,
            edge_sampling_ratio=2.0,
            gnn_layers=1,
            sampler_neighbors=[16],
            nbr_aggregation_hops=1,
        ),
    },
    "s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-239b_1p": {
        "description": (
            "s49_v23 ported to mmb0-239b_1p (239 MERFISH mouse-brain sections, single shared gene panel — NO harmonization needed, unlike mmb20). IDENTICAL model/loss config to the mmb0-1b_smb1-1b_1p s49_v23 benchmarking variant (decoupled-enc + within-batch contrastive wt=10/k=5 + cell-VQ codebook-diversity wt=10 on the s48_v2 spine: cell-w=1, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec; cell RVQ = (30,90), niche RVQ = (30,90); gnn=1, nbr_hops=1, edge_sampling_ratio=2.0). Dataset swapped via _patch_dual_mmb239: TRAIN ON ALL 239 sections (test_adata_ids omitted), val = 10% in-section cell split — no whole-section test holdout (intended for producing SQUINT codes across all sections for the downstream tissue-scale JEPA). bs kept at 512 (faithful to s49_v23). !! SCALE WARNING: 239 sections is ~12x mmb20 (already memory-heavy at 19 sections); bs=512 over a 239-section resident graph WILL be very memory-heavy and likely OOMs except on the largest GPUs — if so, lower batch_size (256/128/64) then edge_sampling_ratio then sampler. REQUIRES a BLOB BUILD: --build-blob --build-blob-dataset mmb0-239b_1p (k=[8,16] pre-populated)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10.0, k_pos=5, T=0.1)",
            "+decoupled-enc(independent niche MLP)",
            "+codebook-diversity(branch=cell, wt=10.0, T=100.0)",
            "+mmb239(train=ALL-239, test=none, bs512, sampler[16], gnn1, nbr1, edge_sampling=2.0)",
        ],
        "build": lambda: _patch_dual_mmb239(
    _patch_dual_codebook_diversity(
                _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                _patch_dual_attr_recon_weight(
                _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                _patch_dual_decoder_width(
                _patch_dual_decoupled_encoders(
                _patch_dual_encoder_deeper(
                _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                _patch_dual_rvq(
                _BD(),
                branch="niche", codebook_sizes=(30, 90),
            ),
                branch="cell", codebook_sizes=(30, 90),
            ),
            ),
                hidden_channels=[400, 400, 256],
            ),
            ),
                hidden_channels=[32],
            ),
                n_neighs=16,
            ),
                num_neighbors=[16],
            ),
                weight=1.0,
            ),
                batch_size=512, lr=7e-4,
            ),
            ),
                enabled=True,
            ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
                weight=10.0, temperature=100.0, branch="cell",
            ),
            batch_size=512,
            edge_sampling_ratio=2.0,
            gnn_layers=1,
            sampler_neighbors=[16],
            nbr_aggregation_hops=1,
        ),
    },
    "s49_v24_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-200+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49 sweep, variant 24: L2=200 codebook + contrastive wt=200 (size + stronger contrastive combo). Built on the s48_v2 spine (s42 winner + cell-w=1.0 + within-batch contrastive cell loss at wt=10, k=5, T=0.1), which already lifts cell-NMI without hurting niche metrics. This variant changes only the axes called out in the summary; all other knobs match the spine. REQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(cell=[30, 200], niche=[30,90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=200.0, k_pos=5, T=0.1)",
        ],
        "build": lambda: _patch_dual_contrastive_cell_within_batch(
            _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
            _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
            _patch_dual_graph_knn(
            _patch_dual_decoder_width(
            _patch_dual_encoder_deeper(
            _patch_dual_decoder_covariate(
            _patch_dual_rvq(
            _patch_dual_rvq(
            _BD(),
            branch="niche", codebook_sizes=(30, 90),
        ),
            branch="cell", codebook_sizes=(30, 200),
        ),
        ),
            hidden_channels=[400, 400, 256],
        ),
            hidden_channels=[32],
        ),
            n_neighs=16,
        ),
            num_neighbors=[16],
        ),
            weight=1.0,
        ),
            batch_size=512, lr=7e-4,
        ),
        ),
            enabled=True,
        ),
            wt_contrastive_cell=200.0, k_pos=5, temperature=0.1,
        ),
    },
    # -----------------------------------------------------------------------
    # s50 sweep — 16 ablations: shared-trunk encoder size sweep on s49_v7 spine
    # -----------------------------------------------------------------------
    # Spine = s49_v7 (s48_v2 + codebook-diversity on cell-VQ wt=100) -- the
    # variant whose integration metrics held up while cell-NMI lifted. The
    # ONLY axis varied is the encoder MLP shape (no Y-shape, no decoupling).
    #
    # NB: the cell-VQ codebook embedding dim = last encoder layer dim, so
    # variants ending in 256 isolate the trunk-shape effect; variants ending
    # in other dims also vary the codebook embedding space.
    #
    # Variant index order: 1-layer (v1-v4), 2-layer (v5-v9),
    # 3-layer (v10-v13), 4-layer + tweaks (v14-v16).
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v50
    "s50_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 1: shared-trunk encoder shape [64]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[64] -> z_mlp[64] split to VQ_cell (RVQ [30, 90], dim=64) + GNN -> VQ_niche. Encoder MLP params ~28k (vs s49_v7's ~436k, 15.8x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (64 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[64])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[64],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 2: shared-trunk encoder shape [128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~55k (vs s49_v7's ~436k, 7.9x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 3: shared-trunk encoder shape [256]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256] -> z_mlp[256] split to VQ_cell (RVQ [30, 90], dim=256) + GNN -> VQ_niche. Encoder MLP params ~111k (vs s49_v7's ~436k, 3.9x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (256 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-512+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 4: shared-trunk encoder shape [512]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[512] -> z_mlp[512] split to VQ_cell (RVQ [30, 90], dim=512) + GNN -> VQ_niche. Encoder MLP params ~221k (vs s49_v7's ~436k, 2.0x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (512 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[512])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[512],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-64-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 5: shared-trunk encoder shape [64, 64]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[64, 64] -> z_mlp[64] split to VQ_cell (RVQ [30, 90], dim=64) + GNN -> VQ_niche. Encoder MLP params ~32k (vs s49_v7's ~436k, 13.7x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (64 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[64, 64])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[64, 64],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 6: shared-trunk encoder shape [128, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[128, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~72k (vs s49_v7's ~436k, 6.1x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 7: shared-trunk encoder shape [256, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~143k (vs s49_v7's ~436k, 3.0x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 8: shared-trunk encoder shape [256, 256]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256, 256] -> z_mlp[256] split to VQ_cell (RVQ [30, 90], dim=256) + GNN -> VQ_niche. Encoder MLP params ~176k (vs s49_v7's ~436k, 2.5x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (256 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 9: shared-trunk encoder shape [400, 256]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[400, 256] -> z_mlp[256] split to VQ_cell (RVQ [30, 90], dim=256) + GNN -> VQ_niche. Encoder MLP params ~275k (vs s49_v7's ~436k, 1.6x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (256 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 10: shared-trunk encoder shape [128, 128, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[128, 128, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~88k (vs s49_v7's ~436k, 4.9x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128, 128, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[128, 128, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-128-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 11: shared-trunk encoder shape [256, 128, 64]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256, 128, 64] -> z_mlp[64] split to VQ_cell (RVQ [30, 90], dim=64) + GNN -> VQ_niche. Encoder MLP params ~152k (vs s49_v7's ~436k, 2.9x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (64 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128, 64])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 128, 64],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 12: shared-trunk encoder shape [256, 256, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256, 256, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~209k (vs s49_v7's ~436k, 2.1x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 256, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 13: shared-trunk encoder shape [400, 256, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[400, 256, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~308k (vs s49_v7's ~436k, 1.4x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 14: shared-trunk encoder shape [256, 256, 256, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[256, 256, 256, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~275k (vs s49_v7's ~436k, 1.6x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 256, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[256, 256, 256, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 15: shared-trunk encoder shape [400, 256, 128, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[400, 256, 128, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~325k (vs s49_v7's ~436k, 1.3x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256, 128, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 256, 128, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 16: shared-trunk encoder shape [400, 400, 128]. Built on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100) -- a higher-diversity variant from s49. The ONLY change from s49_v7 is the encoder MLP shape. \n\nArchitecture (single shared MLP, NO Y-shape, NO decoupling): x[431] -> MLP[400, 400, 128] -> z_mlp[128] split to VQ_cell (RVQ [30, 90], dim=128) + GNN -> VQ_niche. Encoder MLP params ~385k (vs s49_v7's ~436k, 1.1x smaller). \n\nNote: the cell-VQ codebook embedding dim equals the last encoder layer dim (128 here vs 256 in s49_v7). Variants ending in 256 isolate the trunk-shape effect; variants ending in other dims also shrink/grow the codebook embedding space. \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 128])",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_encoder_deeper(
                                                _patch_dual_decoder_covariate(
                                                    _patch_dual_rvq(
                                                        _patch_dual_rvq(_BD(),
                                                            branch="niche", codebook_sizes=(30, 90)),
                                                        branch="cell", codebook_sizes=(30, 90),
                                                    ),
                                                ),
                                                hidden_channels=[400, 400, 128],
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    # -----------------------------------------------------------------------
    # s50 sweep — 8 DECOUPLED-encoder variants (v17-v24) appended to v1-v16
    # -----------------------------------------------------------------------
    # Same spine as v1-v16, with +decoupled-encoders added. Curated subset
    # of shapes since decoupled-enc 2x's the encoder MLP param count.
    "s50_v17_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 17: DECOUPLED encoders, shape [128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~55k per trunk x 2 = ~111k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v18_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 18: DECOUPLED encoders, shape [256] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[256], codebook dim = 256 (last MLP layer dim). Encoder MLP params ~111k per trunk x 2 = ~221k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v19_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 19: DECOUPLED encoders, shape [128, 128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[128, 128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~72k per trunk x 2 = ~144k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128, 128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[128, 128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v20_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 20: DECOUPLED encoders, shape [256, 128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[256, 128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~143k per trunk x 2 = ~287k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[256, 128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v21_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 21: DECOUPLED encoders, shape [256, 256] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[256, 256], codebook dim = 256 (last MLP layer dim). Encoder MLP params ~176k per trunk x 2 = ~353k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[256, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v22_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 22: DECOUPLED encoders, shape [128, 128, 128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[128, 128, 128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~88k per trunk x 2 = ~177k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[128, 128, 128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[128, 128, 128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v23_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 23: DECOUPLED encoders, shape [256, 256, 128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[256, 256, 128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~209k per trunk x 2 = ~419k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[256, 256, 128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[256, 256, 128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    "s50_v24_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-400-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s50 sweep, variant 24: DECOUPLED encoders, shape [400, 256, 128] per branch. Cell trunk + niche trunk are two INDEPENDENT MLP modules with the same shape but disjoint weights — spatial gradients (nbr-NB, niche-commit, adjacency-BCE) flow only through the niche MLP, never reaching the cell MLP. \n\nBuilt on the s49_v7 spine (s48_v2 + codebook-diversity on cell-VQ at wt=100, T=100), so all loss weights and other hyperparameters match the single-trunk s50 variants 1-16 — the ONLY difference is the +decoupled-enc structural change + this variant's specific shape. \n\nArchitecture: 2 x MLP[400, 256, 128], codebook dim = 128 (last MLP layer dim). Encoder MLP params ~308k per trunk x 2 = ~617k total (vs s49_v7's single-trunk 436k baseline). \n\nREQUIRES BLOB REBUILD (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 256, 128])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=100, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 256, 128],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=100.0, temperature=100.0, branch="cell",
        ),
    },
    # -----------------------------------------------------------------------
    # s49_v23 ports — region-holdout (mmb) + chl59-8b_1p + spatch_1p
    # -----------------------------------------------------------------------
    # Three standalone variants that take the s49_v23 multiseed default
    # (decoupled-enc + diversity-w10 + contrastWB-w10-k5 on s48_v2 spine)
    # and apply ONE extra outer wrapper each:
    #   - +region-holdout  -> +_patch_holdout_regions (mmb dataset)
    #   - +chl59-8b_1p     -> +_patch_dual_chl59_lung5(test=[2,3])
    #   - +spatch_1p       -> +_patch_dual_spatch(train=[2], test='rest')
    #
    # Used as comparators for the s49_v23 multiseed default in:
    #   - plot_pearson_benchmark (region-holdout variant)
    #   - plot_*_benchmark cross-dataset comparisons
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Region-holdout port of the s49_v23 multiseed default variant. Spine: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Adds 25%x25% spatial holdout patches (batch15 upper-left + batch82 lower-right) matching the existing region-holdout reference. Held-out cells get the test data-split tag so post-inference Pearson reports only on unseen cells. Default max_epochs=80."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w128-256+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Region-holdout port of s49_v23, with WIDER decoder: MLP [128, 256] (2-layer, growing) instead of [32] (1-layer, narrow). All other knobs identical to the standard region-holdout port: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], knn16, sampler[16], bs=512, lr=7e-4, within-sec) + 25%x25% spatial holdout patches (batch15 upper-left + batch82 lower-right). The wider decoder gives the NB-rate head ~14x more capacity (272 -> 32 -> 431 = 22k -> 272 -> 128 -> 256 -> 431 = 174k params per decoder), useful for measuring how much per-cell reconstruction quality is decoder-capacity-bound on this dataset. Default max_epochs=80."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[128, 256]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_codebook_diversity(
                _patch_dual_contrastive_cell_within_batch(
                    _patch_dual_adj_within_section_only(
                        _patch_dual_no_batch_int(
                            _patch_dual_batch_lr(
                                _patch_dual_attr_recon_weight(
                                    _patch_dual_sampler_neighbors(
                                        _patch_dual_graph_knn(
                                            _patch_dual_decoder_width(
                                                _patch_dual_decoupled_encoders(
                                                    _patch_dual_encoder_deeper(
                                                        _patch_dual_decoder_covariate(
                                                            _patch_dual_rvq(
                                                                _patch_dual_rvq(_BD(),
                                                                    branch="niche", codebook_sizes=(30, 90)),
                                                                branch="cell", codebook_sizes=(30, 90),
                                                            ),
                                                        ),
                                                        hidden_channels=[400, 400, 256],
                                                    ),
                                                ),
                                                hidden_channels=[128, 256],
                                            ),
                                            n_neighs=16,
                                        ),
                                        num_neighbors=[16],
                                    ),
                                    weight=1.0,
                                ),
                                batch_size=512, lr=7e-4,
                            ),
                        ),
                        enabled=True,
                    ),
                    wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
                ),
                weight=10.0, temperature=100.0, branch="cell",
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w400-400+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Region-holdout port of s49_v23 with MIRROR-REVERSE decoder: decoder MLP [400, 400] mirrors the encoder MLP [400, 400, 256] (symmetric autoencoder, decoder reads cell_dim + 16-dim batch covariate = 272 dim, expands through 400 -> 400 -> 431 genes). Spine matches the +dec-w32 and +dec-w128-256 region-holdout ports (decoupled-enc + within-batch contrastive wt=10 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + knn16 + sampler16 + bs=512 + lr=7e-4 + within-sec). The 25%x25% holdout patches (batch15 upper-left + batch82 lower-right) match the existing region-holdout reference. Default max_epochs=80.\n\nDecoder capacity progression across the 3 sibling variants: [32] = ~22k params/branch, [128, 256] = ~174k, [400, 400] = ~333k. Symmetric autoencoder is the natural design choice; the smaller siblings test whether SQUINT needs the full decoder capacity or if the NB-rate head can be cheaper."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[400, 400]  (mirror of encoder [400, 400, 256] in reverse)",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_codebook_diversity(
                _patch_dual_contrastive_cell_within_batch(
                    _patch_dual_adj_within_section_only(
                        _patch_dual_no_batch_int(
                            _patch_dual_batch_lr(
                                _patch_dual_attr_recon_weight(
                                    _patch_dual_sampler_neighbors(
                                        _patch_dual_graph_knn(
                                            _patch_dual_decoder_width(
                                                _patch_dual_decoupled_encoders(
                                                    _patch_dual_encoder_deeper(
                                                        _patch_dual_decoder_covariate(
                                                            _patch_dual_rvq(
                                                                _patch_dual_rvq(_BD(),
                                                                    branch="niche", codebook_sizes=(30, 90)),
                                                                branch="cell", codebook_sizes=(30, 90),
                                                            ),
                                                        ),
                                                        hidden_channels=[400, 400, 256],
                                                    ),
                                                ),
                                                hidden_channels=[400, 400],
                                            ),
                                            n_neighs=16,
                                        ),
                                        num_neighbors=[16],
                                    ),
                                    weight=1.0,
                                ),
                                batch_size=512, lr=7e-4,
                            ),
                        ),
                        enabled=True,
                    ),
                    wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
                ),
                weight=10.0, temperature=100.0, branch="cell",
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w400-400+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+region-holdout+160ep+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Long-train sibling of the mirror-decoder region-holdout variant (`+dec-w400-400+region-holdout`). Only delta: max_epochs=160 (vs base config's 80). Early stopping still ENABLED (patience=10 val passes on val_loss). \n\nThe wider decoder ([400, 400] = ~333k params per branch) has more parameters to fit; the longer training budget lets the NB-rate head converge fully before val_loss-based early stopping kicks in. Pair with SQUINT_WITH_PEARSON=1 if you want a per-epoch Pearson sanity monitor alongside val_loss.\n\nCAVEAT: max_epochs=160 historically regressed niche NMI on the +wide spine when val_loss drifted past the niche-NMI peak. Worth checking the per-epoch Pearson + NMI traces in wandb after the run finishes."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[400, 400]  (mirror of encoder [400, 400, 256] in reverse)",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+region_holdout(test_regions={15: upper-left 25%x25%, 82: lower-right 25%x25%})",
            "+max_epochs=160  (default 80; early stop still ON)",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_max_epochs(
                _patch_dual_codebook_diversity(
                    _patch_dual_contrastive_cell_within_batch(
                        _patch_dual_adj_within_section_only(
                            _patch_dual_no_batch_int(
                                _patch_dual_batch_lr(
                                    _patch_dual_attr_recon_weight(
                                        _patch_dual_sampler_neighbors(
                                            _patch_dual_graph_knn(
                                                _patch_dual_decoder_width(
                                                    _patch_dual_decoupled_encoders(
                                                        _patch_dual_encoder_deeper(
                                                            _patch_dual_decoder_covariate(
                                                                _patch_dual_rvq(
                                                                    _patch_dual_rvq(_BD(),
                                                                        branch="niche", codebook_sizes=(30, 90)),
                                                                    branch="cell", codebook_sizes=(30, 90),
                                                                ),
                                                            ),
                                                            hidden_channels=[400, 400, 256],
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400],
                                                ),
                                                n_neighs=16,
                                            ),
                                            num_neighbors=[16],
                                        ),
                                        weight=1.0,
                                    ),
                                    batch_size=512, lr=7e-4,
                                ),
                            ),
                            enabled=True,
                        ),
                        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
                    ),
                    weight=10.0, temperature=100.0, branch="cell",
                ),
                max_epochs=160,
            ),
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Port of the s49_v23 multiseed default variant to chl59-8b_1p (CosMx Lung, 8 sections). Spine: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. REQUIRES BLOB REBUILD for chl59-8b_1p (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    # -----------------------------------------------------------------------
    # chl59-8b_1p — 5 smaller-codebook variants of the s49_v23 spine
    # -----------------------------------------------------------------------
    # Stand-alone single-seed variants (not registered in a DATASET_VARIANTS
    # sweep). Same spine as the existing
    # `+contrastWB-w10-k5+chl59-8b_1p` variant; ONLY rvq codebook sizes
    # (cell L1 30->10, niche L1 30->12, L2 swept), n_neighs and sampler
    # num_neighbors vary. Submit individually via
    # `python examples/run_squint.py --all --variant <name>`.
    "dualvq+rvq-c10-10+rvq-n12-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p s49_v23 spine variant with SMALLER codebooks (cell L1=10, niche L1=12) and varying L2: cell=(10, 10), niche=(12, 10). Graph: knn=16, sampler=16. All other spine knobs unchanged: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission to probe codebook-capacity sensitivity on the CosMx Lung gene panel."
        ),
        "patches": [
            "+rvq(branch=niche, levels=[12, 10])",
            "+rvq(branch=cell, levels=[10, 10])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(12, 10)),
                                                            branch="cell", codebook_sizes=(10, 10),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-c10-30+rvq-n12-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p s49_v23 spine variant with SMALLER codebooks (cell L1=10, niche L1=12) and varying L2: cell=(10, 30), niche=(12, 30). Graph: knn=16, sampler=16. All other spine knobs unchanged: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission to probe codebook-capacity sensitivity on the CosMx Lung gene panel."
        ),
        "patches": [
            "+rvq(branch=niche, levels=[12, 30])",
            "+rvq(branch=cell, levels=[10, 30])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(12, 30)),
                                                            branch="cell", codebook_sizes=(10, 30),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-c10-90+rvq-n12-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p s49_v23 spine variant with SMALLER codebooks (cell L1=10, niche L1=12) and varying L2: cell=(10, 90), niche=(12, 90). Graph: knn=16, sampler=16. All other spine knobs unchanged: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission to probe codebook-capacity sensitivity on the CosMx Lung gene panel."
        ),
        "patches": [
            "+rvq(branch=niche, levels=[12, 90])",
            "+rvq(branch=cell, levels=[10, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(12, 90)),
                                                            branch="cell", codebook_sizes=(10, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-c10-30+rvq-n12-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p s49_v23 spine variant with SMALLER codebooks (cell L1=10, niche L1=12) and varying L2: cell=(10, 30), niche=(12, 30). Graph: knn=8, sampler=8. All other spine knobs unchanged: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission to probe codebook-capacity sensitivity on the CosMx Lung gene panel."
        ),
        "patches": [
            "+rvq(branch=niche, levels=[12, 30])",
            "+rvq(branch=cell, levels=[10, 30])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(12, 30)),
                                                            branch="cell", codebook_sizes=(10, 30),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=8,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-c10-90+rvq-n12-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-8b_1p": {
        "description": (
            "Chl59-8b_1p s49_v23 spine variant with SMALLER codebooks (cell L1=10, niche L1=12) and varying L2: cell=(10, 90), niche=(12, 90). Graph: knn=8, sampler=8. All other spine knobs unchanged: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Train: chl59 adata_batch_id 0+1+4+5+6+7. Test: 2+3 (held-out replicates). Default max_epochs=80. Designed for single-run submission to probe codebook-capacity sensitivity on the CosMx Lung gene panel."
        ),
        "patches": [
            "+rvq(branch=niche, levels=[12, 90])",
            "+rvq(branch=cell, levels=[10, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-8b_1p(test=[2, 3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(12, 90)),
                                                            branch="cell", codebook_sizes=(10, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=8,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            test_batch_idx=[2, 3], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_1p": {
        "description": (
            "Port of the s49_v23 multiseed default variant to spatch_1p. Spine: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Train: spatch_1p adata_batch_id=2 only. Test: every other section. Default max_epochs=80. REQUIRES BLOB REBUILD for spatch_1p (k=16 in n_neighs_list)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_1p(test=rest)",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            train_batch_idx=[2], test_batch_idx="rest", batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_1p-trainall": {
        "description": (
            "s49_v23 spine on spatch_1p, TRAINING ON ALL 6 SECTIONS (the combined spatch dataset: dataset_id_1..6 = ov + hcc + coad). IDENTICAL config to the section-2-holdout '+spatch_1p' variant above, but trains on every section (train_batch_idx=[], test_batch_idx=[]) with a 10% cell-level val split and NO whole-section test holdout — i.e. use all the data to produce SQUINT codes across the whole spatch corpus. Spine: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec; cell RVQ=(30,90), niche RVQ=(30,90)). REQUIRES the spatch_1p blob built with k=16 in n_neighs_list (--build-blob-dataset spatch_1p)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders(niche trunk = independent deep copy)",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_1p(train=ALL-6, test=none)",
        ],
        "build": lambda: _patch_dual_spatch(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            train_batch_idx=[], test_batch_idx=[], batch_size=512,
        ),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+squint_hln": {
        "description": (
            "s49_v23 spine on squint_hln (CosMx human lymph node, manual niches from spatial-niche-benchmark). DEFAULT codebooks: cell RVQ = (30, 90), niche RVQ = (30, 90) (30 L0 codes per branch, 2700 total). Spine: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine (cell-w=1, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Trains on all sections in the blob (this dataset is a SINGLE CosMx section), 10% cell-level val. Labels read via the squint_hln blob's label_names (cell_type_annotation / niche_annotation). REQUIRES the squint_hln blob (--build-blob-dataset squint_hln)."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])", "+decoupled-encoders",
            "+dec-w=[32]", "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=1.0", "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+squint_hln(dataset switch, train=ALL)",
        ],
        "build": lambda: _patch_dual_squint_hln(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "dualvq+rvq-cell-16-169+rvq-niche-4-675+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+squint_hln": {
        "description": (
            "s49_v23 spine on squint_hln with GROUND-TRUTH L0 codebook sizes at ~constant total capacity. cell RVQ = (16, 169) [L0=16 cell codes; 16*169=2704 ~ 2700], niche RVQ = (4, 675) [L0=4 = #manual niches (B cell Zone, T cell Zone, Medulla, Germinal Center); 4*675=2700]. Everything else IDENTICAL to the squint_hln s49_v23 spine above; trains on all sections (single CosMx section), 10% cell-level val. REQUIRES the squint_hln blob."
        ),
        "patches": [
            "+rvq(cell levels=[16, 169], niche levels=[4, 675])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])", "+decoupled-encoders",
            "+dec-w=[32]", "+graph_knn(n_neighs=16)", "+sampler([16])",
            "+wt_attr_reconstr=1.0", "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+squint_hln(dataset switch, train=ALL)",
        ],
        "build": lambda: _patch_dual_squint_hln(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(4, 675),
                                                    ),
                                                    branch="cell", codebook_sizes=(16, 169),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+squint_hln": {
        "description": (
            "squint_hln (CosMx human lymph node) s49_v23 spine with knn=8, sampler=[8] (vs the knn16 default) and DEFAULT codebooks (cell RVQ=(30,90), niche RVQ=(30,90); 30 L0/branch). max_epochs=400 (via _patch_dual_squint_hln). Trains on all sections (single CosMx section), 10% cell-level val. Uses the k=8 spatial graph already in the squint_hln blob (n_neighs_list=[8,16,24]). REQUIRES the squint_hln blob."
        ),
        "patches": [
            "+rvq(branch=both, levels=[30, 90])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])", "+decoupled-encoders",
            "+dec-w=[32]", "+graph_knn(n_neighs=8)", "+sampler([8])",
            "+wt_attr_reconstr=1.0", "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+squint_hln(dataset switch, train=ALL, max_epochs=400)",
        ],
        "build": lambda: _patch_dual_squint_hln(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=8,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "dualvq+rvq-cell-16-169+rvq-niche-4-675+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+squint_hln": {
        "description": (
            "squint_hln (CosMx human lymph node) s49_v23 spine with knn=8, sampler=[8] and GROUND-TRUTH L0 codebooks: cell RVQ=(16,169) [16*169=2704], niche RVQ=(4,675) [L0=4 manual niches; 4*675=2700]. max_epochs=400. Trains on all sections (single CosMx section), 10% cell-level val. Uses the k=8 graph in the squint_hln blob. REQUIRES the squint_hln blob."
        ),
        "patches": [
            "+rvq(cell levels=[16, 169], niche levels=[4, 675])",
            "+decoder_covariate", "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])", "+decoupled-encoders",
            "+dec-w=[32]", "+graph_knn(n_neighs=8)", "+sampler([8])",
            "+wt_attr_reconstr=1.0", "+batch_size=512", "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+squint_hln(dataset switch, train=ALL, max_epochs=400)",
        ],
        "build": lambda: _patch_dual_squint_hln(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(4, 675),
                                                    ),
                                                    branch="cell", codebook_sizes=(16, 169),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=8,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    # FiLM-scale REFERENCE (the s57_v19 coupling) on squint_hln. = the squint_hln
    # s49_v23 spine above + cell-cond niche FiLM scale-only. NOTE: squint_hln is a
    # SINGLE CosMx section, so cross-batch MNN and iLISI/MMD are degenerate — this
    # keeps the within-batch contrastive (cross-MNN reduces to it on one section)
    # and adds FiLM-scale, the component being replicated. RESOLUTION
    # (cell_type_annotation / niche_annotation NMI/ARI) is the meaningful output.
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln": {
        "description": (
            "FiLM-scale reference (s57_v19 coupling) on squint_hln (CosMx human "
            "lymph node, single section, 4 manual niches). = the squint_hln "
            "s49_v23 spine (decoupled RVQ (30,90) + within-batch contrastive "
            "wt=10/k=5 + cell-VQ diversity) PLUS cell-cond niche FiLM scale-only. "
            "Single section -> cross-MNN/iLISI/MMD degenerate; FiLM-scale is the "
            "replicated component; resolution (cell_type_annotation / "
            "niche_annotation NMI/ARI) is the meaningful output. REQUIRES the "
            "squint_hln blob."
        ),
        "patches": ["= <squint_hln s49_v23 spine> + cell-cond niche FiLM scale-only"],
        "build": lambda: _patch_dual_cell_conditioned_niche(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+squint_hln"
                     ]["build"](),
            mode="film_scale"),
    },
    # FiLM-scale squint_hln reference with GROUND-TRUTH L0 codebooks at the MOUSE
    # L1:L0 ratio (x3): cell RVQ = (16, 48) [L0=16 = #cell types], niche RVQ =
    # (4, 12) [L0=4 = #manual niches]. Distinct from the capacity-matched
    # (16,169)/(4,675) squint_hln variant — here L1=3*L0 mirrors mouse (30,90).
    # Same FiLM-scale reference model otherwise; RVQ overrides applied to the
    # spine BEFORE the FiLM patch so the cell-conditioned FiLM embedding sizes to
    # L0=16. Uses the existing squint_hln blob (no rebuild needed).
    "dualvq+rvq-cell-16-48+rvq-niche-4-12+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln": {
        "description": (
            "FiLM-scale squint_hln reference with ground-truth L0 codebooks at "
            "the mouse L1:L0 ratio (x3): cell RVQ = (16, 48) [L0=16 cell types], "
            "niche RVQ = (4, 12) [L0=4 manual niches: B cell Zone, T cell Zone, "
            "Medulla, Germinal Center]. = the +filmscale+squint_hln reference but "
            "with these RVQ sizes (vs the (30,90) default). Distinct from the "
            "capacity-matched (16,169)/(4,675) variant. Uses the existing "
            "squint_hln blob (full panel). REQUIRES the squint_hln blob."
        ),
        "patches": [
            "+rvq(cell levels=[16, 48], niche levels=[4, 12])",
            "= <squint_hln s49_v23 spine> + cell-cond niche FiLM scale-only",
        ],
        "build": lambda: _patch_dual_cell_conditioned_niche(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper"
                             "+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4"
                             "+within-sec+decoupled-enc+diversity-w10"
                             "+contrastWB-w10-k5+squint_hln"]["build"](),
                    branch="niche", codebook_sizes=(4, 12),
                ),
                branch="cell", codebook_sizes=(16, 48),
            ),
            mode="film_scale"),
    },
    # ----------------------------------------------------------------------
    # squint_hln GENE-FILTERING comparison (matches the niche-benchmark paper's
    # feature-selection axis: all detected genes / top-2000 HVG / top-2000 SVG).
    # Each is the EXACT FiLM-scale squint_hln reference above, only redirected to
    # a gene-filtered blob via `_patch_dataset_name` (model byte-identical; the
    # gene input dim is derived from the blob at runtime). Build the blobs first
    # with `preprocess_squint_hln.py` + `--build-blob-dataset squint_hln_<tag>`.
    # ----------------------------------------------------------------------
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln_allgenes": {
        "description": (
            "FiLM-scale squint_hln reference on ALL detected genes (negative "
            "controls removed + filter_genes only; no HVG/SVG selection). "
            "Identical model to the +squint_hln reference; reads the "
            "squint_hln_allgenes blob. REQUIRES that blob."
        ),
        "patches": ["= <filmscale squint_hln reference> on the all-genes blob"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "squint_hln_allgenes"),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln_hvg2k": {
        "description": (
            "FiLM-scale squint_hln reference on the top-2000 HVGs (scanpy "
            "seurat_v3 on raw counts). Identical model to the +squint_hln "
            "reference; reads the squint_hln_hvg2k blob. REQUIRES that blob."
        ),
        "patches": ["= <filmscale squint_hln reference> on the HVG-2000 blob"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "squint_hln_hvg2k"),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln_svg2k": {
        "description": (
            "FiLM-scale squint_hln reference on the top-2000 SVGs (squidpy "
            "Moran's I). Identical model to the +squint_hln reference; reads "
            "the squint_hln_svg2k blob. REQUIRES that blob."
        ),
        "patches": ["= <filmscale squint_hln reference> on the SVG-2000 blob"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "squint_hln_svg2k"),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln_hvg1k": {
        "description": (
            "FiLM-scale squint_hln reference on the top-1000 HVGs (scanpy "
            "seurat_v3 on raw counts). Identical model to the +squint_hln "
            "reference; reads the squint_hln_hvg1k blob. REQUIRES that blob."
        ),
        "patches": ["= <filmscale squint_hln reference> on the HVG-1000 blob"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "squint_hln_hvg1k"),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+squint_hln_svg1k": {
        "description": (
            "FiLM-scale squint_hln reference on the top-1000 SVGs (squidpy "
            "Moran's I). Identical model to the +squint_hln reference; reads "
            "the squint_hln_svg1k blob. REQUIRES that blob."
        ),
        "patches": ["= <filmscale squint_hln reference> on the SVG-1000 blob"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "squint_hln_svg1k"),
    },
    # FiLM-scale reference on 3 Xenium human skin sections (xhs batch 11/19/32),
    # 1000-gene panel. MULTI-section, so cross-batch integration is meaningful.
    # TWO references, both byte-identical-model to existing SQUINT references,
    # only the contrastive + dataset differ:
    #   (a) WITHIN-batch (= the squint_hln reference) — direct analog of the HLN
    #       runs; (b) CROSS-MNN (= the genuine multi-section SQUINT default, as on
    #       mmb/chl59) — integration-aware (the within-batch special case was
    #       forced only by squint_hln being a single section). Both REQUIRE the
    #       xhs1000-3b_1p blob (build from a silver dir holding adata_batch
    #       {11,19,32}.h5ad). Recommended for multi-section: the cross-MNN one.
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale+xhs1000-3b_1p": {
        "description": (
            "FiLM-scale squint_hln reference (WITHIN-batch contrastive) redirected "
            "to 3 Xenium human skin sections (xhs batch 11/19/32, 1000-gene "
            "panel). Byte-identical model; labels new_annotation / niche_type via "
            "the xhs1000-3b_1p blob. Direct analog of the HLN runs. REQUIRES that "
            "blob."
        ),
        "patches": ["= <filmscale squint_hln reference (within-batch)> on xhs1000-3b_1p"],
        "build": lambda: _patch_dataset_name(
            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32"
                     "+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                     "+decoupled-enc+diversity-w10+contrastWB-w10-k5+filmscale"
                     "+squint_hln"]["build"](),
            "xhs1000-3b_1p"),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+xhs1000-3b_1p": {
        "description": (
            "FiLM-scale reference with CROSS-BATCH MNN contrastive (wt=10, k=1) on "
            "3 Xenium human skin sections (xhs batch 11/19/32). The genuine "
            "multi-section SQUINT default (= the mmb s57_v19 / chl59 coupling): "
            "within-batch contrastive swapped for cross-MNN so cross-section "
            "integration is optimized. Same RVQ (30,90) decoupled + FiLM-scale "
            "model; labels new_annotation / niche_type. REQUIRES the xhs1000-3b_1p "
            "blob. RECOMMENDED for this multi-section dataset."
        ),
        "patches": [
            "= <filmscale squint_hln reference> + within->cross-MNN (wt10,k1) on xhs1000-3b_1p",
        ],
        "build": lambda: _patch_dataset_name(
            _patch_dual_contrastive_cross_batch(
                VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper"
                         "+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4"
                         "+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5"
                         "+filmscale+squint_hln"]["build"](),
                wt_cross=10.0, k_cross=1),
            "xhs1000-3b_1p"),
    },
    # Cross-MNN FiLM-scale xhs reference with CUSTOM codebooks: cell RVQ =
    # (21, 63), niche RVQ = (12, 36) (L0 = 21 cell / 12 niche; L1 = 3x L0).
    # Same model as the cross-MNN +xhs1000-3b_1p reference, only the RVQ sizes
    # differ — applied to the spine BEFORE FiLM so the cell-conditioned FiLM
    # embedding sizes to cell L0=21. REQUIRES the xhs1000-3b_1p blob.
    "dualvq+rvq-cell-21-63+rvq-niche-12-36+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+xhs1000-3b_1p": {
        "description": (
            "Cross-MNN FiLM-scale reference on 3 Xenium human skin sections "
            "(xhs batch 11/19/32) with cell RVQ = (21, 63) and niche RVQ = "
            "(12, 36) [L0 = 21 cell / 12 niche codes; L1 = 3x L0]. = the "
            "+crossmnn+xhs1000-3b_1p reference but with these RVQ sizes (vs the "
            "(30,90) default). Labels new_annotation / niche_type. REQUIRES the "
            "xhs1000-3b_1p blob."
        ),
        "patches": [
            "+rvq(cell levels=[21, 63], niche levels=[12, 36])",
            "= <cross-MNN FiLM-scale xhs reference> with custom codebooks",
        ],
        "build": lambda: _patch_dataset_name(
            _patch_dual_contrastive_cross_batch(
                _patch_dual_cell_conditioned_niche(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            VARIANTS["dualvq+rvq-both+decoder-cov+no-batch-int"
                                     "+enc-deeper+dec-w32+knn16+sampler16+cell-w1"
                                     "+bs512+lr7e-4+within-sec+decoupled-enc"
                                     "+diversity-w10+contrastWB-w10-k5+squint_hln"
                                     ]["build"](),
                            branch="niche", codebook_sizes=(12, 36)),
                        branch="cell", codebook_sizes=(21, 63)),
                    mode="film_scale"),
                wt_cross=10.0, k_cross=1),
            "xhs1000-3b_1p"),
    },
    # FiLM-scale REFERENCE (the FULL s57_v19 config) on chl59-8b_1p (CosMx Lung,
    # 8 sections). Unlike squint_hln (1 section), chl59 is MULTI-section, so this
    # uses the complete s57_v19 coupling = cross-batch MNN (wt=10,k=1) + cell-cond
    # niche FiLM scale-only on the s49_v23 spine (decoupled RVQ (30,90) + cell-VQ
    # diversity). This is the dataset where the FiLM INTEGRATION gain (iLISI/MMD)
    # can actually be tested. Holds out sections [2,3] (== the chl59 sweep).
    # batch_size = chl59 default (128, memory-safe for the Lung panel).
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+chl59-8b_1p": {
        "description": (
            "FiLM-scale reference (FULL s57_v19 config) on chl59-8b_1p (CosMx "
            "Lung, 8 sections). = the s49_v23 spine (decoupled RVQ (30,90) + "
            "cell-VQ diversity) + cross-batch MNN (wt=10,k=1) + cell-cond niche "
            "FiLM scale-only, switched to the chl59 dataset with sections [2,3] "
            "held out. MULTI-section -> cross-MNN + iLISI/MMD integration are "
            "meaningful, so this is the 2nd-dataset replication of the FiLM "
            "integration result. REQUIRES the chl59-8b_1p blob."
        ),
        "patches": [
            "= s49_v23 spine + cross-batch MNN + cell-cond FiLM scale-only "
            "+ chl59-8b_1p (test=[2,3])",
        ],
        "build": lambda: _patch_dual_chl59_lung5(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90))),
            test_batch_idx=[2, 3]),
    },
    # FiLM-scale REFERENCE on chl59-2b_1p (CosMx Lung, 2-sample subset). Same
    # cross-MNN + cell-cond FiLM scale-only s49_v23 spine as the chl59-8b
    # reference, switched to the 2-section subset (train on BOTH sections, no
    # whole-section holdout; bs=512). MULTI-section -> cross-MNN + iLISI/MMD
    # meaningful. REQUIRES the chl59-2b_1p blob (--build-blob-dataset chl59-2b_1p).
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+chl59-2b_1p": {
        "description": (
            "FiLM-scale reference (cross-MNN + cell-cond FiLM scale-only, == the "
            "s57_v19 coupling) on chl59-2b_1p (CosMx Lung, 2 sections), DEFAULT "
            "codebooks cell RVQ=(30,90), niche RVQ=(30,90). Train on both "
            "sections (10% cell-level val, no holdout). REQUIRES the chl59-2b_1p "
            "blob."
        ),
        "patches": ["= s57_v19 reference (cross-MNN + FiLM scale) + chl59-2b_1p (train=ALL)"],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    # Same chl59-2b_1p reference with CUSTOM codebooks: cell RVQ = (10, 30),
    # niche RVQ = (12, 36) [L0 = 10 cell / 12 niche; L1 = 3x L0]. Codebooks set
    # on the spine via _build_s51_spine_codebook BEFORE FiLM (cell-cond FiLM
    # sizes to cell L0=10). REQUIRES the chl59-2b_1p blob.
    "dualvq+rvq-cell-10-30+rvq-niche-12-36+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+chl59-2b_1p": {
        "description": (
            "FiLM-scale reference (cross-MNN + cell-cond FiLM scale-only) on "
            "chl59-2b_1p (CosMx Lung, 2 sections) with cell RVQ = (10, 30) and "
            "niche RVQ = (12, 36) [L0 = 10 cell / 12 niche codes; L1 = 3x L0]. = "
            "the +crossmnn+chl59-2b_1p reference but with these RVQ sizes (vs the "
            "(30,90) default). Train on both sections. REQUIRES the chl59-2b_1p "
            "blob."
        ),
        "patches": [
            "+rvq(cell levels=[10, 30], niche levels=[12, 36])",
            "= <cross-MNN FiLM-scale chl59-2b_1p reference> with custom codebooks",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_squint_default(_build_s51_spine_codebook((10, 30), (12, 36)))),
    },
    # FiLM-scale REFERENCE (the FULL s57_v19 config) on smb1-20b_1p (STARmap+
    # mouse-CNS, 20 sections — the STARmap-only counterpart to mmb20, on its
    # native panel). MULTI-section, so this uses the complete s57_v19 coupling
    # = cross-batch MNN (wt=10,k=1) + cell-cond niche FiLM scale-only on the
    # s49_v23 spine (decoupled RVQ (30,90) + cell-VQ diversity). Trains on ALL
    # 20 sections (10% cell-level val, no whole-section holdout) -> produces
    # SQUINT codes across the dataset. REQUIRES the smb1-20b_1p blob.
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+smb1-20b_1p": {
        "description": (
            "FiLM-scale reference (FULL s57_v19 config) on smb1-20b_1p "
            "(STARmap+ mouse-CNS, 20 sections, native panel). = the s49_v23 "
            "spine (decoupled RVQ (30,90) + cell-VQ diversity) + cross-batch "
            "MNN (wt=10,k=1) + cell-cond niche FiLM scale-only, switched to "
            "smb1-20b_1p (train ALL 20 sections, 10% cell-level val, no "
            "whole-section holdout). MULTI-section -> cross-MNN + iLISI/MMD "
            "integration are meaningful (2nd multi-section replication of the "
            "FiLM integration result, alongside chl59). Faithful spine: bs=512, "
            "edge_sampling=2.0, gnn=1, sampler=[16], nbr_hops=1 — MEMORY-HEAVY "
            "at 20 resident sections; lower batch_size if it OOMs. REQUIRES "
            "the smb1-20b_1p blob (--build-blob --build-blob-dataset smb1-20b_1p)."
        ),
        "patches": [
            "= s49_v23 spine + cross-batch MNN + cell-cond FiLM scale-only "
            "+ smb1-20b_1p (train=ALL)",
        ],
        "build": lambda: _patch_dual_smb1_20b(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    # FiLM-scale smb1-20b_1p reference with CUSTOM codebooks: cell RVQ =
    # (27, 81), niche RVQ = (106, 318) [L0 = 27 cell / 106 niche; L1 = 3x L0].
    # Same model as the +crossmnn+smb1-20b_1p reference, only the RVQ sizes
    # change — set on the spine via _build_s51_spine_codebook BEFORE FiLM (so
    # the cell-cond FiLM embedding sizes to cell L0=27). Large niche L0 (106)
    # matches the fine CCF-region granularity. REQUIRES the smb1-20b_1p blob.
    "dualvq+rvq-cell-27-81+rvq-niche-106-318+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+smb1-20b_1p": {
        "description": (
            "FiLM-scale reference (cross-MNN + cell-cond FiLM scale-only) on "
            "smb1-20b_1p (STARmap+ mouse-CNS, 20 sections) with cell RVQ = "
            "(27, 81) and niche RVQ = (106, 318) [L0 = 27 cell / 106 niche "
            "codes; L1 = 3x L0]. = the +crossmnn+smb1-20b_1p reference but with "
            "these RVQ sizes (vs the (30,90) default). MULTI-section -> cross-MNN "
            "+ iLISI/MMD meaningful. REQUIRES the smb1-20b_1p blob."
        ),
        "patches": [
            "+rvq(cell levels=[27, 81], niche levels=[106, 318])",
            "= <cross-MNN FiLM-scale smb1-20b_1p reference> with custom codebooks",
        ],
        "build": lambda: _patch_dual_smb1_20b(
            _patch_dual_squint_default(_build_s51_spine_codebook((27, 81), (106, 318)))),
    },
    # FiLM-scale REFERENCE (the FULL s57_v19 config) on the three spatch
    # tissue subsets (spatch_ov_1p / spatch_hcc_1p / spatch_coad_1p). Each is
    # MULTI-section (dataset_id_*.h5ad), so all use the complete s57_v19
    # coupling = cross-batch MNN (wt=10,k=1) + cell-cond niche FiLM scale-only
    # on the s49_v23 spine (decoupled RVQ (30,90) + cell-VQ diversity). Train
    # on ALL sections in the subset (10% cell-level val, no whole-section
    # holdout). spatch labels: cell=annotation, niche=spatial_cluster (from
    # the blob). bs=512, edge_sampling=1.0 (spatch s51 convention). REQUIRE
    # the per-subset blobs (--build-blob-dataset spatch_{ov,hcc,coad}_1p).
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+spatch_ov_1p": {
        "description": (
            "FiLM-scale reference (FULL s57_v19 config) on spatch_ov_1p "
            "(ovarian cancer subset). = s49_v23 spine (decoupled RVQ (30,90) "
            "+ cell-VQ diversity) + cross-batch MNN (wt=10,k=1) + cell-cond "
            "niche FiLM scale-only, switched to spatch_ov_1p (train ALL "
            "sections, 10% cell-level val). REQUIRES the spatch_ov_1p blob."
        ),
        "patches": ["= s57_v19 reference (cross-MNN + FiLM scale) + spatch_ov_1p (train=ALL)"],
        "build": lambda: _patch_dual_spatch_ov(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+spatch_hcc_1p": {
        "description": (
            "FiLM-scale reference (FULL s57_v19 config) on spatch_hcc_1p "
            "(hepatocellular carcinoma subset). = s49_v23 spine (decoupled "
            "RVQ (30,90) + cell-VQ diversity) + cross-batch MNN (wt=10,k=1) + "
            "cell-cond niche FiLM scale-only, switched to spatch_hcc_1p (train "
            "ALL sections, 10% cell-level val). REQUIRES the spatch_hcc_1p blob."
        ),
        "patches": ["= s57_v19 reference (cross-MNN + FiLM scale) + spatch_hcc_1p (train=ALL)"],
        "build": lambda: _patch_dual_spatch_hcc(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+spatch_coad_1p": {
        "description": (
            "FiLM-scale reference (FULL s57_v19 config) on spatch_coad_1p "
            "(colon adenocarcinoma subset). = s49_v23 spine (decoupled RVQ "
            "(30,90) + cell-VQ diversity) + cross-batch MNN (wt=10,k=1) + "
            "cell-cond niche FiLM scale-only, switched to spatch_coad_1p (train "
            "ALL sections, 10% cell-level val). REQUIRES the spatch_coad_1p blob."
        ),
        "patches": ["= s57_v19 reference (cross-MNN + FiLM scale) + spatch_coad_1p (train=ALL)"],
        "build": lambda: _patch_dual_spatch_coad(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    # FiLM-scale spatch_coad_1p reference with CUSTOM codebooks: cell RVQ =
    # (16, 48), niche RVQ = (5, 15) [L0 = 16 cell / 5 niche; L1 = 3x L0].
    # Same model as the +crossmnn+spatch_coad_1p reference, only the RVQ sizes
    # change — set on the spine via _build_s51_spine_codebook BEFORE FiLM (so
    # the cell-cond FiLM embedding sizes to cell L0=16). REQUIRES the
    # spatch_coad_1p blob (2 sections).
    "dualvq+rvq-cell-16-48+rvq-niche-5-15+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+spatch_coad_1p": {
        "description": (
            "FiLM-scale reference (cross-MNN + cell-cond FiLM scale-only) on "
            "spatch_coad_1p (colon adenocarcinoma, 2 sections) with cell RVQ = "
            "(16, 48) and niche RVQ = (5, 15) [L0 = 16 cell / 5 niche codes; "
            "L1 = 3x L0]. = the +crossmnn+spatch_coad_1p reference but with these "
            "RVQ sizes (vs the (30,90) default). Labels annotation / "
            "spatial_cluster. REQUIRES the spatch_coad_1p blob."
        ),
        "patches": [
            "+rvq(cell levels=[16, 48], niche levels=[5, 15])",
            "= <cross-MNN FiLM-scale spatch_coad_1p reference> with custom codebooks",
        ],
        "build": lambda: _patch_dual_spatch_coad(
            _patch_dual_squint_default(_build_s51_spine_codebook((16, 48), (5, 15)))),
    },
    # FiLM-scale REFERENCE + REGION HOLDOUT on mmb0-1b_smb1-1b_1p — the new
    # SQUINT reference (== the s57_v19 config: cross-MNN + cell-cond FiLM
    # scale-only on the s49_v23 spine) with a 25%x25% spatial in-painting
    # holdout (batch 15 upper-left + batch 82 lower-right). Both sections stay
    # in the train graph; cells inside the patches are masked OUT of the loss
    # and tagged data_split=test so post-inference Pearson reports on unseen
    # cells (gene-reconstruction / in-painting downstream task). NOT named
    # `s57_v19_*` on purpose: that prefix is matched by the ablation
    # plots/summaries, so a region-holdout sibling under it would collide.
    # Reuses the mmb blob (no rebuild).
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "FiLM-scale reference (the s57_v19 config) + REGION HOLDOUT on "
            "mmb0-1b_smb1-1b_1p. = the s49_v23 spine + cross-batch MNN "
            "(wt=10,k=1) + cell-cond niche FiLM scale-only, PLUS a 25%x25% "
            "spatial holdout patch in batch 15 (upper-left) and batch 82 "
            "(lower-right). Both sections stay in training; patch cells are "
            "masked out of the loss + tagged data_split=test so post-inference "
            "Pearson reports on unseen cells (gene-reconstruction / in-painting "
            "downstream task). Reuses the mmb blob (no rebuild)."
        ),
        "patches": [
            "= s57_v19 reference (cross-MNN + FiLM scale) "
            "+ region-holdout (batch15 + batch82, 25%x25% patches)",
        ],
        "build": lambda: _patch_holdout_regions(
            _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
    },
    # --- BIGGER-DECODER siblings of the FiLM-scale region-holdout reference ---
    # Identical to the reference above except the cell+niche NB-decoder MLP is
    # widened from [32] -> [256] / [400, 400] (base default), applied LAST so it
    # overrides the spine's dec-w[32]. Tests whether decoder capacity lifts the
    # reconstruction metrics (recon ceiling in a VQ-VAE is usually codebook- not
    # decoder-limited, so treat these as a capacity check). Reuse the mmb blob.
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w256+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Bigger-decoder sibling of the FiLM-scale region-holdout reference: "
            "same s57_v19 config (cross-MNN + FiLM scale) + region-holdout, but the "
            "cell+niche NB decoder MLP is widened from [32] to [256] (1 hidden). "
            "Capacity check for reconstruction. Reuses the mmb blob (no rebuild)."
        ),
        "patches": [
            "= FiLM-scale region-holdout reference + dec-w=[256] (both branches)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_holdout_regions(
                _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
            [256]),
    },
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w400x2+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+filmscale+crossmnn-wt10-k1+region-holdout+mmb0-1b_smb1-1b_1p": {
        "description": (
            "Bigger-decoder sibling of the FiLM-scale region-holdout reference: "
            "cell+niche NB decoder MLP = [400, 400] (2 hidden, the dual base "
            "default) vs the reference's [32]. Tests decoder depth+width for "
            "reconstruction. Reuses the mmb blob (no rebuild)."
        ),
        "patches": [
            "= FiLM-scale region-holdout reference + dec-w=[400, 400] (both branches)",
        ],
        "build": lambda: _patch_dual_decoder_width(
            _patch_holdout_regions(
                _patch_dual_squint_default(_build_s51_spine_codebook((30, 90), (30, 90)))),
            [400, 400]),
    },
    # -----------------------------------------------------------------------
    # s49_v23 ablations (canonical-name) — 7 ablations x 2 datasets
    # -----------------------------------------------------------------------
    # Ablations of the s49_v23 spine (decoupled-enc + diversity-w10 +
    # contrastWB-w10-k5):
    #   - base                                  (control)
    #   - no-contrastive                        (drops within-batch contrastive)
    #   - no-adj                                (drops adjacency BCE)
    #   - no-decoder-cov                        (drops learned batch covariate)
    #   - rvq-cell (30, 30) / (30, 10) / (30, 300)   (vary L2 codebook size)
    #
    # Two datasets: mmb0-1b_smb1-1b_1p + chl59-2b_1p (new 2-sample subset at
    # /lustre/scratch126/cellgen/lotfollahi/DATASETS/silver/chl59-2b_1p).
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v51
    #   bash examples/submit_dataset_sweep.sh chl59-2b_1p-ablations-v51
    # ---- mmb0-1b_smb1-1b_1p ----
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: BASE — s49_v23 spine unchanged (control). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    # =======================================================================
    # s53 — CONTINUOUS-LATENT (discretization) ablation family
    #       (addresses the discrete-vs-continuous-latent reviewer comment).
    #       A NEW, self-contained family — SEPARATE from the s51/s52
    #       ablations; nothing existing is re-run.
    # =======================================================================
    "s53_v1_continuous-latent+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "CONTINUOUS-LATENT ABLATION of the mouse-brain winner (addresses the reviewer "
            "comment: 'rationale for discrete codebooks over continuous latents not sufficiently "
            "supported'). This is EXACTLY the variant "
            "'s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+"
            "sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+"
            "contrastWB-w10-k5+mmb0-1b_smb1-1b_1p' WITH THE VQ COMPONENT REMOVED. Its build "
            "calls that variant's OWN build() and then applies _patch_dual_continuous_vq("
            "branch='both'), so the encoder/decoder architecture, latent dimensionality, "
            "graph, sampler, batch size, lr, and every non-VQ loss are byte-for-byte "
            "identical — ONLY the two ResidualVQ bottlenecks become continuous passthroughs "
            "(ContinuousVQ). Effects: z_q = z (continuous encoder latent goes straight to the "
            "decoder, no codebook snap), the commitment loss mse_commit = MSE(z, z_q) == 0, "
            "and the cell-VQ codebook-diversity term is inert (no codebook). The continuous "
            "latent has the SAME dim as the VQ embedding, so it carries >= the discrete "
            "model's capacity — the conservative 'comparable capacity' comparison requested. "
            "Isolates whether DISCRETIZATION (not capacity/depth) drives the gain. SCORING: "
            "emits no meaningful codes (idx are placeholder zeros) — compare to the discrete "
            "s49_v23 via the EMBEDDING metrics in benchmarking.py (cnmi/cari/casw/nasw/gcs on "
            "obsm[latent_key]); do NOT use the code->label path for it. Reuses the s49_v23 "
            "blob (mmb0-1b_smb1-1b_1p, k=16) — NO blob rebuild needed; existing runs untouched."
        ),
        "patches": [
            "= s49_v23_dualvq+rvq-both+...+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
            "+continuous-latent(branch=both)  [ResidualVQ -> ContinuousVQ on cell AND niche:",
            "                                  removes quantization, commit loss is 0,",
            "                                  codebook-diversity inert]",
        ],
        # Build = the EXACT s49_v23 mmb-smb config, then swap both VQ bottlenecks
        # for a continuous passthrough. Referencing the s49_v23 build() directly
        # (rather than re-copying its patch chain) guarantees this stays literally
        # "s49_v23 minus VQ" even if that variant is ever edited. s49_v23's build()
        # returns a fresh deep-copied cfg (via _BD), so mutating it here is safe and
        # does not affect the s49_v23 variant itself.
        "build": lambda: _patch_dual_continuous_vq(
            VARIANTS[
                "s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"
            ]["build"](),
            branch="both",
        ),
    },
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Drops the within-batch contrastive cell loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "-contrastive-cell-within-batch (ablated)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Drops the adjacency BCE loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+no-adj (drops bce_cosine_adjacency_reconstruction_loss)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_no_adj(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
),
    },
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Drops the learned decoder batch covariate. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "-decoder_covariate (ablated)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Cell RVQ L2 = 30 (vs default 90). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 30), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-cell=[30, 30], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 30),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Cell RVQ L2 = 10 (smallest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 10), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-cell=[30, 10], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 10),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: Cell RVQ L2 = 300 (largest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 300), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) ."
        ),
        "patches": [
            "+rvq-cell=[30, 300], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 300),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
    },
    # ---- chl59-2b_1p ----
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: BASE — s49_v23 spine unchanged (control). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Drops the within-batch contrastive cell loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "-contrastive-cell-within-batch (ablated)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Drops the adjacency BCE loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+no-adj (drops bce_cosine_adjacency_reconstruction_loss)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_no_adj(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
),
batch_size=512,
),
    },
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Drops the learned decoder batch covariate. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "-decoder_covariate (ablated)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 90),
                                    ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Cell RVQ L2 = 30 (vs default 90). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 30), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-cell=[30, 30], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 30),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Cell RVQ L2 = 10 (smallest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 10), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-cell=[30, 10], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 10),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: Cell RVQ L2 = 300 (largest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + codebook-diversity on cell-VQ wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], knn16, sampler[16], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 300), niche RVQ = (30, 90). REQUIRES BLOB REBUILD (k=16 in n_neighs_list) for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-cell=[30, 300], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
_patch_dual_adj_within_section_only(
_patch_dual_no_batch_int(
    _patch_dual_batch_lr(
        _patch_dual_attr_recon_weight(
            _patch_dual_sampler_neighbors(
                _patch_dual_graph_knn(
                    _patch_dual_decoder_width(
                        _patch_dual_decoupled_encoders(
                            _patch_dual_encoder_deeper(
                                _patch_dual_decoder_covariate(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _BD(),
                                            branch="niche", codebook_sizes=(30, 90),
                                        ),
                                        branch="cell", codebook_sizes=(30, 300),
                                    ),
                                ),
                                hidden_channels=[400, 400, 256],
                            ),
                        ),
                        hidden_channels=[32],
                    ),
                    n_neighs=16,
                ),
                num_neighbors=[16],
            ),
            weight=1.0,
        ),
        batch_size=512, lr=7e-4,
    ),
),
enabled=True,
),
wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    # -----------------------------------------------------------------------
    # s49_v23 ablations — neighbors-axis extension (v51, knn=8 + knn=24)
    # -----------------------------------------------------------------------
    # Two more variants per dataset: knn=8 + sampler=8 (small matched) and
    # knn=24 + sampler=24 (large matched). The knn=16 axis center IS the
    # v51 base variant — listed a second time in the DATASET_VARIANTS sweep
    # below so the neighbors axis has all 3 levels visible in the results.
    # ---- mmb0-1b_smb1-1b_1p ----
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: neighbors axis — graph k=8, sampler=8 (vs base knn=16+sampler=16). Same spine as the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) + cell-w=1 + no-batch-int + enc-deeper [400, 400, 256] + dec-w[32] + bs=512 + lr=7e-4 + within-sec. Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD with k=8 in n_neighs_list ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=8,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: neighbors axis — graph k=24, sampler=24 (vs base knn=16+sampler=16). Same spine as the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) + cell-w=1 + no-batch-int + enc-deeper [400, 400, 256] + dec-w[32] + bs=512 + lr=7e-4 + within-sec. Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD with k=24 in n_neighs_list ."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)",
            "+sampler([24])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=24,
                                    ),
                                    num_neighbors=[24],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    # ---- chl59-2b_1p ----
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: neighbors axis — graph k=8, sampler=8 (vs base knn=16+sampler=16). Same spine as the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) + cell-w=1 + no-batch-int + enc-deeper [400, 400, 256] + dec-w[32] + bs=512 + lr=7e-4 + within-sec. Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD with k=8 in n_neighs_list for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=8,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: neighbors axis — graph k=24, sampler=24 (vs base knn=16+sampler=16). Same spine as the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) + cell-w=1 + no-batch-int + enc-deeper [400, 400, 256] + dec-w[32] + bs=512 + lr=7e-4 + within-sec. Cell RVQ = (30, 90), niche RVQ = (30, 90). REQUIRES BLOB REBUILD with k=24 in n_neighs_list for the chl59-2b_1p dataset."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)",
            "+sampler([24])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=24,
                                    ),
                                    num_neighbors=[24],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    # -----------------------------------------------------------------------
    # s49_v23 ablation — knn=16 graph + sampler=8 (v51 slot 9 replacement)
    # -----------------------------------------------------------------------
    # Replaces the duplicate-base entry at slot 9. Isolates 'graph density'
    # from 'per-step sampler size': v8 (knn=8+s=8, matched-small) and this
    # variant (knn=16+s=8, dense-graph-sparse-sampler) both sample 8 nbrs
    # per cell per step but operate on DIFFERENT spatial graph densities.
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: knn=16 graph but sampler=8 — keep the default GRAPH density but limit the sampler to draw only 8 neighbors per training step. Direct comparator vs base (knn=16+sampler=16) and vs the matched knn=8+sampler=8 variant: isolates 'graph density' from 'per-step sampler size'. Same spine as the v51 base — only the sampler num_neighbors changes."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: knn=16 graph but sampler=8 — keep the default GRAPH density but limit the sampler to draw only 8 neighbors per training step. Direct comparator vs base (knn=16+sampler=16) and vs the matched knn=8+sampler=8 variant: isolates 'graph density' from 'per-step sampler size'. Same spine as the v51 base — only the sampler num_neighbors changes."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 90)),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[8],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    # -----------------------------------------------------------------------
    # s49_v23 ablation — 2-layer GNN (v51 slot 11)
    # -----------------------------------------------------------------------
    # Bumps GNN num_layers from 1 to 2 with matched sampler [16, 16].
    # `nbr_aggregation_hops` stays at 1 so this isolates 'GNN sees 2
    # hops' from the target-aggregation radius (which `_patch_dual_wide`
    # couples together).
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s49_v23 ablation on mmb0-1b_smb1-1b_1p: GNN depth 1 -> 2 layers (2-hop spatial aggregation), with matched 2-hop sampler [16, 16]. The niche encoder now sees neighbours-of-neighbours per training step. All other knobs identical to the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine. `nbr_aggregation_hops` STAYS at 1 (the niche-side reconstruction target is the 1-hop neighbourhood mean, NOT the 2-hop) — so this isolates the 'GNN sees 2 hops' effect from the 'target is 2-hop mean' effect that `_patch_dual_wide` couples them with. Memory: 2-hop sampler increases per-step activation memory by ~16x relative to 1-hop on knn=16 graph; bs=512 should still fit but the run is slower."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16, 16])  (2-hop, matches gnn-l2)",
            "+gnn-l2 (num_layers: 1 -> 2)",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_gnn_layers(
                                        _patch_dual_graph_knn(
                                            _patch_dual_decoder_width(
                                                _patch_dual_decoupled_encoders(
                                                    _patch_dual_encoder_deeper(
                                                        _patch_dual_decoder_covariate(
                                                            _patch_dual_rvq(
                                                                _patch_dual_rvq(_BD(),
                                                                    branch="niche", codebook_sizes=(30, 90)),
                                                                branch="cell", codebook_sizes=(30, 90),
                                                            ),
                                                        ),
                                                        hidden_channels=[400, 400, 256],
                                                    ),
                                                ),
                                                hidden_channels=[32],
                                            ),
                                            n_neighs=16,
                                        ),
                                        num_layers=2,
                                    ),
                                    num_neighbors=[16, 16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s49_v23 ablation on chl59-2b_1p: GNN depth 1 -> 2 layers (2-hop spatial aggregation), with matched 2-hop sampler [16, 16]. The niche encoder now sees neighbours-of-neighbours per training step. All other knobs identical to the v51 base: decoupled-enc + within-batch contrastive (wt=10, k=5) + cell-VQ codebook-diversity (wt=10) on the s48_v2 spine. `nbr_aggregation_hops` STAYS at 1 (the niche-side reconstruction target is the 1-hop neighbourhood mean, NOT the 2-hop) — so this isolates the 'GNN sees 2 hops' effect from the 'target is 2-hop mean' effect that `_patch_dual_wide` couples them with. Memory: 2-hop sampler increases per-step activation memory by ~16x relative to 1-hop on knn=16 graph; bs=512 should still fit but the run is slower."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16, 16])  (2-hop, matches gnn-l2)",
            "+gnn-l2 (num_layers: 1 -> 2)",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_gnn_layers(
                                        _patch_dual_graph_knn(
                                            _patch_dual_decoder_width(
                                                _patch_dual_decoupled_encoders(
                                                    _patch_dual_encoder_deeper(
                                                        _patch_dual_decoder_covariate(
                                                            _patch_dual_rvq(
                                                                _patch_dual_rvq(_BD(),
                                                                    branch="niche", codebook_sizes=(30, 90)),
                                                                branch="cell", codebook_sizes=(30, 90),
                                                            ),
                                                        ),
                                                        hidden_channels=[400, 400, 256],
                                                    ),
                                                ),
                                                hidden_channels=[32],
                                            ),
                                            n_neighs=16,
                                        ),
                                        num_layers=2,
                                    ),
                                    num_neighbors=[16, 16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    # -----------------------------------------------------------------------
    # s51 ablations on 3 spatch tissue subsets
    # (spatch_ov_1p, spatch_hcc_1p, spatch_coad_1p)
    # -----------------------------------------------------------------------
    # 11 ablations per dataset, mirroring the mmb + chl59-2b_1p v51 sweeps.
    # All variants use the s49_v23 spine; ablation knobs and naming match
    # the existing s51_v1..s51_v11 convention.
    #
    # Blob rebuild required (one-time per dataset):
    #   python examples/run_squint.py --build-blob --build-blob-dataset spatch_ov_1p
    #   python examples/run_squint.py --build-blob --build-blob-dataset spatch_hcc_1p
    #   python examples/run_squint.py --build-blob --build-blob-dataset spatch_coad_1p
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh spatch_ov_1p-ablations-v51
    #   bash examples/submit_dataset_sweep.sh spatch_hcc_1p-ablations-v51
    #   bash examples/submit_dataset_sweep.sh spatch_coad_1p-ablations-v51
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v1 on spatch_ov_1p (ovarian cancer): BASE — s49_v23 spine (control). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_ov_1p": {
        "description": (
            "s51 ablation v2 on spatch_ov_1p (ovarian cancer): Drops the within-batch contrastive cell loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "-contrastive-cell-within-batch (ablated)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
    _patch_dual_codebook_diversity(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        weight=10.0, temperature=100.0, branch="cell",
    ),
    batch_size=512,
),
    },
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_ov_1p": {
        "description": (
            "s51 ablation v3 on spatch_ov_1p (ovarian cancer): Drops the adjacency BCE loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+no-adj (drops bce_cosine_adjacency_reconstruction_loss)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_no_adj(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
),
batch_size=512,
),
    },
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v4 on spatch_ov_1p (ovarian cancer): Drops the learned decoder batch covariate. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "-decoder_covariate (ablated)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v5 on spatch_ov_1p (ovarian cancer): Cell RVQ L2 = 30 (vs default 90). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 30), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 30], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 30),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v6 on spatch_ov_1p (ovarian cancer): Cell RVQ L2 = 10 (smallest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 10), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 10], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 10),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v7 on spatch_ov_1p (ovarian cancer): Cell RVQ L2 = 300 (largest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 300), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 300], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 300),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v8 on spatch_ov_1p (ovarian cancer): Neighbors axis (small): knn=8, sampler=8 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=8, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=8,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v9 on spatch_ov_1p (ovarian cancer): Neighbors axis (asym): knn=16, sampler=8 (dense graph, sparse sampler). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v10 on spatch_ov_1p (ovarian cancer): Neighbors axis (large): knn=24, sampler=24 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=24, sampler=[24], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)",
            "+sampler([24])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=24,
                            ),
                            num_neighbors=[24],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s51 ablation v11 on spatch_ov_1p (ovarian cancer): GNN depth 1 -> 2, matched sampler [16, 16]. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16, 16], gnn_layers=2. REQUIRES BLOB REBUILD for spatch_ov_1p (--build-blob-dataset spatch_ov_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16, 16])",
            "+gnn-l2 (num_layers: 1 -> 2)",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
    _patch_dual_adj_within_section_only(
        _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_gnn_layers(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_layers=2,
                        ),
                        num_neighbors=[16, 16],
                    ),
                    weight=1.0,
                ),
                batch_size=512, lr=7e-4,
            ),
        ),
        enabled=True,
    ),
    wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v1 on spatch_hcc_1p (hepatocellular carcinoma): BASE — s49_v23 spine (control). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v12_dualvq+rvq-cell-17-159+rvq-niche-6-450+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 v12 on spatch_hcc_1p: ground-truth L0 codebook sizes at ~constant total capacity (was 30*90=2700 per branch). cell RVQ = (17, 159) [L0=17 = #cell types; 17*159=2703 ~ 2700], niche RVQ = (6, 450) [L0=6 = #niches; 6*450=2700]. Everything else IDENTICAL to the s51_v1 s49_v23 spine (decoder-cov, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], gnn1, cell-w1, bs512, lr7e-4, within-sec, decoupled-enc, diversity-w10, contrastWB-w10-k5). Trains on all hcc sections, 10% cell-level val. REQUIRES the spatch_hcc_1p blob (k=16)."
        ),
        "patches": [
            "+rvq(cell levels=[17, 159], niche levels=[6, 450])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(6, 450),
                                                    ),
                                                    branch="cell", codebook_sizes=(17, 159),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v13_dualvq+rvq-cell-17-12-13+rvq-niche-6-21-21+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 v13 on spatch_hcc_1p: 3-LEVEL RVQ with ground-truth L0 sizes, total ~constant (~2700 per branch). cell RVQ = (17, 12, 13) [L0=17 = #cell types; 17*12*13=2652], niche RVQ = (6, 21, 21) [L0=6 = #niches; 6*21*21=2646]. Higher levels split the residual capacity into two balanced levels. Everything else IDENTICAL to the s51_v1 s49_v23 spine. Trains on all hcc sections, 10% cell-level val. REQUIRES the spatch_hcc_1p blob (k=16)."
        ),
        "patches": [
            "+rvq(cell levels=[17, 12, 13], niche levels=[6, 21, 21])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(6, 21, 21),
                                                    ),
                                                    branch="cell", codebook_sizes=(17, 12, 13),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_hcc_1p": {
        "description": (
            "s51 ablation v2 on spatch_hcc_1p (hepatocellular carcinoma): Drops the within-batch contrastive cell loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "-contrastive-cell-within-batch (ablated)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
    _patch_dual_codebook_diversity(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        weight=10.0, temperature=100.0, branch="cell",
    ),
    batch_size=512,
),
    },
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_hcc_1p": {
        "description": (
            "s51 ablation v3 on spatch_hcc_1p (hepatocellular carcinoma): Drops the adjacency BCE loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+no-adj (drops bce_cosine_adjacency_reconstruction_loss)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_no_adj(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
),
batch_size=512,
),
    },
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v4 on spatch_hcc_1p (hepatocellular carcinoma): Drops the learned decoder batch covariate. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "-decoder_covariate (ablated)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v5 on spatch_hcc_1p (hepatocellular carcinoma): Cell RVQ L2 = 30 (vs default 90). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 30), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 30], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 30),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v6 on spatch_hcc_1p (hepatocellular carcinoma): Cell RVQ L2 = 10 (smallest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 10), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 10], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 10),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v7 on spatch_hcc_1p (hepatocellular carcinoma): Cell RVQ L2 = 300 (largest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 300), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 300], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 300),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v8 on spatch_hcc_1p (hepatocellular carcinoma): Neighbors axis (small): knn=8, sampler=8 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=8, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=8,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v9 on spatch_hcc_1p (hepatocellular carcinoma): Neighbors axis (asym): knn=16, sampler=8 (dense graph, sparse sampler). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v10 on spatch_hcc_1p (hepatocellular carcinoma): Neighbors axis (large): knn=24, sampler=24 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=24, sampler=[24], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)",
            "+sampler([24])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=24,
                            ),
                            num_neighbors=[24],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s51 ablation v11 on spatch_hcc_1p (hepatocellular carcinoma): GNN depth 1 -> 2, matched sampler [16, 16]. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16, 16], gnn_layers=2. REQUIRES BLOB REBUILD for spatch_hcc_1p (--build-blob-dataset spatch_hcc_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16, 16])",
            "+gnn-l2 (num_layers: 1 -> 2)",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
    _patch_dual_adj_within_section_only(
        _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_gnn_layers(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_layers=2,
                        ),
                        num_neighbors=[16, 16],
                    ),
                    weight=1.0,
                ),
                batch_size=512, lr=7e-4,
            ),
        ),
        enabled=True,
    ),
    wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v1 on spatch_coad_1p (colon adenocarcinoma): BASE — s49_v23 spine (control). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v12_dualvq+rvq-cell-16-169+rvq-niche-5-540+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 v12 on spatch_coad_1p: ground-truth L0 codebook sizes at ~constant total capacity (was 30*90=2700 per branch). cell RVQ = (16, 169) [L0=16 = #cell types; 16*169=2704 ~ 2700], niche RVQ = (5, 540) [L0=5 = #niches; 5*540=2700]. Everything else IDENTICAL to the s51_v1 s49_v23 spine (decoder-cov, no-batch-int, enc-deeper [400,400,256], dec-w[32], knn16, sampler[16], gnn1, cell-w1, bs512, lr7e-4, within-sec, decoupled-enc, diversity-w10, contrastWB-w10-k5). Trains on all coad sections, 10% cell-level val. REQUIRES the spatch_coad_1p blob (k=16)."
        ),
        "patches": [
            "+rvq(cell levels=[16, 169], niche levels=[5, 540])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(5, 540),
                                                    ),
                                                    branch="cell", codebook_sizes=(16, 169),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v13_dualvq+rvq-cell-16-13-13+rvq-niche-5-23-23+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 v13 on spatch_coad_1p: 3-LEVEL RVQ with ground-truth L0 sizes, total ~constant (~2700 per branch). cell RVQ = (16, 13, 13) [L0=16 = #cell types; 16*13*13=2704], niche RVQ = (5, 23, 23) [L0=5 = #niches; 5*23*23=2645]. Higher levels split the residual capacity into two balanced levels (13^2~169 for cell, 23^2~540 for niche). Everything else IDENTICAL to the s51_v1 s49_v23 spine. Trains on all coad sections, 10% cell-level val. REQUIRES the spatch_coad_1p blob (k=16)."
        ),
        "patches": [
            "+rvq(cell levels=[16, 13, 13], niche levels=[5, 23, 23])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(5, 23, 23),
                                                    ),
                                                    branch="cell", codebook_sizes=(16, 13, 13),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_coad_1p": {
        "description": (
            "s51 ablation v2 on spatch_coad_1p (colon adenocarcinoma): Drops the within-batch contrastive cell loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "-contrastive-cell-within-batch (ablated)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
    _patch_dual_codebook_diversity(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        weight=10.0, temperature=100.0, branch="cell",
    ),
    batch_size=512,
),
    },
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_coad_1p": {
        "description": (
            "s51 ablation v3 on spatch_coad_1p (colon adenocarcinoma): Drops the adjacency BCE loss. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+no-adj (drops bce_cosine_adjacency_reconstruction_loss)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_no_adj(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
),
batch_size=512,
),
    },
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v4 on spatch_coad_1p (colon adenocarcinoma): Drops the learned decoder batch covariate. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "-decoder_covariate (ablated)",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v5 on spatch_coad_1p (colon adenocarcinoma): Cell RVQ L2 = 30 (vs default 90). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 30), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 30], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 30),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v6 on spatch_coad_1p (colon adenocarcinoma): Cell RVQ L2 = 10 (smallest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 10), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 10], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 10),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v7 on spatch_coad_1p (colon adenocarcinoma): Cell RVQ L2 = 300 (largest tested). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 300), niche RVQ = (30, 90), knn=16, sampler=[16], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-cell=[30, 300], rvq-niche=[30, 90]",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 300),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[16],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v8 on spatch_coad_1p (colon adenocarcinoma): Neighbors axis (small): knn=8, sampler=8 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=8, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=8)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=8,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v9 on spatch_coad_1p (colon adenocarcinoma): Neighbors axis (asym): knn=16, sampler=8 (dense graph, sparse sampler). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[8], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([8])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_neighbors=[8],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v10 on spatch_coad_1p (colon adenocarcinoma): Neighbors axis (large): knn=24, sampler=24 (matched). Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=24, sampler=[24], gnn_layers=1. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=24)",
            "+sampler([24])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
    _patch_dual_contrastive_cell_within_batch(
        _patch_dual_adj_within_section_only(
            _patch_dual_no_batch_int(
                _patch_dual_batch_lr(
                    _patch_dual_attr_recon_weight(
                        _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=24,
                            ),
                            num_neighbors=[24],
                        ),
                        weight=1.0,
                    ),
                    batch_size=512, lr=7e-4,
                ),
            ),
            enabled=True,
        ),
        wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
    ),
    weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s51 ablation v11 on spatch_coad_1p (colon adenocarcinoma): GNN depth 1 -> 2, matched sampler [16, 16]. Built on the s49_v23 spine (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1, no-batch-int, enc-deeper [400, 400, 256], dec-w[32], bs=512, lr=7e-4, within-sec). Cell RVQ = (30, 90), niche RVQ = (30, 90), knn=16, sampler=[16, 16], gnn_layers=2. REQUIRES BLOB REBUILD for spatch_coad_1p (--build-blob-dataset spatch_coad_1p, k=[8, 16, 24] pre-populated)."
        ),
        "patches": [
            "+rvq-both(levels=[30, 90])",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16, 16])",
            "+gnn-l2 (num_layers: 1 -> 2)",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
_patch_dual_codebook_diversity(
_patch_dual_contrastive_cell_within_batch(
    _patch_dual_adj_within_section_only(
        _patch_dual_no_batch_int(
            _patch_dual_batch_lr(
                _patch_dual_attr_recon_weight(
                    _patch_dual_sampler_neighbors(
                        _patch_dual_gnn_layers(
                            _patch_dual_graph_knn(
                                _patch_dual_decoder_width(
                                    _patch_dual_decoupled_encoders(
                                        _patch_dual_encoder_deeper(
                                            _patch_dual_decoder_covariate(
                                                _patch_dual_rvq(
                                                    _patch_dual_rvq(
                                                        _BD(),
                                                        branch="niche", codebook_sizes=(30, 90),
                                                    ),
                                                    branch="cell", codebook_sizes=(30, 90),
                                                ),
                                            ),
                                            hidden_channels=[400, 400, 256],
                                        ),
                                    ),
                                    hidden_channels=[32],
                                ),
                                n_neighs=16,
                            ),
                            num_layers=2,
                        ),
                        num_neighbors=[16, 16],
                    ),
                    weight=1.0,
                ),
                batch_size=512, lr=7e-4,
            ),
        ),
        enabled=True,
    ),
    wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
),
weight=10.0, temperature=100.0, branch="cell",
),
batch_size=512,
),
    },
    # -----------------------------------------------------------------------
    # s52 — niche RVQ L2 codebook-size ablation (3 vals × 5 datasets)
    # -----------------------------------------------------------------------
    # Same s51 spine; ONLY the niche-side RVQ L2 codebook dimension
    # changes (vs cell-side, which s51 ablated). 3 levels: 10 / 30 / 300
    # (default is 90 — that's the s51 base).
    #
    # Submit via:
    #   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v52
    #   bash examples/submit_dataset_sweep.sh chl59-2b_1p-ablations-v52
    #   bash examples/submit_dataset_sweep.sh spatch_ov_1p-ablations-v52
    #   bash examples/submit_dataset_sweep.sh spatch_hcc_1p-ablations-v52
    #   bash examples/submit_dataset_sweep.sh spatch_coad_1p-ablations-v52
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s52 ablation v1 on mmb0-1b_smb1-1b_1p: niche RVQ L2 = 10 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for mmb0-1b_smb1-1b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 10)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 10),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s52 ablation v2 on mmb0-1b_smb1-1b_1p: niche RVQ L2 = 30 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for mmb0-1b_smb1-1b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 30)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 30),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s52 ablation v3 on mmb0-1b_smb1-1b_1p: niche RVQ L2 = 300 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for mmb0-1b_smb1-1b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 300)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
        ],
        "build": lambda: _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 300),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
    },
    # =======================================================================
    # s54 — L0 codebook-size ablation (Fig S3 extension). Complements the
    #       existing L1 sweeps (s51 = cell L1, s52 = niche L1) by varying the
    #       FIRST RVQ level L0 in {10, 90, 300} while holding L1 = 30. Built on
    #       the s51_v1 / s49_v23 spine (WITH contrastive) via
    #       _build_s51_spine_codebook, so each variant differs from the s51_v1
    #       reference ONLY in the codebook size. Axis centres (L0=30 -> (30,30))
    #       reuse existing variants: cell-L0 centre = s51_v5; niche-L0 = s52_v2.
    # =======================================================================
    "s54_v1_dualvq+rvq-cell-10-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: CELL L0 = 10 (vs default 30); cell L1 held at 30 -> cell RVQ = (10, 30). Niche RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH within-batch contrastive wt=10/k=5 + cell-VQ diversity wt=10 + dec-cov + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + gnn=1 + bs=512 + lr=7e-4 + within-sec) — identical to s51_v5 except cell L0. Cell-L0 axis centre (L0=30, cell (30,30)) = existing s51_v5. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(10, 30), rvq-niche(30, 90)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(10, 30), niche_sizes=(30, 90)),
    },
    "s54_v2_dualvq+rvq-cell-90-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: CELL L0 = 90 (vs default 30); cell L1 held at 30 -> cell RVQ = (90, 30). Niche RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH contrastive). Cell-L0 axis centre (L0=30) = existing s51_v5. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(90, 30), rvq-niche(30, 90)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(90, 30), niche_sizes=(30, 90)),
    },
    "s54_v3_dualvq+rvq-cell-300-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: CELL L0 = 300 (vs default 30); cell L1 held at 30 -> cell RVQ = (300, 30). Niche RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH contrastive). Cell-L0 axis centre (L0=30) = existing s51_v5. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(300, 30), rvq-niche(30, 90)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(300, 30), niche_sizes=(30, 90)),
    },
    "s54_v4_dualvq+rvq-cell-30-90+rvq-niche-10-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: NICHE L0 = 10 (vs default 30); niche L1 held at 30 -> niche RVQ = (10, 30). Cell RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH contrastive) — identical to s52_v2 except niche L0. Niche-L0 axis centre (L0=30, niche (30,30)) = existing s52_v2. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(30, 90), rvq-niche(10, 30)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(30, 90), niche_sizes=(10, 30)),
    },
    "s54_v5_dualvq+rvq-cell-30-90+rvq-niche-90-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: NICHE L0 = 90 (vs default 30); niche L1 held at 30 -> niche RVQ = (90, 30). Cell RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH contrastive). Niche-L0 axis centre (L0=30) = existing s52_v2. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(30, 90), rvq-niche(90, 30)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(30, 90), niche_sizes=(90, 30)),
    },
    "s54_v6_dualvq+rvq-cell-30-90+rvq-niche-300-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s54 L0 codebook ablation on mmb0-1b_smb1-1b_1p: NICHE L0 = 300 (vs default 30); niche L1 held at 30 -> niche RVQ = (300, 30). Cell RVQ unchanged at default (30, 90). Built on the s51_v1/s49_v23 spine (WITH contrastive). Niche-L0 axis centre (L0=30) = existing s52_v2. Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
        ),
        "patches": ["= s51_v1 spine (WITH contrastive); rvq-cell(30, 90), rvq-niche(300, 30)"],
        "build": lambda: _build_s51_spine_codebook(cell_sizes=(30, 90), niche_sizes=(300, 30)),
    },
    # =======================================================================
    # s55 — CROSS-BATCH MNN contrastive sweep. The s49_v23 spine with the
    #       within-batch contrastive loss replaced by
    #       contrastive_cell_attribute_cross_batch_mnn_loss: within-batch
    #       NT-Xent (unchanged) + cross-batch mutual-NN pure-attraction term.
    #       Goal: keep cell-type resolution while IMPROVING batch integration
    #       (iLISI/MMD). Sweeps the cross-batch weight (wt_cross), k_cross, and
    #       an MNN similarity floor. Each build wraps the s49_v23 build, so it
    #       is byte-identical to s49_v23 except the contrastive swap. Reference
    #       = s49_v23 (== within-batch only, wt_cross=0). Reuses the s49_v23
    #       blob (mmb0-1b_smb1-1b_1p, k=16) — no rebuild.
    # =======================================================================
    "s55_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+crossmnn-wt1-k1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s55 cross-batch MNN contrastive: s49_v23 spine, within-batch contrastive REPLACED by within-batch NT-Xent (unchanged: wt=10, k_pos=5, T=0.1) + cross-batch mutual-NN pure-attraction (wt_cross=1.0, k_cross=1, mutual). Conservative cross weight. Reference = s49_v23 (wt_cross=0)."
        ),
        "patches": ["= s49_v23 spine; within-batch contrastive -> cross-batch MNN (wt_cross=1.0, k_cross=1)"],
        "build": lambda: _patch_dual_contrastive_cross_batch(
            VARIANTS["s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"]["build"](),
            wt_cross=1.0, k_cross=1,
        ),
    },
    "s55_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+crossmnn-wt5-k1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s55 cross-batch MNN contrastive: s49_v23 spine + cross-batch mutual-NN attraction at wt_cross=5.0, k_cross=1. Mid cross weight."
        ),
        "patches": ["= s49_v23 spine; cross-batch MNN (wt_cross=5.0, k_cross=1)"],
        "build": lambda: _patch_dual_contrastive_cross_batch(
            VARIANTS["s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"]["build"](),
            wt_cross=5.0, k_cross=1,
        ),
    },
    "s55_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s55 cross-batch MNN contrastive: s49_v23 spine + cross-batch mutual-NN attraction at wt_cross=10.0, k_cross=1 (cross weight = within weight). Strong alignment."
        ),
        "patches": ["= s49_v23 spine; cross-batch MNN (wt_cross=10.0, k_cross=1)"],
        "build": lambda: _patch_dual_contrastive_cross_batch(
            VARIANTS["s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"]["build"](),
            wt_cross=10.0, k_cross=1,
        ),
    },
    "s55_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+crossmnn-wt5-k2+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s55 cross-batch MNN contrastive: s49_v23 spine + cross-batch mutual-NN attraction at wt_cross=5.0, k_cross=2 (more cross matches per anchor)."
        ),
        "patches": ["= s49_v23 spine; cross-batch MNN (wt_cross=5.0, k_cross=2)"],
        "build": lambda: _patch_dual_contrastive_cross_batch(
            VARIANTS["s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"]["build"](),
            wt_cross=5.0, k_cross=2,
        ),
    },
    "s55_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+crossmnn-wt2-k1-floor0.5+mmb0-1b_smb1-1b_1p": {
        "description": (
            "s55 cross-batch MNN contrastive: s49_v23 spine + cross-batch mutual-NN attraction at wt_cross=2.0, k_cross=1, mnn_floor=0.5 (only accept high-confidence cross matches, cosine >= 0.5). Safer matching."
        ),
        "patches": ["= s49_v23 spine; cross-batch MNN (wt_cross=2.0, k_cross=1, mnn_floor=0.5)"],
        "build": lambda: _patch_dual_contrastive_cross_batch(
            VARIANTS["s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p"]["build"](),
            wt_cross=2.0, k_cross=1, mnn_floor=0.5,
        ),
    },
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s52 ablation v1 on chl59-2b_1p: niche RVQ L2 = 10 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for chl59-2b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 10)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 10),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s52 ablation v2 on chl59-2b_1p: niche RVQ L2 = 30 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for chl59-2b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 30)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 30),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p": {
        "description": (
            "s52 ablation v3 on chl59-2b_1p: niche RVQ L2 = 300 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for chl59-2b_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 300)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+chl59-2b_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_chl59_2b_1p(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 300),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s52 ablation v1 on spatch_ov_1p: niche RVQ L2 = 10 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_ov_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 10)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 10),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s52 ablation v2 on spatch_ov_1p: niche RVQ L2 = 30 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_ov_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 30)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 30),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p": {
        "description": (
            "s52 ablation v3 on spatch_ov_1p: niche RVQ L2 = 300 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_ov_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 300)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_ov_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_ov(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 300),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s52 ablation v1 on spatch_hcc_1p: niche RVQ L2 = 10 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_hcc_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 10)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 10),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s52 ablation v2 on spatch_hcc_1p: niche RVQ L2 = 30 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_hcc_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 30)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 30),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p": {
        "description": (
            "s52 ablation v3 on spatch_hcc_1p: niche RVQ L2 = 300 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_hcc_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 300)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_hcc_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_hcc(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 300),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s52 ablation v1 on spatch_coad_1p: niche RVQ L2 = 10 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_coad_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 10)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 10),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s52 ablation v2 on spatch_coad_1p: niche RVQ L2 = 30 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_coad_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 30)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 30),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p": {
        "description": (
            "s52 ablation v3 on spatch_coad_1p: niche RVQ L2 = 300 (vs default 90). Cell RVQ unchanged at (30, 90). All other knobs match the s51 base (decoupled-enc + within-batch contrastive wt=10, k=5 + cell-VQ codebook-diversity wt=10 + cell-w=1 + no-batch-int + enc-deeper + dec-w[32] + knn16 + sampler[16] + bs=512 + lr=7e-4 + within-sec). REQUIRES BLOB REBUILD for spatch_coad_1p only if not already built (s51 already provides k=[8, 16, 24] in n_neighs_list for all 5 datasets)."
        ),
        "patches": [
            "+rvq-cell(30, 90), rvq-niche(30, 300)",
            "+decoder_covariate",
            "+no-batch-int",
            "+enc-deeper(mlp=[400, 400, 256])",
            "+decoupled-encoders",
            "+dec-w=[32]",
            "+graph_knn(n_neighs=16)",
            "+sampler([16])",
            "+wt_attr_reconstr=1.0",
            "+batch_size=512",
            "+lr=7e-4",
            "+adj_within_section_only=True",
            "+contrastive-cell-within-batch(wt=10, k_pos=5, T=0.1)",
            "+codebook-diversity(branch=cell, wt=10, T=100)",
            "+spatch_coad_1p(dataset switch)",
        ],
        "build": lambda: _patch_dual_spatch_coad(
            _patch_dual_codebook_diversity(
            _patch_dual_contrastive_cell_within_batch(
                _patch_dual_adj_within_section_only(
                    _patch_dual_no_batch_int(
                        _patch_dual_batch_lr(
                            _patch_dual_attr_recon_weight(
                                _patch_dual_sampler_neighbors(
                                    _patch_dual_graph_knn(
                                        _patch_dual_decoder_width(
                                            _patch_dual_decoupled_encoders(
                                                _patch_dual_encoder_deeper(
                                                    _patch_dual_decoder_covariate(
                                                        _patch_dual_rvq(
                                                            _patch_dual_rvq(_BD(),
                                                                branch="niche", codebook_sizes=(30, 300),
                                                            ),
                                                            branch="cell", codebook_sizes=(30, 90),
                                                        ),
                                                    ),
                                                    hidden_channels=[400, 400, 256],
                                                ),
                                            ),
                                            hidden_channels=[32],
                                        ),
                                        n_neighs=16,
                                    ),
                                    num_neighbors=[16],
                                ),
                                weight=1.0,
                            ),
                            batch_size=512, lr=7e-4,
                        ),
                    ),
                    enabled=True,
                ),
                wt_contrastive_cell=10.0, k_pos=5, temperature=0.1,
            ),
            weight=10.0, temperature=100.0, branch="cell",
        ),
            batch_size=512,
        ),
    },
}


# ---------------------------------------------------------------------------
# Variant-name prefix convention (sweep 17 onward)
# ---------------------------------------------------------------------------
# Going forward, every NEW ablation-set variant is registered under a
# name of the form `s<sweep>_v<variant>_<base>`:
#
#   - `s<sweep>` = the sweep's number, matching the trailing digit(s)
#                  of `DATASET_VARIANTS["mmb...-ablations-vNN"]`.
#                  E.g. `s17_` for the v17 sweep below, `s18_` for
#                  the next one.
#   - `v<variant>` = the variant's INDEX WITHIN the sweep (1-based,
#                    typically 1..8 for an 8-variant sweep). Order
#                    is whatever the sweep author chooses (historic
#                    score, complexity, etc.).
#
# Example:
#   s17_v3_dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p
#   = the 3rd variant in the 17th sweep.
#
# Benefits:
#   1. Findability per sweep:
#        ls <artifacts>/<dataset> | grep '^s17_'
#      lists every artefact dir from sweep 17. The `v<N>` segment
#      preserves within-sweep ordering.
#   2. Re-runnable history — when a top variant is re-used across
#      sweeps (e.g. the sweep 17 below verifying the
#      `max_epochs=80` revert), the prefixed alias keeps its
#      artefacts separate from historical runs of the same model.
#   3. No cross-sweep collisions — `s17_v1_<base>` and
#      `s18_v1_<base>` (even with the same `<base>`) are distinct
#      VARIANTS keys, so re-using a model across sweeps is safe.
#
# Aliases are registered via `_register_alias(new_name, orig_name)`:
# the new key points at the SAME build lambda as the original, so
# the model + config + dataset_tag are byte-identical to the
# original. Only the artefact directory name differs.
#
# Sweeps registered BEFORE sweep 17 (v1..v16, v6b, v11, etc.) keep
# their flat names — renaming them would break every artefact path
# + log file currently on disk.


def _register_alias(new_name: str, orig_name: str) -> None:
    """Register `new_name` as a fresh VARIANTS key sharing the same
    build lambda + description as `orig_name`.

    The new entry is a Python-level alias only — it produces a separate
    artifact directory (`<artifacts>/<dataset>/<new_name>/<TS>/`)
    because `train()` derives the dir name from the variant key.

    No-op if `new_name` already exists (so re-running this module
    doesn't double-register). Raises if `orig_name` doesn't exist —
    typos should fail loudly rather than silently registering nothing.
    """
    if orig_name not in VARIANTS:
        raise KeyError(
            f"_register_alias: original variant {orig_name!r} not in "
            f"VARIANTS. Check the spelling against existing keys."
        )
    if new_name in VARIANTS:
        return  # idempotent
    orig = VARIANTS[orig_name]
    VARIANTS[new_name] = {
        "description": (
            f"Alias of {orig_name!r}. " +
            orig.get("description", "")
        ),
        "patches": list(orig.get("patches", [])),
        "build":   orig["build"],   # same callable; same config every time
    }


# ---- Sweep 17: top-8 niche-NMI variants (max_epochs=80 fix verification) ----
# All 8 are the historic top performers on niche NMI from
# `summary_long.csv` (pre-2026-05-12 timestamps), aliased under
# `s17_v<N>_<orig>` names where N=1..8 is the variant's index within
# the v17 sweep (sorted by historic best score, descending).
#
# Find this sweep's artefacts with:
#   ls <artifacts>/<dataset> | grep '^s17_'
#
# These re-runs verify that reverting `max_epochs` 200 -> 80 actually
# restores the historic 0.66-0.70 niche NMI numbers — if the
# s17_v<N>_* results match the historic numbers, the regression is
# fixed and the rest of v12-v16 can be re-submitted with confidence.
_register_alias(
    "s17_v1_dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v2_dualvq+wide+rvq-both+decoder-cov+adv",
    "dualvq+wide+rvq-both+decoder-cov+adv",
)
_register_alias(
    "s17_v3_dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v4_dualvq+wide+rvq-both+decoder-cov+adv+nbr-hops-3+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+nbr-hops-3+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v5_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v6_dualvq+wide+rvq-both+decoder-cov+adv-w50+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv-w50+adv-warmup10+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v7_dualvq+wide+rvq-both-3level+decoder-cov+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+mmd-w50+mmb0-1b_smb1-1b_1p",
)
_register_alias(
    "s17_v8_dualvq+wide+rvq-both-3level-20-40-80+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level-20-40-80+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
)


# ---- Sweep 20: 1-knob-at-a-time ablation from s17_v2 baseline -------
# Diagnosing the s18 regression. s17_v2 (`+wide+rvq-both+decoder-cov
# +adv`, niche NMI 0.695) → s18 variants (niche NMI 0.57-0.61) changed
# THREE knobs simultaneously:
#   - architecture:  +wide (h=256)            → +small-h32/64
#   - codebook:      (30, 90) 2-level         → (30, 30) or (30, 30, 30)
#   - sampler:       [8] (default)            → [4] in half of s18
#
# s20 holds two knobs at the s17 reference value and varies the third,
# so the per-knob delta is directly readable from the niche NMI of each
# variant. The fourth variant (s20_v1) re-runs the exact s17 baseline
# as a within-sweep sanity check (same hardware, same code as v2/v3/v4).
#
# Interpretation:
#   - If v1 ≈ 0.69 and v2/v3/v4 split evenly below: all 3 knobs matter.
#   - If only one of v2/v3/v4 drops: that's THE culprit; the other two
#     knobs are safe to change in future sweeps.
#   - If v1 itself drops well below 0.69: something else regressed
#     between now and the s17 runs (data, env, ...) — investigate
#     before reading the v2/v3/v4 deltas.
#
# s20_v1 is an alias of the existing baseline; v2/v3/v4 are new builds
# that don't have prior entries in VARIANTS.
_register_alias(
    "s20_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv",
)

VARIANTS["s20_v2_dualvq+wide+rvq-both-3level-30-30-30+decoder-cov+adv+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s20 sweep, variant 2: spine s17_v2 with CODEBOOK swapped from "
        "(30, 90) 2-level monotone-growing to (30, 30, 30) 3-level "
        "no-growth (the codebook shape s18 used). All other knobs "
        "(+wide h=256, default sampler [8], no warmup) match the s17 "
        "reference. Isolates the codebook contribution to the s18 "
        "regression."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 30, 30])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 30, 30),
                ),
                branch="cell", codebook_sizes=(30, 30, 30),
            ),
        ),
        alpha=1.0, wt_adv_batch=150.0,
    ),
}

VARIANTS["s20_v3_dualvq+small-h64+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s20 sweep, variant 3: spine s17_v2 with ARCHITECTURE swapped "
        "from +wide (h=256) to +small-h64 (h=64 for encoder MLP + GNN "
        "+ both decoders). Codebook stays at (30, 90) 2-level, sampler "
        "stays default [8], no warmup. Isolates the encoder-size "
        "contribution to the s18 regression."
    ),
    "patches": [
        "+small(hidden=64)",
        "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_small(_BD(), hidden=64),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
        alpha=1.0, wt_adv_batch=150.0,
    ),
}

VARIANTS["s20_v4_dualvq+wide+rvq-both+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s20 sweep, variant 4: spine s17_v2 with SAMPLER swapped from "
        "default [8] to [4] (half the per-cell neighbors aggregated by "
        "the niche GNN). +wide h=256, codebook (30, 90) 2-level, no "
        "warmup all kept at the s17 reference value. Isolates the "
        "sampler-fanout contribution to the s18 regression."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+sampler([4])",
    ],
    "build": lambda: _patch_dual_sampler_neighbors(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        num_neighbors=[4],
    ),
}

# ---- Sweep 21: batch-integration loss & niche-neck ablation -----------
# Three orthogonal modifications off the s17_v2 spine
# (`+wide+rvq-both+decoder-cov+adv`, niche NMI 0.695):
#
#   v1: baseline (re-runs s17_v2 for within-sweep comparison)
#   v2: drop all batch-integration losses (no adv, no MMD)
#   v3: replace adv with MMD batch integration (w=100)
#   v4: keep adv, but add a niche-neck MLP that decouples the GNN
#       input from z_mlp (cell-VQ still sees z_mlp directly)
#
# v2 + v3 test "does SQUINT need the adversarial loss?" — v2 is the
# pure-encoder ceiling (`+decoder-cov` covariate is the only
# batch-correction signal), v3 swaps the GRL adversary for a kernel-
# based MMD pressure (deterministic per-step, no min-max game).
#
# v4 tests "is the cell-VQ being pulled by niche-side gradients
# through the shared z_mlp?" — the new niche-neck MLP inserts a
# learned transformation between z_mlp and the GNN, leaving z_mlp
# untouched for the cell-VQ. If v4 lifts cell-NMI without hurting
# niche-NMI, the shared trunk WAS the bottleneck.
# (See `_patch_dual_niche_neck` for the full mechanism.)
VARIANTS["s21_v2_dualvq+wide+rvq-both+decoder-cov+no-batch-int+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s21 sweep, variant 2: drop ALL batch-integration losses (no "
        "adversarial CE, no MMD). The decoder-covariate (+decoder-cov) "
        "is the only batch-correction signal — tests whether SQUINT's "
        "NicheCompass-style covariate concat is enough on its own, or "
        "whether an active encoder-side batch-removal pressure is "
        "load-bearing."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+no-batch-int  (drops adv + MMD; adversarial head disabled)",
    ],
    "build": lambda: _patch_dual_no_batch_int(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
    ),
}

# ---- Sweep 22: batch-size × learning-rate grid -----------------------
# Goal: find the right (batch, lr) pair for scaling up SQUINT
# training. The spine is the historic top-niche-NMI variant
# (`+wide+rvq-both+decoder-cov+adv`, niche NMI 0.695 at batch=256 /
# lr=5e-4 / max_epochs=80 — see sweep 17).
#
# Grid (8 variants):
#   v1: batch=256,  lr=5e-4    (baseline — duplicates s17_v2 config)
#   v2: batch=1024, lr=1e-3    (sqrt-scaled to 4x batch)
#   v3: batch=2048, lr=1.4e-3  (sqrt-scaled to 8x batch)
#   v4: batch=4096, lr=2e-3    (sqrt-scaled to 16x batch)
#   v5: batch=1024, lr=2e-3    (linear-scaled to 4x batch)
#   v6: batch=2048, lr=4e-3    (linear-scaled to 8x batch)
#   v7: batch=4096, lr=8e-3    (linear-scaled to 16x batch)
#   v8: batch=256,  lr=1e-3    (lr-only doubling — control: does lr
#                               alone help even without bigger batch?)
#
# Reading the grid:
#   - v1 → v2 → v3 → v4 traces sqrt-scaling at growing batch.
#   - v5/v6/v7 are the linear-scaling counterparts. Compare against
#     v2/v3/v4 to see which scaling rule is safer for SQUINT's
#     adversarial dynamics on this dataset.
#   - v8 isolates "is lr the bottleneck?": same batch as baseline,
#     2x lr.
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v22.sh
VARIANTS["s22_v2_dualvq+wide+rvq-both+decoder-cov+adv+bs1024+lr1e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 2: s17_v2 spine at batch=1024, lr=1e-3 "
        "(sqrt-scaling 4x batch / 2x lr from baseline). Tests whether "
        "the 4x throughput gain comes with no quality loss."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs1024+lr1e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=1024, lr=1e-3,
    ),
}

VARIANTS["s22_v3_dualvq+wide+rvq-both+decoder-cov+adv+bs2048+lr1.4e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 3: s17_v2 spine at batch=2048, lr=1.4e-3 "
        "(sqrt-scaling 8x batch / sqrt(8)x lr from baseline)."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs2048+lr1.4e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=2048, lr=1.4e-3,
    ),
}

VARIANTS["s22_v4_dualvq+wide+rvq-both+decoder-cov+adv+bs4096+lr2e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 4: s17_v2 spine at batch=4096, lr=2e-3 "
        "(sqrt-scaling 16x batch / 4x lr from baseline). Largest "
        "sqrt-scaled point in the grid."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs4096+lr2e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=4096, lr=2e-3,
    ),
}

VARIANTS["s22_v5_dualvq+wide+rvq-both+decoder-cov+adv+bs1024+lr2e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 5: s17_v2 spine at batch=1024, lr=2e-3 "
        "(linear-scaling 4x batch / 4x lr from baseline). Pair with v2 "
        "to see whether linear or sqrt scaling is the right rule for "
        "SQUINT's adversarial dynamics at 4x batch."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs1024+lr2e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=1024, lr=2e-3,
    ),
}

VARIANTS["s22_v6_dualvq+wide+rvq-both+decoder-cov+adv+bs2048+lr4e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 6: s17_v2 spine at batch=2048, lr=4e-3 "
        "(linear-scaling 8x batch / 8x lr). Pair with v3."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs2048+lr4e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=2048, lr=4e-3,
    ),
}

VARIANTS["s22_v7_dualvq+wide+rvq-both+decoder-cov+adv+bs4096+lr8e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 7: s17_v2 spine at batch=4096, lr=8e-3 "
        "(linear-scaling 16x batch / 16x lr — most aggressive). Pair "
        "with v4. Common failure mode: adversarial CE saturates and "
        "the encoder collapses; early stopping fires."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs4096+lr8e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=4096, lr=8e-3,
    ),
}

VARIANTS["s22_v8_dualvq+wide+rvq-both+decoder-cov+adv+bs256+lr1e-3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s22 sweep, variant 8: s17_v2 spine at batch=256, lr=1e-3 "
        "(default batch + 2x lr). Control: does the LR alone help at "
        "the default batch, or is the batch increase needed?"
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+bs256+lr1e-3",
    ],
    "build": lambda: _patch_dual_batch_lr(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        batch_size=256, lr=1e-3,
    ),
}

# s22_v1 is an alias of the s17_v2 baseline — re-runs the (batch=256,
# lr=5e-4) reference on the same hardware + code as v2..v8 so the
# (batch, lr) deltas are read cleanly within the sweep.
_register_alias(
    "s22_v1_dualvq+wide+rvq-both+decoder-cov+adv+bs256+lr5e-4+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv",
)


# ---- Sweep 23: loss-weight grid for batch integration / NMI Pareto ---
# Goal: improve cell_emb + neighborhood_emb iLISI while preserving (or
# improving) the niche / cell-type NMI numbers.
#
# Diagnosis from the s17-s22 result table:
#   - Baseline s17_v2 (`+wide+rvq-both+decoder-cov+adv`):
#       nbr_NMI=0.68, cell_NMI=0.41, cell_iLISI=0.29, nbr_iLISI=0.04
#   - cell_iLISI is decent (the adv head moves the cell embedding)
#     but nbr_iLISI is consistently near zero.
#   - The cosine adjacency BCE (`wt_adj_reconstr=1000`) and the niche
#     NB reconstruction (`wt_attr_reconstr_nbr=1.0`) BOTH anchor
#     `z_gnn` / `z_q_niche` to per-batch spatial + gene statistics,
#     swamping the adversarial signal (alpha=1, wt_adv_batch=150).
#   - The "no-adj" extreme (s19_v2) lifted nbr_iLISI to 0.18 but
#     tanked nbr_NMI 0.68 → 0.46. The "sampler4" extreme (s20_v4)
#     pushed nbr_iLISI to 0.64 but collapsed nbr_NMI to 0.11.
#
# We want the Pareto frontier in between. 16 variants, all on the
# s17_v2 spine, organised in 4 hypothesis groups:
#
#   Group A (v1-v4): adjacency BCE weight sweep
#     v1: adj-w100   v2: adj-w300   v3: adj-w500   v4: adj-w2000
#     Sweep the dominant niche-side anchor. v1-v3 loosen it (expect
#     nbr_iLISI ↑, nbr_NMI ↓ — find the inflexion). v4 is the
#     control: does heavier adj actually help any metric?
#
#   Group B (v5-v8): stronger adversarial pressure
#     v5: alpha=2, wt=300     (≈4× effective encoder gradient)
#     v6: alpha=3, wt=300     (≈6× effective encoder gradient)
#     v7: alpha=5, wt=150     (5× via alpha only)
#     v8: alpha=1, wt=500     (3.3× via wt_adv_batch only)
#     Hold anchors at baseline. Tests how much iLISI you can buy
#     just by turning up the adversarial gradient. Risk: alpha>2
#     historically caused codebook collapse on smaller setups; the
#     wide+rvq spine should tolerate it but watch nbr_NMI.
#
#   Group C (v9-v12): NB reconstruction balance
#     v9:  nbr-w0.3            (3.3× looser niche NB)
#     v10: nbr-w0.5            (2× looser niche NB)
#     v11: nbr-w2.0            (tighter niche NB — control)
#     v12: cell-w0.5 + nbr-w0.5  (loosen BOTH branches)
#     Loosening the gene-anchor frees the latent for integration
#     without dropping the NB term entirely.
#
#   Group D (v13-v16): combined sweet-spot candidates
#     v13: adj-w300 + alpha=2, wt=300
#     v14: adj-w300 + nbr-w0.5
#     v15: adj-w200 + alpha=3, wt=300                  (aggressive)
#     v16: adj-w500 + alpha=2, wt=300 + nbr-w0.7       (balanced)
#     Pareto candidates layering the most promising A/B/C levers.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v23

# --- Group A: adjacency weight sweep --------------------------------

VARIANTS["s23_v1_dualvq+wide+rvq-both+decoder-cov+adv+adj-w100+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 1: s17_v2 spine with wt_adj_reconstr lowered "
        "10× from 1000 → 100. Tests whether dropping the dominant niche-"
        "side anchor lifts nbr_iLISI substantially while keeping nbr_NMI "
        "in the 0.55-0.65 range. Group A: adj-weight Pareto sweep."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w100",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=100.0,
    ),
}

VARIANTS["s23_v2_dualvq+wide+rvq-both+decoder-cov+adv+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 2: s17_v2 spine with wt_adj_reconstr lowered "
        "from 1000 → 300. Mid-point of the Group A sweep — usually the "
        "best-balanced point in a log-spaced reduction. Submit alongside "
        "v1 and v3 to read the iLISI/NMI Pareto."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=300.0,
    ),
}

VARIANTS["s23_v3_dualvq+wide+rvq-both+decoder-cov+adv+adj-w500+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 3: s17_v2 spine with wt_adj_reconstr lowered "
        "from 1000 → 500. Mild reduction — tests whether even a modest "
        "loosening of the spatial anchor lifts nbr_iLISI."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w500",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=500.0,
    ),
}

VARIANTS["s23_v4_dualvq+wide+rvq-both+decoder-cov+adv+adj-w2000+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 4: s17_v2 spine with wt_adj_reconstr DOUBLED "
        "from 1000 → 2000. Control: does heavier spatial anchoring help "
        "any metric, or does it just push nbr_iLISI further toward 0? "
        "s17_v5 (adj-w3000) suggested it doesn't lift NMI either; this "
        "is the gentler upweight."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w2000",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=2000.0,
    ),
}

# --- Group B: stronger adversarial pressure --------------------------

VARIANTS["s23_v5_dualvq+wide+rvq-both+decoder-cov+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 5: s17_v2 spine with adversarial alpha 1→2 "
        "AND wt_adv_batch 150→300 (≈4× effective encoder gradient). "
        "Group B: turn up adversarial pressure without touching the "
        "niche-side anchors. Risk: alpha>=2 historically caused "
        "codebook collapse on h32 setups; the wide+rvq spine should "
        "tolerate it."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
        alpha=2.0, wt_adv_batch=300.0,
    ),
}

VARIANTS["s23_v6_dualvq+wide+rvq-both+decoder-cov+adv-alpha3-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 6: s17_v2 spine with adversarial alpha 1→3 "
        "AND wt_adv_batch 150→300 (≈6× effective encoder gradient). "
        "More aggressive Group B variant. If nbr_iLISI still doesn't "
        "lift here, the adversarial path can't beat the adjacency BCE "
        "alone — combine with Group A reductions in Group D."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=3.0, wt=300.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
        alpha=3.0, wt_adv_batch=300.0,
    ),
}

VARIANTS["s23_v7_dualvq+wide+rvq-both+decoder-cov+adv-alpha5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 7: s17_v2 spine with adversarial alpha 1→5 "
        "(5× via alpha only, wt_adv_batch unchanged at 150). Isolates "
        "the alpha axis from the wt_adv_batch axis. alpha and wt are "
        "multiplicative in the effective encoder gradient — splitting "
        "v5/v6/v7 vs v8 tells us which knob the classifier responds "
        "to more cleanly."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=5.0, wt=150.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
        alpha=5.0, wt_adv_batch=150.0,
    ),
}

VARIANTS["s23_v8_dualvq+wide+rvq-both+decoder-cov+adv-wt500+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 8: s17_v2 spine with wt_adv_batch 150→500 "
        "(3.3× via wt_adv_batch only, alpha unchanged at 1.0). Companion "
        "to v7 — tests the alpha vs wt_adv_batch split. wt_adv_batch "
        "also accelerates the classifier (sharper adversary), so this "
        "variant should give a more confident batch signal back to the "
        "encoder."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=500.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_wide(_BD()),
                    branch="niche", codebook_sizes=(30, 90),
                ),
                branch="cell", codebook_sizes=(30, 90),
            ),
        ),
        alpha=1.0, wt_adv_batch=500.0,
    ),
}

# --- Group C: NB reconstruction balance ------------------------------

VARIANTS["s23_v9_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w0.3+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 9: s17_v2 spine with wt_attr_reconstr_nbr "
        "1.0 → 0.3 (3.3× looser niche NB). Group C: loosen the per-cell "
        "gene-mean anchor on the niche embedding. The niche NB term "
        "lives at ~150 nats and competes with the adjacency BCE for "
        "z_gnn's directionality — dropping it should free the niche "
        "embedding for integration without disabling reconstruction "
        "entirely (which s19_v3 +no-nbr at weight=0 did, with nbr_NMI=0.46)."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+nbr-w0.3",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=0.3,
    ),
}

VARIANTS["s23_v10_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 10: s17_v2 spine with wt_attr_reconstr_nbr "
        "1.0 → 0.5 (2× looser niche NB). Gentler than v9 — tests the "
        "Group C inflexion point. If both v9 and v10 lift nbr_iLISI "
        "without losing nbr_NMI, weight=0.5 is the safer publication-"
        "ready setting."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s23_v11_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w2.0+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 11: s17_v2 spine with wt_attr_reconstr_nbr "
        "1.0 → 2.0 (2× tighter niche NB). Group C control — does "
        "anchoring the niche embedding HARDER to per-batch gene means "
        "help nbr_NMI, or just push nbr_iLISI lower? Likely the latter, "
        "but worth confirming."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+nbr-w2.0",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=2.0,
    ),
}

VARIANTS["s23_v12_dualvq+wide+rvq-both+decoder-cov+adv+cell-w0.5+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 12: s17_v2 spine with BOTH NB reconstruction "
        "weights halved (wt_attr_reconstr 1.0→0.5 AND wt_attr_reconstr_nbr "
        "1.0→0.5). Tests symmetric loosening — frees both cell and niche "
        "embeddings for integration. Combine with high adversarial in "
        "Group D if v12 lifts iLISI on both."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+cell-w0.5", "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_attr_recon_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=0.5,
        ),
        weight=0.5,
    ),
}

# --- Group D: combined sweet-spot candidates -------------------------

VARIANTS["s23_v13_dualvq+wide+rvq-both+decoder-cov+adj-w300+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 13 (Pareto candidate 1): adj-w300 (Group A "
        "winner candidate) + alpha=2 wt=300 (Group B v5). Layers the "
        "best two single-axis levers together. Hypothesis: relaxed "
        "adjacency lets adversarial actually move the niche embedding. "
        "Expect biggest nbr_iLISI lift in the sweep."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=2.0, wt_adv_batch=300.0,
        ),
        weight=300.0,
    ),
}

VARIANTS["s23_v14_dualvq+wide+rvq-both+decoder-cov+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 14 (Pareto candidate 2): adj-w300 (Group A) "
        "+ nbr-w0.5 (Group C v10). Two independent niche-side "
        "loosenings, default adversarial. Tests whether you can lift "
        "nbr_iLISI without pushing adversarial hard — gentler choice "
        "if v13's adv=2 destabilises training."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300", "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=300.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s23_v15_dualvq+wide+rvq-both+decoder-cov+adj-w200+adv-alpha3-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 15 (Pareto candidate 3, aggressive): adj-w200 "
        "(5× looser than baseline) + alpha=3 wt=300 (≈6× adv gradient). "
        "Maximum integration pressure short of full adversarial collapse. "
        "Expect highest nbr_iLISI of the sweep; if nbr_NMI survives "
        "above 0.55 this is the win."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=3.0, wt=300.0)",
        "+adj-w200",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=3.0, wt_adv_batch=300.0,
        ),
        weight=200.0,
    ),
}

VARIANTS["s23_v16_dualvq+wide+rvq-both+decoder-cov+adj-w500+adv-alpha2-wt300+nbr-w0.7+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s23 sweep, variant 16 (Pareto candidate 4, balanced): adj-w500 "
        "(2× looser) + alpha=2 wt=300 (≈4× adv) + nbr-w0.7 (slightly "
        "looser niche NB). All three levers nudged gently rather than "
        "any pushed hard. If the s17_v2 → s23_v* improvements are "
        "broadly distributed across levers, v16 should be the "
        "publication-ready point: stable training, modest gains on "
        "every axis."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
        "+adj-w500", "+nbr-w0.7",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_wide(_BD()),
                            branch="niche", codebook_sizes=(30, 90),
                        ),
                        branch="cell", codebook_sizes=(30, 90),
                    ),
                ),
                alpha=2.0, wt_adv_batch=300.0,
            ),
            weight=500.0,
        ),
        weight=0.7,
    ),
}


# ---- Sweep 24: scVI-style tight bottleneck architecture -----------
# Hypothesis: SQUINT's current latent dim (256) gives the decoder
# enough capacity to absorb per-batch noise even with `+decoder-cov`,
# so the encoder doesn't NEED to produce batch-invariant z. Tightening
# the bottleneck (scVI uses 10-30; we test 16/32/64) removes the
# decoder's escape hatch — the encoder is forced to discard batch-
# specific features to keep reconstruction quality. Side benefit:
# smaller latent → sharper cosine-VQ geometry → cleaner codes.
#
# Architecture (s24_v1 reference):
#   - Encoder MLP:    [128, 32]    (1 hidden, output 32 latent)
#   - GNN:            SAGE h=32, 1 layer  (already the dual base)
#   - Cell decoder:   [128]         (1 hidden, mirror of encoder)
#   - Niche decoder:  [128]         (1 hidden, mirror of encoder)
#   - Codebook:       RVQ 3-level (30, 20, 10) on BOTH branches
#                      → 30 × 20 × 10 = 6000 distinct composite codes
#                      per branch (vs the s17 spine's 2700)
#   - Decoder covariate: learned embedding, embed_dim=32
#                       (up from default 16; matches latent dim)
#
# Compared to the s17_v2 spine (256-dim latent, [400, 400] decoders,
# RVQ (30, 90)), this is ~10× fewer parameters in the encoder + decoder
# trunk — a real bet on "smaller helps".
#
# 12 variants in 3 groups of 4:
#
#   Group A (v1-v4): architecture / codebook variations
#     v1: the reference (latent=32, RVQ (30,20,10))
#     v2: latent=16  (even tighter — closer to scVI's z=10)
#     v3: latent=64  (looser — does the bottleneck need to be THIS tight?)
#     v4: latent=32 + bigger codebook RVQ (50, 30, 20)
#
#   Group B (v5-v8): batch integration on the tight architecture
#     v5: + adv (alpha=1, wt=150)   (canonical adv on tight arch)
#     v6: + adv (alpha=2, wt=300)   (stronger adv from s23 group B)
#     v7: + mmd-w200                 (MMD only, no adv — see whether
#                                     MMD finally works on small latent)
#     v8: + adv + mmd-w100           (combined batch-int signal)
#
#   Group C (v9-v12): adv + the best s23-flavoured loss-weight levers
#     v9:  + adv + adj-w300         (s23 winner candidate)
#     v10: + adv + nbr-w0.5         (loosened niche NB)
#     v11: + adv + adj-w300 + nbr-w0.5   (combined)
#     v12: + adv (alpha=2, wt=300) + adj-w300  (stronger adv + relaxed adj)
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v24

# --- Group A: architecture / codebook variations ---------------------

VARIANTS["s24_v1_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 1 (REFERENCE): scVI-style tight architecture. "
        "Encoder MLP [128, 32], GNN SAGE h=32 / 1 layer, both decoders "
        "[128] (single hidden, mirror geometry). Codebook RVQ 3-level "
        "(30, 20, 10) on both branches = 6000 composite codes. Decoder "
        "covariate embed_dim=32 (matches latent dim, up from default 16). "
        "NO batch-integration loss yet (groups B/C add adv/mmd on top)."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
    ],
    "build": lambda: _patch_dual_decoder_covariate(
        _patch_dual_rvq(
            _patch_dual_rvq(
                _patch_dual_scvi_compact(
                    _BD(),
                    encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                ),
                branch="niche", codebook_sizes=(30, 20, 10),
            ),
            branch="cell", codebook_sizes=(30, 20, 10),
        ),
        embed_dim=32,
    ),
}

VARIANTS["s24_v2_dualvq+compact-l16+rvq-both-30-20-10+decoder-cov-e16+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 2: even tighter latent = 16 (closer to "
        "scVI's z_dim ≈ 10). Encoder MLP [128, 16], GNN h=16 / 1L, "
        "both decoders [128]. RVQ (30, 20, 10) on both. Decoder "
        "covariate embed_dim=16 (matches new latent). Tests how far "
        "the bottleneck can be pushed before reconstruction NMI breaks."
    ),
    "patches": [
        "+compact(encoder=[128,16], decoder=[128], gnn=16/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=16)",
    ],
    "build": lambda: _patch_dual_decoder_covariate(
        _patch_dual_rvq(
            _patch_dual_rvq(
                _patch_dual_scvi_compact(
                    _BD(),
                    encoder_hidden=128, latent_dim=16, decoder_hidden=128,
                ),
                branch="niche", codebook_sizes=(30, 20, 10),
            ),
            branch="cell", codebook_sizes=(30, 20, 10),
        ),
        embed_dim=16,
    ),
}

VARIANTS["s24_v3_dualvq+compact-l64+rvq-both-30-20-10+decoder-cov-e32+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 3: looser latent = 64. Encoder MLP [128, 64], "
        "GNN h=64 / 1L, both decoders [128]. RVQ (30, 20, 10) on both. "
        "Decoder covariate embed_dim=32 (kept). Mid-ground between the "
        "s17 spine (256) and the v1 reference (32) — does a 4× looser "
        "bottleneck recover any signal that v1 may sacrifice?"
    ),
    "patches": [
        "+compact(encoder=[128,64], decoder=[128], gnn=64/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
    ],
    "build": lambda: _patch_dual_decoder_covariate(
        _patch_dual_rvq(
            _patch_dual_rvq(
                _patch_dual_scvi_compact(
                    _BD(),
                    encoder_hidden=128, latent_dim=64, decoder_hidden=128,
                ),
                branch="niche", codebook_sizes=(30, 20, 10),
            ),
            branch="cell", codebook_sizes=(30, 20, 10),
        ),
        embed_dim=32,
    ),
}

VARIANTS["s24_v4_dualvq+compact-l32+rvq-both-50-30-20+decoder-cov-e32+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 4: reference latent=32 but BIGGER codebook "
        "RVQ (50, 30, 20) = 30000 composite codes (5× v1's 6000). Tests "
        "whether the tight latent + richer codebook combination gives "
        "more discrete capacity without re-introducing batch leakage. "
        "Pair-compare with v1 to read 'does code count matter at this "
        "geometry?'."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[50, 30, 20])",
        "+decoder_covariate(embed_dim=32)",
    ],
    "build": lambda: _patch_dual_decoder_covariate(
        _patch_dual_rvq(
            _patch_dual_rvq(
                _patch_dual_scvi_compact(
                    _BD(),
                    encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                ),
                branch="niche", codebook_sizes=(50, 30, 20),
            ),
            branch="cell", codebook_sizes=(50, 30, 20),
        ),
        embed_dim=32,
    ),
}

# --- Group B: batch integration on the tight architecture ------------

VARIANTS["s24_v5_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 5: v1 reference + canonical adversarial "
        "head (alpha=1, wt=150). Tests whether the tight bottleneck "
        "AMPLIFIES adversarial integration (less capacity to ignore "
        "the adversarial gradient) or starves it (too few dims to "
        "spread batch information across)."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_scvi_compact(
                        _BD(),
                        encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                    ),
                    branch="niche", codebook_sizes=(30, 20, 10),
                ),
                branch="cell", codebook_sizes=(30, 20, 10),
            ),
            embed_dim=32,
        ),
        alpha=1.0, wt_adv_batch=150.0,
    ),
}

VARIANTS["s24_v6_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 6: v1 reference + stronger adversarial "
        "(alpha=2, wt=300; ≈4× effective encoder gradient). Mirrors "
        "s23_v5 on the new tight architecture. If the small latent + "
        "strong adv combination is the publication target, v6 is the "
        "first place it'd land."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_scvi_compact(
                        _BD(),
                        encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                    ),
                    branch="niche", codebook_sizes=(30, 20, 10),
                ),
                branch="cell", codebook_sizes=(30, 20, 10),
            ),
            embed_dim=32,
        ),
        alpha=2.0, wt_adv_batch=300.0,
    ),
}

VARIANTS["s24_v7_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+mmd-w200+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 7: v1 reference + MMD-only batch integration "
        "(wt_mmd_batch=200, no adversarial head). On the s17 spine, MMD "
        "underperformed adv on iLISI; tests whether the smaller latent "
        "lets MMD's distribution-matching pressure actually reach the "
        "encoder (deterministic per-step signal, no min-max instability)."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+mmd-w200",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_decoder_covariate(
            _patch_dual_rvq(
                _patch_dual_rvq(
                    _patch_dual_scvi_compact(
                        _BD(),
                        encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                    ),
                    branch="niche", codebook_sizes=(30, 20, 10),
                ),
                branch="cell", codebook_sizes=(30, 20, 10),
            ),
            embed_dim=32,
        ),
        wt_mmd_batch=200.0,
    ),
}

VARIANTS["s24_v8_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+mmd-w100+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 8: v1 reference + adv (alpha=1, wt=150) + "
        "MMD-w100. Combined adversarial + MMD batch-integration on the "
        "tight architecture. The two pressures are different in nature "
        "(min-max game vs deterministic distribution match) — stacking "
        "may give a more stable integration signal than either alone."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+mmd-w100",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_scvi_compact(
                            _BD(),
                            encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                        ),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
                embed_dim=32,
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        wt_mmd_batch=100.0,
    ),
}

# --- Group C: adv + the best s23-flavoured loss-weight levers --------

VARIANTS["s24_v9_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 9: v5 (tight + adv) + adj-w300 (the s23 "
        "winner candidate). Combines the new architecture with the "
        "best-bet loss weight from s23. Expect highest nbr_iLISI of "
        "the s24 sweep — small latent CAN'T memorise batch info, AND "
        "the niche embedding is no longer over-anchored to spatial "
        "structure."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_scvi_compact(
                            _BD(),
                            encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                        ),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
                embed_dim=32,
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=300.0,
    ),
}

VARIANTS["s24_v10_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 10: v5 (tight + adv) + nbr-w0.5 (loosened "
        "niche NB anchor). Tests whether the tight-latent setup needs "
        "MORE niche-NB looseness or LESS (the small bottleneck already "
        "limits how much per-batch noise the niche embedding can carry)."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_scvi_compact(
                            _BD(),
                            encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                        ),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
                embed_dim=32,
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s24_v11_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 11: v5 (tight + adv) + adj-w300 + nbr-w0.5. "
        "Stacks the two s23 loss-weight winners on the tight architecture. "
        "Most aggressive Pareto candidate of the s24 sweep — if v11 "
        "wins, this is the publication-ready config."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300", "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_rvq(
                        _patch_dual_rvq(
                            _patch_dual_scvi_compact(
                                _BD(),
                                encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                            ),
                            branch="niche", codebook_sizes=(30, 20, 10),
                        ),
                        branch="cell", codebook_sizes=(30, 20, 10),
                    ),
                    embed_dim=32,
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=300.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s24_v12_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv-alpha2-wt300+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s24 sweep, variant 12: v6 (tight + stronger adv) + adj-w300. "
        "Like v11 but trades the niche-NB loosening for a stronger "
        "adversarial gradient. Tests whether the small latent makes "
        "the higher-alpha regime tractable (on the s17 spine alpha>=2 "
        "previously caused codebook collapse; the rvq-both spine "
        "stabilised it — the tight version should be even more stable)."
    ),
    "patches": [
        "+compact(encoder=[128,32], decoder=[128], gnn=32/1L)",
        "+rvq(branch=both, levels=[30, 20, 10])",
        "+decoder_covariate(embed_dim=32)",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_scvi_compact(
                            _BD(),
                            encoder_hidden=128, latent_dim=32, decoder_hidden=128,
                        ),
                        branch="niche", codebook_sizes=(30, 20, 10),
                    ),
                    branch="cell", codebook_sizes=(30, 20, 10),
                ),
                embed_dim=32,
            ),
            alpha=2.0, wt_adv_batch=300.0,
        ),
        weight=300.0,
    ),
}


# ---- Sweep 25: tight architecture (enc/dec=[128], GATv2, RVQ-3L) ----
# Hypothesis: the historic `+wide+rvq-both-3level+decoder-cov+adv
# +enc-shallow` variant gave GOOD niche / cell NMI but WEAK batch
# integration. Tighten the architecture further (symmetric 128-dim
# latents on both branches) and swap SAGE → GATv2; vary the
# integration approach to find a config that lifts iLISI without
# sacrificing the NMI win.
#
# Architecture (all 8 variants share this spine):
#   - Encoder MLP:    [128]  (1 hidden of 128) via _patch_dual_encoder_shallow
#   - GNN:            GATv2, hidden=128, **1 layer** (via +wide with
#                      gnn_layers=1 override, +gatv2 for type,
#                      +gnn-hidden(128) for width)
#   - Codebook:       RVQ 3-level (30, 60, 120) on both branches
#                      = 216k composite codes per branch
#   - Decoder:        [128]  (1 hidden of 128) on BOTH cell and niche
#                      via _patch_dual_decoder_width
#   - Decoder cov:    learned embedding, embed_dim=16
#   - Sampler:        [8] (from +wide override, matches 1-layer GNN)
#   - Nbr-agg hops:   1 (from +wide override)
#
# Both branches end up with SYMMETRIC 128-dim latents (z_mlp=128,
# z_gnn=128). Compared to the historic "+enc-shallow" variant (which
# had encoder=[256], decoder=[400,400], GNN=SAGE h=256), this is
# strictly tighter on every dim AND swaps the message-passing op to
# GATv2.
#
# 8 variants vary ONLY the batch-integration approach. The spine is
# tightened to maximise the latent's batch-invariance pressure, then
# we sweep integration knobs to find where iLISI lifts without
# breaking the NMI win.
#
#   v1: +adv (canonical, α=1.0, wt=150)    — baseline integration
#   v2: +adv-alpha2-wt300 (≈4× encoder grad) — stronger adv
#   v3: +mmd-w200 (MMD only, no adv)        — non-adversarial path
#   v4: +adv + +mmd-w100 (combined)          — stacked pressure
#   v5: +adv + +adj-w300 (relaxed adj BCE)   — loosen niche-side anchor
#   v6: +adv + +nbr-w0.5 (relaxed niche NB)  — loosen reconstruction anchor
#   v7: +adv + +adj-w300 + +nbr-w0.5         — multi-axis relax
#   v8: +adv-alpha2-wt300 + +adj-w300        — strong adv + relaxed adj
#
# Reading the grid:
#   - v1 establishes the new spine's baseline. Compare to historic
#     "+enc-shallow" to see whether the GATv2 + tighter dims alone
#     already help integration.
#   - v2 / v3 / v7 / v8 test pure integration pressure increases.
#   - v5 / v6 isolate niche-side anchor loosening (the s23 levers).
#   - v4 / v7 stack multiple knobs for a Pareto candidate.
#
# What to watch:
#   - nbr_NMI / cell_NMI: shouldn't fall much below the historic
#     "+enc-shallow" levels — if they do, the new spine is too tight.
#   - nbr_iLISI / cell_iLISI: the headline metric this sweep is
#     trying to lift. Historic enc-shallow was weak here.
#   - Pearson NB recon: secondary; the tight latent + tight decoder
#     may hurt fine-grained gene reconstruction.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v25

# Shared spine for every s25 variant. The build chain runs:
#   _BD() → +wide → rvq(niche, [30,60,120]) → rvq(cell, [30,60,120])
#         → +gatv2 → gnn_hidden(128) → enc_shallow([128])
#         → decoder_width([128]) → decoder_covariate (default embed=16)
# Then each variant wraps THIS with its integration-knob patches.

VARIANTS["s25_v1_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 1: tight architecture (enc=[128], "
        "dec=[128], GATv2 GNN h=128 2L, RVQ 3-level (30,60,120)) + "
        "canonical adversarial (alpha=1, wt=150). Establishes the "
        "baseline on the new spine — directly compare to the historic "
        "'+enc-shallow' variant to see whether GATv2 + tighter dims "
        "alone help integration."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        alpha=1.0, wt_adv_batch=150.0,
    ),
}

VARIANTS["s25_v2_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 2: same tight spine as v1, but with "
        "stronger adversarial (alpha=2, wt=300; ≈4× effective "
        "encoder gradient). Tests whether the tighter latent makes "
        "the higher-alpha regime tractable (alpha>=2 previously "
        "caused codebook collapse on smaller setups; the rvq-3level "
        "spine should stabilise it)."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        alpha=2.0, wt_adv_batch=300.0,
    ),
}

VARIANTS["s25_v3_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 3: same tight spine as v1, but with "
        "MMD-only batch integration (wt_mmd_batch=200, no "
        "adversarial head). Tests whether the smaller latent lets "
        "MMD's distribution-matching pressure actually reach the "
        "encoder (deterministic per-step signal, no min-max "
        "instability)."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+mmd-w200",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        wt_mmd_batch=200.0,
    ),
}

VARIANTS["s25_v4_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+mmd-w100+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 4: same tight spine as v1, with adv + MMD-w100 "
        "STACKED. Tests whether combining the two distinct integration "
        "pressures (min-max GRL + deterministic distribution match) "
        "gives a stronger combined effect than either alone."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+mmd-w100",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        wt_mmd_batch=100.0,
    ),
}

VARIANTS["s25_v5_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 5: same tight spine as v1 + canonical adv + "
        "adj-w300 (cosine adjacency BCE weight reduced from default "
        "1000 → 300). Loosens the niche-side spatial anchor — frees "
        "the niche embedding for integration without disabling the "
        "adjacency loss entirely."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=300.0,
    ),
}

VARIANTS["s25_v6_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 6: same tight spine as v1 + canonical adv + "
        "nbr-w0.5 (niche NB attribute-reconstruction weight reduced "
        "from default 1.0 → 0.5). Loosens the per-cell gene-mean "
        "anchor on the niche embedding — complementary to v5's "
        "adjacency reduction."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s25_v7_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 7: same tight spine as v1 + canonical adv + "
        "BOTH niche-side relaxations from v5 + v6 (adj-w300 + nbr-w0.5). "
        "Multi-axis loosening — the strongest niche-side relaxation in "
        "this sweep, pairs naturally with the default-strength adv."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+adj-w300", "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_shallow(
                            _patch_dual_gnn_hidden(
                                _patch_dual_gatv2(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                            branch="niche", codebook_sizes=(30, 60, 120),
                                        ),
                                        branch="cell", codebook_sizes=(30, 60, 120),
                                    ),
                                ),
                                hidden=128,
                            ),
                            hidden_channels=[128],
                        ),
                        hidden_channels=[128],
                    ),
                ),
                alpha=1.0, wt_adv_batch=150.0,
            ),
            weight=300.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s25_v8_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt300+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s25 sweep, variant 8: same tight spine as v1 + stronger adv "
        "(alpha=2, wt=300) + adj-w300. Combines the strongest "
        "integration pressure (v2-style adv) with niche-side anchor "
        "relaxation (v5-style adj). If the high-alpha regime doesn't "
        "collapse on this tight spine, v8 is the aggressive Pareto "
        "candidate."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=300.0)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=2.0, wt_adv_batch=300.0,
        ),
        weight=300.0,
    ),
}


# ---- Sweep 26: s25 with WEAK adversarial (wt=15, warmup=5) ----------
# Mirror of the s25 sweep with two changes baked into every variant
# that uses adversarial:
#   - `wt_adv_batch`: 150 → 15 (10× smaller). Same magnitude regime as
#     the discriminator's natural CE scale (log(2) ≈ 0.69 nats); pulls
#     the encoder back from the aggressive 150-weighted gradient that
#     dominated reconstruction.
#   - `warmup_epochs`: 0 → 5. The GRL alpha is zero for the first 5
#     epochs so cell + niche codes settle on biology before any
#     batch-invariance pressure kicks in. The discriminator still
#     trains during warmup; only the encoder is shielded.
#
# For variants that were "stronger adv" in s25 (v2 with α=2/wt=300,
# v8 with α=2/wt=300), we scale proportionally → α=2, wt=30 here
# (still 10× smaller than the s25 counterpart). v3 (MMD-only, no
# adversarial head at all) is unchanged from s26's perspective.
#
# Hypothesis: s25's adversarial weight may have been too aggressive on
# the tight architecture — pulling the encoder too hard toward batch
# invariance and starving the reconstruction signal. Gentler adv +
# warmup should let the model establish a good NB-reconstruction
# regime first, then apply mild batch-invariance pressure on top.
#
# Same shared spine as s25:
#   GATv2 GNN h=128, 1 layer + enc MLP [128] + dec [128] + RVQ 3L
#   (30, 60, 120) + decoder-cov + sampler [8] + nbr-agg hops 1.
#
# 8 variants, mirroring s25's integration-knob structure:
#   v1: +adv(α=1, wt=15, warmup=5)              ← baseline weak adv
#   v2: +adv(α=2, wt=30, warmup=5)               ← still-weak stronger adv
#   v3: +mmd-w200                                ← unchanged from s25_v3
#   v4: +adv(weak) + +mmd-w100                   ← stacked weak adv + mmd
#   v5: +adv(weak) + +adj-w300                   ← weak adv + relaxed adj
#   v6: +adv(weak) + +nbr-w0.5                   ← weak adv + relaxed niche NB
#   v7: +adv(weak) + +adj-w300 + +nbr-w0.5       ← multi-axis with weak adv
#   v8: +adv(α=2, wt=30, warmup=5) + +adj-w300   ← stronger-weak adv + relaxed adj
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v26

VARIANTS["s26_v1_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 1: same tight spine as s25_v1, but with "
        "WEAK adversarial (α=1, wt=15, warmup=5). 10× smaller "
        "wt_adv_batch + 5-epoch GRL warmup. Establishes the new "
        "weak-adv baseline — compare to s25_v1 to see whether "
        "gentler adv preserves NMI better."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=15.0, warmup=5)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        alpha=1.0, wt_adv_batch=15.0, warmup_epochs=5,
    ),
}

VARIANTS["s26_v2_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt30-warmup5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 2: same tight spine + α=2, wt=30, "
        "warmup=5. Mirror of s25_v2 but with adversarial weight 10× "
        "smaller. Tests whether the higher-alpha regime is more "
        "tractable when the loss-weight magnitude is gentler."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=30.0, warmup=5)",
    ],
    "build": lambda: _patch_dual_adversarial(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        alpha=2.0, wt_adv_batch=30.0, warmup_epochs=5,
    ),
}

VARIANTS["s26_v3_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 3: same tight spine + MMD-only (w=200, no "
        "adversarial head). Identical to s25_v3 since the weak-adv "
        "rescaling doesn't apply to the MMD-only path. Included for "
        "sweep completeness — direct cross-reference for the "
        "non-adversarial path on this spine."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+mmd-w200",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_decoder_covariate(
            _patch_dual_decoder_width(
                _patch_dual_encoder_shallow(
                    _patch_dual_gnn_hidden(
                        _patch_dual_gatv2(
                            _patch_dual_rvq(
                                _patch_dual_rvq(
                                    _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                    branch="niche", codebook_sizes=(30, 60, 120),
                                ),
                                branch="cell", codebook_sizes=(30, 60, 120),
                            ),
                        ),
                        hidden=128,
                    ),
                    hidden_channels=[128],
                ),
                hidden_channels=[128],
            ),
        ),
        wt_mmd_batch=200.0,
    ),
}

VARIANTS["s26_v4_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+mmd-w100+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 4: same tight spine + weak adv (α=1, "
        "wt=15, warmup=5) + MMD-w100. Stacks the two distinct "
        "integration pressures at gentler magnitudes than s25_v4."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=15.0, warmup=5)",
        "+mmd-w100",
    ],
    "build": lambda: _patch_dual_mmd_batch(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=15.0, warmup_epochs=5,
        ),
        wt_mmd_batch=100.0,
    ),
}

VARIANTS["s26_v5_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 5: same tight spine + weak adv (α=1, "
        "wt=15, warmup=5) + adj-w300. Mirror of s25_v5 with gentler "
        "adv — frees the niche side via adj relaxation while keeping "
        "encoder pressure mild."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=15.0, warmup=5)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=15.0, warmup_epochs=5,
        ),
        weight=300.0,
    ),
}

VARIANTS["s26_v6_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 6: same tight spine + weak adv + "
        "nbr-w0.5. Mirror of s25_v6 with gentler adv."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=15.0, warmup=5)",
        "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=1.0, wt_adv_batch=15.0, warmup_epochs=5,
        ),
        weight=0.5,
    ),
}

VARIANTS["s26_v7_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 7: same tight spine + weak adv + "
        "BOTH adj-w300 and nbr-w0.5. Mirror of s25_v7 with gentler "
        "adv — multi-axis relaxation paired with mild encoder "
        "pressure."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=15.0, warmup=5)",
        "+adj-w300", "+nbr-w0.5",
    ],
    "build": lambda: _patch_dual_attr_recon_nbr_weight(
        _patch_dual_adj_weight(
            _patch_dual_adversarial(
                _patch_dual_decoder_covariate(
                    _patch_dual_decoder_width(
                        _patch_dual_encoder_shallow(
                            _patch_dual_gnn_hidden(
                                _patch_dual_gatv2(
                                    _patch_dual_rvq(
                                        _patch_dual_rvq(
                                            _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                            branch="niche", codebook_sizes=(30, 60, 120),
                                        ),
                                        branch="cell", codebook_sizes=(30, 60, 120),
                                    ),
                                ),
                                hidden=128,
                            ),
                            hidden_channels=[128],
                        ),
                        hidden_channels=[128],
                    ),
                ),
                alpha=1.0, wt_adv_batch=15.0, warmup_epochs=5,
            ),
            weight=300.0,
        ),
        weight=0.5,
    ),
}

VARIANTS["s26_v8_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt30-warmup5+adj-w300+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s26 sweep, variant 8: same tight spine + α=2, wt=30, "
        "warmup=5 + adj-w300. Mirror of s25_v8 with adversarial "
        "weight 10× smaller — combines stronger-but-still-gentle adv "
        "with adj relaxation. Aggressive Pareto candidate among the "
        "weak-adv variants."
    ),
    "patches": [
        "+wide(gnn_layers=1)", "+rvq(branch=both, levels=[30, 60, 120])",
        "+gatv2", "+gnn_hidden(128)",
        "+enc-shallow(mlp=[128])", "+dec-w=[128]",
        "+decoder_covariate",
        "+adversarial_batch(alpha=2.0, wt=30.0, warmup=5)",
        "+adj-w300",
    ],
    "build": lambda: _patch_dual_adj_weight(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_decoder_width(
                    _patch_dual_encoder_shallow(
                        _patch_dual_gnn_hidden(
                            _patch_dual_gatv2(
                                _patch_dual_rvq(
                                    _patch_dual_rvq(
                                        _patch_dual_wide(_BD(), gnn_layers=1, sampler_neighbors=[8], nbr_hops=1),
                                        branch="niche", codebook_sizes=(30, 60, 120),
                                    ),
                                    branch="cell", codebook_sizes=(30, 60, 120),
                                ),
                            ),
                            hidden=128,
                        ),
                        hidden_channels=[128],
                    ),
                    hidden_channels=[128],
                ),
            ),
            alpha=2.0, wt_adv_batch=30.0, warmup_epochs=5,
        ),
        weight=300.0,
    ),
}


VARIANTS["s21_v4_dualvq+wide+rvq-both+decoder-cov+adv+niche-neck+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s21 sweep, variant 4: spine s17_v2 + niche-neck MLP. A new "
        "256-d projection layer is inserted between z_mlp and the GNN: "
        "the cell-VQ still consumes z_mlp directly, but the GNN sees "
        "niche_neck(z_mlp). Niche-side gradients (adj-BCE, nbr-NB, "
        "niche-commit) flow back into this NEW tensor, not into z_mlp "
        "— so the cell quantiser is structurally protected from being "
        "pulled by the niche objective. If cell-NMI lifts without "
        "niche-NMI dropping, the shared z_mlp was a real bottleneck."
    ),
    "patches": [
        "+wide", "+rvq(branch=both, levels=[30, 90])",
        "+decoder_covariate",
        "+adversarial_batch(alpha=1.0, wt=150.0)",
        "+niche_neck(hidden_channels=[256])",
    ],
    "build": lambda: _patch_dual_niche_neck(
        _patch_dual_adversarial(
            _patch_dual_decoder_covariate(
                _patch_dual_rvq(
                    _patch_dual_rvq(
                        _patch_dual_wide(_BD()),
                        branch="niche", codebook_sizes=(30, 90),
                    ),
                    branch="cell", codebook_sizes=(30, 90),
                ),
            ),
            alpha=1.0, wt_adv_batch=150.0,
        ),
        hidden_channels=[256],
    ),
}

# s21_v1 + s21_v3 are aliases of existing variants (s17_v2 spine + the
# already-registered +mmd-w100 variant). Re-running them under the
# s21_ prefix gives within-sweep sanity-check artefacts on the same
# hardware + code as v2 + v4, without duplicating the build chains.
_register_alias(
    "s21_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv",
)
_register_alias(
    "s21_v3_dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
)


# ---------------------------------------------------------------------------
# Per-dataset ablation sweep matrix (10 variants per dataset).
# ---------------------------------------------------------------------------
# A consistent 10-axis ablation sweep registered for each of the 3 datasets.
# Run one dataset's sweep per HPC job (one per node) via:
#
#   python examples/run_squint.py --all-dataset mmb0-1b_smb1-1b_1p
#   python examples/run_squint.py --all-dataset chl59-8b_1p
#   python examples/run_squint.py --all-dataset mmb0-1b_smb1-20b_1p
#
# Each variant ablates ONE axis from a fixed spine baseline. The spine is:
#   wide (or narrow on memory-constrained datasets) + RVQ on both branches
#   (k1=30, k2=90) + NicheCompass-style decoder covariate (concat per-cell
#   batch one-hot) + adversarial GRL on FULL z_mlp (alpha=1.0, wt=150).
#
# For wide-spine datasets the 10 axes are:
#   1. baseline       — pure spine
#   2. gatv2          — SAGEConv -> GATv2Conv
#   3. small          — scvi-style hidden=128 everywhere (replaces wide)
#   4. small-latent   — latent / codebook-embedding dim 256 -> 128
#   5. dropout        — p=0.1 on encoder MLP, GNN, both decoders
#   6. dec-film       — FiLM-in-decoder INSTEAD of decoder covariate concat
#   7. no-adv         — drop the adversarial head
#   8. no-arch        — drop +wide (1-hop GNN, default codebook)
#   9. no-rvq         — single VQ k=30 on both branches (no RVQ)
#  10. no-adj         — drop the cosine adjacency BCE loss
#
# For the narrow (memory-constrained) mmb20 dataset, axis 8 changes from
# 'no-arch' to 'codebook-large' (RVQ levels 50/150 instead of 30/90),
# because a wider GNN OOMs at 19 sections on a 44 GiB GPU.

_SWEEP_AXES_WIDE = [
    "baseline", "gatv2", "small", "small-latent", "dropout",
    "dec-film", "no-adv", "no-arch", "no-rvq", "no-adj",
]
_SWEEP_AXES_NARROW = [
    "baseline", "gatv2", "small", "small-latent", "dropout",
    "dec-film", "no-adv", "codebook-large", "no-rvq", "no-adj",
]

_SWEEP_AXIS_DESC = {
    "baseline": (
        "Spine baseline (no ablation). Wide-or-narrow GNN + RVQ on both "
        "branches (k1=30, k2=90) + NicheCompass-style decoder covariate "
        "(concat per-cell batch one-hot) + adversarial GRL on FULL z_mlp "
        "(alpha=1.0, wt_adv_batch=150). Reference point for the 10-axis "
        "sweep."
    ),
    "gatv2": (
        "Spine + SAGEConv -> GATv2Conv on the niche-branch GNN. Tests "
        "per-edge attention vs. uniform mean aggregation. Helps more on "
        "sparse / heterogeneous neighbourhoods."
    ),
    "small": (
        "scvi-style compact architecture: 1 hidden layer of 128 dim "
        "everywhere (encoder MLP, GNN, both decoders). Latent / codebook "
        "embedding dim = 128. ~5x fewer parameters than the SQUINT default. "
        "Replaces +wide (which sets 2-layer GNN at 256 dim). RVQ + "
        "decoder-cov + adv unchanged. Tests whether smaller-is-better "
        "transfers to spatial VQ-VAE."
    ),
    "small-latent": (
        "Spine + latent / codebook-embedding dim 256 -> 128. Encoder MLP "
        "first layer stays at 400; only the last hidden layer + GNN hidden "
        "shrink. Tests whether forcing more semantically-concentrated "
        "codebook embeddings helps."
    ),
    "dropout": (
        "Spine + dropout p=0.1 on encoder MLP, GNN, and both decoders. "
        "Cheap regularization. Useful diagnostic when val Pearson plateaus "
        "while train Pearson keeps climbing."
    ),
    "dec-film": (
        "Spine but FiLM batch-correction INSIDE the cell + niche decoders "
        "(`apply_conditioning='in-MLP'`, condition=cell_batch_id) INSTEAD "
        "of concat-based decoder covariate. FiLM gives the decoder a "
        "per-layer modulation surface; recommended over concat for the "
        "deeper non-linear MLPSoftmax decoders."
    ),
    "no-adv": (
        "Spine WITHOUT the adversarial head. Decoder still gets the "
        "covariate. Tests whether the GRL is contributing batch invariance "
        "beyond what concat covariate alone achieves."
    ),
    "no-arch": (
        "Spine WITHOUT +wide (so 1-hop GNN, sampler=[8], nbr_hops=1, "
        "default codebook size). Tests whether the deeper spatial context "
        "from +wide is load-bearing once RVQ is in place."
    ),
    "no-rvq": (
        "Spine WITHOUT RVQ — single VQ at k=30 on both branches instead "
        "of residual (30, 90). Tests whether the hierarchical RVQ "
        "structure helps over a flat codebook of comparable budget."
    ),
    "no-adj": (
        "Spine WITHOUT the cosine adjacency BCE loss. Niche branch is "
        "supervised only by neighbourhood NB. Tests whether the "
        "adjacency term is load-bearing for spatial code coherence."
    ),
    "codebook-large": (
        "Spine but RVQ levels 30,90 -> 50,150 on both branches (larger "
        "primary + larger residual codebooks). Single coupled "
        "codebook-capacity scan; substituted for +wide on narrow "
        "(memory-constrained) datasets where a 2-hop GNN OOMs."
    ),
}


def _compose_sweep_spine(ablation: str, narrow: bool = False) -> dict:
    """
    Build the dual-VQ spine for the per-dataset ablation sweep, ablating
    exactly one axis. See the SWEEP_AXES tables for the axis catalogue.

    The dataset wrapper (e.g. _patch_dual_chl59_lung5,
    _patch_dual_mmb20_holdout) is applied AFTER this returns — those
    patches are not the responsibility of this function.
    """
    cfg = _BD()

    # Architecture (wide vs. narrow vs. small).
    if ablation == "small":
        # 'small' replaces both +wide AND the default-1-hop GNN (it sets
        # num_layers=1, hidden=128 directly). On narrow datasets the
        # dataset patch later re-asserts num_layers=1 (no conflict).
        cfg = _patch_dual_small(cfg)
    elif ablation == "no-arch":
        # No architectural patch: defaults to 1-hop GNN, default codebook.
        pass
    elif not narrow:
        cfg = _patch_dual_wide(cfg)
    # narrow + non-small: defer GNN config to the dataset wrapper (e.g.
    # _patch_dual_mmb20 sets num_layers=1, sampler=[8], nbr_hops=1).

    # VQ structure.
    if ablation == "no-rvq":
        # Single VQ at base k=30 on both branches — no RVQ wrap.
        pass
    elif ablation == "codebook-large":
        cfg = _patch_dual_rvq(cfg, branch="niche", codebook_sizes=(50, 150))
        cfg = _patch_dual_rvq(cfg, branch="cell",  codebook_sizes=(50, 150))
    else:
        cfg = _patch_dual_rvq(cfg, branch="niche", codebook_sizes=(30, 90))
        cfg = _patch_dual_rvq(cfg, branch="cell",  codebook_sizes=(30, 90))

    # Latent dim shrink.
    if ablation == "small-latent":
        cfg = _patch_dual_small_latent(cfg, latent_dim=128)

    # Dropout.
    if ablation == "dropout":
        cfg = _patch_dual_dropout(cfg, p=0.1)

    # GNN type.
    if ablation == "gatv2":
        cfg = _patch_dual_gatv2(cfg)

    # Decoder batch correction shape.
    if ablation == "dec-film":
        cfg = _patch_dual_decoder_film(cfg)
    else:
        cfg = _patch_dual_decoder_covariate(cfg)

    # Adversarial head.
    if ablation != "no-adv":
        cfg = _patch_dual_adversarial(cfg, alpha=1.0, wt_adv_batch=150.0)

    # Adjacency.
    if ablation == "no-adj":
        cfg = _patch_dual_no_adj(cfg)

    return cfg


def _sweep_variant_name(ablation: str, dataset_tag: str, narrow: bool) -> str:
    """Compose a descriptive variant name for the sweep matrix."""
    arch = "narrow" if narrow else "wide"
    if ablation == "baseline":
        core = f"{arch}+rvq-both+decoder-cov+adv"
    elif ablation == "gatv2":
        core = f"{arch}+rvq-both+decoder-cov+adv+gatv2"
    elif ablation == "small":
        # 'small' overrides architecture; prefix narrow-dataset names with
        # 'narrow+' so the dataset context stays visible.
        core = f"{'narrow+' if narrow else ''}small+rvq-both+decoder-cov+adv"
    elif ablation == "small-latent":
        core = f"{arch}+rvq-both+decoder-cov+adv+small-latent"
    elif ablation == "dropout":
        core = f"{arch}+rvq-both+decoder-cov+adv+dropout"
    elif ablation == "dec-film":
        core = f"{arch}+rvq-both+dec-film+adv"
    elif ablation == "no-adv":
        core = f"{arch}+rvq-both+decoder-cov"
    elif ablation == "no-arch":
        core = f"rvq-both+decoder-cov+adv"
    elif ablation == "no-rvq":
        core = f"{arch}+decoder-cov+adv"
    elif ablation == "no-adj":
        core = f"{arch}+rvq-both+decoder-cov+adv+no-adj"
    elif ablation == "codebook-large":
        core = f"{arch}+rvq-both-large+decoder-cov+adv"
    else:
        raise ValueError(f"unknown sweep ablation: {ablation!r}")
    return f"dualvq+{core}+{dataset_tag}"


def _sweep_patch_tags(ablation: str, narrow: bool) -> list[str]:
    """Patch-tag list for the variant's `patches` field (display only)."""
    arch_tag = "+narrow" if narrow else "+wide"
    rvq_tag = "+rvq(branch=both, levels=[30, 90])"
    base = [arch_tag, rvq_tag, "+decoder_covariate",
            "+adversarial_batch(alpha=1.0, wt=150.0)"]
    if ablation == "baseline":              return list(base)
    if ablation == "gatv2":                 return list(base) + ["+gatv2"]
    if ablation == "small":
        return ["+small(hidden=128 — encoder MLP + GNN + both decoders)", rvq_tag, "+decoder_covariate",
                "+adversarial_batch(alpha=1.0, wt=150.0)"]
    if ablation == "small-latent":          return list(base) + ["+small-latent(latent_dim=128)"]
    if ablation == "dropout":               return list(base) + ["+dropout(p=0.1)"]
    if ablation == "dec-film":
        return [arch_tag, rvq_tag, "+dec-film(in-MLP, cell_batch_id)",
                "+adversarial_batch(alpha=1.0, wt=150.0)"]
    if ablation == "no-adv":                return [arch_tag, rvq_tag, "+decoder_covariate"]
    if ablation == "no-arch":               return [rvq_tag, "+decoder_covariate",
                                                   "+adversarial_batch(alpha=1.0, wt=150.0)"]
    if ablation == "no-rvq":                return [arch_tag, "+decoder_covariate",
                                                   "+adversarial_batch(alpha=1.0, wt=150.0)"]
    if ablation == "no-adj":                return list(base) + ["-cosine_adj"]
    if ablation == "codebook-large":
        return [arch_tag, "+rvq(branch=both, levels=[50, 150])",
                "+decoder_covariate", "+adversarial_batch(alpha=1.0, wt=150.0)"]
    raise ValueError(f"unknown sweep ablation: {ablation!r}")


def _register_dataset_sweep(
        dataset_tag: str,
        dataset_apply_fn,
        narrow: bool = False,
    ) -> list[str]:
    """
    Register the 10-axis sweep variants for a dataset under VARIANTS.

    Idempotent: if a variant with the composed name already exists in
    VARIANTS (e.g. user-curated entries higher up in this file), the
    existing entry is kept and only the name is appended to the returned
    list. This lets the sweep matrix coexist with the hand-written
    variants without clobbering them.

    Returns the ordered list of variant names — used by run_dataset_pipeline.
    """
    axes = _SWEEP_AXES_NARROW if narrow else _SWEEP_AXES_WIDE
    names: list[str] = []
    for ablation in axes:
        name = _sweep_variant_name(ablation, dataset_tag, narrow)
        if name not in VARIANTS:
            # Bind ablation+narrow into closures so each variant builds its own.
            def _build(_a=ablation, _n=narrow, _apply=dataset_apply_fn):
                return _apply(_compose_sweep_spine(_a, narrow=_n))
            VARIANTS[name] = {
                "description": (
                    _SWEEP_AXIS_DESC[ablation]
                    + f" Dataset: {dataset_tag}."
                ),
                "patches": _sweep_patch_tags(ablation, narrow)
                           + [f"+{dataset_tag}"],
                "build": _build,
            }
        names.append(name)
    return names


# ===========================================================================
# s56 — ENCODER CELL/NICHE COUPLING-METHOD ablation (on the cross-batch MNN
# spine). How should the cell-intrinsic and spatial-niche encoders SHARE
# parameters so they "work well together" without spatial losses corrupting
# the cell codes? We sweep a spectrum from full sharing to full decoupling,
# plus three learned/elegant coupling methods drawn from the multi-task
# literature (Misra 2016 cross-stitch; Duong 2015 / Yang & Hospedales 2017
# soft sharing; Yu 2020-style stop-gradient). All built on the s55_v3
# cross-batch-MNN spine, so they're directly comparable to the rest.
#
# Endpoints already defined/run:
#   * DECOUPLED (reference) = s55_v3   (two independent MLP trunks)
#   * COUPLED               = s56_v1   (one shared MLP trunk)
# Partial sharing (Y-shape: shared trunk + per-branch heads, concat back to
# 256-d so codebook/decoder shapes are unchanged):
#   * s56_v2  shared 192 / path 64  (mostly shared)
#   * s56_v3  shared 128 / path 128 (balanced)
#   * s56_v4  shared 64  / path 192 (mostly specialised)
# Learned / elegant coupling:
#   * s56_v5  cross-stitch, one 2x2 mix (4 scalars; Misra 2016)
#   * s56_v6  cross-stitch, per-channel 2x2 (Misra 2016)
#   * s56_v7  stop-gradient on a coupled trunk (gradient-level coupling)
#   * s56_v8  soft parameter-sharing L2 penalty between decoupled trunks
# ===========================================================================
_S56_SPINE_KEY = ("s55_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper"
                  "+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec"
                  "+decoupled-enc+diversity-w10+contrastWB-w10-k5"
                  "+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p")


def _s56_spine():
    """Fresh build of the s55_v3 cross-batch-MNN spine (decoupled encoders),
    rebuilt each call so the coupling patches mutate an independent cfg."""
    return VARIANTS[_S56_SPINE_KEY]["build"]()


VARIANTS["s56_v1_coupled-enc+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s56 coupling ablation on the CROSS-BATCH MNN spine: COUPLED encoder "
        "(cell + niche share ONE MLP trunk; hard parameter sharing) vs the "
        "decoupled default (s55_v3). Built by wrapping the s55_v3 build with "
        "_patch_dual_coupled_encoders, which removes the separate niche-side MLP "
        "(niche_mlp_params) so spatial-loss gradients flow back into the cell-VQ "
        "input. Everything else (cross-batch MNN wt_cross=10/k_cross=1, codebooks, "
        "graph, losses) is identical to s55_v3. Reference (decoupled) = s55_v3. "
        "Reuses the mmb0-1b_smb1-1b_1p blob (k=16)."
    ),
    "patches": ["= s55_v3 (cross-batch spine) + decoupled -> COUPLED encoder"],
    "build": lambda: _patch_dual_coupled_encoders(_s56_spine()),
}

# ---- Partial sharing: Y-shape (shared trunk + per-branch heads) ------------
# `_patch_dual_shared_specific_encoders` builds 3 MLPs (shared + cell-path +
# niche-path) and concatenates [shared, path] for each branch. We vary the
# split point via shared_last_dim / path_last_dim so the concatenated dim
# stays 256 (= the s55_v3 trunk output), keeping codebook + decoder shapes
# unchanged. Higher shared_last_dim = more sharing (closer to s56_v1 coupled);
# higher path_last_dim = more specialisation (closer to s55_v3 decoupled).
_S56_YSHAPE = [
    ("v2", "yshape-shared192-path64",  192, 64,  "mostly shared"),
    ("v3", "yshape-shared128-path128", 128, 128, "balanced"),
    ("v4", "yshape-shared64-path192",  64,  192, "mostly specialised"),
]
for _v, _tag, _sd, _pd, _desc in _S56_YSHAPE:
    VARIANTS[f"s56_{_v}_{_tag}+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s56 coupling ablation (cross-batch MNN spine): partial sharing via "
            f"the Y-shape encoder ({_desc}) — a shared MLP trunk (last dim {_sd}) "
            f"plus per-branch path MLPs (last dim {_pd}), concatenated to "
            f"{_sd + _pd}-d for each branch (= the s55_v3 trunk width, so codebook "
            f"and decoder shapes are unchanged). The shared trunk sees gradients "
            f"from BOTH branches; each path MLP sees only its own branch. "
            f"wt_cross=10/k_cross=1 cross-batch MNN as default. Reference = s55_v3."
        ),
        "patches": [
            f"= s55_v3 spine + Y-shape encoder (shared_last={_sd}, path_last={_pd})",
        ],
        "build": (lambda sd=_sd, pd=_pd: _patch_dual_shared_specific_encoders(
            _s56_spine(), shared_last_dim=sd, path_last_dim=pd)),
    }

# ---- Learned coupling: cross-stitch (Misra et al., CVPR 2016) --------------
VARIANTS["s56_v5_crossstitch-scalar+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s56 coupling ablation (cross-batch MNN spine): CROSS-STITCH coupling "
        "(Misra et al. 2016) with ONE learnable 2x2 mixing matrix (4 scalars) "
        "shared across all channels, mixing the cell- and niche-branch post-MLP "
        "latents before the cell-VQ / GNN split. Spans the shared-to-decoupled "
        "spectrum and learns the optimal mix from data; near-identity init "
        "(0.9 / 0.1). Built on the decoupled s55_v3 spine. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cross-stitch (per_channel=False)"],
    "build": lambda: _patch_dual_cross_stitch(_s56_spine(), per_channel=False),
}
VARIANTS["s56_v6_crossstitch-perchannel+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s56 coupling ablation (cross-batch MNN spine): CROSS-STITCH coupling "
        "(Misra et al. 2016) with an INDEPENDENT 2x2 mix per feature channel "
        "(4 x dim scalars) — the per-unit form closest to the original paper. "
        "More expressive than the scalar variant (s56_v5). Built on the decoupled "
        "s55_v3 spine. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cross-stitch (per_channel=True)"],
    "build": lambda: _patch_dual_cross_stitch(_s56_spine(), per_channel=True),
}

# ---- Gradient-level coupling: stop-gradient on a coupled trunk -------------
VARIANTS["s56_v7_stopgrad-coupled+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s56 coupling ablation (cross-batch MNN spine): STOP-GRADIENT coupling. "
        "One physical shared MLP trunk (coupled), but the niche-path input is "
        "detached so spatial-loss gradients never reach the trunk that produces "
        "the cell-VQ input — the trunk is trained purely by the cell objective, "
        "while the GNN / niche VQ / niche decoder learn on top of the detached "
        "features. Cheapest cell-code protection while physically sharing the "
        "encoder (one .detach()). Reference = s55_v3 (decoupled), contrast vs "
        "s56_v1 (coupled, no detach)."
    ),
    "patches": ["= s55_v3 spine + COUPLED trunk + detach_gnn_input=True"],
    "build": lambda: _patch_dual_stopgrad_coupled(_s56_spine()),
}

# ---- Soft parameter sharing: L2 penalty between decoupled trunks -----------
VARIANTS["s56_v8_softcouple-w1e-3+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s56 coupling ablation (cross-batch MNN spine): SOFT parameter sharing "
        "(Duong 2015 / Yang & Hospedales 2017). Two fully decoupled cell + niche "
        "MLP trunks (as in s55_v3) PLUS an L2 penalty on the distance between "
        "their matched weights (wt_encoder_coupling=1e-3), encouraging the trunks "
        "to stay similar without physically sharing parameters — the lightest-touch "
        "coupling. NOTE: the penalty sums over all MLP weights, so the 1e-3 weight "
        "is a starting point that may need tuning. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine (decoupled) + wt_encoder_coupling=1e-3"],
    "build": lambda: _patch_dual_soft_coupling(_s56_spine(), wt=1e-3),
}


# ===========================================================================
# s57 — ALL ablations re-run on the CROSS-BATCH MNN contrastive spine.
# The s57 "default" contrastive loss is the cross-batch MNN variant
# (wt_cross=10, k_cross=1 == the s55_v3 config), replacing the within-batch
# contrastive used by the s51/s52/s54 ablations. Each s57 variant is generated
# by wrapping the corresponding within-batch ablation variant's build with
# _patch_dual_contrastive_cross_batch (swaps within-batch -> cross-batch MNN),
# so it stays in sync with the source variants.
#
# NOT generated (reused as-is):
#   * cross-batch reference (s49_v23 + cross)  = s55_v3 (already defined)
#   * contrastive-axis comparators             = s51_v1 (within) + s51_v2 (none)
#   * coupled/decoupled axis                   = s55_v3 (decoupled) + s56_v1 (coupled)
# ===========================================================================
_S57_CROSS = dict(wt_cross=10.0, k_cross=1)
# (s57 key tag, source-variant key PREFIX) — source resolved to its full mmb key.
_S57_SPEC = [
    ("no-adj",           "s51_v3_"),    # adjacency reconstruction: drop
    ("no-dec-cov",       "s51_v4_"),    # decoder covariate: drop
    ("gnn-l2",           "s51_v11_"),   # GNN depth: 2 layers
    ("knn8",             "s51_v8_"),    # neighbours: knn8 / sampler8
    ("knn16-sampler8",   "s51_v9_"),    # neighbours: knn16 / sampler8
    ("knn24",            "s51_v10_"),   # neighbours: knn24
    ("rvq-cell-30-10",   "s51_v6_"),    # cell codebook L1=10
    ("rvq-cell-30-30",   "s51_v5_"),    # cell codebook L1=30
    ("rvq-cell-30-300",  "s51_v7_"),    # cell codebook L1=300
    ("rvq-niche-30-10",  "s52_v1_"),    # niche codebook L1=10
    ("rvq-niche-30-30",  "s52_v2_"),    # niche codebook L1=30
    ("rvq-niche-30-300", "s52_v3_"),    # niche codebook L1=300
    ("rvq-cell-10-30",   "s54_v1_"),    # cell codebook L0=10
    ("rvq-cell-90-30",   "s54_v2_"),    # cell codebook L0=90
    ("rvq-cell-300-30",  "s54_v3_"),    # cell codebook L0=300
    ("rvq-niche-10-30",  "s54_v4_"),    # niche codebook L0=10
    ("rvq-niche-90-30",  "s54_v5_"),    # niche codebook L0=90
    ("rvq-niche-300-30", "s54_v6_"),    # niche codebook L0=300
]


def _s57_resolve_source(prefix):
    cands = [k for k in VARIANTS
             if k.startswith(prefix) and k.endswith("+mmb0-1b_smb1-1b_1p")]
    if len(cands) != 1:
        print(f"[s57] WARNING: prefix {prefix!r} matched {len(cands)} variant(s) "
              f"{cands}; skipping that s57 entry.")
        return None
    return cands[0]


for _i, (_tag, _pref) in enumerate(_S57_SPEC, start=1):
    _src = _s57_resolve_source(_pref)
    if _src is None:
        continue
    _key = f"s57_v{_i}_filmscale+{_tag}+mmb0-1b_smb1-1b_1p"
    VARIANTS[_key] = {
        "description": (
            f"s57 ablation on the NEW DEFAULT spine (cross-batch MNN + cell-cond "
            f"niche FiLM scale-only, == the s57_v19 reference / s62_v5). This "
            f"variant = source '{_src}' rebuilt on the FiLM-scale default (wraps "
            f"the source build with _patch_dual_squint_default), so it ABLATES the "
            f"FiLM model (no decoupled confound). Reuses the mmb0-1b_smb1-1b_1p "
            f"blob (k=16). s57 reference = s57_v19 (un-ablated FiLM)."
        ),
        "patches": [
            f"= {_pref}<src> + cross-batch MNN + cell-cond FiLM scale-only",
        ],
        "build": (lambda src=_src: _patch_dual_squint_default(
            VARIANTS[src]["build"]())),
    }


# ---------------------------------------------------------------------------
# s57 SELF-CONTAINED PAPER SET (FiLM-scale DEFAULT) — fold the reference, the
# contrastive axis, the coupling-mechanism comparison, the two-separate-models
# baseline, and the continuous-vs-VQ ablation into the s57 namespace, so running
# ONLY `s57_v*` yields the complete paper set. The DEFAULT / reference model is
# now cell-cond FiLM scale-only (NOT decoupled): the component ablations (v1-v18)
# are rebuilt on the FiLM spine, so they ablate the FiLM model. Builds resolve
# sources LAZILY at submit time. Keep the source *definitions* in this file
# (building blocks); only RUN `s57_v*`.
#   v19 REFERENCE = FiLM scale-only (cross-MNN + cell-cond FiLM scale; == s62_v5)
#   v20 contrastive within-batch + FiLM ;  v21 contrastive none + FiLM
#   v22 coupled ; v23 cross-stitch ; v24 coupled+affine ; v25 decoupled
#       (coupling-axis comparators vs the FiLM reference)
#   v26/v27 two separate models: cell-only / niche-only (reviewer baseline)
#   v28 continuous-latent ; v29 plain discrete VQ ref (the continuous-vs-VQ pair;
#       both within-batch, no FiLM — discretization is a VQ-vs-no-VQ question)
# ---------------------------------------------------------------------------
def _s57_src_build(prefix):
    """Lazy: build the unique mmb0-1b_smb1-1b_1p variant matching `prefix`."""
    return VARIANTS[_s57_resolve_source(prefix)]["build"]()

_S57_EXTRA = [
    # (vN, tag, build-thunk)
    (19, "reference-filmscale",
         lambda: _patch_dual_squint_default(_s57_src_build("s51_v1_"))),
    (20, "contrast-within-batch+film",
         lambda: _patch_dual_cell_conditioned_niche(
             _s57_src_build("s51_v1_"), mode="film_scale")),
    (21, "contrast-none+film",
         lambda: _patch_dual_cell_conditioned_niche(
             _s57_src_build("s51_v2_"), mode="film_scale")),
    (22, "coupling-coupled",      lambda: _s57_src_build("s56_v1_")),
    (23, "coupling-crossstitch",  lambda: _s57_src_build("s56_v5_")),
    (24, "coupling-affine",       lambda: _s57_src_build("s59_v4_")),
    (25, "coupling-decoupled",    lambda: _s57_src_build("s55_v3_")),
    (26, "twomodel-cell-only",    lambda: _s57_src_build("s61_v1_")),
    (27, "twomodel-niche-only",   lambda: _s57_src_build("s61_v2_")),
    (28, "continuous-latent",     lambda: _s57_src_build("s53_v1_")),
    (29, "discrete-vq-ref",       lambda: _s57_src_build("s49_v23_")),
    # Residual-VQ depth ablation (reviewer: "no validation of RVQ, L=1 vs L=2").
    # Default = L=2 residual (30,90) == the s57_v19 reference. These are L=1
    # single-level VQ at 3 sizes (on the FiLM-scale default spine):
    #   v30 capacity-matched K=2700 (=30x90 effective)
    #   v31 parameter-matched K=120 (=30+90 codebook vectors)
    #   v32 drop-the-level    K=30  (just the coarse L0)
    (30, "vq1level-k2700", lambda: _patch_dual_vq(
        _patch_dual_squint_default(_s57_src_build("s51_v1_")),
        branch="both", codebook_size=2700)),
    (31, "vq1level-k120", lambda: _patch_dual_vq(
        _patch_dual_squint_default(_s57_src_build("s51_v1_")),
        branch="both", codebook_size=120)),
    (32, "vq1level-k30", lambda: _patch_dual_vq(
        _patch_dual_squint_default(_s57_src_build("s51_v1_")),
        branch="both", codebook_size=30)),
    # Continuous counterpart of the HEADLINE reference (s57_v19): identical
    # config (FiLM-scale coupling + cross-batch MNN contrastive on the s51_v1
    # spine) with the discrete ResidualVQ bottleneck swapped for a continuous
    # passthrough (ContinuousVQ) on BOTH branches. This is the matched-pair
    # continuous comparator for the discretization ablation — it isolates
    # discreteness alone (everything else = headline), unlike s57_v28 which is
    # continuous on a plain within-batch/no-FiLM spine.
    (33, "continuous-ref-filmscale", lambda: _patch_dual_continuous_vq(
        _patch_dual_squint_default(_s57_src_build("s51_v1_")),
        branch="both")),
]
for _v, _tag, _bld in _S57_EXTRA:
    VARIANTS[f"s57_v{_v}_{_tag}+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s57 self-contained paper set ({_tag}) — FiLM-scale default; "
            f"reference = s57_v19 (cell-cond FiLM scale-only)."
        ),
        "patches": [f"= s57 extra: {_tag}"],
        "build": _bld,
    }


# ===========================================================================
# s65 — EMA-decay (beta) SENSITIVITY SWEEP on the headline reference (s57_v19).
# (s64 is already taken by the squint_hln hyperparameter sweep.)
# Reviewer: "EMA decay beta = 0.8 still asserted, not swept ... a short beta
# sweep would preempt the obvious question." Each variant is the s57_v19
# FiLM-scale reference build (cross-batch MNN + cell-cond FiLM scale-only on the
# s51_v1 RVQ(30,90) spine) with ONLY the codebook EMA decay changed on BOTH
# branches. beta=0.8 IS the reference itself (s57_v19) — NOT re-registered here,
# so the sweep is {0.5, 0.7, 0.8(=ref), 0.9, 0.95, 0.99}. Evaluate on CODEBOOK
# HEALTH (active-code fraction / perplexity / dead codes via
# report_codebook_usage.py --all-seeds) + the usual downstream NMI/ARI/iLISI/MMD.
# ===========================================================================
def _patch_dual_ema_decay(cfg: dict, decay: float) -> dict:
    """Override the codebook EMA decay (beta) on BOTH VQ branches (cell + niche).

    Touches ONLY the `decay` key of `vq_cell_params` / `vq_niche_params` — NOT
    the optimizer `weight_decay` or any loss-schedule decay. Raises if neither
    branch config carries a `decay` key (i.e. not a dual VQ config)."""
    enc = cfg["model"]["encoder_params"]
    n_set = 0
    for _k in ("vq_cell_params", "vq_niche_params"):
        vp = enc.get(_k)
        if isinstance(vp, dict) and "decay" in vp:
            vp["decay"] = float(decay)
            n_set += 1
    if n_set == 0:
        raise ValueError(
            "_patch_dual_ema_decay: no vq_cell_params/vq_niche_params with a "
            "'decay' key found — is this a dual VQ config?")
    return cfg


_S65_EMADECAY = [
    # (vN, slug, beta) — beta=0.8 omitted (== the s57_v19 reference, already run)
    (1, "0p5",  0.5),
    (2, "0p7",  0.7),
    (3, "0p9",  0.9),
    (4, "0p95", 0.95),
    (5, "0p99", 0.99),
]
for _v, _slug, _beta in _S65_EMADECAY:
    VARIANTS[f"s65_v{_v}_emadecay-{_slug}+reference-filmscale+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s65 EMA-decay sensitivity sweep (beta={_beta}) on the s57_v19 "
            f"FiLM-scale reference — codebook-health robustness. beta=0.8 = the "
            f"reference (s57_v19), not re-run here."
        ),
        "patches": [f"= s57_v19 reference + codebook EMA decay beta={_beta}"],
        # default-arg `b=_beta` binds the loop value at def time (avoids the
        # late-binding closure bug); lambdas are lazy so `_patch_dual_squint_default`
        # / `_s57_src_build` (defined below/above) resolve fine at build time.
        "build": (lambda b=_beta: _patch_dual_ema_decay(
            _patch_dual_squint_default(_s57_src_build("s51_v1_")), b)),
    }


# ===========================================================================
# s63 — ALL ablations on the NEW DEFAULT spine = cross-batch-MNN + cell-cond
# niche FiLM scale-only (the coupling that improved spatial-domain integration,
# = s62_v5). Mirrors s57 but adds `_patch_dual_cell_conditioned_niche(mode=
# 'film_scale')` on top. Lets us show every ablation still holds once FiLM
# scale-only is adopted as the default cell/niche coupling. Reuses _S57_SPEC.
# s63 reference (un-ablated default) = s62_v5.
# NOTE: large re-run (18 x 5 = 90 jobs). RECOMMENDED to first replicate the
# FiLM scale-only integration gain on a 2nd dataset before committing this.
# ===========================================================================
def _patch_dual_squint_default(cfg: dict) -> dict:
    """The NEW SQUINT default coupling = cross-batch MNN contrastive + cell-cond
    niche FiLM scale-only, applied on a within-batch decoupled source."""
    return _patch_dual_cell_conditioned_niche(
        _patch_dual_contrastive_cross_batch(cfg, **_S57_CROSS),
        mode="film_scale",
    )


for _i, (_tag, _pref) in enumerate(_S57_SPEC, start=1):
    _src = _s57_resolve_source(_pref)
    if _src is None:
        continue
    _key = f"s63_v{_i}_filmscale+crossmnn-wt10-k1+{_tag}+mmb0-1b_smb1-1b_1p"
    VARIANTS[_key] = {
        "description": (
            f"s63 ablation on the NEW DEFAULT spine (cross-batch MNN + cell-cond "
            f"niche FiLM scale-only, == s62_v5). This variant = source '{_src}' "
            f"with the within-batch contrastive swapped for cross-batch MNN AND "
            f"cell-cond FiLM scale-only added (wraps the source build with "
            f"_patch_dual_squint_default). Reuses the mmb0-1b_smb1-1b_1p blob. "
            f"s63 reference = s62_v5."
        ),
        "patches": [
            f"= {_pref}<src> + cross-batch MNN + cell-cond FiLM scale-only",
        ],
        "build": (lambda src=_src: _patch_dual_squint_default(
            VARIANTS[src]["build"]())),
    }


# ===========================================================================
# s64 — squint_hln DIAGNOSTIC sweep (single CosMx section, 19,718 cells; SMALL).
# Both cell & niche resolution are poor on HLN even with correct labels/counts.
# The reference config is MERFISH-mouse-tuned: bs=512 gives only ~38 steps/epoch
# on this tiny single section (under-trains the codebook), and wt_adj_reconstr
# defaults to 1000 (cranked up to balance ~150-nat MOUSE gene-NB losses) which
# likely DOMINATES the loss on a small dense CosMx graph. This sweep perturbs
# the small-data-sensitive knobs ONE change at a time vs the FiLM-scale
# squint_hln reference. RUN SINGLE-SEED. All use the existing squint_hln
# (full-panel) blob — no rebuild (but that blob must carry labels: cell_type /
# niche). To diagnose on a gene-set blob instead, wrap each build with
# _patch_dataset_name(..., 'squint_hln_svg2k').
# ===========================================================================
_S64_REF_KEY = (
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16"
    "+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10"
    "+contrastWB-w10-k5+filmscale+squint_hln"
)
_S64_SPINE_KEY = (
    "dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16"
    "+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10"
    "+contrastWB-w10-k5+squint_hln"
)


def _s64_ref():
    return VARIANTS[_S64_REF_KEY]["build"]()


def _s64_spine():
    return VARIANTS[_S64_SPINE_KEY]["build"]()


def _s64_nol1(cell_k, niche_k):
    """Single-level VQ (drop the residual L1), applied on the spine BEFORE FiLM
    so the cell-conditioned FiLM embedding sizes to the new cell codebook."""
    return _patch_dual_cell_conditioned_niche(
        _patch_dual_vq(
            _patch_dual_vq(_s64_spine(), branch="niche", codebook_size=niche_k),
            branch="cell", codebook_size=cell_k),
        mode="film_scale")


# (tag, build) — ONE change vs the FiLM-scale squint_hln reference.
_S64_SPEC = [
    # adjacency BCE weight (default 1000 -> reduce; prime suspect on a tiny graph)
    ("adj250",          lambda: _patch_dual_adj_weight(_s64_ref(), 250.0)),
    ("adj100",          lambda: _patch_dual_adj_weight(_s64_ref(), 100.0)),
    ("adj10",           lambda: _patch_dual_adj_weight(_s64_ref(), 10.0)),
    ("adj1",            lambda: _patch_dual_adj_weight(_s64_ref(), 1.0)),
    # batch size (default 512 -> ~38 steps/epoch; lower = more steps), sqrt-scaled lr
    ("bs256-lr5e-4",    lambda: _patch_dual_batch_lr(_s64_ref(), 256, 5e-4)),
    ("bs128-lr3.5e-4",  lambda: _patch_dual_batch_lr(_s64_ref(), 128, 3.5e-4)),
    ("bs64-lr2.5e-4",   lambda: _patch_dual_batch_lr(_s64_ref(), 64, 2.5e-4)),
    ("bs32-lr1.75e-4",  lambda: _patch_dual_batch_lr(_s64_ref(), 32, 1.75e-4)),
    # contrastive weight (default 10 -> reduce / off)
    ("contrast1",       lambda: _patch_dual_contrastive_cell_within_batch(
                            _s64_ref(), wt_contrastive_cell=1.0, k_pos=5)),
    ("contrast0",       lambda: _patch_dual_no_contrastive(_s64_ref())),
    # no L1 residual codebook (single-level VQ)
    ("no-l1-k30",       lambda: _s64_nol1(30, 30)),
    ("no-l1-gt",        lambda: _s64_nol1(16, 4)),
    # reconstruction (NB) weights lower (cell + nbr). The NB NLL is summed over
    # genes and NOT normalized by library size, so it scales with COUNT DEPTH:
    # ~500 on CosMx HLN (772 counts/cell) vs ~145 on mouse MERFISH. Every other
    # weight (adj=1000, contrast=10, ...) was tuned to recon~145, so recon now
    # dominates ~3.4x. wt~145/500~=0.3 restores the mouse balance; 0.5/0.1/0.01
    # bracket it.
    ("recon0.5",        lambda: _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(_s64_ref(), 0.5), 0.5)),
    ("recon0.3",        lambda: _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(_s64_ref(), 0.3), 0.3)),
    ("recon0.1",        lambda: _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(_s64_ref(), 0.1), 0.1)),
    ("recon0.01",       lambda: _patch_dual_attr_recon_nbr_weight(
                            _patch_dual_attr_recon_weight(_s64_ref(), 0.01), 0.01)),
    # --- recommended extras for small single-section data ---
    ("knn8",            lambda: _patch_dual_sampler_neighbors(
                            _patch_dual_graph_knn(_s64_ref(), n_neighs=8),
                            num_neighbors=[8])),
    ("vqwarmup",        lambda: _patch_dual_vq_warmup(_s64_ref(), vq_warmup_epochs=10)),
    ("diversity50",     lambda: _patch_dual_codebook_diversity(_s64_ref(), weight=50.0)),
    ("lr2e-4",          lambda: _patch_dual_batch_lr(_s64_ref(), 512, 2e-4)),
    ("no-film",         lambda: _s64_spine()),
    ("smalldata-combo", lambda: _patch_dual_no_contrastive(
                            _patch_dual_adj_weight(
                                _patch_dual_batch_lr(_s64_ref(), 64, 2.5e-4), 10.0))),
]

for _i, (_tag, _bld) in enumerate(_S64_SPEC, start=1):
    VARIANTS[f"s64_v{_i}_{_tag}+squint_hln"] = {
        "description": (
            f"s64 squint_hln DIAGNOSTIC ({_tag}) — single change vs the FiLM-scale "
            f"squint_hln reference, to find why HLN resolution is poor (small "
            f"single-section CosMx; mmb-tuned defaults). Uses the squint_hln "
            f"(full-panel) blob; run single-seed."
        ),
        "patches": [f"s64 diagnostic: {_tag}"],
        "build": _bld,
    }


# ===========================================================================
# s58 — INFORMATION-FLOW / COMPLEMENTARITY coupling (beyond trunk-sharing).
# The s56 trunk-sharing sweep tied with the decoupled baseline -> sharing the
# trunk is not the lever. s58 instead changes WHAT flows between the branches:
#   #1 COMPOSITIONAL cell->niche coupling: the niche GNN consumes neighbours'
#      cell codes (detached) so the niche code is built on local cell-type
#      composition — directional, asymmetric (cell stays protected).
#   #2 DISENTANGLEMENT penalty: decorrelate the cell & niche latents so the
#      niche code captures only complementary signal.
#   plus #1+#2 combined.
# All on the s55_v3 cross-batch-MNN decoupled spine (_s56_spine()).
# Reference = s55_v3.
# ===========================================================================
VARIANTS["s58_v1_compose-cell2niche-zqcell+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #1 compositional cell->niche coupling (cross-batch MNN spine): the "
        "niche GNN consumes neighbours' QUANTIZED cell codes (z_q_cell, detached) "
        "as extra node features, so the niche representation is built on local "
        "cell-type composition while the cell branch stays gradient-protected. "
        "Full concat (no projection). Reference = s55_v3 (decoupled)."
    ),
    "patches": ["= s55_v3 spine + cell_to_niche(source=z_q_cell, detach=True)"],
    "build": lambda: _patch_dual_compositional_cell_to_niche(
        _s56_spine(), source="z_q_cell", detach=True),
}
VARIANTS["s58_v2_compose-cell2niche-zqcell-proj64+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #1 compositional cell->niche coupling, PROJECTED: neighbours' "
        "z_q_cell (detached) projected to 64-d before concatenation onto the "
        "niche GNN input, so the cell signal doesn't dominate the niche features. "
        "Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cell_to_niche(z_q_cell, detach, project_dim=64)"],
    "build": lambda: _patch_dual_compositional_cell_to_niche(
        _s56_spine(), source="z_q_cell", detach=True, project_dim=64),
}
VARIANTS["s58_v3_compose-cell2niche-zmlp+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #1 compositional cell->niche coupling with the CONTINUOUS cell "
        "latent (z_mlp_cell, detached) instead of the quantized code — ablation "
        "of discrete vs continuous injected cell signal. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cell_to_niche(source=z_mlp_cell, detach=True)"],
    "build": lambda: _patch_dual_compositional_cell_to_niche(
        _s56_spine(), source="z_mlp_cell", detach=True),
}
VARIANTS["s58_v4_compose-cell2niche-zqcell-nodetach+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #1 compositional cell->niche coupling WITHOUT detach — niche "
        "gradients flow back into the cell branch through the injected z_q_cell. "
        "Tests whether protecting the cell branch (the asymmetry) matters. "
        "Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cell_to_niche(z_q_cell, detach=False)"],
    "build": lambda: _patch_dual_compositional_cell_to_niche(
        _s56_spine(), source="z_q_cell", detach=False),
}
VARIANTS["s58_v5_disentangle-w100+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #2 disentanglement penalty (cross-batch MNN spine): "
        "Barlow-Twins-style cross-correlation decorrelation between the cell and "
        "niche latents (wt_disentangle=100) so the niche code captures only "
        "complementary signal. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + disentangle_cell_niche_loss (wt=100)"],
    "build": lambda: _patch_dual_disentangle(_s56_spine(), wt=100.0),
}
VARIANTS["s58_v6_disentangle-w1000+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #2 disentanglement penalty, STRONGER (wt_disentangle=1000). The "
        "penalty is mean squared cross-correlation (in [0,1]); 100 vs 1000 "
        "brackets the upweighting. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + disentangle_cell_niche_loss (wt=1000)"],
    "build": lambda: _patch_dual_disentangle(_s56_spine(), wt=1000.0),
}
VARIANTS["s58_v7_compose-zqcell+disentangle-w100+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s58 #1+#2 COMBINED (cross-batch MNN spine): compositional cell->niche "
        "coupling (z_q_cell, detached) PLUS the disentanglement penalty "
        "(wt=100). Tests whether injecting cell composition and decorrelating "
        "the codes are complementary. Reference = s55_v3."
    ),
    "patches": [
        "= s55_v3 spine + cell_to_niche(z_q_cell, detach) + disentangle (wt=100)",
    ],
    "build": lambda: _patch_dual_disentangle(
        _patch_dual_compositional_cell_to_niche(
            _s56_spine(), source="z_q_cell", detach=True),
        wt=100.0),
}


# ===========================================================================
# s59 — follow-ups to the s56 coupling table (Soft L2 was the only variant with
# no significant degradation + a slight integration trend). Two threads:
#   A. SOFT-L2 weight sweep — chase that integration signal (1e-3 == s56_v8).
#   B. PARAMETER-EFFICIENT coupling: one shared trunk + a tiny identity-init
#      per-branch adapter (affine / lora / mlp). Targets the "fewer params,
#      same performance" argument — coupled halves the encoder MLP params but
#      lost resolution; the adapter restores per-branch specialization cheaply.
# All on the s55_v3 cross-batch-MNN spine (_s56_spine()). Reference = s55_v3.
# ===========================================================================
# A. Soft-L2 weight sweep (decoupled architecture + weight-distance penalty).
_S59_SOFTL2 = [("v1", "1e-4", 1e-4), ("v2", "1e-2", 1e-2), ("v3", "1e-1", 1e-1)]
for _v, _wtag, _wt in _S59_SOFTL2:
    VARIANTS[f"s59_{_v}_softL2-w{_wtag}+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s59 soft-L2 coupling weight sweep (cross-batch MNN spine): two "
            f"decoupled trunks + L2 penalty between matched weights, "
            f"wt_encoder_coupling={_wtag} (1e-3 = s56_v8 was the promising point). "
            f"Tests whether the slight integration gain becomes significant at a "
            f"different strength. Reference = s55_v3."
        ),
        "patches": [f"= s55_v3 spine + soft-L2 coupling (wt={_wtag})"],
        "build": (lambda wt=_wt: _patch_dual_soft_coupling(_s56_spine(), wt=wt)),
    }

# B. Parameter-efficient coupling: shared trunk + per-branch adapter.
#    (tag, BranchAdapter kwargs, ~% of decoupled encoder-MLP params)
_S59_ADAPTERS = [
    ("v4", "coupled-affine-adapter", dict(kind="affine"),            "~50%"),
    ("v5", "coupled-lora-r16",       dict(kind="lora", rank=16),     "~52%"),
    ("v6", "coupled-mlp-adapter",    dict(kind="mlp", hidden=256),   "~80%"),
]
for _v, _tag, _adk, _pct in _S59_ADAPTERS:
    VARIANTS[f"s59_{_v}_{_tag}+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s59 parameter-efficient coupling (cross-batch MNN spine): ONE shared "
            f"MLP trunk + identity-init per-branch {_adk['kind']} adapter (cell "
            f"adapter before VQ_cell, niche adapter before the GNN). Restores the "
            f"per-branch specialization that pure coupled lost, at {_pct} of the "
            f"decoupled encoder-MLP params. Reference = s55_v3 (decoupled); "
            f"contrast vs s56_v1 (coupled, no adapter)."
        ),
        "patches": [f"= s55_v3 spine -> COUPLED trunk + {_adk['kind']} branch adapter"],
        "build": (lambda adk=_adk: _patch_dual_branch_adapter(_s56_spine(), **adk)),
    }


# ===========================================================================
# s60 — NOVEL cross-branch couplings (none overlap s56/s58/s59): VQ/codebook-
# level coupling, conditional coupling, dynamic attention, mutual alignment,
# and a domain-borrowed niche construction. All on the s55_v3 cross-batch-MNN
# decoupled spine (_s56_spine()), all dim-preserving on z_q_cell / z_q_niche
# so decoders / metrics are unchanged. Reference = s55_v3.
# ===========================================================================
VARIANTS["s60_v1_shared-token+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #1 SHARED-TOKEN factorization: a third shared codebook whose "
        "quantized token is projected and ADDED to both z_q_cell and z_q_niche, "
        "so a single discrete shared code couples the branches while each keeps "
        "its private code. Motivated by the high shared linear subspace (CCA "
        "0.87). Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + additive shared-token codebook (dim=64,k=64)"],
    "build": lambda: _patch_dual_shared_token(_s56_spine(), dim=64, codebook_size=64),
}
VARIANTS["s60_v2_cross-branch-residual+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #2 CROSS-BRANCH RESIDUAL VQ: the niche VQ quantizes z_gnn after "
        "subtracting a projection of the (detached) cell code, so the niche "
        "token encodes spatial signal BEYOND ego-cell identity — structured "
        "complementarity (architectural, not a penalty). Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + niche VQ on (z_gnn - proj(z_q_cell.detach()))"],
    "build": lambda: _patch_dual_cross_branch_residual(_s56_spine(), detach=True),
}
VARIANTS["s60_v3_shared-codebook+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #3 SHARED CODEBOOK: both branches quantize into ONE codebook "
        "(vq_niche tied to vq_cell), testing whether a common discrete "
        "vocabulary couples them / helps cross-platform transfer. "
        "Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + tied cell/niche codebook"],
    "build": lambda: _patch_dual_shared_codebook(_s56_spine()),
}
VARIANTS["s60_v4_cellcond-niche-bias+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #4 CELL-CONDITIONED NICHE (bias): a per-cell-code embedding "
        "(indexed by the detached level-0 cell code) is ADDED to the niche VQ "
        "input — 'given cell type, shift the niche representation'. "
        "Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + niche VQ input += Embed(cell_code)"],
    "build": lambda: _patch_dual_cell_conditioned_niche(_s56_spine(), mode="bias"),
}
VARIANTS["s60_v5_cellcond-niche-film+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #5 CELL-CONDITIONED NICHE (FiLM): z_q_niche is affine-modulated by "
        "(gamma,beta) indexed by the detached level-0 cell code — a "
        "hierarchical 'cell type modulates the niche readout' coupling. "
        "Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + z_q_niche FiLM-modulated by cell_code"],
    "build": lambda: _patch_dual_cell_conditioned_niche(_s56_spine(), mode="film"),
}
VARIANTS["s60_v6_niche-attends-cell-codebook+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #6 NICHE ATTENDS CELL CODEBOOK: the niche representation attends "
        "over the K cell-codebook prototypes (a soft cell-type-composition "
        "descriptor) and the result is added to the niche VQ input — a dynamic, "
        "input-dependent coupling (vs static cross-stitch). Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + niche cross-attention over cell codebook"],
    "build": lambda: _patch_dual_niche_attends_cell_codebook(_s56_spine()),
}
VARIANTS["s60_v7_cell-niche-alignment-w100+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #7 MUTUAL ALIGNMENT (Barlow invariance term): push the matched "
        "cell/niche feature dims to be CORRELATED (wt_align=100) — the opposite "
        "sign of the disentanglement penalty (s58), testing whether aligning "
        "helps where decorrelating hurt. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + cell_niche_alignment_loss (wt=100)"],
    "build": lambda: _patch_dual_cell_niche_alignment(_s56_spine(), wt=100.0),
}
VARIANTS["s60_v8_niche-nbr-expr-augment+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s60 #8 BANKSY/CellCharter-style niche augmentation: concat a "
        "non-learned neighbourhood-mean of a 32-d projection of the raw counts "
        "onto the niche GNN input — the domain-standard explicit niche "
        "construction. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + niche GNN input augmented w/ nbr-mean(proj(x))"],
    "build": lambda: _patch_dual_niche_nbr_expr_augment(_s56_spine(), dim=32),
}


# ===========================================================================
# s61 — TWO-SEPARATE-MODELS baseline (reviewer: "is the joint model better than
# two strong separate single-task models?"). Same decoupled spine + hyperparams
# as SQUINT (s55_v3), but each variant keeps only ONE task's losses, so its
# relevant codes are exactly a standalone single-task model's:
#   * cell-only  = drop ALL niche/spatial losses -> read its CELL codes
#   * niche-only = drop ALL cell losses (NB + commit + contrastive) -> NICHE codes
# Compare SQUINT(decoupled, s55_v3) cell vs s61 cell-only, and SQUINT niche vs
# s61 niche-only: per-task PARITY shows joint training costs nothing, then the
# shared-encoder variants (s56_v1 coupled / s59_v4 affine) make the efficiency
# case (both tasks, ~half the encoder params, one training run).
# ===========================================================================
VARIANTS["s61_v1_cell-only+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s61 single-task CELL-ONLY baseline: the s55_v3 decoupled spine with ALL "
        "niche/spatial losses dropped (no nbr-NB, no niche-commit, no adjacency "
        "BCE) — a standalone cell VQ-VAE (cell NB + commit + cross-batch "
        "contrastive + decoder cov). Read its CELL codes as the 'separate cell "
        "model' baseline vs SQUINT's cell codes. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + drop all niche/spatial losses (cell-only)"],
    "build": lambda: _patch_dual_no_spatial(_s56_spine()),
}
VARIANTS["s61_v2_niche-only+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s61 single-task NICHE-ONLY baseline: the s55_v3 decoupled spine with ALL "
        "cell-branch losses dropped (cell NB, cell commit, cross-batch "
        "contrastive) — a standalone niche model (GNN -> niche VQ -> nbr-NB + "
        "adjacency BCE + niche commit). Read its NICHE codes as the 'separate "
        "niche model' baseline vs SQUINT's niche codes. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + drop cell NB/commit + contrastive (niche-only)"],
    "build": lambda: _patch_dual_no_contrastive(
        _patch_dual_no_cell_recon(_s56_spine())),
}


# ===========================================================================
# s62 — FOLLOW-UPS to the two threads that looked on-par / slightly better in
# s56/s60: (A) Y-shape "mostly-specialised" (s56_v4 64/192 was the best Y-shape)
# pushed to even smaller shared trunks; (B) cell-conditioned-niche FiLM variants
# (s60_v5 FiLM lifted Niche iLISI 0.358->0.598**) — pre-VQ / scale-only /
# continuous-conditioned; (C) multi-head niche->cell-codebook attention (s60_v6
# nudged integration). Plus the best-resolution + best-integration COMBO.
# All on the s55_v3 spine. Reference = s55_v3. NOTE: chasing marginal/integration
# signals — niche iLISI has high baseline variance (ref 0.358±0.125), so any
# "win" must replicate and not cost resolution/MMD.
# ===========================================================================
# A. Y-shape, smaller shared trunk (config-only via _patch_dual_shared_specific_encoders).
_S62_YSHAPE = [("v1", 48, 208), ("v2", 32, 224), ("v3", 16, 240)]
for _v, _sd, _pd in _S62_YSHAPE:
    VARIANTS[f"s62_{_v}_yshape-shared{_sd}-path{_pd}+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s62 Y-shape pushed to a SMALLER shared trunk (shared {_sd} / path "
            f"{_pd}, concat {_sd + _pd}=256) — continues the s56 trend that less "
            f"sharing (64/192 best) is better. Reference = s55_v3."
        ),
        "patches": [f"= s55_v3 spine + Y-shape (shared_last={_sd}, path_last={_pd})"],
        "build": (lambda sd=_sd, pd=_pd: _patch_dual_shared_specific_encoders(
            _s56_spine(), shared_last_dim=sd, path_last_dim=pd)),
    }

# B. Cell-conditioned-niche FiLM variants (reuse _patch_dual_cell_conditioned_niche).
_S62_FILM = [
    ("v4", "film-prevq",  "film_prevq"),
    ("v5", "film-scale",  "film_scale"),
    ("v6", "film-cont",   "film_cont"),
]
for _v, _tag, _mode in _S62_FILM:
    VARIANTS[f"s62_{_v}_cellcond-{_tag}+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
        "description": (
            f"s62 cell-conditioned-niche FiLM ablation (mode={_mode}): tweaks the "
            f"s60_v5 FiLM that lifted Niche iLISI. 'film_prevq' applies the affine "
            f"to the niche VQ input (vs post-VQ); 'film_scale' is scale-only; "
            f"'film_cont' generates (gamma,beta) from the continuous cell "
            f"embedding via a linear hypernetwork. Reference = s55_v3."
        ),
        "patches": [f"= s55_v3 spine + cell_conditioned_niche(mode={_mode})"],
        "build": (lambda mode=_mode: _patch_dual_cell_conditioned_niche(
            _s56_spine(), mode=mode)),
    }

# C. Multi-head niche->cell-codebook attention.
VARIANTS["s62_v7_niche-attends-cell-cb-h4+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s62 multi-head (heads=4) niche->cell-codebook attention — tweaks s60_v6 "
        "(single-head nudged integration). Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + niche-attends-cell-codebook (heads=4)"],
    "build": lambda: _patch_dual_niche_attends_cell_codebook(_s56_spine(), heads=4),
}

# D. Best-resolution (Y-shape 64/192) + best-integration (cell-cond FiLM) combo.
VARIANTS["s62_v8_yshape64-192+cellcond-film+crossmnn-wt10-k1+mmb0-1b_smb1-1b_1p"] = {
    "description": (
        "s62 COMBINATION: the best resolution-neutral Y-shape (shared 64 / path "
        "192, = s56_v4) PLUS the cell-conditioned-niche FiLM (post-VQ, = s60_v5) "
        "that improved Niche iLISI — tests whether the integration gain stacks on "
        "the Y-shape. Reference = s55_v3."
    ),
    "patches": ["= s55_v3 spine + Y-shape(64/192) + cell_conditioned_niche(film)"],
    "build": lambda: _patch_dual_cell_conditioned_niche(
        _patch_dual_shared_specific_encoders(
            _s56_spine(), shared_last_dim=64, path_last_dim=192),
        mode="film"),
}


# Map short dataset tag -> ordered list of sweep variant names. The CLI's
# --all-dataset flag accepts these short tags. The full dataset_name (used
# as the cfg key + artifact dir) is set by each variant's build() via the
# corresponding _patch_dual_{chl59_lung5,mmb20_holdout} (or the base config
# for mmb-smb).
DATASET_VARIANTS: dict = {}

DATASET_VARIANTS["mmb0-1b_smb1-1b_1p"] = _register_dataset_sweep(
    dataset_tag="mmb0-1b_smb1-1b_1p",
    dataset_apply_fn=lambda c: c,   # base config IS the mmb-smb dataset
    narrow=False,
)

DATASET_VARIANTS["chl59-8b_1p"] = _register_dataset_sweep(
    dataset_tag="chl59-8b_1p",
    dataset_apply_fn=lambda c: _patch_dual_chl59_lung5(
        c, test_batch_idx=[2, 3]),
    narrow=False,
)

DATASET_VARIANTS["mmb0-1b_smb1-20b_1p"] = _register_dataset_sweep(
    dataset_tag="mmb0-1b_smb1-20b_1p",
    dataset_apply_fn=lambda c: _patch_dual_mmb20_holdout(c),
    narrow=True,
)

# Targeted mmb0-1b_smb1-1b_1p 1-layer-GNN ablation matrix (separate from the
# 10-axis sweep above). The spine is RVQ-both + decoder-cov + adv +
# 1-layer GNN; each variant ablates ONE axis. Run via:
#   python examples/run_squint.py --all-dataset mmb0-1b_smb1-1b_1p-ablations
# Note: the key is NOT a dataset tag — DATASET_VARIANTS doubles here as
# a registry of named variant groups. The runner only cares about the
# value (a list of variant names); it does not require the key to match
# any `dataset_name` / `dataset_tag` on the cfg.
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations"] = [
    # baseline (the spine itself; registered earlier as a sweep variant)
    "dualvq+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    # GNN type axis: SAGE -> GATv2
    "dualvq+rvq-both+decoder-cov+adv+gatv2+mmb0-1b_smb1-1b_1p",
    # GNN depth axis: 1 -> 2 layers (registered as the +wide sweep variant)
    "dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    # Encoder size axis: default -> scvi-style small (hidden=128)
    "dualvq+small+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    # Adversarial weight axis: 150 -> {300, 50}
    "dualvq+rvq-both+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv-w50+mmb0-1b_smb1-1b_1p",
    # Adjacency BCE weight axis: 1000 -> {3000, 250}
    "dualvq+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
]

# mmb-smb 1-layer ablation matrix v2: capacity / regularization /
# batch-correction / spatial-context axes. Each variant ablates ONE
# knob from the same RVQ-both + decoder-cov + adv + 1-layer-GNN spine
# the v1 ablations use. Submit all 8 via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v2
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v2"] = [
    # codebook capacity
    "dualvq+rvq-both-large+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both-3level+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    # latent / regularization
    "dualvq+rvq-both+decoder-cov+adv+latent-128+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv+dropout+mmb0-1b_smb1-1b_1p",
    # batch correction
    "dualvq+rvq-both+decoder-cov+adv+batch-emb32+mmb0-1b_smb1-1b_1p",
    # spatial context / encoder capacity
    "dualvq+rvq-both+decoder-cov+adv+sampler16+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv+gnn-h384+mmb0-1b_smb1-1b_1p",
]

# mmb-smb 2-layer-spine ablation matrix v3: codebook capacity / structure
# scan on top of the v1 winners' combined spine
# (+wide [best on niche ID] + adj-w250 [best on batch integration]).
# Submit all 8 via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v3
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v3"] = [
    # spine baseline (winners combined)
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    # RVQ macro-codebook scan (k1)
    "dualvq+wide+rvq-both-50-90+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-80-90+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    # RVQ both-layer scan
    "dualvq+wide+rvq-both-50-150+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-80-200+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    # 3-level RVQ
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    # CVQ (tree-structured) family
    "dualvq+wide+cvq-both-30-10+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+cvq-both-50-10+decoder-cov+adv+adj-w250+mmb0-1b_smb1-1b_1p",
]

# mmb-smb v4 ablations (final round): hybridise the strongest non-wide
# signals (`+enc-deeper`, `+adj-w3000`) with the v3 winner
# (`+wide+rvq-both+decoder-cov+adv`), and explore a NEW axis —
# adversarial warmup — to recover cell-NMI / cell-pearson regressions.
# See the variant comments above + the v4 selection writeup for the
# per-variant rationale. Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v4.sh
#   # (or: bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v4)
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v4"] = [
    # Single-knob ports of empirical winners onto the v3 spine.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv-w50+mmb0-1b_smb1-1b_1p",
    # Stack of the two top empirical signals.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adj-w3000+mmb0-1b_smb1-1b_1p",
    # NEW axis: adversarial warmup (alpha=0 for first 10 epochs).
    "dualvq+wide+rvq-both+decoder-cov+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    # Independent exploration: longer-range niche target.
    "dualvq+wide+rvq-both+decoder-cov+adv+nbr-hops-3+mmb0-1b_smb1-1b_1p",
    # Kitchen sink of the three top signals.
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adj-w3000+adv-warmup10+mmb0-1b_smb1-1b_1p",
]

# mmb-smb v5 ablations (final-final round): codebook-capacity scan on
# the new champion spine `+wide+rvq-both-3level+decoder-cov+adv+enc-deeper`.
# 6 RVQ-3-level variants (capacity scan + MLP-width pair) +
# 2 CVQ-3-level variants (architectural alternative; CVQ extended from
# 2 to 3 levels in `hierarchical_vq.py` for this round).
# Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v5.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v5"] = [
    # Spine baseline (the new project default).
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    # RVQ-3-level capacity scan: small / large / xlarge.
    "dualvq+wide+rvq-both-3level-50-100-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level-80-160-320+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level-20-40-80+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    # MLP-width ablations (encoder + decoder shrink / grow together).
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mlp-h256+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mlp-h512+mmb0-1b_smb1-1b_1p",
    # 3-level Conditional VQ (tree partitioning) -- architectural A/B.
    "dualvq+wide+cvq-both-3level-30-10-5+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+cvq-both-3level-50-15-5+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
]

# v6 — 8 ablations targeted at improving cell-token NMI on cell_type
# WITHOUT regressing batch integration. See descriptions on each
# variant for the per-experiment hypothesis. Three orthogonal
# mechanisms are tested:
#   - Capacity bottleneck (smaller encoder) + warmup    (3 variants)
#   - Warmup paired with a non-adversarial integration   (3 variants)
#     mechanism (MMD-batch loss): two strengths + an
#     MMD-only ablation (no adversary at all).
#   - Schedule + asymmetric adversary                    (2 variants)
# Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v6.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v6"] = [
    # Group A — smaller encoder + warmup (user hypothesis).
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup5+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small+rvq-both+decoder-cov+adv+adv-warmup5+mmb0-1b_smb1-1b_1p",
    # Group B — warmup paired with MMD batch-invariance (3 variants).
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w200+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    # Group C — schedule + asymmetric adversary.
    "dualvq+wide+rvq-both+decoder-cov+adv-cell-only+adv-w300+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv-cosine+adv-warmup10+mmb0-1b_smb1-1b_1p",
]

# v6b — second wave targeting cell-token NMI on cell_type. The 3 MMD
# variants from v6 errored on a kwarg name mismatch (now fixed); we
# re-run them here. The other v6 variants underperformed; we replace
# them with new mechanisms ablating different parts of the
# warmup-NMI / integration trade-off:
#   - Post-warmup adv-weight scan (adv-w50, adv-w300 with warmup10)
#   - Sharper cell VQ commit (commit-cell-w2 + warmup10)
#   - Combo safety stack (enc-shallow + warmup10 + mmd-w50)
# (A `+ce-w10` semi-supervised entry was here originally but was
# removed when SQUINT was committed to the unsupervised-only design.)
# Submit all 7 via:
#   bash examples/submit_mmb_smb_ablations_v6b.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v6b"] = [
    # Re-runs of the 3 MMD variants (now that mmd_target arg-name is fixed).
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+adv-warmup10+mmd-w200+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    # 4 NEW mechanisms.
    "dualvq+wide+rvq-both+decoder-cov+adv-w50+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv-w300+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w2+adv+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-shallow+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p",
]

# v7 — 8 ablations targeted at improving cell-token NMI on cell_type by
# varying ONLY the cell codebook structure. Niche is held fixed at
# 3-level RVQ (30, 60, 120). Level-1 size is 30 across all variants
# (matches niche level 1). The 10-epoch adv-warmup family from v6/v6b
# is dropped — the post-warmup loss spiked dramatically in those runs
# as the late-arriving adversary tried to undo features the encoder
# had committed during warmup. v7 uses adv-from-epoch-0 (alpha=1.0,
# wt=150.0) throughout.
#
# Cell codebook sweep (level 1 = 30 always):
#   - 1-level VQ:   k ∈ {30, 60, 200}
#   - 2-level RVQ:  (30, 60) / (30, 90) / (30, 200)
#   - 3-level RVQ:  (30, 60, 120) [matches niche] / (30, 90, 270)
# Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v7.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v7"] = [
    # 1-level VQ on the cell side (single codebook, no residual depth).
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-30+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-60+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-niche-30-60-120+vq-cell-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    # 2-level RVQ on the cell side (level 1 = 30; level 2 varies).
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-60+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-90+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-200+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    # 3-level RVQ on the cell side: matches-niche, then wider.
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-60-120+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-niche-30-60-120+rvq-cell-30-90-270+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
]

# v8 — 8 ablations split into two groups of 4 on the v5-anchor spine
# (+wide +rvq-both-3level +decoder-cov +adv, no warmup, no +enc-deeper).
# Group A varies encoder/decoder MLP size (depth, width, latent-dim,
# fully compact); Group B varies batch-integration mechanism on a 2x2
# {MMD-only vs adv+MMD} x {weak 50 vs strong 200} grid.
# Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v8.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v8"] = [
    # Group A — encoder/decoder size sweep (4 variants).
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-shallow+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mlp-h256+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+small-latent+mmb0-1b_smb1-1b_1p",
    "dualvq+small+rvq-both-3level+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    # Group B — MMD + adversarial batch-integration sweep (4 variants, 2x2).
    "dualvq+wide+rvq-both-3level+decoder-cov+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level+decoder-cov+adv+mmd-w200+mmb0-1b_smb1-1b_1p",
]

# v9 — 8 ablations on a SMALLER + FASTER architecture, organised as
# a 2 x 4 grid: { compact (h32 + (30,10,10)), medium (h64 + (30,20,10)) }
# crossed with 4 batch-integration strategies { default adv, boosted adv,
# adv+MMD combo, MMD-only }. Shared spine: NO +wide (1-hop GNN),
# +small(hidden=N), +rvq-both-3level (L0=30, symmetric), +decoder-cov.
# Submit all 8 via:
#   bash examples/submit_mmb_smb_ablations_v9.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v9"] = [
    # Compact size: hidden=32 + (30, 10, 10).
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h32+rvq-both-3level-30-10-10+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    # Medium size: hidden=64 + (30, 20, 10).
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv-w300+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
]

# v11 — single-variant smoke-test sweep used to verify the updated
# DataLoader config (num_workers=8 / persistent_workers=True /
# pin_memory=True) and the 10-CPU LSF allocation actually take effect.
# Picks one representative variant from v9 (medium architecture +
# default adversarial); the exact choice is incidental — any variant
# would do. Useful as the first job to submit after the DataLoader
# change lands, before re-running the full sweeps.
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v11.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v11"] = [
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
]

# v10 — "is spatial supervision hurting cell-type NMI?" diagnostic.
# 4 variants on a fixed medium architecture (h64 + codebook (30, 20, 10))
# crossed over { adv vs mmd-w100 } x { +no-spatial vs +no-adj }.
# Architectural size is held constant so the diagnostic isolates the
# effect of LOSSES (not capacity) on cell-type NMI.
# Submit all 4 via:
#   bash examples/submit_mmb_smb_ablations_v10.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v10"] = [
    # +no-spatial: drops nbr-NB + niche-commit + adj-BCE (all spatial losses).
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+no-spatial+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+no-spatial+mmb0-1b_smb1-1b_1p",
    # +no-adj: keeps nbr-NB + niche-commit, drops ONLY adjacency BCE.
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+adv+no-adj+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-20-10+decoder-cov+mmd-w100+no-adj+mmb0-1b_smb1-1b_1p",
]

# Held-out-region downstream task. Currently a single variant — the
# 1-layer-spine baseline (RVQ-both + decoder-cov + adv) with per-batch
# spatial holdout patches. Add more entries here later (e.g. larger
# patches, different anatomical zones) without touching the submitter.
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-region-holdout"] = [
    "dualvq+rvq-both+decoder-cov+adv+region-holdout+mmb0-1b_smb1-1b_1p",
    "dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+region-holdout+mmb0-1b_smb1-1b_1p",
]

# v12 — commit-loss exploration. 8 variants varying `wt_commit_cell`,
# `wt_commit_niche`, and asymmetric combinations off the shared spine
# `dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10`. Maps
# the commit-loss landscape that the v6b `commit-cell-w2` data point
# only hinted at (0.503 cell NMI vs 0.495 spine).
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v12.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v12"] = [
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-niche-w5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w3+commit-niche-w0.5+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+commit-cell-w0.5+commit-niche-w3+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
]

# v13 — codebook structure. 8 variants varying RVQ depth (2-level vs
# 3-level), level-1+ sizes (no-growth, modest, big), asymmetric cell-
# vs-niche depths, single-level VQ on cell, and composite VQ on cell.
# IMPORTANT: level-0 codebook size is FIXED at 30 across every entry
# (both branches) — this set deliberately does NOT ablate level-0.
# Same spine as v12.
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v13.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v13"] = [
    "dualvq+wide+vq-cell-30+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-cell-30-60+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-cell-30-200+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-cell-30-30+rvq-niche-30-60+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-cell-30-90-270+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+cvq-cell-30-10+rvq-niche-30-60-120+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-cell-30-90-270+rvq-niche-30-90+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
]

# v14 — loss-weight tuning (UNSUPERVISED). 8 variants sweeping
# adjacency-BCE weight (3 weights), adversarial warmup duration
# {5, 20} and weight {500}, plus MMD layered on adv vs pure MMD.
# Same spine as v12. No cell-type-label-consuming entries — see the
# comment block where `_patch_dual_cross_entropy` was removed for
# the rationale.
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v14.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v14"] = [
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w250+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w500+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+adj-w5000+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup5+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mmd-w50+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+mmd-w100+enc-deeper+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv-w500+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup20+mmb0-1b_smb1-1b_1p",
]

# v15 — architecture exploration. 8 variants varying GNN/MLP width,
# latent dim, dropout, GNN depth, and attention type. Same spine as v12.
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v15.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v15"] = [
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gnn-h512+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mlp-h512+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+mlp-h128+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+dropout-p0.1+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+dropout-p0.3+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+small-latent-128+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gnn2+mmb0-1b_smb1-1b_1p",
    "dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+adv-warmup10+gatv2+mmb0-1b_smb1-1b_1p",
]

# v16 — encoder/decoder dim grid with codebook fixed at (30, 30, 30).
# 4 hidden dims (<256) × 2 encoder depths (1-layer / 3-layer) = 8
# variants. Tests whether the (+wide=256) dim of v12-v15 is necessary
# for the symmetric-3-level-30 codebook to learn meaningful clusters
# at both cell-type and niche scales, or whether the model fits in a
# more compact architecture.
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v16.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v16"] = [
    # 1-layer MLP encoder (the `_patch_dual_small` default depth)
    "dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h128+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h192+rvq-both-3level-30-30-30+decoder-cov+adv+adv-warmup10+mmb0-1b_smb1-1b_1p",
    # 3-layer MLP encoder (`+enc-deeper` with the matching width)
    "dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h128+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "dualvq+small-h192+rvq-both-3level-30-30-30+decoder-cov+adv+enc-deeper+adv-warmup10+mmb0-1b_smb1-1b_1p",
]

# Sweep 17 — top-8 niche-NMI variants re-run as `s17_v<N>_*` aliases
# (sweep 17, variant index 1..8 sorted by historic best niche NMI
# descending) on the reverted (max_epochs=80) base config. Verifies
# that the regression diagnosed on 2026-05-12 is fixed: each
# s17_v<N>_* should land within ~0.01 niche NMI of its historic
# pre-05-12 score (0.66 - 0.70 range). All entries point at the
# SAME build lambdas as the original variants — only the artefact
# directory name changes
# (`<artifacts>/<dataset>/s17_v<N>_<orig>/<TS>/`), so the test is a
# direct apples-to-apples re-run.
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v17.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v17"] = [
    "s17_v1_dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    "s17_v2_dualvq+wide+rvq-both+decoder-cov+adv",
    "s17_v3_dualvq+wide+rvq-both+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
    "s17_v4_dualvq+wide+rvq-both+decoder-cov+adv+nbr-hops-3+mmb0-1b_smb1-1b_1p",
    "s17_v5_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+mmb0-1b_smb1-1b_1p",
    "s17_v6_dualvq+wide+rvq-both+decoder-cov+adv-w50+adv-warmup10+mmb0-1b_smb1-1b_1p",
    "s17_v7_dualvq+wide+rvq-both-3level+decoder-cov+mmd-w50+mmb0-1b_smb1-1b_1p",
    "s17_v8_dualvq+wide+rvq-both-3level-20-40-80+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
]

# Sweep 18 — small-arch / no-warmup / sampler {4,8} grid. 2 × 2 × 2 = 8
# variants exploring whether SQUINT can learn cell-type + niche
# structure at <256-d width without adversarial warmup. All variants
# share: `_patch_dual_small(hidden=N)`, `adv` from epoch 0 (no
# warmup), `decoder_covariate`, codebook level-0 = 30, sampler
# variable [4] vs [8].
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v18.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v18"] = [
    "s18_v1_dualvq+small-h32+rvq-both-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p",
    "s18_v2_dualvq+small-h64+rvq-both-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p",
    "s18_v3_dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p",
    "s18_v4_dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p",
    "s18_v5_dualvq+small-h32+rvq-both-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p",
    "s18_v6_dualvq+small-h64+rvq-both-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p",
    "s18_v7_dualvq+small-h32+rvq-both-3level-30-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p",
    "s18_v8_dualvq+small-h64+rvq-both-3level-30-30-30+decoder-cov+adv+sampler8+mmb0-1b_smb1-1b_1p",
]

# Sweep 19 — spatial-loss ablation: 2x2 grid over {adj-BCE on/off} x
# {nbr-NB on/off} = 4 variants on the historic top-niche-NMI spine
# (`+wide+rvq-both+decoder-cov+adv`). Tests whether either spatial
# loss is load-bearing for cell-NMI on this dataset.
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v19.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v19"] = [
    "s19_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s19_v2_dualvq+wide+rvq-both+decoder-cov+adv+no-adj+mmb0-1b_smb1-1b_1p",
    "s19_v3_dualvq+wide+rvq-both+decoder-cov+adv+no-nbr+mmb0-1b_smb1-1b_1p",
    "s19_v4_dualvq+wide+rvq-both+decoder-cov+adv+no-spatial+mmb0-1b_smb1-1b_1p",
]

# Sweep 20 — 1-knob-at-a-time isolation of the s18 niche-NMI regression.
# Anchor is s17_v2 (`+wide+rvq-both+decoder-cov+adv`, niche NMI 0.695).
# Each of v2/v3/v4 changes EXACTLY ONE knob to the s18 value, so the
# resulting niche-NMI delta is directly attributable to that knob.
# v1 re-runs the baseline as a within-sweep sanity check.
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v20.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v20"] = [
    "s20_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s20_v2_dualvq+wide+rvq-both-3level-30-30-30+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s20_v3_dualvq+small-h64+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s20_v4_dualvq+wide+rvq-both+decoder-cov+adv+sampler4+mmb0-1b_smb1-1b_1p",
]

# Sweep 21 — batch-integration loss & niche-neck ablation on the
# s17_v2 spine. 4 variants:
#   v1: spine baseline (re-run of s17_v2)
#   v2: no batch-integration loss (drop adv, no MMD — only +decoder-cov)
#   v3: MMD-w100 batch integration INSTEAD of adv
#   v4: spine + niche-neck MLP (decouples GNN input from z_mlp)
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v21.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v21"] = [
    "s21_v1_dualvq+wide+rvq-both+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s21_v2_dualvq+wide+rvq-both+decoder-cov+no-batch-int+mmb0-1b_smb1-1b_1p",
    "s21_v3_dualvq+wide+rvq-both+decoder-cov+mmd-w100+mmb0-1b_smb1-1b_1p",
    "s21_v4_dualvq+wide+rvq-both+decoder-cov+adv+niche-neck+mmb0-1b_smb1-1b_1p",
]

# Sweep 22 — batch_size × learning_rate grid on the s17_v2 spine.
# 4 batch sizes (256, 1024, 2048, 4096) × 2 LR-scaling rules (sqrt,
# linear) + 1 LR-only control = 8 variants. v1 is an alias of the
# s17_v2 baseline (re-runs at batch=256, lr=5e-4) for within-sweep
# reference. Tests whether SQUINT can train at bigger batch sizes
# without quality regression, and which LR scaling rule is safer.
#
# Submit via:
#   bash examples/submit_mmb_smb_ablations_v22.sh
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v22"] = [
    "s22_v1_dualvq+wide+rvq-both+decoder-cov+adv+bs256+lr5e-4+mmb0-1b_smb1-1b_1p",
    "s22_v2_dualvq+wide+rvq-both+decoder-cov+adv+bs1024+lr1e-3+mmb0-1b_smb1-1b_1p",
    "s22_v3_dualvq+wide+rvq-both+decoder-cov+adv+bs2048+lr1.4e-3+mmb0-1b_smb1-1b_1p",
    "s22_v4_dualvq+wide+rvq-both+decoder-cov+adv+bs4096+lr2e-3+mmb0-1b_smb1-1b_1p",
    "s22_v5_dualvq+wide+rvq-both+decoder-cov+adv+bs1024+lr2e-3+mmb0-1b_smb1-1b_1p",
    "s22_v6_dualvq+wide+rvq-both+decoder-cov+adv+bs2048+lr4e-3+mmb0-1b_smb1-1b_1p",
    "s22_v7_dualvq+wide+rvq-both+decoder-cov+adv+bs4096+lr8e-3+mmb0-1b_smb1-1b_1p",
    "s22_v8_dualvq+wide+rvq-both+decoder-cov+adv+bs256+lr1e-3+mmb0-1b_smb1-1b_1p",
]

# Sweep 23 — drastic loss-weight grid on the s17_v2 spine. Goal:
# improve cell_emb + neighborhood_emb iLISI while preserving (or
# improving) niche / cell-type NMI. See the long block comment above
# `s23_v1` in the VARIANTS section for the full design rationale.
#
# 16 variants in 4 hypothesis groups:
#   A (v1-v4):   adjacency BCE weight sweep
#   B (v5-v8):   stronger adversarial pressure
#   C (v9-v12):  NB reconstruction balance
#   D (v13-v16): combined sweet-spot candidates
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v23
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v23"] = [
    # Group A: adjacency BCE weight sweep
    "s23_v1_dualvq+wide+rvq-both+decoder-cov+adv+adj-w100+mmb0-1b_smb1-1b_1p",
    "s23_v2_dualvq+wide+rvq-both+decoder-cov+adv+adj-w300+mmb0-1b_smb1-1b_1p",
    "s23_v3_dualvq+wide+rvq-both+decoder-cov+adv+adj-w500+mmb0-1b_smb1-1b_1p",
    "s23_v4_dualvq+wide+rvq-both+decoder-cov+adv+adj-w2000+mmb0-1b_smb1-1b_1p",
    # Group B: stronger adversarial pressure
    "s23_v5_dualvq+wide+rvq-both+decoder-cov+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p",
    "s23_v6_dualvq+wide+rvq-both+decoder-cov+adv-alpha3-wt300+mmb0-1b_smb1-1b_1p",
    "s23_v7_dualvq+wide+rvq-both+decoder-cov+adv-alpha5+mmb0-1b_smb1-1b_1p",
    "s23_v8_dualvq+wide+rvq-both+decoder-cov+adv-wt500+mmb0-1b_smb1-1b_1p",
    # Group C: NB reconstruction balance
    "s23_v9_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w0.3+mmb0-1b_smb1-1b_1p",
    "s23_v10_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s23_v11_dualvq+wide+rvq-both+decoder-cov+adv+nbr-w2.0+mmb0-1b_smb1-1b_1p",
    "s23_v12_dualvq+wide+rvq-both+decoder-cov+adv+cell-w0.5+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    # Group D: combined sweet-spot candidates
    "s23_v13_dualvq+wide+rvq-both+decoder-cov+adj-w300+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p",
    "s23_v14_dualvq+wide+rvq-both+decoder-cov+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s23_v15_dualvq+wide+rvq-both+decoder-cov+adj-w200+adv-alpha3-wt300+mmb0-1b_smb1-1b_1p",
    "s23_v16_dualvq+wide+rvq-both+decoder-cov+adj-w500+adv-alpha2-wt300+nbr-w0.7+mmb0-1b_smb1-1b_1p",
]

# Sweep 24 — scVI-style tight bottleneck architecture. 12 variants in
# 3 groups testing whether collapsing the latent / decoder geometry
# (encoder MLP [128, 32], decoder [128], GNN h=32 / 1L, RVQ 3-level
# (30, 20, 10), decoder-cov embed_dim=32) gives the encoder a stronger
# incentive to discard batch-specific features → higher iLISI without
# sacrificing NMI.
#
#   A (v1-v4):  architecture / codebook variations
#   B (v5-v8):  batch integration on the tight architecture
#   C (v9-v12): adv + the best s23-flavoured loss-weight levers
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v24
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v24"] = [
    # Group A: architecture / codebook variations
    "s24_v1_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+mmb0-1b_smb1-1b_1p",
    "s24_v2_dualvq+compact-l16+rvq-both-30-20-10+decoder-cov-e16+mmb0-1b_smb1-1b_1p",
    "s24_v3_dualvq+compact-l64+rvq-both-30-20-10+decoder-cov-e32+mmb0-1b_smb1-1b_1p",
    "s24_v4_dualvq+compact-l32+rvq-both-50-30-20+decoder-cov-e32+mmb0-1b_smb1-1b_1p",
    # Group B: batch integration on the tight architecture
    "s24_v5_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+mmb0-1b_smb1-1b_1p",
    "s24_v6_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p",
    "s24_v7_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+mmd-w200+mmb0-1b_smb1-1b_1p",
    "s24_v8_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+mmd-w100+mmb0-1b_smb1-1b_1p",
    # Group C: adv + the best s23-flavoured loss-weight levers
    "s24_v9_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+adj-w300+mmb0-1b_smb1-1b_1p",
    "s24_v10_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s24_v11_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s24_v12_dualvq+compact-l32+rvq-both-30-20-10+decoder-cov-e32+adv-alpha2-wt300+adj-w300+mmb0-1b_smb1-1b_1p",
]

# Sweep 25 — tight-architecture integration sweep. 8 variants on a
# shared spine: GATv2 GNN h=128 2L + encoder MLP [128] + decoder [128]
# + RVQ 3-level (30, 60, 120) + decoder-cov. Symmetric 128-dim
# latents on both branches. Variants vary the batch-integration
# approach to lift iLISI without sacrificing NMI on the new tight
# spine.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v25
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v25"] = [
    "s25_v1_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+mmb0-1b_smb1-1b_1p",
    "s25_v2_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt300+mmb0-1b_smb1-1b_1p",
    "s25_v3_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p",
    "s25_v4_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+mmd-w100+mmb0-1b_smb1-1b_1p",
    "s25_v5_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+adj-w300+mmb0-1b_smb1-1b_1p",
    "s25_v6_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s25_v7_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s25_v8_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt300+adj-w300+mmb0-1b_smb1-1b_1p",
]

# Sweep 26 — s25 spine with WEAK adversarial (wt_adv_batch 10×
# smaller + 5-epoch GRL warmup). Same 8-variant integration grid as
# s25, but every adv-using variant runs with wt=15 (vs 150) /
# wt=30 (vs 300) + warmup_epochs=5.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v26
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v26"] = [
    "s26_v1_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+mmb0-1b_smb1-1b_1p",
    "s26_v2_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt30-warmup5+mmb0-1b_smb1-1b_1p",
    "s26_v3_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+mmd-w200+mmb0-1b_smb1-1b_1p",
    "s26_v4_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+mmd-w100+mmb0-1b_smb1-1b_1p",
    "s26_v5_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+adj-w300+mmb0-1b_smb1-1b_1p",
    "s26_v6_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s26_v7_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-wt15-warmup5+adj-w300+nbr-w0.5+mmb0-1b_smb1-1b_1p",
    "s26_v8_dualvq+gatv2+enc128+dec128+rvq3l-30-60-120+decoder-cov+adv-alpha2-wt30-warmup5+adj-w300+mmb0-1b_smb1-1b_1p",
]

# Sweep 27 — encoder-shape ablations on the +enc-deeper+dec-w256
# spine (= `dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w256`).
# All 4 variants share decoder=[256], 1L SAGE GNN h=256, RVQ (30,90),
# decoder-cov embed=16, adv α=1 wt=150. Only the encoder MLP varies.
# Note: where the encoder output dim differs from 256, the
# cell-branch latent (z_mlp) and niche-branch latent (z_gnn=256)
# become asymmetric — see VARIANTS dict entries for details.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v27
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v27"] = [
    "s27_v1_dualvq+rvq-both+decoder-cov+adv+enc-512+dec-w256+mmb0-1b_smb1-1b_1p",
    "s27_v2_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+dec-w256+mmb0-1b_smb1-1b_1p",
    "s27_v3_dualvq+rvq-both+decoder-cov+adv+enc-256-256-256-256+dec-w256+mmb0-1b_smb1-1b_1p",
    "s27_v4_dualvq+rvq-both+decoder-cov+adv+enc-512-256-128+dec-w256+mmb0-1b_smb1-1b_1p",
]

# Sweep 28 — same encoder shapes as s27, but with 2-LAYER GNN via
# +wide (sampler [8, 8], 2-hop nbr aggregation). Cross-reference
# s27_vN ↔ s28_vN to read the "does 2-hop niche aggregation help
# this encoder shape?" A/B at each shape.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v28
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v28"] = [
    "s28_v1_dualvq+wide+rvq-both+decoder-cov+adv+enc-512+dec-w256+mmb0-1b_smb1-1b_1p",
    "s28_v2_dualvq+wide+rvq-both+decoder-cov+adv+enc-128-128-128+dec-w256+mmb0-1b_smb1-1b_1p",
    "s28_v3_dualvq+wide+rvq-both+decoder-cov+adv+enc-256-256-256-256+dec-w256+mmb0-1b_smb1-1b_1p",
    "s28_v4_dualvq+wide+rvq-both+decoder-cov+adv+enc-512-256-128+dec-w256+mmb0-1b_smb1-1b_1p",
]

# Sweep 29 — batch-integration mechanism comparison on the
# `+enc-deeper+dec-w256` spine. 6 variants comparing the new
# `_patch_dual_mmd_prior` (NicheCompass-style MMD-to-prior) against
# `_patch_dual_adversarial` and `_patch_dual_mmd_batch` alone and in
# combination. Tests whether non-adversarial prior-matching can
# replace or complement the adversarial integration.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v29
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v29"] = [
    "s29_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
    "s29_v2_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
    "s29_v3_dualvq+rvq-both+decoder-cov+mmd-prior-w200+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
    "s29_v4_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
    "s29_v5_dualvq+rvq-both+decoder-cov+adv+mmd-prior-w50+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
    "s29_v6_dualvq+rvq-both+decoder-cov+mmd-prior-w50+mmd-batch-w100+enc-deeper+dec-w256+mmb0-1b_smb1-1b_1p",
]

# Sweep 30 — 3-level RVQ codebook variants on the
# `+enc-deeper+dec-w32` baseline. L0 stays fixed at 30 (matches the
# parent's macro level); 8 distinct (L1, L2) shapes vary across the
# sweep, ranging from 27k composite codes (30, 30, 30) to 243k
# (30, 90, 90). Hypothesis: an extra residual quantisation level
# gives the codebook finer-grained partitioning of the latent —
# useful for rare cell types / niches.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v30
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v30"] = [
    "s30_v1_dualvq+rvq-both-30-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v2_dualvq+rvq-both-30-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v3_dualvq+rvq-both-30-30-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v4_dualvq+rvq-both-30-60-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v5_dualvq+rvq-both-30-60-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v6_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v7_dualvq+rvq-both-30-60-120+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s30_v8_dualvq+rvq-both-30-90-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
]

# Sweep 31 — reduced codebook + encoder dim on the
# `+enc-deeper+dec-w32` baseline. Progressively shrinks the latent
# dim from 256 down to [128] and [64]. GNN hidden width is bumped
# in lock-step with the encoder output so the cell + niche latents
# stay symmetric.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v31
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v31"] = [
    "s31_v1_dualvq+rvq-both+decoder-cov+adv+enc-400-400-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v2_dualvq+rvq-both+decoder-cov+adv+enc-400-400-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v3_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v4_dualvq+rvq-both+decoder-cov+adv+enc-200-200-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v5_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v6_dualvq+rvq-both+decoder-cov+adv+enc-128-128-64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v7_dualvq+rvq-both+decoder-cov+adv+enc-shallow128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p",
    "s31_v8_dualvq+rvq-both+decoder-cov+adv+enc-shallow64+gnn-h64+dec-w32+mmb0-1b_smb1-1b_1p",
]

# Sweep 32 — k-NN graph k × batch-integration loss on the
# `+enc-deeper+dec-w32` baseline. 2 (k ∈ {12, 16}) × 4 (default
# adv, stronger adv α=2 wt=300, mmd-batch w=200, mmd-prior w=50)
# = 8 variants. Each row uses sampler == k (the GNN sees ALL
# spatial neighbours per cell).
#
# REQUIRES BLOB REBUILD if the on-disk blob doesn't already have
# k=12 + k=16 graphs:
#   python examples/run_squint.py --build-blob
# (the current `make_dataset_blob_config()` ships
# `n_neighs_list = [6, 8, 12, 16]`).
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v32
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v32"] = [
    "s32_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s32_v2_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s32_v3_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s32_v4_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s32_v5_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p",
    "s32_v6_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p",
    "s32_v7_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p",
    "s32_v8_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w32+knn16+sampler16+mmb0-1b_smb1-1b_1p",
]

# Sweep 33 — loss-weight ablations on the `+enc-deeper+dec-w32`
# baseline. Two axes:
#   v1-v3: STRONGER cell-NB recon (wt_attr_reconstr = 1.5, 2.0, 3.0)
#   v4-v7: adjacency BCE sweep (wt_adj_reconstr = 300, 500, 2000, 3000)
#   v8:    combination of v2 (cell w=2.0) + v6 (adj w=2000)
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v33
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v33"] = [
    "s33_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w1.5+mmb0-1b_smb1-1b_1p",
    "s33_v2_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+mmb0-1b_smb1-1b_1p",
    "s33_v3_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w3.0+mmb0-1b_smb1-1b_1p",
    "s33_v4_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w300+mmb0-1b_smb1-1b_1p",
    "s33_v5_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w500+mmb0-1b_smb1-1b_1p",
    "s33_v6_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w2000+mmb0-1b_smb1-1b_1p",
    "s33_v7_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+adj-w3000+mmb0-1b_smb1-1b_1p",
    "s33_v8_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+adj-w2000+mmb0-1b_smb1-1b_1p",
]

# Sweep 34 — Pareto-stack combinations of the s30..s33 knobs on the
# `+enc-deeper+dec-w32` baseline. Each variant stacks 2-3 of the
# individually-promising knobs.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v34
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v34"] = [
    "s34_v1_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s34_v2_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s34_v3_dualvq+rvq-both-30-60-90+decoder-cov+adv-alpha2-wt300+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s34_v4_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
    "s34_v5_dualvq+rvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+cell-w2.0+adj-w500+mmb0-1b_smb1-1b_1p",
    "s34_v6_dualvq+rvq-both-30-30-30+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+mmb0-1b_smb1-1b_1p",
    "s34_v7_dualvq+rvq-both+decoder-cov+adv-alpha2-wt300+enc-200-200-128+gnn-h128+dec-w32+adj-w300+mmb0-1b_smb1-1b_1p",
    "s34_v8_dualvq+rvq-both+decoder-cov+adv+enc-128-128-128+gnn-h128+dec-w32+knn12+sampler12+mmb0-1b_smb1-1b_1p",
]

# Sweep 35 — replace RVQ with CVQ (ConditionalVQ, tree-structured)
# on the `+enc-deeper+dec-w32` baseline. v1-v3 are 2-level CVQ
# (K1, K2); v4-v8 are 3-level CVQ (K1, K2, K3). The shapes mirror
# s30's RVQ shapes so each CVQ variant has a direct RVQ analog
# in s30 (and via s34_v1 / s30_v6 / s30_v8 etc.).
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v35
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v35"] = [
    "s35_v1_dualvq+cvq-both-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v2_dualvq+cvq-both-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v3_dualvq+cvq-both-30-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v4_dualvq+cvq-both-30-30-30+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v5_dualvq+cvq-both-30-30-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v6_dualvq+cvq-both-30-60-60+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v7_dualvq+cvq-both-30-60-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
    "s35_v8_dualvq+cvq-both-30-90-90+decoder-cov+adv+enc-deeper+dec-w32+mmb0-1b_smb1-1b_1p",
]

# Sweep 36 — 8 best-previous-run baselines × {with batch-int, no batch-int}
# under the new `adj_within_section_only=True` BCE adjacency scope.
#
# Reads off:
#   (a) "Did each previous winner survive the BCE-scope change?"
#       (compare odd v_i to its pre-s36 sibling run.)
#   (b) "Is the within-section BCE scope alone enough to integrate
#        batches WITHOUT an explicit adv / mmd-batch / mmd-prior loss?"
#       (compare even v_i to its odd partner.)
#
# Pairs (with-batch-int / no-batch-int):
#   v1  / v2   — enc-deeper+dec-w32                      (mmb-smb best baseline)
#   v3  / v4   — enc-deeper+dec-w32+knn16+sampler8       (densest spatial graph)
#   v5  / v6   — enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12  (s34_v4 spine)
#   v7  / v8   — enc-deeper+dec-w64 + mmd-prior          (NicheCompass-style)
#   v9  / v10  — enc-deeper+dec-w32+knn16+sampler16 + mmd-batch    (s32_v7 spine)
#   v11 / v12  — wide+adj-w3000+enc-shallow+dec-w256
#   v13 / v14  — wide+adj-w3000+enc-shallow+dec-w32
#   v15 / v16  — wide+enc-512+dec-w256                   (s28_v1 spine)
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v36
#
# Note: v3-v10 use +knn12 or +knn16 + matching sampler — they REQUIRE the
# blob to contain those k values. `make_dataset_blob_config()` ships
# `n_neighs_list = [6, 8, 12, 16]`; rebuild the blob if needed.
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v36"] = [
    "s36_v1_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v3_dualvq+rvq-both+decoder-cov+adv+enc-deeper+dec-w32+knn16+sampler8+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v5_dualvq+rvq-both+decoder-cov+adv+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn12+sampler12+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v7_dualvq+rvq-both+decoder-cov+mmd-prior-w50+enc-deeper+dec-w64+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w64+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v9_dualvq+rvq-both+decoder-cov+mmd-batch-w200+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v11_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w256+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v12_dualvq+wide+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w256+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v13_dualvq+wide+rvq-both+decoder-cov+adv+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v14_dualvq+wide+rvq-both+decoder-cov+no-batch-int+adj-w3000+enc-shallow+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v15_dualvq+wide+rvq-both+decoder-cov+adv+enc-512+dec-w256+within-sec+mmb0-1b_smb1-1b_1p",
    "s36_v16_dualvq+wide+rvq-both+decoder-cov+no-batch-int+enc-512+dec-w256+within-sec+mmb0-1b_smb1-1b_1p",
]

# Sweep 37 — 16 ablations on TWO no-batch-int spines under within-section BCE.
#
# Now that the adjacency-BCE fix (cross-section pairs excluded from
# negatives) means +no-batch-int variants train sensibly, ablate the four
# knobs the user identified as most likely to matter:
#   axis 1: adjacency reconstruction weight (`wt_adj_reconstr`)
#   axis 2: encoder size BEFORE the cell codebook (shared encoder MLP)
#   axis 3: encoder size AFTER the cell codebook (cell + niche decoder MLP)
#   axis 4: cell-level NB weight (`wt_attr_reconstr`)
#
# Spine A (v1..v8): niche-neck decoupled spine, no batch-int.
# Spine B (v9..v16): +enc-deeper+dec-w32+knn16+sampler16 spine, no batch-int.
#
# Per-spine layout:
#   v0 control   |  v1 deeper-encoder   |  v2 wider-dec   |  v3 narrower-dec
#   v4 adj-w500  |  v5 adj-w2000        |  v6 cell-w2.0   |  v7 combo
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v37
#
# Spine B variants (v9..v16) REQUIRE the blob to contain k=16. Rebuild with
# `python examples/run_squint.py --build-blob` if needed.
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v37"] = [
    # Spine A: niche-neck spine
    "s37_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+niche-neck-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256+dec-w32+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+adj-w500+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+niche-neck-256-256+dec-w32+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+niche-neck-256-256+dec-w32+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
    # Spine B: enc-deeper + knn16 + sampler16 spine
    "s37_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+niche-neck-256+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+niche-neck-256-256+dec-w32+knn16+sampler16+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+adj-w500+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s37_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
]

# Sweep 38 — 16 ablations around the s37_v15 base:
#   base = no-batch-int + enc-deeper [400, 400, 256] + dec-w[32] + knn16 +
#          sampler [16] + cell-w=2.0 + within-sec BCE.
#
# Axes:
#   v1-v5  : encoder dimensions & sizes (axis 1)
#   v6-v11 : loss weights (axis 2 — adj / cell-NB / nbr-NB)
#   v12-v14: batch_size + learning rate (axis 3, sqrt-scaled)
#   v15-v16: Pareto combos
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v38
#
# REQUIRES BLOB REBUILD if k=16 isn't already in the on-disk blob
# (`make_dataset_blob_config()` ships `n_neighs_list = [6, 8, 12, 16]`).
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v38"] = [
    # Axis 1: encoder dimensions (latent 64 / 128 / 256 / 512) + depth control
    "s38_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-64+gnn-h64+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-128+gnn-h128+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-256+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-512-512-512+gnn-h512+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-shallow+dec-w32+knn16+sampler16+cell-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    # Axis 2: loss weights
    "s38_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+adj-w500+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w3.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+nbr-w0.5+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+nbr-w2.0+within-sec+mmb0-1b_smb1-1b_1p",
    # Axis 3: batch_size + learning rate (sqrt-scaled)
    "s38_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs1024+lr1e-3+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w2.0+bs128+lr3.5e-4+within-sec+mmb0-1b_smb1-1b_1p",
    # Combos
    "s38_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w3.0+adj-w2000+within-sec+mmb0-1b_smb1-1b_1p",
    "s38_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+cell-w3.0+nbr-w0.5+adj-w2000+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
]

# Sweep 39 — cell-NMI Pareto sweep on the s37_v15 spine (cell-w dropped).
#
# Four orthogonal axes targeting cluster separation (not reconstruction):
#   A — cell-branch codebook cardinality (v1/v2)
#   B — VQ warmup (v3)
#   C — tighter latent (v4)
#   D — codebook orth-reg (v5)
# Plus 11 mixes (v6-v16).
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v39
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v39"] = [
    # Singletons (axes A-D)
    "s39_v1_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s39_v2_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s39_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p",
    "s39_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s39_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
    # Pair combos (A × B/C/D for cell (30, 30))
    "s39_v6_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p",
    "s39_v7_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s39_v8_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
    # Pair combos (A × B/C/D for cell (30, 10))
    "s39_v9_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p",
    "s39_v10_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s39_v11_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
    # Pair combos (B/C/D mutual)
    "s39_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p",
    "s39_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
    "s39_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
    # Triple + kitchen sink
    "s39_v15_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+mmb0-1b_smb1-1b_1p",
    "s39_v16_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-200-200-128+gnn-h128+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+vq-warmup10+orth-reg-w0.2+mmb0-1b_smb1-1b_1p",
]

# Sweep 40 — targeted cell-NMI follow-up (8 variants).
# Corrects the s39 errors: orth-reg at wt=0.2 was 1000x too weak, and
# the cell-codebook (L1, L2) variants in s39 only changed L2 — cell-NMI
# is computed against L1 indices.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v40
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v40"] = [
    "s40_v1_dualvq+rvq-both+niche-neck-256+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v2_dualvq+rvq-cell-15-90+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v3_dualvq+rvq-cell-20-90+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v4_dualvq+vq-cell-15+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v5_dualvq+rvq-both+orth-reg-w20+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v6_dualvq+rvq-both+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v7_dualvq+rvq-cell-15-90+rvq-niche-30-90+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s40_v8_dualvq+vq-cell-15+rvq-niche-30-90+orth-reg-w200+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s42 sweep — encoder architecture size sweep.
# 8 decoupled-enc variants (vary trunk shape) +
# 8 shared-specific-enc variants (vary shared / path last-layer dims).
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v42
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v42"] = [
    # 8 decoupled-enc variants (vary _patch_dual_encoder_deeper hidden_channels)
    "s42_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-64+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-512+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-200-200-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s42_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-800-800-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    # 8 shared-specific-enc (Y-shape) variants (vary shared_last_dim, path_last_dim)
    "s42_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path64+mmb0-1b_smb1-1b_1p",
    "s42_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared128-path128+mmb0-1b_smb1-1b_1p",
    "s42_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared256-path256+mmb0-1b_smb1-1b_1p",
    "s42_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared192-path64+mmb0-1b_smb1-1b_1p",
    "s42_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path192+mmb0-1b_smb1-1b_1p",
    "s42_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared128-path64+mmb0-1b_smb1-1b_1p",
    "s42_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared64-path128+mmb0-1b_smb1-1b_1p",
    "s42_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+shared192-path128+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s43 sweep — strictly-decoupled architecture, encoder × decoder size grid.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v43
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v43"] = [
    "s43_v1_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v2_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v3_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v4_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-128+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v5_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v6_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v7_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v8_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v9_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v10_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v11_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v12_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256-128+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v13_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v14_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w64+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v15_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w128+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
    "s43_v16_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-400-256+dec-w256+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s44 sweep — 1 negative-test variant (no-recon on the s43_v5 spine).
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v44
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v44"] = [
    "s44_v1_dualvq+rvq-both+decoder-cov+decoupled-dec-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+decoupled-enc+no-recon+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s45 sweep — shared MLP + no cell-NB diagnostic.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v45
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v45"] = [
    "s45_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5.0+bs512+lr7e-4+within-sec+no-cell-nb+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s46 sweep — cell-NB weight escalation on the s42 winner spine.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v46
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v46"] = [
    "s46_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w100+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s46_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w300+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s46_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w500+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
    "s46_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1000+bs512+lr7e-4+within-sec+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s47 sweep — contrastive cell aux loss + codebook diversity.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v47
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v47"] = [
    "s47_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w0.5-k5+mmb0-1b_smb1-1b_1p",
    "s47_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+mmb0-1b_smb1-1b_1p",
    "s47_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w2.0-k5+mmb0-1b_smb1-1b_1p",
    "s47_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k10+mmb0-1b_smb1-1b_1p",
    "s47_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+diversity-w1+mmb0-1b_smb1-1b_1p",
    "s47_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+diversity-w10+mmb0-1b_smb1-1b_1p",
    "s47_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+diversity-w1+mmb0-1b_smb1-1b_1p",
    "s47_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrast-w1.0-k5+diversity-w10+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s48 sweep — within-batch contrastive cell loss weight sweep.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v48
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v48"] = [
    "s48_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w1-k5+mmb0-1b_smb1-1b_1p",
    "s48_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s48_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w30-k5+mmb0-1b_smb1-1b_1p",
    "s48_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k5+mmb0-1b_smb1-1b_1p",
    "s48_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w300-k5+mmb0-1b_smb1-1b_1p",
    "s48_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p",
    "s48_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k10+mmb0-1b_smb1-1b_1p",
    "s48_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1.0+bs512+lr7e-4+within-sec+contrastWB-w100-k5+diversity-w1+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s49 sweep — 24 ablations on the s48_v2 (contrastWB-w10) spine.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v49
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v49"] = [
    "s49_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w5+bs512+lr7e-4+within-sec+cell-w5+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-256-128+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-128+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+enc-256+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc-256+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w300+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-30+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-60+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-200+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-500+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-dec-cov+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-dec-cov+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep120+mmb0-1b_smb1-1b_1p",
    "s49_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep160+mmb0-1b_smb1-1b_1p",
    "s49_v17_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep240+mmb0-1b_smb1-1b_1p",
    "s49_v18_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+contrastWB-w1000-k5+ep320+mmb0-1b_smb1-1b_1p",
    "s49_v19_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p",
    "s49_v20_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p",
    "s49_v21_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+shared-specific-enc+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p",
    "s49_v22_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+shared-specific-enc+contrastWB-w1000-k5+mmb0-1b_smb1-1b_1p",
    "s49_v23_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s49_v24_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+rvq-cell-30-200+contrastWB-w200-k5+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s50 sweep — single-trunk encoder size sweep on s49_v7 spine.
#
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v50
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v50"] = [
    "s50_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v4_dualvq+rvq-both+decoder-cov+no-batch-int+enc-512+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v5_dualvq+rvq-both+decoder-cov+no-batch-int+enc-64-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v6_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v7_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-128-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-128-64+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v12_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v13_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v14_dualvq+rvq-both+decoder-cov+no-batch-int+enc-256-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v15_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-256-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v16_dualvq+rvq-both+decoder-cov+no-batch-int+enc-400-400-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    # ----- 8 decoupled-encoder variants -----
    "s50_v17_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v18_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v19_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v20_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v21_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-256+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v22_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-128-128-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v23_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-256-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
    "s50_v24_dualvq+rvq-both+decoder-cov+no-batch-int+decoupled-enc-400-256-128+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+diversity-w100+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
]


# -----------------------------------------------------------------------
# s49_v23 ablation sweeps — 7 variants each on 2 datasets.
# Submit via:
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-ablations-v51
#   bash examples/submit_dataset_sweep.sh chl59-2b_1p-ablations-v51
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v51"] = [
    # 7 original ablations
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 1. base (control)
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+mmb0-1b_smb1-1b_1p",  # 2. -contrastive
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+mmb0-1b_smb1-1b_1p",  # 3. -adjacency
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 4. -decoder-cov
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 5. rvq-cell (30, 30)
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 6. rvq-cell (30, 10)
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 7. rvq-cell (30, 300)
    # neighbors-axis extension
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 8. knn=8 + sampler=8  (NEW)
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 9. knn=16 + sampler=8 (dense graph + sparse sampler — NEW, replaces dup)
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 10. knn=24 + sampler=24 (NEW)
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # 11. 2-layer GNN + sampler [16, 16]  (NEW)
]

DATASET_VARIANTS["chl59-2b_1p-ablations-v51"] = [
    # 7 original ablations
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 1. base (control)
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+chl59-2b_1p",  # 2. -contrastive
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+chl59-2b_1p",  # 3. -adjacency
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 4. -decoder-cov
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 5. rvq-cell (30, 30)
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 6. rvq-cell (30, 10)
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 7. rvq-cell (30, 300)
    # neighbors-axis extension
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 8. knn=8 + sampler=8  (NEW)
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 9. knn=16 + sampler=8 (dense graph + sparse sampler — NEW, replaces dup)
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 10. knn=24 + sampler=24 (NEW)
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # 11. 2-layer GNN + sampler [16, 16]  (NEW)
]
# DATASET_VARIANTS entries for the s51 spatch sweeps.

DATASET_VARIANTS["spatch_ov_1p-ablations-v51"] = [
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 1. base (control)
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_ov_1p",  # 2. -contrastive
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_ov_1p",  # 3. -adjacency
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 4. -decoder-cov
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 5. rvq-cell (30, 30)
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 6. rvq-cell (30, 10)
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 7. rvq-cell (30, 300)
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 8. knn=8 + sampler=8
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 9. knn=16 + sampler=8
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 10. knn=24 + sampler=24
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # 11. 2-layer GNN + sampler [16, 16]
]

DATASET_VARIANTS["spatch_hcc_1p-ablations-v51"] = [
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 1. base (control)
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_hcc_1p",  # 2. -contrastive
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_hcc_1p",  # 3. -adjacency
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 4. -decoder-cov
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 5. rvq-cell (30, 30)
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 6. rvq-cell (30, 10)
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 7. rvq-cell (30, 300)
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 8. knn=8 + sampler=8
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 9. knn=16 + sampler=8
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 10. knn=24 + sampler=24
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # 11. 2-layer GNN + sampler [16, 16]
]

DATASET_VARIANTS["spatch_coad_1p-ablations-v51"] = [
    "s51_v1_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 1. base (control)
    "s51_v2_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+spatch_coad_1p",  # 2. -contrastive
    "s51_v3_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+no-adj+spatch_coad_1p",  # 3. -adjacency
    "s51_v4_dualvq+rvq-both+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 4. -decoder-cov
    "s51_v5_dualvq+rvq-cell-30-30+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 5. rvq-cell (30, 30)
    "s51_v6_dualvq+rvq-cell-30-10+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 6. rvq-cell (30, 10)
    "s51_v7_dualvq+rvq-cell-30-300+rvq-niche-30-90+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 7. rvq-cell (30, 300)
    "s51_v8_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn8+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 8. knn=8 + sampler=8
    "s51_v9_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler8+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 9. knn=16 + sampler=8
    "s51_v10_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn24+sampler24+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 10. knn=24 + sampler=24
    "s51_v11_dualvq+rvq-both+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16-16+gnn-l2+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # 11. 2-layer GNN + sampler [16, 16]
]
# DATASET_VARIANTS entries for the s52 niche-codebook sweeps.

DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-ablations-v52"] = [
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # niche RVQ (30, 10)
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # niche RVQ (30, 30)
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",  # niche RVQ (30, 300)
]

DATASET_VARIANTS["chl59-2b_1p-ablations-v52"] = [
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # niche RVQ (30, 10)
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # niche RVQ (30, 30)
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+chl59-2b_1p",  # niche RVQ (30, 300)
]

DATASET_VARIANTS["spatch_ov_1p-ablations-v52"] = [
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # niche RVQ (30, 10)
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # niche RVQ (30, 30)
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_ov_1p",  # niche RVQ (30, 300)
]

DATASET_VARIANTS["spatch_hcc_1p-ablations-v52"] = [
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # niche RVQ (30, 10)
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # niche RVQ (30, 30)
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_hcc_1p",  # niche RVQ (30, 300)
]

DATASET_VARIANTS["spatch_coad_1p-ablations-v52"] = [
    "s52_v1_dualvq+rvq-cell-30-90+rvq-niche-30-10+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # niche RVQ (30, 10)
    "s52_v2_dualvq+rvq-cell-30-90+rvq-niche-30-30+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # niche RVQ (30, 30)
    "s52_v3_dualvq+rvq-cell-30-90+rvq-niche-30-300+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+spatch_coad_1p",  # niche RVQ (30, 300)
]

# -----------------------------------------------------------------------
# s53 — CONTINUOUS-LATENT (discretization) ablation family
#       (addresses the discrete-vs-continuous-latent reviewer comment).
#       A NEW, self-contained sweep — ONLY the
#       continuous variant; the discrete s49_v23 control is NOT listed, so
#       running this re-trains nothing that already exists. Compare its
#       metrics against the already-computed s49_v23 run.
# Runner (single-seed, metrics-only, matching how s49_v23 was evaluated):
#   bash examples/submit_dataset_sweep.sh mmb0-1b_smb1-1b_1p-continuous-ablation
# SCORING: the continuous model emits no meaningful codes, so compare it to
# s49_v23 via the EMBEDDING metrics in benchmarking.py (cnmi/cari/casw/nasw/
# gcs on obsm[latent_key]); do NOT use the code->label path for it — that
# path stays a "free clustering" bonus the discrete model gets and this can't.
# -----------------------------------------------------------------------
DATASET_VARIANTS["mmb0-1b_smb1-1b_1p-continuous-ablation"] = [
    "s53_v1_continuous-latent+decoder-cov+no-batch-int+enc-deeper+dec-w32+knn16+sampler16+cell-w1+bs512+lr7e-4+within-sec+decoupled-enc+diversity-w10+contrastWB-w10-k5+mmb0-1b_smb1-1b_1p",
]


def _ablation_summary(variant: str) -> dict:
    """A small, human-readable record of what this run is. Saved next to the
    full materialised config so you can grep / cat the summary without
    diffing the whole config across runs."""
    spec = VARIANTS[variant]
    return {
        "variant": variant,
        "description": spec["description"],
        "patches_applied": spec["patches"],
    }


# ---------------------------------------------------------------------------
# Optional helper: patch .uns metadata if your harmonised files don't have it
# ---------------------------------------------------------------------------

def patch_anndata_uns():
    """
    Run this ONCE if your two .h5ad files are missing the .uns keys SQUINT
    needs. Skip it if they're already set.
    """
    import anndata as ad

    silver = DATA_ROOT / "silver" / DATASET_NAME

    files = {
        silver / "harmonised_merfish_mouse_brain_239-batch_batch82_shared_genes.h5ad": {
            "batch": "batch82",
            "dataset_id": "mmb0",
            "tissue": "mouse_brain",
            "species": "mouse",
        },
        silver / "harmonised_starmap_plus_mouse_cns_batch15_shared_genes.h5ad": {
            "batch": "batch15",
            "dataset_id": "smb1",
            "tissue": "mouse_brain",
            "species": "mouse",
        },
    }

    for path, uns in files.items():
        adata = ad.read_h5ad(path)
        for k, v in uns.items():
            adata.uns[k] = v
        # SQUINT expects obs['cell_id'] containing "batchN"; if missing, fabricate.
        if "cell_id" not in adata.obs.columns:
            adata.obs["cell_id"] = [
                f"{uns['dataset_id']}_{uns['batch']}_{i}"
                for i in range(adata.n_obs)
            ]
        adata.write_h5ad(path)
        print(f"patched {path.name}: uns={uns}, n_obs={adata.n_obs}")


def harmonize_anndata_var():
    """
    Make `.var` identical across all AnnData files in the silver folder.

    InMemoryDatasetBlob.process() requires `adata.var` to be *strictly* equal
    across batches (`pandas.DataFrame.equals`) — not just the gene set, but
    the row order, the column set and the column dtypes.  MERFISH vs.
    STARmap+ files almost always disagree on extra `.var` metadata
    (probe ids, QC columns, ensembl ids only on one platform, etc.) and
    that's enough to trigger:

        ValueError: All batches must have the same gene panel

    This helper:
      1. computes the intersection of gene names across files,
      2. subsets each AnnData to those genes in a *common sorted order*,
      3. replaces `.var` with a fresh DataFrame whose only content is that
         shared, sorted gene index (no extra columns), and
      4. writes the harmonised file back in place.

    Run this ONCE before --build-blob if you hit the panel-mismatch error.
    """
    import anndata as ad
    import pandas as pd

    silver = DATA_ROOT / "silver" / DATASET_NAME
    files = sorted(silver.glob("*.h5ad"))
    if len(files) < 2:
        raise SystemExit(f"Expected >=2 .h5ad files under {silver}, found {len(files)}")

    # 1. Read once just to compute the shared gene set.
    var_indices = []
    for p in files:
        a = ad.read_h5ad(p, backed="r") if hasattr(ad, "read_h5ad") else ad.read(p)
        var_indices.append(set(a.var_names))
        a.file.close() if a.isbacked else None

    shared = sorted(set.intersection(*var_indices))
    if not shared:
        raise SystemExit("No genes are shared across all .h5ad files; cannot harmonise.")
    print(f"Shared genes: {len(shared)} (out of {[len(v) for v in var_indices]})")

    # 2. + 3. + 4. Subset each file to the shared gene list, drop .var columns.
    for p in files:
        a = ad.read_h5ad(p)
        before_n_vars = a.n_vars
        a = a[:, shared].copy()                  # subset + force same row order
        a.var = pd.DataFrame(index=a.var_names)  # drop all .var columns
        # Sanity: also clear any varm / varp that would still depend on old order.
        for k in list(a.varm.keys()):
            del a.varm[k]
        for k in list(a.varp.keys()):
            del a.varp[k]
        a.write_h5ad(p)
        print(f"harmonised {p.name}: n_vars {before_n_vars} -> {a.n_vars} "
              f"(.var has {len(a.var.columns)} columns)")


# ---------------------------------------------------------------------------
# Build dataset blob (one-time preprocessing)
# ---------------------------------------------------------------------------

def build_blob(dataset: str = "mmb0-1b_smb1-1b_1p", backend: str = "in-memory",
               container: str = "file", cross_panel: bool = False,
               exclude_sections: Optional[List[str]] = None,
               include_sections: Optional[List[str]] = None,
               min_panels_per_gene: int = 1, batch_key: str = "batch",
               name_suffix: str = "", resume: bool = False):
    """
    Build the in-memory PyG DatasetBlob in-process.

    `dataset` selects which builder config to use:
      - 'mmb0-1b_smb1-1b_1p' (default): MERFISH MB + STARmap MB
                               (`mmb0-1b_smb1-1b_1p_coord_aligned`).
      - 'chl59-8b_1p'        : CosMx Lung 8-sample dataset (`chl59-8b_1p`),
                               built from /nfs/team361/sb75/DATASETS/silver/chl59-8b_1p
      - 'mmb0-1b_smb1-20b_1p': MERFISH MB + 20-section STARmap+ CNS dataset
                               (`mmb0-1b_smb1-20b_1p_shared_genes`).

    Implementation note: previously this shelled out to
        squint-reproducibility/analysis/create_in_memory_dataset_blob.py
    via subprocess.  That works only if the analysis script on the cluster
    is in sync with the squint package — and it isn't always.  To avoid
    cross-repo file-sync issues we instead instantiate ``InMemoryDatasetBlob``
    here directly with the same arguments.
    """
    CONFIG_OUT_DIR.mkdir(parents=True, exist_ok=True)
    if dataset == "chl59-8b_1p":
        cfg = make_dataset_blob_config_chl59()
        cfg_path = CONFIG_OUT_DIR / "build_blob_chl59-8b_1p.yaml"
    elif dataset == "chl59-2b_1p":
        cfg = make_dataset_blob_config_chl59_2b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_chl59-2b_1p.yaml"
    elif dataset == "mmb0-1b_smb1-20b_1p":
        cfg = make_dataset_blob_config_mmb20()
        cfg_path = CONFIG_OUT_DIR / "build_blob_mmb0-1b_smb1-20b_1p.yaml"
    elif dataset == "mmb0-239b_1p":
        cfg = make_dataset_blob_config_mmb239()
        cfg_path = CONFIG_OUT_DIR / "build_blob_mmb0-239b_1p.yaml"
    elif dataset == "smb1-20b_1p":
        cfg = make_dataset_blob_config_smb1_20b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_smb1-20b_1p.yaml"
    elif dataset == "mmb0-1b_smb1-1b_1p":
        cfg = make_dataset_blob_config()
        cfg_path = CONFIG_OUT_DIR / "build_blob_mmb0-1b_smb1-1b_1p.yaml"
    elif dataset == "spatch_1p":
        cfg = make_dataset_blob_config_spatch()
        cfg_path = CONFIG_OUT_DIR / "build_blob_spatch_1p.yaml"
    elif dataset == "spatch_ov_1p":
        cfg = make_dataset_blob_config_spatch_ov()
        cfg_path = CONFIG_OUT_DIR / "build_blob_spatch_ov_1p.yaml"
    elif dataset == "spatch_hcc_1p":
        cfg = make_dataset_blob_config_spatch_hcc()
        cfg_path = CONFIG_OUT_DIR / "build_blob_spatch_hcc_1p.yaml"
    elif dataset == "spatch_coad_1p":
        cfg = make_dataset_blob_config_spatch_coad()
        cfg_path = CONFIG_OUT_DIR / "build_blob_spatch_coad_1p.yaml"
    elif dataset == "squint_hln":
        cfg = make_dataset_blob_config_squint_hln()
        cfg_path = CONFIG_OUT_DIR / "build_blob_squint_hln.yaml"
    elif dataset in ("squint_hln_allgenes", "squint_hln_hvg1k", "squint_hln_hvg2k",
                     "squint_hln_svg1k", "squint_hln_svg2k"):
        cfg = make_dataset_blob_config_squint_hln_geneset(dataset)
        cfg_path = CONFIG_OUT_DIR / f"build_blob_{dataset}.yaml"
    elif dataset == "squint_vht":
        cfg = make_dataset_blob_config_squint_vht()
        cfg_path = CONFIG_OUT_DIR / "build_blob_squint_vht.yaml"
    elif dataset == "xhs1000-39b_1p":
        cfg = make_dataset_blob_config_xhs1000_39b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_xhs1000-39b_1p.yaml"
    elif dataset == "xhc38-4b_1p":
        cfg = make_dataset_blob_config_xhc38_4b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_xhc38-4b_1p.yaml"
    elif dataset == "xhb42-3b_1p":
        cfg = make_dataset_blob_config_xhb42_3b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_xhb42-3b_1p.yaml"
    elif dataset == "xhs1000-3b_1p":
        cfg = make_dataset_blob_config_xhs_3b()
        cfg_path = CONFIG_OUT_DIR / "build_blob_xhs1000-3b_1p.yaml"
    elif dataset == "hst_corpus_110m":
        cfg = make_dataset_blob_config()  # default config name == DATASET_NAME == hst_corpus_110m
        cfg_path = CONFIG_OUT_DIR / "build_blob_hst_corpus_110m.yaml"
    else:
        raise ValueError(
            f"Unknown --build-blob-dataset {dataset!r}. Choices: "
            f"'mmb0-1b_smb1-1b_1p', 'chl59-8b_1p', 'chl59-2b_1p', "
            f"'mmb0-1b_smb1-20b_1p', 'mmb0-239b_1p', 'smb1-20b_1p', "
            f"'spatch_1p', 'spatch_ov_1p', 'spatch_hcc_1p', "
            f"'spatch_coad_1p', 'squint_hln', 'squint_hln_allgenes', "
            f"'squint_hln_hvg1k', 'squint_hln_hvg2k', 'squint_hln_svg1k', "
            f"'squint_hln_svg2k', 'squint_vht', 'xhs1000-3b_1p', 'hst_corpus_110m'."
        )
    with open(cfg_path, "w") as f:
        yaml.safe_dump(cfg, f, sort_keys=False)
    print(f"Wrote dataset-blob config to {cfg_path}")

    # Make squint's vqniche package importable (in case the active venv hasn't
    # been pip-installed editable).
    if str(SQUINT_PKG / "src") not in sys.path:
        sys.path.insert(0, str(SQUINT_PKG / "src"))

    ds_cfg = cfg["dataset"]

    if backend == "on-disk":
        # Streaming backend: one DB row per tissue section, lazily fetched.
        # Required for corpora too large to collate into one resident graph
        # (e.g. hst_corpus_110m ~= 0.6 TB in-memory). Reuses the same
        # per-section preprocessing (process_anndata_batch) as the
        # in-memory blob; only the storage layer differs.
        from vqniche.dataset.on_disk_dataset import OnDiskDatasetBlob
        out_dir = (Path(ds_cfg["data_directory_path"]) / "gold"
                   / "on-disk-PyG-dataset-blob" / ds_cfg["name"])
        print(f"Building ON-DISK dataset blob into {out_dir} (container={container})")
        dataset_blob = OnDiskDatasetBlob(
            name=ds_cfg["name"],
            feature_names=ds_cfg["feature_names"],
            label_names=ds_cfg["label_names"],
            graph_kwargs=ds_cfg["graph_kwargs"],
            data_directory_path=ds_cfg["data_directory_path"],
            pre_filter=ds_cfg["pre_filter"],
            overwrite=ds_cfg["overwrite"],
            software_paths=cfg["software_paths"],
            backend=container,
            cross_panel=cross_panel,
            exclude_sections=exclude_sections,
            include_sections=include_sections,
            output_suffix=name_suffix,
            resume=resume,
            # Corpus builds pass `--min-panels-per-gene 2` (V 18,937 -> 9,574;
            # 9,353 of the 9,363 dropped genes are chp60's, and chr78 keeps
            # 100%) and `--batch-key dataset_batch` (uns['batch'] is a
            # within-dataset counter: `batch0` names 80 unrelated experiments).
            min_panels_per_gene=min_panels_per_gene,
            batch_key=batch_key,
        )
        print(f"Built {len(dataset_blob)} sections; "
              f"section ids: {getattr(dataset_blob, 'section_ids', None)}")
        print(f"Processed data saved at {dataset_blob.processed_dir}")
    else:
        from vqniche.dataset.in_memory_dataset_blob import InMemoryDatasetBlob
        print(f"Building IN-MEMORY dataset blob into "
              f"{Path(ds_cfg['data_directory_path']) / 'gold' / 'in-memory-PyG-dataset-blob' / ds_cfg['name']}")
        dataset_blob = InMemoryDatasetBlob(
            name=ds_cfg["name"],
            feature_names=ds_cfg["feature_names"],
            label_names=ds_cfg["label_names"],
            graph_kwargs=ds_cfg["graph_kwargs"],
            data_directory_path=ds_cfg["data_directory_path"],
            pre_transform=ds_cfg["pre_transform"],
            pre_filter=ds_cfg["pre_filter"],
            overwrite=ds_cfg["overwrite"],
            software_paths=cfg["software_paths"],
        )
        for data_batch in dataset_blob:
            print(f"Batch: {data_batch.adata_batch_id}")
            print(f"Data: {data_batch}")
        print(f"Processed data saved at {dataset_blob.processed_dir}")


# ---------------------------------------------------------------------------
# torch.compile compatibility shim
# ---------------------------------------------------------------------------

def _install_compile_safe_one_hot() -> None:
    """
    Replace `torch.nn.functional.one_hot` with a scatter-based equivalent
    so torch.compile(dynamic=True) can trace through the VQ codebook
    lookup.

    Why this is needed
    ------------------
    Native `F.one_hot` calls `.numel()` internally on the input. Under
    dynamo's FakeTensor mode with dynamic shapes (mandatory for SQUINT —
    NeighborLoader yields variable-shape batches), `.numel()` can't be
    resolved on symbolically-shaped inputs and compile fails with:

        TorchRuntimeError: Failed running call_function one_hot(...):
        Cannot call numel() on tensor with symbolic sizes/strides

    The crash site is `vector_quantize_pytorch.gumbel_sample` (called
    every step inside the VQ codebook lookup), so compile crashes on
    first forward.

    The fix
    -------
    Build the one-hot tensor manually with `scatter`, which dynamo
    handles fine with symbolic shapes. Logically identical output to
    the native `F.one_hot` for the `(tensor, int num_classes)` form
    that the VQ library uses.

    Scope
    -----
    Modifies the global `torch.nn.functional.one_hot` binding. Any
    code path through `F.one_hot` in this process picks up the
    replacement, including downstream library code (vector_quantize_pytorch
    in particular). Idempotent — calling multiple times is safe (a
    private marker prevents double-patching).

    Performance
    -----------
    The scatter-based path is marginally slower than native `F.one_hot`
    in eager mode (one extra kernel launch), but the difference is
    negligible (~µs per call) compared to the 1.3-2× wall-clock win
    that torch.compile delivers once it can trace the full graph. The
    patch is applied ONLY when `compile_model=True` to avoid the
    eager-mode regression for non-compile runs.
    """
    import torch
    import torch.nn.functional as F

    # Guard against double-patching.
    if getattr(F.one_hot, "_squint_compile_safe", False):
        return

    _original_one_hot = F.one_hot

    def _scatter_one_hot(tensor, num_classes: int = -1):
        if num_classes == -1:
            # Auto-detect form (`F.one_hot(tensor)` without explicit
            # num_classes) — defer to original. The VQ library never
            # uses this form, so a graph break here is acceptable.
            return _original_one_hot(tensor, num_classes)
        # Specialise on the codebook size (a model-level constant)
        # rather than treating it as a symbolic value.
        num_classes = int(num_classes)
        out_shape = list(tensor.shape) + [num_classes]
        out = torch.zeros(out_shape, dtype=torch.long, device=tensor.device)
        return out.scatter(-1, tensor.unsqueeze(-1), 1)

    _scatter_one_hot._squint_compile_safe = True  # type: ignore[attr-defined]
    F.one_hot = _scatter_one_hot
    print(
        "Trainer: installed compile-safe F.one_hot replacement "
        "(scatter-based, for torch.compile(dynamic=True) compatibility "
        "with vector_quantize_pytorch.gumbel_sample)."
    )


# ---------------------------------------------------------------------------
# Train (in-process so we control the wandb project AND the artifact layout)
# ---------------------------------------------------------------------------

def train(
        variant: str,
        train_precision:     Optional[str] = None,
        train_strategy:      Optional[str] = None,
        compile_model:       Optional[bool] = None,
        train_deterministic: Optional[bool] = None,
        split_sections_json: Optional[str] = None,
        max_steps: Optional[int] = None,
    ):
    """
    Train SQUINT.

    Artifact layout (flat, NOT controlled by wandb; ablation-aware):
        <ARTIFACTS_DIR>/<DATASET_NAME>/<VARIANT>/<YYYYMMDD_HHMMSS>/
            user_specified_config.yaml       <- canonical config for predict()
            ablation_summary.yaml            <- variant + description + patches
            checkpoints/<best>.ckpt          <- pl.callbacks.ModelCheckpoint
            wandb/run-<id>/...               <- wandb local cache (logger)

    Cloud logging to the "squint" wandb project still works via WandbLogger.
    The directory printed at the end is what you pass to --run-dir for
    inference; predict() expects the flat layout above (no `files/` subdir).

    Parameters
    ----------
    train_precision
        Lightning Trainer(precision=...) override. `None` (default)
        means "use `cfg['trainer']['precision']`", which is
        `"bf16-mixed"` in the canonical base config. Pass any
        Lightning precision string (`"32-true"`, `"bf16-mixed"`,
        `"16-mixed"`, …) to override per-run without editing the
        base config. Bridges from the `--precision` CLI flag.
    train_strategy
        Lightning Trainer(strategy=...) override. `None` (default)
        means "use `cfg['trainer']['strategy']`", which is `"auto"`
        in the base config (resolves to `SingleDeviceStrategy` on
        single-GPU jobs, skipping the DDP find-unused traversal).
        Bridges from the `--strategy` CLI flag.
    compile_model
        Whether to wrap the model with `torch.compile(...,
        dynamic=True)` after `initialize_model`. `None` (default)
        means "use `cfg['trainer']['compile_model']`", which is
        True in the base config. Bridges from the `--no-compile`
        CLI flag (which sets this to False).
    """
    # Make the squint package importable in case the active venv hasn't been
    # pip-installed editable. We no longer import anything from
    # squint-reproducibility/analysis here — train() is fully in-process.
    if str(SQUINT_PKG / "src") not in sys.path:
        sys.path.insert(0, str(SQUINT_PKG / "src"))

    from datetime import datetime

    import torch
    import pytorch_lightning as pl
    import wandb
    from pytorch_lightning.loggers import WandbLogger

    from vqniche.initializers.initialize import (
        initialize_dataset_blob,
        initialize_databatch,
        initialize_datamodule,
        initialize_model,
        initialize_streaming_probe,
    )

    # Numerical settings (cudnn.benchmark is now controlled by the
    # Trainer via the `deterministic` cfg key — see Trainer construction
    # below — so don't set it manually here).
    torch.set_float32_matmul_precision("medium")

    # ---- Resolve variant from registry ------------------------------------
    if variant not in VARIANTS:
        raise SystemExit(
            f"Unknown variant '{variant}'. Known variants: "
            f"{sorted(VARIANTS.keys())}. (Add new variants to the VARIANTS "
            f"registry near the top of this file.)"
        )
    cfg = VARIANTS[variant]["build"]()
    summary = _ablation_summary(variant)

    # Whole-section splits from a split spec, overriding whatever in-section
    # cell split the variant configured. A CLI flag rather than a variant
    # patch because the SAME variant is trained against different splits
    # (SQUINT-holdout vs SQUINT-full differ ONLY in whether this is applied),
    # and because the spec is a generated artifact, not source.
    # A step-budget override, for preflighting a long run: the same variant and
    # the same split, stopped after a handful of steps, so the invariants that
    # decide whether the run is worth hours (decoder_covariate_dim, which
    # sections each split visits, no batch-label collision) are checked against
    # the real thing rather than assumed.
    if max_steps is not None:
        prev = cfg["trainer"].get("max_steps")
        cfg["trainer"]["max_steps"] = int(max_steps)
        # val_check_interval must not exceed the budget, or Lightning never
        # validates and `monitor=val_loss` has nothing to rank checkpoints by.
        vci = cfg["trainer"].get("val_check_interval")
        if vci and vci > int(max_steps):
            cfg["trainer"]["val_check_interval"] = max(1, int(max_steps) // 2)
            print(f"Trainer: val_check_interval {vci} -> "
                  f"{cfg['trainer']['val_check_interval']} to fit the "
                  f"--max-steps override")
        print(f"Trainer: max_steps override from CLI = {max_steps} "
              f"(variant had {prev!r})")

    if split_sections_json:
        _patch_corpus_split(cfg, split_sections_json)
    elif cfg["dataset"].get("requires_split_sections"):
        raise SystemExit(
            f"Variant {variant!r} covers a multi-dataset corpus and requires "
            f"--split-sections-json (e.g. _splitspec.json).\n"
            f"Without it every section is used for training, so the model sees "
            f"all 219 held-out sections and can support no zero-shot claim -- "
            f"and nothing in the logs would say so.\n"
            f"Pass --split-sections-json, or use a variant that does not "
            f"declare `requires_split_sections`."
        )

    # Apply CLI-level precision override (if any). Keeps the base
    # config's bf16-mixed default in place when `train_precision is
    # None`, but lets `--precision 32-true` / `--precision 16-mixed`
    # win for a single run without editing make_train_config_*().
    if train_precision is not None:
        prev = cfg["trainer"].get("precision", "32-true")
        cfg["trainer"]["precision"] = train_precision
        print(
            f"Trainer: precision override from CLI = {train_precision} "
            f"(base config had {prev!r})"
        )
    # Same pattern for strategy + compile.
    if train_strategy is not None:
        prev = cfg["trainer"].get("strategy", "ddp_find_unused_parameters_true")
        cfg["trainer"]["strategy"] = train_strategy
        print(
            f"Trainer: strategy override from CLI = {train_strategy} "
            f"(base config had {prev!r})"
        )
    if compile_model is not None:
        prev = cfg["trainer"].get("compile_model", False)
        cfg["trainer"]["compile_model"] = bool(compile_model)
        print(
            f"Trainer: compile_model override from CLI = {bool(compile_model)} "
            f"(base config had {prev!r})"
        )
    if train_deterministic is not None:
        prev = cfg["trainer"].get("deterministic", True)
        cfg["trainer"]["deterministic"] = bool(train_deterministic)
        print(
            f"Trainer: deterministic override from CLI = "
            f"{bool(train_deterministic)} (base config had {prev!r})"
        )

    # ---- Resolve our own RUN_DIR ------------------------------------------
    # Each variant gets its own subdir under the dataset, so different
    # ablations don't collide and we can grep run dirs by variant.
    # Filesystem-safe slug: 'poc+nbr' -> 'poc+nbr' is fine on POSIX, but we
    # play it safe and keep '+' (no spaces, no slashes).
    variant_slug = variant.replace("/", "_").replace(" ", "_")
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    # Append the seed so PARALLEL multi-seed jobs that start in the same
    # wall-clock second get DISTINCT run dirs. Without this, the seconds-
    # granularity timestamp + `mkdir(exist_ok=True)` below let two seed jobs
    # silently share one dir and clobber each other's predicted_adata.h5ad /
    # checkpoints — the cause of duplicate run_dirs in seed_run_index.csv.
    # Matches the `<TS>_seed_<N>` layout the multi-seed runner assumes.
    _run_seed = cfg.get("experiment", {}).get("seed", None)
    if _run_seed is not None:
        timestamp = f"{timestamp}_seed{_run_seed}"
    # Route artifacts under the variant's CHOSEN dataset, using the
    # short `dataset_tag` (e.g. `mmb0-1b_smb1-1b_1p`) rather than the
    # full `dataset_name` (e.g. `mmb0-1b_smb1-1b_1p_coord_aligned`). The
    # tag drops technical suffixes (`_coord_aligned`, `_shared_genes`)
    # that belong on the silver / blob paths but make on-disk artifact
    # dirs unwieldy. Falls back to `dataset_name` if a variant doesn't
    # set a tag.
    run_dataset_tag = cfg["dataset"].get(
        "dataset_tag",
        cfg["dataset"].get("dataset_name", DATASET_NAME),
    )
    run_dir = ARTIFACTS_DIR / run_dataset_tag / variant_slug / timestamp
    run_dir.mkdir(parents=True, exist_ok=True)
    ckpt_dir = run_dir / "checkpoints"
    ckpt_dir.mkdir(parents=True, exist_ok=True)

    # Persist the full materialised config in TWO places:
    #   (a) the canonical location predict() expects (flat, inside run_dir),
    #   (b) an at-a-glance copy under <ARTIFACTS_DIR>/configs/ for diffing
    #       across runs.
    config_path_in_run = run_dir / "user_specified_config.yaml"
    with open(config_path_in_run, "w") as f:
        yaml.safe_dump(cfg, f, sort_keys=False)

    # Ablation summary lives next to the config so a quick `cat` tells you
    # what kind of run this was without diffing the whole config.
    summary_path = run_dir / "ablation_summary.yaml"
    with open(summary_path, "w") as f:
        yaml.safe_dump(summary, f, sort_keys=False)

    CONFIG_OUT_DIR.mkdir(parents=True, exist_ok=True)
    config_path_archive = CONFIG_OUT_DIR / f"train_{variant_slug}_{timestamp}.yaml"
    with open(config_path_archive, "w") as f:
        yaml.safe_dump(cfg, f, sort_keys=False)

    print(f"Variant: {variant}")
    print(f"  description: {summary['description']}")
    print(f"  patches:     {summary['patches_applied']}")
    print(f"Run directory: {run_dir}")
    print(f"  config:      {config_path_in_run}")
    print(f"  summary:     {summary_path}")
    print(f"  checkpoints: {ckpt_dir}")
    print(f"  archive:     {config_path_archive}")

    # ---- Resolve HVG transform based on apply_hvg flag --------------------
    # The config stores apply_hvg and n_hvg as first-class keys so variants
    # can toggle them via a simple patch, without duplicating the full
    # gene_count_transform_names list.  We materialise the transform list here
    # just before handing the config to the initializers.
    if cfg["dataset"].get("apply_hvg", False):
        cfg["dataset"]["gene_count_transform_names"] = ["SubsetHVG"]
        cfg["dataset"]["gene_count_transform_params"] = {
            "n_genes": cfg["dataset"].get("n_hvg", 2000)
        }
    else:
        cfg["dataset"]["gene_count_transform_names"] = []
        cfg["dataset"]["gene_count_transform_params"] = {}

    # ---- Determinism (mirror train_model.train) ---------------------------
    pl.seed_everything(cfg["experiment"]["seed"])

    # ---- Dataset / Databatch / Datamodule ---------------------------------
    dataset_blob = initialize_dataset_blob(cfg)

    # Translate `train_batches` / `val_batches` (which are stored as
    # `adata_batch_id` values — what `SpatialBatchSplit` matches against
    # `data.adata_batch_id`) into the position indices that
    # `cfg["dataset"]["adata_batch_idx"]` actually expects. The mapping
    # is dataset-dependent: it depends on the value of
    # `data.adata_batch_id` for each section, which is only known after
    # the blob is loaded. Doing this at runtime avoids hardcoded
    # id-to-position helpers per dataset (which were fragile — e.g.
    # mmb20 uses parsed `uns['batch']` ids while chl59 falls back to
    # file alphabetical position).
    #
    # `test_batches` are NOT loaded — they're held out from training
    # entirely and only consumed by predict().
    #
    # `train_batches` is OPTIONAL: when omitted (or empty) the training
    # set defaults to every blob section that isn't in `test_batches` /
    # `val_batches`. This lets variants only specify what's held out for
    # evaluation; the train side is auto-derived from the blob. If neither
    # train nor test is set, every section is used for training.
    _ts_params = cfg["dataset"].get("train_transform_params", {})
    _train_ids = list(_ts_params.get("train_batches", []) or [])
    _val_ids   = list(_ts_params.get("val_batches",   []) or [])
    # `test_batches` may also be the sentinel string "rest" — meaning
    # "every blob section not in train+val". This is the symmetric
    # opposite of the train-side auto-default below. Used by
    # `_patch_dual_spatch` (spatch_1p) so a smoke test can say "train one section,
    # test everything else" without enumerating the other section ids
    # up front. Keep the sentinel as-is here; it's resolved AFTER the
    # blob is loaded (id_to_pos is needed to enumerate "rest").
    _test_ids_raw = _ts_params.get("test_batches", []) or []
    if isinstance(_test_ids_raw, str):
        if _test_ids_raw != "rest":
            raise ValueError(
                f"test_batches must be a list of ids or the sentinel "
                f"string 'rest'; got {_test_ids_raw!r}."
            )
        _test_ids_is_rest = True
        _test_ids = []   # placeholder until we have the blob's ids
    else:
        _test_ids_is_rest = False
        _test_ids = list(_test_ids_raw)

    # adata_batch_id -> row position. The on-disk blob records the ids in its
    # manifest, so read them from there: indexing every row would deserialize
    # the whole corpus (one full section per integer) before training starts.
    _id_to_pos: dict = {}
    _manifest_ids = getattr(dataset_blob, "section_ids", None)
    if _manifest_ids:
        for _pos, _bid in enumerate(_manifest_ids):
            _id_to_pos[int(_bid)] = _pos
    else:
        for _pos in range(len(dataset_blob)):
            _d = dataset_blob[_pos]
            _id_to_pos[int(_d.adata_batch_id)] = _pos
    _all_blob_ids = sorted(_id_to_pos.keys())

    if _test_ids_is_rest:
        _excluded = set(int(i) for i in _train_ids) | set(int(i) for i in _val_ids)
        _test_ids = [int(i) for i in _all_blob_ids if int(i) not in _excluded]
        cfg["dataset"]["train_transform_params"]["test_batches"] = list(_test_ids)
        print(
            f"test_batches='rest' -> resolved to all blob sections "
            f"except train+val: {_test_ids}"
        )

    if not _train_ids:
        _excluded = set(int(i) for i in _test_ids) | set(int(i) for i in _val_ids)
        _train_ids = [int(i) for i in _all_blob_ids if int(i) not in _excluded]
        cfg["dataset"]["train_transform_params"]["train_batches"] = list(_train_ids)
        print(
            f"train_batches not specified -> defaulting to all blob "
            f"sections except test+val: {_train_ids}"
        )

    _needed_ids = list(dict.fromkeys(_train_ids + _val_ids))
    if _needed_ids:
        _missing = [i for i in _needed_ids if int(i) not in _id_to_pos]
        if _missing:
            raise RuntimeError(
                f"Requested adata_batch_ids {_missing} are not present "
                f"in the dataset blob. Available ids (in the blob): "
                f"{_all_blob_ids}. Either rebuild the blob or "
                f"adjust the train/val/test ids in the variant patch."
            )
        cfg["dataset"]["adata_batch_idx"] = sorted(set(
            _id_to_pos[int(i)] for i in _needed_ids
        ))
        print(
            f"Resolved adata_batch_idx from train+val ids "
            f"{_needed_ids} -> positions "
            f"{cfg['dataset']['adata_batch_idx']} via blob's "
            f"adata_batch_id mapping."
        )

    # Re-save the (now fully-resolved) config. The earlier dump at
    # `config_path_in_run` was made BEFORE this block ran — at that
    # point `adata_batch_idx=[]` (placeholder set by the dataset patch
    # since positions can only be derived after the blob is loaded) and
    # `train_batches` may have been empty (auto-default fills it from
    # blob ids minus test/val). `predict()` reads
    # `user_specified_config.yaml` to know which sections to run
    # inference on; if we leave the placeholder there, predict() loads
    # zero sections and crashes with
    # `batch_size=0 (BatchSampler)` deep inside torch_geometric.
    # The archive copy gets the same refresh so on-disk diffs across
    # runs remain comparable.
    with open(config_path_in_run, "w") as f:
        yaml.safe_dump(cfg, f, sort_keys=False)
    with open(config_path_archive, "w") as f:
        yaml.safe_dump(cfg, f, sort_keys=False)

    # Re-init the blob now that `train_transform_params['train_batches']`
    # is fully resolved. `initialize_dataset_blob` builds the
    # `SpatialBatchSplit` transform from the cfg AT CONSTRUCTION TIME
    # and stores it on the blob; mutating the cfg after the fact does
    # NOT update the live transform, so the auto-defaulted train_batches
    # would be ignored — every section's `SpatialBatchSplit.forward()`
    # check would miss, no `train_mask` would be set, and the next step
    # would AttributeError on `data.train_mask`.
    # Re-initialising is cheap: the on-disk processed blob is reused, only
    # the transform pipeline is rebuilt.
    dataset_blob = initialize_dataset_blob(cfg)

    # Streaming backend: `initialize_databatch` would collate every section
    # into one resident graph, which is precisely what streaming avoids. Use a
    # one-section probe for the model dims instead, and hand the blob itself to
    # the datamodule (which dispatches on its type).
    _streaming = cfg["dataset"].get("backend", "in-memory") == "on-disk"
    if _streaming:
        # ---- ONE batch label->dense map, shared by the model and the loader --
        # The model's decoder-covariate embedding is sized from this map
        # (`initialize_streaming_probe` -> n_distinct_batches) and the loader
        # stamps `adata_batch_ids` from it. If the two are derived separately
        # they disagree the moment splits restrict one of them: sorted-unique
        # ids RENUMBER on any change of membership, so every cell would read a
        # valid-but-wrong embedding row. Compute it once, here, and hand the
        # same object to both.
        #
        # Restricted to the TRAIN sections when whole-section splits are in
        # use, which is what makes zero-shot evaluation correct: every
        # embedding row then receives gradient, and a held-out section's label
        # is absent, so it takes the unseen path (mean of the trained rows)
        # rather than conditioning on an untrained one.
        _split_sections = cfg["datamodule"].get("split_sections", None)
        _label_map = None
        if _split_sections:
            _train_rows = _resolve_train_rows(dataset_blob, _split_sections)
            _label_map = dataset_blob.batch_label_to_dense(rows=_train_rows)
            print(f"Streaming: batch map restricted to {len(_train_rows)} train "
                  f"sections -> {len(_label_map)} batches "
                  f"(blob-wide would be "
                  f"{len(dataset_blob.batch_label_to_dense())}).")
            cfg["datamodule"]["batch_label_to_dense"] = _label_map
        data_batch = initialize_streaming_probe(
            cfg, dataset_blob, batch_label_to_dense=_label_map,
        )
        _datamodule_input = dataset_blob
    else:
        data_batch = initialize_databatch(config=cfg, dataset_blob=dataset_blob)
        _datamodule_input = data_batch

    datamodule_batch = initialize_datamodule(
        config=cfg,
        data=_datamodule_input,
        obs_per_batch_id=getattr(dataset_blob, 'obs_per_batch_id', None),
    )

    # ---- Model: bind condition dims if FiLM is enabled --------------------
    if "conditioning_params" in cfg["model"]["encoder_params"]:
        cfg["model"]["encoder_params"]["conditioning_params"]["condition_dim"] = (
            data_batch.encoder_condition_dim
        )
    if "spatial_prior_params" in cfg["model"]["encoder_params"]:
        cfg["model"]["encoder_params"]["spatial_prior_params"][
            "spatial_prior_feature_dim"
        ] = data_batch.spatial_prior_feature_dim
    # Helper: derive the number of distinct batches in this run from the
    # densified per-cell batch IDs. `adata_batch_ids` is ALWAYS populated
    # by `initialize_databatch` (regardless of whether encoder FiLM is on),
    # so this is a reliable source. `encoder_condition_dim` was the wrong
    # source — it only gets set when FiLM is enabled, which is not the
    # case for adversarial-only or covariate-only variants.
    def _n_distinct_batches(db) -> int:
        # The streaming probe carries the corpus-wide count explicitly: it is
        # built from ONE section, so `adata_batch_ids.max()` there would only
        # ever see that section's own batch.
        explicit = getattr(db, "n_distinct_batches", None)
        if explicit is not None:
            return int(explicit)
        return int(db.adata_batch_ids.max().item()) + 1

    # NicheCompass-style decoder covariate (concat-based batch correction).
    # When the user enables it via _patch_dual_decoder_covariate, the dual
    # model expects `decoder_covariate_dim` to equal the dimensionality of
    # the per-cell batch one-hot. Set it from the data loader here.
    if (
        cfg["model"].get("model_name") == "VQNiche_Dual"
        and cfg["model"].get("decoder_covariate_dim_request") is True
    ):
        cfg["model"]["decoder_covariate_dim"] = _n_distinct_batches(data_batch)

    # Domain-adversarial batch-invariance head. When requested, sized to
    # the number of distinct batches.
    if (
        cfg["model"].get("model_name") == "VQNiche_Dual"
        and cfg["model"].get("adversarial_batch_dim_request") is True
    ):
        cfg["model"]["adversarial_batch_dim"] = _n_distinct_batches(data_batch)

    # Bind FiLM condition dims onto whichever decoder param dicts the
    # current config carries. Single-codebook configs (VQNiche etc.) use
    # `attribute_decoder_params` + `adjacency_decoder_params`. The
    # dual-codebook config (VQNiche_Dual) uses
    # `attribute_decoder_cell_params` + `attribute_decoder_niche_params`
    # and has no adjacency decoder.
    for dec_key in ("attribute_decoder_params",
                    "attribute_decoder_cell_params",
                    "attribute_decoder_niche_params"):
        dec_params = cfg["model"].get(dec_key)
        if dec_params is None:
            continue
        if "conditioning_params" in dec_params:
            dec_params["conditioning_params"]["condition_dim"] = (
                data_batch.attr_decoder_condition_dim
            )
    if "adjacency_decoder_params" in cfg["model"]:
        adj_params = cfg["model"]["adjacency_decoder_params"]
        if "conditioning_params" in adj_params:
            adj_params["conditioning_params"]["condition_dim"] = (
                data_batch.adj_decoder_condition_dim
            )

    # Cross-panel blobs build the model at the gene VOCABULARY width, not at
    # this object's own gene width. `initialize_streaming_probe` attaches
    # `gene_vocab_size` for exactly this; see the comment there for why it
    # cannot be delivered via `num_features` (a shadowing @property). One value
    # sizes every vocabulary-wide tensor, because they all derive from
    # `in_channels`: the encoder trunks' first layers, both decoder output
    # layers, `dispersion` and `dispersion_niche`.
    _in_channels = getattr(data_batch, "gene_vocab_size", None)
    if _in_channels is None:
        _in_channels = data_batch.num_features
    else:
        print(f"Model input width: {_in_channels} (gene vocabulary), vs "
              f"{data_batch.num_features} genes on the probe section.")

    model = initialize_model(
        config=cfg,
        in_channels=_in_channels,
        out_channels=data_batch.num_classes,
    )

    # ---- Optional torch.compile wrap ----------------------------------
    # Defaults to True via the base config. `dynamic=True` is critical
    # because NeighborLoader yields variable-shape batches (different
    # seed + neighbour counts per step) — without it, torch.compile
    # recompiles every batch and loses all benefit. First batch is
    # slow (compile + autotune); subsequent batches see the speedup.
    #
    # Before compiling, install the compile-safe F.one_hot replacement
    # (scatter-based, see `_install_compile_safe_one_hot` for the full
    # rationale). Without it, dynamo crashes on the VQ codebook's
    # `gumbel_sample` because native F.one_hot can't handle symbolic
    # input shapes. With the patch, the entire model (including the
    # VQ subgraph) compiles cleanly.
    #
    # We also enable `torch._dynamo.config.suppress_errors = True` as
    # a belt-and-braces safety net: if any OTHER unforeseen subgraph
    # turns out to be incompatible, dynamo falls back to eager on
    # that frame instead of crashing the whole training run.
    _compile_model = bool(cfg["trainer"].get("compile_model", False))
    if _compile_model:
        try:
            _install_compile_safe_one_hot()
            import torch._dynamo
            torch._dynamo.config.suppress_errors = True
            print(
                "Trainer: applying torch.compile(model, dynamic=True). "
                "Compile-safe F.one_hot installed; suppress_errors=True "
                "enabled as backstop."
            )
            model = torch.compile(model, dynamic=True)
            print("Trainer: torch.compile applied (first batch will be slow).")
        except Exception as e:  # noqa: BLE001
            print(
                f"WARNING: torch.compile failed ({type(e).__name__}: {e}); "
                f"falling back to eager-mode model. Pass `--no-compile` to "
                f"silence this warning."
            )
    else:
        print("Trainer: compile_model=False (eager-mode model).")

    # ---- Logger: wandb cloud logging, local cache lives inside run_dir ----
    logging_enabled = cfg["logging"].get("enabled", True)
    if logging_enabled:
        # Make timestamp prominent in the wandb run identity:
        #   - `name`: shows up as the run title in the wandb UI
        #   - `tags`: filterable tag (variant + timestamp)
        #   - `config["timestamp"]` and `config["run_dir"]`: top-level keys in
        #     the run's "Config" panel so the timestamp is the first thing
        #     visible alongside the variant.
        run_name = f"{variant}-{timestamp}"
        # Wandb caps tag length at 64 chars; truncate the `variant:` tag if
        # the variant slug itself is long enough that the prefixed tag
        # would overflow. The full variant name is still in `name=run_name`
        # (no length cap), in `config["variant"]`, and in the run_dir path,
        # so nothing is lost — this only affects the filterable tag in the
        # wandb UI.
        def _wandb_tag(prefix: str, value: str, max_len: int = 64) -> str:
            tag = f"{prefix}{value}"
            if len(tag) <= max_len:
                return tag
            keep = max_len - len(prefix) - 1  # leave 1 char for an ellipsis marker
            return f"{prefix}{value[:keep]}~"
        # `init_timeout=300` raises the default `wandb.init` 90s ceiling
        # so the LSF cluster's slow handshake to api.wandb.ai (especially
        # from the DDP child rank, which re-runs wandb.init) doesn't
        # crash long-running ablations. `WANDB_INIT_TIMEOUT=300` is also
        # set in main() so children inherit it; this kwarg makes the
        # value self-documenting at the construction site.
        # wandb caps group names at 128 chars. `adata_batch_idx` can be a
        # long list (e.g. all 239 sections trained at once -> [0,1,...,238]),
        # which overflows the limit. Keep the detailed `dataset:batch=[...]`
        # group for short lists (so existing runs don't re-group), fall back
        # to a batch COUNT for long ones, and hard-cap at 128 either way.
        _group = (f"{cfg['dataset']['dataset_name']}:"
                  f"batch={cfg['dataset']['adata_batch_idx']}")
        if len(_group) > 128:
            try:
                _n_batch = len(cfg['dataset']['adata_batch_idx'])
            except TypeError:
                _n_batch = cfg['dataset']['adata_batch_idx']
            _group = (f"{cfg['dataset']['dataset_name']}:"
                      f"n_batch={_n_batch}")
        _group = _group[:128]
        logger = WandbLogger(
            save_dir=str(run_dir),                    # local cache -> <run_dir>/wandb/
            project=WANDB_PROJECT,
            name=run_name,
            # Group by dataset+batches so all ablations of one dataset cluster
            # together. The variant goes in `tags` so we can filter the wandb
            # UI to e.g. all "poc+nbr" runs across timestamps.
            group=_group,
            job_type="train",
            tags=[
                _wandb_tag("variant:",   variant),
                _wandb_tag("timestamp:", timestamp),
            ],
            mode="offline" if cfg["logging"]["offline"] else "online",
            log_model=cfg["logging"]["log_model"],
            settings=wandb.Settings(init_timeout=300),
            config={
                # Hoist run identity to the top of the wandb Config panel.
                "timestamp":         timestamp,
                "variant":           variant,
                "run_dir":           str(run_dir),
                "ablation_summary":  summary,
                **cfg,
            },
        )
    else:
        logger = False

    # ---- Callbacks: checkpoint + early stopping ----------------------------
    callbacks: list = []

    enable_checkpointing = cfg["trainer"]["enable_checkpointing"]
    if enable_checkpointing:
        monitor = cfg["trainer"]["monitor"]
        # filename templates known to ModelCheckpoint -> map monitor metric to
        # a filename pattern PL can format. Fall back to val_loss.
        _filename_by_monitor = {
            "val_loss":                                 "{epoch}-{val_loss:.3f}",
            "train_loss":                               "{epoch}-{train_loss:.3f}",
            "val_pearson_cell_wise":                    "{epoch}-{val_pearson_cell_wise:.2f}",
            "val_pearson_1hop_nbr":                     "{epoch}-{val_pearson_1hop_nbr:.2f}",
            "val_pearson_gene_wise":                    "{epoch}-{val_pearson_gene_wise:.2f}",
            "val_pearson_gene_wise_1hop_nbr":           "{epoch}-{val_pearson_gene_wise_1hop_nbr:.2f}",
            "train_pearson_cell_wise":                  "{epoch}-{train_pearson_cell_wise:.2f}",
            "train_pearson_1hop_nbr":                   "{epoch}-{train_pearson_1hop_nbr:.2f}",
            "train_pearson_gene_wise":                  "{epoch}-{train_pearson_gene_wise:.2f}",
            "train_pearson_gene_wise_1hop_nbr":         "{epoch}-{train_pearson_gene_wise_1hop_nbr:.2f}",
            # log1p variants — cell branch
            "train_pearson_gene_wise_log1p":                    "{epoch}-{train_pearson_gene_wise_log1p:.3f}",
            "train_pearson_gene_wise_log1p_median":             "{epoch}-{train_pearson_gene_wise_log1p_median:.3f}",
            "train_pearson_gene_wise_hvg50_log1p":              "{epoch}-{train_pearson_gene_wise_hvg50_log1p:.3f}",
            "train_pearson_gene_wise_hvg50_log1p_median":       "{epoch}-{train_pearson_gene_wise_hvg50_log1p_median:.3f}",
            "train_pearson_cell_wise_log1p":                    "{epoch}-{train_pearson_cell_wise_log1p:.3f}",
            "train_pearson_cell_wise_log1p_median":             "{epoch}-{train_pearson_cell_wise_log1p_median:.3f}",
            # log1p variants — neighbourhood branch
            "train_pearson_gene_wise_1hop_nbr_log1p":           "{epoch}-{train_pearson_gene_wise_1hop_nbr_log1p:.3f}",
            "train_pearson_gene_wise_1hop_nbr_log1p_median":    "{epoch}-{train_pearson_gene_wise_1hop_nbr_log1p_median:.3f}",
            "train_pearson_gene_wise_hvg50_1hop_nbr_log1p":     "{epoch}-{train_pearson_gene_wise_hvg50_1hop_nbr_log1p:.3f}",
            "train_pearson_gene_wise_hvg50_1hop_nbr_log1p_median": "{epoch}-{train_pearson_gene_wise_hvg50_1hop_nbr_log1p_median:.3f}",
            "train_pearson_cell_wise_1hop_nbr_log1p":           "{epoch}-{train_pearson_cell_wise_1hop_nbr_log1p:.3f}",
            "train_pearson_cell_wise_1hop_nbr_log1p_median":    "{epoch}-{train_pearson_cell_wise_1hop_nbr_log1p_median:.3f}",
            # val equivalents
            "val_pearson_gene_wise_log1p":                      "{epoch}-{val_pearson_gene_wise_log1p:.3f}",
            "val_pearson_gene_wise_log1p_median":               "{epoch}-{val_pearson_gene_wise_log1p_median:.3f}",
            "val_pearson_gene_wise_hvg50_log1p":                "{epoch}-{val_pearson_gene_wise_hvg50_log1p:.3f}",
            "val_pearson_gene_wise_hvg50_log1p_median":         "{epoch}-{val_pearson_gene_wise_hvg50_log1p_median:.3f}",
            "val_pearson_cell_wise_log1p":                      "{epoch}-{val_pearson_cell_wise_log1p:.3f}",
            "val_pearson_cell_wise_log1p_median":               "{epoch}-{val_pearson_cell_wise_log1p_median:.3f}",
            "val_pearson_gene_wise_1hop_nbr_log1p":             "{epoch}-{val_pearson_gene_wise_1hop_nbr_log1p:.3f}",
            "val_pearson_gene_wise_1hop_nbr_log1p_median":      "{epoch}-{val_pearson_gene_wise_1hop_nbr_log1p_median:.3f}",
            "val_pearson_gene_wise_hvg50_1hop_nbr_log1p":       "{epoch}-{val_pearson_gene_wise_hvg50_1hop_nbr_log1p:.3f}",
            "val_pearson_gene_wise_hvg50_1hop_nbr_log1p_median": "{epoch}-{val_pearson_gene_wise_hvg50_1hop_nbr_log1p_median:.3f}",
            "val_pearson_cell_wise_1hop_nbr_log1p":             "{epoch}-{val_pearson_cell_wise_1hop_nbr_log1p:.3f}",
            "val_pearson_cell_wise_1hop_nbr_log1p_median":      "{epoch}-{val_pearson_cell_wise_1hop_nbr_log1p_median:.3f}",
        }
        if monitor in (None, "none", ""):
            # No metric to rank by -- save on a pure step schedule. Needed when
            # the validation loop is disabled (`_patch_no_val`): with no
            # `val_loss` a monitored ModelCheckpoint would warn
            # "could not find the monitored key" and save nothing. `train_loss`
            # is not a usable substitute either, because it is logged with
            # `on_epoch=True, on_step=False` (base_model.py:842) so it only
            # enters `callback_metrics` at epoch end -- which never arrives
            # when the step budget is a fraction of one epoch.
            #
            # `{step}` rather than `{epoch}` in the filename for the same
            # reason: epoch is pinned at 0 for the whole run.
            monitor = None
            filename = "{epoch}-{step}"
        else:
            filename = _filename_by_monitor.get(
                monitor, "{epoch}-{val_loss:.3f}"
            )
            if monitor not in _filename_by_monitor:
                monitor = "val_loss"

        checkpoint_params = cfg["trainer"]["checkpoint_params"]
        callbacks.append(
            pl.callbacks.ModelCheckpoint(
                dirpath=str(ckpt_dir),
                monitor=monitor,
                filename=filename,
                **checkpoint_params,
            )
        )

    # ---- Early-stopping callback -------------------------------------------
    es_cfg = cfg["trainer"].get("early_stopping_params", {})
    if es_cfg.get("enabled", True):
        callbacks.append(
            pl.callbacks.EarlyStopping(
                monitor   = es_cfg.get("monitor", "train_nb_attribute_reconstruction_loss"),
                mode      = es_cfg.get("mode", "min"),
                patience  = es_cfg.get("patience", 20),
                min_delta = es_cfg.get("min_delta", 0.1),
                verbose   = True,
            )
        )

    callbacks = callbacks or None   # PL wants None, not [], when empty

    # ---- Trainer ----------------------------------------------------------
    # bf16-mixed by default (see base config `trainer.precision`).
    # `.get` fallback to "32-true" keeps backward compat for any caller
    # that builds a cfg without this key.
    _train_precision = cfg["trainer"].get("precision", "32-true")
    # Strategy: "auto" by default (see base config `trainer.strategy`).
    # `.get` fallback to the historic value preserves compat for
    # callers that build a cfg without the key.
    _train_strategy = cfg["trainer"].get(
        "strategy", "ddp_find_unused_parameters_true",
    )
    # Determinism: False by default (see base config). `benchmark` is
    # the inverse — when we're not enforcing determinism, let cuDNN
    # autotune for ~5-10% speedup. Backward-compat default `True`
    # (legacy) for any caller that builds a cfg without the new key.
    _train_deterministic = bool(cfg["trainer"].get("deterministic", True))
    _train_benchmark     = not _train_deterministic
    print(f"Trainer: precision     = {_train_precision}")
    print(f"Trainer: strategy      = {_train_strategy}")
    print(f"Trainer: deterministic = {_train_deterministic} "
          f"(benchmark = {_train_benchmark})")
    trainer = pl.Trainer(
        accelerator="auto",
        devices="auto",
        # Where Lightning puts anything it writes without an explicit path.
        # Set so `BaseModel.on_fit_end`'s fallback lands in the RUN dir rather
        # than the cwd (i.e. the repo root) when no logger is attached -- see
        # that hook for why the no-logger case exists.
        default_root_dir=str(run_dir),
        deterministic=_train_deterministic,
        benchmark=_train_benchmark,
        precision=_train_precision,
        logger=logger,
        callbacks=callbacks,
        strategy=_train_strategy,
        max_epochs=cfg["trainer"]["max_epochs"],
        # Optional STEP budget. Lightning's default of -1 means "no step
        # limit, stop on max_epochs", so variants that don't set the key
        # behave exactly as before. `_patch_step_budget` sets it for runs
        # where epochs are the wrong unit — see that function's docstring.
        max_steps=int(cfg["trainer"].get("max_steps", -1)),
        # Run val every N train epochs (N=2 by default — see base
        # config). Halves val-pass overhead at the cost of running
        # early stopping on a coarser val grid. `getattr`-style
        # fallback to 1 keeps backward compat for any cfg that
        # doesn't carry the key.
        # None means "ignore epoch boundaries and validate purely on the
        # global step grid", which is what a step budget needs — see
        # `_patch_step_budget`. Kept nullable rather than coerced with int(),
        # which would raise on None.
        check_val_every_n_epoch=(
            None if cfg["trainer"].get("check_val_every_n_epoch", 1) is None
            else int(cfg["trainer"]["check_val_every_n_epoch"])
        ),
        # Validate every N optimizer steps when set to an int (Lightning's
        # default is the float 1.0 = "once per training epoch"). Required
        # alongside `max_steps`: on a corpus-scale dataset a step budget is
        # a fraction of ONE epoch, so an epoch-grained val cadence would
        # never fire -- and with `check_val_every_n_epoch != 1`
        # ModelCheckpoint saves only from `on_validation_end`, so the run
        # would finish with an empty `checkpoints/`. Keep the type: int =
        # steps, float = fraction of an epoch.
        val_check_interval=cfg["trainer"].get("val_check_interval", 1.0),
        # 0 disables the validation loop outright (`_patch_no_val`). Default
        # 1.0 = "use all of it", so variants that don't set it are unaffected.
        limit_val_batches=cfg["trainer"].get("limit_val_batches", 1.0),
        enable_checkpointing=enable_checkpointing,
        num_sanity_val_steps=0,
        enable_progress_bar=False,
        enable_model_summary=True,
    )

    # ---- Fit --------------------------------------------------------------
    print("Training Model...")
    try:
        trainer.fit(model=model, datamodule=datamodule_batch)
    finally:
        # WandbLogger.experiment is the `wandb.Run`; closing it flushes uploads.
        if logging_enabled and logger is not False:
            try:
                logger.experiment.finish()
            except Exception:
                pass

    print()
    print("=" * 78)
    print("Training finished. Run directory (pass to --run-dir when predicting):")
    print(f"  {run_dir}")
    print("=" * 78)
    return run_dir


# ---------------------------------------------------------------------------
# Predict / inference: folder of .h5ad -> single AnnData with SQUINT outputs
# ---------------------------------------------------------------------------

def _infer_adata_files_in_dir(silver_dir: Path) -> list[Path]:
    files = sorted(p for p in silver_dir.glob("*.h5ad"))
    if not files:
        raise FileNotFoundError(f"No .h5ad files found in {silver_dir}")
    return files


def _build_clean_adata_from_inference(
    inference_data: dict,
    source_adatas: dict,
    id_to_path: dict,
    gene_names: list | None = None,
    test_batch_ids: list | None = None,
    test_regions: dict | None = None,
    # Legacy kwargs kept for signature back-compat — ignored.
    label_categories_dict: dict | None = None,
    source_paths: list | None = None,
    obs_per_batch_id: dict | None = None,
    obs_per_filename: dict | None = None,
    cross_panel: bool = False,
) -> "ad.AnnData":
    """
    Build the predicted AnnData by starting from the source AnnDatas
    themselves and bolting model outputs on top.

    Strategy
    --------
    1. For each cell in the inference output (ordered by
       `inference_data["adata_batch_ids"]` + `obs_row_index`), slice
       the corresponding row out of its source AnnData. Sub-AnnDatas
       are concatenated and reordered to match the inference cell order.
    2. If `gene_names` is supplied (typically the canonical / HVG gene
       panel saved at blob-build time), reindex `var` to that set so
       `adata.X` matches the model's input dimensionality (and therefore
       so do the model output layers).
    3. Stamp model outputs into `obsm` / `obs` / `layers` / `uns`. The
       `data_split` column is derived from `test_batch_ids` (which the
       saved training config carries forward via `train_transform_params`'
       `test_batches`).

    Why this is preferable to synthesising an AnnData from torch tensors:
    every original `obs` column (e.g. `cell_type`, `Sub_molecular_*`,
    `ccf_region_name`, `donor_id`, …) and `var` metadata flows through
    automatically. `ad.concat(..., join="outer")` handles partially-
    present columns by NaN-filling cells from sources that don't carry
    them. No manual obs propagation, no h5py dtype-coercion gymnastics,
    no failure modes from mismatched id-derivations between
    `obs_per_batch_id.pkl` and `dataset_blob.pt`.

    Parameters
    ----------
    inference_data:
        The collated dict produced by `collate_predict_outputs`. Must
        contain `adata_batch_ids` and `obs_row_index` per cell.
    source_adatas:
        Mapping `adata_batch_id -> ad.AnnData` for every section the
        cells in `inference_data` came from. The id derivation must
        match `InMemoryDatasetBlob._derive_adata_batch_id`; use
        `predict()`'s helper to build it.
    id_to_path:
        Mapping `adata_batch_id -> Path` of the source files. Used to
        stamp `adata.obs["source_file"]` per cell.
    gene_names:
        Optional canonical gene panel (typically HVG-subset or full
        panel) loaded from `dataset_blob.processed_dir/gene_panel.pkl`.
        When set, `var` is reindexed to this list so model outputs in
        `layers` (which are in this gene-space) match `var` length.
    test_batch_ids:
        `adata_batch_id` values that were held out from training; cells
        from these sections get `obs["data_split"] = "test"`,
        everything else is "train".
    test_regions:
        Optional mapping `adata_batch_id -> region_spec` for the held-
        out-region downstream task (`SpatialBatchSplit.test_regions`).
        Cells whose `obsm["spatial"]` falls inside the region for their
        batch ALSO get `data_split = "test"` (in addition to whatever
        `test_batch_ids` already marked). Region keys: `x_min`, `x_max`,
        `y_min`, `y_max` (absolute) and/or their `_pct` percentile
        equivalents (resolved against this batch's per-section coordinate
        range — same convention as the training-time
        `_is_in_single_region`).
    """
    import anndata as ad
    import numpy as np
    import pandas as pd
    import torch as _torch  # local alias so we can dtype-check below

    def to_np(x):
        # Avoid an unnecessary GPU->CPU copy when the tensor is already
        # on CPU. The predict-collator path already moves cached
        # tensors to CPU; an extra .cpu() call is a no-op semantically
        # but allocates a fresh tensor under the hood (multi-GB on
        # mmb20 across several layers + obsm slots).
        #
        # bf16 guard: NumPy has no bfloat16 dtype, so `t.numpy()` on a
        # bf16 tensor raises `TypeError: Got unsupported ScalarType
        # BFloat16`. This happens when the multi-seed pipeline runs
        # predict with `Trainer(precision="bf16-mixed")` and some
        # output tensor stays in bf16 after autocast (e.g. the VQ
        # codebook lookup result, which has no autocast cast-back
        # rule). Upcast to fp32 before conversion — no-op for tensors
        # already in fp32 / int / bool, only triggers on the autocast
        # leftovers. fp16 has the same issue formally, but numpy DOES
        # support float16 so it's fine; we only need to guard bf16.
        if hasattr(x, "cpu"):
            t = x if not x.is_cuda else x.cpu()
            if t.dtype is _torch.bfloat16:
                t = t.float()
            return t.numpy()
        return np.asarray(x)

    # ---- sanity checks on inputs ----
    if "adata_batch_ids" not in inference_data:
        raise RuntimeError(
            "_build_clean_adata_from_inference: inference_data is missing "
            "'adata_batch_ids' — cannot reconstruct cell -> source mapping."
        )
    if "obs_row_index" not in inference_data:
        raise RuntimeError(
            "_build_clean_adata_from_inference: inference_data is missing "
            "'obs_row_index'. Rebuild the dataset blob with the latest "
            "InMemoryDatasetBlob.process_anndata_batch (which stamps "
            "obs_row_index per cell)."
        )

    batch_ids_np = to_np(inference_data["adata_batch_ids"]).astype(int)
    row_idx_np   = to_np(inference_data["obs_row_index"]).astype(int)
    n_cells      = len(batch_ids_np)
    if len(row_idx_np) != n_cells:
        raise RuntimeError(
            f"adata_batch_ids has length {n_cells} but obs_row_index has "
            f"length {len(row_idx_np)} — they must be aligned."
        )

    # ---- group cells by source section ----
    by_bid: dict = {}
    for k in range(n_cells):
        by_bid.setdefault(int(batch_ids_np[k]), []).append(
            (k, int(row_idx_np[k]))
        )

    missing = [b for b in by_bid if b not in source_adatas]
    if missing:
        raise RuntimeError(
            f"Cells reference adata_batch_ids {sorted(missing)} but no "
            f"source AnnData was provided for those ids. Available source "
            f"ids: {sorted(source_adatas)}. Check that the silver dir "
            f"contains the files used at blob-build time."
        )

    # ---- slice + var-reindex each source ----
    sub_adatas = []
    for bid in sorted(by_bid):
        cells       = by_bid[bid]
        inf_pos     = np.array([c[0] for c in cells], dtype=int)
        src_rows    = [c[1] for c in cells]
        source      = source_adatas[bid]

        # Bounds check — better here than at concat time when the error
        # would be opaque.
        if any(r < 0 or r >= source.n_obs for r in src_rows):
            bad = [r for r in src_rows if r < 0 or r >= source.n_obs]
            raise RuntimeError(
                f"obs_row_index out of bounds for adata_batch_id={bid} "
                f"(n_obs in source = {source.n_obs}); offending rows: "
                f"{bad[:5]}{'...' if len(bad) > 5 else ''}. Likely the "
                f"silver file changed since the blob was built — rebuild."
            )

        sub = source[src_rows].copy()
        if gene_names is not None:
            if list(sub.var_names) != list(gene_names):
                missing = [g for g in gene_names if g not in set(sub.var_names)]
                if missing and cross_panel:
                    # CROSS-PANEL: a narrow section legitimately lacks part of
                    # the vocabulary. `gene_names` is the union over the built
                    # sections, so a 319-gene section is missing 100 of the 419
                    # by construction -- not a corrupt silver file.
                    #
                    # Reindex WITH zero fill, which is the same widening the
                    # model input got (`scatter_sections_to_columns`), so `.X`
                    # and `.layers['X_hat']` line up column-for-column at
                    # vocabulary width with exact zeros at unmeasured genes.
                    # That keeps every existing consumer working unchanged --
                    # `compute_inference_metrics.py` and the Pearson variants
                    # pair columns positionally -- and lets a per-panel analysis
                    # restrict to a section's measured genes when it wants to.
                    #
                    # The zeros are not a prediction of zero expression; they
                    # mark "not measured". Anything comparing X to X_hat over
                    # the FULL vocabulary would count them as perfect
                    # agreement and inflate its score, so per-panel work must
                    # mask (see `_perpanelrecon.py`).
                    sub = ad.AnnData(
                        X=np.zeros((sub.n_obs, len(gene_names)),
                                   dtype=np.float32),
                        obs=sub.obs.copy(),
                        var=pd.DataFrame(index=list(gene_names)),
                    )
                    present = [g for g in gene_names
                               if g in set(source.var_names)]
                    src_sub = source[src_rows][:, present]
                    src_X = src_sub.X
                    if hasattr(src_X, "toarray"):
                        src_X = src_X.toarray()
                    cols = [list(gene_names).index(g) for g in present]
                    sub.X[:, cols] = np.asarray(src_X, dtype=np.float32)
                    print(
                        f"  adata_batch_id={bid}: widened {len(present)} "
                        f"measured genes to the {len(gene_names)}-gene "
                        f"vocabulary ({len(missing)} unmeasured, zero-filled)."
                    )
                else:
                    try:
                        sub = sub[:, list(gene_names)].copy()
                    except KeyError as exc:
                        raise RuntimeError(
                            f"Source for adata_batch_id={bid} (file "
                            f"{id_to_path[bid].name}) is missing genes from the "
                            f"canonical panel: {exc}"
                        )
            # After reindex, var_names MUST match the canonical panel
            # exactly (same order, same set). Mismatch here means the
            # earlier reindex failed silently — better to crash than to
            # write a misaligned predicted_adata.h5ad whose `.X` and
            # `.layers['X_hat']` live in different gene orders.
            if list(sub.var_names) != list(gene_names):
                raise RuntimeError(
                    f"Gene-order mismatch after reindex for "
                    f"adata_batch_id={bid} (file {id_to_path[bid].name}): "
                    f"sub.var_names[:5]={list(sub.var_names)[:5]}, "
                    f"gene_names[:5]={list(gene_names)[:5]}. The source "
                    f"AnnData's `.var` is incompatible with the canonical "
                    f"gene panel; rebuild the dataset blob or restore the "
                    f"silver file's gene order."
                )

        # Track inference position so we can reorder after concat. Stored
        # in obs (so it survives concat); deleted just before return.
        sub.obs["_inf_pos"] = inf_pos
        sub_adatas.append(sub)

    # ---- concatenate + reorder to inference cell order ----
    if len(sub_adatas) == 1:
        adata = sub_adatas[0]
    else:
        # `join="outer"` so source-specific obs/var columns NaN-fill where
        # absent. `index_unique="-"` makes obs_names globally unique even
        # when sources share cell barcodes.
        adata = ad.concat(
            sub_adatas, axis=0, join="outer", index_unique="-",
        )

    perm = np.argsort(adata.obs["_inf_pos"].values)
    adata = adata[perm].copy()
    del adata.obs["_inf_pos"]

    if adata.n_obs != n_cells:
        raise RuntimeError(
            f"Reconstructed adata.n_obs={adata.n_obs} != inference n_cells="
            f"{n_cells}. Cell ordering broke during concat — this is a bug."
        )

    # ---- per-cell SQUINT outputs ---------------------------------------------

    # Spatial coordinates (overwrite source's `spatial` if present, since
    # the model emits its own copy of the same data).
    if "XY_coordinates" in inference_data:
        adata.obsm["spatial"] = to_np(inference_data["XY_coordinates"])

    # Per-cell scalars: which source section + filename + train/test.
    adata.obs["adata_batch_id"] = batch_ids_np.astype(int)
    name_for_id = {bid: id_to_path[bid].name for bid in id_to_path}
    adata.obs["source_file"] = pd.Categorical(
        [name_for_id[bid] for bid in batch_ids_np]
    )
    test_set = set(int(b) for b in (test_batch_ids or []))
    is_test = np.array(
        [int(b) in test_set for b in batch_ids_np], dtype=bool
    )

    # Held-out-region downstream task: mark cells whose spatial xy falls
    # inside per-batch test_regions. Resolve percentile bounds against
    # each section's actual coord range (mirrors SpatialBatchSplit's
    # `_is_in_single_region` semantics — keeping the predict-time data
    # split consistent with the train-time one).
    if test_regions and "spatial" in adata.obsm:
        regions_normed: dict = {int(k): v for k, v in test_regions.items()}
        spatial = np.asarray(adata.obsm["spatial"], dtype=float)
        if spatial.shape[1] >= 2:
            for bid, region_spec in regions_normed.items():
                cells_in_bid = (batch_ids_np.astype(int) == int(bid))
                if not cells_in_bid.any():
                    continue
                # Resolve per-batch xy range from THIS batch's cells (not
                # the global range) so percentile bounds line up with
                # what SpatialBatchSplit computed at training time.
                xy_b = spatial[cells_in_bid, :2]
                x_lo, y_lo = xy_b.min(axis=0)
                x_hi, y_hi = xy_b.max(axis=0)
                regions_list = (
                    region_spec if isinstance(region_spec, list)
                    else [region_spec]
                )
                in_any = np.zeros(spatial.shape[0], dtype=bool)
                for r in regions_list:
                    rx_min = float(r["x_min"]) if "x_min" in r and r["x_min"] is not None else (
                        x_lo + float(r["x_min_pct"]) * (x_hi - x_lo)
                        if "x_min_pct" in r and r["x_min_pct"] is not None else float("-inf")
                    )
                    rx_max = float(r["x_max"]) if "x_max" in r and r["x_max"] is not None else (
                        x_lo + float(r["x_max_pct"]) * (x_hi - x_lo)
                        if "x_max_pct" in r and r["x_max_pct"] is not None else float("inf")
                    )
                    ry_min = float(r["y_min"]) if "y_min" in r and r["y_min"] is not None else (
                        y_lo + float(r["y_min_pct"]) * (y_hi - y_lo)
                        if "y_min_pct" in r and r["y_min_pct"] is not None else float("-inf")
                    )
                    ry_max = float(r["y_max"]) if "y_max" in r and r["y_max"] is not None else (
                        y_lo + float(r["y_max_pct"]) * (y_hi - y_lo)
                        if "y_max_pct" in r and r["y_max_pct"] is not None else float("inf")
                    )
                    in_box = (
                        (spatial[:, 0] >= rx_min)
                        & (spatial[:, 0] <= rx_max)
                        & (spatial[:, 1] >= ry_min)
                        & (spatial[:, 1] <= ry_max)
                    )
                    in_any |= in_box
                # Region only applies to its own batch's cells.
                is_test |= (cells_in_bid & in_any)

    adata.obs["data_split"] = pd.Categorical(
        ["test" if t else "train" for t in is_test],
        categories=["train", "test"],
    )

    # Embeddings -> obsm.
    if "H_latent" in inference_data:
        adata.obsm["X_squint"]            = to_np(inference_data["H_latent"])
    if "H_quantized" in inference_data:
        adata.obsm["X_squint_quantized"]  = to_np(inference_data["H_quantized"])
    if "H_adj" in inference_data:
        adata.obsm["X_squint_adj"]        = to_np(inference_data["H_adj"])
    if "H_quantized_cell" in inference_data:
        adata.obsm["cell_emb"]            = to_np(inference_data["H_quantized_cell"])
    if "H_quantized_niche" in inference_data:
        adata.obsm["neighborhood_emb"]    = to_np(inference_data["H_quantized_niche"])
    if "H_latent_cell" in inference_data:
        adata.obsm["cell_latent"]         = to_np(inference_data["H_latent_cell"])
    if "H_latent_niche" in inference_data:
        adata.obsm["neighborhood_latent"] = to_np(inference_data["H_latent_niche"])

    # Codebook indices: 1D -> obs, multi-level -> obsm.
    if "Indices" in inference_data:
        idx = to_np(inference_data["Indices"])
        if idx.ndim == 1 or (idx.ndim == 2 and idx.shape[1] == 1):
            adata.obs["code_index"] = idx.reshape(-1).astype(int)
        else:
            adata.obsm["code_indices"] = idx.astype(int)
    for src_key, prefix in [("Indices_cell", "cell"),
                            ("Indices_niche", "neighborhood")]:
        if src_key not in inference_data:
            continue
        idx = to_np(inference_data[src_key])
        if idx.ndim == 1 or (idx.ndim == 2 and idx.shape[1] == 1):
            adata.obs[f"{prefix}_code_index"] = idx.reshape(-1).astype(int)
        else:
            adata.obsm[f"{prefix}_code_indices"] = idx.astype(int)

    # Reconstructions + 1-hop neighbourhood ground truth -> layers.
    if "X_hat" in inference_data:
        adata.layers["X_hat"]     = to_np(inference_data["X_hat"])
    if "X_hat_nbr" in inference_data:
        adata.layers["X_hat_nbr"] = to_np(inference_data["X_hat_nbr"])
    if "X_nbr" in inference_data:
        adata.layers["X_nbr"]     = to_np(inference_data["X_nbr"])

    # Global metadata.
    num_quantizers = int(inference_data.get("num_quantizers", 1))
    cb_sizes       = inference_data.get("codebook_sizes", None)
    squint_meta = {
        "codebook_size":     int(inference_data.get("codebook_size", 0)),
        "num_heads":         int(inference_data.get("num_heads", 1)),
        "num_quantizers":    num_quantizers,
        "codebook_sizes":    list(cb_sizes) if cb_sizes is not None else None,
        "separate_codebook": bool(inference_data.get("separate", False)),
    }
    is_dual = (
        "H_quantized_cell"  in inference_data
        or "H_quantized_niche" in inference_data
    )
    if is_dual:
        squint_meta["dual"] = True
        for branch in ("cell", "niche"):
            squint_meta[f"codebook_size_{branch}"] = int(
                inference_data.get(f"codebook_size_{branch}", 0)
            )
            squint_meta[f"num_quantizers_{branch}"] = int(
                inference_data.get(f"num_quantizers_{branch}", 1)
            )
            cb_sz = inference_data.get(f"codebook_sizes_{branch}", None)
            if cb_sz is not None:
                squint_meta[f"codebook_sizes_{branch}"] = list(cb_sz)
    adata.uns["squint"] = squint_meta
    if "edge_index" in inference_data:
        # Graph-level object — keep in uns.
        adata.uns["edge_index"] = to_np(inference_data["edge_index"])

    return adata


def predict(
    run_dir: str,
    silver_dir: str | None = None,
    model_ckpt_fname: str | None = None,
    output_dir: str | None = None,
    precision: Optional[str] = None,
    strategy: Optional[str] = None,
    compression: Optional[str] = None,
    predict_batch_size: Optional[int] = None,
):
    """
    Run inference on a folder of .h5ad files using a trained SQUINT
    checkpoint.  Returns a single AnnData with SQUINT outputs packaged
    into .obsm / .obs / .layers, and writes it to disk.

    Parameters
    ----------
    run_dir
        Path to the run directory of the trained model. Two layouts are
        supported (resolved automatically by the helpers in
        ``vqniche.utils.parse_test_configs``):
          (a) flat (current ``train()``): ``<run_dir>/user_specified_config.yaml``
              and ``<run_dir>/checkpoints/*.ckpt`` directly under run_dir.
          (b) legacy wandb-controlled: same files under ``<run_dir>/files/``.
    silver_dir
        Folder containing the .h5ad files to run inference on. Defaults to
        the silver folder this script was configured with.
    model_ckpt_fname
        Optional explicit checkpoint path. If omitted, the best checkpoint
        is auto-selected from ``<run_dir>/checkpoints/`` (or the legacy
        ``<run_dir>/files/checkpoints/``).
    output_dir
        Where to write predicted_adata.h5ad. Defaults to ``run_dir`` itself
        — predictions land alongside checkpoints / config /
        ablation_summary.yaml so everything for one training run lives in
        one folder.
    precision
        Lightning `Trainer(precision=...)` override for the predict pass.
        `None` (default) → fp32, identical to training. Set to
        `"bf16-mixed"` (recommended on H100/H200/A100) or `"16-mixed"`
        to enable autocast — typically 1.5-2x faster on the encoder /
        decoder MLPs. CAVEAT: the VQ codebook lookup uses `argmin` over
        squared L2 distances; in mixed precision the distance compute
        can drift by a few ULPs, which on borderline cells can change
        which code wins. The output AnnData written to disk will then
        differ by a small number of `cell_code_index` /
        `neighborhood_code_index` flips vs fp32 — usually invisible in
        NMI / ARI / iLISI summaries but worth A/B-testing on a single
        seed before adopting for a publication run. Use `precision=None`
        if you need bit-exact reproducibility against the training run.
    strategy
        Lightning `Trainer(strategy=...)` override for the predict pass.
        `None` (default) → `"ddp_find_unused_parameters_true"`, the
        legacy training-time strategy. On single-GPU LSF jobs (the
        common case) this is wasteful — DDP wrapping + the
        find-unused-params traversal adds overhead with no benefit.
        Pass `"auto"` to let Lightning pick `SingleDeviceStrategy`
        when only one GPU is visible (typically ~5-15% predict speedup).
    compression
        AnnData write compression for `predicted_adata.h5ad`.
        `None` (default) → `"gzip"` at level 4 (3-10x smaller, ~30-60 s
        on a 100k-cell mmb-smb file). `"lzf"` is 3-5x faster to write
        but ~1.5x bigger on disk — useful when the write is on the
        critical path of a downstream pipeline waiting on the file.
        Now that the write is excluded from `runtime_seconds`, this
        only affects wall-clock UX, not the recorded runtime.
        Use `"gzip"` for paper / archival outputs; `"lzf"` for
        benchmark sweeps where a downstream metric runner is blocked
        on the write.
    """
    # --- Imports done lazily so the script's CLI / config helpers don't
    #     pull torch on every invocation. ---
    if str(REPRO_REPO / "analysis") not in sys.path:
        sys.path.insert(0, str(REPRO_REPO / "analysis"))
    if str(SQUINT_PKG / "src") not in sys.path:
        sys.path.insert(0, str(SQUINT_PKG / "src"))

    import pickle
    import anndata as ad
    import torch
    import pytorch_lightning as pl
    from predict_model import collate_predict_outputs
    from vqniche.utils.parse_test_configs import collect_test_configs
    from vqniche.initializers.initialize import (
        initialize_dataset_blob,
        initialize_databatch,
        initialize_datamodule,
        set_model_class,
    )

    torch.backends.cudnn.benchmark = False
    torch.set_float32_matmul_precision("medium")

    run_dir = str(run_dir)

    # Load the user-saved training config FIRST so we can derive the
    # silver_dir from the variant's chosen dataset (chl59-8b_1p,
    # mmb0-1b_smb1-20b_1p_shared_genes, ...). Without this, predict() would
    # always look under the module-level DATASET_NAME and miss non-default
    # datasets.
    # NOTE: collect_test_configs takes a parameter named `wandb_run_dir` for
    # historical reasons; it now supports both the flat and legacy layouts.
    config = collect_test_configs(
        wandb_run_dir=run_dir,
        model_ckpt_fname=model_ckpt_fname,
    )
    print(f"Using checkpoint: {config['model']['model_ckpt_fname']}")

    # ---- predict_batch_size override -----------------------------------------
    # Needed because the inference cache aggregates 1-hop neighbours with
    # `X_nbr.index_add_(0, col, X[row])` (utils/loss_utils.py:136), and `X[row]`
    # materialises an [n_edges, n_genes] gather. Predict runs with
    # `predict_sample_neighbors_for_inference=False` (full neighbourhood), so
    # n_edges grows with the batch: at the base default of 65,536 seeds on the
    # 4,948-gene R0 panel that single gather asked for 47.51 GiB and OOM'd an
    # 80 GB H100. The cost is O(batch x degree x genes), so it bites on wide
    # panels regardless of how much host RAM the job has -- lowering the batch
    # is the direct lever, and predict is a single pass so more batches costs
    # little.
    if predict_batch_size is not None:
        _old_pbs = config["datamodule"]["inference_params"].get("predict_batch_size")
        config["datamodule"]["inference_params"]["predict_batch_size"] = int(predict_batch_size)
        print(
            f"Predict: predict_batch_size {_old_pbs} -> {int(predict_batch_size)} "
            f"(override; the 1-hop neighbour gather is O(batch x degree x genes))."
        )

    if silver_dir:
        silver_dir = Path(silver_dir)
    else:
        cfg_root = config["dataset"].get("root_data_dir") or str(DATA_ROOT)
        cfg_name = config["dataset"].get("dataset_name") or DATASET_NAME
        silver_dir = Path(cfg_root) / "silver" / cfg_name

    # Sanity: list of .h5ad files (also used to label cells with their source).
    source_paths = _infer_adata_files_in_dir(silver_dir)
    print(f"Running inference on {len(source_paths)} file(s) under {silver_dir}:")
    for p in source_paths:
        print(f"  - {p.name}")

    # Determinism (mirrors predict_model.test).
    pl.seed_everything(config["experiment"]["seed"])

    # Build dataset + datamodule + model from the saved config.
    dataset_blob = initialize_dataset_blob(config)

    # ---- Streaming (`backend: on-disk`) configs -------------------------------
    # The on-disk branch of `initialize_dataset_blob` attaches the composed
    # transforms as `section_transform` -- the streaming loader applies them per
    # section -- and deliberately leaves PyG's `transform` hook unset, so that
    # `get(idx)` stays cheap for the metadata reads that only want cell counts.
    # `initialize_databatch` below builds its data_list as
    # `[dataset_blob[idx] for idx in adata_batch_idx]`, so on a streaming config
    # it would collate RAW sections: no `data.x` / `data.y` (produced by
    # SetExperimentDataKeys) and no split masks (SpatialBatchSplit).
    #
    # Attaching the hook here makes `dataset_blob[idx]` behave exactly like the
    # in-memory blob, so the whole tested predict path downstream is reused
    # unchanged rather than duplicated for streaming. Note predict does NOT
    # stream: `data` becomes a collated Batch, so `initialize_datamodule`
    # dispatches to InMemoryDataModule (it switches on the type of `data`, not
    # on `config['dataset']['backend']`).
    #
    # Safe for the streaming loader as well -- `KSectionBlockLoader._fetch_section`
    # reads via `dataset.get(row)`, which bypasses `Dataset.__getitem__` and so
    # can never double-apply the transforms.
    #
    # CAVEAT, and the reason this is a stop-gap rather than the answer: collating
    # the whole blob is exactly what streaming exists to avoid. It is fine at R0
    # scale (39 sections / 519,058 cells / 4,948 genes ~= 10 GB dense for `x`,
    # ~21 GB at the collation peak) and hopeless for the 110M-cell corpus, which
    # needs predict routed through `OnDiskStreamingDataModule.predict_dataloader`
    # plus a streaming writer for `predicted_adata.h5ad`.
    _section_tf = getattr(dataset_blob, "section_transform", None)
    if _section_tf is not None and getattr(dataset_blob, "transform", None) is None:
        dataset_blob.transform = _section_tf
        print(
            f"Predict: streaming blob detected ({type(dataset_blob).__name__}); "
            f"attached the per-section transforms to PyG's `transform` hook so "
            f"the {len(dataset_blob)} sections are transformed before collation."
        )

    with open(Path(dataset_blob.processed_dir) / "label_categories.pkl", "rb") as f:
        label_categories = pickle.load(f)

    # Expand `adata_batch_idx` to EVERY section in the blob so predict()
    # also encodes whole-section holdouts (e.g. chl59 batches 2 + 3,
    # mmb20 batches 15 + 82). The saved training config carries
    # `adata_batch_idx = [train+val positions]` only — without this
    # override held-out sections never enter the dataloader, never get
    # encoded, and never appear in `predicted_adata.h5ad` (so the
    # downstream `plot_code_indices_spatial` etc. silently skip them).
    # Existing behaviour is preserved when no sections are held out:
    # `range(len(blob))` equals the original `adata_batch_idx` whenever
    # train+val already covers the whole blob.
    _orig_idx = list(config["dataset"].get("adata_batch_idx", []))
    config["dataset"]["adata_batch_idx"] = list(range(len(dataset_blob)))
    if config["dataset"]["adata_batch_idx"] != _orig_idx:
        print(
            f"Predict: expanding adata_batch_idx {_orig_idx} -> "
            f"all {len(dataset_blob)} sections "
            f"{config['dataset']['adata_batch_idx']} so held-out sections "
            f"are encoded and visible in downstream plots."
        )

    # Try to load the canonical gene panel saved at blob-build time so we can
    # propagate gene names through HVG selection into the predicted AnnData.
    gene_panel = None
    gene_panel_path = Path(dataset_blob.processed_dir) / "gene_panel.pkl"
    if gene_panel_path.exists():
        with open(gene_panel_path, "rb") as f:
            gene_panel = pickle.load(f)

    # Reconstruct the TRAIN-TIME `label_to_dense` mapping for batch one-
    # hot. The trained decoder-covariate / FiLM / adversary head all
    # have a fixed input/output dim N = number of train batches. With
    # the predict-time `adata_batch_idx` expansion above (so held-out
    # sections also get encoded), `obs_batch` now contains labels the
    # train pipeline never saw — re-densifying observed labels would
    # produce a one-hot dim > N and trigger CUDA index OOB inside the
    # adversary's softmax / decoder concat.
    # Mirror `build_batch_one_hot_from_obs`'s sorted-unique densification
    # but ONLY over train sections; held-out cells fall back to dense
    # ID 0 (= the first/reference train batch). Codes for held-out
    # cells are then "what would this cell look like if it were in the
    # reference batch" — usable for code-index visualisation and for
    # post-hoc Pearson, but the batch-correction effect is treated as
    # if from the reference batch.
    _train_batch_ids = (
        config.get("dataset", {})
              .get("train_transform_params", {})
              .get("train_batches", []) or []
    )
    _train_batch_ids_set = {int(b) for b in _train_batch_ids}
    _train_labels: set = set()
    for _pos in range(len(dataset_blob)):
        _d = dataset_blob[_pos]
        if int(_d.adata_batch_id) in _train_batch_ids_set:
            grp = getattr(_d, "obs_batch", None)
            if grp:
                _train_labels.add(str(grp[0]))
    _train_label_to_dense: dict = {
        lbl: i for i, lbl in enumerate(sorted(_train_labels))
    }

    # ---- Streaming backend uses a DIFFERENT train-time map --------------------
    # The reconstruction above mirrors `build_batch_one_hot_from_obs`, which
    # densifies over TRAIN sections only -- correct for the in-memory backend.
    # `OnDiskStreamingDataModule` does not do that: `initialize_datamodule`
    # constructs it without `batch_label_to_dense`, so it falls back to
    # `dataset.batch_label_to_dense()`, which is derived from the MANIFEST and
    # therefore spans every section in the blob, held-out ones included. (Hence
    # R0's "n_distinct_batches=39 (over 39 sections)" and a `batch_embedding` of
    # 624 params = 39 rows x 16 dim, four of whose rows never receive a gradient.)
    #
    # Rebuilding a 35-label map at predict time then shifts every dense id: with
    # batch0 / batch22 / batch3 / batch7 held out, sorted-unique over 35 labels
    # maps batch1->0 where training used batch1->1, and so on for all 35 shared
    # labels. Each cell's decoder covariate would be looked up in the wrong
    # embedding row.
    #
    # Scope of the damage had this gone unnoticed: the covariate is concatenated
    # to the QUANTISED latent and only feeds the decoders, and this variant has
    # no encoder batch conditioning (`no-batch-int`, FiLM off, and the collated
    # batch reports encoder_condition_dim=0), so `Indices_cell` / `Indices_niche`
    # would still have been correct while `X_hat` and every reconstruction
    # metric derived from it would have been silently wrong.
    #
    # Predict must reproduce whatever map training used, so read it from the
    # dataset for streaming blobs rather than reconstructing it.
    if hasattr(dataset_blob, "batch_label_to_dense"):
        _stream_map = dataset_blob.batch_label_to_dense()
        if _stream_map and _stream_map != _train_label_to_dense:
            print(
                f"Predict: streaming blob -- using the manifest-wide "
                f"label->dense map training actually used "
                f"({len(_stream_map)} labels) instead of the "
                f"train-sections-only reconstruction ({len(_train_label_to_dense)} "
                f"labels). Rebuilding would shift every dense id and index the "
                f"decoder covariate embedding on the wrong row."
            )
            _train_label_to_dense = dict(_stream_map)

    if _train_label_to_dense:
        print(
            f"Predict: train-time label->dense map (used for batch "
            f"covariate at predict): {_train_label_to_dense}"
        )

    data_batch = initialize_databatch(
        config=config,
        dataset_blob=dataset_blob,
        batch_label_to_dense=_train_label_to_dense or None,
        unknown_batch_label_dense_id=0,
    )
    datamodule = initialize_datamodule(
        config=config,
        data=data_batch,
        obs_per_batch_id=getattr(dataset_blob, 'obs_per_batch_id', None),
    )

    # Recover the gene names the MODEL was trained on. The model's X_hat
    # output (and `inference_data['X']` cache) is in `gene_panel.index`
    # column order — every section is reindexed to that canonical panel
    # at blob-build time (`process_anndata_batch` line 367-371). The
    # source AnnDatas on disk may have a *different* native `var_names`
    # order (different platforms / different upstream tools); without a
    # reindex, `adata.X` from the source slice and `adata.layers['X_hat']`
    # from the model output land in different gene orders → cell-wise
    # pearson tanks because columns are paired wrong.
    #
    # Two cases:
    #   - SubsetHVG transform was applied: `hvg_indices` is stored per
    #     section; gene_names = canonical panel sliced to those indices.
    #   - No HVG transform: gene_names = the FULL canonical panel.
    #
    # `_build_clean_adata_from_inference` then reindexes every source
    # slice to `gene_names`, guaranteeing X_hat and X share gene order.
    gene_names = None
    if gene_panel is not None:
        try:
            sec0 = dataset_blob[0]
            if hasattr(sec0, "hvg_indices") and sec0.hvg_indices is not None:
                idxs = sec0.hvg_indices.cpu().numpy().tolist()
                # Sanity: HVG selection runs per-section; verify all sections
                # converged on the same gene set (otherwise the model would
                # have crashed on dim mismatch — but warn just in case).
                for i in range(1, len(dataset_blob)):
                    other = dataset_blob[i].hvg_indices.cpu().numpy().tolist()
                    if other != idxs:
                        print(
                            f"WARNING: section {i} HVG indices differ from "
                            f"section 0 — using section 0 names; downstream "
                            f"per-gene comparisons may be off."
                        )
                        break
                gene_names = [str(gene_panel.index[j]) for j in idxs]
                print(f"Resolved gene names for {len(gene_names)} HVGs from gene_panel.pkl.")
            else:
                # No HVG transform: pin to the full canonical gene panel so
                # the source slice is reindexed to match X_hat's column order.
                gene_names = [str(g) for g in gene_panel.index]
                print(
                    f"Resolved gene names for {len(gene_names)} genes from "
                    f"gene_panel.pkl (no HVG; using full canonical panel)."
                )
        except Exception as exc:  # noqa: BLE001
            print(f"NOTE: could not recover gene names ({exc}); falling back to integer-string names.")

    Model = set_model_class(config["model"]["model_name"])
    model = Model.load_from_checkpoint(config["model"]["model_ckpt_fname"])
    model.eval()

    # Resolve optimization overrides. Each kwarg defaults to the legacy
    # training-parity value when not set; the multi-seed runner
    # (`_run_one_seed.py`) opts INTO the faster settings explicitly so
    # ablation runs (which go through `run_squint.py --all` and never
    # pass these kwargs) stay on the conservative path.
    _trainer_precision = precision  # e.g. None / "bf16-mixed" / "16-mixed"
    _trainer_strategy  = (
        strategy if strategy is not None else "ddp_find_unused_parameters_true"
    )
    _write_compression = compression if compression is not None else "gzip"
    if _trainer_precision is not None:
        print(f"Predict: precision override = {_trainer_precision} (autocast enabled)")
    if strategy is not None:
        print(f"Predict: strategy override  = {_trainer_strategy}")
    print(f"Predict: AnnData compression = {_write_compression}")

    trainer_kwargs = dict(
        accelerator="auto",
        devices="auto",
        # `deterministic=True` matches the training cfg (also True
        # after the 2026-05-12 revert — see base config). Previously
        # toggled to False to match a brief training-side
        # `deterministic: False` experiment; that experiment was
        # reverted because cuDNN benchmark mode destabilised the
        # adversarial min-max game. Both train and predict now back
        # at the historic deterministic=True.
        deterministic=True,
        logger=False,
        strategy=_trainer_strategy,
        enable_checkpointing=False,
        num_sanity_val_steps=0,
        enable_progress_bar=False,
        enable_model_summary=False,
    )
    if _trainer_precision is not None:
        # Lightning accepts the precision kwarg only when explicitly set —
        # passing `None` triggers the "missing positional" code path on
        # some versions, so gate the addition.
        trainer_kwargs["precision"] = _trainer_precision
    trainer = pl.Trainer(**trainer_kwargs)

    print("Predicting model...")
    predict_data_cache = trainer.predict(model=model, datamodule=datamodule)
    predict_data_dict = collate_predict_outputs(
        data_cache=predict_data_cache,
        model=model,
        predict_dataloader=datamodule.predict_dataloader(),
    )

    # ----------------------------------------------------------------------
    # Two post-inference fixups in one pass over `predict_data_dict`:
    #
    # (1) Replace per-cell `adata_batch_ids` (DENSE, what the model used
    #     for one-hot indexing) with per-cell RAW ids (`uns['batch']`-
    #     derived ints, e.g. 15, 82). `source_adatas` below is keyed by
    #     raw blob ids; without this swap the source-AnnData lookup
    #     fails ("Cells reference adata_batch_ids [0, 1] but available
    #     source ids are [15, 82]"). The raw ids come straight from
    #     `data_batch.adata_batch_ids_raw` — set in `initialize_databatch`
    #     via `data_batch.adata_batch_id[data_batch.batch]` and cached
    #     by `_cache_inference_data`. We do NOT invert the train-time
    #     densification: that's ambiguous when held-out batches all
    #     collapse to dense=0 via the unknown-label fallback.
    #
    # (2) Undo PyG's auto-offset on `obs_row_index`. Any tensor whose key
    #     contains the substring 'index' triggers PyG's
    #     `Data.__inc__()` magic during `Batch.from_data_list` — it adds
    #     `num_nodes` to make `edge_index` etc. contiguous across the
    #     batched graph. `obs_row_index` is NOT an edge index but gets
    #     caught by the same heuristic, so per-section indices in
    #     [0, n_obs_section) get shifted by the cumulative node count of
    #     all preceding sections. We recover the original per-section
    #     index by subtracting `cum_offset_for_that_section[raw_id]`.
    if hasattr(data_batch, "obs_batch") and data_batch.obs_batch is not None:
        # Build the per-raw-id PyG offset (cumulative n_cells of all
        # sections preceding this raw id in concat order). Drives the
        # obs_row_index unshift below.
        _raw_to_pyg_offset: dict = {}
        _cum_offset = 0
        for _sec_idx in range(len(data_batch.obs_batch)):
            _group = data_batch.obs_batch[_sec_idx]
            if not _group:
                continue
            _raw_section_id = data_batch.adata_batch_id[_sec_idx]
            _raw_id = int(
                _raw_section_id.item() if hasattr(_raw_section_id, "item")
                else _raw_section_id
            )
            _raw_to_pyg_offset[_raw_id] = _cum_offset
            _cum_offset += len(_group)

        if "adata_batch_ids_raw" in predict_data_dict:
            import numpy as _np
            _raw_ids = predict_data_dict["adata_batch_ids_raw"]
            _raw_ids_np = (
                _raw_ids.cpu().numpy() if hasattr(_raw_ids, "cpu")
                else _np.asarray(_raw_ids)
            ).astype(int)
            # (1) point downstream code at the raw ids — overwrites the
            #     dense ids with raw ids; downstream consumers
            #     (`_build_clean_adata_from_inference`, the data_split
            #     tagger) all expect raw ids.
            predict_data_dict["adata_batch_ids"] = torch.tensor(
                _raw_ids_np, dtype=torch.long
            )
            print(
                f"Set per-cell adata_batch_ids from cached raw ids "
                f"(unique: {sorted(set(_raw_ids_np.tolist()))})."
            )

            # (2) Undo PyG's auto-offset on obs_row_index using the
            #     per-raw-id cumulative offset.
            if "obs_row_index" in predict_data_dict and any(
                _v != 0 for _v in _raw_to_pyg_offset.values()
            ):
                _row = predict_data_dict["obs_row_index"]
                _row_np = (_row.cpu().numpy() if hasattr(_row, "cpu")
                           else _np.asarray(_row)).astype(int)
                _missing_raw = sorted(set(_raw_ids_np.tolist()) - _raw_to_pyg_offset.keys())
                if _missing_raw:
                    raise RuntimeError(
                        f"Cells reference raw adata_batch_ids "
                        f"{_missing_raw} that have no offset in "
                        f"data_batch (known raw ids: "
                        f"{sorted(_raw_to_pyg_offset)})."
                    )
                _offsets = _np.array(
                    [_raw_to_pyg_offset[int(r)] for r in _raw_ids_np],
                    dtype=int,
                )
                predict_data_dict["obs_row_index"] = torch.tensor(
                    _row_np - _offsets, dtype=torch.long
                )
                print(
                    f"Undid PyG auto-offset on obs_row_index using "
                    f"per-raw-id offsets {_raw_to_pyg_offset}."
                )
        else:
            print(
                "WARN: predict_data_dict has no `adata_batch_ids_raw`. "
                "The dataset blob predates the per-cell-raw-id field; "
                "dense ids will be passed through unchanged. If you "
                "have whole-section holdouts, source-AnnData lookup may "
                "fail — rebuild the blob to populate the field."
            )

    # Build the clean AnnData.
    #
    # We start from the source AnnDatas themselves (concatenated in
    # inference cell order) and bolt model outputs on top. This is far
    # simpler than synthesising a fresh AnnData from torch tensors and
    # then trying to graft labels back: every original `obs` / `var`
    # column flows through automatically, including `cell_type`,
    # `Sub_molecular_*`, `ccf_region_name`, `donor_id`, etc. Cells from
    # a source that doesn't carry a given column get NaN via
    # `ad.concat(..., join="outer")`.
    #
    # Build `id_to_path` (and `source_adatas`) by matching each silver
    # file to a blob section via cell_id intersection — NOT by
    # re-deriving the id from `uns['batch']` on the silver file. The
    # latter is fragile: if the blob was built when `uns['batch']`
    # wasn't parseable (e.g. the user added it later, or the format
    # differs), the freshly-derived id won't match the id stamped on
    # the cells in `dataset_blob.pt`. cell_id matching is robust because
    # cell ids are stable across silver-file edits and survive the blob
    # build verbatim.
    #
    # `test_batch_ids` comes from the saved training config — sections
    # in this list were held out from training. The builder stamps
    # `adata.obs["data_split"] = "test"` for cells whose source section
    # is in this list (everything else is "train", with val cells folded
    # in per the user spec). compute_inference_metrics.py reads it for
    # stratified NMI / ARI / Pearson.
    print(f"Loading {len(source_paths)} source AnnDatas + matching them "
          f"to blob sections via cell_id ...")

    # Step 1: read each blob section's first cell_id and adata_batch_id.
    # The blob has the canonical id assignments (whatever they are).
    blob_id_to_first_cell: dict = {}
    for _pos in range(len(dataset_blob)):
        _d = dataset_blob[_pos]
        if hasattr(_d, "cell_id") and len(_d.cell_id) > 0:
            blob_id_to_first_cell[int(_d.adata_batch_id)] = str(_d.cell_id[0])
        else:
            print(f"  WARN: blob position {_pos} has no cell_id list — "
                  f"section can't be matched to a silver file by cell_id.")

    # Steps 2 + 3 (fused): each silver file is read in FULL only once.
    # The previous version opened every file twice — first in
    # `backed='r'` mode to lift `obs['cell_id'].iloc[0]`, then again in
    # full-read mode for row-slicing in `_build_clean_adata_from_inference`.
    # On mmb20 (21 files, multi-GB each on NFS) the second read pass
    # alone added 30-60 s. Read once into a per-path cache, do the
    # cell_id matching against the in-memory copy, then point
    # `source_adatas` at the same objects.
    silver_first_cells: dict = {}
    silver_full_adata: dict = {}
    for p in source_paths:
        try:
            a = ad.read_h5ad(p)  # FULL read; needed by _build_clean_adata_from_inference anyway
        except Exception as exc:
            print(f"  WARN: could not read {p.name}: {exc}")
            continue
        silver_full_adata[p] = a
        silver_first_cells[p] = str(a.obs["cell_id"].iloc[0])

    # Step 3: invert. id_to_path[bid] = the silver file whose first
    # cell_id matches the blob's id-bid section.
    id_to_path: dict = {}
    unmatched_blob_ids: list = []
    used_paths: set = set()
    for bid, blob_first in blob_id_to_first_cell.items():
        match_p = None
        for p, silver_first in silver_first_cells.items():
            if p in used_paths:
                continue
            if silver_first == blob_first:
                match_p = p
                break
        if match_p is None:
            unmatched_blob_ids.append(bid)
        else:
            id_to_path[bid] = match_p
            used_paths.add(match_p)

    if unmatched_blob_ids:
        raise RuntimeError(
            f"Could not match blob adata_batch_ids {unmatched_blob_ids} "
            f"to any silver file via cell_id intersection. Available "
            f"silver files: {[p.name for p in source_paths]}. "
            f"Available blob ids: {sorted(blob_id_to_first_cell)}. "
            f"Likely cause: silver dir contents differ from what was "
            f"used to build the dataset blob."
        )

    # Step 4: point source_adatas at the cached, already-loaded AnnDatas.
    source_adatas: dict = {}
    for bid, p in id_to_path.items():
        source_adatas[bid] = silver_full_adata[p]
        print(f"  adata_batch_id={bid:<3d}  {p.name}  "
              f"(n_obs={source_adatas[bid].n_obs}, "
              f"n_vars={source_adatas[bid].n_vars})")

    test_batch_ids = (
        config.get("dataset", {})
              .get("train_transform_params", {})
              .get("test_batches", [])
        or []
    )
    # Per-batch held-out regions (downstream gene-reconstruction task).
    # Read from the saved training config so predict()'s `data_split`
    # tagging matches what `SpatialBatchSplit` masked out at train time.
    test_regions = (
        config.get("dataset", {})
              .get("train_transform_params", {})
              .get("test_regions", None)
    )
    adata = _build_clean_adata_from_inference(
        inference_data=predict_data_dict,
        source_adatas=source_adatas,
        id_to_path=id_to_path,
        gene_names=gene_names,
        test_batch_ids=list(test_batch_ids),
        test_regions=test_regions,
        # Narrow sections legitimately lack part of the vocabulary; reindex
        # with zero fill instead of demanding every gene be present.
        cross_panel=bool(getattr(dataset_blob, 'cross_panel', False))
    )
    adata.uns["squint"]["run_dir"] = run_dir
    adata.uns["squint"]["ckpt"] = str(config["model"]["model_ckpt_fname"])

    # If the run was produced by the variant-aware layout, surface the variant
    # name + ablation description on the predicted AnnData so plotting scripts
    # can label themselves without re-parsing the path.
    summary_path = Path(run_dir) / "ablation_summary.yaml"
    if summary_path.exists():
        try:
            with open(summary_path, "r") as f:
                _summary = yaml.safe_load(f) or {}
            adata.uns["squint"]["variant"] = str(_summary.get("variant", ""))
            adata.uns["squint"]["variant_description"] = str(_summary.get("description", ""))
        except Exception as exc:  # noqa: BLE001
            print(f"NOTE: could not read {summary_path} ({exc}); skipping variant tagging.")

    # Resolve output path.
    # Default: write inference outputs (predicted_adata.h5ad + downstream
    # plots / metrics) directly into the run_dir alongside checkpoints
    # and the user-saved config — so everything for one training run
    # lives in one folder
    # (`<ARTIFACTS_DIR>/<dataset>/<variant>/<timestamp>/`). `--output-dir`
    # still overrides if you want a separate location.
    if output_dir is None:
        output_dir = Path(run_dir)
    output_dir = Path(output_dir)
    output_dir.mkdir(parents=True, exist_ok=True)
    out_path = output_dir / "predicted_adata.h5ad"

    # AnnData can't write nested-dict .uns -> flatten the squint metadata.
    # We keep the dict but cast values to writeable types.
    adata.uns["squint"] = {k: (str(v) if not isinstance(v, (int, float, bool)) else v)
                          for k, v in adata.uns["squint"].items()}
    # gzip compression shrinks the predicted_adata.h5ad 3-10x at
    # negligible CPU cost — matters most when the file lives on shared
    # NFS (network transfer dominates). compression_opts=4 is a sane
    # default (level 4: balanced compression / write speed). h5py
    # accepts integers 0-9; raise for higher ratio at write-time cost.
    #
    # The write step is wrapped in a wall-clock timer and the duration
    # is stamped to `predict_write_seconds.txt` next to the AnnData.
    # `_run_one_seed.py` reads that stamp and SUBTRACTS it from the
    # Phase-1 `train + predict` total so the recorded `runtime_seconds`
    # excludes disk I/O — matching the "scaffolding excluded" runtime
    # methodology used by the baseline runners (which never include an
    # AnnData write in their timer). Best-effort write: if the stamp
    # can't be created (read-only filesystem, etc.), the runtime
    # number just doesn't get the subtraction — same as before this
    # change.
    import time as _time
    _t_write_start = _time.time()
    # Honour the caller's compression choice; only `gzip` supports a
    # numeric level via `compression_opts`. h5py treats `lzf` as a
    # plain filter (no opts) and rejects the extra kwarg if passed.
    _write_kwargs: dict = {"compression": _write_compression}
    if _write_compression == "gzip":
        _write_kwargs["compression_opts"] = 4
    adata.write_h5ad(out_path, **_write_kwargs)
    _write_seconds = _time.time() - _t_write_start
    try:
        (output_dir / "predict_write_seconds.txt").write_text(
            f"{_write_seconds:.3f}\n"
        )
    except OSError as _exc:
        # Sidecar is best-effort. Log + continue.
        print(f"NOTE: could not write predict_write_seconds.txt ({_exc}); "
              "the AnnData was still written but the runtime aggregator "
              "won't be able to subtract the write cost from this seed.")
    print(f"  predicted_adata.h5ad write took {_write_seconds:.1f}s "
          "(excluded from runtime_seconds)")

    print()
    print("=" * 78)
    print(f"Wrote inference AnnData to: {out_path}")
    print(f"  n_cells   = {adata.n_obs}")
    print(f"  n_genes   = {adata.n_vars}")
    print(f"  obsm keys = {list(adata.obsm.keys())}")
    print(f"  obs cols  = {list(adata.obs.columns)}")
    print(f"  layers    = {list(adata.layers.keys())}")
    print("=" * 78)
    return adata


# ---------------------------------------------------------------------------
# CLI
# ---------------------------------------------------------------------------

def run_inference_and_analysis(
        run_dir: str,
        silver_dir: Optional[str] = None,
        output_dir: Optional[str] = None,
        model_ckpt_fname: Optional[str] = None,
        label_keys: str = "cell_type,niche,Sub_molecular_tissue_region,ccf_region_name",
        batch_rename: Optional[str] = None,
        cell_label_keys: Optional[str] = None,
        niche_label_keys: Optional[str] = None,
        skip_predict: bool = False,
        skip_code_index_plots: bool = False,
        skip_svg_plots: bool = False,
        skip_umap: bool = False,
        skip_metrics: bool = False,
        predict_precision: Optional[str] = None,
        predict_strategy: Optional[str] = None,
        predict_compression: Optional[str] = None,
    ) -> Path:
    """
    Steps 2-6 of the end-to-end pipeline (everything AFTER `train()`):

      2. predict(run_dir)          -> writes predicted_adata.h5ad into the
                                       run_dir itself
      3. plot_code_indices_spatial -> per-batch spatial plots of code indices
      4. plot_svg_reconstruction   -> SVG vs. reconstruction sanity plots
      5. plot_latent_umap          -> UMAP of each latent slot (rapids env)
      6. compute_inference_metrics -> NMI/ARI vs. ground-truth labels +
                                       batch integration (iLISI, ASW, MMD)
                                       + avg cosine similarity

    Plot + metrics scripts run via subprocess so each gets a clean
    process / cuda state and one's output doesn't pollute the others.
    Output files (subdirs of the predict folder) are written next to
    `predicted_adata.h5ad` in the run_dir.

    Returns the resolved `predict_dir` (= output_dir if set, else run_dir).
    """
    import os
    import subprocess

    # ------------------------------------------------------------------
    # 2. Predict
    # ------------------------------------------------------------------
    # Resolve predict_dir up-front so the skip-predict path can still
    # locate an existing predicted_adata.h5ad for downstream steps.
    if output_dir is None:
        predict_dir = Path(run_dir)
    else:
        predict_dir = Path(output_dir)
    predicted_adata_path = predict_dir / "predicted_adata.h5ad"

    if skip_predict:
        print()
        print("=" * 78)
        print(f"[2/6] SKIPPED predict — using existing {predicted_adata_path}")
        print("=" * 78)
        if not predicted_adata_path.exists():
            raise FileNotFoundError(
                f"--skip-predict requested but {predicted_adata_path} is missing."
            )
    else:
        print()
        print("=" * 78)
        print(f"[2/6] Predict on run_dir={run_dir}")
        print("=" * 78)
        predict(
            run_dir=run_dir,
            silver_dir=silver_dir,
            model_ckpt_fname=model_ckpt_fname,
            output_dir=output_dir,
            precision=predict_precision,
            strategy=predict_strategy,
            compression=predict_compression,
        )
        if not predicted_adata_path.exists():
            raise FileNotFoundError(
                f"Expected predicted AnnData at {predicted_adata_path} but it's missing."
            )
        print(f"[2/6] Done. predicted_adata={predicted_adata_path}")

    # ------------------------------------------------------------------
    # 3. plot_code_indices_spatial
    # ------------------------------------------------------------------
    examples_dir = Path(__file__).parent
    common_args = ["--predicted-adata", str(predicted_adata_path)]

    if skip_code_index_plots:
        print()
        print("=" * 78)
        print("[3/6] SKIPPED plot_code_indices_spatial")
        print("=" * 78)
    else:
        print()
        print("=" * 78)
        print("[3/6] plot_code_indices_spatial")
        print("=" * 78)
        subprocess.run(
            [sys.executable, str(examples_dir / "plot_code_indices_spatial.py")] + common_args,
            check=True,
        )

    # ------------------------------------------------------------------
    # 4. plot_svg_reconstruction
    # ------------------------------------------------------------------
    if skip_svg_plots:
        print()
        print("=" * 78)
        print("[4/6] SKIPPED plot_svg_reconstruction")
        print("=" * 78)
    else:
        print()
        print("=" * 78)
        print("[4/6] plot_svg_reconstruction")
        print("=" * 78)
        subprocess.run(
            [sys.executable, str(examples_dir / "plot_svg_reconstruction.py")] + common_args,
            check=True,
        )

    # ------------------------------------------------------------------
    # 5. plot_latent_umap
    # ------------------------------------------------------------------
    # The UMAP step is the only one that benefits from rapids-singlecell
    # (GPU UMAP via cuML). Rapids is fragile to install alongside torch +
    # pyg in a single venv, so it lives in a separate conda env. Invoke
    # the UMAP script via a wrapper that does `module load cellgen/conda`
    # + `conda activate $RAPIDS_ENV` before running. Override the env
    # path at submit time:
    #   RAPIDS_ENV=/path/to/conda/env bash examples/submit_dataset_sweep.sh ...
    # If $RAPIDS_ENV is unset OR the env is missing, fall back to running
    # the script in the current (squint uv) venv — plot_latent_umap.py's
    # `_detect_gpu_umap_backend()` then auto-falls-back to scanpy CPU.
    if skip_umap:
        print()
        print("=" * 78)
        print("[5/6] SKIPPED plot_latent_umap")
        print("=" * 78)
    else:
        # Free torch's cached GPU memory FIRST. The rapids subprocess uses
        # the same GPU (LSF gives the job one exclusive device), and torch's
        # caching allocator can hold gigabytes of "freed" blocks after
        # predict() completes. Without this release, rapids' first cupy
        # allocation fails with `MemoryError: failed to allocate <small>
        # bytes` even though no work is actually running on the GPU. Wrap in
        # try/except so the path still works in CPU-only configurations.
        try:
            import gc as _gc
            import torch as _torch
            _gc.collect()
            if _torch.cuda.is_available():
                _torch.cuda.empty_cache()
                _torch.cuda.synchronize()
                print(f"[5/6] released parent torch CUDA cache before rapids subprocess")
        except Exception as _e:  # noqa: BLE001
            print(f"[5/6] could not release torch CUDA cache (ok): {_e}")
        print()
        print("=" * 78)
        print("[5/6] plot_latent_umap")
        print("=" * 78)
        umap_args = list(common_args) + ["--label-keys", label_keys]
        if batch_rename:
            umap_args += ["--batch-rename", batch_rename]

        rapids_env = os.environ.get(
            "RAPIDS_ENV", "/nfs/team361/sb75/ENVS/rapids-singlecell"
        )
        rapids_wrapper = examples_dir / "_run_umap_rapids.sh"
        if Path(rapids_env).exists() and rapids_wrapper.exists():
            print(f"[5/6] using rapids conda env: {rapids_env}")
            subprocess.run(
                ["bash", str(rapids_wrapper), *umap_args],
                check=True,
                env={**os.environ, "RAPIDS_ENV": rapids_env},
            )
        else:
            print(
                f"[5/6] rapids env not found at {rapids_env} (or wrapper "
                f"missing); running plot_latent_umap.py in the current venv "
                f"(scanpy CPU fallback)."
            )
            subprocess.run(
                [sys.executable, str(examples_dir / "plot_latent_umap.py")] + umap_args,
                check=True,
            )

    # ------------------------------------------------------------------
    # 6. compute_inference_metrics
    # ------------------------------------------------------------------
    # NMI/ARI between cell + niche codes and ground-truth labels, plus
    # batch integration metrics (iLISI / ASW / MMD) and avg cosine
    # similarity on every embedding obsm slot.
    #
    # Defaults to the user's preferred label sets:
    #   cell  codes   vs. {cell_type, cell_types}
    #   niche codes   vs. {niche, Sub_molecular_tissue_region, ccf_region_name}
    # — labels not present on the AnnData are skipped silently, so the
    # same script works for both the mouse-brain (cell_type +
    # Sub_molecular_tissue_region + ccf_region_name) and chl59 lung
    # (cell_type + niche) datasets.
    if skip_metrics:
        print()
        print("=" * 78)
        print("[6/6] SKIPPED compute_inference_metrics")
        print("=" * 78)
    else:
        print()
        print("=" * 78)
        print("[6/6] compute_inference_metrics")
        print("=" * 78)
        metrics_args = list(common_args)
        if cell_label_keys is not None:
            metrics_args += ["--cell-label-keys", cell_label_keys]
        if niche_label_keys is not None:
            metrics_args += ["--niche-label-keys", niche_label_keys]
        subprocess.run(
            [sys.executable, str(examples_dir / "compute_inference_metrics.py")] + metrics_args,
            check=True,
        )

    print()
    print("=" * 78)
    print("Pipeline complete.")
    print(f"  Run dir:           {run_dir}")
    print(f"  Predicted AnnData: {predicted_adata_path}")
    print(f"  Plots:             {predict_dir}/{{code_index_plots,svg_plots,umap_plots}}/")
    print(f"  Metrics:           {predict_dir}/metrics/")
    print("=" * 78)
    return predict_dir


def run_all_pipeline(
        variant: str,
        silver_dir: Optional[str] = None,
        output_dir: Optional[str] = None,
        model_ckpt_fname: Optional[str] = None,
        label_keys: str = "cell_type,niche,Sub_molecular_tissue_region,ccf_region_name",
        batch_rename: Optional[str] = None,
        cell_label_keys: Optional[str] = None,
        niche_label_keys: Optional[str] = None,
        skip_predict: bool = False,
        skip_code_index_plots: bool = False,
        skip_svg_plots: bool = False,
        skip_umap: bool = False,
        skip_metrics: bool = False,
        train_precision: Optional[str] = None,
        train_strategy: Optional[str] = None,
        compile_model: Optional[bool] = None,
        train_deterministic: Optional[bool] = None,
        predict_precision: Optional[str] = None,
        predict_strategy: Optional[str] = None,
        predict_compression: Optional[str] = None,
    ) -> None:
    """
    End-to-end pipeline:

      1. train(variant) -> writes <ARTIFACTS_DIR>/<dataset>/<variant>/<TS>/
      2-6. inference + plots + metrics (see run_inference_and_analysis)

    To re-run only steps 2-6 against an already-trained checkpoint, use
    `examples/run_inference.py --variant <V> --timestamp <TS>` (it calls
    `run_inference_and_analysis` directly).

    The five `skip_*` flags are forwarded to `run_inference_and_analysis`
    — use them (or `--metrics-only` on the CLI, which sets the three plot
    skips) to suppress the expensive analysis steps in ablation /
    benchmark runs where only the metrics CSVs are needed.
    """
    # ------------------------------------------------------------------
    # 1. Train
    # ------------------------------------------------------------------
    print("=" * 78)
    print(f"[1/6] Train  variant={variant!r}")
    print("=" * 78)
    run_dir = train(
        variant,
        train_precision=train_precision,
        train_strategy=train_strategy,
        compile_model=compile_model,
        train_deterministic=train_deterministic,
    )
    if run_dir is None:
        # Older train() signatures didn't return; fall back to looking up
        # the most recent run dir for this variant. Resolve the variant's
        # dataset tag from its build so the lookup lands in the same dir
        # train() would have created (chl59 / mmb20 don't live under the
        # mmb-smb default).
        variant_slug = variant.replace("/", "_")
        _variant_cfg = VARIANTS[variant]["build"]()
        _variant_tag = _variant_cfg["dataset"].get(
            "dataset_tag",
            _variant_cfg["dataset"].get("dataset_name", DATASET_NAME),
        )
        candidates = sorted((ARTIFACTS_DIR / _variant_tag / variant_slug).glob("*/"))
        if not candidates:
            raise RuntimeError(f"Could not auto-locate run_dir for variant {variant}")
        run_dir = str(candidates[-1])
    print(f"[1/6] Done. run_dir={run_dir}")

    run_inference_and_analysis(
        run_dir=run_dir,
        silver_dir=silver_dir,
        output_dir=output_dir,
        model_ckpt_fname=model_ckpt_fname,
        label_keys=label_keys,
        batch_rename=batch_rename,
        cell_label_keys=cell_label_keys,
        niche_label_keys=niche_label_keys,
        skip_predict=skip_predict,
        skip_code_index_plots=skip_code_index_plots,
        skip_svg_plots=skip_svg_plots,
        skip_umap=skip_umap,
        skip_metrics=skip_metrics,
        predict_precision=predict_precision,
        predict_strategy=predict_strategy,
        predict_compression=predict_compression,
    )


def resolve_run_dir(variant: str, timestamp: str) -> Path:
    """
    Look up the run_dir for a given (variant, timestamp).

    Maps to `<ARTIFACTS_DIR>/<dataset_tag>/<variant_slug>/<timestamp>`,
    where `dataset_tag` is read from the variant's build config (the
    short identifier — `mmb0-1b_smb1-1b_1p`, `chl59-8b_1p`,
    `mmb0-1b_smb1-20b_1p`). Falls back to `dataset_name` if a variant
    doesn't set a tag.

    Raises if the variant doesn't exist or the resolved directory is
    missing on disk (probably the timestamp is wrong, or the run was
    deleted).
    """
    if variant not in VARIANTS:
        raise KeyError(
            f"Unknown variant {variant!r}. Use --list-variants on "
            f"run_squint.py to see registered variants."
        )
    variant_slug = variant.replace("/", "_").replace(" ", "_")
    cfg = VARIANTS[variant]["build"]()
    dataset_tag = cfg["dataset"].get(
        "dataset_tag",
        cfg["dataset"].get("dataset_name", DATASET_NAME),
    )
    run_dir = ARTIFACTS_DIR / dataset_tag / variant_slug / timestamp
    if not run_dir.exists():
        # Surface neighbouring timestamps so the user sees what's
        # available without having to ls the directory themselves.
        parent = ARTIFACTS_DIR / dataset_tag / variant_slug
        siblings = (
            sorted(p.name for p in parent.glob("*") if p.is_dir())
            if parent.exists() else []
        )
        raise FileNotFoundError(
            f"Run dir not found: {run_dir}. "
            f"Available timestamps under {parent}: "
            f"{siblings if siblings else '<none>'}"
        )
    return run_dir


def run_dataset_pipeline(
        dataset: str,
        variants: Optional[List[str]] = None,
        continue_on_error: bool = True,
        **pipeline_kwargs,
    ) -> dict:
    """
    Run `run_all_pipeline` for every variant in DATASET_VARIANTS[dataset].

    Use this for overnight per-HPC-node sweeps (one node per dataset):
      python examples/run_squint.py --all-dataset mmb0-1b_smb1-1b_1p
      python examples/run_squint.py --all-dataset chl59-8b_1p
      python examples/run_squint.py --all-dataset mmb0-1b_smb1-20b_1p

    Parameters
    ----------
    dataset : short dataset tag (key of DATASET_VARIANTS).
    variants : optional explicit subset of variant names to run; defaults
        to the full DATASET_VARIANTS[dataset] list.
    continue_on_error : if True (default), a failure in one variant prints
        the traceback and proceeds to the next. Set False to abort on the
        first failure.
    pipeline_kwargs : forwarded to run_all_pipeline (silver_dir,
        output_dir, model_ckpt_fname, label_keys, batch_rename,
        cell_label_keys, niche_label_keys).

    Returns
    -------
    A dict {variant_name: "ok" | "<error message>"} summarising the run,
    also printed to stdout at the end.
    """
    import traceback as _tb

    if dataset not in DATASET_VARIANTS:
        raise ValueError(
            f"Unknown dataset {dataset!r}. Choose from "
            f"{sorted(DATASET_VARIANTS.keys())}."
        )
    todo = list(variants) if variants else list(DATASET_VARIANTS[dataset])

    print("=" * 78)
    print(f"Dataset sweep: {dataset}  ({len(todo)} variants)")
    for v in todo:
        print(f"  - {v}")
    print("=" * 78)

    results: dict = {}
    for i, variant in enumerate(todo, 1):
        print()
        print("#" * 78)
        print(f"# [{i}/{len(todo)}] {variant}")
        print("#" * 78)
        try:
            run_all_pipeline(variant=variant, **pipeline_kwargs)
            results[variant] = "ok"
        except Exception as exc:  # noqa: BLE001
            err = f"{type(exc).__name__}: {exc}"
            results[variant] = err
            print(f"\n!!! Variant {variant!r} FAILED: {err}")
            _tb.print_exc()
            if not continue_on_error:
                print("Aborting sweep (continue_on_error=False).")
                break

    print()
    print("=" * 78)
    print(f"Dataset sweep complete: {dataset}")
    for v in todo:
        status = results.get(v, "skipped")
        print(f"  {status:30s}  {v}")
    print("=" * 78)
    return results


def main():
    # ----- wandb robustness -------------------------------------------------
    # The default `wandb.init` timeout is 90 seconds, which is too tight
    # on the Sanger LSF cluster where the network handshake to
    # api.wandb.ai is sometimes slow — especially in DDP runs where the
    # child rank's `wandb.init` (forked via PL's subprocess_script
    # launcher) re-handshakes and can hit the ceiling. Bumping the
    # timeout to 300s (and propagating via env var so DDP children
    # inherit it) avoids the
    #     wandb.errors.errors.CommError: Run initialization has timed
    #     out after 90.0 sec
    # crash that otherwise loses an entire run mid-training.
    os.environ.setdefault("WANDB_INIT_TIMEOUT", "300")

    p = argparse.ArgumentParser()
    p.add_argument("--patch-uns", action="store_true",
                   help="Patch .uns/.obs of the two .h5ad files (one-time, only if missing).")
    p.add_argument("--harmonize-var", action="store_true",
                   help="Subset and align .var across the two .h5ad files (one-time, "
                        "run if --build-blob fails with 'All batches must have the same gene panel').")
    p.add_argument("--build-blob", action="store_true",
                   help="Build the in-memory PyG DatasetBlob (one-time). "
                        "Use --build-blob-dataset to pick which dataset.")
    p.add_argument("--build-blob-dataset", type=str,
                   default="mmb0-1b_smb1-1b_1p",
                   choices=[
                       "mmb0-1b_smb1-1b_1p",
                       "chl59-8b_1p",
                       "chl59-2b_1p",
                       "mmb0-1b_smb1-20b_1p",
                       "mmb0-239b_1p",
                       "hst_corpus_110m",
                       # Cross-panel test targets: xhc38 is NESTED (419 with
                       # 319 a strict subset), xhb42 is a TRUE two-way union
                       # (350 / 317, intersection 262). Both need
                       # --cross-panel; without it the builder hard-raises on
                       # the panel mismatch, which is the correct default.
                       "xhc38-4b_1p",
                       "xhb42-3b_1p",
                       "smb1-20b_1p",
                       "spatch_1p",
                       "spatch_ov_1p",
                       "spatch_hcc_1p",
                       "spatch_coad_1p",
                       "squint_hln",
                       "squint_hln_allgenes",
                       "squint_hln_hvg1k",
                       "squint_hln_hvg2k",
                       "squint_hln_svg1k",
                       "squint_hln_svg2k",
                       "squint_vht",
                       "xhs1000-3b_1p",
                       "xhs1000-39b_1p",
                   ],
                   help="Which dataset to build (default: "
                        "mmb0-1b_smb1-1b_1p — MERFISH + STARmap mouse "
                        "brain, 1+1 sections). "
                        "'chl59-8b_1p' = /nfs/team361/sb75/DATASETS/"
                        "silver/chl59-8b_1p (CosMx Lung, 8 samples). "
                        "'chl59-2b_1p' = /lustre/scratch126/cellgen/"
                        "lotfollahi/DATASETS/silver/chl59-2b_1p (CosMx "
                        "Lung, 2-sample subset for ablations). "
                        "'mmb0-1b_smb1-20b_1p' = /lustre/.../silver/"
                        "mmb0-1b_smb1-20b_1p_shared_genes (MERFISH + 20 "
                        "STARmap, harmonised by "
                        "examples/harmonize_mmb_smb20_panels.py first).")
    p.add_argument("--train", action="store_true",
                   help="Train SQUINT on the WHOLE dataset blob (logs to wandb project 'squint').")
    p.add_argument(
        "--variant", type=str,
        default="dualvq+wide+rvq-both-3level+decoder-cov+adv+enc-deeper+mmb0-1b_smb1-1b_1p",
        help="Which training/ablation config to use. The default is the "
             "v5 anchor — the project's current best variant on the "
             "mmb0-1b_smb1-1b_1p dataset (combines `+wide` for niche "
             "NMI, `+enc-deeper` for batch integration, and "
             "`+rvq-both-3level` for Pearson reconstruction). "
             "Use --list-variants to see all registered variants.",
    )
    p.add_argument("--build-blob-backend", type=str,
                   default="in-memory",
                   choices=["in-memory", "on-disk"],
                   help="DatasetBlob storage backend for --build-blob. "
                        "in-memory = single collated dataset_blob.pt "
                        "(default, OOMs on very large corpora); on-disk = "
                        "streaming OnDiskDataset, one section per DB row "
                        "(use for hst_corpus_110m).")
    p.add_argument("--cross-panel", action="store_true",
                   help=(
                       "Build an on-disk blob whose sections may carry "
                       "DIFFERENT gene panels: the vocabulary becomes the union "
                       "over the built sections, each section is stored at its "
                       "own width with a `gene_ids` index into it, and the "
                       "union is assembled per block at load time. Off by "
                       "default, where mismatched panels hard-raise. Note the "
                       "vocabulary is derived from the sections you build, so "
                       "`--exclude-sections` is what sets it."
                   ))
    p.add_argument("--exclude-sections", type=str, nargs="*", default=None,
                   help=(
                       "Section file stems to leave out of the build (e.g. "
                       "chp60-1b_1p's one 18,893-gene section, which "
                       "contributes ~9,366 genes nothing else measures and so "
                       "doubles the vocabulary). Applied BEFORE the vocabulary "
                       "is computed, so a built section can never carry a gene "
                       "outside it."
                   ))
    p.add_argument("--max-steps", type=int, default=None,
                   help=(
                       "Override the variant's step budget. Intended for "
                       "preflighting a long run: same variant, same split, "
                       "stopped after a few steps, so the invariants worth "
                       "checking before an overnight job are verified against "
                       "the real config. `val_check_interval` is reduced to fit "
                       "if it would exceed the budget."
                   ))
    p.add_argument("--split-sections-json", type=str, default=None,
                   help=(
                       "Path to a split spec (`_splitspec.json`) applying "
                       "WHOLE-SECTION train/val/test splits, overriding the "
                       "variant's in-section cell split. Sections are named by "
                       "rel path ('subdir/file.h5ad'); stems are not unique "
                       "across a corpus. Train is implicit -- everything not "
                       "named. This is the ONLY difference between the "
                       "held-out model (which can make zero-shot claims) and "
                       "the full model (which cannot, having seen every "
                       "section). Streaming backend only."
                   ))
    p.add_argument("--resume-build", action="store_true",
                   help=(
                       "Continue an interrupted on-disk build instead of "
                       "starting over: sections already written are kept and "
                       "the build continues after them. Overrides the config's "
                       "`overwrite`, which would otherwise clear the store. "
                       "Refuses to resume if the build parameters changed "
                       "(vocabulary, panel threshold, batch key, file list), "
                       "because mixing sections built under different "
                       "vocabularies would corrupt `gene_ids` with nothing to "
                       "raise."
                   ))
    p.add_argument("--build-blob-name-suffix", type=str, default="",
                   help=(
                       "Append this to the blob's output name, e.g. "
                       "'_subset21', so a partial/subset build gets its own "
                       "gold directory and cannot touch the real one. Silver "
                       "input is unaffected."
                   ))
    p.add_argument("--include-sections", type=str, nargs="*", default=None,
                   help=(
                       "Build ONLY these sections, named by relative path "
                       "('subdir/file.h5ad') or an unambiguous file stem. "
                       "Mutually exclusive with --exclude-sections. Applied "
                       "before the vocabulary is computed, so V is the union "
                       "over just these sections. Use it to validate a "
                       "pipeline change on a representative subset -- a few "
                       "datasets spanning several panels -- in minutes, rather "
                       "than discovering a build-stopping bug hours into the "
                       "full corpus."
                   ))
    p.add_argument("--min-panels-per-gene", type=int, default=1,
                   help=(
                       "Keep only genes measured by at least N DISTINCT PANELS "
                       "(a panel is a distinct gene set -- 46 across the corpus "
                       "-- not a section). Cross-panel builds only. 1 (default) "
                       "keeps the full union and is bit-identical to before. "
                       "Measured over all 636 corpus sections by "
                       "`_vocabpolicy.py`: 1 -> 18,937 genes, 2 -> 9,574, "
                       "3 -> 6,581. At 2 the cap is a chp60-1b_1p operation and "
                       "almost nothing else -- 9,353 of the 9,363 dropped genes "
                       "are its -- while chr78-11b_1p (the narrowest panel at "
                       "169 genes) keeps 100%%. Each section's out-of-vocabulary "
                       "columns are sliced from storage and the loss is "
                       "reported per section, never applied silently."
                   ))
    p.add_argument("--batch-key", type=str, default="batch",
                   choices=["batch", "dataset_batch"],
                   help=(
                       "What counts as 'the batch' for the decoder covariate, "
                       "FiLM conditioning and the GRL adversary. 'batch' "
                       "(default) is uns['batch'], correct for the paper's "
                       "1-3 section setting. Use 'dataset_batch' "
                       "(f'{dataset_id}_{batch}') for a corpus: uns['batch'] is "
                       "a WITHIN-DATASET counter, so `batch0` is used by 80 "
                       "different datasets and 627 of 636 sections carry a "
                       "colliding label -- which both merges unrelated "
                       "experiments into one batch and makes held-out sections "
                       "look already-seen. TERRA uses the same composite key on "
                       "this corpus (cell_tokenizers.py:1097)."
                   ))
    p.add_argument("--container", type=str,
                   default="file",
                   choices=["file", "sqlite"],
                   help="Storage container for --build-blob-backend on-disk. "
                        "file (default) = one file per tissue section; sqlite = "
                        "one BLOB per section in a single sqlite.db. Use 'file' "
                        "for real corpora: sqlite caps a single value at 1e9 "
                        "bytes and real sections reach ~7 GB dense, so the "
                        "sqlite container cannot store them at all. 'sqlite' is "
                        "kept for reading/rebuilding older blobs.")
    p.add_argument("--list-variants", action="store_true",
                   help="Print all registered ablation variants and exit.")
    p.add_argument("--list-dataset-sweeps", action="store_true",
                   help="Print the per-dataset sweep matrix (DATASET_VARIANTS) "
                        "and exit. The 10 variant names per dataset are what "
                        "--all-dataset iterates over.")
    p.add_argument("--variants-for-dataset", type=str, default=None,
                   help="Print just the variant names for one DATASET_VARIANTS "
                        "key (one per line, no decoration) and exit. Intended "
                        "for shell-script consumption (e.g. LSF submitters that "
                        "bsub one job per variant). Use --list-dataset-sweeps "
                        "for the human-readable form.")
    p.add_argument("--predict", action="store_true",
                   help="Run inference on a folder of .h5ad files using a trained checkpoint.")
    p.add_argument("--all", action="store_true",
                   help="End-to-end: train -> predict -> 3 plot scripts "
                        "(plot_code_indices_spatial, plot_svg_reconstruction, "
                        "plot_latent_umap) -> compute_inference_metrics "
                        "(NMI/ARI vs ground-truth labels + iLISI/ASW/MMD batch "
                        "integration + avg cosine similarity). Convenience "
                        "wrapper for `--train --variant <V>` followed by "
                        "`--predict --run-dir <auto>`.")
    p.add_argument("--all-dataset", type=str, default=None,
                   choices=sorted(DATASET_VARIANTS.keys()),
                   help="Run --all (full pipeline) for EVERY variant in the "
                        "given dataset's 10-axis sweep, sequentially. "
                        "Submit one HPC job per dataset (one node each) for "
                        "an overnight sweep. Use --list-dataset-sweeps to "
                        "see the variant names. Failures within the sweep "
                        "are logged but the loop continues (set "
                        "--abort-on-error to stop instead).")
    p.add_argument("--abort-on-error", action="store_true",
                   help="With --all-dataset, abort the sweep on the first "
                        "variant that errors out instead of continuing to the "
                        "next one.")
    p.add_argument("--batch-rename", type=str, default=None,
                   help="Optional comma-separated rename for adata_batch_id "
                        "categories in UMAP plots (e.g. 'MERFISH,STARmap PLUS').")
    p.add_argument(
        "--label-keys", type=str,
        default="cell_type,niche,Sub_molecular_tissue_region,ccf_region_name,annotation,spatial_cluster",
        help="Comma-separated obs columns to color UMAPs and code-index "
             "plots by. Defaults cover the supported datasets — missing "
             "columns are silently skipped per UMAP: "
             "cell_type (mmb mouse brain, chl59 CosMx Lung); "
             "Sub_molecular_tissue_region, ccf_region_name (mmb); "
             "niche (chl59); "
             "annotation, spatial_cluster (spatch_{ov, hcc, coad}_1p). "
             "One UMAP panel is emitted per label per latent slot.",
    )
    p.add_argument("--cell-label-keys", type=str, default=None,
                   help="Comma-separated obs columns to compare CELL codes against "
                        "in the post-inference NMI/ARI metrics (default: lets the "
                        "metrics script use its own defaults of cell_type,cell_types). "
                        "Forwarded to compute_inference_metrics.py via --all.")
    p.add_argument("--niche-label-keys", type=str, default=None,
                   help="Comma-separated obs columns to compare NICHE codes against "
                        "in the post-inference NMI/ARI metrics (default: lets the "
                        "metrics script use its own defaults of niche,"
                        "Sub_molecular_tissue_region,ccf_region_name). Forwarded to "
                        "compute_inference_metrics.py via --all.")
    p.add_argument("--run-dir", type=str, default=None,
                   help="Path to the run directory of the trained model "
                        "(<ARTIFACTS_DIR>/<DATASET_NAME>/<TIMESTAMP>). Required with --predict.")
    p.add_argument("--wandb-run-dir", type=str, default=None,
                   help="DEPRECATED alias for --run-dir (kept for backward compatibility).")
    p.add_argument("--silver-dir", type=str, default=None,
                   help="Folder of .h5ad files for inference (default: same silver "
                        "folder used for training).")
    p.add_argument("--model-ckpt-fname", type=str, default=None,
                   help="Optional path to a specific .ckpt; otherwise the best "
                        "checkpoint is auto-selected.")
    p.add_argument("--output-dir", type=str, default=None,
                   help="Where to write predicted_adata.h5ad and downstream "
                        "plot/metrics output. Defaults to the run_dir itself "
                        "so all artifacts for one training run live in one "
                        "folder.")
    # Opt-in switch for the heavy per-epoch Pearson metrics. Off by
    # default since `compute_inference_metrics.py` produces the
    # benchmark CSVs at the end of training anyway. See
    # `_resolve_metrics_list()` for what gets enabled.
    p.add_argument(
        "--with-pearson", action="store_true",
        help="Compute the full Pearson metrics list every val epoch "
             "during training (turns the in-flight wandb Pearson curves "
             "back on). OFF by default — expect ~10-20x slower epochs "
             "when enabled, because each metric reconstructs the full "
             "val AnnData and runs a cells x genes correlation. The "
             "final benchmark CSVs are produced by "
             "`compute_inference_metrics.py` at the end of the "
             "pipeline regardless of this flag. Equivalent to setting "
             "the env var SQUINT_WITH_PEARSON=1.",
    )

    # Train-time mixed-precision toggle. Default `None` means
    # "honour the base config value" — which is currently
    # `"bf16-mixed"` (see `make_train_config_poc().trainer.precision`
    # for the rationale + safety notes). So invoking the CLI without
    # this flag gives you bf16-mixed training; pass `--precision 32-true`
    # to fall back to fp32 for a paranoid reproducibility comparison.
    #
    # Why a CLI flag (not just the cfg key):
    #   - Lets you A/B bf16 vs fp32 on the same variant + same seed
    #     by changing one CLI arg, without touching the base config.
    #   - Lets the LSF submit scripts override per-job (e.g. one
    #     job pinned to fp32 as a control while every other job runs
    #     bf16) without re-rendering YAMLs.
    #   - Applies uniformly across --train, --all, and --all-dataset.
    p.add_argument(
        "--precision", type=str, default=None,
        help="Lightning Trainer(precision=...) override for the TRAIN "
             "pass. Default: None = honour `cfg['trainer']['precision']` "
             "from the base config (currently 'bf16-mixed' — see the "
             "base-config trainer block for the bf16 rationale). "
             "Common values: 'bf16-mixed' (default-on; Ampere/Hopper "
             "20-40%% wall-clock saving with no GradScaler needed), "
             "'32-true' (legacy fp32, slowest but most reproducible), "
             "'16-mixed' (fp16 + GradScaler — validate against the "
             "adversarial alpha schedule before adopting). Affects the "
             "TRAIN Trainer only — predict() has its own precision "
             "wiring via `predict_precision` on run_all_pipeline().",
    )

    # Train-time Trainer strategy. Default `None` = honour the cfg
    # value (currently "auto"). Override to
    # `"ddp_find_unused_parameters_true"` for the historic single-
    # GPU DDP-wrapped behaviour, or `"ddp"` for true multi-GPU DDP
    # without find-unused.
    p.add_argument(
        "--predict-batch-size", type=int, default=None,
        help=(
            "Override `inference_params.predict_batch_size` for --predict. The "
            "1-hop neighbour aggregation gathers an [n_edges, n_genes] tensor, so "
            "the base default of 65536 OOMs a GPU on wide panels (47.5 GiB on the "
            "4948-gene R0 panel). Try 4096-8192."
        ),
    )
    p.add_argument(
        "--strategy", type=str, default=None,
        help="Lightning Trainer(strategy=...) override for the TRAIN "
             "pass. Default: None = honour `cfg['trainer']['strategy']` "
             "(currently 'auto' — resolves to SingleDeviceStrategy on "
             "1-GPU jobs, skipping the DDP find-unused traversal). "
             "Override to 'ddp_find_unused_parameters_true' for the "
             "historic single-GPU DDP-wrapped behaviour (e.g. if "
             "torch.compile + auto-strategy interacts badly with a "
             "specific variant). 'ddp' for true multi-GPU DDP without "
             "find-unused (needs every param to receive a gradient "
             "every step).",
    )

    # torch.compile control flags. Compile is ON by default in the
    # base config (`trainer.compile_model: True`). The VQ `gumbel_sample`
    # incompatibility is handled by `_install_compile_safe_one_hot()`
    # which monkey-patches F.one_hot with a scatter-based equivalent
    # that handles dynamo's symbolic shapes. If both `--compile` and
    # `--no-compile` are passed, `--no-compile` wins.
    p.add_argument(
        "--compile", action="store_true",
        help="Force torch.compile ON for the TRAIN model (already the "
             "default in the canonical base config). Useful when a "
             "per-variant cfg flips `compile_model: False` and you "
             "want to override back to compile for a single run.",
    )
    p.add_argument(
        "--no-compile", action="store_true",
        help="Disable torch.compile on the TRAIN model. Default is to "
             "compile (base config `trainer.compile_model: True`). "
             "Compile is ~1.3-2× wall-clock on SQUINT's small-model / "
             "many-ops regime; pass this flag if a specific variant "
             "errors at compile time or you need bit-exact "
             "reproduction against a pre-compile run.",
    )

    # Determinism opt-in. Default is `deterministic: False` in the base
    # config (which enables `Trainer(benchmark=True)`, ~5-10% wall-clock
    # saving). `--deterministic` flips it back ON for the current run
    # — useful for publication-final runs / regression diagnostics
    # where bit-exact within-seed reproducibility matters.
    p.add_argument(
        "--deterministic", action="store_true",
        help="Force `Trainer(deterministic=True)` (default is False, "
             "with `benchmark=True` for ~5-10%% speedup). Pass this for "
             "publication-final runs or A/B-vs-historic regression "
             "diagnostics where bit-exact within-seed reproducibility "
             "matters more than wall-clock.",
    )

    # ---- Inference / analysis step toggles (forwarded to --all / --predict)
    # The end-to-end pipeline runs 6 steps: train, predict,
    # plot_code_indices_spatial, plot_svg_reconstruction,
    # plot_latent_umap, compute_inference_metrics. Each step is
    # independently skippable. For ablation / benchmark sweeps where
    # only the metric CSVs are needed, pass `--metrics-only` to drop
    # the three plot stages while keeping predict + metrics.
    p.add_argument(
        "--skip-predict", action="store_true",
        help="Skip the predict() step. Downstream steps will load the "
             "existing `predicted_adata.h5ad` from the run dir — fails "
             "if that file is missing.",
    )
    p.add_argument(
        "--skip-code-index-plots", action="store_true",
        help="Skip `plot_code_indices_spatial.py` (step 3/6).",
    )
    p.add_argument(
        "--skip-svg-plots", action="store_true",
        help="Skip `plot_svg_reconstruction.py` (step 4/6).",
    )
    p.add_argument(
        "--skip-umap", action="store_true",
        help="Skip `plot_latent_umap.py` (step 5/6). The UMAP step is "
             "typically the most expensive analysis stage (rapids GPU "
             "UMAP / scanpy CPU fallback both 5-15 min on 100k cells).",
    )
    p.add_argument(
        "--skip-metrics", action="store_true",
        help="Skip `compute_inference_metrics.py` (step 6/6). Rarely "
             "useful — the metrics CSVs are the whole point of the "
             "benchmark pipeline.",
    )
    p.add_argument(
        "--metrics-only", action="store_true",
        help="Shortcut: enable --skip-code-index-plots, --skip-svg-plots, "
             "and --skip-umap. Keeps predict() + metrics, drops the "
             "three plot stages. This is the recommended setting for "
             "ablation sweeps where only the benchmark CSVs matter "
             "(the ablation submission scripts pass this by default).",
    )
    args = p.parse_args()

    # `--metrics-only` is a convenience alias — promote it into the
    # three explicit plot-skip flags BEFORE the args.all / args.predict
    # branches consume them. Per-flag overrides still work
    # (e.g. `--metrics-only --skip-metrics` would also skip metrics,
    # though that's a weird combo to want).
    if args.metrics_only:
        args.skip_code_index_plots = True
        args.skip_svg_plots = True
        args.skip_umap = True

    # Propagate the CLI flag to the env-var that `_resolve_metrics_list()`
    # checks at config-build time. Setting the env var here (before any
    # `train()` call below materialises a cfg) ensures the new
    # `train_metrics_list` / `test_metrics_list` reflect the chosen
    # behaviour for this invocation. Setting it directly via env var on
    # the command line (`SQUINT_WITH_PEARSON=1 python ...`) also works
    # — this just bridges from CLI flag to env var.
    if args.with_pearson:
        os.environ["SQUINT_WITH_PEARSON"] = "1"

    if args.list_variants:
        print("Registered ablation variants:")
        for name, spec in VARIANTS.items():
            print(f"  {name}")
            print(f"    description: {spec['description']}")
            print(f"    patches:     {spec['patches']}")
        return

    if args.list_dataset_sweeps:
        print("Per-dataset sweep matrix (DATASET_VARIANTS):")
        print("Run all variants of one dataset sequentially via:")
        print("  python examples/run_squint.py --all-dataset <DATASET>")
        print()
        for ds in sorted(DATASET_VARIANTS.keys()):
            names = DATASET_VARIANTS[ds]
            print(f"  {ds}  ({len(names)} variants)")
            for n in names:
                print(f"    - {n}")
            print()
        return

    if args.variants_for_dataset is not None:
        if args.variants_for_dataset not in DATASET_VARIANTS:
            raise SystemExit(
                f"Unknown DATASET key {args.variants_for_dataset!r}. "
                f"Available: {sorted(DATASET_VARIANTS.keys())}."
            )
        for n in DATASET_VARIANTS[args.variants_for_dataset]:
            print(n)
        return

    if not (args.patch_uns or args.harmonize_var or args.build_blob
            or args.train or args.predict or args.all
            or args.all_dataset):
        p.print_help()
        return

    if args.patch_uns:
        patch_anndata_uns()
    if args.harmonize_var:
        harmonize_anndata_var()
    if args.build_blob:
        build_blob(dataset=args.build_blob_dataset,
                   backend=args.build_blob_backend,
                   cross_panel=args.cross_panel,
                   exclude_sections=args.exclude_sections,
                   include_sections=args.include_sections,
                   name_suffix=args.build_blob_name_suffix,
                   resume=args.resume_build,
                   min_panels_per_gene=args.min_panels_per_gene,
                   batch_key=args.batch_key,
                   container=args.container)
    # Resolve --compile / --no-compile into a tri-state override:
    #   `--no-compile`               → False (always wins)
    #   `--compile` (alone)          → True
    #   neither flag                 → None (use cfg value, currently False)
    if args.no_compile:
        _compile_override = False
    elif args.compile:
        _compile_override = True
    else:
        _compile_override = None
    # `--deterministic` is a "force on" flag; default (not passed) means
    # "use cfg value" so we pass None when the flag is absent.
    _deterministic_override = True if args.deterministic else None
    if args.train:
        train(
            args.variant,
            train_precision=args.precision,
            train_strategy=args.strategy,
            compile_model=_compile_override,
            train_deterministic=_deterministic_override,
            split_sections_json=args.split_sections_json,
            max_steps=args.max_steps,
        )
    if args.predict:
        run_dir = args.run_dir or args.wandb_run_dir
        if run_dir is None:
            raise SystemExit("--predict requires --run-dir")
        if args.wandb_run_dir is not None and args.run_dir is None:
            print("Note: --wandb-run-dir is deprecated; use --run-dir instead.")
        predict(
            run_dir=run_dir,
            silver_dir=args.silver_dir,
            model_ckpt_fname=args.model_ckpt_fname,
            output_dir=args.output_dir,
            predict_batch_size=args.predict_batch_size,
        )

    # --all: end-to-end train -> predict -> 3 plot scripts -> metrics.
    if args.all:
        run_all_pipeline(
            variant=args.variant,
            silver_dir=args.silver_dir,
            output_dir=args.output_dir,
            model_ckpt_fname=args.model_ckpt_fname,
            label_keys=args.label_keys,
            batch_rename=args.batch_rename,
            cell_label_keys=args.cell_label_keys,
            niche_label_keys=args.niche_label_keys,
            skip_predict=args.skip_predict,
            skip_code_index_plots=args.skip_code_index_plots,
            skip_svg_plots=args.skip_svg_plots,
            skip_umap=args.skip_umap,
            skip_metrics=args.skip_metrics,
            train_precision=args.precision,
            train_strategy=args.strategy,
            compile_model=_compile_override,
            train_deterministic=_deterministic_override,
        )

    # --all-dataset: loop --all over every variant in the dataset's sweep.
    if args.all_dataset:
        run_dataset_pipeline(
            dataset=args.all_dataset,
            continue_on_error=not args.abort_on_error,
            silver_dir=args.silver_dir,
            output_dir=args.output_dir,
            model_ckpt_fname=args.model_ckpt_fname,
            label_keys=args.label_keys,
            batch_rename=args.batch_rename,
            cell_label_keys=args.cell_label_keys,
            niche_label_keys=args.niche_label_keys,
            train_precision=args.precision,
            train_strategy=args.strategy,
            compile_model=_compile_override,
            train_deterministic=_deterministic_override,
            skip_predict=args.skip_predict,
            skip_code_index_plots=args.skip_code_index_plots,
            skip_svg_plots=args.skip_svg_plots,
            skip_umap=args.skip_umap,
            skip_metrics=args.skip_metrics,
        )


if __name__ == "__main__":
    main()
